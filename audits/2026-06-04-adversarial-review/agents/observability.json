{
  "category": "observability",
  "finding_count": 29,
  "findings": [
    {
      "id": "OBS-001",
      "severity": "blocking",
      "category": "observability",
      "subcategory": "ci-boot-failure",
      "title": "AUDIT_HASH_KEY never set in CI — API cannot boot in E2E jobs",
      "file": ".github/workflows/ci.yml",
      "line": "262-273",
      "code_evidence": "      - name: Start Backend API\n        run: |\n          pnpm --filter api start > /tmp/api.log 2>&1 &\n          echo \"Waiting for API to be ready...\"\n          timeout 60 bash -c 'until curl -f http://localhost:4000/api/health 2>/dev/null; do sleep 2; done'\n        env:\n          DATABASE_URL: postgresql://orchestr_a:orchestr_a_dev_password@localhost:5432/orchestr_a_v2_e2e\n          JWT_SECRET: test-jwt-secret-key\n          REDIS_URL: redis://localhost:6379\n          PORT: 4000\n          ALLOWED_ORIGINS: http://localhost:3000\n          TZ: Europe/Paris",
      "description": "apps/api/src/main.ts calls assertAuditHashKey(process.env.AUDIT_HASH_KEY) at line 69 unconditionally — enforced in ALL environments, not just production. audit-hash-key.ts throws if the key is absent or shorter than 32 chars. The e2e-smoke and e2e-tests jobs in ci.yml both start the API (pnpm --filter api start) without AUDIT_HASH_KEY in the environment. The API will throw on bootstrap and never reach the health endpoint, so the timeout-based readiness probe will expire, causing the jobs to fail.",
      "root_cause": "assertAuditHashKey is enforced universally (intentional by OBS-028 design), but the CI e2e job environment blocks were never updated to provide a non-production test value for AUDIT_HASH_KEY.",
      "impact": "E2E smoke tests and full E2E suite will always fail in CI because the API cannot boot. Every push to master or any PR targeting master breaks the e2e-smoke and e2e-tests jobs. The bug is latent if the tests were never run in CI or were passing via some other mechanism (pre-built image, skip), but as written the workflow is broken.",
      "suggested_fix": "Add AUDIT_HASH_KEY to the env block of every 'Start Backend API' step in both e2e-smoke and e2e-tests jobs, using a stable but non-secret test value of at least 32 chars. Example: AUDIT_HASH_KEY: 'ci-test-audit-hash-key-32-chars!!'. Also add it to the Build Applications step in case the build exercises any boot-path code.",
      "acceptance_criteria": [
        "E2E smoke and full e2e jobs complete without the 60-second API readiness timeout expiring",
        "assertAuditHashKey receives a value >= 32 chars in all CI steps that start the API",
        "The fix uses a dedicated CI-only key, not a production secret"
      ],
      "verification_command": "grep -n 'AUDIT_HASH_KEY' .github/workflows/ci.yml",
      "related_findings": [
        "SEC-002"
      ],
      "cluster_id": "K",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. (1) assertAuditHashKey at main.ts:69 is unconditional — verified verbatim. (2) audit-hash-key.ts throws when key is absent or <32 chars — verified verbatim. (3) grep -n AUDIT_HASH_KEY .github/workflows/ci.yml returns ZERO hits across the entire file. (4) Both e2e-smoke (line 262-273) and e2e-tests (line 397-408) 'Start Backend API' env blocks confirmed missing AUDIT_HASH_KEY. No mitigation found: no AUDIT_HASH_KEY in any env context of ci.yml. Finding stands at severity=blocking."
    },
    {
      "id": "OBS-002",
      "severity": "important",
      "category": "observability",
      "subcategory": "correlation-id-not-persisted",
      "title": "requestId never populated in persisted audit-log payload despite declared schema field and ALS infrastructure",
      "file": "apps/api/src/audit/audit.service.ts",
      "line": "184-200",
      "code_evidence": "    void this.auditPersistence\n      .log({\n        action: event.action,\n        entityType: ENTITY_TYPE_BY_ACTION[event.action] ?? 'Auth',\n        entityId: this.resolveEntityId(event),\n        actorId: event.userId ?? null,\n        payload: {\n          ip: event.ip,\n          details: event.details,\n          success: event.success,\n          timestamp: entry.timestamp,\n          ...(event.ua !== undefined ? { ua: event.ua } : {}),\n          ...(event.reason !== undefined ? { reason: event.reason } : {}),\n          ...(event.before !== undefined ? { before: event.before } : {}),\n          ...(event.after !== undefined ? { after: event.after } : {}),\n        },\n      })",
      "description": "The securityEnvelope Zod schema (payload-schemas.ts line 70) declares requestId as an optional field, with the comment: 'lets an SRE stitch a security-envelope audit row back to the originating HTTP request'. The ALS infrastructure (requestIdStore, getRequestId()) is fully wired in main.ts and request-id.context.ts. However, AuditService.log() never calls getRequestId() and never injects the correlation ID into the payload object. The field is always absent from every persisted LOGIN_SUCCESS, LOGIN_FAILURE, ACCOUNT_LOCKED, ACCESS_DENIED, REGISTER, PASSWORD_CHANGED, and RELEASE_DEPLOYED row.",
      "root_cause": "AuditService.log() was not updated to call getRequestId() when the ALS infrastructure (OBS-009) was added, leaving the schema-declared field permanently vacant.",
      "impact": "Security incident investigation is impaired: when an SRE receives a LOGIN_FAILURE audit row they cannot correlate it to the originating HTTP request's structured log line (which carries method, URL, response time, and other context). The ALS context is available at call time (the onRequest hook fires before any service code), so the correlation is technically achievable but silently missing from the immutable trail.",
      "suggested_fix": "In AuditService.log(), import getRequestId from '../common/fastify/request-id.context' and add requestId to the payload spread: ...(getRequestId() !== undefined ? { requestId: getRequestId() } : {}). This is a one-line change that populates the already-declared schema field with no schema migration needed.",
      "acceptance_criteria": [
        "A LOGIN_FAILURE audit row persisted during an HTTP request carries a requestId field in its payload",
        "The requestId value matches the x-request-id header (or generated UUID) visible in the Fastify access log for the same request",
        "Rows written outside an HTTP context (background jobs, scripts) still have no requestId (field absent, not null)"
      ],
      "verification_command": "N/A — manual verification: grep -n 'getRequestId' apps/api/src/audit/audit.service.ts",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. (1) Code evidence at lines 184-200 is verbatim correct. (2) grep getRequestId audit.service.ts returns ZERO hits — the function is never imported or called. (3) payload-schemas.ts line 70 confirms `requestId: optStr` is declared in the Zod schema. (4) No other code path in audit.service.ts or audit-persistence.service.ts injects requestId. The schema field is permanently vacant. Finding stands at severity=important."
    },
    {
      "id": "OBS-003",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-logout",
      "title": "POST /auth/logout emits no audit row — session termination is untracked",
      "file": "apps/api/src/auth/auth.controller.ts",
      "line": "220-243",
      "code_evidence": "  async logout(\n    @Body() body: LogoutDto,\n    @CurrentUser() user: AuthenticatedUser,\n    @Req() req: any,\n    @Res({ passthrough: true }) reply?: FastifyReply,\n  ): Promise<void> {\n    const refreshToken =\n      body?.refreshToken ?? readRefreshCookie(req) ?? undefined;\n    if (refreshToken) {\n      await this.refreshTokenService.revoke(refreshToken);\n    }",
      "description": "The logout handler revokes the refresh token and blacklists the JTI, but calls neither `auditService.log()` nor `auditPersistence.log()`. No `LOGOUT` action exists in the `AuditAction` enum. An auditor cannot reconstruct which sessions were voluntarily terminated versus which tokens simply expired, and cannot correlate \"user A logged in at T1 and logged out at T2\" from the audit trail alone.",
      "root_cause": "No audit emission was ever added to the logout controller path, and no LOGOUT enum member was defined in AuditAction.",
      "impact": "Session lifecycle is not fully observable. Forensic analysis of a compromised account cannot determine if the legitimate user was still logged in at a given point, or if an ongoing session was terminated.",
      "suggested_fix": "1. Add `LOGOUT = 'LOGOUT'` to the `AuditAction` enum in `audit-action.enum.ts`. 2. Add it to the `ENTITY_TYPE_BY_ACTION` map in `audit.service.ts`. 3. In `AuthController.logout()`, call `this.auditService.log({ action: AuditAction.LOGOUT, userId: user.id, ip: clientIp(req), ua: req.headers?.['user-agent'], details: ..., success: true })` after the blacklist step.",
      "acceptance_criteria": [
        "A row with action='LOGOUT' appears in audit_logs after POST /auth/logout",
        "The row captures the actor userId and the originating IP/UA"
      ],
      "verification_command": "grep -n 'LOGOUT' apps/api/src/audit/audit-action.enum.ts apps/api/src/auth/auth.controller.ts",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: auth.controller.ts lines 220-243 contain no audit emission. audit-action.enum.ts has no LOGOUT member (confirmed). No global audit interceptor exists in the codebase. Finding is structural."
    },
    {
      "id": "OBS-004",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "clients: create/update/hardDelete and project-assignment mutations emit no audit_log row",
      "file": "apps/api/src/clients/clients.service.ts",
      "line": "50-65",
      "code_evidence": "  async create(dto: CreateClientDto): Promise<Client> {\n    try {\n      return await this.prisma.client.create({\n        data: {\n          name: dto.name,\n        },\n      });\n    } catch (err) {\n      if (isUniqueViolation(err)) {",
      "description": "ClientsService has no AuditPersistenceService injected. None of the following mutating operations write an audit_log row: create() (line 50), update() (line 235), hardDelete() (line 259), assignClientToProject() (line 300), or removeClientFromProject() (line 329). In a government context, client (commanditaire) management has procurement implications. Hard-deleting a client produces no forensic record. The assignClientToProject / removeClientFromProject calls modify project relationships silently.",
      "root_cause": "AuditPersistenceService was never injected into ClientsService.",
      "impact": "Client creation, modification, deletion, and project association changes leave no audit trail. After a hard delete of a client record, there is no way to know who deleted it or what the client's name/state was at deletion time.",
      "suggested_fix": "Inject AuditPersistenceService. Emit CLIENT_CREATED, CLIENT_UPDATED (with before snapshot), CLIENT_DELETED (with snapshot), CLIENT_ASSIGNED_TO_PROJECT, CLIENT_REMOVED_FROM_PROJECT. Add the actor userId to the service method signatures where it is not already present (create currently receives no actor). Update the controller to pass @CurrentUser('id').",
      "acceptance_criteria": [
        "POST /clients produces an audit_log row",
        "PATCH /clients/:id produces an audit_log row with before/after",
        "DELETE /clients/:id produces an audit_log row with client snapshot before deletion"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 50-65 matches verbatim. Constructor (line 48) verified: only PrismaService injected. No AuditPersistenceService import found. ClientsController.create() does not inject @CurrentUser, so actor identity is also not captured at the controller layer for this endpoint."
    },
    {
      "id": "OBS-005",
      "severity": "important",
      "category": "observability",
      "subcategory": "no-external-error-reporter",
      "title": "Sentry / GlitchTip / OpenTelemetry stubs are no-ops — unhandled exceptions and 500s are log-only with no alerting",
      "file": "apps/api/src/common/filters/all-exceptions.filter.ts",
      "line": "33-40",
      "code_evidence": "// ---------------------------------------------------------------------------\n// Sentry no-op stub — replace with `import * as Sentry from '@sentry/node'`\n// when the dependency is added.\n// ---------------------------------------------------------------------------\nconst SentryClient = {\n  captureException: (_err: unknown, _ctx?: Record<string, unknown>): void => {\n    // no-op until @sentry/node is introduced\n  },\n};",
      "description": "AllExceptionsFilter (OBS-017) and NoopErrorReporter (OBS-010) both document that they are stubs pending operator choice of a tracking backend (Sentry, GlitchTip, OpenTelemetry). SentryClient.captureException is a no-op at line 38. installGlobalErrorHandlers(new NoopErrorReporter()) in main.ts logs to stdout only. No external error reporting is wired. In a production government application (French local authority), unhandled exceptions during an active session produce a 500 that is logged locally but never surfaces as an alert. Log rotation or container restart can discard the evidence.",
      "root_cause": "The operator decision on the tracking backend (Sentry vs GlitchTip for RGPD sovereignty) was intentionally deferred; the stubs are placeholders, not accidentally missing code.",
      "impact": "P0 production errors that do not crash the process (e.g. DB query failures, unexpected NPE in a service method) are silently swallowed by AllExceptionsFilter and logged to stdout only. In a containerized deployment with log forwarding to a file (api.log) there is no on-call alert path. Operators will only discover service degradation via monitoring dashboards or user complaints.",
      "suggested_fix": "Wire a real ErrorReporter. For RGPD-sovereign self-hosted deployments, GlitchTip (https://glitchtip.com) is the recommended Sentry-compatible alternative that can be co-located on the VPS. For managed cloud: Sentry DSN via SENTRY_DSN env var. Minimum implementation: if (process.env.SENTRY_DSN) { const Sentry = await import('@sentry/node'); Sentry.init({ dsn: process.env.SENTRY_DSN }); } and replace NoopErrorReporter with a SentryErrorReporter that calls Sentry.captureException(). The existing interface and wiring hooks require no structural change.",
      "acceptance_criteria": [
        "A thrown, uncaught exception in a service method is captured by the error reporter and visible in the configured backend within 60 seconds",
        "If SENTRY_DSN / tracking DSN is not set, the application falls back to NoopErrorReporter without crashing (existing behavior retained)",
        "The reporter does not forward PII (email, names) in exception context — only stack trace + requestId"
      ],
      "verification_command": "N/A — manual verification: grep -rn 'NoopErrorReporter\\|SentryClient\\|captureException' apps/api/src/",
      "related_findings": [],
      "cluster_id": "K",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. Code evidence at lines 33-40 is verbatim correct. The stub is intentionally documented. No mitigation found — no SENTRY_DSN env var wiring, no alternative reporter implementation. Finding stands at severity=important (intentional gap, not accidental)."
    },
    {
      "id": "OBS-006",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "documents: create and soft-delete emit no audit_log row (only DOCUMENT_READ is audited)",
      "file": "apps/api/src/documents/documents.service.ts",
      "line": "64-73",
      "code_evidence": "    return this.prisma.document.create({\n      data: {\n        ...createDocumentDto,\n        uploadedBy: userId,\n      },\n      include: {\n        project: { select: { id: true, name: true } },\n      },\n    });\n  }",
      "description": "DocumentsService wires AuditPersistenceService and correctly emits DOCUMENT_READ (OBS-006) for GET /:id. However, create() (line 64), update() (line 172), and remove()/soft-delete (line 204) all perform mutations with no corresponding audit log emission. A document upload (create) and a soft-delete are sensitive operations: government document management policies typically require knowing who uploaded and who deleted a document. The update() path also passes no currentUser, so actor identity is lost there too.",
      "root_cause": "OBS-006 was scoped only to DOCUMENT_READ; the write/delete paths were not included in the audit emission.",
      "impact": "Document uploads, metadata changes, and soft-deletions leave no audit trail beyond the DB timestamps. After a soft-delete, the deletedAt column records timing but not actor identity. If a document is soft-deleted inappropriately, there is no audit evidence of who did it.",
      "suggested_fix": "In DocumentsService.create(), after prisma.document.create(), emit DOCUMENT_CREATED with payload={documentId, projectId, mimeType, sizeBytes, actorId}. In remove(), before the soft-delete update, emit DOCUMENT_DELETED with payload={documentId, projectId, uploadedBy, deletedBy: currentUser?.id}. For update(), pass currentUser from the controller (currently absent) and emit DOCUMENT_UPDATED with a before/after snapshot of changed metadata fields.",
      "acceptance_criteria": [
        "POST /documents produces an audit_log row with action DOCUMENT_CREATED",
        "DELETE /documents/:id produces an audit_log row with action DOCUMENT_DELETED and the deletedBy actor",
        "PATCH /documents/:id produces an audit_log row with before/after metadata"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 64-73 matches verbatim. AuditPersistenceService IS injected (line 39) but only DOCUMENT_READ is emitted (line 144-166). create() (line 64), update() (line 172), and remove() (line 183/204) have no audit emission calls. AuditAction enum has no DOCUMENT_CREATED, DOCUMENT_UPDATED, or DOCUMENT_DELETED members — only DOCUMENT_READ and DOCUMENT_DOWNLOADED. DocumentsController.update() does not inject @CurrentUser, so update() currently receives no actor identity."
    },
    {
      "id": "OBS-007",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-reject-cancellation",
      "title": "rejectCancellation() (CANCELLATION_REQUESTED → APPROVED) emits no audit row",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2283-2319",
      "code_evidence": "    return this.prisma.$transaction(async (tx) => {\n      const current = await tx.leave.findUnique({ where: { id } });\n      if (!current) {\n        throw new NotFoundException('Demande de congé introuvable');\n      }\n      if (current.status !== LeaveStatus.CANCELLATION_REQUESTED) {\n        throw new ConflictException(\n          'La demande de congé a été modifiée pendant le traitement. Veuillez réessayer.',\n        );\n      }\n\n      return tx.leave.update({\n        where: { id },\n        data: { status: LeaveStatus.APPROVED },",
      "description": "The `rejectCancellation()` method transitions a leave from CANCELLATION_REQUESTED back to APPROVED. All other leave status transitions (approve, reject, cancel, requestCancel, update) emit a durable `auditPersistence.log()` call. This method is the sole exception: the manager decision to deny a cancellation request and restore the leave to APPROVED is silently not recorded in `audit_logs`.",
      "root_cause": "The `rejectCancellation()` method was added (COR-009) to complete the cancellation state machine but its audit emit was not included, unlike the symmetric `cancel()` path.",
      "impact": "A manager who denies a cancellation request and thereby forces an employee to take a leave they wanted to cancel leaves no audit trace. In case of a dispute, there is no record of this decision.",
      "suggested_fix": "Add `LEAVE_CANCELLATION_REJECTED = 'LEAVE_CANCELLATION_REJECTED'` to AuditAction (or reuse LEAVE_REJECTED with a before.status=CANCELLATION_REQUESTED marker). Inside the `$transaction`, after `tx.leave.update()`, emit `await this.auditPersistence.log({ action: AuditAction.LEAVE_CANCELLATION_REJECTED, entityType: 'Leave', entityId: id, actorId: currentUserId ?? null, payload: { before: { status: current.status }, after: { status: updated.status }, targetUserId: current.userId } })`.",
      "acceptance_criteria": [
        "After PATCH /leaves/:id/reject-cancellation, a durable audit_logs row records the before (CANCELLATION_REQUESTED) and after (APPROVED) states",
        "The actorId field is the manager's user id"
      ],
      "verification_command": "grep -n 'rejectCancellation\\|auditPersistence' apps/api/src/leaves/leaves.service.ts | head -10",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: leaves.service.ts lines 2278-2319 confirmed verbatim. Full grep of audit calls in the file shows none in range 2280-2320. All other status-transition methods (approve at 1834, reject at 1981, cancel at 2112, requestCancel at 2220) have auditPersistence.log() calls; rejectCancellation uniquely lacks one. Finding confirmed."
    },
    {
      "id": "OBS-008",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-delegation",
      "title": "createDelegation() and deactivateDelegation() emit no audit row",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2376-2406",
      "code_evidence": "    // Créer la délégation\n    const delegation = await this.prisma.leaveValidationDelegate.create({\n      data: {\n        delegatorId,\n        delegateId,\n        startDate,\n        endDate,\n        isActive: true,\n      },\n      include: {\n        delegator: {\n          select: {\n            id: true,\n            firstName: true,\n            lastName: true,",
      "description": "Delegations are RBAC-adjacent sensitive actions: they transfer leave approval authority from one user (delegator) to another (delegate) for a time window. Both `createDelegation()` (lines 2329–2407) and `deactivateDelegation()` (lines 2454–2485) perform state-changing DB writes with no `auditPersistence.log()` or `auditService.log()` call. A delegator could silently transfer their approval authority to any user in the system without any trace.",
      "root_cause": "Delegation management was implemented without audit instrumentation.",
      "impact": "Audit trail for leave approval authority delegation is completely absent. This is a compliance and forensic gap: if a delegation is abused to approve fraudulent leaves, the delegation lifecycle is invisible.",
      "suggested_fix": "Add `DELEGATION_CREATED` and `DELEGATION_DEACTIVATED` to AuditAction (and ENTITY_TYPE_BY_ACTION). Emit `auditPersistence.log()` in both `createDelegation()` and `deactivateDelegation()`. The payload should include delegatorId, delegateId, startDate, endDate, and actorId.",
      "acceptance_criteria": [
        "A durable audit_logs row exists after creating a delegation",
        "A durable audit_logs row exists after deactivating a delegation",
        "Both rows capture the delegator, delegate, and the actor performing the operation"
      ],
      "verification_command": "grep -n 'createDelegation\\|deactivateDelegation\\|auditPersistence' apps/api/src/leaves/leaves.service.ts | head -20",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: createDelegation() at lines 2329-2407 and deactivateDelegation() at lines 2454-2485 confirmed verbatim. Full grep of audit calls in leaves.service.ts shows none in ranges 2329-2407 or 2454-2485. Finding confirmed."
    },
    {
      "id": "OBS-009",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-leave-create",
      "title": "Leave create path emits no durable audit row for PENDING/declaredByManager paths; selfApprove path mislabeled",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "679-693",
      "code_evidence": "    // Trace d'audit séparée pour les auto-validations (finding #6). La\n    // colonne `selfApproved` rend la distinction lisible dans la table,\n    // l'entrée d'audit la rend visible dans le flux de logs sécurité.\n    if (canSelfApprove) {\n      this.auditService.log({\n        action: AuditAction.LEAVE_APPROVED,\n        userId: requestingUserId,\n        targetId: leave.id,\n        details: `Auto-validation par ${requestingUserId} (selfApproved=true)`,\n        success: true,\n      });\n    }\n\n    return leave;",
      "description": "The `create()` method (lines 333-693) has three outcome paths: (1) PENDING leave submission, (2) declaredByManager (immediately APPROVED), (3) canSelfApprove (immediately APPROVED). Paths (1) and (2) emit no audit record at all. Path (3) calls `auditService.log()` which IS durably written to `audit_logs` via the dual-write mechanism (DAT-002), but the action is wrong: it emits LEAVE_APPROVED rather than LEAVE_CREATED, misclassifying creation as an approval event. The full leave lifecycle (update, delete, approve, reject, cancel) is audited, but the creation event is entirely absent for paths (1) and (2).",
      "root_cause": "The create() path was instrumented for the status-transition event (self-approve) but a LEAVE_CREATED emission was never added for any path. The auditService dual-write was not recognized by the original reviewer.",
      "impact": "An auditor cannot establish when a PENDING leave request was submitted, who submitted it, or whether a manager-declared leave on behalf of another user was authorised at that moment. This is a compliance gap for RGPD and HR policy. The selfApprove path is durably logged but under the wrong action code, polluting LEAVE_APPROVED analytics.",
      "suggested_fix": "Add `LEAVE_CREATED = 'LEAVE_CREATED'` to AuditAction and ENTITY_TYPE_BY_ACTION. After the transaction, emit `auditPersistence.log()` for all three paths covering the actor, target user, initial status, leaveTypeId, dates. Replace the current `auditService.log(LEAVE_APPROVED)` with `auditService.log(LEAVE_CREATED)` for the selfApprove path.",
      "acceptance_criteria": [
        "After POST /leaves, a durable audit_logs row with action='LEAVE_CREATED' exists",
        "The payload captures the actor, the target user, and the initial status"
      ],
      "verification_command": "grep -n 'LEAVE_CREATED\\|auditPersistence' apps/api/src/leaves/leaves.service.ts | head -20",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "medium",
      "notes": "DOWNGRADED from high to medium. Original description incorrectly stated auditService.log() is 'console/SecurityAudit only — not persisted to audit_logs'. Verification of audit.service.ts lines 152-207 shows AuditService has a DAT-002 dual-write: it calls auditPersistence.log() fire-and-forget for every action, including LEAVE_APPROVED. Therefore the selfApprove path IS durably logged, but under the wrong action code. The core gap (no LEAVE_CREATED, no audit for paths 1 and 2) remains real and confirmed. LEAVE_CREATED is absent from audit-action.enum.ts."
    },
    {
      "id": "OBS-010",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-project-create-update-remove",
      "title": "Project create, update, and soft-delete (status→CANCELLED) emit no audit rows",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "89-687",
      "code_evidence": "  async remove(id: string, user?: ProjectMutationUser) {\n    // Defense-in-depth: enforce ownership even if guard is bypassed.\n    if (user) {\n      await this.assertProjectOwnershipOrBypass(id, user);\n    }\n\n    const project = await this.prisma.project.findUnique({\n      where: { id },\n    });\n\n    if (!project) {\n      throw new NotFoundException('Projet introuvable');\n    }\n\n    await this.prisma.project.update({\n      where: { id },\n      data: { status: ProjectStatus.CANCELLED },\n    });\n\n    return { message: 'Projet annulé avec succès' };\n  }",
      "description": "Only the `archive()`, `unarchive()`, and `hardDelete()` methods emit audit rows. The three core lifecycle methods are unaudited: `create()` (lines 89–221) creates a project and silently adds the creator as a member with no audit; `update()` (lines 548–662) modifies arbitrary project fields including status, manager, and sponsor with no before/after audit; `remove()` (lines 667–687) transitions a project to CANCELLED status with no audit. This means the vast majority of project lifecycle events are invisible in the audit trail.",
      "root_cause": "Audit was retroactively added only to the archive/unarchive/hardDelete paths; the foundational CRUD operations were never instrumented.",
      "impact": "A project manager can create, rename, change the managing team, and cancel a project with no forensic evidence in audit_logs. Status changes (DRAFT→IN_PROGRESS→COMPLETED→CANCELLED) are untracked. For a government HR/project management system this is a significant accountability gap.",
      "suggested_fix": "Add PROJECT_CREATED, PROJECT_UPDATED, PROJECT_CANCELLED to AuditAction and ENTITY_TYPE_BY_ACTION. In create(): emit PROJECT_CREATED with the project id and creatorId. In update(): capture the before-state for key fields (name, status, managerId, sponsorId) and emit PROJECT_UPDATED with before/after diff. In remove(): emit PROJECT_CANCELLED.",
      "acceptance_criteria": [
        "After POST /projects, an audit_logs row with action='PROJECT_CREATED' exists",
        "After PATCH /projects/:id, an audit_logs row with action='PROJECT_UPDATED' and before/after state exists",
        "After DELETE /projects/:id, an audit_logs row with action='PROJECT_CANCELLED' exists"
      ],
      "verification_command": "grep -n 'auditPersistence\\|auditService\\|AuditAction' apps/api/src/projects/projects.service.ts | head -20",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: grep of projects.service.ts shows audit calls only at lines 710 (PROJECT_ARCHIVED), 748 (PROJECT_UNARCHIVED), 855 (PROJECT_DELETED). create() at 89-221, update() at 548-662, remove() at 667-687 confirmed verbatim with no audit calls. PROJECT_CREATED/PROJECT_UPDATED/PROJECT_CANCELLED absent from audit-action.enum.ts. Finding confirmed."
    },
    {
      "id": "OBS-011",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "settings: all write operations emit no audit_log row and do not capture actor identity",
      "file": "apps/api/src/settings/settings.service.ts",
      "line": "235-265",
      "code_evidence": "  async update(key: string, value: unknown, description?: string) {\n    if (!SettingsService.isKnownKey(key)) {\n      throw new BadRequestException(`Unknown setting key: ${key}`);\n    }\n\n    const stringValue =\n      typeof value === 'string' ? value : JSON.stringify(value);\n\n    const setting = await this.prisma.appSettings.upsert({",
      "description": "SettingsService.update(), bulkUpdate(), resetToDefault(), resetAllToDefaults(), and remove() all mutate application settings with no audit log emission. Critically, the controller does not even pass CurrentUser to the service (settings.controller.ts lines 51-68), so there is no mechanism to record who changed a setting. Settings include security-relevant values: maxTeleworkDaysPerWeek, defaultLeaveDays, and planning.visibleDays. A settings change that affects entitlements (e.g. increasing defaultLeaveDays) should be auditable.",
      "root_cause": "AuditPersistenceService is not injected in SettingsService; furthermore, the controller's write endpoints (PUT /:key, POST /bulk, POST /:key/reset, POST /reset-all, DELETE /:key) do not extract or forward CurrentUser to the service, making actor attribution impossible without refactoring.",
      "impact": "Application-level configuration changes — including leave policy parameters — are applied silently with no record of who made the change or what the previous value was. A compromised admin account could alter settings and the change would be undetectable in the audit trail.",
      "suggested_fix": "1) Add a currentUserId parameter to SettingsService.update() and its callers. 2) Inject AuditPersistenceService. 3) After each successful upsert, emit SETTINGS_CHANGED with payload={key, before: previousValue, after: newValue, actorId}. 4) Update SettingsController to pass @CurrentUser('id') to the service. 5) Add SETTINGS_CHANGED to AuditAction enum.",
      "acceptance_criteria": [
        "PUT /settings/:key produces an audit_log row with action SETTINGS_CHANGED, before value, after value, and actor_id",
        "POST /settings/bulk produces one SETTINGS_CHANGED row per changed key",
        "POST /settings/reset-all produces audit rows for all reset keys",
        "The actor_id in each audit row matches the JWT sub of the requester"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 235-265 matches verbatim. No AuditPersistenceService import or injection found in settings.service.ts. AuditAction enum has no SETTINGS_CHANGED member. The controller does not use @CurrentUser on any mutating handler, so the service signature change is a prerequisite for actor attribution."
    },
    {
      "id": "OBS-012",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-task-lifecycle",
      "title": "Task create, update, and delete emit no audit rows — only CSV export is audited",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "841-885",
      "code_evidence": "  async remove(id: string, user?: { id: string; role: string | null }) {\n    const task = await this.prisma.task.findUnique({\n      where: { id },\n      include: {\n        dependents: true,\n        assignees: true,\n      },\n    });\n\n    if (!task) {\n      throw new NotFoundException('Tâche introuvable');\n    }\n    // ...\n    await this.prisma.task.delete({\n      where: { id },\n    });\n\n    return { message: 'Tâche supprimée avec succès' };\n  }",
      "description": "In `TasksService`, only the `exportProjectTasksCsv()` method emits an audit row (`DATA_EXPORTED`). The `create()` method (task creation), `update()` (status changes, assignee changes, priority changes, all sensitive in project governance), and `remove()` (task deletion) all perform DB writes without any audit emission. Task status transitions (TODO→IN_PROGRESS→DONE) represent project execution state changes that are material for project governance and compliance.",
      "root_cause": "The audit instrumentation strategy focused on higher-sensitivity domains (leaves, RBAC, users) and task operations were not included.",
      "impact": "Project task lifecycle is entirely absent from the audit trail. A contributor can change task status, reassign tasks, or delete tasks without any record. For a government project management system tracking deliverables, this is an accountability gap.",
      "suggested_fix": "At minimum, add audit rows for status changes in `update()` (TASK_STATUS_CHANGED: before.status, after.status, taskId, projectId) and for `remove()` (TASK_DELETED: task snapshot). Add corresponding AuditAction members.",
      "acceptance_criteria": [
        "After PATCH /tasks/:id with a status change, an audit_logs row records the status transition",
        "After DELETE /tasks/:id, an audit_logs row records the deletion"
      ],
      "verification_command": "grep -n 'auditPersistence\\|auditService\\|AuditAction\\|emitDataExported' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: tasks.service.ts grep shows only two audit references — line 45 (injection) and line 1907 (emitDataExported for CSV export). remove() at lines 841-885 confirmed verbatim with no audit call. TASK_* action codes absent from audit-action.enum.ts. Finding confirmed."
    },
    {
      "id": "OBS-013",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "telework: all CRUD and recurring-rule mutations emit no audit_log row",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "71-145",
      "code_evidence": "    const telework = await this.prisma.teleworkSchedule.create({\n      data: {\n        userId,\n        date: teleworkDate,\n        isTelework,\n        isException,\n      },\n      include: {\n        user: {\n          select: {",
      "description": "TeleworkService has no AuditPersistenceService injected. None of the following mutating operations write an audit_log row: create() (line 124), update() (line 574), remove() (line 620), createRecurringRule() (line 744), updateRecurringRule() (line 802), removeRecurringRule() (line 846), and generateSchedulesFromRules() (line 917). Telework scheduling is sensitive HR data; in particular, an admin (telework:manage_any) can create/delete/modify telework entries on behalf of any employee with no audit trail.",
      "root_cause": "AuditPersistenceService was never injected into TeleworkService; the constructor only wires PrismaService and PermissionsService.",
      "impact": "HR administrators can silently create, modify, or delete telework records for any employee. generateSchedulesFromRules() can bulk-create many TeleworkSchedule rows in a single call with no audit trail. Retroactive forensic reconstruction is impossible.",
      "suggested_fix": "Inject AuditPersistenceService. Add audit emissions after each mutating DB call: TELEWORK_CREATED, TELEWORK_UPDATED (with before snapshot), TELEWORK_DELETED (with snapshot), TELEWORK_RULE_CREATED, TELEWORK_RULE_UPDATED, TELEWORK_RULE_DELETED, TELEWORK_SCHEDULES_GENERATED (with created count). Include targetUserId (the affected employee) and actorId (currentUserId) in each payload.",
      "acceptance_criteria": [
        "POST /telework produces an audit_log row with action TELEWORK_CREATED",
        "PATCH /telework/:id produces an audit_log row with before/after fields",
        "DELETE /telework/:id produces an audit_log row with a snapshot of the deleted entry",
        "Admin creating telework for another user (targetUserId != actorId) is visible in the audit trail"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "AuditAction enum has no TELEWORK_* members. Constructor verified (line 63-66): only PrismaService and PermissionsService. Code at line 124 matches verbatim."
    },
    {
      "id": "OBS-014",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "third-parties: all mutating operations (create/update/hardDelete/assign/detach) emit no audit_log row",
      "file": "apps/api/src/third-parties/third-parties.service.ts",
      "line": "180-187",
      "code_evidence": "  async hardDelete(id: string): Promise<void> {\n    const tp = await this.prisma.thirdParty.findUnique({ where: { id } });\n    if (!tp) {\n      throw new NotFoundException(`Third party ${id} not found`);\n    }\n    // Cascade FK handles time_entries, task_third_party_assignees, project_third_party_members\n    await this.prisma.thirdParty.delete({ where: { id } });\n  }",
      "description": "ThirdPartiesService has no AuditPersistenceService injected. The hardDelete() path (line 180) deletes a third-party record and — per the comment — cascades to time_entries, taskThirdPartyAssignees, and projectThirdPartyMembers. This destroys multiple linked records with zero audit trail. Similarly, create() (line 23), update() (line 115), assignToTask() (line 276), unassignFromTask() (line 336), attachToProject() (line 350), and detachFromProject() (line 390) are all silent from an observability standpoint.",
      "root_cause": "AuditPersistenceService was never injected into ThirdPartiesService.",
      "impact": "A hard delete of a third party silently cascades to delete all associated time entries and project/task assignments. In a government context where third parties are contractors, this constitutes destruction of audit-relevant procurement and time-accounting data with no forensic record. The hardDelete currently requires no blocking guard (the controller accepts DELETE directly after a 404 check).",
      "suggested_fix": "Inject AuditPersistenceService. Before hardDelete(), capture a full snapshot of the third party + cascade counts (from getDeletionImpact()) and emit THIRD_PARTY_DELETED with the snapshot. Emit THIRD_PARTY_CREATED, THIRD_PARTY_UPDATED (with before snapshot), THIRD_PARTY_ASSIGNED_TO_TASK, THIRD_PARTY_UNASSIGNED_FROM_TASK, THIRD_PARTY_ATTACHED_TO_PROJECT, THIRD_PARTY_DETACHED_FROM_PROJECT.",
      "acceptance_criteria": [
        "DELETE /third-parties/:id produces an audit_log row with the full third-party snapshot and cascade impact counts",
        "POST /third-parties produces an audit_log row",
        "PATCH /third-parties/:id produces an audit_log row with before/after"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 180-187 matches verbatim. No AuditPersistenceService import or injection found. ThirdPartiesController.remove() does not read @CurrentUser, so actor identity is also absent from the hardDelete flow."
    },
    {
      "id": "OBS-015",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "time-tracking: create/update/delete emit no audit_log row",
      "file": "apps/api/src/time-tracking/time-tracking.service.ts",
      "line": "208-249",
      "code_evidence": "    return this.prisma.timeEntry.create({\n      data: {\n        userId: actor.kind === 'user' ? actor.userId : null,\n        thirdPartyId: actor.kind === 'thirdParty' ? actor.thirdPartyId : null,\n        declaredById: currentUser.id,\n        date: new Date(date),\n        hours,\n        activityType,\n        taskId,\n        projectId: effectiveProjectId,",
      "description": "TimeTrackingService has no AuditPersistenceService injected and no audit log emission for any of the three mutating operations: create (line 208), update (line 586), or remove (line 644). Time entry declarations are payroll-adjacent data in a French government system; creation, modification, and deletion of work hours logged should each produce a durable audit_log row. The third-party declaration path (declaredById != userId) is particularly sensitive — an admin declaring hours on behalf of a contractor leaves no audit trace beyond the DB row itself.",
      "root_cause": "AuditPersistenceService was never injected into TimeTrackingService; the constructor only wires PrismaService, ThirdPartiesService, PermissionsService, OwnershipService, and AccessScopeService.",
      "impact": "There is no way to reconstruct who declared, modified, or deleted a time entry after the fact. In a government context, inflated hour declarations or malicious deletions go undetected in the audit trail. The remove() path hard-deletes the DB row (line 644), so deleted entries vanish completely with no forensic record.",
      "suggested_fix": "Inject AuditPersistenceService into TimeTrackingService. In create(), after the prisma.timeEntry.create(), emit an audit log with action=TIME_ENTRY_CREATED (new enum value), actorId=currentUser.id, entityType='TimeEntry', entityId=entry.id, payload={hours, activityType, date, taskId, projectId, declaredForThirdParty: !!thirdPartyId}. In update(), capture the before-state from the findUnique at line 518 and emit TIME_ENTRY_UPDATED with before/after snapshot. In remove(), capture the full entry before deletion and emit TIME_ENTRY_DELETED.",
      "acceptance_criteria": [
        "POST /time-tracking produces an audit_log row with action TIME_ENTRY_CREATED",
        "PATCH /time-tracking/:id produces an audit_log row with action TIME_ENTRY_UPDATED containing before/after fields",
        "DELETE /time-tracking/:id produces an audit_log row with action TIME_ENTRY_DELETED containing a snapshot of the deleted entry",
        "The actorId in each audit row matches the currentUser.id (declaredById, not the target userId)"
      ],
      "verification_command": "curl -s -H 'Authorization: Bearer <TOKEN>' -X DELETE http://localhost:3000/time-tracking/<ENTRY_ID> && psql $DATABASE_URL -c \"SELECT action, actor_id, entity_id FROM audit_logs WHERE action LIKE 'TIME_ENTRY%' ORDER BY created_at DESC LIMIT 1;\"",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "AuditAction enum (audit-action.enum.ts) has no TIME_ENTRY_* members, confirming no audit emission was ever added. Constructor verified: only PrismaService, ThirdPartiesService, PermissionsService, OwnershipService, AccessScopeService. Code at line 208 matches verbatim."
    },
    {
      "id": "OBS-016",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-user-import",
      "title": "POST /users/import bulk-creates users with no audit row per created user",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1400-1437",
      "code_evidence": "        const user = await this.prisma.user.create({\n          data: {\n            email: userData.email,\n            login: userData.login,\n            passwordHash,\n            firstName: userData.firstName,\n            lastName: userData.lastName,\n            roleId,\n            departmentId,\n            isActive: true,\n          },",
      "description": "In `importUsers()`, each successfully created user triggers a `prisma.user.create()` call inside a per-row try/catch loop. No `auditPersistence.log()` or `auditService.log()` is called for any created user. A bulk import of N users leaves N user provisioning events entirely untracked in the audit trail.",
      "root_cause": "The import path was written independently of the single-user create path and inherited the same missing audit instrumentation.",
      "impact": "Bulk user provisioning (e.g. at system roll-out or when onboarding a new department) is unaudited. Combined with a compromised admin, this creates a silent attack vector for mass account creation.",
      "suggested_fix": "After each successful `prisma.user.create()` in the import loop, emit `auditPersistence.log({ action: AuditAction.USER_CREATED, entityType: 'User', entityId: user.id, actorId: callerRoleCode !== undefined ? ..., payload: { importedRow: i+1, roleId: user.roleId } })`. A batch-level summary audit row (action: SYSTEM_BACKFILL or a dedicated USERS_IMPORTED) would also be acceptable and less verbose.",
      "acceptance_criteria": [
        "After POST /users/import, at least one audit_logs row exists per created user referencing the action and the new user id"
      ],
      "verification_command": "grep -n 'auditPersistence\\|auditService\\|AuditAction' apps/api/src/users/users.service.ts | grep -A2 -B2 '1400\\|1430'",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: lines 1400-1437 confirmed verbatim. Grep of entire users.service.ts confirms no audit call between lines 1398-1448. Finding confirmed."
    },
    {
      "id": "OBS-017",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing-audit-user-create",
      "title": "POST /users (admin-side user creation) emits no audit row",
      "file": "apps/api/src/users/users.service.ts",
      "line": "153-219",
      "code_evidence": "    const user = await this.prisma.user.create({\n      data: {\n        email: createUserDto.email,\n        login: createUserDto.login,\n        passwordHash,\n        firstName: createUserDto.firstName,\n        lastName: createUserDto.lastName,\n        roleId,\n        departmentId: createUserDto.departmentId,\n        avatarUrl: createUserDto.avatarUrl,\n        // SEC-011 — isActive is server-controlled on create (Model A: admins\n        // create active users). The CreateUserDto no longer carries this field,\n        // so the value is never caller-supplied. State changes go through the\n        // UPDATE path, which audits USER_DEACTIVATED / USER_REACTIVATED.\n        isActive: true,\n      },",
      "description": "After the `prisma.user.create()` call in `UsersService.create()`, no `auditPersistence.log()` or `auditService.log()` is invoked. Self-registration via `/auth/register` does emit a `REGISTER` audit, but the admin-side POST /users creation (which creates an immediately-active account) generates no audit entry. This means account provisioning by administrators is invisible in the audit trail.",
      "root_cause": "The `create()` method never received an audit emit, while the focus was on update (role change, deactivation, service membership) and delete paths.",
      "impact": "An admin who provisions a user account leaves no audit trail. A compromised admin account could create privileged users without any forensic evidence in `audit_logs`. RGPD accountability for account creation is absent for the admin path.",
      "suggested_fix": "Add `USER_CREATED = 'USER_CREATED'` to AuditAction (and to ENTITY_TYPE_BY_ACTION). After the `prisma.user.create()` and the optional `userService.createMany()`, emit: `await this.auditPersistence.log({ action: AuditAction.USER_CREATED, entityType: 'User', entityId: user.id, actorId: caller?.id ?? null, payload: { after: { roleId: user.roleId, departmentId: user.departmentId, isActive: user.isActive } } })`. The caller id should be threaded from the controller.",
      "acceptance_criteria": [
        "A durable audit_logs row with action='USER_CREATED' appears after POST /users",
        "The actorId field is the creating admin's id"
      ],
      "verification_command": "grep -n 'USER_CREATED\\|auditPersistence\\|auditService' apps/api/src/users/users.service.ts | head -20",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: grep of all audit calls in users.service.ts shows the first audit emission is at line 683 (update path). Lines 140-220 (create()) contain zero audit calls. USER_CREATED absent from audit-action.enum.ts. No global interceptor. Finding confirmed."
    },
    {
      "id": "OBS-018",
      "severity": "important",
      "category": "observability",
      "subcategory": "missing_error_boundary",
      "title": "No React error boundary or Next.js error.tsx in any of the 6 scanned route segments",
      "file": "apps/web/app/[locale]/leaves/page.tsx",
      "line": "1",
      "code_evidence": "\"use client\";\n\nimport { useEffect, useState } from \"react\";\nimport { useTranslations } from \"next-intl\";\nimport { MainLayout } from \"@/components/MainLayout\";",
      "description": "None of the 6 route segments (leaves, telework, time-tracking, clients, third-parties, events) have a co-located `error.tsx` file (Next.js App Router error boundary convention) or a React `<ErrorBoundary>` wrapper in their page component. A runtime rendering error (e.g., null-dereference when API returns unexpected shape, invalid date passed to `new Date()`) in any of these pages will crash the entire route segment and show a white screen with a browser error. The absence of `error.tsx` is confirmed by filesystem search — no such file exists under `apps/web/app/[locale]/` for any of the target directories.",
      "root_cause": "No error.tsx or React ErrorBoundary created for these route segments, leaving unhandled render errors as full-page crashes.",
      "impact": "Any rendering exception (e.g., `leave.user.department.name` when department is null, or a malformed date string from the API) will crash the entire page with no recovery path. Users see a blank/crash screen instead of a graceful error message. Severity is elevated for pages like leaves and events that are used daily by all staff.",
      "suggested_fix": "Create `apps/web/app/[locale]/leaves/error.tsx` (and similarly for the other 5 directories) following the Next.js App Router pattern: `'use client'; export default function Error({ reset }) { return <div>... <button onClick={reset}>Réessayer</button></div>; }`. Additionally add a shared `<ErrorBoundary>` wrapper inside `MainLayout` as a catch-all.",
      "acceptance_criteria": [
        "Each of the 6 directories has an error.tsx file.",
        "Navigating to the page after an injected rendering error shows a user-facing error message with a retry button.",
        "The error is logged via logger.error."
      ],
      "verification_command": "find apps/web/app/\\[locale\\]/{leaves,telework,time-tracking,clients,third-parties,events} -name 'error.tsx' 2>/dev/null | wc -l",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Filesystem check confirmed 0 error.tsx files in all 6 directories. This finding covers all 6 route segments under the shard."
    },
    {
      "id": "OBS-019",
      "severity": "important",
      "category": "observability",
      "subcategory": "misleading-security-info",
      "title": "Profile page shows `new Date()` (current time) as 'last login' instead of actual last-login timestamp",
      "file": "apps/web/app/[locale]/profile/page.tsx",
      "line": "344-356",
      "code_evidence": "                <p className=\"text-sm text-gray-600\">\n                  {t(\"security.loginHistory.lastLogin\")}{\" \"}\n                  {new Date().toLocaleDateString(\n                    locale === \"en\" ? \"en-US\" : \"fr-FR\",\n                    {\n                      day: \"numeric\",\n                      month: \"long\",\n                      year: \"numeric\",\n                      hour: \"2-digit\",\n                      minute: \"2-digit\",\n                    },\n                  )}\n                </p>",
      "description": "The Security tab of the profile page displays a 'last login' date but uses `new Date()` — the current page-render time — instead of the actual last-login timestamp from the authenticated user object. This renders the security control useless: the displayed time will always be 'right now', giving users no ability to detect unauthorised logins. Additionally, `new Date()` re-evaluates on every render, so the displayed time can update while the user watches the page.",
      "root_cause": "Placeholder implementation was never replaced with the actual `user.lastLoginAt` (or equivalent) field.",
      "impact": "Users cannot detect account compromise via the last-login indicator. The security UI is misleading — it implies the feature works when it does not. GDPR-relevant: French public-sector apps (this is a collectivité locale app) may have specific logging/audit requirements.",
      "suggested_fix": "If the API provides `lastLoginAt` on the `User` type, use `user.lastLoginAt` here. If not, either remove the section or fetch it from the API (e.g. `GET /auth/me` already returns the user object). Ensure the `User` type and the Prisma schema include `lastLoginAt: DateTime?`.",
      "acceptance_criteria": [
        "The 'last login' date shown equals the server-recorded last-login timestamp, not the current browser time",
        "The value does not change when the page re-renders without a new login"
      ],
      "verification_command": "N/A — manual verification: note displayed date, reload page, confirm it does NOT change",
      "related_findings": [],
      "cluster_id": "K",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed at profile/page.tsx lines 344-356. Additionally confirmed that `lastLoginAt` does not exist in the Prisma schema (packages/database/prisma/schema.prisma) or in packages/types/, so even if the code were fixed, the backend field does not exist yet either. Confirmed."
    },
    {
      "id": "OBS-020",
      "severity": "important",
      "category": "observability",
      "subcategory": "no-audit-retention-policy",
      "title": "Audit log retention policy undeclared and unenforced — audit_logs grows unbounded",
      "file": "packages/database/prisma/schema.prisma",
      "line": "1220",
      "code_evidence": "///  - trigger BEFORE UPDATE/DELETE -> RAISE EXCEPTION (audit_logs_no_update_delete) ;",
      "description": "The audit_logs table has an immutability trigger (audit_logs_no_update_delete) that prevents UPDATE and DELETE. The app_user role also has UPDATE/DELETE/TRUNCATE REVOKEd via init-roles.sql. These controls ensure tamper-resistance, but they also mean there is no mechanism to expire old rows. No retention period is declared in code, schema comments, or any migration. No table partitioning (pg_partman, declarative partitioning) is defined. For a French public body subject to RGPD Article 5(1)(e) (storage limitation) and the Cour des Comptes audit requirement, the audit trail must be both durable AND bounded. Growing a single unpartitioned table without a retention policy creates operational risk (table bloat, slow sequential scans) and potential RGPD non-compliance (storing audit PII — actorEmail, IP — indefinitely).",
      "root_cause": "The immutability design (OBS-002/DAT-009) correctly prioritizes tamper-resistance but does not address the complementary need for a documented and enforced time-bounded retention window.",
      "impact": "Over a multi-year production lifetime, audit_logs will accumulate millions of rows with no automated pruning. Query performance on the hash-chain verification scans (which are sequential) will degrade. If actorEmail or IP is considered personal data under RGPD, indefinite storage without a legal basis or declared retention period constitutes non-compliance. A RGPD audit or Cour des Comptes review that asks 'what is your retention policy for this table?' has no documented answer.",
      "suggested_fix": "1. Declare a retention policy in a schema comment on audit_logs (e.g. 10 years for Cour des Comptes, then archive). 2. Implement range partitioning on createdAt (one partition per year) so old partitions can be detached and archived without violating the immutability trigger on the active partition. 3. Add a pg_cron or application-level job to DETACH partitions older than the retention threshold and move them to cold storage (not DROP). The immutability trigger applies per-partition; detach = archive, not delete.",
      "acceptance_criteria": [
        "A retention policy (duration + archival procedure) is documented in the schema comment or a POLICY.md",
        "audit_logs is partitioned by RANGE on createdAt with at least a yearly partition granularity",
        "A test or migration comment demonstrates that the immutability trigger and hash-chain verification work correctly on partitioned tables"
      ],
      "verification_command": "N/A — manual verification: check packages/database/prisma/migrations/ for any CREATE TABLE audit_logs ... PARTITION BY statement",
      "related_findings": [],
      "cluster_id": "H",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. Code evidence at line 1220 is verbatim correct. The immutability trigger comment is present at that exact line. No PARTITION BY statement exists in the schema. The finding correctly identifies that DELETE/TRUNCATE revocation + immutability trigger means no automated pruning path exists. Confirmed as a governance/compliance gap. Finding stands at severity=important."
    },
    {
      "id": "OBS-021",
      "severity": "nit",
      "category": "observability",
      "subcategory": "wrong-audit-action-for-reset-token",
      "title": "generateResetToken() logs PASSWORD_CHANGED (wrong action) — action mislabeling confirmed, non-persistence claim refuted",
      "file": "apps/api/src/auth/auth.service.ts",
      "line": "524-529",
      "code_evidence": "    this.auditService.log({\n      action: AuditAction.PASSWORD_CHANGED,\n      userId: createdById,\n      details: `Password reset token generated for user ${targetUser.id}`, // OBS-027: opaque id, not login\n      success: true,\n    });",
      "description": "The `generateResetToken()` path calls `auditService.log()` and misclassifies the event as `PASSWORD_CHANGED`. No password was changed at this point — only a reset token was issued. The correct action would be something like `PASSWORD_RESET_TOKEN_ISSUED`. Note: contrary to the original finding description, `auditService.log()` DOES write to `audit_logs` via the DAT-002 dual-write mechanism in AuditService (lines 152-207 of audit.service.ts). The event IS durably persisted but under the wrong action code, which pollutes analytics querying for PASSWORD_CHANGED events.",
      "root_cause": "The action code was copied from the `resetPassword()` path without adapting it to the token-issuance event.",
      "impact": "The event is durably logged (contrary to original finding) but under a misleading action code. Analytics querying PASSWORD_CHANGED for compliance reports will include token-issuance events. The impact is reduced compared to original assessment since there IS a durable audit trail, just mislabeled.",
      "suggested_fix": "1. Add `PASSWORD_RESET_TOKEN_ISSUED = 'PASSWORD_RESET_TOKEN_ISSUED'` to AuditAction and ENTITY_TYPE_BY_ACTION. 2. Replace the PASSWORD_CHANGED action code in generateResetToken(). Optionally switch to auditPersistence.log() directly to be explicit about intent.",
      "acceptance_criteria": [
        "After POST /auth/reset-password-token, an audit_logs row with action='PASSWORD_RESET_TOKEN_ISSUED' exists",
        "No action='PASSWORD_CHANGED' row is emitted by this path"
      ],
      "verification_command": "grep -n 'generateResetToken\\|PASSWORD_CHANGED\\|auditService.log\\|auditPersistence' apps/api/src/auth/auth.service.ts",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "medium",
      "notes": "DOWNGRADED from high to medium. Original finding incorrectly stated auditService.log() goes to 'console/SecurityAudit sink only — NOT persisted to audit_logs'. Adversarial verification of audit.service.ts lines 152-207 shows AuditService.log() performs a DAT-002 dual-write: it calls auditPersistence.log() fire-and-forget for every action. ENTITY_TYPE_BY_ACTION[PASSWORD_CHANGED]='User' is defined, so the persistence call succeeds. The event IS durably logged. The mislabeled action code (PASSWORD_CHANGED vs PASSWORD_RESET_TOKEN_ISSUED) is confirmed real and reduces finding to a data-quality/analytics concern rather than an untracked security event."
    },
    {
      "id": "OBS-022",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "comments: create/update/delete emit no audit_log row",
      "file": "apps/api/src/comments/comments.service.ts",
      "line": "160-184",
      "code_evidence": "  async remove(id: string, userId: string, currentUser?: AccessUser) {\n    const comment = await this.findOne(id, currentUser);\n\n    // Seul l'auteur ou un rôle de gestion globale peut supprimer le commentaire d'autrui.\n    if (comment.authorId !== userId) {\n      const permissions = (await this.permissionsService.getPermissionsForRole(\n        currentUser?.role\n          ? typeof currentUser.role === 'string'\n            ? currentUser.role\n            : currentUser.role.code\n          : null,\n      )) as readonly string[];",
      "description": "CommentsService has no AuditPersistenceService injected. create() (line 38), update() (line 128), and remove() (line 160) emit no audit_log rows. The remove() path at line 160 explicitly allows a user with comments:delete_any, projects:manage_any, or tasks:manage_any to delete another user's comment; this management-level action on behalf of others warrants an audit trail. Severity is nit because comments are lower-sensitivity than HR/financial data.",
      "root_cause": "AuditPersistenceService was never injected into CommentsService.",
      "impact": "Admin deletion of another user's comment (the cross-ownership path in remove()) is invisible in the audit trail. A moderation action that might be contested leaves no record.",
      "suggested_fix": "Inject AuditPersistenceService. Emit COMMENT_DELETED (with the comment snapshot) at minimum for the cross-ownership deletion path. COMMENT_CREATED and COMMENT_UPDATED are lower priority.",
      "acceptance_criteria": [
        "DELETE /comments/:id where actorId != comment.authorId produces an audit_log row"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 160-184 matches verbatim. No AuditPersistenceService import or injection found in comments.service.ts."
    },
    {
      "id": "OBS-023",
      "severity": "nit",
      "category": "observability",
      "subcategory": "pii-in-redact-gap",
      "title": "req.body.login not in Fastify redact paths — user identifier would appear in logs if body serialization is enabled",
      "file": "apps/api/src/common/fastify/redact.config.ts",
      "line": "1-37",
      "code_evidence": "export const fastifyLoggerOptions = {\n  level: process.env.LOG_LEVEL ?? 'info',\n  redact: {\n    paths: [\n      // Request headers\n      'req.headers.authorization',\n      'req.headers.cookie',\n      'req.headers[\"x-api-key\"]',\n      'req.headers[\"proxy-authorization\"]',\n\n      // Request body — auth / token fields\n      'req.body.password',\n      'req.body.currentPassword',\n      'req.body.newPassword',\n      'req.body.refreshToken',\n      'req.body.token',",
      "description": "The login DTO (auth/dto/login.dto.ts) uses the field name 'login' (not 'email') for the user's identifier. The redact config does not include 'req.body.login'. Fastify's built-in pino logger does NOT serialize request bodies by default, so in normal operation this field never reaches log output. However, if a custom serializer or log middleware is added later that serializes the request body, the login identifier (a 'prenom.nom' string that is PII for French public servants) would appear in structured logs unredacted. The risk is latent, not currently exploited.",
      "root_cause": "The redact path list was likely built around the password fields specifically; the login identifier was not considered a redaction target because Fastify does not log bodies by default.",
      "impact": "If body serialization is ever enabled (e.g. for debugging a production issue), login identifiers for every auth request will appear in log output. For a French public authority, 'prenom.nom' is considered personal data under RGPD.",
      "suggested_fix": "Add 'req.body.login' to the redact paths array in redact.config.ts as a defensive measure. Also add the corresponding test in redact.config.spec.ts. This is a one-line addition with no functional impact.",
      "acceptance_criteria": [
        "'req.body.login' appears in fastifyLoggerOptions.redact.paths",
        "redact.config.spec.ts has a test asserting paths contains 'req.body.login'"
      ],
      "verification_command": "grep 'req.body.login' apps/api/src/common/fastify/redact.config.ts",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. Code evidence for lines 1-37 is verbatim correct (minor quote style difference: file uses straight quotes, finding uses escaped double quotes, but content is semantically identical). The full redact.config.ts was read: req.body.login does NOT appear in paths. Severity remains nit because Fastify does not serialize bodies by default — the risk is latent."
    },
    {
      "id": "OBS-024",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing-audit-log",
      "title": "events: create/update/delete and participant mutations emit no audit_log row",
      "file": "apps/api/src/events/events.service.ts",
      "line": "578-585",
      "code_evidence": "    await this.prisma.event.delete({\n      where: { id },\n    });\n\n    return { message: 'Événement supprimé avec succès' };\n  }\n\n  /**\n   * Récupérer les événements d'un utilisateur",
      "description": "EventsService has no AuditPersistenceService injected. None of create() (line 82), update() (line 411), remove() (line 565), addParticipant() (line 735), removeParticipant() (line 829), or stopRecurrence() (line 791) emit an audit log row. Events can be linked to projects and carry participant lists; their modification or deletion is a lightweight coordination action rather than a sensitive HR/financial mutation. Severity is nit rather than important because events are lower-risk than leave/telework/settings, though completeness of the audit trail is affected.",
      "root_cause": "AuditPersistenceService was never injected into EventsService.",
      "impact": "Event lifecycle changes (creation, modification, deletion, participant changes) leave no audit trail. Lower immediate risk than telework/settings, but a complete audit trail for a planning system ideally includes event mutations.",
      "suggested_fix": "Inject AuditPersistenceService and emit EVENT_CREATED, EVENT_UPDATED, EVENT_DELETED, EVENT_PARTICIPANT_ADDED, EVENT_PARTICIPANT_REMOVED audit rows. Since these are high-frequency, use void + .catch() (fire-and-forget) as per the existing DOCUMENT_READ pattern in documents.service.ts.",
      "acceptance_criteria": [
        "DELETE /events/:id produces an audit_log row",
        "POST /events/:id/participants produces an audit_log row"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code at lines 578-585 matches verbatim (delete at line 580, message return at line 584). No AuditPersistenceService import or injection found in events.service.ts."
    },
    {
      "id": "OBS-025",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing-audit-project-members",
      "title": "Project member add/update/remove emit no audit rows",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "891-1041",
      "code_evidence": "  async addMember(projectId: string, addMemberDto: AddMemberDto) {\n    // ...\n    // Ajouter le membre\n    const member = await this.prisma.projectMember.create({\n      data: {\n        projectId,\n        userId,\n        role: role || 'Membre',\n        ...(allocation !== undefined && { allocation }),\n        ...(startDate && { startDate: new Date(startDate) }),\n        ...(endDate && { endDate: new Date(endDate) }),\n      },",
      "description": "The three project membership methods — `addMember()` (lines 891–951), `updateMember()` (lines 957–1003), and `removeMember()` (lines 1008–1041) — perform DB writes with no audit emission. Project membership defines who has access to a project and in what role (allocation, project role). Adding/removing members changes the project's permission perimeter but is untracked.",
      "root_cause": "Project membership management never received audit instrumentation.",
      "impact": "A project owner can add or remove team members without any audit record. For accountability over who had project access at a given time this is a gap, though less critical than the lifecycle events in O2-7.",
      "suggested_fix": "Add PROJECT_MEMBER_ADDED / PROJECT_MEMBER_REMOVED / PROJECT_MEMBER_UPDATED to AuditAction and ENTITY_TYPE_BY_ACTION. Emit in each method after the DB write.",
      "acceptance_criteria": [
        "After POST /projects/:id/members, an audit_logs row with action='PROJECT_MEMBER_ADDED' exists"
      ],
      "verification_command": "grep -n 'addMember\\|removeMember\\|updateMember\\|auditPersistence' apps/api/src/projects/projects.service.ts | head -20",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: addMember() at 891-951, updateMember() at 957-1003, removeMember() at 1008-1041 confirmed verbatim. No audit calls present. The only audit calls in projects.service.ts are at lines 710, 748, 855 (all outside member-management range). Finding confirmed."
    },
    {
      "id": "OBS-026",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing error boundary",
      "title": "No error boundary anywhere in the [locale] subtree — unhandled render errors crash the entire app",
      "file": "apps/web/app/[locale]/layout.tsx",
      "line": "1-30",
      "code_evidence": "      <QueryProvider>{children}</QueryProvider>",
      "description": "The `app/[locale]/` directory has no `error.tsx` file (Next.js App Router error boundary). `find apps/web/app/[locale] -name 'error.tsx'` returns nothing. An unhandled JavaScript error in any component within the locale subtree (e.g., SkillsMatrix, reports charts, SuiviPage) will crash the entire application and display a white screen instead of a user-facing error message. This is especially relevant given that several components (SkillsMatrix, ProjectsDetailTable, AdvancedAnalyticsTab sub-components) do their own loading/error state management but their render logic can still throw on malformed API data.",
      "root_cause": "No `error.tsx` file created at the `app/[locale]/` or page-level directories.",
      "impact": "Any unhandled render error shows a blank screen with no user-facing feedback. Affects all authenticated users.",
      "suggested_fix": "Create `apps/web/app/[locale]/error.tsx` with a user-friendly error boundary component that displays a 'Something went wrong' message and a retry/navigate-home button.",
      "acceptance_criteria": [
        "An error.tsx file exists at apps/web/app/[locale]/error.tsx",
        "Throwing an error in a page component renders the error boundary instead of crashing the app"
      ],
      "verification_command": "find apps/web/app/\\[locale\\] -name 'error.tsx'",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Verified: `find /home/alex/Documents/REPO/ORCHESTRA/apps/web/app/[locale] -name 'error.tsx'` returned no results. The layout.tsx code is verbatim. No error.tsx at any subdirectory level either."
    },
    {
      "id": "OBS-027",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing-error-boundary",
      "title": "Profile page (`profile/page.tsx`) has no error boundary — uncaught render errors cause a blank screen",
      "file": "apps/web/app/[locale]/profile/page.tsx",
      "line": "23-639",
      "code_evidence": "export default function ProfilePage() {\n  const t = useTranslations(\"profile\");\n  const tCommon = useTranslations(\"common\");\n  const locale = useLocale();\n  const router = useRouter();\n  const pathname = usePathname();\n  const user = useAuthStore((state) => state.user);",
      "description": "The ProfilePage component is a large client component (~640 lines) that integrates avatar upload, ICS import/export, password change modal, and several async service calls. None of these operations are wrapped in a React error boundary. If any of the child components (`IcsExportSection`, `IcsImportSection`, `UserAvatar`) throws during render (e.g. a type error on unexpected API shape), the entire page crashes with a blank screen and no user-facing error message.",
      "root_cause": "No error boundary component wraps the page or its sub-sections.",
      "impact": "Users get a blank screen on render errors with no recovery path. The profile page hosts security-critical functionality (password change), so a crash there is impactful.",
      "suggested_fix": "Wrap the page content (or individual sections like ICS import/export) in a React error boundary: `<ErrorBoundary fallback={<p>Erreur de chargement</p>}>`. Next.js App Router supports `error.tsx` files at the route segment level as an alternative.",
      "acceptance_criteria": [
        "A rendering error in IcsImportSection or IcsExportSection shows a user-friendly error message rather than a blank screen",
        "The rest of the profile page remains functional when one section errors"
      ],
      "verification_command": "N/A — manual verification: throw in IcsExportSection render, confirm error boundary catches it",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed. No error.tsx file exists anywhere under apps/web/app/ (confirmed via find). No ErrorBoundary component import in profile/page.tsx or in the IcsExportSection/IcsImportSection component files. Next.js ErrorBoundary references found are internal framework code only (in .next/ build artifacts), not app-level usage. Confirmed."
    },
    {
      "id": "OBS-028",
      "severity": "nit",
      "category": "observability",
      "subcategory": "missing-error-boundary",
      "title": "No error boundary wrapping any of the three page-level components",
      "file": "apps/web/app/[locale]/projects/page.tsx",
      "line": "1",
      "code_evidence": "\"use client\";\n\nimport { useEffect, useState, useCallback, useMemo } from \"react\";\nimport { useRouter, useSearchParams } from \"next/navigation\";\nimport { useTranslations, useLocale } from \"next-intl\";\nimport { MainLayout } from \"@/components/MainLayout\";",
      "description": "None of the three pages (projects/page.tsx, tasks/page.tsx, planning/page.tsx) or their parent layouts are wrapped in a React Error Boundary. An unhandled render-time error in any child component (e.g. unexpected null from the API, a bug in a component reading task.project.name when project is null) will result in a blank white screen for the user with no actionable message. The planning page in particular delegates entirely to the dynamically loaded PlanningView component with no fallback.",
      "root_cause": "No Error Boundary component was added to these routes at the Next.js page level or the MainLayout level.",
      "impact": "Any uncaught render error in projects, tasks, or planning pages will crash the entire page for the user with no recovery path and no user-facing error message.",
      "suggested_fix": "Add a Next.js `error.tsx` (App Router convention) next to each `page.tsx`, or wrap `<MainLayout>` in a shared `<ErrorBoundary fallback={...}>` component that displays a user-friendly error message and a retry button.",
      "acceptance_criteria": [
        "Throwing an error from a child component inside MainLayout displays an error fallback UI instead of a blank page",
        "The error is reported (via logger or monitoring service)"
      ],
      "verification_command": "find apps/web/app/\\[locale\\]/projects apps/web/app/\\[locale\\]/tasks apps/web/app/\\[locale\\]/planning -name 'error.tsx' | wc -l",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "observability",
      "confidence": "high",
      "notes": "Adversarial check: no error.tsx found in any of the three directories. No ErrorBoundary import found in these pages. Confirmed."
    },
    {
      "id": "OBS-029",
      "severity": "suggestion",
      "category": "observability",
      "subcategory": "no-real-deploy-workflow",
      "title": "CI pipeline has no real VPS deployment step — 'notify-success' job only echoes, docker-publish does not SSH to prod",
      "file": ".github/workflows/ci.yml",
      "line": "579-592",
      "code_evidence": "  notify-success:\n    name: Notify Success\n    runs-on: ubuntu-latest\n    needs: [lint, backend-tests, frontend-tests, build]\n    if: success()\n\n    steps:\n      - name: Success Notification\n        run: |\n          echo \"✅ CI/CD Pipeline completed successfully!\"\n          echo \"All tests passed and build validated.\"\n          echo \"Commit: ${{ github.sha }}\"\n          echo \"Branch: ${{ github.ref_name }}\"",
      "description": "The CI pipeline's final job ('notify-success') only echoes to stdout. docker-publish.yml pushes Docker images to GHCR but does not SSH to the production VPS (92.222.35.25) to perform a docker compose pull + up. There is no deployment workflow: the production deployment is a manual operation (confirmed by MEMORY.md: 'deploy workflow is fake'). This means CI green ≠ prod updated, and there is no audit trail of which CI run led to which prod deployment. The RELEASE_DEPLOYED audit action exists in the codebase and writes to audit_logs, but it is only triggered by DeploymentsService on container boot — the Github Actions pipeline cannot trigger it.",
      "root_cause": "Deployment to the VPS was never automated in the GitHub Actions pipeline; it is performed manually via SSH. The ci.yml workflow was built as a CI-only pipeline, not a CD pipeline.",
      "impact": "No deployment observability from the CI side. A developer who merges to master cannot confirm from the GitHub Actions run that their change is on production. The RELEASE_DEPLOYED audit row is the only trace of a deployment, and it is generated by the running container, not by the CI run. There is no rollback automation. This is a process/operational risk, not a code defect.",
      "suggested_fix": "Add a deployment job after docker-build that: (1) SSHs to the VPS using a GitHub secret (PROD_SSH_KEY), (2) pulls the new image via docker compose pull, (3) runs docker compose up -d --env-file .env.production, (4) waits for the health endpoint. Alternatively, add a webhook-based trigger on the VPS that pulls and restarts on new GHCR image availability. Either approach should be gated on a manual approval step for production.",
      "acceptance_criteria": [
        "A GitHub Actions job exists that performs an actual docker compose pull + up on the VPS after a successful build",
        "The job is gated on manual approval (environment protection rule) for the production environment",
        "Deployment failures surface as failed workflow runs with actionable error output"
      ],
      "verification_command": "grep -n 'ssh\\|rsync\\|appleboy/ssh-action' .github/workflows/*.yml",
      "related_findings": [],
      "cluster_id": "K",
      "found_by": "observability",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: Fully confirmed. Code evidence at lines 579-592 is verbatim correct. This is a known, intentional gap documented in project MEMORY.md ('Deploy workflow is fake'). No SSH action, no appleboy/ssh-action, no real VPS deployment step found in any workflow file. Finding stands at severity=suggestion."
    }
  ],
  "verified_clean": [
    "A HealthModule (apps/api/src/health/) exists and is registered in app.module.ts, providing /api/health (outside this shard's scan scope)",
    "A MetricsModule (apps/api/src/metrics/) exists and is registered in app.module.ts, providing a Prometheus-format /api/metrics endpoint (outside this shard's scan scope but confirms instrumentation exists)",
    "API proxy route.ts uses process.env.API_URL — not user-controlled, no SSRF via URL manipulation",
    "AUDIT_HASH_KEY is enforced at boot in ALL environments (assertAuditHashKey); attempted-login identifiers are stored as HMAC (not raw) in the immutable trail, preventing PII persistence",
    "AdvancedAnalyticsTab sub-components (WorkloadChart, RecentActivity, MilestonesCompletion): correct TanStack Query usage with staleTime",
    "All pages use 'use client' directive correctly — no attempted server-side secret access",
    "All services use Axios (api instance) with Bearer token from localStorage — no cookie-based auth mixing issues",
    "AllExceptionsFilter (OBS-017) correctly masks non-HttpException errors to safe { statusCode, message, timestamp, path } responses — full error details are logged server-side only, never forwarded to the client",
    "Audit row immutability is enforced at the database level via the audit_logs_no_update_delete BEFORE UPDATE/DELETE trigger (created in migration 20260525190000_audit_logs_immutability_hash_chain_actor_snapshot)",
    "AuditAction enum is the single source of truth for action codes; compile-time witnesses in audit-action.compile-witness.ts and audit-payload-registry.compile-witness.ts prevent adding an action without a schema or entity-type mapping",
    "AuditPersistenceService uses pg_advisory_xact_lock to serialize all audit inserts, ensuring a totally ordered hash chain with no fork risk under concurrent writes",
    "Auth decisions (canEdit/canDelete) are not computed client-side: role/permissions come from server via useAuthBootstrap",
    "Auth decisions (canEditProject, canDeleteProject, canEdit, canCreateProject, canCreateTask) are all derived from hasPermission() which reads from the RBAC permission store — no client-side role string comparison",
    "AuthProvider guards all pages against unauthenticated access and blocks render until permissionsLoaded=true",
    "Bcrypt-shaped values in the login field are detected and replaced with 'unknown' before persisting to audit_logs, preventing credential-in-trail leaks",
    "Blob URL for template download (leaves/page.tsx:524) correctly calls URL.revokeObjectURL() immediately after click",
    "CSP is correctly applied to response headers in production only (dev correctly skipped for HMR)",
    "CSP nonce: pages use no inline scripts or style injection that would require nonce handling",
    "CacheService logs cache failures as warnings (not errors) and degrades gracefully (fail-open) — Redis unavailability never throws to callers",
    "Fastify structured logging is configured with PII redact paths for authorization headers, cookie, password fields, token fields, and sensitive PII body fields (validationComment, reason, motif, justification) in redact.config.ts",
    "GanttChart is lazy-loaded with dynamic() and ssr:false — correct for a heavy client-side component",
    "ImportPreviewModal: no XSS risk; all preview data rendered as text nodes via JSX",
    "JWT/access_token localStorage storage is intentional per CLAUDE.md (not flagged)",
    "Locale/i18n routing: useLocale() used consistently for locale prefix; no manual locale string hardcoding",
    "Next.js catch-all [...path] route normalizes path segments at the router level, mitigating naive ../ traversal",
    "No XSS via CSS injection — leaveType.color from API is used directly in style prop (backgroundColor, color) without HTML interpolation; CSS property value injection has no code-execution path in React",
    "No XSS via dangerouslySetInnerHTML anywhere in scanned scope",
    "No barrel import performance issues in the scanned service files (each imports directly from @/lib/api)",
    "No console.* calls with PII — all files use the centralized logger with PII scrubbing (logger.ts)",
    "No console.log with PII: all debug logging goes through the logger utility imported from @/lib/logger",
    "No console.log/console.error with PII in scanned files (replaced by structured logger)",
    "No console.log/warn/debug calls with PII in scanned pages or components",
    "No console.log/warn/error/info in any non-test source file under apps/api/src/audit/, apps/api/src/common/, or apps/api/src/main.ts — all logging goes through NestJS Logger (which routes to pino/Fastify)",
    "No dangerouslySetInnerHTML usage found in any scanned file",
    "No dangerouslySetInnerHTML usage in any of the 8 scanned files",
    "No dangerouslySetInnerHTML usage in any scanned file",
    "No hardcoded role constants (Role.ADMIN, Role.MANAGER) in client-side auth decisions — all RBAC checks use hasPermission() from usePermissions hook",
    "No localStorage or sessionStorage access for tokens in any scanned file",
    "No open redirect (no user-controlled string passed directly to router.push/replace without validation beyond the ProjectsDetailTable locale bug which is not exploitable for external redirects)",
    "No open redirect vulnerabilities — all router.push() calls use hardcoded locale-prefixed paths",
    "No open redirect: all router.push() calls use locale-prefixed internal paths constructed from controlled variables",
    "No open redirect: login and register pages hardcode /${locale}/dashboard post-login; no searchParams redirect parameter",
    "No secrets or API keys embedded in client bundles in scanned files",
    "No secrets or API tokens hardcoded in client-side source files",
    "No secrets or tokens leaked to client bundle (no API keys, JWT_SECRET, etc. in component files)",
    "No secrets, tokens, or credentials exposed to the client bundle in any scanned file",
    "No unbounded list renders in the scanned auth/profile pages",
    "Optimistic updates: no optimistic update pattern is used — all mutations await the API call before updating state (safe but slower)",
    "Password reset token is read from URL searchParams (not localStorage) — no persistence risk",
    "Payload schema validation (Zod, strict mode) runs before each audit INSERT — malformed payloads are rejected, no partial rows",
    "PlanningView is lazy-loaded with dynamic() and ssr:false — correct for a calendar/planning component",
    "QuickTimeEntryInput: properly validates hours before submitting; no SSRF or injection risk",
    "Request-ID (correlation ID) is generated and propagated via AsyncLocalStorage in request-id.context.ts; every request gets a stable UUID or honors x-request-id header (sanitized: CR/LF stripped, capped at 128 chars)",
    "SkillsMatrix: no XSS risk; all user data is text-rendered via JSX, not innerHTML",
    "SuiviPage fetches user data before performing access check and stops all downstream fetches if access is denied",
    "TanStack Query is not used in scanned pages (they use direct service calls or Zustand) — no staleTime issues",
    "TaskCard correctly uses useLocale and prefixes all navigation with /${locale}/",
    "Test files exist for leaves (TST-023) and telework covering RBAC affordances",
    "The app_user role has UPDATE/DELETE/TRUNCATE REVOKEd on audit_logs via init-roles.sql — DB-level defense-in-depth against application-path bypass",
    "The docker-publish.yml workflow performs real Docker image builds and pushes to GHCR — it is not a stub",
    "The recompute-chain.ts utility shares computeRowHash with the write path — no hash reimplementation risk between maintenance scripts and the write path",
    "UserAvatar uses Next.js Image component with auth-gated blob URLs for uploaded avatars (SEC-016 addressed)",
    "Zustand auth store does not persist role/permissions to localStorage (only minimal display cache)",
    "_dev/gantt-base/page.tsx: dev page uses logger.info correctly, no production exposure",
    "audit-action.enum.ts: enum contains no TIME_ENTRY_*, TELEWORK_*, SETTINGS_CHANGED, CLIENT_*, THIRD_PARTY_*, EVENT_*, COMMENT_* values — confirms no audit emissions exist in these modules",
    "audit/audit-action.enum.ts: reviewed — no LOGOUT, USER_CREATED, PROJECT_CREATED, PROJECT_UPDATED, PROJECT_CANCELLED, TASK_* members exist (confirms gaps are structural)",
    "auth.service.ts logout() redirects to bare /login (handled by next-intl middleware locale routing) — not a user-controlled URL",
    "auth/auth.controller.ts: all non-logout endpoints reviewed",
    "auth/auth.service.ts: account locked → durable AuditAction.ACCOUNT_LOCKED with before/after (auditService.log)",
    "auth/auth.service.ts: all service methods reviewed",
    "auth/auth.service.ts: login failure → durable AuditAction.LOGIN_FAILURE (auditService.log)",
    "auth/auth.service.ts: login success → durable AuditAction.LOGIN_SUCCESS (auditService.log)",
    "auth/auth.service.ts: register → AuditAction.REGISTER (auditService.log)",
    "auth/auth.service.ts: resetPassword (token consumption) → AuditAction.PASSWORD_CHANGED (auditService.log)",
    "canEdit and canDelete flags in leaves/page.tsx (lines 757, 767) correctly use API-returned flags from the leave object, not client-side computation",
    "change-password/page.tsx correctly redirects to /${locale}/login after success (not to arbitrary URL)",
    "clients.service.ts: hardDelete() blocks deletion when projectsCount > 0 — referential integrity clean",
    "clients/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "clients/page.tsx and third-parties/page.tsx permission checks (canCreate, canUpdate, canDelete) correctly use hasPermission() hook",
    "comments.service.ts: remove() checks comments:delete_any / projects:manage_any / tasks:manage_any before cross-ownership deletion — RBAC clean",
    "computeRowHash covers action, entityType, entityId, actorId, schemaVersion, createdAt, payload (stable JSON), and prevHash — tamper detection covers the full row including schema version",
    "dashboard/page.tsx: uses Promise.all with per-catch error isolation for parallel fetches, no unhandled rejection",
    "departments/page.tsx: no locale routing bugs; uses inline modals with no navigation",
    "documents.service.ts: DOCUMENT_READ (OBS-006) correctly emits an audit_log row for GET /:id after access check passes, with fire-and-forget + .catch() pattern — clean",
    "documents.service.ts: create() enforces access-scope check via assertCanAccessProject before the DB write — access control clean",
    "documents.service.ts: remove() enforces ownership-or-manage_any before soft-delete — access control clean",
    "events.service.ts: ensureCanMutate() enforces service-layer ownership check — access control clean",
    "events/page.tsx canCreateEvent() and canDeleteEvent() correctly use hasPermission() hook",
    "forgot-password/page.tsx is a static info page — no form submission, no CSRF risk",
    "leaves/leaves.controller.ts: all endpoints reviewed",
    "leaves/leaves.service.ts: all service methods reviewed",
    "leaves/leaves.service.ts: approve → AuditAction.LEAVE_APPROVED durable with actor/subject snapshots inside tx (auditPersistence.log)",
    "leaves/leaves.service.ts: cancel → AuditAction.LEAVE_CANCELLED durable with before/after status inside tx (auditPersistence.log)",
    "leaves/leaves.service.ts: deleteBalance → AuditAction.LEAVE_BALANCE_ADJUSTED durable with operation='DELETE' (auditPersistence.log)",
    "leaves/leaves.service.ts: reject → AuditAction.LEAVE_REJECTED durable with actor/subject snapshots inside tx (auditPersistence.log)",
    "leaves/leaves.service.ts: remove (hard delete) → AuditAction.LEAVE_DELETED durable with full snapshot (auditPersistence.log)",
    "leaves/leaves.service.ts: requestCancel → AuditAction.LEAVE_CANCELLATION_REQUESTED durable with actor snapshot inside tx (auditPersistence.log)",
    "leaves/leaves.service.ts: update → AuditAction.LEAVE_UPDATED durable with before/after dates/days inside tx (auditPersistence.log)",
    "leaves/leaves.service.ts: upsertBalance → AuditAction.LEAVE_BALANCE_ADJUSTED durable with before/after totalDays and operation (auditPersistence.log)",
    "localStorage token exposure: auth state is in Zustand store (memory), not directly read/written in these pages",
    "localStorage usage in MyTasksSection is limited to UI preference (undeclaredExpanded) — no sensitive data stored",
    "mailto: href in third-parties/[id]/page.tsx uses server-sourced contactEmail — no open redirect to external HTTP URLs",
    "middleware.ts uses crypto.randomUUID() for nonce — cryptographically secure",
    "planning/__tests__/page.test.tsx: test file exists and covers basic rendering",
    "process-level unhandledRejection and uncaughtException handlers are installed via installGlobalErrorHandlers before the app boots (idempotent, WeakSet-guarded)",
    "projects/[id]/page.tsx: handleHardDeleteProject, handleAddMember, handleAttachClients, handleDetachClient, handleSaveMilestone, handleSaveTask all have try/catch with user-facing error toasts",
    "projects/__tests__/page.test.tsx: test file exists and covers filter and rendering behavior",
    "projects/page.tsx: handleCreate has try/catch with user-facing error toast",
    "projects/projects.controller.ts: all endpoints reviewed",
    "projects/projects.service.ts: all service methods reviewed",
    "projects/projects.service.ts: archive → AuditAction.PROJECT_ARCHIVED durable inside tx (auditPersistence.log)",
    "projects/projects.service.ts: hardDelete → AuditAction.PROJECT_DELETED durable with column snapshot before delete (auditPersistence.log)",
    "projects/projects.service.ts: unarchive → AuditAction.PROJECT_UNARCHIVED durable inside tx (auditPersistence.log)",
    "rbac/roles.controller.ts: all endpoints reviewed",
    "rbac/roles.service.ts: all service methods reviewed",
    "rbac/roles.service.ts: createRole → AuditAction.ROLE_CREATED durable with after-state (auditPersistence.log)",
    "rbac/roles.service.ts: default role change → AuditAction.ROLE_DEFAULT_CHANGED durable with before/after roleId (auditPersistence.log)",
    "rbac/roles.service.ts: deleteRole → AuditAction.ROLE_DELETED durable with snapshot before delete (auditPersistence.log)",
    "rbac/roles.service.ts: updateRole → AuditAction.ROLE_UPDATED durable with before/after (auditPersistence.log) — no-op DTOs emit nothing",
    "reset-password/page.tsx correctly disables inputs when token is missing",
    "settings.service.ts: update() blocks unknown setting keys via isKnownKey() allowlist — input validation clean",
    "settings/page.tsx: isAdmin evaluated only after AuthProvider ensures permissionsLoaded=true, no flash redirect",
    "tasks/[id]/page.tsx: handleSave, handleDelete, handleDependencyChange all have try/catch with user-facing error toasts",
    "tasks/__tests__/page.test.tsx: test file exists and covers multiple interaction flows",
    "tasks/page.tsx: handleStatusChange, handleDeleteTask, handleDateChange all have try/catch with user-facing error toasts",
    "tasks/tasks.controller.ts: all endpoints reviewed",
    "tasks/tasks.service.ts: all service methods reviewed",
    "tasks/tasks.service.ts: exportProjectTasksCsv → DATA_EXPORTED durable via emitDataExported helper (auditPersistence.log)",
    "telework.service.ts: create/update/remove check telework:manage_any before modifying another user's telework — access control clean",
    "telework/page.tsx fetchTeleworkData useCallback has correct deps [currentMonth, selectedUserId, tc]",
    "third-parties.service.ts: hardDelete() proceeds but cascade FK is documented — cascade behavior documented",
    "third-parties/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "time-tracking.service.ts: ensureCanMutate() provides service-layer ownership defense-in-depth — access control clean",
    "time-tracking.service.ts: findAll() enforces strict 403 when filtering by another user's userId without view_any — access control clean",
    "useAuthBootstrap correctly fetches /auth/me + /auth/me/permissions in parallel and is the source of truth",
    "usePermissions hook reads from server-hydrated permissions array, not from client-computed role checks",
    "users/users.controller.ts: all endpoints reviewed",
    "users/users.service.ts: all service methods reviewed",
    "users/users.service.ts: changePassword → AuditAction.PASSWORD_CHANGED durable with before/after forcePasswordChange (auditPersistence.log)",
    "users/users.service.ts: hardDelete → AuditAction.USER_DELETED durable with column snapshot before erasure (auditPersistence.log)",
    "users/users.service.ts: remove (soft-delete) → AuditAction.USER_DEACTIVATED durable (auditPersistence.log)",
    "users/users.service.ts: resetPassword (admin) → AuditAction.PASSWORD_RESET_BY_ADMIN durable with before/after updatedAt (auditPersistence.log)",
    "users/users.service.ts: update with departmentId change → AuditAction.DEPARTMENT_CHANGED durable (auditPersistence.log)",
    "users/users.service.ts: update with isActive false → AuditAction.USER_DEACTIVATED durable with before/after (auditPersistence.log)",
    "users/users.service.ts: update with isActive true → AuditAction.USER_REACTIVATED durable with before/after (auditPersistence.log)",
    "users/users.service.ts: update with role change → AuditAction.ROLE_CHANGE durable with before/after roleCode (auditPersistence.log)",
    "users/users.service.ts: update with serviceIds change → AuditAction.SERVICE_MEMBERSHIP_CHANGED durable with added/removed diff (auditPersistence.log)",
    "withAccessControl HOC correctly renders null when permissions are absent"
  ]
}
