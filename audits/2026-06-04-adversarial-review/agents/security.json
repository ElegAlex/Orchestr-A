{
  "category": "security",
  "finding_count": 66,
  "findings": [
    {
      "id": "SEC-001",
      "severity": "important",
      "category": "security",
      "subcategory": "TLS / HTTPS",
      "title": "Committed nginx.conf has no :443 TLS block; live TLS is terminated by an out-of-repo host nginx (IaC drift) and HSTS is absent",
      "file": "nginx/nginx.conf",
      "line": "87-194",
      "code_evidence": "    server {\n        listen 80;\n        server_name orchestr-a.com www.orchestr-a.com _;\n\n        # ACME challenge for Let's Encrypt\n        location /.well-known/acme-challenge/ {\n            root /var/www/certbot;\n        }",
      "description": "The nginx configuration defines only a single HTTP server block listening on port 80. docker-compose.prod.yml publishes port 443 (`\"${HTTPS_PORT:-443}:443\"`) and mounts certbot certificates (`certbot_certs:/etc/nginx/ssl:ro`), but nginx.conf has no corresponding `listen 443 ssl` block, no TLS directives, no HTTP-to-HTTPS redirect, and no HSTS header. Port 443 in production will silently refuse connections (TCP RST or hang) while all traffic runs over plaintext HTTP, rendering in-transit encryption entirely absent.",
      "root_cause": "The nginx.conf was written with a comment stating 'behind host nginx SSL proxy', but TLS termination is never actually configured either in this file or verifiably in any other shipped artifact.",
      "impact": "Verified read-only against live prod (2026-06-04): production is NOT plaintext. A host-level certbot-managed nginx (Let's Encrypt cert CN=orchestr-a.com, valid 2026-05-20..2026-08-18) terminates TLS on :443 and 301-redirects http->https, proxying to the inner container on 127.0.0.1:8080. Residual risks: (1) HSTS absent (no Strict-Transport-Security header) -> first-request SSL-strip/downgrade window; (2) the TLS edge exists only on the box, not in the repo or docker-compose.prod.yml -> unreviewable, not reproducible from version control.",
      "suggested_fix": "(1) Add 'add_header Strict-Transport-Security \"max-age=63072000; includeSubDomains; preload\" always;' to the :443 server. (2) Bring the host nginx TLS terminator into version control / IaC (or move the 443+certbot config into the committed nginx.conf and publish 443 from the container) so the edge is reproducible and reviewable. (3) Confirm TLSv1/1.1 are effectively disabled (live http-context ssl_protocols still lists them; certbot include sets 1.2/1.3).",
      "acceptance_criteria": [
        "nginx -t passes with HTTPS server block",
        "HTTP requests to port 80 return 301 redirect to https://",
        "curl -I https://orchestr-a.com returns 200 with Strict-Transport-Security header",
        "curl -I http://orchestr-a.com returns 301"
      ],
      "verification_command": "curl -sSI --resolve orchestr-a.com:443:92.222.35.25 https://orchestr-a.com/ | grep -i strict-transport-security  # expect HSTS header present after fix",
      "related_findings": [
        "SEC-033",
        "SEC-032"
      ],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Downgraded blocking->important after read-only live verification (operator-approved in-place patch). Evidence: audits/2026-06-04-adversarial-review/SEC-001-prod-verification.md. Live: certbot.timer active, http->https 301, valid LE cert; the committed nginx.conf genuinely lacks a 443 block (code_evidence still accurate) but is the inner container behind the host TLS terminator."
    },
    {
      "id": "SEC-002",
      "severity": "important",
      "category": "security",
      "subcategory": "Missing AUDIT_HASH_KEY in CI — E2E API startup will crash",
      "title": "CI e2e-smoke and e2e-tests jobs start the API without AUDIT_HASH_KEY, causing assertAuditHashKey() to abort in all environments",
      "file": ".github/workflows/ci.yml",
      "line": "263-272",
      "code_evidence": "      - name: Start Backend API\n        run: |\n          pnpm --filter api start > /tmp/api.log 2>&1 &\n          echo \"Waiting for API to be ready...\"\n          timeout 60 bash -c 'until curl -f http://localhost:4000/api/health 2>/dev/null; do sleep 2; done'\n        env:\n          DATABASE_URL: postgresql://orchestr_a:orchestr_a_dev_password@localhost:5432/orchestr_a_v2_e2e\n          JWT_SECRET: test-jwt-secret-key\n          REDIS_URL: redis://localhost:6379\n          PORT: 4000\n          ALLOWED_ORIGINS: http://localhost:3000\n          TZ: Europe/Paris",
      "description": "Both `e2e-smoke` and `e2e-tests` CI jobs start the API server without setting the `AUDIT_HASH_KEY` environment variable. The `assertAuditHashKey()` function in main.ts is enforced in ALL environments (not just production) and will throw an Error if `AUDIT_HASH_KEY` is absent or shorter than 32 characters. This means both E2E jobs fail silently (the API crashes immediately, the `curl` healthcheck loop times out after 60 seconds, and E2E tests never run). The security gate — correct — but its absence from CI blocks E2E test coverage.",
      "root_cause": "The AUDIT_HASH_KEY requirement was added as a universal (non-prod-gated) boot assertion, but the CI workflow was not updated to provide the variable in E2E startup steps.",
      "impact": "All E2E tests in CI are silently skipped/fail because the API never starts. Security coverage from E2E tests is absent. If CI is mis-read as passing (e.g., due to `timeout` exit code handling), regressions go undetected.",
      "suggested_fix": "Add `AUDIT_HASH_KEY: a-32-char-or-longer-test-audit-hash-key` to the `env:` block of every 'Start Backend API' step in ci.yml (both e2e-smoke and e2e-tests jobs). Use a 32+ character test value; it is not a secret in this context (it protects the audit trail HMAC, and the test DB is ephemeral).",
      "acceptance_criteria": [
        "Both e2e-smoke and e2e-tests jobs in CI successfully start the API (healthcheck passes within 60s)",
        "AUDIT_HASH_KEY is present and >= 32 chars in all 'Start Backend API' env blocks"
      ],
      "verification_command": "grep -A 15 'Start Backend API' .github/workflows/ci.yml | grep AUDIT_HASH_KEY",
      "related_findings": [],
      "cluster_id": "K",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: ci.yml lines 262-273 (e2e-smoke) and lines 397-408 (e2e-tests) both confirmed to have identical env blocks missing AUDIT_HASH_KEY. assertAuditHashKey() in apps/api/src/common/config/audit-hash-key.ts confirmed to run in ALL environments unconditionally (no NODE_ENV check). The api will throw 'AUDIT_HASH_KEY must be set and at least 32 characters' at boot, preventing the healthcheck from ever succeeding within the 60s timeout."
    },
    {
      "id": "SEC-003",
      "severity": "important",
      "category": "security",
      "subcategory": "input-validation",
      "title": "LoginDto.password has no @MaxLength — long password input allows DoS via bcrypt CPU exhaustion",
      "file": "apps/api/src/auth/dto/login.dto.ts",
      "line": "13-19",
      "code_evidence": "  @ApiProperty({\n    description: 'Mot de passe',\n    example: 'admin123',\n  })\n  @IsString()\n  @MinLength(6)\n  password: string;",
      "description": "The LoginDto's password field only enforces @IsString and @MinLength(6). There is no @MaxLength. bcrypt is deliberately CPU-intensive: hashing or comparing a very long string (e.g. 10 KB) can consume hundreds of milliseconds. Even with the @Throttle limit (5/min per IP) an attacker can send 5 requests per minute each with a 1 MB password body, generating significant CPU load. The per-IP throttle mitigates rate but not payload size. Many bcrypt libraries also truncate at 72 bytes, but the comparison still iterates on the full input up to that truncation in some implementations, and the server reads and deserialises the full body. NestJS's ValidationPipe does not cap body size by itself; a separate global limit should exist, but DTO-level MaxLength is the defense-in-depth layer at the validation gate.",
      "root_cause": "LoginDto was never given a @MaxLength on password, and the global throttle was treated as the sole guard against credential-stuffing DoS.",
      "impact": "A single attacker IP can send 5 requests/minute each with a multi-MB password string, causing high CPU load on the auth path. Under sustained attack from multiple IPs this degrades auth latency for legitimate users.",
      "suggested_fix": "Add @MaxLength(1024) to LoginDto.password (and optionally @MaxLength(254) to login, matching the NIST recommendation). Also confirm that Fastify's bodyLimit is set to a reasonable value (e.g. 1 MB) in main.ts to cap the raw body size before DTO validation.",
      "acceptance_criteria": [
        "POST /auth/login with a password longer than 1024 characters returns 400 Bad Request.",
        "Login with a valid <= 1024 char password still succeeds."
      ],
      "verification_command": "grep -n 'MaxLength\\|bodyLimit' apps/api/src/auth/dto/login.dto.ts apps/api/src/main.ts 2>/dev/null | head -20",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Adversarial check: main.ts line 96-98 sets bodyLimit=1048576 (1 MiB, SEC-025) which provides an outer bound — a single request body cannot exceed 1 MB. However, a 1 MB password still represents ~14,000 characters feeding into bcrypt. The DTO-level MaxLength is still absent and the defense-in-depth gap is real. Confidence downgraded from high to medium because the 1 MiB body limit substantially mitigates the worst-case payload size."
    },
    {
      "id": "SEC-004",
      "severity": "important",
      "category": "security",
      "subcategory": "idor",
      "title": "GET /projects/:projectId/clients — missing project-scope access check (IDOR)",
      "file": "apps/api/src/clients/clients.service.ts",
      "line": "290-298",
      "code_evidence": "  async listProjectClients(projectId: string) {\n    return this.prisma.projectClient.findMany({\n      where: { projectId },\n      include: {\n        client: true,\n      },\n      orderBy: { createdAt: 'asc' },\n    });\n  }",
      "description": "The listProjectClients method, called by GET /projects/:projectId/clients in ProjectsClientsController, performs no project ownership or membership check before returning clients attached to a project. Any authenticated user holding the 'clients:read' permission can enumerate the client organisations associated with any project, regardless of whether they are a member of that project. The rest of the codebase (documents, tasks, time-tracking) consistently calls accessScope.assertCanAccessProject() before returning project-scoped data; this endpoint skips that pattern entirely.",
      "root_cause": "listProjectClients never calls assertCanAccessProject(), unlike every other project-scoped read endpoint in the codebase.",
      "impact": "Authenticated users with clients:read can enumerate client names (organisation names, active status) attached to any project — including projects they are not members of and that may be confidential.",
      "suggested_fix": "Inject AccessScopeService into ClientsService and call assertCanAccessProject(projectId, currentUser) at the start of listProjectClients. Thread the currentUser from ProjectsClientsController.list() through the service call, matching the pattern used in DocumentsService.create().",
      "acceptance_criteria": [
        "A user without project membership receives 403 on GET /projects/:otherProjectId/clients",
        "A user with projects:manage_any still receives the list (bypass preserved)",
        "Existing members receive the list unchanged"
      ],
      "verification_command": "grep -n 'assertCanAccessProject' apps/api/src/clients/clients.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Pattern gap confirmed: grep of clients.service.ts and the controller (projects-clients.controller.ts) shows zero calls to assertCanAccessProject. DocumentsService.create() at line 57 and ProjectsService/TasksService/TimeTrackingService all enforce this check — absent only here.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 290-298; controller (projects-clients.controller.ts:39-41) passes no currentUser to listProjectClients; grep confirms assertCanAccessProject is absent from the entire clients module."
    },
    {
      "id": "SEC-005",
      "severity": "important",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "CreateCommentDto.content and UpdateCommentDto.content lack @MaxLength — unbounded free-text field",
      "file": "apps/api/src/comments/dto/create-comment.dto.ts",
      "line": "9-11",
      "code_evidence": "  @IsString()\n  @IsNotEmpty()\n  content: string;",
      "description": "The `content` field in both CreateCommentDto and UpdateCommentDto is validated with @IsString() and @IsNotEmpty() but has no @MaxLength constraint. An authenticated user with `comments:create` can POST an arbitrarily large string, consuming DB storage (TEXT column is unlimited in PostgreSQL), and potentially triggering DoS-class conditions in any downstream code that copies or logs the content. The UpdateCommentDto (line 7-9 in update-comment.dto.ts) has the same issue.",
      "root_cause": "Free-text fields that accept arbitrary user input were not capped at definition time.",
      "impact": "Any user with comments:create (down to CONTRIBUTEUR / OBSERVATEUR who both have tasks:read and therefore can reach the comment endpoint) can store very large blobs in the database. If content is ever serialised to memory (e.g. audit log, CSV export) this may cause OOM on small instances.",
      "suggested_fix": "Add `@MaxLength(10000)` (or an appropriate domain limit) to `content` in both CreateCommentDto and UpdateCommentDto. Example: `@MaxLength(10000, { message: 'Comment content cannot exceed 10 000 characters' })`.",
      "acceptance_criteria": [
        "POST /comments with content longer than the limit returns HTTP 400",
        "PATCH /comments/:id with content longer than the limit returns HTTP 400"
      ],
      "verification_command": "curl -s -X POST http://localhost:3000/api/comments -H 'Authorization: Bearer <token>' -H 'Content-Type: application/json' -d '{\"content\":\"'$(python3 -c 'print(\"A\"*20001)')'\" ,\"taskId\":\"<uuid>\"}' | jq .statusCode",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: verbatim code confirmed in create-comment.dto.ts lines 9-11 and update-comment.dto.ts lines 6-8. No @MaxLength present anywhere in either file. 1 MiB body limit in main.ts (SEC-025) provides a coarse cap but a single 1 MiB comment is still excessive. Same pattern exists for CreateTaskDto.description (no @MaxLength), ImportTaskDto.description and .title, CreatePredefinedTaskDto.name/description/color/icon, CreateSubtaskDto.description — all lack @MaxLength. This finding focuses on the comment vector as the most reachable by low-privilege users."
    },
    {
      "id": "SEC-006",
      "severity": "important",
      "category": "security",
      "subcategory": "broken-access-control",
      "title": "epics findAll and milestones findAll return all records regardless of project membership",
      "file": "apps/api/src/epics/epics.service.ts",
      "line": "30-57",
      "code_evidence": "  async findAll(page = 1, limit = 1000, projectId?: string) {\n    const safeLimit = Math.min(limit || 1000, 1000);\n    const skip = (page - 1) * safeLimit;\n    const where = projectId ? { projectId } : {};\n\n    const [data, total] = await Promise.all([\n      this.prisma.epic.findMany({\n        where,",
      "description": "GET /epics and GET /milestones have no access-scope filter. Any authenticated user holding the quasi-universal epics:read / milestones:read permission (all roles down to OBSERVATEUR and CONTRIBUTEUR) can enumerate ALL epics and milestones across ALL projects, including those they are not a member of. The parallel routes GET /projects/:id and individual findOne paths do enforce membership via assertProjectMembership / assertCanAccessProject, but the list endpoints simply have no such filter. The milestones findAll is identical (apps/api/src/milestones/milestones.service.ts lines 53-88).",
      "root_cause": "The findAll methods in epics and milestones services never check project membership or inject a caller-scoped WHERE clause, unlike the projects service which uses AccessScopeService.",
      "impact": "A low-privilege user (OBSERVATEUR, CONTRIBUTEUR) can harvest the names, descriptions, priorities, and related project IDs of every epic and milestone in the system by iterating GET /epics?page=N and GET /milestones?page=N. This leaks internal project planning information to users who were explicitly excluded from those projects.",
      "suggested_fix": "Inject the caller's user ID and role into findAll, then apply the same membership-scoped WHERE clause used in ProjectsService.findAll: join through project.members where userId=callerId unless the caller holds projects:manage_any. Example: `where.project = { members: { some: { userId: callerId } } }` for non-privileged callers.",
      "acceptance_criteria": [
        "GET /epics without projectId as OBSERVATEUR returns only epics whose parent project has the caller as a member",
        "GET /milestones without projectId as CONTRIBUTEUR returns only milestones whose parent project has the caller as a member",
        "ADMIN and RESPONSABLE (with projects:manage_any) still receive all records"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: EpicsModule has no AccessScopeService injected; EpicsService.findAll signature takes no userId parameter; controller passes no currentUser to findAll. Same confirmed for MilestonesService.findAll (lines 53-88). No membership filter anywhere in the list path."
    },
    {
      "id": "SEC-007",
      "severity": "important",
      "category": "security",
      "subcategory": "information-disclosure",
      "title": "Unauthenticated health endpoint reveals per-component infrastructure status (DB/Redis) to any caller",
      "file": "apps/api/src/health/health.service.ts",
      "line": "78-86",
      "code_evidence": "    if (dbStatus === 'down' || redisStatus === 'down') {\n      throw new ServiceUnavailableException({\n        status: 'degraded',\n        db: dbStatus,\n        redis: redisStatus,\n      });\n    }\n\n    return { status: 'ok', db: dbStatus, redis: redisStatus };",
      "description": "The /health endpoint is decorated @Public() and returns per-component status: { status, db, redis } in every response. On 200, it confirms both DB and Redis are reachable. On 503, it reveals exactly which dependency (db vs redis) is down. This endpoint is not rate-limited beyond the global defaults (30/s per IP) and requires no authentication. An attacker can infer infrastructure topology (what backing services are present) and monitor for partial outages in real time, which aids targeted timing attacks during degraded states.",
      "root_cause": "HealthStatus interface exposes named component fields ('db', 'redis') directly in the HTTP body returned to any caller, with no authentication requirement.",
      "impact": "Persistent attacker reconnaissance: confirms the tech stack uses PostgreSQL and Redis, monitors per-component availability without authentication. During a DB outage, attacker learns the DB is down (not Redis) — informing timing of follow-up attack vectors that exploit degraded state.",
      "suggested_fix": "Return only { status: 'ok' | 'degraded' } to unauthenticated callers. Log per-component detail server-side only, or gate the granular payload behind a @Roles([ADMIN]) authenticated endpoint. Alternatively, return a minimal { ok: boolean } and rely on Prometheus metrics for operator drill-down.",
      "acceptance_criteria": [
        "GET /api/health returns at most { status: 'ok' | 'degraded' } to unauthenticated callers — no 'db' or 'redis' fields",
        "Per-component detail is logged server-side and/or exposed on an authenticated /api/health/detail endpoint",
        "E2E smoke test verifies 200 body does not contain 'db' or 'redis' keys"
      ],
      "verification_command": "curl -s http://localhost:4000/api/health | jq 'keys'",
      "related_findings": [],
      "cluster_id": "K",
      "found_by": "security",
      "confidence": "high",
      "notes": "Code evidence verified verbatim in health.service.ts lines 78-86. health.controller.ts confirms @Public() decorator with no auth guard. The comment 'Does NOT expose process.uptime, NODE_ENV, or any internal process info' is accurate for those specific fields but db/redis component names ARE infrastructure detail and are exposed. Finding confirmed as-is.",
      "adversarial_verdict": "CONFIRMED — code evidence verbatim, @Public() confirmed in controller, no guard or filtering mitigates the disclosure."
    },
    {
      "id": "SEC-008",
      "severity": "important",
      "category": "security",
      "subcategory": "input validation / DoS",
      "title": "ImportLeavesDto.leaves array has no upper-bound limit, enabling memory-exhaustion via large payloads",
      "file": "apps/api/src/leaves/dto/import-leaves.dto.ts",
      "line": "61-70",
      "code_evidence": "export class ImportLeavesDto {\n  @ApiProperty({\n    description: 'Liste des congés à importer',\n    type: [ImportLeaveDto],\n  })\n  @IsArray()\n  @ValidateNested({ each: true })\n  @Type(() => ImportLeaveDto)\n  leaves: ImportLeaveDto[];\n}",
      "description": "The `leaves` array in `ImportLeavesDto` has no `@ArrayMaxSize()` constraint. While the global body limit is 1 MiB (main.ts), a 1 MiB JSON array of `ImportLeaveDto` objects can contain several thousand rows. The `importLeaves()` service method then iterates all rows, calling `getHolidayKeySet()` (a DB query) on each row inside a transaction, plus `findValidatorForUser()` for each row. This can translate to thousands of sequential DB queries per import call, causing significant CPU/DB load. The `validateImport` endpoint (dry-run) has the same issue and even loads ALL active users into memory upfront. Both endpoints require only `leaves:create` permission (not a restricted admin-only permission), so any contributor-level user can trigger this.",
      "root_cause": "No `@ArrayMaxSize(N)` decorator on `ImportLeavesDto.leaves`; no per-request DB query budget inside `importLeaves()`.",
      "impact": "Authenticated users with `leaves:create` permission can craft a ~1 MiB JSON payload containing ~5000 leave rows, triggering thousands of DB queries per request. Repeated calls can saturate the database connection pool. The `leaves:create` permission is available to broad roles (CONTRIBUTEUR and above).",
      "suggested_fix": "Add `@ArrayMaxSize(500)` (or a configurable limit) to `ImportLeavesDto.leaves`. Consider batching `getHolidayKeySet` calls across the entire date range rather than per row, and caching `findValidatorForUser` results per userId within the batch.",
      "acceptance_criteria": [
        "POST /leaves/import with `leaves` containing 501 items returns HTTP 400.",
        "POST /leaves/import/validate with 501 items returns HTTP 400."
      ],
      "verification_command": "N/A — manual verification: POST /leaves/import with payload {\"leaves\": [... 501 items ...]}, expect 400.",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "The body limit of 1 MiB (SEC-025) mitigates extreme cases but does not prevent the hundreds-of-rows scenario. Adversarial review confirmed: import-leaves.dto.ts lines 61-70 verbatim, no @ArrayMaxSize present. No compensating per-request row limit found elsewhere in the controller or service."
    },
    {
      "id": "SEC-009",
      "severity": "important",
      "category": "security",
      "subcategory": "authorization / IDOR",
      "title": "canValidate() delegation fallback grants approve rights across the entire org, bypassing service-scope",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "1638-1650",
      "code_evidence": "    // Vérifier les délégations actives\n    const today = new Date();\n    const activeDelegation =\n      await this.prisma.leaveValidationDelegate.findFirst({\n        where: {\n          delegateId: validatorId,\n          isActive: true,\n          startDate: { lte: today },\n          endDate: { gte: today },\n        },\n      });\n\n    return activeDelegation !== null;",
      "description": "The final branch of `canValidate()` checks whether the caller is an active delegate of ANY delegator in the system, without scoping the check to the leave's owner, department, or service perimeter. A user delegated by Manager A (who covers Dept A) can therefore approve a pending leave belonging to any user in any other department. This directly contradicts the intention of delegations (which are supposed to cover the delegating manager's own perimeter) and the scope logic enforced everywhere else in the service (`getServiceIds`, `getManagedUserIds`). The `findValidatorForUser()` helper correctly scopes delegation lookups to `delegatorId: managerId` (the leave owner's department manager), but `canValidate()` does not.",
      "root_cause": "The delegation lookup at the end of `canValidate()` matches any active delegation where the caller is `delegateId`, with no filter on `delegatorId` nor any perimeter check linking the delegator to the leave's owner.",
      "impact": "Any user who has been delegated by even one manager can approve (or reject) leave requests from any other employee in the organization, regardless of service or department perimeter. Affects confidentiality (a delegate sees and can act on leaves outside their scope) and integrity (unauthorized leave approvals).",
      "suggested_fix": "After checking the delegator's service perimeter, further restrict the delegation lookup: fetch the leave's `validatorId` (the assigned manager), then query for a delegation scoped to that delegatorId: `{ delegateId: validatorId, delegatorId: leave.validatorId, isActive: true, startDate: { lte: today }, endDate: { gte: today } }`. If `leave.validatorId` is null, fall back to the department manager lookup as in `findValidatorForUser`.",
      "acceptance_criteria": [
        "A delegate of Manager A cannot call POST /leaves/:id/approve on a leave whose `validatorId` belongs to Manager B (expect 403).",
        "A delegate of Manager A CAN approve a leave whose `validatorId` is Manager A (expect 200).",
        "Existing unit test for `canValidate` covers cross-department delegation boundary."
      ],
      "verification_command": "N/A — manual verification: create two managers M1, M2 each with subordinates, create delegation for M1->D, then have D try to approve a leave belonging to M2's subordinate.",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "The comment at line 712 acknowledges the correct scope ('COR-005: scope the delegation lookup to the user's own department manager') for findValidatorForUser, but canValidate does not apply the same discipline. Adversarial review confirmed: the approve() method at line 1704 calls canValidate(), and the delegation check at lines 1638-1650 has no delegatorId filter. findValidatorForUser() at lines 712-735 correctly scopes to `delegatorId: managerId` — the asymmetry is real and exploitable."
    },
    {
      "id": "SEC-010",
      "severity": "important",
      "category": "security",
      "subcategory": "Swagger Basic Auth / credentials parsing",
      "title": "Swagger Basic Auth: passwords containing ':' are silently truncated, enabling credential bypass with a shorter guessable suffix",
      "file": "apps/api/src/main.ts",
      "line": "198-200",
      "code_evidence": "            const [user, pass] = Buffer.from(auth.slice(6), 'base64')\n              .toString()\n              .split(':');",
      "description": "The Basic Auth credential parser uses `.split(':')` without a limit argument. If the configured SWAGGER_PASS contains a colon (e.g. `p@ss:word`), destructuring assignment `const [user, pass]` will receive only `p@ss` as `pass`, silently discarding the `word` suffix. The safeEqual comparison then compares the truncated string against the full configured password — which will fail — locking out the legitimate user. Conversely, an attacker who knows the password up to the first colon can log in with `p@ss` as the password. The HTTP Basic Auth RFC 7617 specifies that only the FIRST colon is a separator; the rest of the decoded string is the password.",
      "root_cause": "`.split(':')` without a count/limit parameter splits on ALL colons; the RFC-correct approach is to split on the FIRST colon only.",
      "impact": "If SWAGGER_PASS contains a colon, the effective password is only the substring before the first colon. An attacker knowing the password structure can authenticate with a shorter value. Conversely, a legitimately chosen strong password containing ':' silently becomes weaker.",
      "suggested_fix": "Use a regex or indexOf to split only on the first colon:\n```typescript\nconst colonIndex = decoded.indexOf(':');\nconst user = decoded.slice(0, colonIndex);\nconst pass = decoded.slice(colonIndex + 1);\n```\nFull fix:\n```typescript\nconst decoded = Buffer.from(auth.slice(6), 'base64').toString();\nconst sep = decoded.indexOf(':');\nconst user = sep >= 0 ? decoded.slice(0, sep) : decoded;\nconst pass = sep >= 0 ? decoded.slice(sep + 1) : '';\n```",
      "acceptance_criteria": [
        "Swagger Basic Auth correctly authenticates when SWAGGER_PASS='p@ss:word' and the request uses password 'p@ss:word'",
        "Swagger Basic Auth rejects requests using only 'p@ss' as password when SWAGGER_PASS='p@ss:word'",
        "Unit test covers colon-in-password scenario"
      ],
      "verification_command": "N/A — manual verification: inspect main.ts line 198-200",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: main.ts lines 198-200 code evidence matches verbatim. The `.split(':')` without limit is confirmed. safeEqual() in main.ts (lines 34-38) uses timingSafeEqual correctly but operates on an already-truncated `pass` value when the password contains colons. The finding correctly characterizes the RFC 7617 violation. Low probability in practice (passwords containing ':' are unusual) but the parser is structurally incorrect."
    },
    {
      "id": "SEC-011",
      "severity": "important",
      "category": "security",
      "subcategory": "timing-attack",
      "title": "Metrics METRICS_TOKEN comparison uses non-constant-time string equality",
      "file": "apps/api/src/metrics/metrics.controller.ts",
      "line": "33",
      "code_evidence": "    const token = process.env['METRICS_TOKEN'];\n    if (token) {\n      const expected = `Bearer ${token}`;\n      if (!authorization || authorization !== expected) {\n        throw new UnauthorizedException('Invalid or missing METRICS_TOKEN');\n      }\n    }",
      "description": "The METRICS_TOKEN check on line 33 uses the JavaScript `!==` string operator, which short-circuits on the first differing character and is therefore vulnerable to timing side-channels. An attacker with a high-resolution clock or many samples could statistically determine the token value one character at a time. main.ts already defines a `safeEqual` helper using `crypto.timingSafeEqual`, but it is not imported or used here.",
      "root_cause": "Direct string comparison (`!==`) instead of a constant-time comparison function such as `crypto.timingSafeEqual`.",
      "impact": "A determined attacker with access to the metrics endpoint can exploit differential timing to brute-force the METRICS_TOKEN, gaining unauthenticated read access to internal Prometheus metrics (request counts, routes, latencies — useful for profiling the API internals).",
      "suggested_fix": "Import `timingSafeEqual` from `crypto` (or re-export `safeEqual` from main.ts into a utility), then replace the `authorization !== expected` check with `timingSafeEqual(Buffer.from(authorization), Buffer.from(expected))` after verifying both buffers have the same length.",
      "acceptance_criteria": [
        "Token comparison uses crypto.timingSafeEqual or equivalent constant-time function",
        "A buffer-length mismatch (different-length token) is handled before calling timingSafeEqual to avoid the 'buffers must be equal length' exception"
      ],
      "verification_command": "grep -n 'authorization' apps/api/src/metrics/metrics.controller.ts",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "safeEqual() is already implemented in main.ts line 34-38 using timingSafeEqual — it just needs to be extracted to a shared utility and used here. Adversarial re-check: code_evidence verified verbatim at lines 30-36 of metrics.controller.ts. No other guard or middleware provides constant-time comparison for this endpoint. The safeEqual helper in main.ts is a local function, not exported, confirming the gap is real."
    },
    {
      "id": "SEC-012",
      "severity": "important",
      "category": "security",
      "subcategory": "prometheus-label-injection",
      "title": "Prometheus metric labels built from unsanitized req.path — label injection possible",
      "file": "apps/api/src/metrics/metrics.service.ts",
      "line": "37",
      "code_evidence": "  recordRequest(\n    method: string,\n    route: string,\n    status: number,\n    durationMs: number,\n  ): void {\n    const labels = `method=\"${method}\",route=\"${route}\",status=\"${status}\"`;\n",
      "description": "The Prometheus text-format label string on line 37 is assembled by direct template interpolation of `route` (which equals `req.path` from the interceptor, line 34 of metrics.interceptor.ts). The `req.path` value comes from the raw request URL path and is not sanitized. A request whose path contains `\"` or `}` will corrupt the label grammar, e.g. GET /api/test\"} injected{x=\"1 produces the line `http_requests_total{method=\"GET\",route=\"/api/test\"} injected{x=\"1\",status=\"200\"} 1`. This is exploitable for Prometheus scraper confusion and could, in edge scenarios, overwrite metric values in Prometheus.",
      "root_cause": "Route label value is taken verbatim from `req.path` and interpolated into the Prometheus text format without escaping double quotes or other special characters.",
      "impact": "An attacker can craft requests whose path corrupts the `/api/metrics` output, confusing Prometheus scrapers. At minimum this causes Prometheus parse errors; in sophisticated scenarios it can inject arbitrary label key-value pairs into metric exposition.",
      "suggested_fix": "Escape double-quote characters in all label values before building the label string, or use an established Prometheus client library (prom-client) that handles escaping automatically. At minimum: `route.replace(/\\\\/g, '\\\\\\\\').replace(/\"/g, '\\\\\"').replace(/\\n/g, '\\\\n')`.",
      "acceptance_criteria": [
        "A request path containing '\"' or '}' does not break Prometheus text format output",
        "Unit test for renderMetrics with a label containing special characters passes"
      ],
      "verification_command": "grep -n 'labels\\|route\\|method' apps/api/src/metrics/metrics.service.ts | head -15",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial re-check: code_evidence verified verbatim at line 37 of metrics.service.ts. The interceptor (metrics.interceptor.ts line 34) confirms route = req.path with no sanitization. The renderMetrics() method (lines 66-80) interpolates the raw labels string directly into the output. No escaping or normalization layer exists anywhere in the pipeline."
    },
    {
      "id": "SEC-013",
      "severity": "important",
      "category": "security",
      "subcategory": "broken-access-control",
      "title": "POST /milestones/:id/complete lacks project-membership check",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "162-168",
      "code_evidence": "  async complete(id: string) {\n    await this.findOne(id);\n    return this.prisma.milestone.update({\n      where: { id },\n      data: { status: MilestoneStatus.COMPLETED },\n    });\n  }",
      "description": "The `complete` endpoint marks any milestone as COMPLETED without verifying the caller is a member of the milestone's parent project. The controller handler (milestones.controller.ts:116) passes only the milestone ID to milestonesService.complete; no currentUserId or currentUserRole is passed, and the service performs no membership assertion. By contrast, the sibling methods update() and remove() call assertProjectMembership. Any user with the milestones:update permission (quasi-universal: all roles except OBSERVATEUR) can complete a milestone in any project.",
      "root_cause": "The complete() service method was added or refactored without applying the same assertProjectMembership guard that update() and remove() use.",
      "impact": "Any user with milestones:update can mark milestones they have no business accessing as completed, corrupting project progress tracking across the organization.",
      "suggested_fix": "Update the controller to pass currentUserId and currentUserRole to milestonesService.complete, and add the assertProjectMembership call at the start of that method, mirroring update() and remove().",
      "acceptance_criteria": [
        "A user who is not a member of the parent project receives 403 when calling POST /milestones/:id/complete",
        "A member of the project (or a user with projects:manage_any) can complete the milestone"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "COR-057"
      ],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: controller line 116 confirmed: `complete(@Param('id', ParseUUIDPipe) id: string)` — no @CurrentUser. Service complete() signature is `async complete(id: string)` — no user params. update() and remove() both accept currentUserId/currentUserRole and call assertProjectMembership. Gap is real."
    },
    {
      "id": "SEC-014",
      "severity": "important",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "BulkAssignmentDto.userIds and BulkAssignmentDto.dates have no @ArrayMaxSize — cartesian product DoS",
      "file": "apps/api/src/predefined-tasks/dto/bulk-assignment.dto.ts",
      "line": "22-39",
      "code_evidence": "  @IsArray()\n  @ArrayMinSize(1)\n  @IsUUID('all', { each: true })\n  userIds: string[];\n\n  @IsArray()\n  @ArrayMinSize(1)\n  @IsDateString({}, { each: true })\n  dates: string[];",
      "description": "The `createBulkAssignment` endpoint accepts arbitrary-length `userIds` and `dates` arrays with only a minimum size of 1. The service iterates `userIds × dates` (cartesian product) with sequential Prisma `create` calls inside the loop. A caller with `predefined_tasks:assign` can submit 1000 users × 365 dates = 365 000 DB insert attempts in one request. The same issue affects `CreateBulkRecurringRulesDto.userIds` and `daysOfWeek`.",
      "root_cause": "Bulk operation DTOs were not bounded with @ArrayMaxSize on either dimension.",
      "impact": "A privileged user with `predefined_tasks:assign` can trigger tens of thousands of sequential DB writes per request, causing response timeouts and potential DB connection pool exhaustion.",
      "suggested_fix": "Add `@ArrayMaxSize(100)` to `userIds` and `@ArrayMaxSize(365)` to `dates` in `BulkAssignmentDto`. Apply equivalent limits to `CreateBulkRecurringRulesDto.userIds` and `.daysOfWeek`. Enforce the same in the service as a defence-in-depth check.",
      "acceptance_criteria": [
        "POST /predefined-tasks/assignments/bulk with 101 userIds returns HTTP 400",
        "POST /predefined-tasks/recurring-rules/bulk with 101 userIds returns HTTP 400"
      ],
      "verification_command": "N/A — manual verification: craft a body with large userIds/dates arrays, confirm 400 is returned after the fix",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: verbatim code confirmed at bulk-assignment.dto.ts lines 26-39. @ArrayMinSize(1) present, no @ArrayMaxSize on either field. Finding confirmed. The daysOfWeek array in CreateBulkRecurringRulesDto is bounded at 0-6 per element but not in count; a validated array of 7 elements is the maximum semantic payload and is fine, but the userIds array is the main risk vector here."
    },
    {
      "id": "SEC-015",
      "severity": "important",
      "category": "security",
      "subcategory": "input-validation",
      "title": "GET /projects/:id/snapshots uses @Param('id') without ParseUUIDPipe",
      "file": "apps/api/src/projects/projects.controller.ts",
      "line": "183-196",
      "code_evidence": "  @Get(':id/snapshots')\n  @RequirePermissions('reports:view')\n  @ApiOperation({ summary: 'Get progress snapshots for a project' })\n  async getSnapshots(\n    @Param('id') id: string,\n    @CurrentUser() currentUser: AuthenticatedUser,\n    @Query('from') from?: string,\n    @Query('to') to?: string,\n  ) {",
      "description": "Every other route in ProjectsController uses `@Param('id', ParseUUIDPipe)` to validate and sanitize the resource ID. The getSnapshots handler at line 187 uses `@Param('id')` with no pipe. The raw string is then forwarded to projectsService.getSnapshots and used in a Prisma findMany `where: { projectId }`. While Prisma parameterizes values (no SQL injection), an attacker can probe for non-404 responses with arbitrary strings, and the missing UUID validation allows bypass of route-level input contract.",
      "root_cause": "ParseUUIDPipe was accidentally omitted from the @Param('id') decorator on the getSnapshots handler.",
      "impact": "Malformed / non-UUID project IDs are accepted and forwarded to the database layer. Combined with an invalid UUID, PostgreSQL will return 0 rows (uuid_generate_v4 cast failure is caught by Prisma and returns empty array), but the pattern breaks the uniform input validation contract and creates a discrepancy between documented and actual behaviour.",
      "suggested_fix": "Change `@Param('id')` to `@Param('id', ParseUUIDPipe)` on line 187, mirroring all other handlers in the same controller.",
      "acceptance_criteria": [
        "GET /projects/not-a-uuid/snapshots returns 400 Bad Request",
        "GET /projects/<valid-uuid>/snapshots continues to return 200 with results"
      ],
      "verification_command": "grep -n 'getSnapshots\\|ParseUUIDPipe' apps/api/src/projects/projects.controller.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: controller lines 183-196 verbatim confirmed. @Param('id') with no pipe present. All sibling handlers verified to use @Param('id', ParseUUIDPipe). Missing pipe is a real inconsistency."
    },
    {
      "id": "SEC-016",
      "severity": "important",
      "category": "security",
      "subcategory": "input-validation",
      "title": "Snapshot date filter params (from/to) are not validated and passed directly to new Date()",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1244-1249",
      "code_evidence": "    const where: any = { projectId };\n    if (from || to) {\n      where.date = {};\n      if (from) where.date.gte = new Date(from);\n      if (to) where.date.lte = new Date(to);\n    }",
      "description": "The `from` and `to` query parameters for GET /projects/:id/snapshots are accepted as raw strings in the controller and forwarded to the service without any validation. The service passes them directly to `new Date()`. If an invalid date string is supplied (e.g., `from=not-a-date`), `new Date('not-a-date')` produces an Invalid Date object. Prisma will propagate this as an invalid timestamp to PostgreSQL, likely resulting in a 500 Internal Server Error rather than a 400 Bad Request, leaking stack trace details.",
      "root_cause": "No @IsDateString validation is applied to the from/to query parameters before they reach the service.",
      "impact": "Supplying invalid date strings triggers unhandled Invalid Date propagation which may result in a 500 response that leaks server internals. An attacker can probe the error response for framework and query structure details.",
      "suggested_fix": "Add a DTO class with `@IsDateString() @IsOptional() from?: string` and `@IsDateString() @IsOptional() to?: string` and apply `@Query() query: SnapshotFilterDto` in the controller. Alternatively add explicit isNaN checks in the service before using the date values.",
      "acceptance_criteria": [
        "GET /projects/:id/snapshots?from=not-a-date returns 400 Bad Request",
        "GET /projects/:id/snapshots?from=2025-01-01&to=2026-01-01 continues to work correctly"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: verified_clean note mentions milestones.service.ts:314-320 isNaN check, but that is in the validateImport preview path — NOT in getSnapshots. The projects.service.ts getSnapshots path at lines 1244-1249 has NO isNaN guard, no try/catch, no DTO validation before new Date(). Finding stands."
    },
    {
      "id": "SEC-017",
      "severity": "important",
      "category": "security",
      "subcategory": "broken-access-control",
      "title": "hardDelete lacks defense-in-depth ownership assertion present in all sibling mutation methods",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "833-886",
      "code_evidence": "  async hardDelete(id: string, user: ProjectMutationUser) {\n    const project = await this.prisma.project.findUnique({\n      where: { id },\n    });\n\n    if (!project) {\n      throw new NotFoundException('Projet introuvable');\n    }\n\n    const { canDelete, dependencies } = await this.checkProjectDependencies(id);",
      "description": "All other project mutation methods (update, remove, archive, unarchive, updateMember, removeMember) open with an `await this.assertProjectOwnershipOrBypass(id, user)` call as defense-in-depth. The hardDelete method is the sole exception — it relies exclusively on the OwnershipGuard decorator in the controller. If the guard is bypassed (e.g., a future refactor adds an internal call path, or the guard registration order changes), the service layer provides no backstop. This is an inconsistency in a system that has explicitly documented (see comment BUG-04 / BUG-08 SEC-06) the intentional double-enforcement pattern.",
      "root_cause": "The assertProjectOwnershipOrBypass call was omitted when hardDelete was written, breaking the project-wide defense-in-depth contract.",
      "impact": "If the OwnershipGuard is bypassed (internal service-to-service call, guard ordering bug, test harness), an unprivileged user could permanently delete a project without ownership verification.",
      "suggested_fix": "Add `await this.assertProjectOwnershipOrBypass(id, user);` as the first line of hardDelete(), before the findUnique call, mirroring the pattern in remove() and archive().",
      "acceptance_criteria": [
        "hardDelete throws ForbiddenException when called with a user who is neither owner nor holder of projects:manage_any, even if the OwnershipGuard is bypassed",
        "Existing unit tests for hardDelete still pass"
      ],
      "verification_command": "grep -n 'assertProjectOwnershipOrBypass' apps/api/src/projects/projects.service.ts",
      "related_findings": [
        "COR-025"
      ],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: grep confirms assertProjectOwnershipOrBypass called at lines 555, 670, 693, 730, 970, 1015 (update, remove, archive, unarchive, updateMember, removeMember). hardDelete at line 833 has NO such call — confirmed omission. @OwnershipCheck decorator IS present on the controller, so the controller-level guard exists, but the service-layer backstop is absent unlike all siblings."
    },
    {
      "id": "SEC-018",
      "severity": "important",
      "category": "security",
      "subcategory": "input-validation / DoS",
      "title": "ImportSkillsDto.skills array has no @ArrayMaxSize — sequential DB writes enable per-request DoS",
      "file": "apps/api/src/skills/dto/import-skills.dto.ts",
      "line": "46-55",
      "code_evidence": "export class ImportSkillsDto {\n  @ApiProperty({\n    description: 'Liste des compétences à importer',\n    type: [ImportSkillDto],\n  })\n  @IsArray()\n  @ValidateNested({ each: true })\n  @Type(() => ImportSkillDto)\n  skills: ImportSkillDto[];\n}",
      "description": "The `skills` array in `ImportSkillsDto` is validated with `@IsArray()` and `@ValidateNested()` but has no `@ArrayMaxSize()` constraint. The global body limit is 1 MiB. A minimal valid skill entry (`{\"name\":\"a\",\"category\":\"TECHNICAL\"}`) is ~35 bytes, allowing approximately 30,000 entries per request. `importSkills()` in `skills.service.ts` processes each item in a sequential `for` loop, performing one `prisma.skill.create()` call per entry (plus an initial `prisma.skill.findMany()`). `validateImport()` similarly iterates with per-item logic. Both endpoints require `skills:create` permission (legitimate users only), but any user holding that role can trigger thousands of DB writes per HTTP request.",
      "root_cause": "No `@ArrayMaxSize()` decorator on `ImportSkillsDto.skills`, combined with a sequential per-item DB write loop in the service, means a single large payload causes unbounded database load.",
      "impact": "An authenticated user with `skills:create` permission can submit a 1 MiB array of ~30,000 skills and cause thousands of sequential Prisma `create` calls per request, potentially saturating the database connection pool and degrading service for all other users.",
      "suggested_fix": "Add `@ArrayMaxSize(500)` (or a similarly small bound) from `class-validator` to the `skills` field in `ImportSkillsDto`. Also convert the sequential loop in `importSkills()` to use `prisma.skill.createMany()` or a batched `$transaction` for efficiency.",
      "acceptance_criteria": [
        "POST /api/skills/import with skills array > 500 items returns HTTP 400",
        "POST /api/skills/import/validate with skills array > 500 items returns HTTP 400",
        "POST /api/skills/import with exactly 500 items succeeds (if authorized)"
      ],
      "verification_command": "curl -s -X POST http://localhost:4000/api/skills/import -H 'Authorization: Bearer <token>' -H 'Content-Type: application/json' -d '{\"skills\":[{\"name\":\"s\",\"category\":\"TECHNICAL\"}]}' | jq .  # then repeat with 501 items and expect 400",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Both POST /skills/import and POST /skills/import/validate are affected. Verified: ImportSkillsDto at lines 46-54 has no @ArrayMaxSize. importSkills() service method (skills.service.ts lines 693-730) uses a sequential for loop with prisma.skill.create() per item — no batching, no size guard."
    },
    {
      "id": "SEC-019",
      "severity": "important",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "CreateTaskDto.tags field has no @IsArray / @IsString({ each }) / @MaxLength validators — arbitrary data injection",
      "file": "apps/api/src/tasks/dto/create-task.dto.ts",
      "line": "223-224",
      "code_evidence": "  @IsOptional()\n  tags?: string[];",
      "description": "The `tags` field is declared as `string[]` but the only decorator is `@IsOptional()`. The global ValidationPipe runs with `whitelist:true` + `forbidNonWhitelisted:true` + `transform:true`, so the field is whitelisted. However, without `@IsArray()` + `@IsString({ each: true })` + `@MaxLength(N, { each: true })` + `@ArrayMaxSize(N)`, a caller can submit an array of arbitrary objects, non-string elements, or extremely large arrays/strings. Prisma will store whatever TypeScript receives after transformation, which may include arbitrary nested data depending on how `tags` is typed in the Prisma schema.",
      "root_cause": "The `tags` field was added to the DTO without completing the class-validator decorator set required for array-of-strings validation.",
      "impact": "Authenticated users with `tasks:create` can inject non-string elements or very large tag arrays into the database. If tags are rendered in the UI without escaping, this is a stored XSS surface.",
      "suggested_fix": "Add the full decorator chain:\n```ts\n@IsOptional()\n@IsArray()\n@ArrayMaxSize(20)\n@IsString({ each: true })\n@MaxLength(100, { each: true })\ntags?: string[];\n```",
      "acceptance_criteria": [
        "POST /tasks with tags: [1, 2, 3] (non-strings) returns HTTP 400",
        "POST /tasks with tags: [{evil: true}] returns HTTP 400",
        "POST /tasks with tags containing 21 elements returns HTTP 400"
      ],
      "verification_command": "curl -s -X POST http://localhost:3000/api/tasks -H 'Authorization: Bearer <token>' -H 'Content-Type: application/json' -d '{\"title\":\"t\",\"tags\":[1,2,3]}' | jq .statusCode",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Adversarial review: code evidence verbatim confirmed. HOWEVER: `tags` has no corresponding column in the Prisma Task model (schema.prisma lines 313-357 confirmed). The `...taskData` spread in tasks.service.ts:225 would include `tags`, but Prisma 6 would either throw a type error at compile time or silently ignore the unknown field at runtime. The stored XSS impact claim is therefore invalid as-is — data cannot be persisted. The finding is downgraded from high to medium confidence: the missing validators are a real gap that will matter the moment a `tags String[]` column is added to the schema, and passing non-string array elements (e.g., objects) could cause runtime errors or unexpected behavior before reaching Prisma. The DTO validation gap is confirmed; only the XSS persistence impact is refuted."
    },
    {
      "id": "SEC-020",
      "severity": "important",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "ImportTasksDto.tasks has no @ArrayMaxSize — unbounded bulk import enabling DoS",
      "file": "apps/api/src/tasks/dto/import-tasks.dto.ts",
      "line": "78-87",
      "code_evidence": "export class ImportTasksDto {\n  @ApiProperty({\n    description: 'Liste des tâches à importer',\n    type: [ImportTaskDto],\n  })\n  @IsArray()\n  @ValidateNested({ each: true })\n  @Type(() => ImportTaskDto)\n  tasks: ImportTaskDto[];",
      "description": "The `tasks` array in `ImportTasksDto` has `@IsArray()` and `@ValidateNested()` but no `@ArrayMaxSize()`. The `importTasks` and `validateImport` service methods iterate over every element in a `for` loop with individual Prisma calls inside each iteration (including a `findFirst` duplicate check, `task.create`, and N × `subtask.create` per task). A caller with `tasks:create` can submit thousands of tasks in a single request. This will (a) consume CPU for the nested validation loop, (b) issue potentially thousands of sequential DB round-trips holding a long-lived transaction window, and (c) exhaust the body-size limit only if `rawBody` caps are low (which they may not be).",
      "root_cause": "The DTO array was not bounded with @ArrayMaxSize, and the service has no limit check on tasks.length.",
      "impact": "An authenticated user with `tasks:create` can trigger a slow-loris-style DB overload by submitting a single POST with thousands of task objects. Depending on server and DB configuration, this can degrade service for all users.",
      "suggested_fix": "Add `@ArrayMaxSize(500)` (or a lower business-appropriate limit) to `ImportTasksDto.tasks`. Also add a guard in the service:\n```ts\nif (tasks.length > 500) throw new BadRequestException('Import limited to 500 tasks per request');\n```\nAnd apply `@MaxLength()` to `ImportTaskDto.title`, `description`, `subtasks`.",
      "acceptance_criteria": [
        "POST /tasks/project/:id/import with more than 500 tasks returns HTTP 400",
        "POST /tasks/project/:id/import/validate with more than 500 tasks returns HTTP 400"
      ],
      "verification_command": "N/A — manual verification: craft a body with 1001 task objects and POST to the import endpoint, verify 400 is returned",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: verbatim code confirmed at import-tasks.dto.ts lines 78-87. The 1 MiB bodyLimit in main.ts (SEC-025) caps byte size but not item count — a 1000-element array of short tasks easily fits under 1 MiB. No @ArrayMaxSize found anywhere in the file. Finding confirmed."
    },
    {
      "id": "SEC-021",
      "severity": "important",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "DELETE /tasks/:taskId/raci/:userId/:role — :role path param is unvalidated (no ParseEnumPipe)",
      "file": "apps/api/src/tasks/tasks.controller.ts",
      "line": "407-429",
      "code_evidence": "  @Delete(':taskId/raci/:userId/:role')\n  @RequirePermissions('tasks:update')\n  ...\n  removeRACI(\n    @Param('taskId', ParseUUIDPipe) taskId: string,\n    @Param('userId', ParseUUIDPipe) userId: string,\n    @Param('role') role: RACIRole,",
      "description": "The `:role` path parameter is typed as `RACIRole` in TypeScript but extracted with a plain `@Param('role')` — no `ParseEnumPipe(RACIRole)` is applied. The global ValidationPipe does not validate path parameters; it only operates on `@Body()`. TypeScript types are erased at runtime. As a result, any arbitrary string will be accepted and passed as `role` to `removeRACI()`. In `removeRACI()` the value flows directly into a Prisma `findUnique` where clause: `taskId_userId_role: { taskId, userId, role }`. Prisma will not throw on an unknown enum value at the query level — it will simply return null and the service will throw NotFoundException. However, the lack of enum validation at the boundary is a defence-in-depth gap that could be exploited if Prisma behaviour changes or if an attacker probes for information via differential responses.",
      "root_cause": "NestJS @Param decorators do not apply the global ValidationPipe; enum validation must be applied explicitly via ParseEnumPipe.",
      "impact": "Calling DELETE with a fabricated role string (e.g. 'ADMIN') will not find a matching RACI row and returns 404 — leaking that the combination does not exist. Deeper impact depends on future schema changes. Currently a low-exploitation bug but a concrete validation gap.",
      "suggested_fix": "Use `new ParseEnumPipe(RACIRole)` on the param:\n```ts\n@Param('role', new ParseEnumPipe(RACIRole)) role: RACIRole,\n```",
      "acceptance_criteria": [
        "DELETE /tasks/:id/raci/:userId/INVALID_ROLE returns HTTP 400 (not 404)",
        "DELETE /tasks/:id/raci/:userId/RESPONSIBLE returns HTTP 200 or 404 (valid flow)"
      ],
      "verification_command": "curl -s -X DELETE http://localhost:3000/api/tasks/<taskId>/raci/<userId>/INVALID_ROLE -H 'Authorization: Bearer <token>' | jq .statusCode",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: verbatim code confirmed at tasks.controller.ts lines 407-429. The `@Param('role') role: RACIRole` pattern is real — no ParseEnumPipe present. Prisma's DB-level enum constraint (PostgreSQL native enum) does provide a backend safety net: an invalid enum string causes a Prisma P2023 error which NestJS maps to 500, not silent acceptance. However, the missing client-side validation means the error surface is 500 (internal) instead of 400 (bad request), which is still incorrect behavior and information-leaking in server logs."
    },
    {
      "id": "SEC-022",
      "severity": "important",
      "category": "security",
      "subcategory": "idor",
      "title": "reorderSubtasks does not verify that subtaskIds belong to the taskId — cross-task IDOR",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "2039-2061",
      "code_evidence": "    await this.prisma.$transaction(\n      subtaskIds.map((id, index) =>\n        this.prisma.subtask.update({\n          where: { id },\n          data: { position: index },\n        }),\n      ),\n    );",
      "description": "The `reorderSubtasks` method verifies that the current user can read `taskId` (via `assertCanReadTask`), then issues a Prisma transaction that updates the `position` of each subtask ID in `subtaskIds` by ID only — without filtering `where: { id, taskId }`. An attacker who knows the UUID of a subtask belonging to a task they do not own can include that UUID in the `subtaskIds` array and silently reorder/mutate the `position` field of arbitrary subtasks across the application.",
      "root_cause": "The WHERE clause in the subtask update only uses `{ id }` instead of `{ id, taskId }`, failing to scope updates to the parent task.",
      "impact": "Any authenticated user with `tasks:update` on at least one task can silently alter the `position` field of any subtask in the database, including those belonging to tasks they cannot read. While position is a cosmetic ordering field, this represents a clear IDOR that undermines the object-ownership model.",
      "suggested_fix": "Add `taskId` to each where clause:\n```ts\nawait this.prisma.$transaction(\n  subtaskIds.map((id, index) =>\n    this.prisma.subtask.update({\n      where: { id, taskId },   // scope to parent task\n      data: { position: index },\n    }),\n  ),\n);\n```\nAlso add a pre-check that all provided subtask IDs belong to the given taskId before starting the transaction.",
      "acceptance_criteria": [
        "Attempting to reorder subtasks of another task returns HTTP 403 or the update is silently ignored (no position change on foreign subtask)",
        "Unit test confirms that subtask.update is called with `{ id, taskId }` composite key"
      ],
      "verification_command": "N/A — manual verification: create two tasks each with one subtask, then POST /tasks/:id1/subtasks/reorder with subtaskId from task2 and verify that subtask2.position changes",
      "related_findings": [
        "COR-029"
      ],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: code evidence verbatim confirmed at tasks.service.ts lines 2051-2058. The assertCanReadTask guard on line 2045 only checks the caller's access to the parent taskId, not ownership of each subtask ID. No additional guard found in the controller (tasks.controller.ts:596-608) or the ReorderSubtasksDto. IDOR is real and exploitable."
    },
    {
      "id": "SEC-023",
      "severity": "important",
      "category": "security",
      "subcategory": "input-validation",
      "title": "userId in CreateTeleworkDto and CreateRecurringRuleDto uses @IsString() instead of @IsUUID()",
      "file": "apps/api/src/telework/dto/create-telework.dto.ts",
      "line": "23-25",
      "code_evidence": "  @IsOptional()\n  @IsString()\n  userId?: string;",
      "description": "The userId field in CreateTeleworkDto (and identically in CreateRecurringRuleDto) is annotated with @IsString() instead of @IsUUID(). Although the field is optional and the service performs a downstream Prisma lookup that will 404 if the ID does not exist, a non-UUID string accepted by the pipeline will cause Prisma to throw an internal database error (invalid UUID format) rather than a clean NestJS NotFoundException, potentially leaking implementation details. More importantly, the intent of the field is clearly a UUID primary key; @IsString() is under-constrained. All other ID fields in the same codebase (e.g., taskId, projectId in CreateTimeEntryDto) correctly use @IsUUID().",
      "root_cause": "Copy-paste pattern chose @IsString() instead of @IsUUID() for optional userId body fields in telework DTOs.",
      "impact": "A caller can supply any string (e.g., SQL-like string, overly long string) as userId. Prisma will send a malformed UUID to PostgreSQL, producing a 500 with a Prisma error message rather than a clean 400/404; the stack trace may be visible in development/permissive logging.",
      "suggested_fix": "Replace @IsString() with @IsUUID() on the userId field in both CreateTeleworkDto and CreateRecurringRuleDto. Also add @IsUUID() to the same field in UpdateRecurringRuleDto (inherited via PartialType but explicit annotation is cleaner):\n```\n@IsOptional()\n@IsUUID()\nuserId?: string;\n```",
      "acceptance_criteria": [
        "POST /api/telework with body { date: '2026-06-01', userId: 'not-a-uuid' } returns HTTP 400 (class-validator error), not HTTP 500.",
        "POST /api/telework/recurring-rules with body { dayOfWeek: 1, startDate: '2026-06-01', userId: 'not-a-uuid' } returns HTTP 400."
      ],
      "verification_command": "curl -s -X POST http://localhost:4000/api/telework -H 'Authorization: Bearer <admin_token>' -H 'Content-Type: application/json' -d '{\"date\":\"2026-06-01\",\"userId\":\"not-a-uuid\"}' | jq '.statusCode'",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verbatim code verified at create-telework.dto.ts lines 23-25 and create-recurring-rule.dto.ts lines 19-21. No upstream UUID coercion, pipe, or interceptor compensates for the missing @IsUUID(). taskId and projectId in CreateTimeEntryDto correctly use @IsUUID() confirming this is an inconsistency. Same pattern applies to CreateRecurringRuleDto (apps/api/src/telework/dto/create-recurring-rule.dto.ts lines 19-21). UpdateRecurringRuleDto inherits via PartialType so is implicitly affected."
    },
    {
      "id": "SEC-024",
      "severity": "important",
      "category": "security",
      "subcategory": "DoS / query-amplification",
      "title": "Unbounded date range in GET /telework triggers O(days × rules) sequential DB queries with no upper bound",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "180-182",
      "code_evidence": "    // Auto-expand recurring rules into individual schedules for the requested range\n    if (startDate && endDate) {\n      await this.expandRecurringRulesForRange(startDate, endDate, userId);\n    }",
      "description": "The findAll service method calls expandRecurringRulesForRange whenever both startDate and endDate query params are provided. That private method iterates over every calendar day in the range for every active recurring rule, issuing up to two sequential Prisma queries per matching day (findUnique + create). There is no upper bound on the date range accepted from the user-supplied query params. A user with telework:read (any authenticated user who has permission to list telework) can pass startDate=1970-01-01&endDate=2099-12-31, causing ~47,450 loop iterations per rule. With, e.g., 10 active rules each covering one day per week, this translates to ~7,000 sequential DB queries per request, effectively DoS-ing the database connection pool for the duration of the call.",
      "root_cause": "expandRecurringRulesForRange is called with unvalidated user-supplied date strings and performs N×M sequential DB I/O with no range cap or batching.",
      "impact": "Any user with telework:read permission (minimum CONTRIBUTEUR role) can exhaust the Prisma connection pool and stall all API requests for the duration of the query, causing a denial of service.",
      "suggested_fix": "1. Add a maximum range cap in the controller or service (e.g., 366 days): reject or clamp if endDate - startDate > 366 days.\n2. Alternatively, move the recurring-rule expansion to a background job / cron and remove the inline call from findAll.\n3. Short-term: add @IsDateString() validation on the query params and validate that endDate >= startDate and the range <= 366 days in the service before calling expandRecurringRulesForRange.",
      "acceptance_criteria": [
        "GET /api/telework?startDate=1970-01-01&endDate=2099-12-31 returns HTTP 400 with a message about the date range.",
        "GET /api/telework?startDate=2026-01-01&endDate=2026-12-31 (366 days) completes in under 2 seconds regardless of the number of active recurring rules."
      ],
      "verification_command": "time curl -s 'http://localhost:4000/api/telework?startDate=2020-01-01&endDate=2026-12-31' -H 'Authorization: Bearer <user_token>' > /dev/null",
      "related_findings": [],
      "cluster_id": "D",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verbatim code verified at telework.service.ts lines 179-182 (comment on 179, if-block on 180-182). No range cap exists anywhere in the service, controller, or global pipes — startDate/endDate are raw @Query() strings with no DTO class-validator constraints. The expandRecurringRulesForRange private method (lines 292-362) confirms the O(days × rules) sequential DB I/O pattern with findUnique + create per matching day. The same issue exists in generateSchedulesFromRules (lines 855-942), triggered by POST /telework/recurring-rules/generate. findForPlanningOverview (line 246) uses internal callers only, lower risk. The global ValidationPipe (whitelist: true, forbidNonWhitelisted: true) cannot constrain query params not bound to a DTO class."
    },
    {
      "id": "SEC-025",
      "severity": "important",
      "category": "security",
      "subcategory": "idor",
      "title": "GET /tasks/:taskId/third-party-assignees — missing task-scope access check (IDOR)",
      "file": "apps/api/src/third-parties/third-parties.service.ts",
      "line": "310-321",
      "code_evidence": "  async listTaskAssignees(taskId: string) {\n    return this.prisma.taskThirdPartyAssignee.findMany({\n      where: { taskId },\n      include: {\n        thirdParty: true,\n        assignedBy: {\n          select: { id: true, firstName: true, lastName: true },\n        },\n      },\n      orderBy: { createdAt: 'asc' },\n    });\n  }",
      "description": "listTaskAssignees, called by GET /tasks/:taskId/third-party-assignees in TasksThirdPartyAssigneesController, returns all third parties assigned to any task without checking task or project access for the requesting user. Any user with third_parties:read can enumerate third-party assignees for any task, including tasks in confidential projects.",
      "root_cause": "listTaskAssignees never calls assertCanReadTask() or assertCanAccessProject(), unlike the tasks module which consistently enforces scope.",
      "impact": "Disclosure of contractor/partner identities on tasks the caller cannot read, potentially leaking confidential project personnel information.",
      "suggested_fix": "Call accessScope.assertCanReadTask(taskId, currentUser) at the top of listTaskAssignees. Inject AccessScopeService and thread currentUser from the controller.",
      "acceptance_criteria": [
        "A non-member receives 403 on GET /tasks/:confidentialTaskId/third-party-assignees",
        "Assigned task members receive the list unchanged",
        "Users with tasks:manage_any bypass the check"
      ],
      "verification_command": "grep -n 'assertCanReadTask\\|assertCanAccessProject' apps/api/src/third-parties/third-parties.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Confidential tasks (confidential: true) are specifically protected in AccessScopeService.taskReadWhere; this gap bypasses that protection.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 310-321; controller (tasks-third-party-assignees.controller.ts:32-33) passes no currentUser to listTaskAssignees; grep confirms assertCanReadTask is absent from third-parties.service.ts while present in tasks.service.ts, comments.service.ts, and time-tracking.service.ts."
    },
    {
      "id": "SEC-026",
      "severity": "important",
      "category": "security",
      "subcategory": "idor",
      "title": "GET /projects/:projectId/third-party-members — missing project-scope access check (IDOR)",
      "file": "apps/api/src/third-parties/third-parties.service.ts",
      "line": "323-334",
      "code_evidence": "  async listProjectMembers(projectId: string) {\n    return this.prisma.projectThirdPartyMember.findMany({\n      where: { projectId },\n      include: {\n        thirdParty: true,\n        assignedBy: {\n          select: { id: true, firstName: true, lastName: true },\n        },\n      },\n      orderBy: { createdAt: 'asc' },\n    });\n  }",
      "description": "listProjectMembers, called by GET /projects/:projectId/third-party-members in ProjectsThirdPartyMembersController, returns all third-party organisations (contractors, legal entities) attached to any project without checking project membership. A user with third_parties:read can iterate any projectId to discover which external partners are involved in projects they have no business seeing.",
      "root_cause": "listProjectMembers never calls assertCanAccessProject(), unlike task, document, and time-tracking equivalents.",
      "impact": "Disclosure of sensitive contractor/partner relationships to users outside the project, potentially revealing procurement or contractual information.",
      "suggested_fix": "Inject AccessScopeService into ThirdPartiesService and call assertCanAccessProject(projectId, currentUser) at the start of listProjectMembers. Pass currentUser from the controller.",
      "acceptance_criteria": [
        "Non-member with third_parties:read receives 403 on GET /projects/:otherProjectId/third-party-members",
        "Project members receive the list unchanged",
        "Users with projects:manage_any bypass the check"
      ],
      "verification_command": "grep -n 'assertCanAccessProject' apps/api/src/third-parties/third-parties.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Same pattern gap as security-S4-1 in the sibling module.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 323-334; controller (projects-third-party-members.controller.ts:32-33) passes no currentUser to listProjectMembers; grep confirms assertCanAccessProject is absent from the entire third-parties service for these list methods."
    },
    {
      "id": "SEC-027",
      "severity": "important",
      "category": "security",
      "subcategory": "access-control",
      "title": "POST /users/:id/reset-password uses 'users:manage_roles' permission — semantically wrong and may grant unintended reset capability to role managers",
      "file": "apps/api/src/users/users.controller.ts",
      "line": "430-431",
      "code_evidence": "  @Post(':id/reset-password')\n  @RequirePermissions('users:manage_roles')\n  @HttpCode(HttpStatus.OK)\n  @ApiOperation({\n    summary:\n      \"Réinitialiser le mot de passe d'un utilisateur (Admin/Responsable)\",",
      "description": "The admin password-reset endpoint at POST /users/:id/reset-password requires the permission 'users:manage_roles', not 'users:reset_password'. The complementary token-generation endpoint at POST /auth/reset-password-token uses 'users:reset_password'. This means that any role template carrying 'users:manage_roles' (role administration) can directly reset any subordinate user's password, even if the operator intended role administration and password reset to be separate privileges. Conversely, a role template that holds 'users:reset_password' but not 'users:manage_roles' cannot use this direct-reset endpoint, even though it can use the token-generation path. The inconsistency means the password-reset access surface is wider than explicitly designed: role managers gain implicit password-reset authority.",
      "root_cause": "The direct-reset endpoint was wired to 'users:manage_roles' instead of the purpose-built 'users:reset_password' permission, possibly because it was added alongside role-management features.",
      "impact": "Any role bound to a template that includes 'users:manage_roles' can directly reset subordinate users' passwords. If the operator assigns 'users:manage_roles' to a role they intend as pure role-administration (not credential management), they silently gain password-reset authority over subordinates. This widens the blast radius of a compromised account with role-management access.",
      "suggested_fix": "Change line 431 to @RequirePermissions('users:reset_password'). Verify that all templates intended to perform admin password resets carry 'users:reset_password'. If both are needed, add @RequireAnyPermission('users:manage_roles', 'users:reset_password') with a comment explaining the dual need.",
      "acceptance_criteria": [
        "A caller with only 'users:manage_roles' (and not 'users:reset_password') gets 403 on POST /users/:id/reset-password.",
        "A caller with 'users:reset_password' gets 200 on POST /users/:id/reset-password (provided hierarchy check passes).",
        "E2E: RBAC matrix confirms the reset-password endpoint is not reachable by REFERENT_TECHNIQUE roles that only hold manage_roles."
      ],
      "verification_command": "grep -rn \"users:reset_password\\|users:manage_roles\" apps/api/src/users/users.controller.ts apps/api/src/auth/auth.controller.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: users.controller.ts line 431 confirmed `@RequirePermissions('users:manage_roles')`. auth.controller.ts line 314 uses `@RequirePermissions('users:reset_password')` for the token-path — the mismatch is real. Both endpoints still enforce hierarchy in the service layer via roleHierarchy.assertCanAssignRole, so there is no privilege escalation, but the permission surface inconsistency is confirmed."
    },
    {
      "id": "SEC-028",
      "severity": "important",
      "category": "security",
      "subcategory": "session-invalidation",
      "title": "Admin password reset (POST /users/:id/reset-password) does not bump JWT nbf, leaving existing access tokens valid for up to 15 min",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1089-1159",
      "code_evidence": "    await this.refreshTokenService.revokeAllForUser(userId);\n\n    // OBS-004 — durable admin-reset event. Renamed from the SEC-003 free-string\n    // 'PASSWORD_RESET_ADMIN' to the AuditAction enum value (advances OBS-024's\n    // enum-vs-free-string unification). The console-parity auditService.log\n    // below still emits PASSWORD_CHANGED — that dual sink is OBS-024 territory.\n    await this.auditPersistence.log({",
      "description": "UsersService.resetPassword (the admin path, reached via POST /users/:id/reset-password) calls refreshTokenService.revokeAllForUser to revoke all refresh tokens, but does NOT call jwtNotBefore.bumpUser. As a result, any live access token belonging to the target user continues to be valid until its natural TTL expires (default 15 minutes). The complementary path — AuthService.resetPassword (reached via POST /auth/reset-password with a token) — correctly calls BOTH revokeAllForUser AND jwtNotBefore.bumpUser (auth.service.ts line 585). The SEC-019 rationale is already documented in the codebase: 'without this bump a stolen access token stays valid until it expires (≤15 min) after the victim resets.'",
      "root_cause": "jwtNotBefore.bumpUser was added to AuthService.resetPassword (SEC-019) but the equivalent admin path in UsersService.resetPassword was not updated.",
      "impact": "After an admin force-resets a user's password (e.g. in response to credential compromise), the target user's existing access tokens remain usable for up to 15 minutes. An attacker who has stolen an access token can continue to act as that user despite the forced reset, until token TTL expires.",
      "suggested_fix": "Add `await this.jwtNotBefore.bumpUser(userId);` immediately after `await this.refreshTokenService.revokeAllForUser(userId);` in UsersService.resetPassword (around line 1130). JwtNotBeforeService is already injected in UsersService (line 62) so no additional wiring is needed.",
      "acceptance_criteria": [
        "After POST /users/:id/reset-password, a previously-issued access token for the target user is rejected by JwtStrategy.validate on the next request.",
        "The Redis key jwt:nbf:<userId> exists with a value equal to approximately now+1 (seconds) and TTL <= JWT_ACCESS_TTL + 60s.",
        "Integration test: issue access token, admin-reset password, verify 401 on the old token within 1 second."
      ],
      "verification_command": "grep -n 'bumpUser' apps/api/src/users/users.service.ts",
      "related_findings": [
        "COR-039",
        "COR-007"
      ],
      "cluster_id": "N",
      "found_by": "security",
      "confidence": "high",
      "notes": "jwtNotBefore is already injected (constructor line 62, field declared at line 63). The fix is one line. Adversarial check confirms: bumpUser appears at lines 781 and 826 (for deactivation/removal paths) but NOT in the resetPassword body (lines 1089-1159). AuthService.resetPassword at line 585 correctly calls bumpUser — the omission in the admin path is confirmed."
    },
    {
      "id": "SEC-029",
      "severity": "important",
      "category": "security",
      "subcategory": "client_side_auth",
      "title": "Leave cancellation request button gated by client-side userId comparison instead of API canRequestCancel flag",
      "file": "apps/web/app/[locale]/leaves/page.tsx",
      "line": "776-786",
      "code_evidence": "            {!showValidationActions &&\n              leave.status === LeaveStatus.APPROVED &&\n              leave.userId === user?.id && (\n                <button\n                  onClick={() => handleRequestCancel(leave.id)}\n                  className=\"p-2 text-orange-600 hover:bg-orange-50 rounded-lg transition\"\n                  title=\"Demander l'annulation\"\n                >\n                  ↩️\n                </button>\n              )}",
      "description": "The 'request cancellation' button is shown when `leave.status === LeaveStatus.APPROVED && leave.userId === user?.id`. This is a client-side ownership check using the locally stored `user.id` from the auth store (JWT token). Per the CLAUDE.md memory rule, auth decisions must use API-computed flags (canEdit/canDelete per resource). While the API itself should enforce the authorization, the button relies on the frontend correctly reading `user.id` from the JWT, which could be manipulated by a XSS attacker or lead to display inconsistency if a manager is viewing someone else's leave in the all-leaves tab (where `leave.userId !== user.id` may incorrectly hide or show the button). The `canEdit` and `canDelete` flags are correctly API-sourced (lines 757, 767) — this one slip is inconsistent with the established pattern.",
      "root_cause": "The requestCancel affordance check was implemented with a direct client-side `userId === user.id` comparison rather than an API-provided `canRequestCancel` flag on the leave object.",
      "impact": "Minor inconsistency with the established API-flag pattern. A manager viewing another user's approved leave in the all-leaves tab (showUser=true, showValidationActions=false) will NOT see the button due to userId mismatch — which is correct by accident. The risk is forward: if the all-leaves view ever shows canRequestCancel for delegates, this logic would break. The API enforces the rule, so no actual unauthorized action is possible.",
      "suggested_fix": "Add a `canRequestCancel` boolean flag to the Leave API response (similar to `canEdit`/`canDelete`). Use that flag: `(leave as Leave & { canRequestCancel?: boolean }).canRequestCancel && leave.status === LeaveStatus.APPROVED`.",
      "acceptance_criteria": [
        "Backend returns canRequestCancel: boolean in each Leave object.",
        "Frontend renders the cancel-request button solely based on the API flag.",
        "No client-side userId comparison remains for authorization decisions."
      ],
      "verification_command": "grep -n 'leave.userId === user' apps/web/app/\\[locale\\]/leaves/page.tsx",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Code confirmed verbatim at lines 776-786. The API enforces the actual permission, so this is primarily an architectural inconsistency rather than an exploitable vulnerability. Severity is 'important' rather than 'blocking'."
    },
    {
      "id": "SEC-030",
      "severity": "important",
      "category": "security",
      "subcategory": "auth decisions computed client-side",
      "title": "SuiviPage: access control (checkAccess) computed client-side from Zustand store data — no server-side enforcement of the scope filter",
      "file": "apps/web/app/[locale]/users/[id]/suivi/page.tsx",
      "line": "95-111, 168-170, 181-183",
      "code_evidence": "  const checkAccess = (targetUser: User): boolean => {\n    if (!currentUser) return false;\n    if (currentUser.id === userId) return true;\n    if (hasPermission(\"users:manage_roles\")) return true;\n    if (hasPermission(\"users:manage\")) {\n      const managedServiceIds = (currentUser.managedServices || []).map(\n        (ms) => ms.id,\n      );\n      const targetServiceIds = (targetUser.userServices || []).map(\n        (us) => us.service?.id,\n      );\n      return targetServiceIds.some(\n        (id) => id && managedServiceIds.includes(id),\n      );\n    }\n    return false;\n  };",
      "description": "The `checkAccess` function runs entirely in the browser, using data from the Zustand auth store (`currentUser.managedServices`). It determines whether the current user may view another user's tasks, leaves, telework, time stats, and skills. If an attacker manipulates the Zustand store (e.g., via browser devtools or by replaying a modified `/auth/me` response), they can bypass the access check and then fetch all downstream data (tasks, leaves, telework, time) from the API — which may or may not enforce the same scope filter server-side. The comment in the code itself acknowledges: 'RBAC V0 — permission-based'. The six parallel API calls on line 191–198 use `userId` directly without the result of `checkAccess` being communicated to the API layer.",
      "root_cause": "Access scoping (managedServices filter) is enforced only in the frontend; the API endpoints are called directly with the target `userId` regardless of whether `checkAccess` returns true or false at the server level.",
      "impact": "If API endpoints `/tasks?assigneeId=...`, `/leaves?userId=...`, `/telework?userId=...`, `/time-tracking/stats?userId=...`, `/skills/users/:id` do not independently enforce managedService scoping, any authenticated user with `users:manage` permission can access any other user's data by calling those endpoints directly (bypassing the UI gate).",
      "suggested_fix": "Verify that each API endpoint accessed in the parallel fetch (tasksService.getByAssignee, leavesService.getAll with userId, teleworkService.getByDateRange with userId, timeTrackingService.getStats with userId, skillsService.getUserSkills with userId, projectsService.getByUser with userId) enforces the managedService scope server-side. If not, add a server-side check in each endpoint. The frontend check is a UX enhancement, not a security control.",
      "acceptance_criteria": [
        "API endpoints enforce managedService scope independently of the frontend check",
        "A user with users:manage but no managedService overlap with target user receives 403 from the API"
      ],
      "verification_command": "N/A — manual verification of API endpoint guards required",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Code evidence verified verbatim. The checkAccess function (lines 95-111) and its use at lines 168-170 are exactly as cited. The parallel API calls at lines 191-198 are confirmed. Confidence remains medium — this is a genuine frontend-only gate pattern but whether it constitutes a vulnerability depends on backend enforcement, which requires cross-referencing the API shard."
    },
    {
      "id": "SEC-031",
      "severity": "important",
      "category": "security",
      "subcategory": "host-header-injection",
      "title": "API proxy forwards `host` header to internal backend (host-header injection)",
      "file": "apps/web/app/api/[...path]/route.ts",
      "line": "4-18",
      "code_evidence": "const HOP_BY_HOP = new Set([\n  \"connection\",\n  \"keep-alive\",\n  \"transfer-encoding\",\n  \"upgrade\",\n  \"proxy-connection\",\n  \"te\",\n  \"trailer\",\n]);\n\nfunction filterHeaders(headers: Headers): Record<string, string> {\n  const out: Record<string, string> = {};\n  headers.forEach((value, key) => {\n    if (!HOP_BY_HOP.has(key.toLowerCase())) {\n      out[key] = value;\n    }\n  });\n  return out;\n}",
      "description": "The `filterHeaders` function only removes hop-by-hop headers. The `host` header (plus `x-forwarded-for`, `x-forwarded-proto`, `x-real-ip`) is forwarded verbatim to the internal API at `http://localhost:4000`. If the NestJS API uses the `host` header to generate absolute URLs (e.g. password-reset links, ICS download URLs, CORS decisions, or email tokens), an attacker who controls the Host header in their request to the Next.js frontend can inject an arbitrary host, causing the API to emit URLs pointing to an attacker-controlled domain.",
      "root_cause": "`host` is not in the HOP_BY_HOP filter set, so it passes through the proxy to the internal API unchanged.",
      "impact": "If the NestJS API uses `request.get('host')` or `req.headers.host` to build absolute URLs (e.g. password-reset links sent by email), an attacker can send `Host: attacker.com` to the Next.js proxy, which forwards it to the API. The API then embeds `attacker.com` in a reset-password email, leading to credential theft. Severity depends on whether the backend uses the host header; at minimum this leaks internal infra details (the internal API receives the public hostname).",
      "suggested_fix": "Add `'host'` (and optionally `'x-forwarded-for'`, `'x-forwarded-host'`, `'x-real-ip'`) to the `HOP_BY_HOP` set, or add an explicit block list. Example: `const BLOCKED = new Set(['host', 'x-forwarded-host', 'x-real-ip']); if (!HOP_BY_HOP.has(key) && !BLOCKED.has(key)) { out[key] = value; }`",
      "acceptance_criteria": [
        "Sending `Host: attacker.com` to `POST /api/auth/login` does NOT forward `host: attacker.com` to the NestJS backend",
        "Existing authenticated requests still work with the correct Bearer token forwarded"
      ],
      "verification_command": "curl -s -X POST http://localhost:3000/api/auth/login -H 'Host: evil.com' -H 'Content-Type: application/json' -d '{\"login\":\"x\",\"password\":\"x\"}' -v 2>&1 | grep -i '< host'",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Code evidence verbatim confirmed. DOWNGRADED from high to medium: adversarial review found that the NestJS API does NOT use the `host` request header anywhere in its source — password-reset URLs are built from `process.env.FRONTEND_URL` (auth.service.ts:540), not from the request host. No `req.headers.host` or equivalent usage found in apps/api/src/. The structural gap (missing `host` in the filter set) is real but its impact is currently limited to leaking the public hostname to the internal API; there is no active exploit path in the current codebase. Remains confirmed because the defensive fix costs nothing and future code could introduce host-header trust. Severity held at 'important' as a defence-in-depth issue."
    },
    {
      "id": "SEC-032",
      "severity": "important",
      "category": "security",
      "subcategory": "Header injection / Proxy header spoofing",
      "title": "nginx forwards client-supplied X-Forwarded-Proto ($http_x_forwarded_proto) instead of $scheme, enabling proto spoofing",
      "file": "nginx/nginx.conf",
      "line": "131",
      "code_evidence": "            proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;",
      "description": "Both the /api and / location blocks forward the X-Forwarded-Proto header using `$http_x_forwarded_proto` — the value of the incoming request's header — rather than `$scheme` (nginx's own connection scheme). If this nginx instance is the edge proxy (or if the upstream host-level proxy doesn't strip the header), a client can send `X-Forwarded-Proto: https` on an HTTP request and the backend will believe the connection is HTTPS. This can bypass HTTPS-required guards, influence redirect generation, and affect cookie Secure flag logic based on the detected protocol.",
      "root_cause": "The variable `$http_x_forwarded_proto` reflects the client-supplied header value without sanitization; `$scheme` would reflect the actual nginx-to-backend transport (and the outer proxy, if real, should set the canonical value before forwarding).",
      "impact": "A client on HTTP can claim to be on HTTPS. Any backend logic gating on `X-Forwarded-Proto` (e.g., cookie Secure enforcement, HTTPS-only redirects) can be bypassed. Combined with the absence of TLS (INFRA-1), this means the API cannot distinguish HTTP from HTTPS at all.",
      "suggested_fix": "Replace `$http_x_forwarded_proto` with `$scheme` in both location blocks:\n```nginx\nproxy_set_header X-Forwarded-Proto $scheme;\n```\nIf there IS a host-level proxy doing TLS termination and setting X-Forwarded-Proto, the inner nginx should OVERWRITE the header with `$scheme` (the value seen by inner nginx), not blindly forward what the outer chain sent — the correct value will be set by the TLS-terminating proxy upstream.",
      "acceptance_criteria": [
        "curl -H 'X-Forwarded-Proto: https' http://orchestr-a.com/api/health does not result in backend receiving X-Forwarded-Proto: https when the actual connection is HTTP",
        "nginx config uses $scheme for X-Forwarded-Proto on both /api and / locations"
      ],
      "verification_command": "N/A — manual verification: grep 'X-Forwarded-Proto' nginx/nginx.conf",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: nginx.conf line 131 (location /api) and line 182 (location /) both use `$http_x_forwarded_proto`. The trust-proxy config (apps/api/src/common/fastify/trust-proxy.config.ts) scopes trust to loopback+uniquelocal only, which correctly prevents IP spoofing from public networks, but does not mitigate the proto-spoofing issue since the client header is forwarded as-is by nginx before reaching Fastify."
    },
    {
      "id": "SEC-033",
      "severity": "important",
      "category": "security",
      "subcategory": "TLS / HSTS",
      "title": "No Strict-Transport-Security header emitted by nginx, web middleware, or API helmet",
      "file": "nginx/nginx.conf",
      "line": "96-106",
      "code_evidence": "        add_header X-Frame-Options \"SAMEORIGIN\" always;\n        add_header X-Content-Type-Options \"nosniff\" always;\n        add_header X-XSS-Protection \"1; mode=block\" always;\n        add_header Referrer-Policy \"strict-origin-when-cross-origin\" always;\n        add_header Permissions-Policy \"camera=(), microphone=(), geolocation=()\" always;",
      "description": "The server block in nginx.conf emits five security headers but omits Strict-Transport-Security (HSTS). The web middleware (apps/web/middleware.ts) emits only CSP. The API helmet configuration in main.ts does not explicitly set HSTS options. Without HSTS, browsers will not upgrade future HTTP requests to HTTPS automatically, leaving users vulnerable to SSL-stripping attacks even after the TLS block is added (finding INFRA-1).",
      "root_cause": "HSTS was not included in the security headers block when the nginx config was authored.",
      "impact": "After adding TLS (INFRA-1 fix), users who first visit via HTTP are still vulnerable to SSL-stripping for the lifetime of that session. Preload list submission is blocked without a valid HSTS header.",
      "suggested_fix": "Add `add_header Strict-Transport-Security 'max-age=63072000; includeSubDomains; preload' always;` to the HTTPS server block in nginx.conf. Do NOT set it on the HTTP-only block (would violate RFC 6797 requirement that HSTS only be sent over TLS). Optionally add it to the helmet configuration in main.ts with `{ strictTransportSecurity: { maxAge: 63072000, includeSubDomains: true, preload: true } }`.",
      "acceptance_criteria": [
        "curl -I https://orchestr-a.com returns Strict-Transport-Security: max-age=63072000; includeSubDomains; preload",
        "HTTP-only responses do not include HSTS header"
      ],
      "verification_command": "curl -sI https://orchestr-a.com | grep -i strict-transport",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: nginx.conf lines 96-106 code evidence matches verbatim. The @fastify/helmet registration in main.ts (lines 135-144) does not include strictTransportSecurity options. apps/web/src/lib/csp.ts buildCsp() only emits CSP directives, no HSTS. No HSTS header anywhere in the shipped codebase."
    },
    {
      "id": "SEC-034",
      "severity": "nit",
      "category": "security",
      "subcategory": "information-disclosure",
      "title": "Stdout audit log uses unkeyed plain SHA256 (8 chars) for attempted-login identifier — rainbow-table reversible for common emails",
      "file": "apps/api/src/audit/audit.service.ts",
      "line": "103-104",
      "code_evidence": "const hashAttemptedLogin = (value: string): string =>\n  createHash('sha256').update(value).digest('hex').slice(0, 8);",
      "description": "For the stdout security log entry, attempted login emails are hashed with plain SHA256 (no HMAC key, no salt). The 8-character hex truncation reduces brute-force space but SHA256 is publicly computable and preimage-computable for any email in a dictionary. Server logs are 'broadly shipped' (per comments) to log aggregators like CloudWatch/ELK. An attacker with read access to logs can rainbow-table-reverse the 8-char hash against a list of known usernames/emails for the organization. Note: the persisted DB value uses a proper HMAC-keyed hash (hashAttemptedSubject) — only the stdout path is affected.",
      "root_cause": "hashAttemptedLogin uses createHash('sha256') without a secret key, making it dictionary-reversible unlike the keyed HMAC used for DB persistence.",
      "impact": "An attacker with read access to server logs (log aggregator breach, misconfigured S3 bucket) can recover attempted login targets for accounts with predictable email patterns, enabling targeted spear-phishing or account enumeration.",
      "suggested_fix": "Use the same HMAC key (AUDIT_HASH_KEY) for the stdout digest as for DB persistence: createHmac('sha256', process.env.AUDIT_HASH_KEY).update(value.trim().toLowerCase()).digest('hex').slice(0, 8). This makes the stdout hash correlation-usable without being dictionary-reversible.",
      "acceptance_criteria": [
        "hashAttemptedLogin is replaced with a keyed HMAC using AUDIT_HASH_KEY",
        "Unit test verifies the stdout digest is not equal to unkeyed sha256 of the same input",
        "AUDIT_HASH_KEY validation at boot (assertAuditHashKey) already covers this — no new boot assertion needed"
      ],
      "verification_command": "N/A — manual verification: inspect audit.service.ts hashAttemptedLogin implementation",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed at lines 103-104. DB persistence path at line 255 correctly uses createHmac. The asymmetry between stdout (unkeyed SHA256) and DB (HMAC) paths is real. Attempts to disprove: no wrapper or key injection surrounds hashAttemptedLogin — it is called directly at line 142 with attemptedEmail. No mitigating guard found.",
      "adversarial_verdict": "CONFIRMED — verbatim code present, asymmetry with DB path verified, no compensating control found."
    },
    {
      "id": "SEC-035",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "QueryClientsDto.search and QueryThirdPartyDto.search missing @MaxLength — unbounded query string parameter",
      "file": "apps/api/src/clients/dto/query-clients.dto.ts",
      "line": "13-16",
      "code_evidence": "  @ApiPropertyOptional({ description: 'Search on name (ilike)' })\n  @IsOptional()\n  @IsString()\n  search?: string;",
      "description": "The search parameter in both QueryClientsDto (clients) and QueryThirdPartyDto (third-parties) has no @MaxLength. The value is passed to Prisma as a LIKE pattern (mode: insensitive), which is parameterized (no injection risk), but an arbitrarily long search string could stress the DB full-scan ILIKE query. The GET body limit does not apply to query string parameters; Fastify/nginx URL limits are the only cap (~8KB by default).",
      "root_cause": "MaxLength omitted from search fields in both query DTOs.",
      "impact": "A very long search string (e.g. 8KB of pattern) causes a PostgreSQL ILIKE on the organizationName/name column with a huge pattern, slightly degrading performance. Low impact given DB size constraints.",
      "suggested_fix": "Add @MaxLength(200) or @MaxLength(255) to the search fields in both QueryClientsDto and QueryThirdPartyDto.",
      "acceptance_criteria": [
        "GET /clients?search=<>200-char-string returns 400",
        "GET /third-parties?search=<>200-char-string returns 400",
        "Normal search queries work unchanged"
      ],
      "verification_command": "grep -n 'search\\|MaxLength' apps/api/src/clients/dto/query-clients.dto.ts apps/api/src/third-parties/dto/query-third-party.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Same pattern in both DTOs. Third-parties has the same omission at line 31-33.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 13-16 (QueryClientsDto) and lines 30-33 (QueryThirdPartyDto); neither DTO imports or uses MaxLength for the search field; the Max import in both files applies only to the limit pagination field."
    },
    {
      "id": "SEC-036",
      "severity": "nit",
      "category": "security",
      "subcategory": "information-disclosure",
      "title": "uploads-auth.hook.ts skips jti blacklist and nbf checks — logged-out tokens remain valid for avatar reads up to access TTL",
      "file": "apps/api/src/common/fastify/uploads-auth.hook.ts",
      "line": "78-83",
      "code_evidence": "    try {\n      verifier.verify(token);\n    } catch {\n      return unauthorized(reply);\n    }\n  };\n}",
      "description": "The uploads auth hook (SEC-016) only checks that the Bearer token's signature is valid and has not expired — it does NOT check the jti blacklist (JwtBlacklistService) or the per-user nbf (JwtNotBeforeService). The comment in the file acknowledges this intentionally: 'a logged-out-but-unexpired token revealing a profile photo for ≤ the access TTL is negligible vs. the anonymous hole'. However, this also means that after a forced password reset or account deactivation (both of which bump nbf via JwtNotBeforeService), the old token can still be used to retrieve avatar images for the remaining access TTL. For a public-sector app handling HR data (org chart avatars could be sensitive), this could be a privacy concern. The mitigating factors are: (1) avatarUrl is already returned to every authenticated user in summaries, so avatars are not confidential; (2) the TTL gap is ≤15 min.",
      "root_cause": "Intentional design decision documented in the hook's comment — the blacklist/nbf checks were omitted as disproportionate for avatar access.",
      "impact": "A logged-out user (via /auth/logout) or a deactivated/reset user can continue to fetch avatar images for up to JWT_ACCESS_TTL after the security event. Low severity because avatars are already visible to all authenticated users in summaries.",
      "suggested_fix": "Accept as intentional, or optionally add a lightweight blacklist check by injecting JwtBlacklistService into the hook factory: `if (payload.jti && await blacklist.isBlacklisted(payload.jti)) return unauthorized(reply);`. This adds one Redis round-trip per avatar fetch but closes the logout-bypass for avatar reads.",
      "acceptance_criteria": [
        "After POST /auth/logout, GET /api/uploads/avatars/<id>.jpg with the old token returns 401."
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: code at lines 78-83 verbatim confirmed — only verifier.verify(token) called, no blacklist/nbf check. Finding is intentional by design (documented in hook comment). Retained as confirmed because: (1) the code evidence is accurate, (2) the gap is real even if intentional, (3) operators need to make an explicit accept/close decision. Classified as nit/informational."
    },
    {
      "id": "SEC-037",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "CreateDocumentDto.description missing @MaxLength — unbounded free-text field",
      "file": "apps/api/src/documents/dto/create-document.dto.ts",
      "line": "53-60",
      "code_evidence": "  @ApiProperty({\n    description: 'Description du document',\n    example: 'Document de spécifications techniques',\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The description field has @IsString() and @IsOptional() but no @MaxLength constraint. A client can supply a very long description string. The 1MB global body limit in main.ts provides a hard ceiling, but best practice (and the existing pattern in this same DTO: name has @MaxLength(255), url has @MaxLength(2048)) is to explicitly cap free-text fields. This also documents intent in the API contract.",
      "root_cause": "MaxLength decorator omitted from an optional free-text field.",
      "impact": "Near-megabyte description strings can be persisted; minor storage waste and potential UI rendering issues on the frontend.",
      "suggested_fix": "Add @MaxLength(2000) or a similar business-appropriate limit to description, consistent with the notes field cap used in ThirdParty.",
      "acceptance_criteria": [
        "POST /documents with description longer than the cap returns 400",
        "POST /documents with a short description is accepted"
      ],
      "verification_command": "grep -n 'MaxLength\\|description' apps/api/src/documents/dto/create-document.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Partially mitigated by the 1MB Fastify body limit.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 53-60; DTO imports MaxLength (line 9) and applies it to name (line 50) and url (line 74) but not description; create-document.dto.ts has no MaxLength decorator on the description field."
    },
    {
      "id": "SEC-038",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "CreateDocumentDto.size missing @IsInt() and @Min(0) — negative or non-integer values accepted",
      "file": "apps/api/src/documents/dto/create-document.dto.ts",
      "line": "88-90",
      "code_evidence": "  @ApiProperty({ description: 'Taille en bytes', example: 2048576 })\n  @IsNotEmpty()\n  size: number;",
      "description": "The size field is declared as number with only @IsNotEmpty(). No @IsInt(), @IsNumber(), or @Min(0) decorator is present. A client can submit size: -1 or size: 0 which will pass DTO validation. The service only rejects values > 200_000_000. The DB column is Int (Prisma), but negative values are valid PostgreSQL integers. The self-reported nature of the field means this is primarily a data-quality issue, but it allows storage of semantically invalid metadata (negative file sizes).",
      "root_cause": "Incomplete validator coverage on the size field in CreateDocumentDto.",
      "impact": "Negative or non-integer size values are persisted to the database, corrupting document metadata. Analytics or reporting based on size aggregation could produce incorrect results.",
      "suggested_fix": "Add @IsInt() @Min(0) to the size field. Also add @Max(MAX_DOCUMENT_SIZE_BYTES) to move the cap from the service layer to the DTO, making the constraint self-documenting.",
      "acceptance_criteria": [
        "POST /documents with size: -1 returns 400",
        "POST /documents with size: 0 is accepted (or rejected if 0-byte files should be disallowed)",
        "POST /documents with size: 999999999999 returns 400 if @Max is added"
      ],
      "verification_command": "grep -n 'IsInt\\|IsNumber\\|@Min\\|@Max' apps/api/src/documents/dto/create-document.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "UpdateDocumentDto inherits this gap via PartialType(CreateDocumentDto). GlobalValidationPipe uses transform:true but NOT enableImplicitConversion, so JSON-native negative numbers pass through as-is. Service-layer check only guards the upper bound (>200_000_000), not negative values.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 88-90; service enforces only upper bound (documents.service.ts:50); ValidationPipe config (main.ts:163-167) does not use enableImplicitConversion; no lower-bound guard exists anywhere in the stack."
    },
    {
      "id": "SEC-039",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "projectId query filter in epics/milestones findAll not validated as UUID",
      "file": "apps/api/src/epics/epics.controller.ts",
      "line": "54",
      "code_evidence": "    @Query('projectId') projectId?: string,\n  ) {\n    return this.epicsService.findAll(page, limit, projectId);\n  }",
      "description": "The projectId query parameter in GET /epics and GET /milestones is accepted as a raw string and passed directly to a Prisma where clause. While Prisma parameterizes the query (no SQL injection), an invalid UUID string will result in a Prisma P2023 error (Invalid value for database column type uuid) that propagates as a 500 rather than a 400. The same issue exists in milestones controller at line 82.",
      "root_cause": "No ParseUUIDPipe or DTO-level @IsUUID validation is applied to the projectId query parameter.",
      "impact": "Supplying a non-UUID projectId causes an unhandled Prisma error resulting in a 500 response instead of a clean 400. Leaks stack trace in non-production environments.",
      "suggested_fix": "Add a custom pipe or a DTO with `@IsUUID() @IsOptional() projectId?: string` and use `@Query() query: EpicFilterDto` in the controller.",
      "acceptance_criteria": [
        "GET /epics?projectId=not-a-uuid returns 400 Bad Request",
        "GET /epics?projectId=<valid-uuid> continues to return filtered results"
      ],
      "verification_command": "grep -n 'projectId' apps/api/src/epics/epics.controller.ts apps/api/src/milestones/milestones.controller.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: epics.controller.ts line 54 confirmed verbatim — @Query('projectId') projectId?: string with no pipe. milestones.controller.ts line 82 same pattern confirmed. No UUID validation on query param."
    },
    {
      "id": "SEC-040",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "CreateEventDto.description has no @MaxLength — unbounded DB write",
      "file": "apps/api/src/events/dto/create-event.dto.ts",
      "line": "36-38",
      "code_evidence": "  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The `description` field has `@IsString()` and `@IsOptional()` but no `@MaxLength`. The global ValidationPipe(whitelist:true, forbidNonWhitelisted:true) does not bound the value length. A caller can POST an arbitrarily large string (limited only by network/body size) which gets inserted into PostgreSQL. The Event.description column in schema.prisma is typed as `String?` with no length constraint. The title field correctly has @MaxLength(200).",
      "root_cause": "Missing @MaxLength decorator on the optional description field in CreateEventDto.",
      "impact": "An authenticated user with events:create permission can insert very large strings into the events table, potentially consuming disk space or causing downstream rendering issues.",
      "suggested_fix": "Add `@MaxLength(5000)` (or an appropriate project-wide limit) to the description field, matching any similar DTO convention in the codebase.",
      "acceptance_criteria": [
        "POST /events with description longer than the configured max returns HTTP 400",
        "POST /events with description within the limit succeeds"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "UpdateEventDto extends PartialType(CreateEventDto) so it inherits the same gap. Adversarial verification: lines 36-38 of create-event.dto.ts confirmed verbatim — @IsString() + @IsOptional() with no @MaxLength. Title has @MaxLength(200) at line 28, confirming the pattern gap is real.",
      "adversarial_verdict": "CONFIRMED — code evidence verbatim present; no compensating guard found elsewhere (checked controller, service, no body-size limit targeting description specifically)"
    },
    {
      "id": "SEC-041",
      "severity": "nit",
      "category": "security",
      "subcategory": "input validation / missing validation",
      "title": "GET /holidays/import-french: year query param parsed via parseInt without range bounds or ParseIntPipe",
      "file": "apps/api/src/holidays/holidays.controller.ts",
      "line": "127-131",
      "code_evidence": "  async importFrenchHolidays(\n    @Query('year') year: string,\n    @CurrentUser() user: User,\n  ) {\n    const targetYear = year ? parseInt(year, 10) : new Date().getFullYear();",
      "description": "The `year` query parameter on `POST /holidays/import-french` is received as a raw string and parsed with `parseInt(year, 10)` in the handler body. Unlike the sibling `GET /holidays/year/:year` route (which uses `ParseIntPipe`), this endpoint applies no NestJS pipe validation. A NaN value (e.g. `year=abc`) silently becomes `NaN`, which causes the Easter calculation and Date.UTC calls in `importFrenchHolidays()` to produce invalid dates (`Invalid Date`). No range check is applied either, so `year=1600` or `year=9999` would generate malformed holiday inserts. The endpoint requires `holidays:create` (admin-level) so the attack surface is limited.",
      "root_cause": "Manual `parseInt` in the handler instead of `ParseIntPipe` with range validation, inconsistent with the `GET /holidays/year/:year` pattern.",
      "impact": "An admin supplying a non-numeric `year` causes silent NaN propagation; `Date.UTC(NaN, ...)` produces `Invalid Date`, likely causing a Prisma write error or inserting a null/invalid date. Low severity given admin-only access.",
      "suggested_fix": "Replace `@Query('year') year: string` with `@Query('year', new ParseIntPipe({ optional: true })) year?: number` and add a range check in the service (e.g., 2000 ≤ year ≤ 2100) consistent with `ImportSchoolVacationDto`.",
      "acceptance_criteria": [
        "POST /holidays/import-french?year=abc returns HTTP 400.",
        "POST /holidays/import-french?year=1600 returns HTTP 400 or is rejected by the service."
      ],
      "verification_command": "N/A — manual verification: POST /holidays/import-french?year=NaN (as admin), observe response.",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "The `GET /holidays/year/:year` endpoint correctly uses ParseIntPipe; this inconsistency in the import endpoint is the gap. Adversarial review confirmed: holidays.controller.ts line 130 verbatim `parseInt(year, 10)` with no pipe on the @Query decorator. holidays.service.ts importFrenchHolidays() has no isNaN guard — `Date.UTC(NaN, 0, 1)` propagates Invalid Date into Prisma upsert calls. Global ValidationPipe does not intercept raw @Query string params without a DTO class. Endpoint is @Post not @Get as the finding notes say, but that does not affect validity."
    },
    {
      "id": "SEC-042",
      "severity": "nit",
      "category": "security",
      "subcategory": "input validation / missing constraint",
      "title": "CreateLeaveDto.reason and ImportLeaveDto.comment lack @MaxLength, allowing oversized free-text storage",
      "file": "apps/api/src/leaves/dto/create-leave.dto.ts",
      "line": "74-81",
      "code_evidence": "  @ApiProperty({\n    description: 'Raison du congé',\n    example: \"Vacances d'été\",\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  reason?: string;",
      "description": "The `reason` field in `CreateLeaveDto` (stored as `comment` in the DB) has no `@MaxLength` annotation. The same issue exists for `comment` in `ImportLeaveDto`. ApproveLeaveDto and RejectLeaveDto correctly enforce `@MaxLength(2000)`. Without a consistent ceiling, a caller can submit a very large `reason` string (up to the global 1 MiB body limit), which will be persisted to the `leaves.comment` column. The DB column may have its own length limit (depends on the Prisma schema type), but no application-level guard surfaces a meaningful error before attempting the write.",
      "root_cause": "Missing `@MaxLength` decorator on `CreateLeaveDto.reason` and `ImportLeaveDto.comment`.",
      "impact": "Large free-text values persist to the DB comment column; if the column is `TEXT` there is no DB-side truncation. Audit trail entries then echo these large strings into the audit payload JSON, inflating audit_logs storage.",
      "suggested_fix": "Add `@MaxLength(2000)` to `CreateLeaveDto.reason` and `ImportLeaveDto.comment`, consistent with `ApproveLeaveDto.comment` and `RejectLeaveDto.reason`.",
      "acceptance_criteria": [
        "POST /leaves with `reason` longer than 2000 chars returns HTTP 400.",
        "POST /leaves/import with a `comment` longer than 2000 chars on any row returns HTTP 400."
      ],
      "verification_command": "N/A — manual verification: POST /leaves with reason=<2001-char string>, expect 400.",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Also applies to ImportLeaveDto.comment (import-leaves.dto.ts line 55-58). Adversarial review confirmed verbatim: create-leave.dto.ts lines 74-81 for `reason`, import-leaves.dto.ts lines 55-58 for `comment`. No @MaxLength found in either. ApproveLeaveDto/RejectLeaveDto do have @MaxLength(2000) — inconsistency confirmed."
    },
    {
      "id": "SEC-043",
      "severity": "nit",
      "category": "security",
      "subcategory": "input validation / missing format check",
      "title": "ImportLeaveDto.userEmail uses @IsString/@IsNotEmpty instead of @IsEmail, and CreateLeaveDto.leaveTypeId / ImportLeaveDto lack @IsUUID",
      "file": "apps/api/src/leaves/dto/import-leaves.dto.ts",
      "line": "13-20",
      "code_evidence": "export class ImportLeaveDto {\n  @ApiProperty({\n    description: \"Email de l'utilisateur\",\n    example: 'user@example.com',\n  })\n  @IsString()\n  @IsNotEmpty()\n  userEmail: string;",
      "description": "The `userEmail` field in `ImportLeaveDto` is validated only as a non-empty string, not as a valid email address (`@IsEmail()`). Similarly, `CreateLeaveDto.leaveTypeId` and `UpsertLeaveBalanceDto.leaveTypeId`/`userId` accept any non-empty string rather than a UUID (`@IsUUID()`). Although the service resolves these by DB lookup (preventing actual injection), the absence of format validation means malformed payloads only fail at the DB round-trip (returning a generic not-found) rather than producing an early, clear 400 validation error. This also masks enumeration timing differences.",
      "root_cause": "Missing `@IsEmail()` on `ImportLeaveDto.userEmail` and `@IsUUID()` on FK-typed string fields in several leave DTOs.",
      "impact": "Malformed email/UUID inputs produce delayed errors (DB lookup) rather than fast early rejection. Minor timing oracle for user enumeration if the lookup timing differs, though Prisma parameterization prevents injection.",
      "suggested_fix": "Add `@IsEmail()` to `ImportLeaveDto.userEmail`. Add `@IsUUID()` to `CreateLeaveDto.leaveTypeId`, `UpsertLeaveBalanceDto.leaveTypeId`, and `UpsertLeaveBalanceDto.userId` (where non-null). Add `@IsUUID()` to `CreateLeaveDto.targetUserId`.",
      "acceptance_criteria": [
        "POST /leaves/import with userEmail='not-an-email' returns HTTP 400.",
        "POST /leaves with leaveTypeId='not-a-uuid' returns HTTP 400."
      ],
      "verification_command": "N/A — manual verification: POST /leaves with leaveTypeId='INJECTION_ATTEMPT', expect 400.",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "CreateLeaveDto.targetUserId is also @IsString without @IsUUID. Adversarial review confirmed: grep of IsUUID/IsEmail across create-leave.dto.ts, import-leaves.dto.ts, upsert-leave-balance.dto.ts returned zero results. All three files confirmed to use only @IsString/@IsNotEmpty for UUID and email fields."
    },
    {
      "id": "SEC-044",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "Milestone bulk import array has no @ArrayMaxSize — DoS via oversized payload",
      "file": "apps/api/src/milestones/dto/import-milestones.dto.ts",
      "line": "31-39",
      "code_evidence": "export class ImportMilestonesDto {\n  @ApiProperty({\n    description: 'Liste des jalons à importer',\n    type: [ImportMilestoneDto],\n  })\n  @IsArray()\n  @ValidateNested({ each: true })\n  @Type(() => ImportMilestoneDto)\n  milestones: ImportMilestoneDto[];",
      "description": "The importMilestones endpoint accepts an array of milestone objects with no upper bound on array size. The service processes them one by one in a for loop (milestones.service.ts:193-235), issuing a separate DB query per item for duplicate detection (findFirst) plus one create per item. A large array (e.g., 10,000 entries) causes a long-running transaction, excessive DB connections, and potential memory exhaustion. The global Fastify body size limit is the only backstop.",
      "root_cause": "No @ArrayMaxSize decorator was applied to the milestones array in ImportMilestonesDto.",
      "impact": "A user with milestones:create permission can trigger a DoS by submitting a large milestones array, causing DB saturation and long-running HTTP requests.",
      "suggested_fix": "Add `@ArrayMaxSize(500)` (or a reasonable limit matching business needs) to the milestones field. Also refactor the import loop to use a single findMany for duplicate detection and a single createMany for inserts.",
      "acceptance_criteria": [
        "POST /milestones/project/:id/import with milestones array length > 500 returns 400 Bad Request",
        "Import with <= 500 entries continues to work"
      ],
      "verification_command": "grep -n 'ArrayMaxSize\\|milestones' apps/api/src/milestones/dto/import-milestones.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: import-milestones.dto.ts lines 31-39 confirmed verbatim. No @ArrayMaxSize decorator present. Only @IsArray, @ValidateNested, @Type decorators on the array field."
    },
    {
      "id": "SEC-045",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "Unvalidated date query strings passed to new Date() in exportIcs — possible HTTP 500 instead of 400",
      "file": "apps/api/src/planning-export/planning-export.service.ts",
      "line": "56-57",
      "code_evidence": "    const dateFilter: Record<string, unknown> = {};\n    if (start) dateFilter['gte'] = new Date(start);\n    if (end) dateFilter['lte'] = new Date(end);",
      "description": "The `start` and `end` query parameters in GET /planning-export/ics are accepted as raw strings from the controller with no @IsISO8601 or @IsDateString validation. They are passed directly to `new Date()`. If the string is not a valid date (e.g. 'foobar'), `new Date('foobar')` produces `Invalid Date`, and passing NaN to Prisma's date filter will likely cause a runtime error (HTTP 500) rather than a clean HTTP 400 Bad Request. The PlanningOverviewQueryDto (planning module) correctly uses @IsISO8601, demonstrating the right pattern. Note: GET /events/range (getEventsByRange) DOES have an isNaN guard at lines 663-665 catching the Invalid Date and returning HTTP 400 — making that endpoint safe. The vulnerability is isolated to exportIcs.",
      "root_cause": "No DTO with @IsDateString/@IsISO8601 validation wrapping the date query parameters in exportIcs; no isNaN guard in service method.",
      "impact": "An authenticated user can trigger HTTP 500 responses by sending malformed date strings to GET /planning-export/ics?start=notadate, leaking internal error information and impacting observability signal quality.",
      "suggested_fix": "Create a typed query DTO for GET /planning-export/ics using @IsISO8601 + @IsOptional on the date parameters, matching the PlanningOverviewQueryDto pattern.",
      "acceptance_criteria": [
        "GET /planning-export/ics?start=notadate returns HTTP 400 with a validation error message"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "events.service.ts:getEventsByRange lines 660-665 has isNaN checks — that endpoint is safe. The original finding's title has been narrowed to exportIcs only. Confidence remains high for that specific path.",
      "adversarial_verdict": "CONFIRMED (scope narrowed) — exportIcs lines 56-57 confirmed verbatim: no isNaN check before Prisma call. getEventsByRange (events.service.ts:663-665) is correctly guarded — that part of the original claim was overstated but noted in the finding's own notes. The vulnerability in exportIcs is real."
    },
    {
      "id": "SEC-046",
      "severity": "nit",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "CreatePredefinedTaskDto free-text fields (name, description, color, icon) lack @MaxLength",
      "file": "apps/api/src/predefined-tasks/dto/create-predefined-task.dto.ts",
      "line": "16-47",
      "code_evidence": "  @IsString()\n  @IsNotEmpty()\n  name: string;\n\n  ...\n  @IsString()\n  @IsOptional()\n  description?: string;\n\n  ...\n  @IsString()\n  @IsOptional()\n  color?: string;\n\n  ...\n  @IsString()\n  @IsOptional()\n  icon?: string;",
      "description": "The `name`, `description`, `color`, and `icon` fields in `CreatePredefinedTaskDto` are validated with @IsString() but have no @MaxLength. The `color` field has a @Matches regex for time fields but not for color itself. `icon` (intended for emoji) is unbounded. A user with `predefined_tasks:create` can store very large strings in these fields.",
      "root_cause": "MaxLength constraints were omitted during initial DTO creation.",
      "impact": "Low severity for privileged-only endpoint (`predefined_tasks:create`), but unbounded DB strings are a correctness and DoS risk.",
      "suggested_fix": "Add: `@MaxLength(200)` to `name`, `@MaxLength(2000)` to `description`, `@MaxLength(20)` to `color`, `@MaxLength(10)` to `icon`.",
      "acceptance_criteria": [
        "POST /predefined-tasks with name > 200 chars returns HTTP 400"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review: verbatim code confirmed at create-predefined-task.dto.ts lines 17-47. The @Matches decorators are for startTime/endTime (HH:mm format), not for color. No @MaxLength on any of name, description, color, or icon. Finding confirmed."
    },
    {
      "id": "SEC-047",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "description fields lack @MaxLength on CreateProjectDto, CreateEpicDto, CreateMilestoneDto",
      "file": "apps/api/src/projects/dto/create-project.dto.ts",
      "line": "28-36",
      "code_evidence": "  @ApiProperty({\n    description: 'Description détaillée du projet',\n    example:\n      \"Refonte complète de l'application de gestion RH avec migration vers une architecture moderne\",\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The `description` field in CreateProjectDto, CreateEpicDto (line 24-27), and CreateMilestoneDto (line 20-23) is decorated with @IsString @IsOptional but has no @MaxLength constraint. An attacker can submit a multi-megabyte string as a description, which will be stored verbatim in the database and returned in API responses. The global body size limit (bodyLimit in main.ts / Fastify config) is the only backstop. The name fields in the same DTOs correctly have @MaxLength. Same issue in ImportMilestoneDto (name, description, dueDate all lack @MaxLength/@IsDateString).",
      "root_cause": "MaxLength constraints were applied to name fields but not to free-text description fields.",
      "impact": "Oversized descriptions may cause excessive memory allocation during serialization and inflate stored data. Low severity given the global body limit, but breaks input contract.",
      "suggested_fix": "Add `@MaxLength(2000)` (or appropriate limit) to description fields in CreateProjectDto, CreateEpicDto, CreateMilestoneDto. For ImportMilestoneDto, add @MaxLength(200) on name, @MaxLength(2000) on description, and change dueDate from @IsString to @IsDateString.",
      "acceptance_criteria": [
        "POST /projects with description.length > 2000 returns 400 Bad Request",
        "POST /milestones/project/:id/import with oversized name returns 400 Bad Request"
      ],
      "verification_command": "grep -n 'MaxLength\\|description' apps/api/src/projects/dto/create-project.dto.ts apps/api/src/epics/dto/create-epic.dto.ts apps/api/src/milestones/dto/create-milestone.dto.ts apps/api/src/milestones/dto/import-milestones.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: create-project.dto.ts lines 28-36 confirmed verbatim. description field has only @IsString and @IsOptional — no @MaxLength. name field at lines 22-26 correctly has @MaxLength(100). No mitigating constraint found."
    },
    {
      "id": "SEC-048",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "managerId and sponsorId in CreateProjectDto use @IsString instead of @IsUUID",
      "file": "apps/api/src/projects/dto/create-project.dto.ts",
      "line": "87-103",
      "code_evidence": "  @ApiProperty({\n    description: 'ID du chef de projet',\n    example: 'uuid-du-manager',\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  managerId?: string;\n\n  @ApiProperty({\n    description: 'ID du sponsor',\n    example: 'uuid-du-sponsor',\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  sponsorId?: string;",
      "description": "managerId and sponsorId are used as UUID foreign keys to the users table, but are validated only as @IsString. Prisma will accept any string and then fail at the DB level with a foreign-key or UUID cast error if a non-UUID is supplied, returning a 500 instead of a 400. The AddMemberDto correctly uses @IsUUID() for userId. Inconsistent validation contract.",
      "root_cause": "Copy/paste inconsistency: userId in AddMemberDto has @IsUUID, but manager/sponsor IDs in CreateProjectDto do not.",
      "impact": "Supplying a non-UUID managerId causes an unhandled Prisma error (P2023 or invalid UUID format) which propagates as a 500 Internal Server Error rather than a clean 400.",
      "suggested_fix": "Replace `@IsString()` with `@IsUUID()` for both managerId and sponsorId in CreateProjectDto.",
      "acceptance_criteria": [
        "POST /projects with managerId='not-a-uuid' returns 400 Bad Request",
        "POST /projects with valid UUID managerId continues to work"
      ],
      "verification_command": "grep -n 'managerId\\|sponsorId\\|IsString\\|IsUUID' apps/api/src/projects/dto/create-project.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: create-project.dto.ts lines 87-103 confirmed verbatim. Both managerId and sponsorId use @IsString() @IsOptional() with no @IsUUID(). No service-level UUID validation found for these fields."
    },
    {
      "id": "SEC-049",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "visibleStatuses in UpdateProjectDto uses @IsString (not @IsEnum) allowing arbitrary status strings",
      "file": "apps/api/src/projects/dto/update-project.dto.ts",
      "line": "18-29",
      "code_evidence": "  @ApiProperty({\n    description:\n      'Statuts de tâches visibles dans la vue Kanban de ce projet (vide = tous visibles)',\n    isArray: true,\n    required: false,\n  })\n  @IsOptional()\n  @IsArray()\n  @IsString({ each: true })\n  visibleStatuses?: string[];",
      "description": "The sibling field hiddenStatuses uses `@IsEnum(TaskStatus, { each: true })` to enforce valid enum values. visibleStatuses uses only `@IsString({ each: true })`, allowing any arbitrary string to be stored. These values are stored as a JSON array in the project row and later used to filter Kanban columns. Injecting unknown status codes could cause front-end rendering errors or bypass intended visibility restrictions.",
      "root_cause": "hiddenStatuses received enum validation but visibleStatuses was left with the weaker @IsString constraint.",
      "impact": "An attacker can store arbitrary strings in visibleStatuses, potentially causing front-end errors when the Kanban board tries to match task statuses against the stored list, or confusing future business logic that reads this field.",
      "suggested_fix": "Replace `@IsString({ each: true })` with `@IsEnum(TaskStatus, { each: true })` for visibleStatuses, mirroring hiddenStatuses.",
      "acceptance_criteria": [
        "PATCH /projects/:id with visibleStatuses=['INVALID_STATUS'] returns 400 Bad Request",
        "PATCH /projects/:id with visibleStatuses=['IN_PROGRESS','TODO'] succeeds"
      ],
      "verification_command": "grep -n 'visibleStatuses\\|hiddenStatuses\\|IsEnum\\|IsString' apps/api/src/projects/dto/update-project.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: update-project.dto.ts lines 14-29 confirmed verbatim. hiddenStatuses uses @IsEnum(TaskStatus, { each: true }); visibleStatuses uses @IsString({ each: true }). The asymmetry is clearly present."
    },
    {
      "id": "SEC-050",
      "severity": "nit",
      "category": "security",
      "subcategory": "unsafe-api-usage",
      "title": "$executeRawUnsafe used with string-interpolated constant for DDL trigger name — unsafe API pattern in maintenance scripts",
      "file": "apps/api/src/scripts/normalize-action-codes.ts",
      "line": "143-145",
      "code_evidence": "      await tx.$executeRawUnsafe(\n        `ALTER TABLE audit_logs DISABLE TRIGGER ${IMMUTABILITY_TRIGGER}`,\n      );",
      "description": "Two maintenance scripts (normalize-action-codes.ts, recompute-chain-on-schema-bump.ts) use $executeRawUnsafe with string interpolation of the IMMUTABILITY_TRIGGER constant ('audit_logs_no_update_delete'). While IMMUTABILITY_TRIGGER is a compile-time string constant — never user-supplied — using $executeRawUnsafe establishes a pattern that would immediately become a SQL injection sink if the trigger name were ever refactored to accept a parameter. Prisma $executeRaw tagged template cannot be used for DDL identifier interpolation (PostgreSQL does not support parameterized identifiers), so the safer long-term fix is format_ident() via a separate helper or a hardcoded non-parameterized string.",
      "root_cause": "Prisma's $executeRaw tagged template cannot parameterize SQL identifiers (table/trigger names); the developer correctly chose $executeRawUnsafe but the string-interpolation pattern is risky if the constant is ever made dynamic.",
      "impact": "No current impact since IMMUTABILITY_TRIGGER is a hardcoded string constant. Risk materializes only if a future refactor passes a caller-controlled trigger name to the function — which would become a full DDL injection.",
      "suggested_fix": "Use a hardcoded string literal directly instead of interpolating the constant: `ALTER TABLE audit_logs DISABLE TRIGGER audit_logs_no_update_delete` — makes it obvious there is no dynamic component. Alternatively, add an allowlist assertion: if (IMMUTABILITY_TRIGGER !== 'audit_logs_no_update_delete') throw new Error('unexpected trigger name').",
      "acceptance_criteria": [
        "DDL string in $executeRawUnsafe does not use string interpolation of any variable",
        "Static analysis (eslint no-restricted-syntax or custom rule) flags $executeRawUnsafe with template literals"
      ],
      "verification_command": "grep -n 'executeRawUnsafe' apps/api/src/scripts/normalize-action-codes.ts apps/api/src/scripts/recompute-chain-on-schema-bump.ts",
      "related_findings": [],
      "cluster_id": "P",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Code evidence verbatim confirmed at lines 143-145 of normalize-action-codes.ts, and at lines 121-123 and 129-131 of recompute-chain-on-schema-bump.ts. IMMUTABILITY_TRIGGER is a module-level const string — no dynamic input path found. Both scripts require DATABASE_MIGRATION_URL, operator-only access. Impact correctly assessed as latent/future risk only. Confidence maintained at medium given compile-time constant safety.",
      "adversarial_verdict": "CONFIRMED at medium confidence — pattern is real and present in both scripts, but no current exploitability given the compile-time constant and operator-only access requirement."
    },
    {
      "id": "SEC-051",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation / missing UUID pipe on query param",
      "title": "departmentId query parameter in GET /services and GET /skills/matrix is not validated as UUID — invalid values cause DB-level errors surfaced as 500",
      "file": "apps/api/src/services/services.controller.ts",
      "line": "59-75",
      "code_evidence": "  findAll(\n    @Query('page', new ParseIntPipe({ optional: true })) page?: number,\n    @Query('limit', new ParseIntPipe({ optional: true })) limit?: number,\n    @Query('departmentId') departmentId?: string,\n  ) {\n    return this.servicesService.findAll(page, limit, departmentId);\n  }",
      "description": "The `departmentId` query parameter in `GET /api/services` (and `GET /api/skills/matrix` in skills.controller.ts line 92) is accepted as a raw string without a `ParseUUIDPipe`. The value is passed directly to Prisma as a `where.departmentId` filter. If the value is not a valid UUID, the PostgreSQL driver raises an `invalid_text_representation` error (PrismaClientKnownRequestError P2023), which propagates as an HTTP 500. The global `AllExceptionsFilter` masks the stack trace in the response, but generates a 500 log entry instead of a clean 400 validation error.",
      "root_cause": "Missing `ParseUUIDPipe` (with `optional: true`) on the `departmentId` query parameter in both controller methods.",
      "impact": "Any authenticated user with `services:read` or `skills:manage_matrix` permission can trigger repeated 500 error log entries by passing a malformed `departmentId`. This is not exploitable for data leakage (the error is masked), but degrades observability and could obscure real errors in logs.",
      "suggested_fix": "Apply `@Query('departmentId', new ParseUUIDPipe({ optional: true })) departmentId?: string` in both `ServicesController.findAll()` and `SkillsController.getMatrix()`. The same pattern is already used for path params (e.g., `@Param('departmentId', ParseUUIDPipe)`).",
      "acceptance_criteria": [
        "GET /api/services?departmentId=not-a-uuid returns HTTP 400 (not 500)",
        "GET /api/skills/matrix?departmentId=not-a-uuid returns HTTP 400 (not 500)",
        "GET /api/services?departmentId=<valid-uuid> continues to work correctly"
      ],
      "verification_command": "curl -s -o /dev/null -w '%{http_code}' 'http://localhost:4000/api/services?departmentId=not-a-uuid' -H 'Authorization: Bearer <token>'  # expect 400, currently returns 500",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verified: services.controller.ts line 72 has bare @Query('departmentId') with no pipe. skills.controller.ts line 92 has bare @Query('departmentId') with no pipe. skills.service.ts line 365 assigns departmentId directly to whereUser.departmentId and passes to Prisma — no service-level UUID validation. The pattern with ParseUUIDPipe is already used for path params in both files (e.g., services.controller.ts line 89, skills.controller.ts line 114)."
    },
    {
      "id": "SEC-052",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "UpdateSettingDto.value and .description lack @MaxLength — unbounded strings stored to DB",
      "file": "apps/api/src/settings/dto/update-setting.dto.ts",
      "line": "4-20",
      "code_evidence": "export class UpdateSettingDto {\n  @ApiProperty({\n    description: 'La valeur du paramètre (JSON stringifié)',\n    example: '\"dd/MM/yyyy\"',\n  })\n  @IsString()\n  @IsNotEmpty()\n  value: string;\n\n  @ApiPropertyOptional({\n    description: 'Description du paramètre',\n    example: \"Format de date utilisé dans l'application\",\n  })\n  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The `value` and `description` fields in UpdateSettingDto have `@IsString()` and `@IsNotEmpty()` but no `@MaxLength()`. An authenticated user with `settings:update` permission (admin-tier) can submit arbitrarily long strings that are persisted to the appSettings table. Although `settings:update` is a privileged permission, defence-in-depth requires bounding free-text inputs at the DTO layer to prevent accidental or deliberate memory/storage exhaustion. The global ValidationPipe bodyLimit of 1 MiB provides a coarse backstop, but no per-field cap exists.",
      "root_cause": "Missing @MaxLength decorator on `value` and `description` fields in UpdateSettingDto.",
      "impact": "A privilege-abusing admin can store very large blobs in appSettings (up to the 1 MiB body limit per request), potentially causing DB bloat and excessive memory use when settings are loaded at module init (initializeDefaultSettings iterates all keys).",
      "suggested_fix": "Add `@MaxLength(10000)` to `value` and `@MaxLength(500)` to `description` in UpdateSettingDto. Adjust limits based on realistic maximum setting value sizes.",
      "acceptance_criteria": [
        "Sending a value longer than 10000 chars to PUT /settings/:key returns 400",
        "Sending a description longer than 500 chars returns 400"
      ],
      "verification_command": "grep -n 'MaxLength\\|IsString\\|IsNotEmpty' apps/api/src/settings/dto/update-setting.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial re-check: code_evidence verified verbatim at lines 4-19 of update-setting.dto.ts. No @MaxLength import exists in the file. The settings service isKnownKey allowlist prevents writing to unknown keys but does NOT bound the size of the value written to known keys. The 1 MiB bodyLimit is the only backstop."
    },
    {
      "id": "SEC-053",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation / missing MaxLength",
      "title": "description fields in CreateSkillDto, CreateDepartmentDto, CreateServiceDto have no @MaxLength",
      "file": "apps/api/src/skills/dto/create-skill.dto.ts",
      "line": "27-35",
      "code_evidence": "  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The optional `description` field in `CreateSkillDto`, `CreateDepartmentDto`, and `CreateServiceDto` has `@IsString()` and `@IsOptional()` but no `@MaxLength()`. Any authenticated user with the appropriate create/update permission can submit a description of up to 1 MiB. While the global body limit (1 MiB) provides an outer bound, there is no DTO-level constraint preventing storage of a very large description string that could degrade rendering or storage.",
      "root_cause": "No `@MaxLength()` decorator was applied to optional free-text description fields in these DTOs.",
      "impact": "Low direct security impact (bounded by 1 MiB body limit and requires authenticated access), but inconsistent with defence-in-depth and could facilitate large data storage per record.",
      "suggested_fix": "Add `@MaxLength(1000)` (or a business-appropriate limit) to `description` in `CreateSkillDto`, `CreateDepartmentDto`, and `CreateServiceDto`.",
      "acceptance_criteria": [
        "POST /api/skills with description > 1000 chars returns HTTP 400",
        "POST /api/departments with description > 1000 chars returns HTTP 400",
        "POST /api/services with description > 1000 chars returns HTTP 400"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verified in all three DTOs: create-skill.dto.ts lines 33-35, create-department.dto.ts lines 29-31, create-service.dto.ts lines 31-32 — all have @IsString() @IsOptional() only on description, no @MaxLength."
    },
    {
      "id": "SEC-054",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation / missing MaxLength",
      "title": "ImportSkillDto.name and .description lack @MaxLength — inconsistent with CreateSkillDto which enforces @MaxLength(100)",
      "file": "apps/api/src/skills/dto/import-skills.dto.ts",
      "line": "15-44",
      "code_evidence": "export class ImportSkillDto {\n  @ApiProperty({\n    description: 'Nom de la compétence',\n    example: 'React',\n  })\n  @IsString()\n  @IsNotEmpty()\n  name: string;\n\n  @ApiProperty({ description: 'Description', required: false })\n  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The `ImportSkillDto` used for bulk import accepts `name` without `@MaxLength` (nor `@MinLength`), and `description` without `@MaxLength`. By contrast, `CreateSkillDto` enforces `@MinLength(2)` and `@MaxLength(100)` on `name`. A caller can embed a `name` of arbitrary length (up to the 1 MiB body limit) in each array element. The service-side validation in `validateImport()` does check `skillData.name.length < 2 || skillData.name.length > 100` and emits an error line — but the DTO itself is the correct place to enforce this, and the `importSkills()` path trusts the data without that same service-level check.",
      "root_cause": "The `ImportSkillDto` was not aligned with `CreateSkillDto` when the bulk import feature was added; length constraints were omitted from the import DTO.",
      "impact": "Very large `name` or `description` values (up to body limit) are accepted by `importSkills()` and written to the database without length validation, potentially storing oversized data that could break downstream display logic. Risk is bounded by the 1 MiB global body limit.",
      "suggested_fix": "Add `@MinLength(2)` and `@MaxLength(100)` to `name`, and `@MaxLength(500)` to `description` in `ImportSkillDto`, matching the constraints in `CreateSkillDto`.",
      "acceptance_criteria": [
        "POST /api/skills/import with a skill name > 100 chars returns 400",
        "POST /api/skills/import/validate with a skill name < 2 chars returns an error in the response preview"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "The service-side validateImport() catches the length issue in the preview path (skills.service.ts lines 597-605), but importSkills() (lines 693-730) does not perform the same check — confirmed by direct reading. The importSkills() path writes to DB with name directly from skillData.name with no length guard."
    },
    {
      "id": "SEC-055",
      "severity": "nit",
      "category": "security",
      "subcategory": "missing-input-validation",
      "title": "ImportTaskDto.assigneeEmail has no @IsEmail — email validation is done by case-insensitive Map lookup instead",
      "file": "apps/api/src/tasks/dto/import-tasks.dto.ts",
      "line": "39-45",
      "code_evidence": "  @ApiProperty({\n    description: \"Email de l'utilisateur assigné\",\n    required: false,\n  })\n  @IsString()\n  @IsOptional()\n  assigneeEmail?: string;",
      "description": "The `assigneeEmail` field in `ImportTaskDto` is typed as a string with @IsString() but no @IsEmail() constraint. The import service resolves assignees by looking up the email in a pre-fetched Map (case-insensitive). While functionally this prevents invalid emails from matching, the lack of a DTO-level @IsEmail() means: (1) any string passes validation, (2) extremely long strings are not capped, and (3) the absence of a format check makes the field semantics opaque.",
      "root_cause": "Email validation was deferred to the service lookup logic rather than enforced at the DTO boundary.",
      "impact": "Low — the service-layer Map lookup gracefully handles non-matching strings. However, @MaxLength is still missing and a very long string will pass the DTO unimpeded.",
      "suggested_fix": "Add `@IsEmail()` and `@MaxLength(254)` to `assigneeEmail` in `ImportTaskDto`.",
      "acceptance_criteria": [
        "POST /tasks/project/:id/import with an assigneeEmail of 'not-an-email' returns HTTP 400"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Adversarial review: verbatim code confirmed at import-tasks.dto.ts lines 39-45. @IsString() only, no @IsEmail(), no @MaxLength. The service-layer lookup provides functional mitigation for email format issues but not for length. Finding confirmed at medium confidence as originally rated."
    },
    {
      "id": "SEC-056",
      "severity": "nit",
      "category": "security",
      "subcategory": "missing-ownership-guard",
      "title": "PATCH/DELETE recurring-rules/:id routes lack @OwnershipCheck decorator (rely solely on service-layer check)",
      "file": "apps/api/src/telework/telework.controller.ts",
      "line": "310-335",
      "code_evidence": "  @Patch('recurring-rules/:id')\n  @RequirePermissions('telework:update')\n  @ApiOperation({ summary: 'Modifier une règle de télétravail récurrent' })\n  @ApiResponse({ status: 200, description: 'Règle mise à jour' })\n  @ApiResponse({ status: 404, description: 'Règle introuvable' })\n  updateRecurringRule(\n    @Param('id', ParseUUIDPipe) id: string,",
      "description": "The PATCH /telework/recurring-rules/:id and DELETE /telework/recurring-rules/:id routes do not carry @OwnershipCheck + OwnershipGuard, unlike the analogous PATCH/:id and DELETE/:id routes for TeleworkSchedule and for TimeEntry (which use @UseGuards(JwtAuthGuard, OwnershipGuard) + @OwnershipCheck). Ownership enforcement for recurring rules is implemented correctly at the service layer (telework.service.ts updateRecurringRule/removeRecurringRule), but there is no declarative guard-level enforcement. This is a defense-in-depth gap: a future refactor could bypass the service check while the lack of guard annotation goes unnoticed.",
      "root_cause": "The recurring-rule sub-resource was added after the ownership guard pattern was established for TeleworkSchedule, and the guard decorator was not applied.",
      "impact": "No current exploit — the service-layer check is correct and will throw ForbiddenException. The gap becomes exploitable only if the service check is later removed or bypassed via an internal caller.",
      "suggested_fix": "Add OwnershipService support for 'teleworkRecurringRule' resource, then annotate both routes:\n```typescript\n@Patch('recurring-rules/:id')\n@UseGuards(JwtAuthGuard, OwnershipGuard)\n@OwnershipCheck({ resource: 'teleworkRecurringRule', bypassPermission: 'telework:manage_any' })\n@RequirePermissions('telework:update')\n```\nOr, at minimum, document why guard-level ownership is intentionally omitted.",
      "acceptance_criteria": [
        "PATCH /api/telework/recurring-rules/<other-user-rule-id> by a non-admin user returns HTTP 403.",
        "DELETE /api/telework/recurring-rules/<other-user-rule-id> by a non-admin user returns HTTP 403."
      ],
      "verification_command": "N/A — manual verification (service-level check already enforces this; guard absence is a defense-in-depth gap)",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verbatim code verified at telework.controller.ts lines 310-335. OwnershipGuard is a global APP_GUARD (common.module.ts lines 17-20) but is opt-in: it returns true immediately (line 44-46 of ownership.guard.ts) when no @OwnershipCheck metadata is present on the route. The PATCH/DELETE recurring-rules/:id routes have no @OwnershipCheck decorator, so the guard provides no protection there. Service-level check at telework.service.ts lines 785-793 (updateRecurringRule) and 836-843 (removeRecurringRule) correctly enforces ownership. The service-level check is correct; this is defense-in-depth only."
    },
    {
      "id": "SEC-057",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "CreateThirdPartyDto.contactEmail missing @MaxLength",
      "file": "apps/api/src/third-parties/dto/create-third-party.dto.ts",
      "line": "36-39",
      "code_evidence": "  @ApiPropertyOptional()\n  @IsOptional()\n  @IsEmail()\n  contactEmail?: string;",
      "description": "contactEmail is validated with @IsEmail() which enforces valid email syntax but imposes no explicit byte-length cap. RFC 5321 limits email addresses to 254 characters; class-validator's @IsEmail() may or may not enforce this internally depending on version. The Prisma schema stores this as String? (TEXT in PostgreSQL) with no DB-level length constraint. Explicit @MaxLength(254) documents intent and adds a defence-in-depth layer.",
      "root_cause": "@MaxLength not added alongside @IsEmail() on the contactEmail field.",
      "impact": "Oversized email strings (>254 chars) may be stored, which cannot be valid email addresses. Downstream display/email-sending code may behave unexpectedly.",
      "suggested_fix": "Add @MaxLength(254) to contactEmail field, consistent with RFC 5321 limits.",
      "acceptance_criteria": [
        "POST /third-parties with contactEmail of 300 characters returns 400",
        "POST /third-parties with a valid email up to 254 chars is accepted"
      ],
      "verification_command": "grep -n 'contactEmail\\|MaxLength\\|IsEmail' apps/api/src/third-parties/dto/create-third-party.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "UpdateThirdPartyDto inherits this gap via PartialType(CreateThirdPartyDto). All other string fields in CreateThirdPartyDto (organizationName @MaxLength(255), contactFirstName @MaxLength(100), contactLastName @MaxLength(100), notes @MaxLength(2000)) have explicit MaxLength — contactEmail is the only exception.",
      "adversarial_verdict": "CONFIRMED — code_evidence verbatim at lines 36-39; create-third-party.dto.ts verified — MaxLength is imported (line 8) and applied to every other string field except contactEmail; @IsEmail() alone is the sole validator on this field."
    },
    {
      "id": "SEC-058",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "description field in CreateTimeEntryDto lacks @MaxLength constraint",
      "file": "apps/api/src/time-tracking/dto/create-time-entry.dto.ts",
      "line": "71-73",
      "code_evidence": "  @IsString()\n  @IsOptional()\n  description?: string;",
      "description": "The description field in CreateTimeEntryDto has @IsString() and @IsOptional() but no @MaxLength(). The global ValidationPipe has whitelist: true and forbidNonWhitelisted: true but does not impose length constraints. The server body limit is 1 MiB (set in main.ts), which prevents the worst case, but a description field value of ~1 MB is still legal from the validator's perspective and will be persisted verbatim to the database. This is inconsistent with best practices for free-text fields.",
      "root_cause": "No @MaxLength decorator was added when the DTO was authored.",
      "impact": "Users can store description strings up to ~1 MB (the server body limit), potentially inflating storage and slowing queries. The 1 MiB bodyLimit in main.ts provides a hard stop against the extreme case.",
      "suggested_fix": "Add @MaxLength(2000) (or an appropriate business-appropriate limit) to the description field:\n```typescript\n@IsString()\n@IsOptional()\n@MaxLength(2000)\ndescription?: string;\n```",
      "acceptance_criteria": [
        "POST /api/time-tracking with description longer than 2000 characters returns HTTP 400."
      ],
      "verification_command": "python3 -c \"import sys; print('a'*2001)\" | xargs -I{} curl -s -X POST http://localhost:4000/api/time-tracking -H 'Authorization: Bearer <token>' -H 'Content-Type: application/json' -d '{\"date\":\"2026-06-01\",\"hours\":4,\"activityType\":\"DEVELOPMENT\",\"taskId\":\"<uuid>\",\"description\":\"{}\"}' | jq '.statusCode'",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Verbatim code verified at create-time-entry.dto.ts lines 71-73. No @MaxLength present. The 1 MiB bodyLimit (main.ts line 98, `bodyLimit: 1048576`) caps the absolute worst case. Impact is storage/query-performance rather than a direct security exploit."
    },
    {
      "id": "SEC-059",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "CreateUserDto.login and ImportUserDto.login missing @MaxLength — unbounded login field",
      "file": "apps/api/src/users/dto/create-user.dto.ts",
      "line": "46-52",
      "code_evidence": "  @ApiProperty({\n    description: 'Login (format: prenom.nom)',\n    example: 'marie.martin',\n  })\n  @IsString()\n  @MinLength(3)\n  login: string;",
      "description": "CreateUserDto.login and ImportUserDto.login (same pattern at import-users.dto.ts lines 28-34) have @IsString and @MinLength(3) but no @MaxLength. The RegisterDto.login correctly has @MaxLength(50). Without a cap, an adversary with 'users:create' or 'users:import' can store an arbitrarily long login string in the DB. The DB column may have its own limit (likely varchar), but the DTO layer should enforce it independently for defense-in-depth and to produce a cleaner 400 rather than a DB-layer 500 on overflow.",
      "root_cause": "RegisterDto was hardened with @MaxLength(50) but the same constraint was not copied to CreateUserDto and ImportUserDto when they were defined.",
      "impact": "An admin or importer could create users with very long login strings, potentially causing DB truncation/errors or storage abuse. Low severity because the endpoint is admin-only (users:create / users:import).",
      "suggested_fix": "Add @MaxLength(50) to CreateUserDto.login and ImportUserDto.login, matching RegisterDto.login.",
      "acceptance_criteria": [
        "POST /users with a login longer than 50 characters returns 400 Bad Request.",
        "POST /users/import with a login longer than 50 characters reports a per-row error, not a 500."
      ],
      "verification_command": "grep -n 'MaxLength\\|MinLength' apps/api/src/users/dto/create-user.dto.ts apps/api/src/users/dto/import-users.dto.ts",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: create-user.dto.ts line 49 confirms @MinLength(3) with no @MaxLength on login. import-users.dto.ts line 29 same pattern. Prisma schema shows `login String @unique` with no @db.VarChar(N) — PostgreSQL will map this to TEXT (unbounded), so there is no DB-level cap either. Finding is fully confirmed."
    },
    {
      "id": "SEC-060",
      "severity": "nit",
      "category": "security",
      "subcategory": "input-validation",
      "title": "getUsersPresence accepts arbitrary dateStr with no format validation — invalid date causes NaN Date passed to $queryRaw",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1679-1684",
      "code_evidence": "  async getUsersPresence(dateStr?: string) {\n    const targetDate = dateStr ? new Date(dateStr) : new Date();\n    const startOfDay = new Date(targetDate);\n    startOfDay.setHours(0, 0, 0, 0);\n    const endOfDay = new Date(targetDate);\n    endOfDay.setHours(23, 59, 59, 999);",
      "description": "The date query parameter is passed directly to new Date(dateStr) without validation. If dateStr is an invalid date string (e.g. 'not-a-date', 'NaN', ''), new Date() produces an Invalid Date object (whose getTime() returns NaN). This NaN Date is then passed as a bound parameter to the Prisma $queryRaw template literal. Prisma will serialize NaN/Invalid Date in a DB-driver-specific way: PostgreSQL will likely reject it with a type error (500 Internal Server Error), but the error message from AllExceptionsFilter is safely opaqued to 'Internal server error'. The path is: GET /users/presence?date=<badstring> → service crash → 500. This is a denial-of-service concern and a poor user experience. The SQL injection risk is nil because Prisma's tagged template literal parameterizes values.",
      "root_cause": "The date query parameter is not validated before use — no regex or isValid() check, no DTO validator.",
      "impact": "Any authenticated user with 'users:read' can trigger a 500 by passing an invalid date string to GET /users/presence. This is a minor DoS/observability issue.",
      "suggested_fix": "In the controller, add a query parameter validator: either use a custom pipe (@Query('date', new ParseDatePipe())) or add a guard in the service: `if (dateStr && isNaN(new Date(dateStr).getTime())) throw new BadRequestException('Date format invalide — use YYYY-MM-DD');`",
      "acceptance_criteria": [
        "GET /users/presence?date=not-a-date returns 400 Bad Request.",
        "GET /users/presence?date=2026-06-04 returns 200 with presence data."
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial check: controller at line 185 passes raw string directly: `getUsersPresence(@Query('date') date?: string)` with no pipe. Service line 1679-1684 verbatim confirmed. No isNaN guard present. No injection risk (parameterized), but 500 on invalid date is confirmed real."
    },
    {
      "id": "SEC-061",
      "severity": "nit",
      "category": "security",
      "subcategory": "csp-missing-directives",
      "title": "CSP policy missing `object-src 'none'` and `base-uri 'self'` directives",
      "file": "apps/web/src/lib/csp.ts",
      "line": "16-26",
      "code_evidence": "  const directives = [\n    `default-src 'self'`,\n    `script-src 'self' 'nonce-${nonce}'`,\n    `style-src 'self' 'unsafe-inline'`,\n    `img-src 'self' data: blob:`,\n    `font-src 'self' data:`,\n    `connect-src 'self'`,\n    `frame-ancestors 'none'`,\n  ];\n  return directives.join(\"; \");",
      "description": "The CSP policy is missing two recommended directives: `object-src 'none'` (prevents Flash/plugin injection, not covered by `default-src 'self'` in all browsers/configurations) and `base-uri 'self'` (prevents base-tag injection attacks where an attacker inserts a `<base href>` element to redirect relative URLs to an attacker-controlled origin). While `default-src 'self'` covers many fallback scenarios, explicit directives for `object-src` and `base-uri` are recommended by OWASP and CSP Level 3 spec for hardened policies.",
      "root_cause": "The CSP was built to cover the main script injection threat; lower-priority directives were not included.",
      "impact": "Low impact in practice (object-src: no plugin content used; base-uri: requires attacker to inject HTML into the page first). However, the absence of `object-src 'none'` means the policy is not CSP Level 3 compliant and could fail automated security scans (e.g. Observatory, SecurityHeaders.com).",
      "suggested_fix": "Add `object-src 'none'` and `base-uri 'self'` to the directives array in `buildCsp()`.",
      "acceptance_criteria": [
        "The CSP header includes `object-src 'none'`",
        "The CSP header includes `base-uri 'self'`",
        "SecurityHeaders.com gives grade A or higher"
      ],
      "verification_command": "curl -s -I https://localhost:3000/ | grep -i content-security-policy",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed in apps/web/src/lib/csp.ts lines 16-25. Neither object-src nor base-uri appear in the directives array. Confirmed."
    },
    {
      "id": "SEC-062",
      "severity": "nit",
      "category": "security",
      "subcategory": "Dev environment / credential exposure",
      "title": "Dev docker-compose.yml exposes Redis (6379) and PostgreSQL (5432) on all host interfaces without authentication",
      "file": "docker-compose.yml",
      "line": "28-29",
      "code_evidence": "  redis:\n    image: redis:7.4-alpine\n    container_name: orchestr-a-redis\n    restart: unless-stopped\n    ports:\n      - \"${REDIS_PORT:-6379}:6379\"",
      "description": "The development docker-compose.yml binds both PostgreSQL (port 5432) and Redis (port 6379) to all host interfaces (`0.0.0.0`) via the `ports:` directive. Redis has no password (`requirepass`) configured. PostgreSQL uses a well-known default password (`orchestr_a_dev_password`, hardcoded in docker-compose.yml and ci.yml). On a developer machine connected to a network, both services are reachable by anyone on that network segment without authentication.",
      "root_cause": "Default development configuration prioritizes convenience (no auth, exposed ports) without restricting binding to loopback.",
      "impact": "On shared networks (office Wi-Fi, CI runners with misconfigured network isolation), any host can read/write the development database and cache. CI runner PostgreSQL is also exposed (ci.yml publishes port 5432 with default password). Risk is limited to development/CI environments.",
      "suggested_fix": "Bind to loopback only: change `ports: - \"${REDIS_PORT:-6379}:6379\"` to `ports: - \"127.0.0.1:${REDIS_PORT:-6379}:6379\"` and similarly for PostgreSQL. Alternatively use `expose:` (internal only) and rely on the docker network. Add a Redis password for dev parity with prod: `command: redis-server --requirepass ${REDIS_PASSWORD:-dev_password}`.",
      "acceptance_criteria": [
        "netstat/ss on the dev host shows Redis and PostgreSQL bound to 127.0.0.1, not 0.0.0.0",
        "An external host on the same network cannot connect to port 5432 or 6379"
      ],
      "verification_command": "docker compose up -d && ss -tlnp | grep -E '5432|6379'",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: docker-compose.yml lines 11-12 (PostgreSQL) and lines 28-29 (Redis) both use bare port mappings without 127.0.0.1 binding. Neither service has password authentication in the dev compose. Prod compose correctly uses `expose:` (not `ports:`) for both and requires REDIS_PASSWORD via requirepass — the discrepancy between dev and prod is real and intentional but creates risk on shared networks."
    },
    {
      "id": "SEC-063",
      "severity": "nit",
      "category": "security",
      "subcategory": "Inconsistent framing policy",
      "title": "nginx sets X-Frame-Options: SAMEORIGIN but CSP sets frame-ancestors: 'none' — conflicting framing policies",
      "file": "nginx/nginx.conf",
      "line": "102",
      "code_evidence": "        add_header X-Frame-Options \"SAMEORIGIN\" always;",
      "description": "The nginx server block emits `X-Frame-Options: SAMEORIGIN` for the `/` (frontend) location. The Next.js CSP middleware (`apps/web/src/lib/csp.ts`) emits `frame-ancestors 'none'` in the Content-Security-Policy response header. Modern browsers honor `frame-ancestors` (CSP level 2) over `X-Frame-Options` when both are present, so the effective policy is `frame-ancestors 'none'` (no embedding allowed). However, `X-Frame-Options: SAMEORIGIN` remains in the response and contradicts the CSP. This creates auditor confusion, may cause issues with legacy browsers that only honor XFO, and the two headers together could interact unexpectedly with some proxy/WAF configurations.",
      "root_cause": "The nginx headers were authored before or independently of the CSP middleware implementation, and the two were never reconciled.",
      "impact": "Minor: effective policy in modern browsers is the stricter `frame-ancestors 'none'` (good). In legacy browsers that only respect XFO, the policy is `SAMEORIGIN` (weaker than intended). Auditors may flag the inconsistency.",
      "suggested_fix": "Align the policies. If the intent is 'no framing' (as expressed by `frame-ancestors 'none'`), change nginx to `add_header X-Frame-Options \"DENY\" always;` or remove it entirely (since frame-ancestors supersedes XFO in modern browsers). Do NOT change the CSP middleware.",
      "acceptance_criteria": [
        "X-Frame-Options value in nginx is either DENY or absent",
        "CSP frame-ancestors: 'none' remains in the web middleware"
      ],
      "verification_command": "curl -sI http://localhost/ | grep -iE 'x-frame-options|content-security-policy'",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: nginx.conf line 102 code evidence matches verbatim. apps/web/src/lib/csp.ts line 23 confirmed to have `frame-ancestors 'none'` in buildCsp(). The /_next/static location block (nginx.conf line 161) also sets `X-Frame-Options: SAMEORIGIN` with the same inconsistency. The conflict is real and the CSP module correctly uses frame-ancestors 'none', making XFO SAMEORIGIN the weaker stray header."
    },
    {
      "id": "SEC-064",
      "severity": "nit",
      "category": "security",
      "subcategory": "Rate limiting coverage",
      "title": "nginx rate limiting applies only to /api — frontend / and /_next/static locations are unthrottled",
      "file": "nginx/nginx.conf",
      "line": "174-193",
      "code_evidence": "        location / {\n            proxy_pass http://web_backend;\n            proxy_http_version 1.1;\n\n            # Headers\n            proxy_set_header Host $host;\n            proxy_set_header X-Real-IP $remote_addr;\n            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n            proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;",
      "description": "The nginx `limit_req` and `limit_conn` directives are defined for the `/api` location block only (lines 121-122). The frontend (`location /`) and static asset (`location /_next/static`) locations have no rate limiting. An attacker can flood the Next.js web process with unbounded requests. While Next.js itself is statically served for most pages, SSR routes and API proxy calls are still processed per-request by the web container.",
      "root_cause": "Rate limiting was scoped to the API backend only, under the assumption that the frontend is static. Next.js SSR routes are not static and can consume significant server resources per request.",
      "impact": "DoS via resource exhaustion of the web container is possible without any throttle. This is less severe than an API-layer DoS because there are no database queries on pure static pages, but SSR pages involve full rendering.",
      "suggested_fix": "Add a separate rate limit zone for the web frontend and apply it to `location /`:\n```nginx\nlimit_req_zone $binary_remote_addr zone=web_limit:10m rate=30r/s;\n# ...\nlocation / {\n    limit_req zone=web_limit burst=100 nodelay;\n    ...\n}\n```\nThe rate can be higher than the API (30r/s vs 10r/s) to accommodate normal browsing patterns.",
      "acceptance_criteria": [
        "nginx.conf defines a rate limit zone for the web frontend",
        "location / applies limit_req from that zone"
      ],
      "verification_command": "grep -n 'limit_req' nginx/nginx.conf",
      "related_findings": [],
      "cluster_id": "L",
      "found_by": "security",
      "confidence": "high",
      "notes": "Adversarial review confirmed: nginx.conf defines limit_req_zone (line 68) and limit_conn_zone (line 69). location /api applies both (lines 121-122). location / (lines 174-193) and location /_next/static (lines 153-169) have no limit_req or limit_conn directives. Code evidence for lines 174-193 matches verbatim."
    },
    {
      "id": "SEC-065",
      "severity": "suggestion",
      "category": "security",
      "subcategory": "defense-in-depth",
      "title": "getEventsByRange and findOne RBAC filter defaults to unscoped on null role — inconsistent with findAll",
      "file": "apps/api/src/events/events.service.ts",
      "line": "681-690",
      "code_evidence": "    // Scope events for non-management users: only their own or participated\n    if (currentUserId && currentUserRole) {\n      const permissions =\n        await this.permissionsService.getPermissionsForRole(currentUserRole);\n      if (!permissions.includes('events:readAll')) {\n        where.OR = [\n          { participants: { some: { userId: currentUserId } } },\n          { createdById: currentUserId },\n        ];\n      }\n    }",
      "description": "In `getEventsByRange` (lines 681-690) and `findOne` (line 390-403), the RBAC scope filter is applied only when `currentUserId && currentUserRole` are both truthy. If `currentUserRole` is null, the filter block is skipped and the query is unscoped (returns all events). By contrast, `findAll` (lines 277-290) calls `getPermissionsForRole(null)` which correctly returns `[]`, and applies the scope filter because `!permissions.includes('events:readAll')` is true. Today, PermissionsGuardV2 (global APP_GUARD, enforce mode) rejects all null-role callers before reaching the controller, so this code path is unreachable in normal operation. However, the default-open pattern is fragile: if the guard configuration changes, or if these service methods are called internally with null roles, the scope filter would silently be omitted.",
      "root_cause": "Defensive RBAC filter uses `if (currentUserId && currentUserRole)` (default-open) instead of unconditionally calling `getPermissionsForRole` (which safely returns [] for null role), inconsistent with the findAll pattern.",
      "impact": "Not currently exploitable (blocked by PermissionsGuardV2). If guard configuration changes or internal callers pass null roles, would expose all events without scope filter.",
      "suggested_fix": "Replace `if (currentUserId && currentUserRole)` with unconditional `getPermissionsForRole(currentUserRole)` call (handles null safely), matching the findAll pattern.",
      "acceptance_criteria": [
        "getEventsByRange applies scope filter even when currentUserRole is null",
        "findOne applies IDOR check even when currentUserRole is null"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "security",
      "confidence": "medium",
      "notes": "Confidence downgraded from high to medium: currently not exploitable due to PermissionsGuardV2 enforcement. findOne IDOR check at line 390: `if (currentUserId && currentUserRole && !(await this.hasManagementAccess(currentUserRole)))` — same default-open pattern confirmed verbatim. The inconsistency with findAll (lines 277-278 unconditionally calls getPermissionsForRole) is real and confirmed.",
      "adversarial_verdict": "CONFIRMED (confidence downgraded to medium) — both code patterns confirmed verbatim. The default-open vs default-closed inconsistency between getEventsByRange/findOne and findAll is real. However, current exploitability is zero due to PermissionsGuardV2 as APP_GUARD. Keeping as a defense-in-depth finding with reduced confidence."
    },
    {
      "id": "SEC-066",
      "severity": "suggestion",
      "category": "security",
      "subcategory": "resource-exhaustion",
      "title": "ICS import processes unbounded VEVENT count with sequential DB writes and no per-field length cap",
      "file": "apps/api/src/planning-export/planning-export.service.ts",
      "line": "211-269",
      "code_evidence": "  async importIcs(\n    icsContent: string,\n    userId: string,\n  ): Promise<{ imported: number; skipped: number }> {\n    const parsed = nodeIcal.sync.parseICS(icsContent);\n    let imported = 0;\n    let skipped = 0;\n\n    for (const key of Object.keys(parsed)) {",
      "description": "The `importIcs` function iterates over all VEVENTs in a parsed ICS file without any count cap. ImportIcsDto allows up to 5 MB of ICS content, which can contain thousands of VEVENT entries. Each event triggers a sequential `prisma.event.create` call. Additionally, the title (line 253) and description (line 254) extracted from VEVENT fields are truncated by `stripHtml` for HTML tags but are not length-capped before being written to the DB. The global throttle (30 req/s, 600 req/min) limits repeated calls but a single 5MB ICS can still cause a long-running sequential DB write loop.",
      "root_cause": "No maximum VEVENT count guard and no per-field @MaxLength truncation on ICS-extracted data before DB insert.",
      "impact": "A user with leaves:create permission can upload a large ICS file to trigger a long-running series of DB inserts, degrading API responsiveness. Also allows arbitrarily large title/description strings to be inserted from ICS (bypassing CreateEventDto validators since this path does not go through the DTO).",
      "suggested_fix": "Add a MAX_VEVENT_IMPORT constant (e.g. 500) and break early once exceeded. Truncate title to 200 chars and description to 5000 chars after stripHtml. Consider using createMany in a single transaction instead of sequential creates.",
      "acceptance_criteria": [
        "POST /planning-export/ics/import with ICS containing >500 VEVENTs returns HTTP 422 or imports only the first 500",
        "ICS VEVENT titles longer than 200 chars are truncated on import"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "security",
      "confidence": "high",
      "notes": "previewImport (line 172-209) has the same no-count-cap issue but is read-only so impact is lower.",
      "adversarial_verdict": "CONFIRMED — importIcs lines 211-269 confirmed verbatim; lines 251-261 confirm sequential prisma.event.create per VEVENT with no count guard; lines 253-254 confirm stripHtml applied but no .substring(0, N) truncation; ImportIcsDto enforces @MaxLength(5MB) on the whole payload but no per-event field cap"
    }
  ],
  "verified_clean": [
    ".github/workflows/backlog-coherence.yml: no secrets; read-only checkout",
    ".github/workflows/ci.yml: no hardcoded production secrets; all prod secrets use GitHub Secrets mechanism",
    ".github/workflows/docker-publish.yml: uses GITHUB_TOKEN for GHCR auth; no hardcoded registry credentials",
    ".github/workflows/permission-matrix-coverage.yml: no secrets; read-only checkout",
    "API proxy route.ts uses process.env.API_URL — not user-controlled, no SSRF via URL manipulation",
    "AddMemberDto and UpdateMemberDto use @IsUUID for userId",
    "AdvancedAnalyticsTab sub-components (WorkloadChart, RecentActivity, MilestonesCompletion): correct TanStack Query usage with staleTime",
    "All Prisma queries use parameterized ORM calls — no SQL injection vector",
    "All Prisma queries use parameterized values — no string concatenation SQL",
    "All controller route handlers in skills, departments, services, personal-todos have either @RequirePermissions or @AllowSelfService decorators — no unguarded routes",
    "All controller routes have @RequirePermissions guard — no unguarded mutating routes",
    "All controller routes in leaves.controller.ts, leave-types.controller.ts, holidays.controller.ts, school-vacations.controller.ts use @RequirePermissions or @AllowSelfService — no unguarded mutating routes found",
    "All mutating routes on TeleworkSchedule (POST, PATCH :id, DELETE :id) have @RequirePermissions guard",
    "All mutating routes on TimeEntry (POST, PATCH :id, DELETE :id) have @RequirePermissions guard",
    "All pages use 'use client' directive correctly — no attempted server-side secret access",
    "All path param IDs use ParseUUIDPipe (departments/:id, services/:id, skills/:id, skills/user/:userId, etc.)",
    "All route :id params use ParseUUIDPipe — no raw string-to-DB for entity PKs",
    "All route params use ParseUUIDPipe EXCEPT getSnapshots :id (finding S2-4)",
    "All services use Axios (api instance) with Bearer token from localStorage — no cookie-based auth mixing issues",
    "ApproveLeaveDto.comment and RejectLeaveDto.reason both have @MaxLength(2000)",
    "AssignSkillDto: skillId has @IsUUID, level has @IsEnum(SkillLevel)",
    "Auth decisions (canEdit/canDelete) are not computed client-side: role/permissions come from server via useAuthBootstrap",
    "Auth decisions (canEditProject, canDeleteProject, canEdit, canCreateProject, canCreateTask) are all derived from hasPermission() which reads from the RBAC permission store — no client-side role string comparison",
    "AuthProvider guards all pages against unauthenticated access and blocks render until permissionsLoaded=true",
    "Blob URL for template download (leaves/page.tsx:524) correctly calls URL.revokeObjectURL() immediately after click",
    "CSP is correctly applied to response headers in production only (dev correctly skipped for HMR)",
    "CSP nonce: pages use no inline scripts or style injection that would require nonce handling",
    "CSV export filename is sanitized via regex replace(/[^a-zA-Z0-9-_]/g, '_') before use in Content-Disposition header",
    "CreateDepartmentDto: name has @MinLength(2) + @MaxLength(100), managerId has @IsUUID",
    "CreateLeaveTypeDto: code field validated with @Matches(/^[A-Z_]+$/) and @MaxLength(50); color with @Matches hex pattern — well-constrained",
    "CreatePersonalTodoDto: text has @MaxLength(200)",
    "CreateServiceDto: name has @MinLength(2) + @MaxLength(100), color has @Matches(/^#[0-9A-Fa-f]{6}$/), departmentId and managerId have @IsUUID",
    "CreateSkillDto: name has @MinLength(2) + @MaxLength(100), category has @IsEnum(SkillCategory), requiredCount has @IsInt + @Min(1)",
    "Defense-in-depth ownership assertion implemented in update/remove/archive/unarchive/updateMember/removeMember (6 of 7 mutation methods)",
    "Delegation creation (createDelegation) double-checks delegator permission in service layer even though controller already requires leaves:manage_delegations",
    "Document URL not fetched server-side — no SSRF risk; URL is stored metadata only",
    "GET /telework/:id has @OwnershipCheck + service-level IDOR check in findOne (telework.service.ts:392-400)",
    "GET /time-tracking/:id has service-level IDOR check in findOne (time-tracking.service.ts:487-500) — correct",
    "GanttChart is lazy-loaded with dynamic() and ssr:false — correct for a heavy client-side component",
    "Global JwtAuthGuard is registered as APP_GUARD in auth.module.ts — all routes require authentication unless @Public",
    "Global JwtAuthGuard, ForcePasswordChangeGuard, ThrottlerBehindProxyGuard, and PermissionsGuardV2 are all registered as APP_GUARD and apply to all routes",
    "Global ThrottlerBehindProxyGuard registered as APP_GUARD in AuthModule; school-vacations import endpoint adds stricter @Throttle (3/min)",
    "Global ValidationPipe configured with whitelist:true and forbidNonWhitelisted:true in main.ts (prevents mass-assignment)",
    "Global ValidationPipe has whitelist:true and forbidNonWhitelisted:true (main.ts:164-165) — extra properties stripped",
    "Global ValidationPipe in main.ts has whitelist:true + forbidNonWhitelisted:true — extra body fields are stripped",
    "Global ValidationPipe is configured with whitelist:true and forbidNonWhitelisted:true — extra body properties are stripped/rejected",
    "Global body limit of 1 MiB (bodyLimit: 1048576) is set on the Fastify adapter",
    "Global body limit set to 1 MiB (SEC-025) in main.ts Fastify config",
    "Global bodyLimit of 1 MiB set in main.ts FastifyAdapter config — prevents unbounded body attacks",
    "Global: 1MB body limit on Fastify adapter",
    "Global: APP_GUARD enforces RBAC, RBAC_GUARD_MODE=permissive blocked in production",
    "Global: JwtAuthGuard applied as APP_GUARD — all endpoints require JWT by default; ForcePasswordChangeGuard also applied globally",
    "Global: No $queryRawUnsafe or $executeRawUnsafe found in any file under the three audited paths",
    "Global: ThrottlerBehindProxyGuard applied as APP_GUARD (global rate limiting); specific @Throttle decorators only on auth endpoints which is appropriate",
    "Global: ValidationPipe configured with whitelist: true, forbidNonWhitelisted: true, transform: true",
    "Global: ValidationPipe configured with whitelist:true + forbidNonWhitelisted:true + transform:true in main.ts",
    "Import milestones dueDate validated via new Date() in service — invalid dates caught with isNaN check (milestones.service.ts:314-320) in validateImport path",
    "ImportPreviewModal: no XSS risk; all preview data rendered as text nodes via JSX",
    "JWT authentication enforced globally via JwtAuthGuard APP_GUARD; @AllowSelfService correctly gates authenticated self-service routes",
    "JWT configuration reads secret from ConfigService (not hardcoded), with access TTL configured",
    "JWT/access_token localStorage storage is intentional per CLAUDE.md (not flagged)",
    "Locale/i18n routing: useLocale() used consistently for locale prefix; no manual locale string hardcoding",
    "Next.js catch-all [...path] route normalizes path segments at the router level, mitigating naive ../ traversal",
    "No $queryRawUnsafe or $executeRawUnsafe calls in any production service file (only in *.int.spec.ts test harness)",
    "No $queryRawUnsafe or $executeRawUnsafe calls in any scanned module — all DB queries use Prisma parameterized APIs",
    "No $queryRawUnsafe or $executeRawUnsafe found in projects/epics/milestones services — all queries use Prisma structured API",
    "No $queryRawUnsafe or $executeRawUnsafe in any file in scope",
    "No JWT handling, cookie flags, or auth logic in either module",
    "No XSS via CSS injection — leaveType.color from API is used directly in style prop (backgroundColor, color) without HTML interpolation; CSS property value injection has no code-execution path in React",
    "No XSS via dangerouslySetInnerHTML anywhere in scanned scope",
    "No barrel import performance issues in the scanned service files (each imports directly from @/lib/api)",
    "No console.* calls with PII — all files use the centralized logger with PII scrubbing (logger.ts)",
    "No console.log with PII: all debug logging goes through the logger utility imported from @/lib/logger",
    "No console.log/console.error with PII in scanned files (replaced by structured logger)",
    "No console.log/warn/debug calls with PII in scanned pages or components",
    "No dangerouslySetInnerHTML usage found in any scanned file",
    "No dangerouslySetInnerHTML usage in any of the 8 scanned files",
    "No dangerouslySetInnerHTML usage in any scanned file",
    "No external URL fields in any DTO — no SSRF risk",
    "No external URL fields in either module — no SSRF surface",
    "No file upload endpoints in either module — no MIME/magic-byte surface",
    "No file upload endpoints in these four modules — file upload checklist not applicable",
    "No file upload handling in any scanned module",
    "No file upload in these modules",
    "No hardcoded role constants (Role.ADMIN, Role.MANAGER) in client-side auth decisions — all RBAC checks use hasPermission() from usePermissions hook",
    "No hardcoded secrets, tokens, or passwords found in any file in scope",
    "No hardcoded secrets, tokens, or passwords found in any scanned source file",
    "No hardcoded secrets, tokens, or passwords found in any service or controller",
    "No hardcoded secrets, tokens, or passwords in any file in the two modules",
    "No hardcoded secrets, tokens, or passwords in any scanned file",
    "No localStorage or sessionStorage access for tokens in any scanned file",
    "No open redirect (no user-controlled string passed directly to router.push/replace without validation beyond the ProjectsDetailTable locale bug which is not exploitable for external redirects)",
    "No open redirect vulnerabilities — all router.push() calls use hardcoded locale-prefixed paths",
    "No open redirect: all router.push() calls use locale-prefixed internal paths constructed from controlled variables",
    "No open redirect: login and register pages hardcode /${locale}/dashboard post-login; no searchParams redirect parameter",
    "No raw SQL ($queryRawUnsafe/$executeRawUnsafe) in telework.service.ts or time-tracking.service.ts — all DB access via parameterized Prisma ORM",
    "No secret values written to logs in any scanned service",
    "No secrets or API keys embedded in client bundles in scanned files",
    "No secrets or API tokens hardcoded in client-side source files",
    "No secrets or tokens leaked to client bundle (no API keys, JWT_SECRET, etc. in component files)",
    "No secrets, tokens, or credentials exposed to the client bundle in any scanned file",
    "No unbounded list renders in the scanned auth/profile pages",
    "No user-supplied URL fields (no SSRF surface in these modules)",
    "No user-supplied URL fields found; school-vacations importFromOpenData uses a hardcoded government URL — no SSRF vector",
    "Optimistic updates: no optimistic update pattern is used — all mutations await the API call before updating state (safe but slower)",
    "OwnershipGuard is registered as APP_GUARD in CommonModule",
    "PATCH :id and DELETE :id on TeleworkSchedule use @RequirePermissions + @OwnershipCheck (OwnershipGuard is a global APP_GUARD in CommonModule)",
    "PATCH :id and DELETE :id on TimeEntry correctly use @UseGuards(JwtAuthGuard, OwnershipGuard) + @OwnershipCheck with bypassPermission",
    "ParseIntPipe used for year param in GET /holidays/year/:year and GET /school-vacations (year query)",
    "ParseUUIDPipe used on all :id path parameters across all four modules",
    "Password reset token is read from URL searchParams (not localStorage) — no persistence risk",
    "PermissionsGuardV2 is active (registered in RbacModule as APP_GUARD); the stale 'not registered' comment in permissions.guard.ts is outdated documentation only",
    "PermissionsGuardV2 is registered as APP_GUARD in RbacModule and defaults to 'enforce' mode",
    "Personal todos ownership check: update() and delete() in personal-todos.service.ts explicitly verify todo.userId === userId before allowing mutation (prevents IDOR)",
    "PersonalTodosService cleanupOldCompleted is always scoped to the authenticated userId — no cross-user data leakage",
    "PlanningView is lazy-loaded with dynamic() and ssr:false — correct for a calendar/planning component",
    "Production boot assertions prevent RBAC permissive mode, weak JWT secret, and auth token exposure",
    "QuickTimeEntryInput: properly validates hours before submitting; no SSRF or injection risk",
    "RBAC perimeter checks (service scope via getManagedUserIds/getServiceIds) consistently applied in leaves service for findAll, update, remove, cancel, approve, reject, rejectCancellation",
    "Self-approval (leaves:self_approve) correctly prevented from approving others' leaves (approve() gate: leave.userId === validatorId throws ForbiddenException)",
    "Serializable transaction + single retry on P2034 used for leave creation (DAT-024) — race condition properly handled",
    "SkillsMatrix: no XSS risk; all user data is text-rendered via JSX, not innerHTML",
    "Snapshot capture endpoint uses reports:export permission (not broadly available)",
    "SuiviPage fetches user data before performing access check and stops all downstream fetches if access is denied",
    "TanStack Query is not used in scanned pages (they use direct service calls or Zustand) — no staleTime issues",
    "TaskCard correctly uses useLocale and prefixes all navigation with /${locale}/",
    "Test files exist for leaves (TST-023) and telework covering RBAC affordances",
    "ThrottlerBehindProxyGuard is a global APP_GUARD — telework and time-tracking inherit global rate limits",
    "UpdatePersonalTodoDto: text has @MaxLength(200), completed has @IsBoolean",
    "UpdateSkillLevelDto: level has @IsEnum(SkillLevel)",
    "UserAvatar uses Next.js Image component with auth-gated blob URLs for uploaded avatars (SEC-016 addressed)",
    "Zustand auth store does not persist role/permissions to localStorage (only minimal display cache)",
    "_dev/gantt-base/page.tsx: dev page uses logger.info correctly, no production exposure",
    "apps/api/src/analytics/advanced/analytics-advanced.controller.ts — all 6 routes have @RequirePermissions('reports:view'); @CurrentUser() passed to service",
    "apps/api/src/analytics/advanced/services/milestones-completion.service.ts — project scope enforced; no raw SQL; no secret logging",
    "apps/api/src/analytics/advanced/services/project-health.service.ts — accessScope.projectScopeWhere enforced; no raw SQL",
    "apps/api/src/analytics/advanced/services/recent-activity.service.ts — accessScope.projectScopeWhere; days bounded @Max(365); no secret logging",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — projectIds from query filtered through DB scope (AND [projectScope, archivedClause]) before use; no IDOR",
    "apps/api/src/analytics/advanced/services/tasks-breakdown.service.ts — user-supplied projectIds intersected with projectScope (not trusted alone); scope enforced",
    "apps/api/src/analytics/advanced/services/workload.service.ts — accessScope.projectScopeWhere called; limit bounded to @Max(100) in DTO",
    "apps/api/src/analytics/advanced/snapshot-scheduler.service.ts — Redis leader lock prevents duplicate runs; no hardcoded secrets; Redis password via ConfigService",
    "apps/api/src/analytics/analytics.controller.ts — all routes have @RequirePermissions; uses @CurrentUser(); no raw SQL",
    "apps/api/src/analytics/analytics.service.ts — all Prisma queries parameterized; user scope enforced via AccessScopeService.projectScopeWhere; no hardcoded secrets",
    "apps/api/src/analytics/dto/analytics-query.dto.ts — @IsEnum and @IsUUID on all fields; whitelist/forbidNonWhitelisted enforced globally",
    "apps/api/src/audit/audit-action.compile-witness.ts — compile-time only; no runtime security concerns",
    "apps/api/src/audit/audit-action.enum.ts — no security issues; compile-time closed enum",
    "apps/api/src/audit/audit-payload-registry.compile-witness.ts — compile-time only; no runtime security concerns",
    "apps/api/src/audit/audit-persistence.service.ts — advisory lock serializes chain inserts; all SQL uses parameterized $queryRaw/$executeRaw tagged templates; no user-controlled string reaches raw SQL; actorEmail/Label snapshot is a controlled DB lookup",
    "apps/api/src/audit/audit.module.ts — @Global() correct; no issues",
    "apps/api/src/audit/audit.service.ts — attemptedEmail excluded from loggable before stdout; BCRYPT_SHAPE guard prevents credential persistence; HMAC-keyed entityId for DB persistence; fire-and-forget pattern with .catch() ensures no crash on audit failure",
    "apps/api/src/audit/export-audit.helper.ts — no user input; fire-and-forget with .catch(); payload fields are typed",
    "apps/api/src/audit/payload-schemas.ts — Zod .strict() on every schema prevents unknown keys; AuditPayloadValidationError surfaces failures correctly; no injection vectors",
    "apps/api/src/audit/recompute-chain.ts — all SQL uses parameterized $queryRaw/$executeRaw tagged templates; $executeRaw UPDATE uses parameterized values (${prevHash}, ${newRowHash}, ${row.id}); not user-reachable (operator script only)",
    "apps/api/src/clients/clients.controller.ts — all routes have @RequirePermissions, ParseUUIDPipe on params, no raw SQL",
    "apps/api/src/clients/clients.service.ts — all Prisma queries parameterized, no $queryRawUnsafe, hardcoded secrets absent",
    "apps/api/src/clients/dto/create-client.dto.ts — name has @Length(1,255), @IsString, @Transform trim",
    "apps/api/src/clients/dto/update-client.dto.ts — optional fields properly validated, @IsBoolean on isActive",
    "apps/api/src/clients/projects-clients.controller.ts — @RequirePermissions on all routes, ParseUUIDPipe on all params",
    "apps/api/src/common/config/audit-hash-key.ts: AUDIT_HASH_KEY minimum 32 chars enforced in all environments",
    "apps/api/src/common/config/jwt-secret.ts: JWT_SECRET minimum 32 chars enforced in production",
    "apps/api/src/common/error-reporter.ts: process-level unhandledRejection/uncaughtException handlers installed idempotently; no external egress in noop impl",
    "apps/api/src/common/fastify/cors.config.ts: production requires CORS_ORIGIN or ALLOWED_ORIGINS, throws on absence",
    "apps/api/src/common/fastify/redact.config.ts: authorization, cookie, password, token fields redacted from Fastify logs",
    "apps/api/src/common/fastify/request-id.context.ts: x-request-id header sanitized (CR/LF stripped, capped at 128 chars) to prevent log injection",
    "apps/api/src/common/fastify/trust-proxy.config.ts: trustProxy scoped to loopback+uniquelocal only (SEC-013), prevents IP spoofing from public networks",
    "apps/api/src/common/fastify/uploads-auth.hook.ts: OPTIONS preflight correctly exempted, actual GET/HEAD requires Bearer token",
    "apps/api/src/common/filters/all-exceptions.filter.ts: non-HttpException errors return opaque 500 — no stack/message leakage to client",
    "apps/api/src/common/guards/ownership.guard.ts: ownership checked server-side via DB query; bypassPermission uses permission system not hardcoded roles",
    "apps/api/src/common/services/access-scope.service.ts: project/task/document/user read scopes use parameterized Prisma queries, no raw SQL",
    "apps/api/src/common/services/cache.service.ts: Redis password loaded from config, not hardcoded; no secrets logged",
    "apps/api/src/common/services/ownership.service.ts: all resource ownership checks use parameterized Prisma findUnique/findFirst queries",
    "apps/api/src/common/services/role-hierarchy.service.ts: role assignment enforcement prevents privilege escalation above caller's template rank",
    "apps/api/src/common/upload/magic-bytes.validator.ts: file-type library used for magic-byte validation; MIME allowlist enforced; no DB-trusted filename used",
    "apps/api/src/common/validators/password-policy.ts: password complexity regex requires uppercase+digit+special; @IsStrongPassword decorator centralizes policy",
    "apps/api/src/deployments/deployments.module.ts — no issues",
    "apps/api/src/deployments/deployments.service.ts — no controller/route exposure; parameterized SQL ($queryRaw tagged template); no hardcoded secrets; RELEASE_SHA/DEPLOYED_BY from env only; no user-supplied input paths",
    "apps/api/src/documents/documents.controller.ts — @RequirePermissions on all routes, OwnershipGuard on PATCH/DELETE, ParseUUIDPipe on id params",
    "apps/api/src/documents/documents.service.ts — Prisma queries parameterized, no raw SQL, audit trail fires after access check",
    "apps/api/src/documents/dto/create-document.dto.ts — @IsUrl with protocol allowlist (http/https), @IsIn on mimeType (SVG excluded), @MaxLength(255) on name, @MaxLength(2048) on url",
    "apps/api/src/documents/dto/update-document.dto.ts — inherits CreateDocumentDto validators via PartialType",
    "apps/api/src/events/dto/create-event.dto.ts — title has @MaxLength(200) @MinLength(3); time fields have @Matches regex; IDs are @IsUUID; global ValidationPipe(whitelist:true, forbidNonWhitelisted:true) prevents mass-assignment",
    "apps/api/src/events/events.controller.ts — all routes carry @RequirePermissions or @AllowSelfService; mutation routes use @UseGuards(JwtAuthGuard, OwnershipGuard) + @OwnershipCheck; getEventsByUser has inline permission check for cross-user access",
    "apps/api/src/events/events.service.ts — no raw SQL / $queryRawUnsafe; all DB queries use Prisma parameterized queries; ensureCanMutate provides service-layer defense-in-depth ownership check",
    "apps/api/src/events/events.service.ts:findAll — null-role path is correctly default-closed via getPermissionsForRole(null) returning []",
    "apps/api/src/health/health.controller.ts — @Public() correct for monitoring; no sensitive fields in route handler; @ServiceUnavailableException body is the only concern (captured in S10-1)",
    "apps/api/src/health/health.module.ts — no secrets, no SQL, no auth bypass",
    "apps/api/src/main.ts: /api/uploads/ requires valid Bearer JWT via createUploadsAuthHook (SEC-016)",
    "apps/api/src/main.ts: @fastify/helmet registered with CSP directives for the API",
    "apps/api/src/main.ts: AUDIT_HASH_KEY enforced in all environments (OBS-028)",
    "apps/api/src/main.ts: AUTH_EXPOSE_RESET_TOKEN=true blocked in production (SEC-018)",
    "apps/api/src/main.ts: CORS configured with explicit origin allowlist via resolveAllowedOrigins()",
    "apps/api/src/main.ts: File upload size limited to 2MiB via fastifyMultipart limits",
    "apps/api/src/main.ts: JWT_SECRET strength enforced in production via assertJwtSecretStrength (SEC-026)",
    "apps/api/src/main.ts: RBAC_GUARD_MODE=permissive blocked in production (SEC-001)",
    "apps/api/src/main.ts: Swagger enabled in prod requires SWAGGER_USER and SWAGGER_PASS",
    "apps/api/src/main.ts: Swagger protected with HTTP Basic Auth using timing-safe comparison (timingSafeEqual)",
    "apps/api/src/main.ts: ValidationPipe with whitelist:true, forbidNonWhitelisted:true enforced globally",
    "apps/api/src/main.ts: bodyLimit 1MiB set on Fastify adapter (SEC-025)",
    "apps/api/src/metrics/metrics.module.ts — MetricsController marked @Public; token guard handled in controller",
    "apps/api/src/planning-export/dto/import-ics.dto.ts — @MaxLength(5*1024*1024) prevents oversized ICS payloads at DTO layer",
    "apps/api/src/planning-export/planning-export.controller.ts — exportIcs protected by @AllowSelfService (JWT still required, user scoped to own data); import routes protected by @RequirePermissions('leaves:create'); user-agent capped at 512 chars",
    "apps/api/src/planning-export/planning-export.service.ts — stripHtml() applied to all ICS-extracted free-text fields (not susceptible to ReDoS — negated char class); Prisma parameterized queries throughout; audit trail recorded for data export (OBS-007); no hardcoded secrets",
    "apps/api/src/planning-export/planning-export.service.ts:exportIcs — self-service scoped: queries always filtered by userId from JWT; no cross-user data leakage",
    "apps/api/src/planning/dto/planning-overview-query.dto.ts — @IsISO8601 + @IsNotEmpty on both date fields",
    "apps/api/src/planning/planning.controller.ts — single GET route protected by @RequirePermissions('users:read'); @CurrentUser used for scoping; no sensitive data hardcoded",
    "apps/api/src/planning/planning.service.ts — aggregates sub-service calls each of which applies its own RBAC; no raw SQL; no hardcoded secrets",
    "apps/api/src/schema-constraints/ — all files are *.int.spec.ts test files; no production code; no security concerns in scope",
    "apps/api/src/scripts/backfill-snapshots.ts — operator script; no user input; no SQL injection vectors",
    "apps/api/src/scripts/normalize-action-codes.ts — data-bound SQL (FROM_VALUE, TO_VALUE) uses safe $executeRaw tagged template; $executeRawUnsafe only for DDL identifier (noted in S10-3); requires DATABASE_MIGRATION_URL (operator-only); dry-run rollback pattern is correct",
    "apps/api/src/scripts/normalize-action-codes.witness.ts — test harness only; $executeRawUnsafe uses parameterized $1 placeholder for targetId; hardcoded EXPLAIN query in witness is static string",
    "apps/api/src/scripts/recompute-chain-on-schema-bump.ts — same pattern as normalize-action-codes.ts; $executeRawUnsafe only for DDL (noted in S10-3); operator-only script",
    "apps/api/src/scripts/recompute-chain-on-schema-bump.witness.ts — $queryRawUnsafe EXPLAIN call uses fully hardcoded SQL string with no interpolation",
    "apps/api/src/scripts/system-backfill-audit.ts — typed payload; no user input; DEPLOYED_BY/DEPLOY_USER from env only",
    "apps/api/src/settings/settings.controller.ts — all routes have @RequirePermissions; settings:read for reads, settings:update for mutations",
    "apps/api/src/settings/settings.service.ts — update() enforces isKnownKey allowlist; findByCategory() validates against AppSettingsCategory enum; all Prisma queries parameterized",
    "apps/api/src/testing/testing.controller.ts — @Public() with dual defense (module-level conditional import + handler-level NODE_ENV check); no DTOs or user input accepted; global ThrottlerBehindProxyGuard applies; design tradeoffs are intentional and documented (TST-017)",
    "apps/api/src/testing/testing.module.ts — no issues",
    "apps/api/src/third-parties/dto/assign-third-party-to-project.dto.ts — @IsUUID, @IsInt/@Min/@Max on allocation",
    "apps/api/src/third-parties/dto/assign-third-party-to-task.dto.ts — @IsUUID, @IsNotEmpty",
    "apps/api/src/third-parties/dto/create-third-party.dto.ts — @IsEnum on type, @MaxLength on all string fields except contactEmail, @IsEmail on contactEmail",
    "apps/api/src/third-parties/dto/update-third-party.dto.ts — PartialType inheritance, @IsBoolean on isActive",
    "apps/api/src/third-parties/projects-third-party-members.controller.ts — @RequirePermissions on all routes, ParseUUIDPipe on params",
    "apps/api/src/third-parties/tasks-third-party-assignees.controller.ts — @RequirePermissions on all routes, ParseUUIDPipe on params",
    "apps/api/src/third-parties/third-parties.controller.ts — all routes have @RequirePermissions, ParseUUIDPipe on params",
    "apps/api/src/third-parties/third-parties.service.ts — Prisma queries parameterized, no raw SQL, no hardcoded secrets",
    "apps/web/middleware.ts: CSP only enforced in production; dev mode skips to allow HMR",
    "apps/web/middleware.ts: per-request nonce generated via crypto.randomUUID(), injected into CSP for Next.js hydration scripts (SEC-CSP-001)",
    "apps/web/src/lib/csp.ts: frame-ancestors 'none' prevents clickjacking",
    "apps/web/src/lib/csp.ts: script-src uses nonce-only (no unsafe-inline); style-src retains unsafe-inline (lower risk, common framework need)",
    "auth.service.ts logout() redirects to bare /login (handled by next-intl middleware locale routing) — not a user-controlled URL",
    "auth/auth.controller.ts: all routes have @Public, @AllowSelfService, or @RequirePermissions; throttle applied to all public auth routes",
    "auth/auth.module.ts: correct APP_GUARD order — ThrottlerBehindProxyGuard → JwtAuthGuard → ForcePasswordChangeGuard",
    "auth/auth.service.ts: no hardcoded secrets; password compared via bcrypt; validateUser returns null uniformly on bad password and disabled account (SEC-005 timing collapse); reset token stored as SHA-256 hash, not plaintext; AUTH_EXPOSE_RESET_TOKEN gated to non-production only",
    "auth/dto/login.dto.ts: @IsString, @MinLength on both fields; no free-form HTML fields",
    "auth/dto/refresh-token.dto.ts: @IsOptional + @IsString; no injection risk",
    "auth/dto/register.dto.ts: @IsEmail, @IsStrongPassword, @MaxLength on all fields; @Matches(NO_CONTROL_CHARS) on name fields; registration gated by REGISTRATION_ENABLED env flag",
    "auth/dto/reset-password-token.dto.ts: @IsUUID on userId — prevents non-UUID injection",
    "auth/dto/reset-password.dto.ts: @IsString on token, @IsStrongPassword on newPassword",
    "auth/guards/force-password-change.guard.ts: reads DB-live forcePasswordChange flag, not stale JWT claim",
    "auth/guards/jwt-auth.guard.ts: properly skips @Public routes and delegates to passport-jwt",
    "auth/guards/throttler-behind-proxy.guard.ts: uses clientIp() — correct IP extraction behind nginx proxy",
    "auth/jwt-blacklist.service.ts: fail-CLOSED on Redis read error; fail-CLOSED on write error (throws 503); proper TTL expiry",
    "auth/jwt-not-before.service.ts: correct unit handling (all seconds); +1 same-second guard; fail-OPEN on read (acceptable — blacklist checked first)",
    "auth/login-lockout.service.ts: per-(account,IP) keying via SHA-256 hash (no raw identifier in Redis); progressive lockout with cap; fail-OPEN documented",
    "auth/refresh-token.service.ts: tokens stored as SHA-256 hash; serializable transaction on rotate; reuse detection revokes all tokens for user; randomBytes(48) entropy",
    "auth/strategies/jwt.strategy.ts: checks jti blacklist then nbf before DB lookup; rejects inactive users; attaches jti+exp for logout blacklisting",
    "auth/strategies/local.strategy.ts: delegates to authService.validateUser; no direct DB access",
    "canEdit and canDelete flags in leaves/page.tsx (lines 757, 767) correctly use API-returned flags from the leave object, not client-side computation",
    "canManageLeave: correctly checks leaves:manage_any before falling back to service perimeter",
    "change-password/page.tsx correctly redirects to /${locale}/login after success (not to arbitrary URL)",
    "clientIds array uses @IsString({ each: true }) with UUID validation via isUUID(t) in service (projects.service.ts:265-269)",
    "clients/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "clients/page.tsx and third-parties/page.tsx permission checks (canCreate, canUpdate, canDelete) correctly use hasPermission() hook",
    "comments/comments.controller.ts — all routes have @RequirePermissions(…); CurrentUser always extracted; no @Public route",
    "comments/comments.service.ts — findAll() applies accessScope.taskReadWhere() filter; findOne() calls assertCanReadTask()",
    "comments/comments.service.ts — no raw SQL; no hardcoded secrets; no SSRF",
    "comments/comments.service.ts — update() enforces authorId === userId ownership check; remove() checks authorId or comments:delete_any/projects:manage_any/tasks:manage_any",
    "common/config/audit-hash-key.ts: AUDIT_HASH_KEY >= 32 chars enforced at boot in ALL environments",
    "common/config/jwt-secret.ts: JWT_SECRET length >= 32 enforced at boot in production",
    "common/error-reporter.ts: no external egress in default reporter; unhandledRejection and uncaughtException handled",
    "common/fastify/cors.config.ts: production boot-assert if CORS_ORIGIN not set; no wildcard fallback in prod",
    "common/fastify/redact.config.ts: authorization, cookie, password fields, refreshToken, access_token, passwordHash all redacted from Fastify logs",
    "common/fastify/request-id.context.ts: x-request-id header capped to 128 chars, CR/LF stripped — log injection prevention",
    "common/fastify/trust-proxy.config.ts: ['loopback','uniquelocal'] — most restrictive safe config for nginx-in-Docker topology",
    "common/fastify/uploads-auth.hook.ts: authentication gate on /api/uploads/* before static handler; OPTIONS preflight correctly allowed",
    "common/filters/all-exceptions.filter.ts: non-HttpException errors return opaque 500 (no stack/message leakage); request ID threaded for correlation",
    "common/guards/ownership.guard.ts: requires authenticated user; reads resource ID from route params; bypassPermission checked via PermissionsService",
    "common/services/access-scope.service.ts: management-tier bypass is permission-driven (users:manage), not role-code; horizontal scope enforced for findOne/getUsersBy*",
    "common/services/ownership.service.ts: all lookups select only ownership field; returns false on not-found (404/403 decision left to caller)",
    "common/services/role-hierarchy.service.ts: ADMIN template bypass explicit; callerRank must be strictly greater than targetRank; no hardcoded role codes in business logic",
    "common/upload/magic-bytes.validator.ts: file-type library used for magic-byte check; MIME allowlist per upload kind; rejection on unknown type",
    "common/validators/password-policy.ts: single source of truth for password policy; min 8 chars + uppercase + digit + special char; imperative validatePasswordStrength for non-DTO paths",
    "dashboard/page.tsx: uses Promise.all with per-catch error isolation for parallel fetches, no unhandled rejection",
    "dayOfWeek in CreateRecurringRuleDto has @Min(0) @Max(6) — correct range check",
    "departments/page.tsx: no locale routing bugs; uses inline modals with no navigation",
    "docker-compose.prod.yml: DATABASE_PASSWORD, REDIS_PASSWORD, JWT_SECRET, AUDIT_HASH_KEY, APP_DATABASE_PASSWORD all required (:? operator) — no defaults",
    "docker-compose.prod.yml: PostgreSQL uses scram-sha-256 authentication",
    "docker-compose.prod.yml: RBAC_GUARD_MODE hardcoded to 'enforce' in API environment",
    "docker-compose.prod.yml: Redis uses requirepass with mandatory REDIS_PASSWORD",
    "docker-compose.prod.yml: api and web containers use cap_drop: ALL and no-new-privileges:true",
    "docker-compose.prod.yml: postgres, redis, api, web all use expose: (not ports:) except nginx — no direct DB/cache access from host",
    "events/page.tsx canCreateEvent() and canDeleteEvent() correctly use hasPermission() hook",
    "forgot-password/page.tsx is a static info page — no form submission, no CSRF risk",
    "generateSchedulesFromRules: non-manage_any users are scoped to their own rules only",
    "holidays.service.ts: Easter calculation and all date math use server-side computation only, no user input in date formulas",
    "hours field in CreateTimeEntryDto has @Min(0.25) @Max(24) — correct range check",
    "import-leaves.dto.ts: dates validated as @IsString/@IsNotEmpty; service validates via new Date() + isNaN check before inserting",
    "leave-types.service.ts: system types protected from delete and sensitive-field updates via isSystem flag check",
    "leave-types/code/:code passes code to Prisma via parameterized where clause — no SQL injection",
    "leaves.service.ts findOne: ownership check present — non-owners without manage perimeter get 403",
    "localStorage token exposure: auth state is in Zustand store (memory), not directly read/written in these pages",
    "localStorage usage in MyTasksSection is limited to UI preference (undeclaredExpanded) — no sensitive data stored",
    "mailto: href in third-parties/[id]/page.tsx uses server-sourced contactEmail — no open redirect to external HTTP URLs",
    "middleware.ts uses crypto.randomUUID() for nonce — cryptographically secure",
    "nginx/nginx.conf: API CSP correctly delegated to @fastify/helmet",
    "nginx/nginx.conf: CSP intentionally absent from nginx HTML path (correctly delegates to Next.js nonce middleware)",
    "nginx/nginx.conf: X-Content-Type-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy headers set",
    "nginx/nginx.conf: server_tokens off hides nginx version",
    "planning/__tests__/page.test.tsx: test file exists and covers basic rendering",
    "predefined-tasks/dto/bulk-assignment.dto.ts — @ArrayMinSize(1) present on both userIds and dates; @IsUUID and @IsDateString validators present",
    "predefined-tasks/dto/create-predefined-task.dto.ts — startTime/endTime validated with @Matches regex; weight bounded with @Min(1)/@Max(5)",
    "predefined-tasks/dto/create-recurring-rule.dto.ts — cross-field IsValidRecurrenceConfig class-level validator ensures recurrenceType/dayOfWeek/monthlyOrdinal/monthlyDayOfMonth coherence",
    "predefined-tasks/occurrence-generator.ts — pure function; no I/O; no injection surface; bounded by caller-provided rangeStart/rangeEnd",
    "predefined-tasks/predefined-tasks.controller.ts — all route handlers have @RequirePermissions(…); JwtAuthGuard applied globally",
    "predefined-tasks/predefined-tasks.service.ts — createAssignment() checks predefined task isActive; assertTeleworkCompatibility() run before assignment creation",
    "predefined-tasks/predefined-tasks.service.ts — no hardcoded secrets or tokens",
    "predefined-tasks/predefined-tasks.service.ts — no raw SQL; all queries parameterized via Prisma ORM",
    "predefined-tasks/predefined-tasks.service.ts — removeAssignment() does findUnique before delete (existence check); no IDOR — any assignment ID can be deleted by anyone with predefined_tasks:assign (design choice)",
    "prisma/prisma.service.ts: cleanDatabase() gated to non-production; no raw SQL in service; slow query logging at 200ms threshold",
    "projects/[id]/page.tsx: handleHardDeleteProject, handleAddMember, handleAttachClients, handleDetachClient, handleSaveMilestone, handleSaveTask all have try/catch with user-facing error toasts",
    "projects/__tests__/page.test.tsx: test file exists and covers filter and rendering behavior",
    "projects/page.tsx: handleCreate has try/catch with user-facing error toast",
    "projects:manage_any bypass permission used consistently in OwnershipGuard and assertProjectOwnershipOrBypass",
    "rbac/dto/create-role.dto.ts: @IsString, @MinLength, @MaxLength, @Matches(SCREAMING_SNAKE_CASE) on code; @IsIn(ROLE_TEMPLATE_KEYS) on templateKey",
    "rbac/dto/update-role.dto.ts: only label/description/isDefault editable; templateKey excluded from DTO",
    "rbac/permissions.guard.ts: PermissionsGuardV2 is zero-trust in enforce mode; @Public, @AllowSelfService, @RequirePermissions, @RequireAnyPermission all handled; fail-closed on uncovered routes in enforce mode",
    "rbac/permissions.service.ts: RBAC V4 compile-time via ROLE_TEMPLATES; Redis cache with negative-TTL; singleflight coalescing; no raw SQL",
    "rbac/roles.controller.ts: all endpoints require 'users:manage_roles'; ParseUUIDPipe on :id params",
    "rbac/roles.service.ts: isSystem=true roles are immutable (403 on update/delete); templateKey immutable after creation; all Prisma queries parameterized",
    "reset-password/page.tsx correctly disables inputs when token is missing",
    "school-vacations.service.ts importFromOpenData: uses AbortController with 10s timeout on external fetch; URL is not user-controlled; zone/year validated by DTO",
    "settings/page.tsx: isAdmin evaluated only after AuthProvider ensures permissionsLoaded=true, no flash redirect",
    "taskId and projectId in CreateTimeEntryDto use @IsUUID() — correct",
    "tasks/[id]/page.tsx: handleSave, handleDelete, handleDependencyChange all have try/catch with user-facing error toasts",
    "tasks/__tests__/page.test.tsx: test file exists and covers multiple interaction flows",
    "tasks/dto/add-dependency.dto.ts — dependsOnId validated with @IsUUID + @IsNotEmpty",
    "tasks/dto/assign-raci.dto.ts — userId validated with @IsUUID; role validated with @IsEnum(RACIRole)",
    "tasks/dto/create-subtask.dto.ts — title has @MinLength(1) + @MaxLength(500); isCompleted uses @IsBoolean; position uses @IsInt + @Min(0)",
    "tasks/dto/create-task.dto.ts — cross-field constraint ProjectRequiredWhenParentedConstraint correctly guards orphan+epic/milestone combinations",
    "tasks/dto/create-task.dto.ts — title has @MinLength(3) + @MaxLength(200); status/priority use @IsEnum; UUIDs validated with @IsUUID; dates validated with @IsDateString; times validated with @Matches regex",
    "tasks/dto/reorder-subtasks.dto.ts — subtaskIds validated as @IsArray + @IsUUID('4', { each: true })",
    "tasks/dto/update-task.dto.ts — extends PartialType; epicId/milestoneId/projectId redeclared without the cross-field check (intentional — documented in the file)",
    "tasks/page.tsx: handleStatusChange, handleDeleteTask, handleDateChange all have try/catch with user-facing error toasts",
    "tasks/tasks.controller.ts — all controller route handlers have @RequirePermissions(…); no @Public route present; JwtAuthGuard + PermissionsGuard applied globally via APP_GUARD",
    "tasks/tasks.service.ts — CSV export sanitizes the filename via replace(/[^a-zA-Z0-9-_]/g, '_'); Content-Disposition header uses the sanitized value",
    "tasks/tasks.service.ts — all Prisma queries use parameterized ORM calls; no $queryRawUnsafe or $executeRawUnsafe; no string-concatenated SQL",
    "tasks/tasks.service.ts — circular dependency detection uses an explicit BFS visited-set; no injection surface",
    "tasks/tasks.service.ts — findAll() applies accessScope.taskReadWhere(currentUser) RBAC filter; findOne() calls assertCanReadTask(); getTasksByProject() calls assertCanAccessProject()",
    "tasks/tasks.service.ts — getTasksByAssignee() enforces tasks:readAll permission when querying another user's tasks",
    "tasks/tasks.service.ts — no SSRF risk (no user-supplied URLs are fetched)",
    "tasks/tasks.service.ts — no file upload handling",
    "tasks/tasks.service.ts — no hardcoded secrets, tokens, or passwords in source",
    "tasks/tasks.service.ts — reorderSubtasks() / createSubtask() / updateSubtask() / deleteSubtask() all call assertCanReadTask() before mutations",
    "telework.service.ts findAll: non-readAll users are scoped to their own userId — no horizontal IDOR on list endpoint",
    "telework/page.tsx fetchTeleworkData useCallback has correct deps [currentMonth, selectedUserId, tc]",
    "third-parties/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "time-tracking.service.ts findAll: strict ForbiddenException if userId filter targets another user without time_tracking:view_any permission",
    "useAuthBootstrap correctly fetches /auth/me + /auth/me/permissions in parallel and is the source of truth",
    "usePermissions hook reads from server-hydrated permissions array, not from client-computed role checks",
    "users/dto/avatar-preset.dto.ts: @IsIn(VALID_PRESETS) — whitelist-only validation",
    "users/dto/change-password.dto.ts: @IsString on currentPassword, @IsStrongPassword on newPassword",
    "users/dto/create-user.dto.ts: @IsEmail, @MaxLength(254) on email; @IsUUID on departmentId and serviceIds; @Matches(AVATAR_URL_PATTERN) on avatarUrl; @IsStrongPassword on password; isActive not in CreateUserDto (SEC-011)",
    "users/dto/import-users.dto.ts: @IsEmail, @MaxLength(254) on email; @IsStrongPassword on password; @ValidateNested + @Type for nested validation",
    "users/dto/reset-password.dto.ts (users): @IsStrongPassword on newPassword",
    "users/dto/update-user.dto.ts: extends CreateUserDto via PartialType; isActive explicitly declared with @IsBoolean; password @IsStrongPassword",
    "users/users.controller.ts: all routes have @RequirePermissions or @AllowSelfService; ParseUUIDPipe on all :id params; avatar upload is throttled",
    "users/users.service.ts: horizontal scope enforced via accessScope.assertCanManageUser on update/remove; role hierarchy enforced via roleHierarchy.assertCanAssignRole on all role assignments; bcrypt cost=12; no $queryRawUnsafe or string-concatenated SQL; $queryRaw uses parameterized template literals; avatar path validated via AVATAR_URL_PATTERN before DB write; magic-byte check via assertMagicBytes; avatar filename constructed server-side from userId, never from user-supplied filename",
    "withAccessControl HOC correctly renders null when permissions are absent"
  ]
}
