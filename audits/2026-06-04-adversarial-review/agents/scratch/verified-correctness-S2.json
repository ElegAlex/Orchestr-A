{
  "dimension": "correctness",
  "shard": "S2",
  "confirmed": [
    {
      "id": "correctness-S2-1",
      "severity": "important",
      "category": "correctness",
      "subcategory": "date-time",
      "title": "captureSnapshots() uses server-local midnight instead of UTC midnight for deduplication query",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1158-1166",
      "code_evidence": "    const now = new Date();\n    const startOfDay = new Date();\n    startOfDay.setHours(0, 0, 0, 0);\n\n    // Batch-fetch existing snapshots for today (1 query)\n    const existingToday = await this.prisma.projectSnapshot.findMany({\n      where: {\n        projectId: { in: projects.map((p) => p.id) },\n        date: startOfDay,\n      },",
      "description": "The schema comment on ProjectSnapshot explicitly states `date stored as startOfDay (midnight UTC)` (@@unique([projectId, date]) // COR-014: DB-level race guard; date stored as startOfDay (midnight UTC)). The deduplication lookup uses `startOfDay.setHours(0, 0, 0, 0)` which sets to server local midnight, not UTC midnight. If the prod server is not in UTC (or its TZ env differs), the `date: startOfDay` query will never match existing rows, causing the `alreadySnapshotted` set to always be empty. This defeats the deduplication check, and every invocation of captureSnapshots() on non-UTC servers will try to insert a new snapshot (relying only on the DB unique constraint's skipDuplicates guard), meaning `captured` always returns 0 (all are skipped by skipDuplicates) but the dedup logic is silently broken.",
      "root_cause": "`setHours(0,0,0,0)` resets to local timezone midnight, not UTC midnight, while PostgreSQL stores and compares DateTime columns in UTC.",
      "impact": "On non-UTC servers (e.g. Europe/Paris), the lookup timestamp will be off by 1-2 hours; existing snapshots are never found in the dedup query; every call attempts insertion and is silently skipped by skipDuplicates. The design intent (COR-014) is violated.",
      "suggested_fix": "Replace `startOfDay.setHours(0, 0, 0, 0)` with UTC-anchored midnight: `startOfDay.setUTCHours(0, 0, 0, 0)`. Also use `setUTCHours` for `now` if it is used for the same day boundary. This aligns the dedup query with what PostgreSQL stored.",
      "acceptance_criteria": [
        "On a server with TZ=Europe/Paris, running captureSnapshots() twice in the same day returns {captured: 0} on the second call (dedup works)",
        "The date stored in project_snapshots equals the UTC midnight of the current day"
      ],
      "verification_command": "grep -n 'setHours\\|setUTCHours' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED — line 1160 uses setHours(0,0,0,0); schema comment at schema.prisma:229 explicitly states 'midnight UTC'. No UTC normalization found elsewhere in captureSnapshots(). Prod server noted as UTC (MEMORY) but code is fragile and violates documented convention."
    },
    {
      "id": "correctness-S2-2",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "update() does not validate partial date changes against existing project dates",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "583-588",
      "code_evidence": "    // Vérifier les dates si fournies\n    if (startDate && endDate && new Date(endDate) < new Date(startDate)) {\n      throw new BadRequestException(\n        'La date de fin doit être postérieure ou égale à la date de début',\n      );\n    }",
      "description": "The date invariant (endDate >= startDate) is only enforced when both `startDate` and `endDate` are simultaneously provided in the PATCH request. The existing project's dates (in `existingProject`) are loaded at line 558-560 but are never used in the validation. A PATCH with only `endDate: '2020-01-01'` on a project with `startDate: '2025-01-01'` passes validation and stores an end date before the start date, violating the core scheduling invariant. Similarly, a PATCH with only `startDate: '2030-01-01'` on a project with `endDate: '2025-01-01'` also bypasses validation.",
      "root_cause": "The date comparison condition requires both `startDate && endDate` to be truthy, but partial updates only supply one; the check is never evaluated against the existing persisted value.",
      "impact": "Projects can end up with endDate < startDate after a partial PATCH, corrupting scheduling logic, analytics (date ranges), and milestone overdue calculations that depend on this invariant.",
      "suggested_fix": "Compute the effective start and end: `const effectiveStart = startDate ? new Date(startDate) : existingProject.startDate; const effectiveEnd = endDate ? new Date(endDate) : existingProject.endDate;` Then validate `if (effectiveEnd && effectiveStart && effectiveEnd < effectiveStart) throw BadRequestException(...)`.",
      "acceptance_criteria": [
        "PATCH /projects/:id with only endDate='2020-01-01' on a project with startDate='2025-01-01' returns 400",
        "PATCH /projects/:id with only startDate='2030-01-01' on a project with endDate='2025-01-01' returns 400",
        "PATCH /projects/:id with both valid dates in correct order returns 200"
      ],
      "verification_command": "grep -n 'startDate && endDate' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED — existingProject is fetched at line 558 but existingProject.startDate/endDate are not referenced in the date validation block at line 584. The guard fires only when both startDate and endDate are truthy in the PATCH payload."
    },
    {
      "id": "correctness-S2-3",
      "severity": "important",
      "category": "correctness",
      "subcategory": "transaction-boundary",
      "title": "hardDelete(): audit log written outside transaction — audit records 'deleted' even if delete fails",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "855-883",
      "code_evidence": "    // Final snapshot to the audit trail BEFORE the row is erased (audit\n    // suggested-fix b). Plain await — not transactional with the delete — to\n    // match the existing archive()/unarchive() emission pattern; the audit\n    // pipeline (AuditPersistenceService) is out of DAT-007 scope.\n    await this.auditPersistence.log({\n      action: AuditAction.PROJECT_DELETED,\n      entityType: 'Project',\n      entityId: id,\n      actorId: user.id,\n      payload: {\n        snapshot: {\n          id: project.id,",
      "description": "The PROJECT_DELETED audit event is emitted and awaited before `prisma.project.delete()`. If the delete fails (e.g., a new FK constraint is added between the dependency check and the delete, or any unexpected DB error occurs), the audit log contains a 'deleted' record for a project that still exists. The comment explicitly acknowledges this: 'Plain await — not transactional with the delete'. This is a known trade-off documented in code, but it creates an inconsistency: the immutable audit trail claims the project was deleted while it remains in the DB.",
      "root_cause": "Audit log and delete are two separate non-transactional operations; failure of the second leaves the audit trail in a false state.",
      "impact": "The audit trail, which is supposed to be an immutable record for compliance purposes, will contain a PROJECT_DELETED entry for a project that still exists if the delete fails. Compliance audits relying on this trail will be incorrect.",
      "suggested_fix": "Wrap both `auditPersistence.log` and `prisma.project.delete` in a single `$transaction`. If the audit service does not support transactional context, emit the audit event AFTER the delete succeeds. Alternatively, treat the audit as a best-effort post-delete step and explicitly document this as an accepted risk.",
      "acceptance_criteria": [
        "If prisma.project.delete() throws, no PROJECT_DELETED audit record should exist for that project",
        "Or: clear documentation that this is an accepted risk with compensating controls"
      ],
      "verification_command": "grep -n 'auditPersistence.log\\|project.delete' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED with downgraded confidence (high to medium). The code comment explicitly acknowledges the non-transactional pattern as intentional and scoped out of DAT-007. The failure window is narrow (dependency check runs first, then audit, then delete). However the compliance implication is real — a failed delete after audit write produces a false PROJECT_DELETED record in the immutable chain. The inline comment does not constitute a formally accepted-risk decision documented outside the code."
    },
    {
      "id": "correctness-S2-4",
      "severity": "important",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "importMilestones(): per-row findFirst+create is a TOCTOU race with no uniqueness guarantee",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "193-231",
      "code_evidence": "    for (let i = 0; i < milestones.length; i++) {\n      const milestoneData = milestones[i];\n      const lineNum = i + 2; // +2 car ligne 1 = header, index commence à 0\n\n      try {\n        // Vérifier que le nom n'existe pas déjà dans le projet\n        const existingMilestone = await this.prisma.milestone.findFirst({\n          where: {\n            projectId,\n            name: milestoneData.name,\n          },\n        });\n\n        if (existingMilestone) {\n          result.skipped++;\n",
      "description": "The import loop performs a `findFirst` followed by a `create` in separate, non-transactional DB calls for each milestone. If two concurrent import requests are in flight simultaneously (or two rows in the same batch with identical names), both may pass the `findFirst` check (finding no existing milestone) and both attempt `create`. The `create` would then fail with a DB constraint violation (if a unique constraint on (projectId, name) exists) or silently succeed creating a duplicate (if no such constraint exists). The catch block at line 226-231 would catch the constraint error and count it as `errors`, not `skipped`, producing misleading stats. Also, the sequential per-row loop performs N times 2 DB queries for N milestones instead of a batch approach.",
      "root_cause": "No transactional read-then-write or upsert pattern; the check-then-act is separated in time.",
      "impact": "Concurrent imports may create duplicate milestones (no DB unique constraint on (projectId, name) exists per schema.prisma lines 284-299) or report misleading error counts. Performance degrades linearly with import size.",
      "suggested_fix": "Pre-fetch all existing milestone names for the project in one query before the loop (like validateImport does at line 268-272), then use `createMany` with `skipDuplicates: true` for the batch. Or use `upsert` per row. This eliminates the TOCTOU window.",
      "acceptance_criteria": [
        "Two concurrent POST /milestones/project/:id/import requests with the same milestone names do not create duplicates",
        "Import of N milestones performs O(1) DB queries for the existence check, not O(N)"
      ],
      "verification_command": "grep -n 'findFirst\\|createMany\\|skipDuplicates' apps/api/src/milestones/milestones.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED and severity note added. The Milestone schema (schema.prisma lines 284-299) has NO @@unique([projectId, name]) constraint — only @@index([projectId]). This means duplicate milestones by name CAN be silently inserted with no DB-level guard. The TOCTOU race is unprotected at both application and DB layers. validateImport() correctly pre-fetches names before the loop but importMilestones() does not follow the same pattern."
    },
    {
      "id": "correctness-S2-5",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "epics.service update()/remove(): double-read TOCTOU — epic fetched in membership check then again in findOne()",
      "file": "apps/api/src/epics/epics.service.ts",
      "line": "78-99",
      "code_evidence": "    if (currentUserId) {\n      await this.assertProjectMembership(id, currentUserId, currentUserRole);\n    }\n    await this.findOne(id);\n    return this.prisma.epic.update({\n      where: { id },\n      data: updateEpicDto,\n      include: { project: { select: { id: true, name: true } } },\n    });",
      "description": "The `update()` and `remove()` methods each make 3 separate DB calls: (1) `assertProjectMembership` fetches the epic (with project+members), (2) `findOne` fetches the epic again, (3) the actual update/delete. Between calls 1 and 3, another actor could delete the epic, causing the final update/delete to fail silently or with an unhandled P2025 Prisma error. The P2025 'Record not found' from `prisma.epic.update()` where the row was deleted between the access check and the update would surface as an unhandled internal server error rather than a clean 404.",
      "root_cause": "Authorization check, existence check, and mutation are three separate non-transactional operations with a TOCTOU window.",
      "impact": "A concurrent deletion between the membership/existence checks and the actual update/delete produces an unhandled Prisma P2025 error instead of a clean 404 NotFoundException, resulting in a 500 internal server error.",
      "suggested_fix": "Catch P2025 Prisma errors in update/delete and convert to NotFoundException. Alternatively, consolidate into a single transaction that reads and writes atomically. The membership check could be done once, and the findOne result reused instead of re-fetching.",
      "acceptance_criteria": [
        "If an epic is deleted by another actor between the membership check and the update, the API returns 404, not 500"
      ],
      "verification_command": "grep -n 'findOne\\|assertProjectMembership\\|epic.update\\|epic.delete' apps/api/src/epics/epics.service.ts",
      "related_findings": ["correctness-S2-6"],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED. Code verbatim at lines 78-99. AllExceptionsFilter (all-exceptions.filter.ts lines 57-68) passes HttpException through and returns 500 for all other errors — no P2025 to 404 mapping exists anywhere in the codebase. The race window is real; severity remains nit because concurrent epic deletion between these two reads is rare in practice."
    },
    {
      "id": "correctness-S2-6",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "milestones.service update()/remove(): same double-read TOCTOU as epics — unhandled P2025 on concurrent delete",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "102-135",
      "code_evidence": "    if (currentUserId) {\n      await this.assertProjectMembership(id, currentUserId, currentUserRole);\n    }\n    await this.findOne(id);\n    const { dueDate, ...data } = updateMilestoneDto;\n\n    return this.prisma.milestone.update({\n      where: { id },\n      data: {\n        ...data,\n        ...(dueDate && { dueDate: new Date(dueDate) }),\n      },\n      include: { project: { select: { id: true, name: true } } },\n    });",
      "description": "Same triple-fetch TOCTOU pattern as epics.service.ts: assertProjectMembership fetches the milestone (with project+members), findOne fetches it again, then update/delete runs. A concurrent delete between calls 1 and 3 would cause prisma.milestone.update() or delete() to throw an unhandled P2025, surfacing as a 500 instead of a 404.",
      "root_cause": "Authorization check, existence check, and mutation are three separate non-transactional operations with a TOCTOU window.",
      "impact": "A concurrent deletion between checks and the actual mutation produces an unhandled Prisma P2025 error instead of a clean 404, resulting in a 500 internal server error.",
      "suggested_fix": "Same as correctness-S2-5: catch P2025 and map to NotFoundException, or consolidate the reads.",
      "acceptance_criteria": [
        "If a milestone is deleted by another actor between the membership check and the update, the API returns 404, not 500"
      ],
      "verification_command": "grep -n 'findOne\\|assertProjectMembership\\|milestone.update\\|milestone.delete' apps/api/src/milestones/milestones.service.ts",
      "related_findings": ["correctness-S2-5"],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED. Code verbatim at lines 102-135. Same AllExceptionsFilter analysis applies — no P2025 to 404 mapping. complete() at lines 162-168 also has a double-fetch (findOne then update) without P2025 handling."
    },
    {
      "id": "correctness-S2-7",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "captureSnapshots() uses hardcoded string literal 'ACTIVE' instead of ProjectStatus enum",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1147",
      "code_evidence": "    const projects = await this.prisma.project.findMany({\n      where: { status: 'ACTIVE' },",
      "description": "The `captureSnapshots` method uses the raw string `'ACTIVE'` instead of the `ProjectStatus.ACTIVE` enum imported at line 21. While Prisma accepts strings for enum fields, this bypasses TypeScript's compile-time enum safety: if `ProjectStatus.ACTIVE` were ever renamed or restructured, this hardcoded string would silently stop matching any projects, capturing zero snapshots forever with no type error. The rest of the codebase consistently uses `ProjectStatus.*` enum values for status comparisons.",
      "root_cause": "Direct string literal used instead of the imported `ProjectStatus` enum.",
      "impact": "Silent snapshot collection failure if the enum value changes; inconsistency with codebase convention; TypeScript type safety not leveraged.",
      "suggested_fix": "Replace `{ status: 'ACTIVE' }` with `{ status: ProjectStatus.ACTIVE }`. The `ProjectStatus` enum is already imported at line 21.",
      "acceptance_criteria": [
        "captureSnapshots() uses ProjectStatus.ACTIVE in the where clause",
        "No raw string 'ACTIVE' for status filtering in projects.service.ts"
      ],
      "verification_command": "grep -n \"status: 'ACTIVE'\" apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED. Line 1147 verbatim: `where: { status: 'ACTIVE' }`. ProjectStatus enum imported at line 21 as `import { ProjectStatus, TaskStatus } from 'database'`. All other status references in the file use ProjectStatus.* (lines 122, 568, 683). This is a genuine inconsistency with no compensating mechanism."
    },
    {
      "id": "correctness-S2-8",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "milestones complete() bypasses project membership authorization check",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "162-168",
      "code_evidence": "  async complete(id: string) {\n    await this.findOne(id);\n    return this.prisma.milestone.update({\n      where: { id },\n      data: { status: MilestoneStatus.COMPLETED },\n    });\n  }",
      "description": "The `complete()` method does not perform an `assertProjectMembership` check, unlike `update()` and `remove()`. Any authenticated user with the `milestones:update` permission (required at the controller level) can mark any milestone as COMPLETED regardless of whether they are a member of the project that owns the milestone. The `update()` and `remove()` methods both enforce membership; `complete()` should do the same.",
      "root_cause": "The membership guard was added to update() and remove() but not to complete(), creating an inconsistency in authorization enforcement.",
      "impact": "A user in project A with milestones:update permission can complete milestones belonging to project B, even if they have no membership in project B.",
      "suggested_fix": "Add the same pattern as update()/remove(): accept currentUserId and currentUserRole parameters, call assertProjectMembership() before the update. Also update the controller to pass the current user's id and role code.",
      "acceptance_criteria": [
        "POST /milestones/:id/complete by a user not in the milestone's project returns 403",
        "POST /milestones/:id/complete by a member of the project returns 200"
      ],
      "verification_command": "grep -n 'complete\\|assertProjectMembership' apps/api/src/milestones/milestones.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED. Controller line 116: `complete(@Param('id', ParseUUIDPipe) id: string)` — no @CurrentUser injected, no user context passed to service. Service complete() at lines 162-168 has no assertProjectMembership call. update() and remove() both enforce membership. The authorization bypass is real and consistent across service and controller layers."
    }
  ],
  "refuted": []
}
