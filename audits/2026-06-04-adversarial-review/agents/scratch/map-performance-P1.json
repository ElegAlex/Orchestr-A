{
  "dimension": "performance",
  "shard": "P1",
  "commit": "0997b30e2d44289239d5ba5d8f4f463d58748ad0",
  "findings": [
    {
      "id": "performance-P1-1",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded-page-size",
      "title": "Default and max page size of 1000 on list endpoints (projects, epics, milestones)",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "231-238",
      "code_evidence": "  async findAll(\n    page = 1,\n    limit = 1000,\n    status?: ProjectStatus,\n    userId?: string,\n    userRole?: string,\n    clients?: string,\n    archived: ArchivedFilter = ArchivedFilter.ACTIVE,\n  ) {\n    const safeLimit = Math.min(limit || 1000, 1000);",
      "description": "All three list endpoints (projects, epics, milestones) default to limit=1000 and cap at 1000. A single unauthenticated-but-permissioned request can transfer thousands of rows with their full include tree (manager, sponsor, members×5, clients, _count, tasks-for-progress). For the projects list each row includes up to 5 full member objects and a per-project client array. With 1000 projects this easily exceeds 1 MB of JSON per response.",
      "root_cause": "Default parameter `limit = 1000` and `Math.min(limit || 1000, 1000)` sets the ceiling equal to the default rather than a fraction of it.",
      "impact": "Large deployments (≥ 200 projects) will produce payloads of several hundred KB to multiple MB per request, increasing memory pressure on the API process and network latency for clients. The frontend fetches the full list on the project dashboard without ever supplying a limit, so the default 1000 is always used in practice.",
      "suggested_fix": "Reduce default limit to 20 and cap to 100: `limit = 20`, `const safeLimit = Math.min(limit || 20, 100)`. Apply the same change to `epics.service.ts` (line 31) and `milestones.service.ts` (line 59). If the frontend needs all projects for a dropdown, introduce a separate lightweight endpoint that returns only id+name.",
      "acceptance_criteria": [
        "GET /projects without ?limit returns at most 20 rows",
        "GET /projects?limit=10000 is capped to 100",
        "Same constraint applied to GET /epics and GET /milestones"
      ],
      "verification_command": "curl -s -H 'Authorization: Bearer <token>' 'http://localhost:3000/api/projects' | jq '.meta.limit'",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": "Same pattern in epics.service.ts:31 and milestones.service.ts:59 (both default+cap at 1000)."
    },
    {
      "id": "performance-P1-2",
      "severity": "important",
      "category": "performance",
      "subcategory": "n+1-query",
      "title": "importUsers executes N sequential DB round-trips (findFirst + bcrypt + create) per row",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1301-1446",
      "code_evidence": "    for (let i = 0; i < users.length; i++) {\n      const userData = users[i];\n      const rowNum = i + 2;\n\n      try {\n        const existingUser = await this.prisma.user.findFirst({\n          where: {\n            // DAT-015: case-insensitive lookup\n            OR: [\n              { email: { equals: userData.email, mode: 'insensitive' } },\n              { login: { equals: userData.login, mode: 'insensitive' } },\n            ],\n          },\n        });",
      "description": "For every row in the import array the loop performs: (1) a `user.findFirst` duplicate check, (2) a `bcrypt.hash` (cost 12, ~200-400ms), (3) a `user.create`, (4) optionally a `userService.createMany`. For a 100-row import this is 100 sequential bcrypt hashes (20-40s total CPU) plus 200-300 sequential Prisma round-trips over a single HTTP request. There is no batch size limit on the import DTO.",
      "root_cause": "The import loop is fully sequential (`await` inside a `for` loop) and performs per-row duplicate checking instead of a pre-flight bulk check. No batch ceiling on the input array.",
      "impact": "A 100-user import occupies an API worker thread for 20-40 seconds, blocking the event loop and starving concurrent requests. An attacker with the `users:import` permission can submit a large array to amplify CPU load. The HTTP request will also time out under nginx's default 60s proxy_read_timeout for large imports.",
      "suggested_fix": "Pre-flight: batch-resolve all duplicate emails/logins with a single `findMany({ where: { OR: [{ email: { in: emails } }, { login: { in: logins } }] } })` before the loop. Move bcrypt hashing to a worker thread pool (piscina / worker_threads) or reduce the cost factor for bulk operations. Add a `@Max(100)` constraint on `ImportUsersDto.users`. Use `createMany` for the non-duplicate rows.",
      "acceptance_criteria": [
        "POST /users/import with 100 rows completes in < 15s",
        "POST /users/import with > 100 rows returns 400",
        "Duplicate detection still works correctly after the batching refactor"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": "The validateImport method (line 1454) wisely pre-fetches all existing users at once (line 1479-1481); the same pattern should be applied to importUsers."
    },
    {
      "id": "performance-P1-3",
      "severity": "important",
      "category": "performance",
      "subcategory": "n+1-query",
      "title": "importMilestones executes N sequential findFirst + create per milestone",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "193-224",
      "code_evidence": "    for (let i = 0; i < milestones.length; i++) {\n      const milestoneData = milestones[i];\n      const lineNum = i + 2; // +2 car ligne 1 = header, index commence à 0\n\n      try {\n        // Vérifier que le nom n'existe pas déjà dans le projet\n        const existingMilestone = await this.prisma.milestone.findFirst({\n          where: {\n            projectId,\n            name: milestoneData.name,\n          },\n        });",
      "description": "For each milestone in the import batch the loop makes a `milestone.findFirst` duplicate check followed by a `milestone.create`, producing 2N sequential database round-trips. The `validateImport` method correctly pre-fetches all existing names into a Set (line 268-274), but the actual `importMilestones` does not reuse this pattern.",
      "root_cause": "The `importMilestones` loop was written without the pre-batch optimization used by `validateImport`; no max batch size is enforced.",
      "impact": "A 200-milestone import makes 400 sequential DB round-trips. Under a ~1ms pg latency this adds ~400ms of unnecessary serialization; under higher latency (cloud, docker network) the overhead multiplies.",
      "suggested_fix": "Pre-fetch existing milestone names with a single `findMany` before the loop (mirror `validateImport` line 268-274), build a Set, then use `createMany` for new milestones in a single batch. Add a `@Max(500)` decorator on `ImportMilestonesDto.milestones`.",
      "acceptance_criteria": [
        "POST /milestones/project/:id/import with 200 milestones makes at most 3 DB queries (findUnique project, findMany existing, createMany)",
        "Duplicate detection by name still works"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": null
    },
    {
      "id": "performance-P1-4",
      "severity": "important",
      "category": "performance",
      "subcategory": "redundant-db-query",
      "title": "assertCanAccessProject issues two project.count queries for the same projectId",
      "file": "apps/api/src/common/services/access-scope.service.ts",
      "line": "85-98",
      "code_evidence": "  async assertCanAccessProject(\n    projectId: string,\n    user: AccessUser | undefined,\n    bypassPermissions: readonly string[] = ['projects:manage_any'],\n  ): Promise<void> {\n    const projectExists = await this.prisma.project.count({\n      where: { id: projectId },\n    });\n    if (projectExists === 0) throw new NotFoundException('Projet introuvable');\n\n    if (!(await this.canAccessProject(projectId, user, bypassPermissions))) {\n      throw new ForbiddenException('Accès projet non autorisé');",
      "description": "`assertCanAccessProject` first executes `project.count({ where: { id: projectId } })` to check existence, then calls `canAccessProject` which immediately executes a second `project.count({ where: { id: projectId, ...projectAccessWhere(user) } })`. For privileged callers (who have `projects:manage_any`) the second count is skipped, but for all other callers two round-trips hit the projects table for every guarded endpoint. This pattern fires on `getProjectStats`, `getSnapshots`, `findOne`, and the milestones/epics ownership assertion path.",
      "root_cause": "Existence check and access check are two separate `count` queries instead of a single combined query whose result encodes both.",
      "impact": "Every call to a project-scoped endpoint by a non-admin user produces 2 project count queries instead of 1, doubling the DB overhead for the access-gate path.",
      "suggested_fix": "Merge both counts into one: if `canAccessProject` returns false, distinguish 404 from 403 by running a bare `project.count({ where: { id: projectId } })` only on that failure path. Alternatively, `assertCanAccessProject` can run a single `project.count` with the combined `AND [{ id: projectId }, projectAccessWhere(user)]` condition; a zero result maps to 404 only when a bare-id count is also zero (one extra query only in the error path).",
      "acceptance_criteria": [
        "A successful GET /projects/:id by a CONTRIBUTEUR triggers exactly 1 project DB query for the access gate (verifiable via Prisma query log)",
        "404 is still returned for a non-existent id",
        "403 is still returned for an existing id outside the user's scope"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": "Same double-query pattern exists in assertCanManageUser (access-scope.service.ts line 282-294) for the users module."
    },
    {
      "id": "performance-P1-5",
      "severity": "important",
      "category": "performance",
      "subcategory": "n+1-query",
      "title": "assertProjectMembership in EpicsService fetches all project members (include: { project: { include: { members: true } } })",
      "file": "apps/api/src/epics/epics.service.ts",
      "line": "115-123",
      "code_evidence": "    const epic = await this.prisma.epic.findUnique({\n      where: { id: epicId },\n      include: { project: { include: { members: true } } },\n    });\n    if (!epic) throw new NotFoundException('Epic introuvable');\n\n    const isMember = epic.project.members.some((m) => m.userId === userId);",
      "description": "To check if a user is a member of an epic's project, the guard fetches ALL ProjectMember rows for that project (unbounded) and filters in-memory. A project with 100 members transfers 100 rows over the wire when a simple `count` with a WHERE clause would suffice. The same pattern is replicated in `milestones.service.ts` at line 150-158.",
      "root_cause": "`include: { members: true }` fetches the entire members relation instead of using a targeted `project.count` or `projectMember.count` with a `{ projectId, userId }` filter.",
      "impact": "For every epic/milestone update or delete by a non-admin, the API fetches all project members. On a project with 50 members this transfers ~50x more data than necessary and prevents index-only lookups on the ProjectMember composite key.",
      "suggested_fix": "Replace the include+in-memory filter with a direct membership count:\n```ts\nconst memberCount = await this.prisma.projectMember.count({\n  where: { projectId: epic.projectId, userId },\n});\nif (memberCount === 0) throw new ForbiddenException('Not a member of this project');\n```\nThis requires reading `epic.projectId` from a separate slim query first, or including only `{ project: { select: { id: true } } }` in the epic lookup.",
      "acceptance_criteria": [
        "PATCH /epics/:id makes at most 3 DB queries for a non-admin caller (permissions lookup, epic select, membership count)",
        "Same fix applied to milestones.service.ts assertProjectMembership"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": "milestones.service.ts line 150-158 has the identical issue: `include: { project: { include: { members: true } } }`."
    },
    {
      "id": "performance-P1-6",
      "severity": "important",
      "category": "performance",
      "subcategory": "redundant-db-query",
      "title": "assertCanAssignRole executes 4 DB queries when the caller is not ADMIN (resolveTemplateKey called twice in assertCanAssignRole + twice again in canAssignRole)",
      "file": "apps/api/src/common/services/role-hierarchy.service.ts",
      "line": "83-103",
      "code_evidence": "  async assertCanAssignRole(\n    callerRoleCode: string | null | undefined,\n    targetRoleCode: string | null | undefined,\n  ): Promise<void> {\n    if (!callerRoleCode || !targetRoleCode) return;\n    const [targetTemplateKey, callerTemplateKey] = await Promise.all([\n      this.resolveTemplateKey(targetRoleCode),\n      this.resolveTemplateKey(callerRoleCode),\n    ]);\n    if (targetTemplateKey === 'ADMIN' && callerTemplateKey !== 'ADMIN') {\n      throw new ForbiddenException(\n        'Seul un administrateur peut cibler un rôle rattaché au template ADMIN',\n      );\n    }\n    if (callerTemplateKey === 'ADMIN') return;\n    if (!(await this.canAssignRole(callerRoleCode, targetRoleCode))) {",
      "description": "`assertCanAssignRole` resolves both role templateKeys (2 DB queries), then if neither is ADMIN it calls `canAssignRole(callerRoleCode, targetRoleCode)` which resolves the same two templateKeys again (2 more DB queries). Net result: 4 `role.findUnique` calls for the non-ADMIN path. This fires on every user create, update, import row, and password reset. `resolveTemplateKey` has no cache.",
      "root_cause": "`canAssignRole` is a public method that does its own template resolution; `assertCanAssignRole` cannot pass the already-resolved keys to it.",
      "impact": "Each of the 4 DB queries is cheap individually but: (a) during `importUsers` this multiplies by N rows, (b) on every `POST /users` or `PATCH /users/:id` it adds 4 sequential round-trips to the critical path.",
      "suggested_fix": "Extract a private `_canAssignRoleFromKeys(callerTplKey, targetTplKey): boolean` that takes pre-resolved keys, and have both `canAssignRole` and `assertCanAssignRole` call it after the resolution step. Alternatively, add simple in-process memoization (Map<code, templateKey>) since templateKeys are stable for the lifetime of a request.",
      "acceptance_criteria": [
        "POST /users triggers exactly 2 role.findUnique calls for the hierarchy check (not 4)",
        "POST /users/import with 100 rows triggers 200 role.findUnique calls at most (2 per row), not 400"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": null
    },
    {
      "id": "performance-P1-7",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded-fetch",
      "title": "getProjectsByUser fetches ALL tasks for every project to compute progress (no take limit)",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1109-1113",
      "code_evidence": "        tasks: {\n          select: {\n            status: true,\n          },\n        },",
      "description": "`getProjectsByUser` includes all tasks of each project with no `take` limit, just to compute a `progress` percentage in JS. A user who is a member of 20 projects each with 500 tasks will cause 10,000 task rows to be transferred from the DB to the API process per request, then discarded after counting. The `findAll` endpoint has a similar issue — it solved it with a `groupBy` (comment `PER-005`) but `getProjectsByUser` was not updated with the same fix.",
      "root_cause": "Progress computation is done by fetching all task rows and filtering in memory, rather than using a `groupBy` aggregation query.",
      "impact": "For users with many projects containing many tasks, the response payload and DB-to-API transfer can reach hundreds of KB per request. The endpoint is called on the dashboard load for every user.",
      "suggested_fix": "Apply the same `groupBy` pattern as `findAll` (lines 357-379): run a single `task.groupBy({ by: ['projectId', 'status'], where: { projectId: { in: projectIds } }, _count: { _all: true } })` after the project query, then map progress from the result. Remove the `tasks: { select: { status: true } }` include.",
      "acceptance_criteria": [
        "GET /projects/user/:id triggers at most 2 DB queries for the project list (findMany + groupBy), regardless of task count",
        "Progress percentage is identical to the findAll endpoint for the same projects"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": null
    },
    {
      "id": "performance-P1-8",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded-fetch",
      "title": "getProjectStats fetches all tasks and all members without select projection on members",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1276-1288",
      "code_evidence": "        tasks: {\n          select: {\n            id: true,\n            status: true,\n            estimatedHours: true,\n            priority: true,\n          },\n        },\n        members: true,\n        epics: {\n          select: {\n            progress: true,\n          },\n        },",
      "description": "`getProjectStats` includes `members: true` (no select clause), which returns ALL columns of ALL ProjectMember rows. The only field consumed is `project.members.length` (line 1371). Additionally, `tasks` is fetched unbounded (no `take` limit) to compute statistics. For a project with 1000 tasks and 50 members this transfers the full member records (allocation, startDate, endDate, role string) unnecessarily.",
      "root_cause": "`members: true` was used as a convenience; only the count is needed. `tasks` is also over-fetched since only counts per-status and sum of hours are needed.",
      "impact": "Every call to GET /projects/:id/stats over-fetches member columns and potentially large task arrays, increasing DB-to-API data transfer and JSON serialization time.",
      "suggested_fix": "Replace `members: true` with `_count: { select: { members: true } }` and use `project._count.members`. Replace the tasks include with a `task.groupBy` + a separate `task.aggregate` for `_sum: { estimatedHours: true }`, and fetch actual hours from `timeEntry.aggregate` with `_sum: { hours: true }` using the task IDs filter.",
      "acceptance_criteria": [
        "GET /projects/:id/stats does not return member data in the Prisma query response",
        "Response payload and computed values are identical before and after the fix"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": null
    },
    {
      "id": "performance-P1-9",
      "severity": "nit",
      "category": "performance",
      "subcategory": "cache-stale-risk",
      "title": "CacheService uses TTL-only eviction — no mutation invalidation for analytics endpoints",
      "file": "apps/api/src/common/services/cache.service.ts",
      "line": "1-59",
      "code_evidence": " * - TTL-only eviction (no mutation-bust hooks) within this initial bounded scope\n *   (AC#6 forbids touching mutation service paths in this commit).",
      "description": "The `CacheService` design comment explicitly documents that it relies on TTL-only eviction. For analytics and report endpoints this means cached results can be stale for up to the configured TTL after mutations (new project, task status change, etc.). The comment references AC#6 as the blocking constraint. If the TTL is set to several minutes, dashboard metrics can lag significantly.",
      "root_cause": "Intentional design trade-off documented in the code, with a forward reference to a future fix that has not yet been applied.",
      "impact": "Analytics metrics (project counts, task stats) shown to users may be stale by the full TTL duration after mutations. For a deployed system with active users, this degrades trust in the dashboard.",
      "suggested_fix": "Add targeted cache invalidation calls (`cacheService.del(key)`) in mutation service methods (e.g., in `ProjectsService.create/update/archive`). The key schema must include the userId scope to match the key format used at read time. Alternatively, reduce the TTL from minutes to 30s for endpoints where freshness matters.",
      "acceptance_criteria": [
        "Creating a project while the analytics cache is populated causes the next analytics request to return updated data (not the stale cached value)"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "medium",
      "notes": "CacheService is used by analytics/planning endpoints (not directly in the scanned paths), but the design is defined here. The scanned modules do not use CacheService directly."
    },
    {
      "id": "performance-P1-10",
      "severity": "nit",
      "category": "performance",
      "subcategory": "redundant-db-query",
      "title": "JwtStrategy.validate makes a full user.findUnique on every authenticated request",
      "file": "apps/api/src/auth/strategies/jwt.strategy.ts",
      "line": "60-92",
      "code_evidence": "    const user = await this.prisma.user.findUnique({\n      where: { id: payload.sub },\n      select: {\n        id: true,\n        email: true,\n        login: true,\n        firstName: true,\n        lastName: true,\n        // RBAC V4 : relation vers table `roles`. Les guards/services\n        // consomment user.role.code + user.role.templateKey.\n        roleId: true,\n        role: {\n          select: {\n            id: true,\n            code: true,\n            label: true,\n            templateKey: true,\n            isSystem: true,\n          },\n        },",
      "description": "Every HTTP request (after JWT signature verification) triggers a `user.findUnique` with a JOIN on the `role` table to re-read active status, role, and the `forcePasswordChange` flag from the DB. This is intentional for `forcePasswordChange` freshness (the comment on line 83-88 explains this), but it adds 1 DB query to the critical path of every authenticated request. Under moderate load (100 req/s) this is 100 DB queries/s just for auth validation.",
      "root_cause": "Security requirement: `forcePasswordChange` and `isActive` must be DB-authoritative. However the full user profile (name, avatarUrl, avatarPreset, email, login) is also fetched but never needed by the guard itself.",
      "impact": "At 100 concurrent users making frequent API calls, ~100 DB round-trips per second are spent solely on auth validation fetches. The role JOIN doubles the data transfer vs. a minimal (id, isActive, forcePasswordChange, role.code, role.templateKey) select.",
      "suggested_fix": "Narrow the select to only the fields needed for auth decisions: `{ id, isActive, forcePasswordChange, roleId, role: { select: { code, templateKey } } }`. All other profile fields (firstName, lastName, avatarUrl, etc.) can be fetched lazily by `getProfile` only when the client explicitly requests them.",
      "acceptance_criteria": [
        "JwtStrategy.validate query fetches at most 6 columns (id, isActive, forcePasswordChange, roleId + role.code + role.templateKey)",
        "ForcePasswordChange guard still works correctly after the change"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": "This is a well-known trade-off in stateless JWT architectures; the finding is a nit because the security motivation is documented and the query is indexed on PK. The narrowed select would reduce data transfer by ~60%."
    },
    {
      "id": "performance-P1-11",
      "severity": "nit",
      "category": "performance",
      "subcategory": "unbounded-fetch",
      "title": "validateImport fetches ALL users (email+login) for duplicate detection — unbounded full-table scan",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1479-1481",
      "code_evidence": "      this.prisma.user.findMany({\n        select: { email: true, login: true },\n      }),",
      "description": "`validateImport` pre-fetches every user row (email + login) into memory to build Sets for duplicate detection. On a deployment with 10,000 users this loads 10,000 rows per dry-run validation call. While this is better than per-row queries (as done by `importUsers`), it does not scale. No `take` or `where` limit is applied.",
      "root_cause": "The pre-fetch pattern was chosen as an optimization over per-row queries but was not bounded.",
      "impact": "At 10,000+ users, the full-table scan of (email, login) adds significant data transfer on every import validation. Memory usage in the API process spikes to hold the full arrays before Set construction.",
      "suggested_fix": "Replace the full-table scan with targeted lookups: extract all unique emails and logins from the import batch, then query `user.findMany({ where: { OR: [{ email: { in: emails } }, { login: { in: logins } }] } })` to fetch only potential duplicates. This reduces the scan to at most `batch_size` matches rather than the full user table.",
      "acceptance_criteria": [
        "POST /users/import/validate with 50 users makes a single bounded query (WHERE email IN (...) OR login IN (...)) instead of a full-table findMany"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": null
    },
    {
      "id": "performance-P1-12",
      "severity": "nit",
      "category": "performance",
      "subcategory": "unbounded-fetch",
      "title": "captureSnapshots fetches ALL active projects with full task/milestone arrays — no pagination",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1145-1152",
      "code_evidence": "  async captureSnapshots() {\n    const projects = await this.prisma.project.findMany({\n      where: { status: 'ACTIVE' },\n      include: {\n        tasks: { select: { status: true } },\n        milestones: { select: { status: true, dueDate: true } },\n      },\n    });",
      "description": "`captureSnapshots` loads ALL active projects at once, each with their full task and milestone arrays. For 500 active projects with an average of 200 tasks and 20 milestones each, this loads 100,000 task rows and 10,000 milestone rows in a single query. The comment credits this as a 2-query optimization (PER-003), which is correct vs. the N+1 it replaced, but the in-memory data volume is unbounded.",
      "root_cause": "The snapshot cron job was optimized from N+1 to 2 queries, but the first query has no `take` ceiling, so the entire active-project table is hydrated in one shot.",
      "impact": "On large deployments, the snapshot job can transiently consume hundreds of MB of Node.js heap while processing the full result set. If run frequently, this creates GC pressure.",
      "suggested_fix": "Process snapshots in batches (e.g., 100 projects at a time) using cursor-based pagination: `findMany({ where: { status: 'ACTIVE', id: { gt: cursor } }, take: 100, orderBy: { id: 'asc' } })`. Wrap each batch in its own `createMany`. For very large deployments, replace the in-memory task/milestone array with a `task.groupBy` per batch.",
      "acceptance_criteria": [
        "captureSnapshots processes at most 100 projects per DB round-trip",
        "All active projects still receive a snapshot after the refactor"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "medium",
      "notes": "For the current scale of use (local government, likely < 200 active projects) this is a nit. It becomes blocking above ~500 active projects."
    }
  ],
  "verified_clean": [
    "PermissionsService: Redis cache with in-process singleflight prevents stampede on simultaneous role permission lookups (permissions.service.ts)",
    "ProjectsService.findAll: uses a single task.groupBy fan-out instead of N per-project queries for progress calculation (PER-005 comment, projects.service.ts:357-379)",
    "ProjectsService.captureSnapshots: uses 2-query pattern (findMany + createMany with skipDuplicates) instead of N+1 inserts (PER-003 comment, projects.service.ts:1145-1226)",
    "UsersService.validateImport: pre-fetches all existing emails/logins in one batch query before the loop (users.service.ts:1479-1481)",
    "UsersService.getUsersPresence: consolidated from 5 sequential findMany fan-outs to a single $queryRaw with EXISTS sub-queries (PER-016 comment, users.service.ts:1702)",
    "ProjectsService.findAll: uses Promise.all([findMany, count]) for parallel execution (projects.service.ts:284-354)",
    "EpicsService.findAll and MilestonesService.findAll: use Promise.all([findMany, count]) for parallel execution",
    "JwtBlacklistService: Redis key TTL tied to token remaining lifetime — no unbounded key accumulation",
    "LoginLockoutService: SHA-256 keying prevents Redis key growth from unbounded identifiers",
    "RolesService.listRoles: uses _count select for userCount, not a separate query per role",
    "AccessScopeService.canManageUser: batches department + service checks with Promise.all",
    "RefreshTokenService.rotate: wrapped in Serializable transaction to prevent TOCTOU race",
    "ProjectsService.hardDelete / checkProjectDependencies: uses Promise.all for 4 parallel count queries",
    "ProjectsService.findAll: clients include uses { include: { client: { select: { id, name } } } } — minimal client projection",
    "ProjectsService.addMember: checks project and user existence individually (sequential) — acceptable since it's a mutation, not a list",
    "OwnershipService.isProjectOwner: uses minimal select on project (only 3 ownership-relevant fields) and a fallback membership findFirst",
    "PrismaService: registers slow-query event listener (threshold 200ms) for observability"
  ],
  "files_scanned": [
    "apps/api/src/auth/auth.controller.ts",
    "apps/api/src/auth/auth.module.ts",
    "apps/api/src/auth/auth.service.ts",
    "apps/api/src/auth/decorators/allow-password-change.decorator.ts",
    "apps/api/src/auth/decorators/current-user.decorator.ts",
    "apps/api/src/auth/decorators/public.decorator.ts",
    "apps/api/src/auth/dto/login.dto.ts",
    "apps/api/src/auth/dto/refresh-token.dto.ts",
    "apps/api/src/auth/dto/register.dto.ts",
    "apps/api/src/auth/dto/reset-password.dto.ts",
    "apps/api/src/auth/dto/reset-password-token.dto.ts",
    "apps/api/src/auth/guards/force-password-change.guard.ts",
    "apps/api/src/auth/guards/jwt-auth.guard.ts",
    "apps/api/src/auth/guards/throttler-behind-proxy.guard.ts",
    "apps/api/src/auth/jwt-blacklist.service.ts",
    "apps/api/src/auth/jwt-not-before.service.ts",
    "apps/api/src/auth/login-lockout.service.ts",
    "apps/api/src/auth/refresh-token.service.ts",
    "apps/api/src/auth/strategies/jwt.strategy.ts",
    "apps/api/src/auth/strategies/local.strategy.ts",
    "apps/api/src/common/common.module.ts",
    "apps/api/src/common/config/audit-hash-key.ts",
    "apps/api/src/common/config/jwt-secret.ts",
    "apps/api/src/common/decorators/ownership-check.decorator.ts",
    "apps/api/src/common/error-reporter.ts",
    "apps/api/src/common/fastify/cors.config.ts",
    "apps/api/src/common/fastify/redact.config.ts",
    "apps/api/src/common/fastify/request-id.context.ts",
    "apps/api/src/common/fastify/trust-proxy.config.ts",
    "apps/api/src/common/fastify/uploads-auth.hook.ts",
    "apps/api/src/common/filters/all-exceptions.filter.ts",
    "apps/api/src/common/guards/ownership.guard.ts",
    "apps/api/src/common/services/access-scope.service.ts",
    "apps/api/src/common/services/cache.service.ts",
    "apps/api/src/common/services/ownership.service.ts",
    "apps/api/src/common/services/role-hierarchy.service.ts",
    "apps/api/src/common/upload/magic-bytes.validator.ts",
    "apps/api/src/common/validators/password-policy.ts",
    "apps/api/src/epics/dto/create-epic.dto.ts",
    "apps/api/src/epics/dto/update-epic.dto.ts",
    "apps/api/src/epics/epics.controller.ts",
    "apps/api/src/epics/epics.module.ts",
    "apps/api/src/epics/epics.service.ts",
    "apps/api/src/milestones/dto/create-milestone.dto.ts",
    "apps/api/src/milestones/dto/import-milestones.dto.ts",
    "apps/api/src/milestones/dto/update-milestone.dto.ts",
    "apps/api/src/milestones/milestones.controller.ts",
    "apps/api/src/milestones/milestones.module.ts",
    "apps/api/src/milestones/milestones.service.ts",
    "apps/api/src/prisma/prisma.service.ts",
    "apps/api/src/projects/dto/add-member.dto.ts",
    "apps/api/src/projects/dto/archived-filter.dto.ts",
    "apps/api/src/projects/dto/create-project.dto.ts",
    "apps/api/src/projects/dto/query-projects.dto.ts",
    "apps/api/src/projects/dto/update-member.dto.ts",
    "apps/api/src/projects/dto/update-project.dto.ts",
    "apps/api/src/projects/projects.controller.ts",
    "apps/api/src/projects/projects.module.ts",
    "apps/api/src/projects/projects.service.ts",
    "apps/api/src/rbac/decorators/allow-self-service.decorator.ts",
    "apps/api/src/rbac/decorators/require-permissions.decorator.ts",
    "apps/api/src/rbac/dto/create-role.dto.ts",
    "apps/api/src/rbac/dto/update-role.dto.ts",
    "apps/api/src/rbac/permissions.guard.ts",
    "apps/api/src/rbac/permissions.service.ts",
    "apps/api/src/rbac/rbac.module.ts",
    "apps/api/src/rbac/roles.controller.ts",
    "apps/api/src/rbac/roles.service.ts",
    "apps/api/src/users/dto/avatar-preset.dto.ts",
    "apps/api/src/users/dto/change-password.dto.ts",
    "apps/api/src/users/dto/create-user.dto.ts",
    "apps/api/src/users/dto/import-users.dto.ts",
    "apps/api/src/users/dto/reset-password.dto.ts",
    "apps/api/src/users/dto/update-user.dto.ts",
    "apps/api/src/users/users.controller.ts",
    "apps/api/src/users/users.module.ts",
    "apps/api/src/users/users.service.ts"
  ]
}
