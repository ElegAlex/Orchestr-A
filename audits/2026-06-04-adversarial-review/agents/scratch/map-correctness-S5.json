{
  "dimension": "correctness",
  "shard": "S5",
  "commit": "0997b30e2d44289239d5ba5d8f4f463d58748ad0",
  "findings": [
    {
      "id": "correctness-S5-1",
      "severity": "blocking",
      "category": "correctness",
      "subcategory": "authorization_scope_bypass",
      "title": "canValidate: delegation check is not scoped to the leave's assigned validator — any delegate can approve any leave",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "1638-1650",
      "code_evidence": "    // Vérifier les délégations actives\n    const today = new Date();\n    const activeDelegation =\n      await this.prisma.leaveValidationDelegate.findFirst({\n        where: {\n          delegateId: validatorId,\n          isActive: true,\n          startDate: { lte: today },\n          endDate: { gte: today },\n        },\n      });\n\n    return activeDelegation !== null;",
      "description": "The delegation branch of `canValidate` finds any active delegation where the calling user is `delegateId`, without requiring that the delegation's `delegatorId` matches `leave.validatorId`. If user Alice was delegated by Manager A (who manages dept A), she can also approve leaves of employees in dept B whose assigned validator is Manager B — as long as she has any active delegation. The three preceding checks (MANAGE_ANY_LEAVES, exact validatorId match, APPROVE_LEAVES+service-scope) are all bypassed by this last fallback.\n\nThe `findValidatorForUser` docstring explicitly says a delegate found for manager B 'must never become the validator for dept A users', but `canValidate` does exactly that through this undiscriminating delegation query.",
      "root_cause": "The `findFirst` query filters on `delegateId` only, omitting the join condition `delegatorId = leave.validatorId` that would scope the delegation to the leave's actual assigned validator.",
      "impact": "Any user who has received at least one active delegation from any manager can approve or reject any pending leave request in the entire organisation, regardless of service scope. This is a complete authorization bypass for the approval workflow.",
      "suggested_fix": "Add `delegatorId: leave.validatorId` to the where clause (requires the leave be fetched with its `validatorId`). If `leave.validatorId` is null (no validator assigned), the delegation fallback should return false. Example fix:\n```ts\nif (leave.validatorId) {\n  const activeDelegation = await this.prisma.leaveValidationDelegate.findFirst({\n    where: {\n      delegatorId: leave.validatorId,\n      delegateId: validatorId,\n      isActive: true,\n      startDate: { lte: today },\n      endDate: { gte: today },\n    },\n  });\n  if (activeDelegation) return true;\n}\nreturn false;",
      "acceptance_criteria": [
        "A user with an active delegation from manager A cannot approve a leave whose validatorId is manager B",
        "A user with an active delegation from the leave's validatorId can approve that leave",
        "Existing tests for canValidate pass with the fix"
      ],
      "verification_command": "npx jest --testPathPattern leaves.service.spec --no-coverage 2>&1 | grep -E 'PASS|FAIL|canValidate'",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The bug is in the else-branch after the service-scope check succeeds. The fix also aligns with the intent documented in `findValidatorForUser` (COR-005 comment at line 712-714)."
    },
    {
      "id": "correctness-S5-2",
      "severity": "important",
      "category": "correctness",
      "subcategory": "state_machine_missing_audit",
      "title": "rejectCancellation writes no audit log — CANCELLATION_REQUESTED→APPROVED transition is invisible to the audit trail",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2283-2319",
      "code_evidence": "    return this.prisma.$transaction(async (tx) => {\n      const current = await tx.leave.findUnique({ where: { id } });\n      if (!current) {\n        throw new NotFoundException('Demande de congé introuvable');\n      }\n      if (current.status !== LeaveStatus.CANCELLATION_REQUESTED) {\n        throw new ConflictException(\n          'La demande de congé a été modifiée pendant le traitement. Veuillez réessayer.',\n        );\n      }\n\n      return tx.leave.update({\n        where: { id },\n        data: { status: LeaveStatus.APPROVED },\n        include: {",
      "description": "All other status transitions in LeavesService (approve, reject, cancel, requestCancel) emit an audit log entry via `auditPersistence.log` inside or right after the transaction. `rejectCancellation` is the only transition that performs no audit write. A manager who denies a cancellation request leaves zero trace in `audit_logs`. The `cancel()` docstring at line 2113 explicitly notes 'APPROVE_CANCELLATION is merged here', implying the intent was to instrument both paths. `rejectCancellation` was overlooked.",
      "root_cause": "The `rejectCancellation` $transaction callback returns immediately after `tx.leave.update` without calling `auditPersistence.log`, and the outer function has no post-tx audit emit.",
      "impact": "Under a French public-sector audit (RGPD/CADA/Code du travail), every mutation to an employee's leave record must be traceable. A denied cancellation that restores a leave to APPROVED has real payroll consequences (the leave stays consumed) but is now unrecoverable from logs.",
      "suggested_fix": "Add an `await this.auditPersistence.log(...)` call inside the transaction callback, after the update, mirroring the pattern in `cancel()` and `requestCancel()`. Build an actorSnapshot before the transaction using `buildActorSnapshot(currentUserId ?? '', { roleCode: currentUserRole ?? null })`.",
      "acceptance_criteria": [
        "Calling rejectCancellation produces one audit_logs row with action LEAVE_CANCELLATION_REJECTED (or equivalent) and the correct entityId",
        "The audit row is present even when the transaction commits atomically"
      ],
      "verification_command": "N/A — manual verification: call POST /leaves/:id/reject-cancellation then SELECT * FROM audit_logs WHERE entity_id = ':id' ORDER BY created_at DESC LIMIT 5;",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "All other six status-transition methods have audit writes; this one is the sole exception."
    },
    {
      "id": "correctness-S5-3",
      "severity": "important",
      "category": "correctness",
      "subcategory": "non_atomic_write_then_audit",
      "title": "deleteBalance: hard-delete committed before audit log — crash between them produces silent deletion with no audit trace",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2989-3010",
      "code_evidence": "    await this.prisma.leaveBalance.delete({ where: { id } });\n\n    await this.auditPersistence.log({\n      action: AuditAction.LEAVE_BALANCE_ADJUSTED,\n      entityType: 'Leave',\n      entityId: id,\n      actorId: actorId ?? null,\n      payload: {\n        actor: actorSnapshot,\n        subject: {\n          balanceId: id,\n          userId: balance.userId,",
      "description": "The balance is deleted on line 2989 and the audit write is attempted on line 2991, two separate awaits with no enclosing transaction. If the process crashes, is OOM-killed, or `auditPersistence.log` throws between these two lines, the balance row is gone from the database with no audit trace. This contrasts with `remove()` at line 1571 which wraps both the delete and the audit inside a single `this.prisma.$transaction`. The `remove()` docstring at line 1566 acknowledges the audit uses its own Prisma client and is not fully atomic — but `deleteBalance` here does not even attempt the transaction wrapper.",
      "root_cause": "The `deleteBalance` method issues two sequential awaits (delete, then audit) without a transaction, unlike `remove()` which uses `$transaction` to couple them.",
      "impact": "Admin-deleted leave balance overrides leave no audit trail if the API process restarts between the two writes. In a HR audit scenario, a balance that vanished without a trace could be attributed to data corruption rather than an admin action.",
      "suggested_fix": "Wrap the delete + audit in a `this.prisma.$transaction` as done in `remove()`, accepting the same acknowledged trade-off that the audit uses its own client (so the tx guards the delete commit, not the audit write durability):\n```ts\nawait this.prisma.$transaction(async (tx) => {\n  await tx.leaveBalance.delete({ where: { id } });\n  await this.auditPersistence.log({ ... });\n});\n```",
      "acceptance_criteria": [
        "Simulated crash between delete and audit does not result in a deleted balance with no audit row",
        "The audit row appears in audit_logs after a successful deleteBalance call"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The acknowledged trade-off (audit uses its own Prisma client) is documented in the remove() method. deleteBalance should at minimum match that level of coupling."
    },
    {
      "id": "correctness-S5-4",
      "severity": "important",
      "category": "correctness",
      "subcategory": "tz_naive_date_arithmetic",
      "title": "countWorkingDays: local-TZ setDate/getDate arithmetic causes DST off-by-one — one day may be counted twice or skipped",
      "file": "apps/api/src/holidays/holidays.service.ts",
      "line": "460-477",
      "code_evidence": "    let count = 0;\n    const current = new Date(startDate);\n\n    while (current <= endDate) {\n      const dayOfWeek = current.getDay();\n      const dateStr = current.toISOString().split('T')[0];\n\n      // Exclut samedi (6) et dimanche (0) et jours fériés non ouvrés\n      if (\n        dayOfWeek !== 0 &&\n        dayOfWeek !== 6 &&\n        !nonWorkingHolidayDates.has(dateStr)\n      ) {\n        count++;\n      }\n\n      current.setDate(current.getDate() + 1);\n    }",
      "description": "Three separate TZ issues interact here:\n1. `current.setDate(current.getDate() + 1)` uses local-TZ date components. On a Europe/Paris host at the DST transition (end of March: clocks spring forward from 02:00 to 03:00), adding one local day advances the UTC timestamp by only 23 hours, not 24. On the autumn transition (02:00 → 01:00), advancing by one local day adds 25 UTC hours — making `current > endDate` false when it should be true, causing the last day to be counted twice.\n2. `current.getDay()` is local-TZ; `dateStr = current.toISOString().split('T')[0]` is UTC. If startDate is provided as a UTC-midnight Date (typical for @db.Date columns) and the host runs at Europe/Paris (+01/+02), `getDay()` returns the local day-of-week (same as UTC in this case) but `dateStr` is already correct. However, `nonWorkingHolidayDates` is built via `.toISOString().split('T')[0]` on `h.date` which is also UTC — so these two UTC strings match. The primary risk is the DST loop increment.",
      "root_cause": "`setDate/getDate` operates in local calendar time; on a Paris host the DST spring-forward transition causes a loop iteration that advances by only 23 hours, allowing one date to appear in two consecutive loop iterations.",
      "impact": "On the spring DST boundary (end of March, one night per year), `countWorkingDays` may return count+1 (one extra workday) for ranges that span the transition. Used by the `/holidays/working-days/count` API endpoint. Since this endpoint is informational and not used in leave-day balance gating, the impact is limited to incorrect display data, but the same flaw pattern exists if this function is ever reused in balance calculations.",
      "suggested_fix": "Replace the cursor-advance with UTC arithmetic matching the pattern in `leave-year-window.ts`:\n```ts\ncurrent.setUTCDate(current.getUTCDate() + 1);\n```\nand use `current.getUTCDay()` for the day-of-week check to stay consistently in UTC.",
      "acceptance_criteria": [
        "countWorkingDays('2025-03-28', '2025-03-31') returns 2 (Fri 28 + Mon 31, excluding DST Sunday 30 and Saturday 29)",
        "countWorkingDays('2025-10-24', '2025-10-27') returns 2 (Fri 24 + Mon 27)"
      ],
      "verification_command": "node -e \"const d = new Date(Date.UTC(2025,2,29)); d.setDate(d.getDate()+1); console.log(d.toISOString())\" # should be 2025-03-30T22:00:00.000Z on Europe/Paris host, not 2025-03-30T23:00:00.000Z",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The leave-year-window.ts module correctly uses setUTCDate/getUTCDate throughout. This function in holidays.service.ts should adopt the same pattern."
    },
    {
      "id": "correctness-S5-5",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing_field_in_query_projection",
      "title": "getAvailableDays: existing leaves fetched without endHalfDay field — consumed days under-counted when stored leave has endHalfDay",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2587-2614",
      "code_evidence": "    const intersecting = await db.leave.findMany({\n      where: {\n        userId,\n        leaveTypeId,\n        status: {\n          in: [\n            LeaveStatus.APPROVED,\n            LeaveStatus.CANCELLATION_REQUESTED,\n            LeaveStatus.PENDING,\n          ],\n        },\n        startDate: { lt: yearEnd },\n        endDate: { gte: yearStart },\n        ...(options.excludeLeaveId\n          ? { id: { not: options.excludeLeaveId } }\n          : {}),\n      },\n      select: { startDate: true, endDate: true, halfDay: true },\n    });\n\n    const usedThisYear = intersecting.reduce((sum, l) => {\n      const buckets = splitLeaveByYear(\n        l.startDate,\n        l.endDate,\n        l.halfDay ?? null,\n        null,\n        holidayKeys,\n      );",
      "description": "The `select` projection on line 2604 only fetches `halfDay` (the start-half-day) but not `endHalfDay`. When `splitLeaveByYear` is called with `null` as the fourth argument (endHalfDay), it computes consumed days without the end-half-day deduction. Since the database `Leave` model only persists `halfDay` (not `endHalfDay` — verified in schema.prisma), and `create()` only stores `effectiveHalfDay || undefined` in the `halfDay` field (which is `halfDay || startHalfDay`), the `endHalfDay` is intentionally transient and never persisted. However, the stored `days` column DOES include the endHalfDay deduction (via `calculateLeaveDays` in `create()`). This means there is a discrepancy: the `days` column reflects the endHalfDay, but `getAvailableDays` re-derives consumed days without it, causing the available balance to be understated by 0.5 day for each existing leave that had `endHalfDay` set.",
      "root_cause": "The Leave schema has no `endHalfDay` column, so the value is permanently lost after `create()`. `getAvailableDays` re-derives consumption from stored records but cannot recover the missing endHalfDay, leading to 0.5-day under-counting of available balance.",
      "impact": "For any user with an existing approved/pending leave where `endHalfDay` was set at creation time, `getAvailableDays` will report 0.5 fewer available days than were actually consumed (i.e., the user appears to have consumed more leave than they actually did). This can block legitimate new leave requests with a spurious 'Solde insuffisant' error.",
      "suggested_fix": "Either: (a) Add an `endHalfDay HalfDay?` column to the `Leave` model in schema.prisma and persist it in `create()` and `update()` — then include it in the `select` and pass it to `splitLeaveByYear`; or (b) Deprecate `endHalfDay` in the DTO and clarify in documentation that only `halfDay`/`startHalfDay` is persisted.",
      "acceptance_criteria": [
        "Creating a leave with endHalfDay=AFTERNOON and then querying getAvailableDays shows the correctly deducted balance (not 0.5 extra deducted)",
        "The days column stored in DB matches the value computed by getAvailableDays when re-deriving usedThisYear"
      ],
      "verification_command": "N/A — manual verification: create leave with endHalfDay, then call GET /leaves/me/balance and verify available = allocated - stored_days",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The issue is architectural: endHalfDay is accepted in the DTO, affects the `days` computation, but is never persisted. The `getAvailableDays` re-derivation cannot be correct without it. The fix with lowest risk is option (a) — add the column."
    },
    {
      "id": "correctness-S5-6",
      "severity": "important",
      "category": "correctness",
      "subcategory": "tz_naive_year_computation",
      "title": "getLeaveBalance uses new Date().getFullYear() (host-TZ) while balance windows use Paris-anchored parisYearWindow — mismatch on Jan 1 between 00:00 and 01:00 Paris time",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2635",
      "code_evidence": "    const currentYear = new Date().getFullYear();",
      "description": "The leave balance system is explicitly anchored to Europe/Paris calendar year boundaries via `parisYearWindow` (which uses `fromZonedTime('${year}-01-01 00:00:00', 'Europe/Paris')`). However, `getLeaveBalance` computes `currentYear` using `new Date().getFullYear()`, which returns the year in the API process's local timezone (UTC when deployed on standard Linux containers). Between 2026-01-01 00:00 UTC and 2026-01-01 01:00 UTC (or 02:00 in summer — but Jan 1 is winter so +01:00), `getFullYear()` returns 2026 but a Paris employee is still in 2025 (Paris midnight is at 23:00 UTC the prior day). The balance query then fetches 2026 allocations, but the leave query window (`parisYearWindow(currentYear)`) also uses 2026, so the query window is correct for 2026 — the mismatch is that `currentYear` should be the Paris year, not the UTC year.",
      "root_cause": "`new Date().getFullYear()` is UTC-based on most Linux deployments, while Paris year boundaries start one hour before UTC midnight in winter.",
      "impact": "During the 1-hour window on Jan 1 (00:00–01:00 UTC = 01:00–02:00 Paris), a Paris employee checking their balance will see their 2026 balance instead of their 2025 balance. This is a 1-hour/year edge case but can cause confusion during year-end payroll reconciliation.",
      "suggested_fix": "Use `formatInTimeZone(new Date(), LEAVE_TIMEZONE, 'yyyy')` from `date-fns-tz` (already imported in `leave-year-window.ts`) to compute the Paris year:\n```ts\nimport { formatInTimeZone } from 'date-fns-tz';\nimport { LEAVE_TIMEZONE } from './leave-year-window';\nconst currentYear = parseInt(formatInTimeZone(new Date(), LEAVE_TIMEZONE, 'yyyy'), 10);\n```",
      "acceptance_criteria": [
        "At 2027-01-01T00:30:00Z (00:30 UTC = 01:30 Paris), getLeaveBalance returns 2026 as the year (Paris is still on Jan 1 2027, but the 2026 balance window is past)",
        "Integration test for this boundary passes"
      ],
      "verification_command": "N/A — manual verification: mock Date to 2027-01-01T00:30:00Z and call getLeaveBalance, expect year=2027 in the response",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Low probability (1 hour per year) but year-end is exactly when HR teams query balances for statutory reporting."
    },
    {
      "id": "correctness-S5-7",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "inconsistent_error_reporting",
      "title": "importLeaves catch block resets result.created=0 but not result.skipped — error response reports misleading skipped count after full tx rollback",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "3627-3637",
      "code_evidence": "    } catch (error) {\n      // A leave.create failure (or any unrecoverable DB error) aborts the\n      // entire $transaction. Record a single error for the whole batch.\n      result.created = 0;\n      result.errors++;\n      // COR-037 — translate the DB exclusion-constraint violation to a clean message.\n      const message = isLeaveOverlapViolation(error)\n        ? 'Chevauchement détecté avec un congé approuvé existant'\n        : (error as Error).message || 'Erreur inconnue';\n      result.errorDetails.push(`Import annulé: ${message}`);\n    }",
      "description": "When the $transaction rolls back due to a DB error, `result.created` is reset to 0 (correctly — no rows were persisted). However, `result.skipped` is NOT reset. Skipped rows are those that failed validation before reaching `tx.leave.create` (bad email, unknown leave type, bad dates, overlap). Since the entire transaction rolls back, no rows were persisted — but `result.skipped` retains any non-zero count accumulated before the error. The API caller receives a response like `{created:0, skipped:3, errors:1}` which implies 3 rows were intentionally skipped, when in reality the skipped rows were also 'not imported' because of the rollback. The caller might retry only the 'error' row, not realising they need to re-submit the entire file.",
      "root_cause": "The catch block zeroes `result.created` but does not reset `result.skipped` to match the rolled-back reality.",
      "impact": "Misleading API response: callers may believe some rows were deliberately skipped (validation failure) when the entire batch was atomically aborted. Could cause callers to re-import only a subset of the original file, leaving data incomplete.",
      "suggested_fix": "Add `result.skipped = 0;` in the catch block alongside `result.created = 0;`, and clear `result.errorDetails` before pushing the single 'Import annulé' message (to avoid mixing pre-rollback row-level skip details with the global abort message).",
      "acceptance_criteria": [
        "A batch with 2 skipped rows + 1 DB error returns {created:0, skipped:0, errors:1} after catch",
        "The errorDetails array contains only the 'Import annulé' message, not the per-skip messages"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Whether errorDetails should also be cleared depends on desired UX — keeping row-level skip details can help the user fix their file even after a rollback."
    }
  ],
  "verified_clean": [
    "leaves/leave-year-window.ts: all date arithmetic uses UTC (setUTCDate/getUTCDate/parisDayKey via formatInTimeZone) — no host-TZ leakage",
    "leaves/leave-year-window.ts: splitLeaveByYear shortfall distribution is proportional across years — correct cross-year bucket accounting",
    "leaves/leaves.service.ts create(): Serializable isolation + P2034 single-retry — TOCTOU between balance gate and insert properly handled",
    "leaves/leaves.service.ts create(): balance snapshot re-verification after gate before insert — correctly catches concurrent balance modification",
    "leaves/leaves.service.ts update(): excludeLeaveId passed to getAvailableDays — existing leave not double-counted during edit",
    "leaves/leaves.service.ts approve(): re-read inside transaction before update — concurrent PENDING→APPROVED race guarded",
    "leaves/leaves.service.ts approve(): balance re-validation inside tx (COR-008) — reduction between PENDING and approval caught",
    "leaves/leaves.service.ts reject(): re-read inside transaction — TOCTOU guarded same as approve()",
    "leaves/leaves.service.ts cancel(): re-read inside transaction for both APPROVED and CANCELLATION_REQUESTED — concurrent modification detected",
    "leaves/leaves.service.ts requestCancel(): re-read inside transaction — TOCTOU guarded (COR-009)",
    "leaves/leaves.service.ts rejectCancellation(): re-read inside transaction — TOCTOU guarded (COR-009)",
    "leaves/leaves.service.ts checkOverlap(): correctly excludes CANCELLED/REJECTED from overlap window",
    "leaves/leaves.service.ts canManageLeave(): permission check via dynamic RBAC (no hardcoded roles)",
    "leaves/leaves.service.ts getManagedUserIds(): correctly returns 'all' for leaves:manage_any permission",
    "leaves/leaves.service.ts getAvailableDays(): uses parisYearWindow for year boundaries — UTC-correct",
    "leaves/leaves.service.ts importLeaves(): wrapped in single $transaction for atomic batch import (COR-024)",
    "leaves/leaves.service.ts upsertBalance (global): retry loop on P2002 correctly handles concurrent create race without transaction",
    "leaves/leaves.service.ts upsertBalance (individual): Prisma upsert on compound key is atomic",
    "leaves/leaves.service.ts findValidatorForUser(): delegation lookup correctly scoped to department manager (COR-005)",
    "holidays/holidays.service.ts calculateEaster(): uses Date.UTC — no host-TZ shift for Easter computation",
    "holidays/holidays.service.ts importFrenchHolidays(): all fixed holidays use Date.UTC — no off-by-one on +UTC hosts",
    "holidays/holidays.service.ts addDays(): uses setUTCDate — no DST issue",
    "holidays/holidays.service.ts findByYear(): uses Date.UTC for year boundaries — correct on any host TZ",
    "holidays/holidays.service.ts importFrenchHolidays(): P2002 collision silently skipped — idempotent import",
    "holidays/holidays.service.ts onApplicationBootstrap(): error caught and logged — bootstrap failure does not crash the app",
    "school-vacations/school-vacations.service.ts importFromOpenData(): AbortController timeout correctly cleared in finally block",
    "school-vacations/school-vacations.service.ts importFromOpenData(): deduplication by description takes widest date range — correct merge",
    "school-vacations/school-vacations.service.ts importFromOpenData(): P2002 on create → skipped (idempotent)",
    "school-vacations/school-vacations.service.ts create(): P2002 caught and re-thrown as ConflictException",
    "leave-types/leave-types.service.ts reorder(): wrapped in $transaction — all sortOrder updates atomic",
    "leave-types/leave-types.service.ts remove(): guard on _count.leaves before hard-delete — no orphaned leave references",
    "leave-types/leave-types.service.ts update(): system type field restriction enforced before update",
    "leaves/leaves.service.ts remove(): $transaction wraps delete + audit — delete committed atomically with audit",
    "leaves/leaves.service.ts getHolidayKeySet(): widened by ±1 day — edge-case holidays at boundaries included",
    "leaves/leaves.service.ts isLeaveOverlapViolation(): correctly matches both 23P01 and leaves_no_overlap in message",
    "leaves/dto/create-leave.dto.ts: class-validator decorators present for all required fields",
    "leave-types/dto/reorder-leave-types.dto.ts: IsUUID(4, each:true) validates each orderedIds element"
  ],
  "files_scanned": [
    "apps/api/src/leaves/leaves.service.ts",
    "apps/api/src/leaves/leaves.controller.ts",
    "apps/api/src/leaves/leave-year-window.ts",
    "apps/api/src/leaves/leaves.module.ts",
    "apps/api/src/leaves/dto/create-leave.dto.ts",
    "apps/api/src/leaves/dto/update-leave.dto.ts",
    "apps/api/src/leaves/dto/upsert-leave-balance.dto.ts",
    "apps/api/src/leaves/dto/import-leaves.dto.ts",
    "apps/api/src/leaves/dto/approve-leave.dto.ts",
    "apps/api/src/leaves/dto/reject-leave.dto.ts",
    "apps/api/src/leaves/dto/create-delegation.dto.ts",
    "apps/api/src/leave-types/leave-types.service.ts",
    "apps/api/src/leave-types/leave-types.controller.ts",
    "apps/api/src/leave-types/leave-types.module.ts",
    "apps/api/src/leave-types/dto/create-leave-type.dto.ts",
    "apps/api/src/leave-types/dto/update-leave-type.dto.ts",
    "apps/api/src/leave-types/dto/reorder-leave-types.dto.ts",
    "apps/api/src/holidays/holidays.service.ts",
    "apps/api/src/holidays/holidays.controller.ts",
    "apps/api/src/holidays/holidays.module.ts",
    "apps/api/src/holidays/dto/create-holiday.dto.ts",
    "apps/api/src/holidays/dto/update-holiday.dto.ts",
    "apps/api/src/holidays/dto/holiday-range-query.dto.ts",
    "apps/api/src/school-vacations/school-vacations.service.ts",
    "apps/api/src/school-vacations/school-vacations.controller.ts",
    "apps/api/src/school-vacations/school-vacations.module.ts",
    "apps/api/src/school-vacations/dto/create-school-vacation.dto.ts",
    "apps/api/src/school-vacations/dto/update-school-vacation.dto.ts",
    "apps/api/src/school-vacations/dto/school-vacation-range-query.dto.ts",
    "apps/api/src/school-vacations/dto/import-school-vacation.dto.ts"
  ]
}
