{
  "category": "data_integrity",
  "finding_count": 31,
  "findings": [
    {
      "id": "DAT-001",
      "severity": "blocking",
      "category": "data_integrity",
      "subcategory": "audit_immutability_gap",
      "title": "audit_logs created with ON DELETE SET NULL — immutability trigger added 31 days later, leaving a window where audit rows could be silently mutated",
      "file": "packages/database/prisma/migrations/20260424111457_add_weight_and_audit_log/migration.sql",
      "line": "24",
      "code_evidence": "ALTER TABLE \"audit_logs\" ADD CONSTRAINT \"audit_logs_actorId_fkey\" FOREIGN KEY (\"actorId\") REFERENCES \"users\"(\"id\") ON DELETE SET NULL ON UPDATE CASCADE;",
      "description": "At creation time (20260424), audit_logs.actorId FK is declared ON DELETE SET NULL. Any user deletion between migration 20260424111457 and the immutability trigger migration 20260525190000 (31 days later) silently issues an UPDATE on audit_logs rows, setting actorId to NULL. This mutates the audit record without triggering any protection. The schema comment and 20260525190000 migration both acknowledge that SET NULL on an immutable table is problematic — the FK was later changed to NO ACTION — but the 31-day window is unprotected. If any user was hard-deleted during that window in production, those audit rows were silently modified and the actor identity is permanently lost.",
      "root_cause": "The immutability trigger and the FK ON DELETE behavior were designed together but deployed in two separate migrations 31 days apart, leaving the audit table mutable in the interim.",
      "impact": "Any user deletion between 2026-04-24 and 2026-05-25 that had associated audit_logs rows permanently erased the actor identity in those rows; audit trail is no longer reliable for that period. Compliance and forensic audits may be affected.",
      "suggested_fix": "Verify prod audit_logs for rows where actorId IS NULL and actorEmail (if added) is also NULL — these may be the result of the SET NULL window. Additionally, the original migration should have set ON DELETE RESTRICT or NO ACTION to protect immutability from the start. Document the window in a SECURITY_NOTE.",
      "acceptance_criteria": [
        "No audit_logs row has actorId=NULL unless the user was deleted AFTER 2026-05-25 (when actorEmail snapshot was also captured)",
        "Future audit_logs FK additions always use ON DELETE NO ACTION or RESTRICT"
      ],
      "verification_command": "SELECT COUNT(*) FROM audit_logs WHERE \"actorId\" IS NULL AND \"createdAt\" < '2026-05-25';",
      "related_findings": [
        "DAT-013",
        "DAT-028"
      ],
      "cluster_id": "H",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence VERBATIM confirmed at line 24. The immutability trigger migration 20260525190000 lines 95-104 explicitly states 'A SetNull on user deletion issues an UPDATE on audit_logs that the immutability trigger (step 6) would reject', confirming this was a known design flaw fixed retroactively. Current schema is now protected (FK changed to NO ACTION), but historical impact is real and unrecoverable without a backup from the window period."
    },
    {
      "id": "DAT-002",
      "severity": "blocking",
      "category": "data_integrity",
      "subcategory": "missing-foreign-key",
      "title": "TaskRACI.userId has no FK relation — orphaned RACI rows, no cascade on user deletion",
      "file": "packages/database/prisma/schema.prisma",
      "line": "405-417",
      "code_evidence": "model TaskRACI {\n  id        String   @id @default(uuid())\n  taskId    String\n  userId    String\n  role      RACIRole\n  createdAt DateTime @default(now())\n\n  // Relations\n  task Task @relation(fields: [taskId], references: [id], onDelete: Cascade)\n\n  @@unique([taskId, userId, role])\n  @@map(\"task_raci\")\n}",
      "description": "TaskRACI stores a userId column and a composite unique constraint on (taskId, userId, role), but there is NO Prisma relation connecting userId to the User table. There is no `user User @relation(...)` in TaskRACI, and the User model (lines 56-97) has no back-relation to TaskRACI. This means: (1) PostgreSQL has no FK constraint on task_raci.user_id — the DB will happily accept a non-existent userId; (2) when a User is deleted, their RACI rows remain in the table as orphaned records with a dangling userId; (3) no onDelete behavior is enforced. Compare with TaskAssignee (lines 360-374) which correctly declares both `task Task @relation(...)` and `user User @relation(...)` with `onDelete: Cascade`.",
      "root_cause": "The `user` relation side was omitted from TaskRACI when the model was created, leaving userId as a plain String with no FK backing.",
      "impact": "After a User deletion (soft or hard), stale RACI rows survive referencing a non-existent userId. Queries joining task_raci on users will silently return no validator/responsible data. If userId is reused (UUID collision is astronomically unlikely but app logic may reuse logins), wrong user gets RACI assignment. No cascade means security-sensitive RACI data (RESPONSIBLE, ACCOUNTABLE) is never cleaned up.",
      "suggested_fix": "Add the FK relation on TaskRACI and the back-relation on User:\n\n// In model TaskRACI, add:\n  user User @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n// In model User, add to the relations block:\n  taskRaci TaskRACI[]\n\nThen generate and apply a migration:\n  pnpm run db:migrate\n\nThe migration will add a FK constraint `REFERENCES users(id)` on task_raci.user_id and a cascade delete trigger consistent with the TaskAssignee pattern.",
      "acceptance_criteria": [
        "Prisma schema validates without error after adding the relation",
        "psql: `\\d task_raci` shows a FK constraint on user_id REFERENCES users(id)",
        "Deleting a user whose userId appears in task_raci removes those rows (or the FK constraint fires the intended behavior)",
        "User.taskRaci back-relation is accessible from Prisma client queries"
      ],
      "verification_command": "psql $DATABASE_URL -c \"SELECT COUNT(*) FROM information_schema.referential_constraints WHERE constraint_name LIKE '%task_raci%user%';\"",
      "related_findings": [
        "PER-036"
      ],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW CONFIRMED. Verified in schema.prisma lines 405-417 verbatim. Init migration (20251116093059_init/migration.sql line 360) adds only task_raci_taskId_fkey, never a userId FK. No subsequent migration adds it. The User model relations block (lines 56-97) has no taskRaci back-relation. Crucially, users.service.ts hard-delete transaction (lines 948-969) lists taskAssignee.deleteMany but NOT taskRACI.deleteMany — RACI rows are silently orphaned on user deletion. Application layer validates user existence on RACI creation (tasks.service.ts line 1029-1035) but provides NO protection against post-creation user deletion."
    },
    {
      "id": "DAT-003",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "float_precision_hr",
      "title": "DOUBLE PRECISION used for HR-sensitive hours/days columns — rounding errors accumulate",
      "file": "packages/database/prisma/migrations/20251116093059_init/migration.sql",
      "line": "160-215",
      "code_evidence": "    \"estimatedHours\" DOUBLE PRECISION,\n    \"progress\" INTEGER NOT NULL DEFAULT 0,\n...\n    \"hours\" DOUBLE PRECISION NOT NULL,\n...\n    \"days\" DOUBLE PRECISION NOT NULL,",
      "description": "Three columns critical to HR/payroll computations — tasks.estimatedHours, time_entries.hours, and leaves.days — are declared DOUBLE PRECISION (IEEE 754 binary float). Binary floats cannot represent most decimal fractions exactly (e.g. 0.1 + 0.2 ≠ 0.3). For leave-day balances and time-entry aggregations, floating-point accumulation produces incorrect totals that silently diverge from true decimal values.",
      "root_cause": "Prisma Float maps to DOUBLE PRECISION by default; no explicit @db.Decimal annotation was applied to HR-sensitive numeric fields.",
      "impact": "Accumulated rounding errors in leave balance calculations, time entry aggregations, and project hour reporting; payroll/audit discrepancies possible at scale.",
      "suggested_fix": "Convert to NUMERIC: ALTER TABLE time_entries ALTER COLUMN hours TYPE NUMERIC(5,2) USING hours::numeric(5,2); same for leaves.days NUMERIC(6,2) and tasks.estimatedHours NUMERIC(5,2). Fixed by later migration 20260524100100_dat005_convert_float_to_decimal.",
      "acceptance_criteria": [
        "SELECT 0.1::double precision + 0.2::double precision = 0.3 returns false; SELECT 0.1::numeric(5,2) + 0.2::numeric(5,2) = 0.3 returns true",
        "All three columns are NUMERIC after migration"
      ],
      "verification_command": "psql -c \"\\d time_entries\" | grep hours",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at lines 160 and 198 and 215 of init migration. Fixed by migration 20260524100100_dat005_convert_float_to_decimal (2026-05-24) which converts time_entries.hours to NUMERIC(5,2), leaves.days to NUMERIC(6,2), leave_balances.totalDays to NUMERIC(6,2), tasks.estimatedHours to NUMERIC(5,2). Downgraded to low confidence — issue is resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-004",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "cascade_destroys_hr_history",
      "title": "ON DELETE CASCADE on leave_balances.leaveTypeId destroys balance history when leave type is deleted",
      "file": "packages/database/prisma/migrations/20260321105758_add_leave_balances_and_rbac_granularity/migration.sql",
      "line": "27",
      "code_evidence": "ALTER TABLE \"leave_balances\" ADD CONSTRAINT \"leave_balances_leaveTypeId_fkey\" FOREIGN KEY (\"leaveTypeId\") REFERENCES \"leave_type_configs\"(\"id\") ON DELETE CASCADE ON UPDATE CASCADE;",
      "description": "Deleting a leave_type_config cascades to delete all leave_balances rows for that type across all users and years. This means retiring or removing a leave type (e.g., abolishing RTT during a reform) silently erases the historical annual allocations for all employees, making it impossible to verify past entitlements. The ON DELETE RESTRICT or a soft-delete on leave_type_configs would prevent accidental data destruction.",
      "root_cause": "CASCADE chosen for referential convenience without considering that leave_type_configs rows represent system reference data that should be archived, not deleted.",
      "impact": "Deleting a leave type destroys all historical annual balance records for all users, breaking audit trails for leave entitlement verification.",
      "suggested_fix": "Change leave_balances.leaveTypeId FK to ON DELETE RESTRICT. Add isActive flag to leave_type_configs (already present) and use deactivation instead of deletion.",
      "acceptance_criteria": [
        "Attempting to DELETE a leave_type_configs row that has associated leave_balances rows raises a foreign key violation",
        "Leave type deactivation uses isActive=false, not DELETE"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed at line 27 of add_leave_balances_and_rbac_granularity migration. Verified no later migration changes this FK behavior — searched all migration SQL files for 'leave_balances_leaveTypeId_fkey' and found only the creation entry. No fix migration exists. The dat008_026_user_fk_full_erasure migration (2026-06-04) explicitly states it handles user deletion FKs only; leave_type_config FKs are not in scope. REMAINS OPEN."
    },
    {
      "id": "DAT-005",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "nullable_unique_constraint",
      "title": "leave_balances unique index on nullable userId treats multiple NULL rows as distinct — global balance uniqueness not enforced",
      "file": "packages/database/prisma/migrations/20260321105758_add_leave_balances_and_rbac_granularity/migration.sql",
      "line": "4-21",
      "code_evidence": "    \"userId\" TEXT,\n...\nCREATE UNIQUE INDEX \"leave_balances_userId_leaveTypeId_year_key\" ON \"leave_balances\"(\"userId\", \"leaveTypeId\", \"year\");",
      "description": "userId is nullable (TEXT without NOT NULL), representing a 'global' balance row when NULL. Postgres NULLS DISTINCT semantics mean multiple rows with userId=NULL for the same (leaveTypeId, year) all satisfy the unique constraint. A concurrent upsertBalance operation can therefore race-create two global balance rows for the same (leaveTypeId, year), making resolveAllocatedDays non-deterministic.",
      "root_cause": "Standard B-tree unique index treats each NULL as not equal to any other NULL (ISO SQL semantics), so the uniqueness guarantee does not hold for global (userId=NULL) rows.",
      "impact": "Race conditions in concurrent leave balance upserts can create duplicate global balance rows, causing non-deterministic behavior in leave allocation resolution.",
      "suggested_fix": "Add a partial unique index: CREATE UNIQUE INDEX leave_balances_global_unique ON leave_balances (leaveTypeId, year) WHERE userId IS NULL. Fixed by later migration 20260523171000_self_approved_and_global_balance_unique.",
      "acceptance_criteria": [
        "Two concurrent INSERTs with userId=NULL, same leaveTypeId and year result in a unique constraint violation on the second INSERT",
        "Partial index leave_balances_global_unique exists in pg_indexes"
      ],
      "verification_command": "psql -c \"SELECT indexname FROM pg_indexes WHERE tablename='leave_balances' AND indexname='leave_balances_global_unique';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at lines 4 and 21 of add_leave_balances_and_rbac_granularity migration. Fixed by migration 20260523171000_self_approved_and_global_balance_unique which adds: CREATE UNIQUE INDEX \"leave_balances_global_unique\" ON \"leave_balances\" (\"leaveTypeId\", \"year\") WHERE \"userId\" IS NULL. Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-006",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "float_precision_hr",
      "title": "leave_balances.totalDays declared DOUBLE PRECISION — balance summaries accumulate float errors",
      "file": "packages/database/prisma/migrations/20260321105758_add_leave_balances_and_rbac_granularity/migration.sql",
      "line": "7",
      "code_evidence": "    \"totalDays\" DOUBLE PRECISION NOT NULL,",
      "description": "leave_balances.totalDays is declared DOUBLE PRECISION. This column aggregates per-user annual leave allocations and carryovers. Float arithmetic on day counts can produce off-by-epsilon results that surface as fractional days when compared to integer day-count expectations in HR reporting.",
      "root_cause": "Prisma Float maps to DOUBLE PRECISION by default; no explicit @db.Decimal annotation was applied.",
      "impact": "HR balance reports may show residual fractions (e.g., 24.999999999999996 days instead of 25), causing off-by-one bugs in leave approval gating.",
      "suggested_fix": "ALTER TABLE leave_balances ALTER COLUMN totalDays TYPE NUMERIC(6,2) USING totalDays::numeric(6,2). Fixed by later migration 20260524100100.",
      "acceptance_criteria": [
        "leave_balances.totalDays column type is NUMERIC(6,2) in the final schema"
      ],
      "verification_command": "psql -c \"\\d leave_balances\" | grep totalDays",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 7 of add_leave_balances_and_rbac_granularity migration. Fixed by migration 20260524100100_dat005_convert_float_to_decimal (2026-05-24). Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-007",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "cascade_destroys_audit_history",
      "title": "ON DELETE CASCADE on project_snapshots destroys historical trend data when project is hard-deleted",
      "file": "packages/database/prisma/migrations/20260404211126_add_project_snapshots/migration.sql",
      "line": "15",
      "code_evidence": "ALTER TABLE \"project_snapshots\" ADD CONSTRAINT \"project_snapshots_projectId_fkey\" FOREIGN KEY (\"projectId\") REFERENCES \"projects\"(\"id\") ON DELETE CASCADE ON UPDATE CASCADE;",
      "description": "project_snapshots is an append-only time-series table recording project progress over time. With CASCADE, hard-deleting a project destroys all its snapshot history, removing the ability to reconstruct project velocity metrics, detect pattern anomalies, and satisfy retrospective reporting requirements. Snapshots have no independently meaningful identity without the project, but retain analytical value beyond the project lifecycle.",
      "root_cause": "Default cascade behavior applied to time-series child table without considering post-project-lifecycle reporting needs.",
      "impact": "Hard-deleting a project permanently erases all progress snapshots, making historical dashboards impossible to reconstruct.",
      "suggested_fix": "Change to ON DELETE RESTRICT and require project archival (status=CANCELLED/COMPLETED) before allowing hard deletion. Fixed by later migration 20260525200000_dat007_project_fk_restrict_preserve_history.",
      "acceptance_criteria": [
        "Attempting to DELETE a project with snapshots raises a foreign key violation",
        "ProjectsService.checkProjectDependencies surfaces this as ConflictException"
      ],
      "verification_command": "psql -c \"\\d project_snapshots\" | grep projectId",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 15 of add_project_snapshots migration. Fixed by migration 20260525200000_dat007_project_fk_restrict_preserve_history which drops CASCADE and adds RESTRICT on project_snapshots_projectId_fkey. Also fixes tasks, documents, and time_entries FKs in same migration. Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-008",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "project_third_party_members.allocation INTEGER has no CHECK constraint for 0-100 range — unlike project_members.allocation which was fixed in DAT-004",
      "file": "packages/database/prisma/migrations/20260411100717_add_third_parties_and_time_entry_actor_xor/migration.sql",
      "line": "109",
      "code_evidence": "  \"allocation\"    INTEGER,\n  \"assignedById\"  TEXT NOT NULL,\n  \"createdAt\"     TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,\n  CONSTRAINT \"project_third_party_members_pkey\" PRIMARY KEY (\"id\")",
      "description": "project_third_party_members.allocation stores a percentage (0-100%) just like project_members.allocation. The DAT-003/DAT-004 business invariants migration (20260527120000) added CHECK (\"allocation\" BETWEEN 0 AND 100) for project_members but explicitly omitted project_third_party_members. This asymmetry means a buggy service path or direct SQL admin can insert allocation=-5 or allocation=150 for third-party members without any DB-level rejection. The schema comment for project_members reads '0-100%', implying the same semantic for both tables.",
      "root_cause": "The DAT-004 migration added allocation range checks only for project_members but missed the parallel table project_third_party_members which has the same column with the same semantic.",
      "impact": "Out-of-range allocation values for third-party members can corrupt workload calculations and capacity reports, leading to incorrect analytics. Applications may silently accept and display nonsensical values (e.g., -10% or 200% allocation).",
      "suggested_fix": "Add: ALTER TABLE \"project_third_party_members\" ADD CONSTRAINT \"project_third_party_members_allocation_ck\" CHECK (\"allocation\" BETWEEN 0 AND 100);",
      "acceptance_criteria": [
        "INSERT INTO project_third_party_members (..., allocation) VALUES (..., 101) fails with check_violation",
        "INSERT INTO project_third_party_members (..., allocation) VALUES (..., -1) fails with check_violation",
        "INSERT INTO project_third_party_members (..., allocation) VALUES (..., 50) succeeds"
      ],
      "verification_command": "SELECT COUNT(*) FROM project_third_party_members WHERE allocation < 0 OR allocation > 100;",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence VERBATIM confirmed at line 109 of the migration. Exhaustive search across all migrations confirms: project_members gets allocation CHECK in 20260527120000 (line 66: 'project_members_allocation_ck'), but project_third_party_members gets NO CHECK anywhere in the entire migration history. Current schema gap — not a historical window issue."
    },
    {
      "id": "DAT-009",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "cascade_destroys_time_entries",
      "title": "time_entries.thirdPartyId ON DELETE CASCADE silently destroys billing/payroll records when a ThirdParty is deleted",
      "file": "packages/database/prisma/migrations/20260411100717_add_third_parties_and_time_entry_actor_xor/migration.sql",
      "line": "159-161",
      "code_evidence": "ALTER TABLE \"time_entries\"\n  ADD CONSTRAINT \"time_entries_thirdPartyId_fkey\"\n  FOREIGN KEY (\"thirdPartyId\") REFERENCES \"third_parties\"(\"id\") ON DELETE CASCADE ON UPDATE CASCADE;",
      "description": "Time entries are billing and payroll records. When a ThirdParty row is deleted (soft-delete via isActive=false exists but hard-delete is not blocked), all associated time_entries rows are CASCADE deleted. This destroys historical billing/HR records without warning or audit trail. The sister constraint for user-linked time entries (time_entries.userId) is also ON DELETE CASCADE in schema.prisma (line 462), compounding the risk. For an HR management system in French local government, these records may be subject to retention obligations (up to 5 years under French labor law). There is no RESTRICT guard preventing ThirdParty deletion when time entries exist.",
      "root_cause": "The FK was set to ON DELETE CASCADE following the same pattern as task_third_party_assignees (where CASCADE is correct for a join table), but time_entries are substantive financial/HR records that should survive their actor's deletion.",
      "impact": "Deleting a third_party (intentionally or accidentally) irrecoverably destroys all associated time entries. These represent worked hours and may be legally required for payroll and billing purposes. No recovery path exists without a database backup.",
      "suggested_fix": "Change ON DELETE CASCADE to ON DELETE RESTRICT on time_entries.thirdPartyId FK. If deletion of a ThirdParty with associated time entries must be allowed, implement a soft-delete pattern (ThirdParty.isActive already exists) and block hard-deletes via application logic or a BEFORE DELETE trigger that raises an exception when time_entries referencing that ThirdParty exist.",
      "acceptance_criteria": [
        "DELETE FROM third_parties WHERE id = '...' fails with FK violation when time_entries reference that third_party",
        "Integration test: create third_party + time_entry, attempt delete of third_party, expect error"
      ],
      "verification_command": "SELECT tp.id, COUNT(te.id) as te_count FROM third_parties tp JOIN time_entries te ON te.\"thirdPartyId\" = tp.id GROUP BY tp.id HAVING COUNT(te.id) > 0 LIMIT 5;",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence VERBATIM confirmed at lines 159-161. Schema.prisma line 463 confirms ON DELETE Cascade. The hardDelete service (third-parties.service.ts line 180-187) explicitly comments 'Cascade FK handles time_entries, task_third_party_assignees, project_third_party_members' — meaning this CASCADE is intentional design, not an oversight. However, the finding remains valid: the design choice to allow hard-delete of a ThirdParty with associated time entries is architecturally dangerous for an HR/billing system with legal retention requirements. The application intentionally offers this endpoint, making this a design-level risk rather than an implementation bug."
    },
    {
      "id": "DAT-010",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "destructive_drop_no_preflight",
      "title": "RBAC V4 drops role_permissions, permissions, role_configs tables and users.role column with no backup or preflight verification",
      "file": "packages/database/prisma/migrations/20260420120000_rbac_v4_drop_legacy/migration.sql",
      "line": "1-19",
      "code_evidence": "-- ACTION IRRÉVERSIBLE — Assurer un backup avant déploiement prod.\n\n-- 1. Drop FK contrainte role_permissions\nALTER TABLE \"role_permissions\" DROP CONSTRAINT IF EXISTS \"role_permissions_permissionId_fkey\";\nALTER TABLE \"role_permissions\" DROP CONSTRAINT IF EXISTS \"role_permissions_roleConfigId_fkey\";\n\n-- 2. Drop tables legacy\nDROP TABLE IF EXISTS \"role_permissions\";\nDROP TABLE IF EXISTS \"permissions\";\nDROP TABLE IF EXISTS \"role_configs\";\n\n-- 3. Drop colonne users.role\nALTER TABLE \"users\" DROP COLUMN IF EXISTS \"role\";\n\n-- 4. Drop enum type\nDROP TYPE IF EXISTS \"Role\";",
      "description": "This migration irreversibly drops three tables (role_permissions, permissions, role_configs) and the users.role enum column, with only a comment advising to 'ensure a backup before prod deployment'. There is no preflight DO $$ ... RAISE EXCEPTION $$ block verifying that the V0 backfill was 100% complete (every user has a non-null roleId) before the legacy data is destroyed. Unlike the preceding V0 migration which has explicit post-backfill verification, V4 has none. If run on a database where V0's backfill had silently failed (e.g., a user with an unmapped legacy role left roleId NULL), the DROP destroys the only evidence of what roles existed.",
      "root_cause": "The migration relies on a comment warning ('ensure backup') and the upstream V0 verification, but adds no self-contained guard to confirm the RBAC migration state before executing destructive DDL.",
      "impact": "If V0's check was bypassed or the DB was in an intermediate state (partial backup restore, manual migration replay), V4 permanently destroys RBAC data for all users. In normal sequential Prisma migration deployment V0 must pass before V4 can run — but on partial DB restores or manual replay this ordering is not enforced.",
      "suggested_fix": "Add a preflight DO block before the DROPs: DO $$ DECLARE null_count INTEGER; BEGIN SELECT COUNT(*) INTO null_count FROM users WHERE \"roleId\" IS NULL; IF null_count > 0 THEN RAISE EXCEPTION 'RBAC V4 preflight: % users still have NULL roleId — abort DROP', null_count; END IF; END $$;",
      "acceptance_criteria": [
        "If any user has roleId IS NULL, the V4 migration fails with an informative exception",
        "The migration succeeds only when all users have a valid roleId from V0 backfill"
      ],
      "verification_command": "SELECT COUNT(*) FROM users WHERE \"roleId\" IS NULL;",
      "related_findings": [],
      "cluster_id": "P",
      "found_by": "data_integrity",
      "confidence": "medium",
      "notes": "Code evidence VERBATIM confirmed at lines 1-19 (the entire file). V0 migration (20260419192835) does have correct post-backfill verification (lines 100-107: RAISE EXCEPTION if any user has NULL roleId). In normal sequential Prisma deployment V0 must succeed before V4 runs. Risk is limited to partial DB restore or manual migration replay scenarios. Downgraded from high to medium confidence because the normal deployment path is protected by V0's guard."
    },
    {
      "id": "DAT-011",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "predefined_task_recurring_rules lacks a XOR constraint ensuring exactly one of dayOfWeek (WEEKLY) or monthly fields (MONTHLY_DAY/MONTHLY_ORDINAL) is populated per recurrenceType",
      "file": "packages/database/prisma/migrations/20260424124537_add_recurrence_and_completion/migration.sql",
      "line": "8-11",
      "code_evidence": "ALTER TABLE \"predefined_task_recurring_rules\" ADD COLUMN     \"monthlyDayOfMonth\" INTEGER,\nADD COLUMN     \"monthlyOrdinal\" INTEGER,\nADD COLUMN     \"recurrenceType\" TEXT NOT NULL DEFAULT 'WEEKLY',\n    ALTER COLUMN \"dayOfWeek\" DROP NOT NULL;",
      "description": "After this migration, dayOfWeek becomes nullable and two new monthly fields are added. The schema comment says dayOfWeek is NULL for MONTHLY_DAY recurrenceType. But no CHECK constraint enforces the cross-column invariant: when recurrenceType='WEEKLY', dayOfWeek must be non-null and monthly fields must be null; when recurrenceType='MONTHLY_DAY', monthlyDayOfMonth must be non-null; when recurrenceType='MONTHLY_ORDINAL', monthlyOrdinal must be non-null. Without this constraint, it is possible to store recurrenceType='WEEKLY' with dayOfWeek=NULL (null WEEKLY rule), or recurrenceType='MONTHLY_DAY' with both monthlyDayOfMonth and monthlyOrdinal populated (ambiguous rule). No later migration adds this cross-column guard.",
      "root_cause": "The migration drops NOT NULL on dayOfWeek and adds the monthly alternatives without a compensating CHECK constraint to ensure exactly the right fields are populated per recurrenceType.",
      "impact": "Invalid recurrence rule combinations silently accepted at the DB layer produce incorrect or absent task assignments, causing HR teams to miss mandatory weekly predefined tasks or generate duplicate monthly assignments. Bugs are hard to diagnose because the data looks syntactically valid.",
      "suggested_fix": "ALTER TABLE \"predefined_task_recurring_rules\" ADD CONSTRAINT \"ptrr_recurrence_fields_xor_ck\" CHECK ( (\"recurrenceType\" = 'WEEKLY' AND \"dayOfWeek\" IS NOT NULL AND \"monthlyDayOfMonth\" IS NULL AND \"monthlyOrdinal\" IS NULL) OR (\"recurrenceType\" = 'MONTHLY_DAY' AND \"monthlyDayOfMonth\" IS NOT NULL AND \"dayOfWeek\" IS NULL AND \"monthlyOrdinal\" IS NULL) OR (\"recurrenceType\" = 'MONTHLY_ORDINAL' AND \"monthlyOrdinal\" IS NOT NULL AND \"dayOfWeek\" IS NULL AND \"monthlyDayOfMonth\" IS NULL) );",
      "acceptance_criteria": [
        "recurrenceType='WEEKLY' with dayOfWeek=NULL is rejected by check_violation",
        "recurrenceType='MONTHLY_DAY' with both monthlyDayOfMonth and monthlyOrdinal populated is rejected",
        "recurrenceType='WEEKLY' with dayOfWeek=1 and NULL monthly fields succeeds"
      ],
      "verification_command": "SELECT COUNT(*) FROM predefined_task_recurring_rules WHERE (\"recurrenceType\" = 'WEEKLY' AND \"dayOfWeek\" IS NULL) OR (\"recurrenceType\" = 'MONTHLY_DAY' AND \"monthlyDayOfMonth\" IS NULL) OR (\"recurrenceType\" = 'MONTHLY_ORDINAL' AND \"monthlyOrdinal\" IS NULL);",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence VERBATIM confirmed at lines 8-11. The recurrenceType column was promoted to a proper enum by DAT-012 (20260527130000), but no XOR cross-column CHECK was added in DAT-012 or any other migration. The XOR pattern exists for time_entries actor fields in the same migration batch (lines 168-174: time_entries_actor_xor_check), confirming awareness of this pattern. Current schema gap."
    },
    {
      "id": "DAT-012",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "predefined_task_recurring_rules.monthlyOrdinal and monthlyDayOfMonth have no range CHECK constraints despite documented semantics (1..5 and 1..31)",
      "file": "packages/database/prisma/migrations/20260424124537_add_recurrence_and_completion/migration.sql",
      "line": "8-9",
      "code_evidence": "ALTER TABLE \"predefined_task_recurring_rules\" ADD COLUMN     \"monthlyDayOfMonth\" INTEGER,\nADD COLUMN     \"monthlyOrdinal\" INTEGER,",
      "description": "The schema.prisma documents monthlyOrdinal as '1..5 (5 = dernière occurrence du mois) pour MONTHLY_ORDINAL' and monthlyDayOfMonth as '1..31 pour MONTHLY_DAY'. Neither column has a DB-level CHECK constraint anywhere in the migration history. An invalid monthlyDayOfMonth=32 or monthlyOrdinal=0 would be silently accepted, leading to recurrence rule evaluation errors at runtime (no matching day in month, undefined ordinal week). The business invariants migration (20260527120000) that fixed similar issues on weight, progress, allocation, etc. does not cover these fields.",
      "root_cause": "The recurrence rule columns were added as raw INTEGER without accompanying CHECK constraints, and the subsequent DAT-003/DAT-004 cleanup missed them.",
      "impact": "Invalid recurrence rules produce unpredictable task assignment behavior: monthlyDayOfMonth=32 generates no assignments or crashes the recurrence engine, monthlyOrdinal=6 may loop indefinitely or skip an entire month's assignments, corrupting the predefined task scheduling for HR teams.",
      "suggested_fix": "ALTER TABLE \"predefined_task_recurring_rules\" ADD CONSTRAINT \"ptrr_monthly_ordinal_ck\" CHECK (\"monthlyOrdinal\" IS NULL OR (\"monthlyOrdinal\" BETWEEN 1 AND 5)); ALTER TABLE \"predefined_task_recurring_rules\" ADD CONSTRAINT \"ptrr_monthly_day_of_month_ck\" CHECK (\"monthlyDayOfMonth\" IS NULL OR (\"monthlyDayOfMonth\" BETWEEN 1 AND 31));",
      "acceptance_criteria": [
        "INSERT with monthlyOrdinal=6 fails with check_violation",
        "INSERT with monthlyDayOfMonth=32 fails with check_violation",
        "INSERT with monthlyOrdinal=NULL succeeds (nullable column)",
        "INSERT with monthlyDayOfMonth=31 succeeds"
      ],
      "verification_command": "SELECT COUNT(*) FROM predefined_task_recurring_rules WHERE (\"monthlyOrdinal\" IS NOT NULL AND \"monthlyOrdinal\" NOT BETWEEN 1 AND 5) OR (\"monthlyDayOfMonth\" IS NOT NULL AND \"monthlyDayOfMonth\" NOT BETWEEN 1 AND 31);",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence VERBATIM confirmed at lines 8-9. Exhaustive grep across all migrations confirms no CHECK constraint exists for these two columns anywhere. Current schema gap — not a historical window issue."
    },
    {
      "id": "DAT-013",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "audit_immutability",
      "title": "audit_logs immutability trigger does not cover TRUNCATE — entire ledger can be wiped silently",
      "file": "packages/database/prisma/migrations/20260525190000_audit_logs_immutability_hash_chain_actor_snapshot/migration.sql",
      "line": "120-122",
      "code_evidence": "CREATE TRIGGER audit_logs_no_update_delete\n  BEFORE UPDATE OR DELETE ON \"audit_logs\"\n  FOR EACH ROW EXECUTE FUNCTION audit_logs_immutable();",
      "description": "The immutability trigger uses `FOR EACH ROW` and catches only UPDATE and DELETE events. PostgreSQL's TRUNCATE is a DDL-level operation that bypasses row-level triggers entirely. A privileged user (or a bug in a service that has DDL-level access) can execute `TRUNCATE audit_logs` and wipe the entire append-only ledger — hash chain, actor snapshots, and all — without the trigger firing once. The migration's stated goal is to make audit_logs append-only for Cour-des-Comptes compliance; TRUNCATE is the single operation that completely invalidates that guarantee at the DB level.",
      "root_cause": "`FOR EACH ROW BEFORE UPDATE OR DELETE` triggers are not fired by TRUNCATE in PostgreSQL; only a statement-level `BEFORE TRUNCATE` trigger can intercept it.",
      "impact": "Any role with TRUNCATE privilege on audit_logs (typically the database owner, superuser, or the application role if not explicitly revoked) can silently destroy the entire audit ledger. This directly voids the Cour-des-Comptes audit trail guarantee that DAT-009/OBS-002 were designed to enforce. The hash chain and actor snapshots are also wiped, making post-hoc integrity verification impossible. Partial mitigation exists: packages/database/prisma/init-roles.sql line 57 revokes TRUNCATE from app_user (runtime role), so the application runtime path is blocked. However the schema-owning role (orchestr_a/POSTGRES_USER, the migration role) retains TRUNCATE privilege and no statement-level TRUNCATE trigger exists to block it.",
      "suggested_fix": "Add a statement-level TRUNCATE trigger on audit_logs using the same function:\n```sql\nCREATE TRIGGER audit_logs_no_truncate\n  BEFORE TRUNCATE ON \"audit_logs\"\n  FOR EACH STATEMENT EXECUTE FUNCTION audit_logs_immutable();\n```\nAlso revoke TRUNCATE privilege from the application role (defense-in-depth):\n```sql\nREVOKE TRUNCATE ON audit_logs FROM <app_role>;\n```\nThe `audit_logs_immutable()` function already raises a generic `check_violation` exception regardless of TG_OP, so it will work correctly for TRUNCATE without modification.",
      "acceptance_criteria": [
        "TRUNCATE audit_logs raises 'audit_logs is append-only: TRUNCATE is not permitted' (SQLSTATE 23514)",
        "INSERT into audit_logs still succeeds after adding the TRUNCATE trigger",
        "UPDATE/DELETE on audit_logs still raise the same exception as before"
      ],
      "verification_command": "psql $DATABASE_URL -c \"TRUNCATE audit_logs;\" 2>&1 | grep 'audit_logs is append-only'",
      "related_findings": [],
      "cluster_id": "H",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "PostgreSQL docs: 'Row-level triggers fired for TRUNCATE are a PostgreSQL extension. They are not part of the SQL standard.' In practice, TRUNCATE simply does not fire FOR EACH ROW triggers at all. Code evidence verbatim confirmed at lines 120-122. No TRUNCATE trigger found in any migration file (grep across all migrations returned zero results). init-roles.sql line 57 does REVOKE UPDATE, DELETE, TRUNCATE ON audit_logs FROM app_user — this partially mitigates the runtime path but the schema-owning role retains full privilege. The statement-level trigger gap remains unaddressed."
    },
    {
      "id": "DAT-014",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "lock_risk_no_concurrently",
      "title": "25 CREATE INDEX statements in dat011_fk_indexes run without CONCURRENTLY — ACCESS EXCLUSIVE lock on every indexed table",
      "file": "packages/database/prisma/migrations/20260603115724_dat011_fk_indexes/migration.sql",
      "line": "1-74",
      "code_evidence": "-- CreateIndex\nCREATE INDEX \"comments_taskId_idx\" ON \"comments\"(\"taskId\");\n\n-- CreateIndex\nCREATE INDEX \"comments_authorId_idx\" ON \"comments\"(\"authorId\");\n\n-- CreateIndex\nCREATE INDEX \"departments_managerId_idx\" ON \"departments\"(\"managerId\");\n\n-- CreateIndex\nCREATE INDEX \"documents_projectId_idx\" ON \"documents\"(\"projectId\");",
      "description": "All 25 CREATE INDEX statements in this migration (finding originally counted 18, actual count is 25 from grep) use the standard (blocking) form without CONCURRENTLY. PostgreSQL acquires an ACCESS EXCLUSIVE lock for the duration of the index build, which blocks all concurrent reads and writes to the affected table. This migration touches high-traffic tables: comments, documents, events, projects, milestones, password_reset_tokens, predefined_task_recurring_rules, predefined_tasks, services, holidays, leave_validation_delegates, leaves, epics. On a live production database with millions of rows in any of these tables, the migration will cause a complete read+write outage for each table in sequence. Prisma wraps migrations in a transaction, so CONCURRENTLY cannot be used directly — the comment in dat015 acknowledges this pattern — but the risk on large tables remains unmitigated.",
      "root_cause": "All CREATE INDEX statements are non-concurrent, taking ACCESS EXCLUSIVE table locks; acceptable in a small dev DB but causes outages on large production tables.",
      "impact": "On any table with non-trivial data volume, all reads and writes are blocked for the duration of each index build. With 25 indexes in sequence, the total lock window could span seconds to minutes per table, causing request timeouts and visible service interruption on prod.",
      "suggested_fix": "For production deployments, run each CREATE INDEX CONCURRENTLY in a separate migration (or using a custom migration runner outside a transaction). Prisma cannot use CONCURRENTLY inside its default transactional migration wrapper; a workaround is to mark the migration as non-transactional in its migration.sql (add `-- No transaction` at the top for Prisma v3+ with the `--skip-generate` flag, or use a custom migration script). Alternatively, accept the lock window if tables are small and document the maintenance window requirement.",
      "acceptance_criteria": [
        "Either each index is built CONCURRENTLY in a separate non-transactional migration, or the prod deployment runbook documents a maintenance window for this migration",
        "No production requests are dropped or timeout due to table locks during the migration"
      ],
      "verification_command": "SELECT schemaname, tablename, indexname FROM pg_indexes WHERE indexname IN ('comments_taskId_idx','documents_projectId_idx','projects_createdById_idx') AND schemaname='public'; -- verify indexes exist after migration",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "CONFIRMED. Code evidence verbatim present. Migration file has 25 CREATE INDEX statements (finding title said '18' — minor count error, does not affect validity of finding). No CONCURRENTLY keyword and no '-- No transaction' directive present anywhere in the migration. The same pattern recurs in per010, per011, per012, per013, dat029 migrations. Flagged once here at the first occurrence (dat011). The dat015 migration itself explicitly notes 'NOTE: not CONCURRENTLY — migrations run inside a transaction', confirming this is a known constraint but not addressing the lock risk on large tables."
    },
    {
      "id": "DAT-015",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "project_snapshots.progress lacks CHECK (BETWEEN 0 AND 100) — DAT-003/004 coverage gap",
      "file": "packages/database/prisma/migrations/20260603130000_cor014_snapshot_unique_projectid_date/migration.sql",
      "line": "1-8",
      "code_evidence": "-- COR-014: Add unique constraint on (projectId, date) in project_snapshots\n-- Prevents duplicate snapshots from concurrent scheduler ticks.\n-- date is stored as startOfDay (midnight UTC), so this enforces one row per project per day.\n-- DropIndex replaces the plain index with the unique index (subsumes it).\nDROP INDEX IF EXISTS \"project_snapshots_projectId_date_idx\";\n\n-- CreateIndex\nCREATE UNIQUE INDEX \"project_snapshots_projectId_date_key\" ON \"project_snapshots\"(\"projectId\", \"date\");",
      "description": "The COR-014 migration adds the unique constraint on (projectId, date) but does not add CHECK constraints on project_snapshots numeric integrity. Cross-checking against the DAT-003/004 migration (20260527120000_dat003_dat004_business_invariants/migration.sql), that migration adds CHECK constraints for tasks.progress, epics.progress, project_members.allocation, etc. — but project_snapshots.progress (0.00–100.00 per schema comment) is absent. Additionally, there is no CHECK (tasksDone >= 0), CHECK (tasksTotal >= 0), or CHECK (tasksDone <= tasksTotal) on project_snapshots. A buggy scheduler tick or direct SQL fix could insert progress=150 or tasksDone=10 with tasksTotal=5 without any DB-level rejection.",
      "root_cause": "The DAT-003/004 migration that added business invariant CHECK constraints omitted project_snapshots entirely; the COR-014 migration which touches project_snapshots was a natural opportunity to add them but did not.",
      "impact": "A buggy snapshot service, a race condition, or a direct SQL admin correction could insert out-of-range progress values (e.g. > 100%) or impossible task counts (tasksDone > tasksTotal), which would cascade into incorrect analytics, dashboard displays, and KPI reports without any DB-level rejection.",
      "suggested_fix": "Add a follow-up migration:\n```sql\nALTER TABLE \"project_snapshots\"\n  ADD CONSTRAINT \"project_snapshots_progress_ck\" CHECK (\"progress\" BETWEEN 0 AND 100),\n  ADD CONSTRAINT \"project_snapshots_tasksDone_ck\" CHECK (\"tasksDone\" >= 0),\n  ADD CONSTRAINT \"project_snapshots_tasksTotal_ck\" CHECK (\"tasksTotal\" >= 0),\n  ADD CONSTRAINT \"project_snapshots_tasksDone_le_total_ck\" CHECK (\"tasksDone\" <= \"tasksTotal\");\n```\nVerify no existing rows violate these predicates before applying.",
      "acceptance_criteria": [
        "project_snapshots has CHECK (progress BETWEEN 0 AND 100)",
        "project_snapshots has CHECK (tasksDone >= 0) and CHECK (tasksTotal >= 0)",
        "project_snapshots has CHECK (tasksDone <= tasksTotal)",
        "INSERT of progress=101 into project_snapshots is rejected by the DB with a constraint violation"
      ],
      "verification_command": "SELECT conname, consrc FROM pg_constraint WHERE conrelid = 'project_snapshots'::regclass AND contype = 'c';",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "CONFIRMED. COR-014 migration code evidence is verbatim. Full grep of all migrations confirms zero CHECK constraints on project_snapshots. DAT-003/004 migration (20260527120000_dat003_dat004_business_invariants/migration.sql) confirmed to add CHECK constraints for tasks.progress, epics.progress, predefined_tasks.weight, project_members.allocation, leaves.days, leave_balances.totalDays, documents.size — but project_snapshots is explicitly absent. Schema.prisma comment says '0.00 – 100.00' but this is documentation only, not enforced at the DB level."
    },
    {
      "id": "DAT-016",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "dual_uniqueness_semantic_confusion",
      "title": "DAT-015 creates redundant dual-layer uniqueness on email: plain @unique (case-sensitive) + LOWER() functional index (case-insensitive) — Prisma findUnique bypasses the case-insensitive guard",
      "file": "packages/database/prisma/migrations/20260603140000_dat015_email_varchar254_lower_unique/migration.sql",
      "line": "9-16",
      "code_evidence": "-- AlterTable: cap email to VarChar(254) per RFC 5321 max length\nALTER TABLE \"users\" ALTER COLUMN \"email\" SET DATA TYPE VARCHAR(254);\n\n-- CreateIndex: case-insensitive unique index on email (LOWER() functional index)\n-- NOTE: not CONCURRENTLY — migrations run inside a transaction\nCREATE UNIQUE INDEX \"users_email_lower_uk\" ON \"users\" (LOWER(email));\n\n-- CreateIndex: case-insensitive unique index on login\nCREATE UNIQUE INDEX \"users_login_lower_uk\" ON \"users\" (LOWER(login));",
      "description": "The migration comment states 'The @unique PSL constraint is kept; these indexes do the case-insensitive enforcement.' However, the Prisma-managed plain @unique constraint on email (which creates a B-tree index on the raw email column) is case-sensitive and remains active. The new users_email_lower_uk LOWER() functional index is case-insensitive. This dual-layer setup means: (1) INSERT/UPDATE of 'User@Example.com' when 'user@example.com' already exists is correctly BLOCKED by the LOWER() index. (2) However, auth.service.ts validateUser() uses prisma.user.findUnique({ where: { email: login } }) at line 80-81, which uses the exact-match B-tree index. Emails are stored as-is (no toLowerCase before storage confirmed at users.service.ts:155). If the stored canonical form is 'User@Example.com' and the login attempt uses 'user@example.com', findUnique returns null and authentication fails, even though the uniqueness constraint would block creating a second user.",
      "root_cause": "Prisma's @unique PSL constraint generates a plain B-tree index that Prisma uses for findUnique calls; adding a separate LOWER() functional index enforces DB-level case-insensitive uniqueness at write time but does not make Prisma findUnique case-insensitive at read time. Emails are stored as-is (not normalized to lowercase), so the case-sensitivity gap on read paths is real.",
      "impact": "Users who registered with a mixed-case email (or created via admin import without normalization) cannot log in using a different case variant of that email. Authentication silently fails for case-variant email addresses. Registration via self-registration (auth.service.ts) also stores email as-is (line 333: `email: registerDto.email`).",
      "suggested_fix": "For consistent case-insensitive email handling: (1) Normalize all email inputs to lowercase in the application layer (auth.service.ts, users.service.ts) before DB storage AND before lookup. (2) Once all stored emails are lowercase, the LOWER() functional index and the plain @unique become equivalent and the plain one can be dropped via `DROP INDEX users_email_key`. Alternatively, replace Prisma findUnique with findFirst + raw WHERE LOWER(email) = LOWER($1) for auth lookups, but this bypasses Prisma's unique index optimization.",
      "acceptance_criteria": [
        "prisma.user.findUnique({ where: { email: 'USER@EXAMPLE.COM' } }) returns the same row as findUnique({ where: { email: 'user@example.com' } })",
        "Attempting to create a user with 'USER@EXAMPLE.COM' when 'user@example.com' exists fails with a unique constraint error",
        "No two users have emails that differ only in case"
      ],
      "verification_command": "SELECT email, COUNT(*) FROM users GROUP BY LOWER(email) HAVING COUNT(*) > 1; -- should return 0 rows after normalization",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "CONFIRMED. Migration code evidence is verbatim. auth.service.ts line 80-81: `prisma.user.findUnique({ where: { email: login } })` verified — no toLowerCase on login before call. users.service.ts line 155: `email: createUserDto.email` — stored as-is. Registration path (auth.service.ts line 333) also stores as-is. The migration comment acknowledges 'compatible with the existing @unique Prisma declarations used by findUnique calls in auth.service.ts' but this is the issue — findUnique on email is case-sensitive. The same dual-index pattern is applied to login but login is typically typed exactly so the risk is lower there."
    },
    {
      "id": "DAT-017",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing-index-on-fk",
      "title": "TaskDependency.dependsOnTaskId has no index — cascade delete on Task triggers seq-scan",
      "file": "packages/database/prisma/schema.prisma",
      "line": "391-403",
      "code_evidence": "model TaskDependency {\n  id              String   @id @default(uuid())\n  taskId          String\n  dependsOnTaskId String\n  createdAt       DateTime @default(now())\n\n  // Relations\n  task          Task @relation(\"TaskDependencies\", fields: [taskId], references: [id], onDelete: Cascade)\n  dependsOnTask Task @relation(\"DependentTasks\", fields: [dependsOnTaskId], references: [id], onDelete: Cascade)\n\n  @@unique([taskId, dependsOnTaskId])\n  @@map(\"task_dependencies\")\n}",
      "description": "The composite `@@unique([taskId, dependsOnTaskId])` creates an index with `taskId` as the leading column. Queries and FK checks driven from the `dependsOnTaskId` side (e.g., 'which tasks depend on task X?', or the PostgreSQL FK cascade when deleting a Task referenced as dependsOnTask) cannot use this index and will perform a full seq-scan on task_dependencies. As tasks accumulate, this becomes a lock/latency hotspot on every Task deletion or dependency lookup from the 'dependency target' side.",
      "root_cause": "Only the forward-direction unique index was declared; no reverse index was added for the `dependsOnTaskId` FK column.",
      "impact": "Every `DELETE FROM tasks WHERE id = $1` that has rows in task_dependencies (as either taskId or dependsOnTaskId) will seq-scan the table on the dependsOnTaskId side. At scale (thousands of tasks with dependency chains), this produces full-table locks during cascade deletes, potentially blocking the entire task mutation path.",
      "suggested_fix": "Add a reverse index:\n\n  @@index([dependsOnTaskId]) // reverse-lookup: tasks that depend on a given task\n\nMigration: pnpm run db:migrate",
      "acceptance_criteria": [
        "psql: `\\d task_dependencies` shows an index on dependsOnTaskId",
        "EXPLAIN on `SELECT * FROM task_dependencies WHERE \"dependsOnTaskId\" = $1` uses an index scan"
      ],
      "verification_command": "psql $DATABASE_URL -c \"SELECT indexname FROM pg_indexes WHERE tablename = 'task_dependencies';\"",
      "related_findings": [
        "PER-035"
      ],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW CONFIRMED. Verified schema.prisma lines 391-403 verbatim — no @@index([dependsOnTaskId]). Checked ALL migrations referencing task_dependencies (init + DAT-018): init only creates the composite unique index on (taskId, dependsOnTaskId); DAT-018 adds a no-self-loop CHECK and a cycle-prevention trigger — neither adds a dependsOnTaskId index. No migration anywhere adds this index. The DAT-029 pattern (@@index([serviceId]) on UserService, @@index([userId]) on ProjectMember) was not applied here."
    },
    {
      "id": "DAT-018",
      "severity": "important",
      "category": "data_integrity",
      "subcategory": "missing-index-on-fk",
      "title": "UserSkill.skillId has no standalone index — cascade delete on Skill triggers seq-scan",
      "file": "packages/database/prisma/schema.prisma",
      "line": "763-777",
      "code_evidence": "model UserSkill {\n  userId      String\n  skillId     String\n  level       SkillLevel\n  validatedBy String?\n  createdAt   DateTime   @default(now())\n  updatedAt   DateTime   @updatedAt\n\n  // Relations\n  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)\n  skill Skill @relation(fields: [skillId], references: [id], onDelete: Cascade)\n\n  @@id([userId, skillId])\n  @@map(\"user_skills\")\n}",
      "description": "The composite PK `@@id([userId, skillId])` creates a B-tree index with `userId` as the leading column. Looking up 'all users with skill X' (the reverse direction) or firing the `onDelete: Cascade` FK enforcement when deleting a Skill row requires scanning the entire `user_skills` table on the skillId column. The DAT-029 annotation explicitly added @@index([serviceId]) to UserService for the same structural reason; UserSkill was not covered.",
      "root_cause": "The composite PK only covers the userId-leading direction; no standalone index was declared for skillId.",
      "impact": "Deleting a Skill (e.g., when decommissioning an obsolete skill) causes PostgreSQL to seq-scan user_skills to cascade-delete dependent UserSkill rows. For organizations with many users and skills, this can cause table-level lock escalation. Querying 'which users have skill X' (a workload-planning query) also seq-scans.",
      "suggested_fix": "Add a reverse index:\n\n  @@index([skillId]) // DAT-029 pattern: reverse-lookup 'all users with skill X'\n\nMigration: pnpm run db:migrate",
      "acceptance_criteria": [
        "psql: `\\d user_skills` shows an index on skillId",
        "EXPLAIN on `SELECT * FROM user_skills WHERE \"skillId\" = $1` uses an index scan"
      ],
      "verification_command": "psql $DATABASE_URL -c \"SELECT indexname FROM pg_indexes WHERE tablename = 'user_skills';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW CONFIRMED. Verified schema.prisma lines 763-777 verbatim — no @@index([skillId]). Checked all migrations: no migration adds a skillId index on user_skills. The @@id([userId, skillId]) PK only covers userId-leading lookups. Finding is structurally identical to DAT-029 pattern that was applied to UserService but missed UserSkill."
    },
    {
      "id": "DAT-019",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "project_members.allocation has no CHECK BETWEEN 0 AND 100 — negative or impossible percentages accepted",
      "file": "packages/database/prisma/migrations/20251116093059_init/migration.sql",
      "line": "112",
      "code_evidence": "    \"allocation\" INTEGER,\n    \"startDate\" TIMESTAMP(3),\n    \"endDate\" TIMESTAMP(3),\n    \"createdAt\" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,\n\n    CONSTRAINT \"project_members_pkey\" PRIMARY KEY (\"id\")",
      "description": "project_members.allocation represents a percentage allocation of a team member's time to a project. No CHECK constraint enforces the [0, 100] range. Values like allocation=200 or allocation=-10 are silently accepted, producing misleading capacity planning data.",
      "root_cause": "No database-level constraint enforcing the percentage domain; only application-layer DTO validation guards this.",
      "impact": "Invalid allocation values corrupt resource capacity reports and project staffing calculations.",
      "suggested_fix": "ALTER TABLE project_members ADD CONSTRAINT project_members_allocation_ck CHECK (allocation BETWEEN 0 AND 100). Fixed by later migration 20260527120000_dat003_dat004_business_invariants.",
      "acceptance_criteria": [
        "INSERT INTO project_members (allocation) VALUES (101) raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='project_members'::regclass AND contype='c';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 112 of init migration. Fixed by migration 20260527120000_dat003_dat004_business_invariants which adds project_members_allocation_ck CHECK (allocation BETWEEN 0 AND 100). Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-020",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "tasks.progress and epics.progress have no CHECK BETWEEN 0 AND 100 — negative or >100 values accepted",
      "file": "packages/database/prisma/migrations/20251116093059_init/migration.sql",
      "line": "126-161",
      "code_evidence": "    \"progress\" INTEGER NOT NULL DEFAULT 0,\n    \"startDate\" TIMESTAMP(3),\n    \"endDate\" TIMESTAMP(3),\n    \"createdAt\" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    \"updatedAt\" TIMESTAMP(3) NOT NULL,\n\n    CONSTRAINT \"epics_pkey\" PRIMARY KEY (\"id\")\n...\n    \"progress\" INTEGER NOT NULL DEFAULT 0,",
      "description": "Both epics.progress and tasks.progress are INTEGER columns with no CHECK constraint. Values outside [0, 100] are silently accepted at the DB level. While DTO validation exists at the application layer, a buggy service path or direct SQL write can store progress=150 or progress=-5, corrupting dashboards and analytics that assume a percentage.",
      "root_cause": "Prisma does not support CHECK constraints in its DSL; hand-authored CHECK migration was not added alongside the column definition.",
      "impact": "Invalid progress values corrupt project completion dashboards and milestone tracking without any database-level rejection.",
      "suggested_fix": "ALTER TABLE tasks ADD CONSTRAINT tasks_progress_ck CHECK (progress BETWEEN 0 AND 100); same for epics. Fixed by later migration 20260527120000_dat003_dat004_business_invariants.",
      "acceptance_criteria": [
        "INSERT INTO tasks (progress) VALUES (101) raises check constraint violation",
        "INSERT INTO epics (progress) VALUES (-1) raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='tasks'::regclass AND contype='c' AND conname='tasks_progress_ck';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at lines 126 and 161 of init migration. Fixed by migration 20260527120000_dat003_dat004_business_invariants which adds tasks_progress_ck CHECK (progress BETWEEN 0 AND 100) and epics_progress_ck CHECK (progress BETWEEN 0 AND 100). Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-021",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "time_entries.hours has no CHECK for positive value or daily cap — zero and negative entries accepted",
      "file": "packages/database/prisma/migrations/20251116093059_init/migration.sql",
      "line": "198",
      "code_evidence": "    \"hours\" DOUBLE PRECISION NOT NULL,\n    \"description\" TEXT,\n    \"activityType\" \"ActivityType\" NOT NULL,\n    \"createdAt\" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    \"updatedAt\" TIMESTAMP(3) NOT NULL,\n\n    CONSTRAINT \"time_entries_pkey\" PRIMARY KEY (\"id\")",
      "description": "time_entries.hours is NOT NULL but has no CHECK constraint. Negative hours (e.g., -2.0) or absurdly large values (e.g., 1000) are accepted. The DTO layer enforces @Min(0.25) @Max(24) but a buggy service path bypasses this. A row with hours=-5 silently reduces project hour totals and causes incorrect billing calculations.",
      "root_cause": "No CHECK constraint at the DB level; only DTO validation at the application boundary.",
      "impact": "Negative or out-of-range time entries corrupt project billing totals and resource utilization reports.",
      "suggested_fix": "ALTER TABLE time_entries ADD CONSTRAINT time_entries_hours_ck CHECK (hours >= 0 AND hours <= 24). Fixed by later migration 20260528120000_dat032_dat033_position_and_hours_bounds.",
      "acceptance_criteria": [
        "INSERT INTO time_entries (hours) VALUES (-1) raises check constraint violation",
        "INSERT INTO time_entries (hours) VALUES (25) raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='time_entries'::regclass AND contype='c' AND conname='time_entries_hours_ck';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 198 of init migration. Fixed by migration 20260528120000_dat032_dat033_position_and_hours_bounds which adds time_entries_hours_ck CHECK (hours >= 0 AND hours <= 24). Note: the fix uses >= 0 (not > 0) to accommodate legitimate dismissal rows with hours=0 (101 such rows existed in dev DB at time of migration). Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-022",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "leaves.days has no CHECK > 0 — zero-day and negative leave records accepted",
      "file": "packages/database/prisma/migrations/20251116093059_init/migration.sql",
      "line": "215",
      "code_evidence": "    \"days\" DOUBLE PRECISION NOT NULL,\n    \"status\" \"LeaveStatus\" NOT NULL DEFAULT 'APPROVED',\n    \"comment\" TEXT,\n    \"createdAt\" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    \"updatedAt\" TIMESTAMP(3) NOT NULL,\n\n    CONSTRAINT \"leaves_pkey\" PRIMARY KEY (\"id\")",
      "description": "leaves.days is NOT NULL but has no CHECK constraint. A zero or negative day count is a logical impossibility for a leave request. Without this guard, a buggy or malicious caller could create a leave with days=0 (a no-op that still blocks approval queues) or days=-5 (that would inflate computed leave balances).",
      "root_cause": "No CHECK at the DB level enforcing the business invariant that a leave duration must be strictly positive.",
      "impact": "Zero or negative leave day records corrupt HR balance accounting and approval workflow logic.",
      "suggested_fix": "ALTER TABLE leaves ADD CONSTRAINT leaves_days_ck CHECK (days > 0). Fixed by later migration 20260527120000_dat003_dat004_business_invariants.",
      "acceptance_criteria": [
        "INSERT INTO leaves (days) VALUES (0) raises check constraint violation",
        "INSERT INTO leaves (days) VALUES (-1) raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='leaves'::regclass AND contype='c' AND conname='leaves_days_ck';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 215 of init migration. Fixed by migration 20260527120000_dat003_dat004_business_invariants which adds leaves_days_ck CHECK (days > 0). Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-023",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "text_time_fields",
      "title": "tasks.startTime, tasks.endTime, events.startTime, events.endTime stored as TEXT without format validation at DB level",
      "file": "packages/database/prisma/migrations/20260104102501_add_holidays_and_task_fields/migration.sql",
      "line": "12-13",
      "code_evidence": "ALTER TABLE \"tasks\" ADD COLUMN     \"endTime\" TEXT,\nADD COLUMN     \"startTime\" TEXT,\nALTER COLUMN \"projectId\" DROP NOT NULL;",
      "description": "startTime and endTime for tasks and events are stored as TEXT. No CHECK constraint enforces HH:MM format at the database level. Values like '25:99', '9:5', or empty strings are silently accepted. This allows malformed time values to enter the database and corrupt planning, export, and scheduling features that rely on string comparison and parsing.",
      "root_cause": "Using TEXT for time-of-day is a reasonable choice to avoid Prisma DateTime overhead, but without a format CHECK constraint the DB accepts any string.",
      "impact": "Malformed time values corrupt planning displays, calendar exports, and any feature performing time arithmetic via split(':').",
      "suggested_fix": "ADD CONSTRAINT tasks_startTime_format_ck CHECK (startTime ~ '^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$'); same for endTime. Fixed by later migration 20260527140000_dat013_time_format_check.",
      "acceptance_criteria": [
        "INSERT INTO tasks (startTime) VALUES ('25:00') raises check constraint violation",
        "INSERT INTO tasks (startTime) VALUES ('9:5') raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='tasks'::regclass AND contype='c' AND conname LIKE '%time%';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at lines 12-13 of add_holidays_and_task_fields migration. Fixed by migration 20260527140000_dat013_time_format_check which adds tasks_startTime_format_ck, tasks_endTime_format_ck, events_startTime_format_ck, events_endTime_format_ck, predefined_tasks_startTime_format_ck, predefined_tasks_endTime_format_ck with regex '^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$'. Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-024",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "events.recurrenceDay has no CHECK constraint — invalid day-of-week/month values silently accepted",
      "file": "packages/database/prisma/migrations/20260224231534_add_event_recurrence/migration.sql",
      "line": "4",
      "code_evidence": "ADD COLUMN     \"recurrenceDay\" INTEGER,\nADD COLUMN     \"recurrenceEndDate\" TIMESTAMP(3),\nADD COLUMN     \"recurrenceWeekInterval\" INTEGER;",
      "description": "events.recurrenceDay is a nullable INTEGER representing a day-of-week or day-of-month for recurring event logic. No CHECK constraint bounds the value. A value of 32 or -1 would silently corrupt recurring event expansion. Similarly, recurrenceWeekInterval has no CHECK >= 1 constraint, allowing zero or negative intervals that would create infinite or backward-expanding recurrences.",
      "root_cause": "Recurrence columns added as bare INTEGERs without domain constraints; only application-layer validation guards these.",
      "impact": "Out-of-range recurrenceDay or invalid recurrenceWeekInterval values cause recurring event expansion bugs, potentially producing unbounded loops or empty event sets.",
      "suggested_fix": "ALTER TABLE events ADD CONSTRAINT events_recurrenceDay_ck CHECK (\"recurrenceDay\" BETWEEN 0 AND 6); ALTER TABLE events ADD CONSTRAINT events_recurrenceWeekInterval_ck CHECK (\"recurrenceWeekInterval\" >= 1);",
      "acceptance_criteria": [
        "INSERT into events with recurrenceDay=8 raises constraint violation",
        "INSERT into events with recurrenceWeekInterval=0 raises constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='events'::regclass AND contype='c';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "medium",
      "notes": "Code evidence verbatim confirmed at lines 4-6 of add_event_recurrence migration. Searched all migration SQL files for 'recurrenceDay' and 'recurrenceWeekInterval' combined with 'check' or 'constraint' — found zero results. Only the creation migration references these columns; no later migration adds CHECK constraints. REMAINS OPEN — no fix migration exists. Confidence stays medium because recurrenceDay semantic range depends on application logic (may be 0-6 for weekly or 1-31 for monthly)."
    },
    {
      "id": "DAT-025",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "cascade_creator_fk",
      "title": "password_reset_tokens.createdById CASCADE — deleting admin who created token cascades to delete active token",
      "file": "packages/database/prisma/migrations/20260321112607_add_predefined_tasks_telework_recurring_password_reset/migration.sql",
      "line": "121",
      "code_evidence": "ALTER TABLE \"password_reset_tokens\" ADD CONSTRAINT \"password_reset_tokens_createdById_fkey\" FOREIGN KEY (\"createdById\") REFERENCES \"users\"(\"id\") ON DELETE CASCADE ON UPDATE CASCADE;",
      "description": "The createdById FK on password_reset_tokens cascades on delete. If an admin who created a password reset token is themselves deleted before the token is used, the cascade silently deletes the token, causing the password reset link to return 'invalid token' without any explanation. This is a user-facing data loss scenario.",
      "root_cause": "Cascade chosen for convenience without considering the lifecycle mismatch between the creator (admin) and the artifact (password reset token).",
      "impact": "An in-flight password reset for a user fails silently if the admin who created it is deleted before the user clicks the link.",
      "suggested_fix": "Change to ON DELETE SET NULL (with createdById nullable) or ON DELETE RESTRICT. Fixed by later migration 20260604103344_dat008_026_user_fk_full_erasure which changes to SET NULL.",
      "acceptance_criteria": [
        "Deleting an admin user does not delete their issued password_reset_tokens rows",
        "password_reset_tokens.createdById is set to NULL when the creator is deleted"
      ],
      "verification_command": "psql -c \"SELECT confdeltype FROM pg_constraint WHERE conname='password_reset_tokens_createdById_fkey';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at line 121 of add_predefined_tasks_telework_recurring_password_reset migration. Fixed by migration 20260604103344_dat008_026_user_fk_full_erasure which drops password_reset_tokens_createdById_fkey, makes createdById nullable, and re-adds it with ON DELETE SET NULL. Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-026",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "text_enum_columns",
      "title": "predefined_tasks.defaultDuration and predefined_task_assignments.period stored as TEXT — any string accepted",
      "file": "packages/database/prisma/migrations/20260321112607_add_predefined_tasks_telework_recurring_password_reset/migration.sql",
      "line": "22-40",
      "code_evidence": "    \"defaultDuration\" TEXT NOT NULL,\n    \"isActive\" BOOLEAN NOT NULL DEFAULT true,\n    \"createdById\" TEXT NOT NULL,\n...\n    \"period\" TEXT NOT NULL,\n    \"assignedById\" TEXT NOT NULL,",
      "description": "defaultDuration and period are TEXT columns intended to hold closed value sets ('HALF_DAY', 'FULL_DAY', 'TIME_SLOT' and 'MORNING', 'AFTERNOON', 'FULL_DAY' respectively). Without a CHECK constraint or enum type, any string is accepted. A typo like 'HALF_DAI' or 'FULLDAY' would silently corrupt assignment scheduling logic.",
      "root_cause": "Closed string value sets were implemented as TEXT without enum type or CHECK constraint; Prisma does not enforce string enums at the DB level.",
      "impact": "Invalid period or defaultDuration values corrupt predefined task scheduling; the application silently processes unexpected strings.",
      "suggested_fix": "Promote to native PostgreSQL enum types or add CHECK constraints. Fixed by later migration 20260527130000_dat012_promote_string_enums which converts these to proper enums.",
      "acceptance_criteria": [
        "INSERT INTO predefined_tasks (defaultDuration) VALUES ('INVALID') raises type error or check violation",
        "INSERT INTO predefined_task_assignments (period) VALUES ('INVALID') raises type error or check violation"
      ],
      "verification_command": "psql -c \"\\d predefined_tasks\" | grep defaultDuration",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "low",
      "notes": "Code evidence verbatim confirmed at lines 24 and 37 of add_predefined_tasks_telework_recurring_password_reset migration. Fixed by migration 20260527130000_dat012_promote_string_enums which creates PredefinedTaskDuration, DayPeriod, AssignmentCompletionStatus, RecurrenceType, AppSettingsCategory enums and converts columns accordingly. Downgraded to low confidence — issue resolved in current schema state; retained as historical record only."
    },
    {
      "id": "DAT-027",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_check_constraint",
      "title": "telework_recurring_rules.dayOfWeek and predefined_task_recurring_rules.dayOfWeek have no CHECK BETWEEN 0 AND 6",
      "file": "packages/database/prisma/migrations/20260321112607_add_predefined_tasks_telework_recurring_password_reset/migration.sql",
      "line": "7-53",
      "code_evidence": "    \"dayOfWeek\" INTEGER NOT NULL,\n    \"startDate\" TIMESTAMP(3) NOT NULL,\n    \"endDate\" TIMESTAMP(3),\n    \"isActive\" BOOLEAN NOT NULL DEFAULT true,\n    \"createdById\" TEXT NOT NULL,\n...\n    \"dayOfWeek\" INTEGER NOT NULL,\n    \"period\" TEXT NOT NULL,",
      "description": "Both telework_recurring_rules.dayOfWeek and predefined_task_recurring_rules.dayOfWeek store a day-of-week as INTEGER. No CHECK constraint ensures the value is in [0, 6] (Sunday=0 to Saturday=6). A value of 7 or -1 would silently corrupt the recurring schedule logic without any database-level rejection. No later migration adds this constraint.",
      "root_cause": "Day-of-week semantics encoded as bare INTEGER without domain enforcement; no CHECK added in any subsequent migration.",
      "impact": "Out-of-range day-of-week values corrupt recurring schedule generation, producing schedules that never expand or expand incorrectly.",
      "suggested_fix": "ALTER TABLE telework_recurring_rules ADD CONSTRAINT telework_recurring_rules_dayofweek_ck CHECK (\"dayOfWeek\" BETWEEN 0 AND 6); ALTER TABLE predefined_task_recurring_rules ADD CONSTRAINT predefined_task_recurring_rules_dayofweek_ck CHECK (\"dayOfWeek\" BETWEEN 0 AND 6);",
      "acceptance_criteria": [
        "INSERT INTO telework_recurring_rules (dayOfWeek) VALUES (7) raises check constraint violation",
        "INSERT INTO predefined_task_recurring_rules (dayOfWeek) VALUES (-1) raises check constraint violation"
      ],
      "verification_command": "psql -c \"SELECT conname FROM pg_constraint WHERE conrelid='telework_recurring_rules'::regclass AND contype='c';\"",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed at lines 5 and 52 of add_predefined_tasks_telework_recurring_password_reset migration. Searched all migration SQL files for any 'dayOfWeek' combined with 'check' or 'constraint' — found zero results. Migration 20260424124537_add_recurrence_and_completion makes dayOfWeek nullable in predefined_task_recurring_rules (for MONTHLY_ORDINAL/MONTHLY_DAY recurrence types) but adds no CHECK. REMAINS OPEN — no fix migration exists."
    },
    {
      "id": "DAT-028",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "audit_immutability",
      "title": "audit_logs actorId FK retains ON UPDATE CASCADE after migration removes ON DELETE CASCADE — asymmetry can trigger immutability violation",
      "file": "packages/database/prisma/migrations/20260525190000_audit_logs_immutability_hash_chain_actor_snapshot/migration.sql",
      "line": "101-104",
      "code_evidence": "ALTER TABLE \"audit_logs\" DROP CONSTRAINT \"audit_logs_actorId_fkey\";\nALTER TABLE \"audit_logs\" ADD CONSTRAINT \"audit_logs_actorId_fkey\"\n  FOREIGN KEY (\"actorId\") REFERENCES \"users\"(\"id\")\n  ON DELETE NO ACTION ON UPDATE CASCADE;",
      "description": "The migration's stated rationale (step 5 comment, lines 95-99) is that the previous ON DELETE SET NULL caused a cascade UPDATE on audit_logs rows which would be rejected by the immutability trigger (step 6) — so it was changed to ON DELETE NO ACTION. However the migration then re-adds ON UPDATE CASCADE on the same FK. If a users.id value were ever changed (e.g., by a migration that reassigns primary keys, an admin fix, or a future user-merge feature), the cascade UPDATE on audit_logs would fire the immutability trigger and abort the users UPDATE with a confusing 'audit_logs is append-only' error. The risk is latent since uuid ids are immutable in practice, but the asymmetry contradicts the stated design principle.",
      "root_cause": "The migration author correctly removed ON DELETE CASCADE/SET NULL to avoid cascade writes hitting the immutability trigger, but did not extend the same reasoning to ON UPDATE CASCADE.",
      "impact": "Any operation that changes users.id (currently none, but possible in a future migration or admin script) will fail with a confusing SQLSTATE 23514 from the immutability trigger, not a clear FK violation message. This could make users table maintenance operations undebuggably hard. Low probability given uuid usage (schema.prisma uses @default(uuid())), but the constraint specification is internally inconsistent.",
      "suggested_fix": "Change ON UPDATE CASCADE to ON UPDATE NO ACTION to be consistent with the ON DELETE NO ACTION:\n```sql\nALTER TABLE \"audit_logs\" DROP CONSTRAINT \"audit_logs_actorId_fkey\";\nALTER TABLE \"audit_logs\" ADD CONSTRAINT \"audit_logs_actorId_fkey\"\n  FOREIGN KEY (\"actorId\") REFERENCES \"users\"(\"id\")\n  ON DELETE NO ACTION ON UPDATE NO ACTION;\n```\nThe actorEmail/actorLabel snapshot columns already preserve actor identity for the audit record, so there is no data preservation need for cascading an id update.",
      "acceptance_criteria": [
        "audit_logs actorId FK uses ON UPDATE NO ACTION",
        "Attempting to UPDATE users.id raises a FK violation (23503), not an immutability violation (23514)"
      ],
      "verification_command": "psql $DATABASE_URL -c \"SELECT confupdtype FROM pg_constraint WHERE conname='audit_logs_actorId_fkey';\" 2>&1 | grep -E 'a|r'",
      "related_findings": [],
      "cluster_id": "H",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "pg_constraint.confupdtype: 'a' = NO ACTION, 'c' = CASCADE, 'r' = RESTRICT. Current state after migration: 'c' (CASCADE). Expected after fix: 'a' (NO ACTION). Code evidence verbatim confirmed at lines 101-104. No subsequent migration modifies this FK — the most recent migration touching audit_logs (20260604103344_dat008_026_user_fk_full_erasure) explicitly states 'NOT touched: audit_logs.actorId' in its header comment. users.id uses @default(uuid()) in schema.prisma — UUID reassignment is not done in practice by any existing service method, confirming low probability but non-zero architectural risk."
    },
    {
      "id": "DAT-029",
      "severity": "nit",
      "category": "data_integrity",
      "subcategory": "missing_length_check_on_hash_column",
      "title": "documents.contentSha256 is TEXT with no CHECK constraint — any arbitrary string can be stored as a SHA-256 hash",
      "file": "packages/database/prisma/migrations/20260603214608_dat025_document_fk_softdelete/migration.sql",
      "line": "2-3",
      "code_evidence": "-- AlterTable\nALTER TABLE \"documents\" ADD COLUMN     \"contentSha256\" TEXT,\nADD COLUMN     \"deletedAt\" TIMESTAMP(3),\nALTER COLUMN \"uploadedBy\" DROP NOT NULL;",
      "description": "The contentSha256 column stores a SHA-256 hex digest (64 hex characters). It is declared as TEXT (unbounded) with no CHECK constraint. This allows storing values of incorrect length (e.g. 32 chars for MD5, or truncated values) or non-hex characters without DB-level rejection. While the column is described as nullable ('no upload pipeline yet'), once it is populated by the upload service, a corrupted or wrong-format hash will silently pass validation and could later lead to incorrect integrity checks.",
      "root_cause": "Column declared as TEXT without a length constraint or format CHECK; SHA-256 hex digests have a fixed length of 64 characters.",
      "impact": "A bug or typo in the upload pipeline could store a malformed hash (wrong length, non-hex characters, uppercase vs lowercase inconsistency). Integrity verification would then silently fail or throw unexpected errors when the hash is used for comparison.",
      "suggested_fix": "Change to a fixed-length type or add a CHECK constraint:\n```sql\nALTER TABLE \"documents\" ALTER COLUMN \"contentSha256\" TYPE CHAR(64);\n-- or:\nALTER TABLE \"documents\" ADD CONSTRAINT \"documents_contentSha256_ck\" CHECK (\"contentSha256\" ~ '^[0-9a-f]{64}$');\n```\nAlso standardize on lowercase hex output in the upload pipeline.",
      "acceptance_criteria": [
        "INSERT of a 32-character string into documents.contentSha256 fails with a constraint error",
        "INSERT of a non-hex string into documents.contentSha256 fails with a constraint error",
        "INSERT of a valid 64-character lowercase hex string succeeds"
      ],
      "verification_command": "SELECT id, contentSha256 FROM documents WHERE contentSha256 IS NOT NULL AND contentSha256 !~ '^[0-9a-f]{64}$'; -- should return 0 rows if data is clean",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "CONFIRMED. Code evidence is verbatim. Full search of all migrations confirms contentSha256 appears only once (in dat025_document_fk_softdelete) and no subsequent migration adds a CHECK constraint. Schema.prisma declares it as `String?` (TEXT in PostgreSQL) with comment 'nullable — no upload pipeline yet'. Pre-emptive but appropriate to flag before the pipeline is built."
    },
    {
      "id": "DAT-030",
      "severity": "suggestion",
      "category": "data_integrity",
      "subcategory": "audit_completeness",
      "title": "dat037 cascade UPDATE of task.projectId produces no audit_logs rows — N tasks silently rewritten under Cour-des-Comptes scope",
      "file": "packages/database/prisma/migrations/20260528150000_dat037_task_project_consistency/migration.sql",
      "line": "40-47",
      "code_evidence": "-- AUDIT IMPLICATIONS (documented for Cour des Comptes): the parent-side CASCADE\n-- silently rewrites N task rows in one DDL fire. Today this is invariant maintenance\n-- (the task's \"true\" project IS the parent's), but a future auditor may want a row in\n-- audit_logs for each affected task. NOT added here — the existing OBS-002 trigger\n-- pipeline doesn't cover system-derived consistency writes (only user mutations\n-- through the app), and adding it now would be scope creep.",
      "description": "The epics_cascade_projectid_trg and milestones_cascade_projectid_trg AFTER UPDATE triggers silently rewrite task.projectId on potentially many rows (e.g. moving a large epic to another project rewrites all its tasks). The audit_logs table records the parent (epic/milestone) UPDATE, but none of the N cascaded task rewrites appear in audit_logs. A Cour-des-Comptes auditor examining a task's project attribution history has no DB-level evidence of the cascade; they must infer it from the parent's audit entry, which requires cross-referencing epicId/milestoneId and timing. This is acknowledged in the migration comment as deliberate out-of-scope, but the gap is real and worth a future task.",
      "root_cause": "The cascade is implemented as an AFTER UPDATE trigger on epics/milestones that issues a silent UPDATE on tasks, bypassing the application's AuditService which is the only path that writes to audit_logs.",
      "impact": "Post-hoc audit trail for task project re-attribution via epic/milestone moves is incomplete. A task could appear to have always been in its current project despite having been moved silently. Under Cour-des-Comptes audit review, this is an explainability gap rather than a security hole.",
      "suggested_fix": "File a follow-up task (e.g. DAT-037b) to add audit_logs INSERT rows inside the cascade trigger functions for each affected task row, bracketed by a SYSTEM_CONSISTENCY action code. This is non-trivial because the cascade trigger runs at the DB level without access to the app's AuditService; it would require a plpgsql INSERT into audit_logs directly (similar to how OBS-002 inserts RELEASE_DEPLOYED rows). Alternatively, move the cascade logic from a DB trigger into the application service layer (EpicsService.update) where AuditService is already wired.",
      "acceptance_criteria": [
        "When an epic's projectId is changed, audit_logs contains one row per affected task with action='TASK_PROJECT_REASSIGNED' or equivalent",
        "The audit chain remains intact (rowHash chains correctly through the new rows)"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "H",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "The migration author explicitly documented this gap. Severity is suggestion only because the parent's audit row provides indirect derivability and the comment discloses the limitation. Code evidence verbatim confirmed at lines 40-47 of the migration file. EpicsService (apps/api/src/epics/epics.service.ts) contains no AuditService injection or audit logging for cascade task updates — confirmed via grep. The cascade trigger functions (epics_cascade_projectid_to_tasks, milestones_cascade_projectid_to_tasks) issue bare UPDATE statements with no audit side-effects."
    },
    {
      "id": "DAT-031",
      "severity": "suggestion",
      "category": "data_integrity",
      "subcategory": "non_partial_index_on_nullable_hot_filter",
      "title": "password_reset_tokens index (userId, usedAt) is non-partial — hot query path WHERE usedAt IS NULL will scan all rows including consumed tokens",
      "file": "packages/database/prisma/migrations/20260604050007_dat028_password_reset_token_indexes/migration.sql",
      "line": "2-3",
      "code_evidence": "-- CreateIndex\nCREATE INDEX \"password_reset_tokens_userId_usedAt_idx\" ON \"password_reset_tokens\"(\"userId\", \"usedAt\");",
      "description": "The auth service's hot query is `updateMany({ where: { userId, usedAt: null } })` (auth.service.ts:501) — finding all unused tokens for a user. The composite index (userId, usedAt) includes all rows, both used (usedAt IS NOT NULL, historical) and unused (usedAt IS NULL, active). Over time, as users request password resets repeatedly, the index grows with consumed token rows. A partial index WHERE usedAt IS NULL would be far smaller and faster for the hot path, since the active token set is always a tiny fraction of the historical set.",
      "root_cause": "The index was created as a full composite index; the access pattern (filter on usedAt IS NULL) would be more efficiently served by a partial index.",
      "impact": "As the password_reset_tokens table accumulates history (each admin-generated reset creates a row), the index becomes progressively less efficient for the unused-token lookup. This is a gradual performance degradation rather than an immediate bug, but in large deployments or security-heavy environments with frequent token generation it becomes significant.",
      "suggested_fix": "Replace with a partial index:\n```sql\nDROP INDEX IF EXISTS \"password_reset_tokens_userId_usedAt_idx\";\nCREATE INDEX \"password_reset_tokens_userId_active_idx\" ON \"password_reset_tokens\"(\"userId\") WHERE \"usedAt\" IS NULL;\n```\nKeep the expiresAt index for GC scans.",
      "acceptance_criteria": [
        "EXPLAIN ANALYZE on `SELECT * FROM password_reset_tokens WHERE userId = 'x' AND usedAt IS NULL` uses the partial index (Index Scan) rather than a full composite index scan"
      ],
      "verification_command": "EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM password_reset_tokens WHERE \"userId\" = 'test-uuid' AND \"usedAt\" IS NULL;",
      "related_findings": [],
      "cluster_id": "I",
      "found_by": "data_integrity",
      "confidence": "high",
      "notes": "CONFIRMED. Index code evidence is verbatim. auth.service.ts line 501: `where: { userId, usedAt: null }` confirmed. The existing index does provide a benefit over no index, but a partial index would be strictly superior for this access pattern."
    }
  ],
  "verified_clean": [
    "20251116093059_init: all FK relationships use appropriate ON DELETE SET NULL for nullable optional references (departmentId, epicId, milestoneId, assigneeId)",
    "20251116093059_init: migration wrapped in standard Prisma-generated DDL — appears transactional",
    "20251116093059_init: project_members composite unique (projectId, userId) prevents duplicate memberships",
    "20251116093059_init: skills.name unique index prevents duplicate skill definitions",
    "20251116093059_init: task_dependencies composite unique (taskId, dependsOnTaskId) prevents duplicate dependency edges",
    "20251116093059_init: task_raci composite unique (taskId, userId, role) prevents duplicate RACI assignments",
    "20251116093059_init: telework_schedules composite unique (userId, date) prevents duplicate entries per day",
    "20251116093059_init: user_services composite unique (userId, serviceId) prevents duplicate memberships",
    "20251116093059_init: users.email and users.login have unique indexes — business key integrity enforced",
    "20251215120000_add_missing_tables: NOT NULL migration pattern (add nullable → populate → set NOT NULL) is correct and safe",
    "20251215120000_add_missing_tables: app_settings.key unique index enforces singleton settings keys",
    "20251215120000_add_missing_tables: data migration correctly handles NULL leave_type_id by defaulting to CP type (line 92)",
    "20251215120000_add_missing_tables: leave_type_configs.code unique index prevents duplicate leave type codes",
    "20251225160000_add_task_assignees: appropriate CASCADE on both FKs — task assignee list is operational data",
    "20251225160000_add_task_assignees: task_assignees composite unique (taskId, userId) prevents duplicate assignments",
    "20260104102501_add_holidays_and_task_fields: holidays.createdById uses ON DELETE RESTRICT — preserves creator accountability",
    "20260104102501_add_holidays_and_task_fields: holidays.date unique index prevents duplicate holiday entries per date",
    "20260104102501_add_holidays_and_task_fields: leaves.type DROP NOT NULL is safe given leave_type_id was added as the new typed reference",
    "20260214192223_add_chef_projet_events_ext_intervention: event_participants uses composite PK as unique constraint",
    "20260214192223_add_chef_projet_events_ext_intervention: events.projectId uses ON DELETE SET NULL — events survive project deletion",
    "20260214200033_add_rbac_dynamic_permissions: permissions.code and role_configs.code have unique indexes",
    "20260214200033_add_rbac_dynamic_permissions: role_permissions CASCADE on both sides — appropriate for RBAC join table",
    "20260214222914_add_external_intervention_to_events: trivial boolean column addition — no data integrity risk",
    "20260221170751_add_service_color_user_avatar: nullable column additions — no data integrity risk",
    "20260222150213_add_started_status_and_hidden_statuses: enum addition is non-destructive; hiddenStatuses default empty array is correct",
    "20260223094815_add_visible_statuses: visibleStatuses TEXT[] default empty array — no integrity risk",
    "20260223102341_add_metier_roles: enum additions are non-destructive and transactional in PostgreSQL 12+",
    "20260224231534_add_event_recurrence: parentEventId CASCADE is appropriate — child recurring instances should be deleted when parent is deleted",
    "20260321105758_add_leave_balances_and_rbac_granularity: leave_balances composite unique (userId, leaveTypeId, year) correctly prevents duplicate per-user annual balances",
    "20260321112607_add_predefined_tasks_telework_recurring_password_reset: password_reset_tokens.token unique index — security invariant for token uniqueness",
    "20260321112607_add_predefined_tasks_telework_recurring_password_reset: predefined_task_assignments composite unique prevents duplicate assignments per task/user/date/period",
    "20260321112607_add_predefined_tasks_telework_recurring_password_reset: telework_recurring_rules composite unique (userId, dayOfWeek, startDate) prevents duplicate rules",
    "20260330142411_add_cancellation_requested_status: enum addition is non-destructive",
    "20260330185549_add_week_interval_to_recurring_rules: weekInterval NOT NULL DEFAULT 1 — safe default, no pre-existing rows require migration",
    "20260402072633_add_subtasks: CASCADE on subtasks.taskId — subtasks are intrinsically part of a task",
    "20260402072633_add_subtasks: subtasks.taskId index supports efficient task->subtasks lookup",
    "20260404211126_add_project_snapshots: tasksTotal and tasksDone are INTEGER (not float) — correct for count columns",
    "20260404215113_add_project_icon_manager_sponsor: managerId and sponsorId use ON DELETE SET NULL — projects survive personnel changes",
    "20260405120000_remove_started_status: TaskStatus enum replacement is correctly ordered (UPDATE data → DROP DEFAULT → ALTER TYPE → DROP OLD → RENAME); only tasks and projects.hiddenStatuses use the enum, both are updated; migration is effectively atomic under Prisma's implicit transaction wrapper.",
    "20260405215351_add_external_intervention_to_predefined_tasks: simple boolean ADD COLUMN with NOT NULL DEFAULT false — idempotent, no data-loss risk.",
    "20260411100717_add_third_parties_and_time_entry_actor_xor: XOR CHECK constraint (userId IS NOT NULL AND thirdPartyId IS NULL) OR (userId IS NULL AND thirdPartyId IS NOT NULL) is correctly defined; backfill of declaredById from userId is complete with post-backfill verification; migration wraps in explicit BEGIN/COMMIT; pre-checks prevent partial re-runs.",
    "20260415131201_add_force_password_change: boolean ADD COLUMN with NOT NULL DEFAULT false — no data-loss, no cascade risk.",
    "20260415135502_add_refresh_tokens: CASCADE on userId FK is correct (tokens should not outlive users); indexes on tokenHash (UNIQUE), userId, and expiresAt provide correct coverage for authentication and cleanup queries.",
    "20260419192835_rbac_v0_add_roles_table: 26-role seed is idempotent via ON CONFLICT DO NOTHING; backfill is idempotent (WHERE roleId IS NULL); post-backfill check correctly rejects any user with unmapped legacy role; roleId nullable at this stage is intentional (becomes NOT NULL in V4).",
    "20260421152235_add_timeentry_dismissal_and_composite_index: boolean ADD COLUMN, composite index creation — no data-loss, no cascade risk.",
    "20260423075303_add_clients_module: clients and project_clients tables correctly use CASCADE on project_clients FK (join table semantics); clients.isActive soft-delete pattern present.",
    "20260423175724_add_snapshot_analytics_fields: integer counter columns added with NOT NULL DEFAULT 0; composite index on (projectId, date) appropriate for time-series queries.",
    "20260424111457_add_weight_and_audit_log: audit_logs CREATE TABLE structure is correct (JSONB payload, nullable actorId); indexes on (entityType, entityId) and (actorId, createdAt) are appropriate for lookup patterns.",
    "20260427171000_add_predefined_task_telework_allowed: boolean ADD COLUMN with NOT NULL DEFAULT true — no data-loss.",
    "20260506114304_project_archive: nullable archivedAt/archivedById columns correctly allow NULL for non-archived projects; SET NULL on archivedById FK is appropriate (user deletion should not cascade-delete project archive metadata).",
    "20260523124537_drop_max_days_per_year: column drop is documented with Prisma warning comment; leave type configuration logic change is intentional per design.",
    "20260523171000_self_approved_and_global_balance_unique: selfApproved boolean with DEFAULT false correctly identifies pre-existing rows as non-self-approved; partial UNIQUE INDEX on (leaveTypeId, year) WHERE userId IS NULL correctly handles PostgreSQL NULLS DISTINCT semantics for global balance rows.",
    "20260524100000_dat005_backup_float_columns: backup tables use CREATE TABLE IF NOT EXISTS and INSERT ... ON CONFLICT (id) DO NOTHING, making the migration idempotent; splitting into a separate migration from the conversion ensures the backup is committed before the ALTER runs; DOUBLE PRECISION is correct for a backup (preserving the original IEEE 754 representation for audit purposes).",
    "20260603120000_drop_dat005_backup_tables: DROP TABLE IF EXISTS is idempotent; backup tables contain no live business data (only pre-conversion Float snapshots, retention decision documented in comment); no cascade to live tables",
    "20260603120451_per010_leave_missing_indexes: indexes correctly support documented hot queries (user leave history, status-scoped calendar, per-user-per-type range); no lock risk flagged separately from D4-1 cluster",
    "20260603121211_per011_user_missing_indexes: indexes on isActive, departmentId, roleId are standard FK/predicate indexes; no data integrity issue",
    "20260603121745_per012_event_missing_indexes: event_participants.userId, events.date, events.(createdById,date) composite — straightforward FK+predicate indexes; no data integrity issue",
    "20260603122315_per013_task_planning_indexes: tasks endDate/startDate/milestoneId/epicId/(projectId,status) — all correct FK and composite indexes; no issue",
    "20260603130000_cor014_snapshot_unique_projectid_date: unique index correctly replaces plain index; DROP INDEX IF EXISTS is safe; window between DROP and CREATE UNIQUE is transactional (PG atomic DDL within session); addresses COR-014 scheduler race condition",
    "20260603215133_dat022_department_fk_restrict: FK change from non-Restrict to RESTRICT on users.departmentId correctly prevents department deletion when users remain; no data loss risk",
    "20260604050729_add_task_confidential_flag: BOOLEAN NOT NULL DEFAULT false is safe; no data-loss; backfill not needed due to DEFAULT",
    "20260604051255_add_cancelled_to_leave_status: ALTER TYPE ADD VALUE is transactional in PostgreSQL 18 (PG 18 fully supports this in a transaction block); CANCELLED value correctly added to LeaveStatus enum",
    "20260604051843_dat029_join_table_indexes: project_members.userId and user_services.serviceId reverse-lookup indexes are correct; no data integrity issue",
    "20260604103344_dat008_026_user_fk_full_erasure: 12 FK columns correctly changed from NOT NULL to nullable + ON DELETE SET NULL; audit_logs.actorId explicitly excluded (ON DELETE NO ACTION, correct per immutability trigger); comment documents compliance decision",
    "20260604110510_dat026_user_deletedat_shell: simple nullable TIMESTAMP column addition with performance index; no data loss risk; correctly enables soft-delete shell pattern for users with audit_log references",
    "AuditLog immutability: DB-level trigger enforced (documented lines 1219-1226), append-only enforced at privilege layer (TOOL-DEPLOY-001 two-role split lines 10-18).",
    "Business-key @@unique coverage: Service has @@unique([departmentId, name]); UserService has @@unique([userId, serviceId]); ProjectMember has @@unique([projectId, userId]); TaskAssignee has @@unique([taskId, userId]); PredefinedTaskAssignment has @@unique([predefinedTaskId, userId, date, period]); TeleworkSchedule has @@unique([userId, date]); ProjectSnapshot has @@unique([projectId, date]); LeaveBalance has @@unique([userId, leaveTypeId, year]) + partial-unique index for NULL userId case documented at lines 696-698.",
    "Document soft-delete: deletedAt indexed (DAT-025), uploadedBy FK with SetNull on User deletion.",
    "Enums cover all known closed value sets: TaskStatus, Priority, ProjectStatus, MilestoneStatus, LeaveStatus, HalfDay, LeaveType, ActivityType, ThirdPartyType, HolidayType, SchoolVacationZone, SchoolVacationSource, RACIRole, SkillCategory, SkillLevel, PredefinedTaskDuration, DayPeriod, AssignmentCompletionStatus, RecurrenceType, AppSettingsCategory — all present.",
    "FK indexes: all FK columns that are not leading columns of a composite index have explicit @@index declarations (DAT-011 pattern applied consistently across Department, Service, Project, Epic, Milestone, Task, LeaveBalance, Leave, TeleworkRecurringRule, PredefinedTask, PredefinedTaskRecurringRule, Holiday, SchoolVacation, Event, Document, Comment, PasswordResetToken, Role).",
    "Float/Decimal: zero Float fields exist — every money/time/hours/balance field uses Decimal with @db.Decimal precision (DAT-005 annotations at lines 216, 323, 454, 619, 704). No IEEE-754 drift risk.",
    "Holiday.date: @@unique([date]) prevents duplicate public holidays on the same date.",
    "LeaveTypeConfig: missing onDelete on Leave→LeaveTypeConfig (line 635) — leave type cannot be cascade-deleted while leave rows reference it; Prisma defaults to Restrict which is appropriate here.",
    "Nullable vs NOT NULL: roleId on User is intentionally nullable during RBAC V4 backfill transition (documented at lines 35-37); deletedAt nullable is intentional soft-delete marker (DAT-026); all other required business fields are NOT NULL.",
    "PasswordResetToken: token is @unique, expiresAt and userId,usedAt indexed (DAT-028).",
    "ProjectMember.allocation Int? (0-100 range): absence of CHECK is consistent with other range fields; low risk as it's a reporting hint not a financial calculation.",
    "RBAC V4 Role model: code is @unique, templateKey is NOT NULL (documented lines 1196-1198), @@index([templateKey]) and @@index([isDefault]) present.",
    "RefreshToken: tokenHash is @unique, expiresAt and userId are indexed — security-sensitive fields properly constrained.",
    "SchoolVacation: @@unique([name, zone, year]) prevents duplicate vacation periods.",
    "TaskDependency self-reference (taskId == dependsOnTaskId): not protected by CHECK in DB; application layer guards this. Risk is low (circular dependency detection is the real concern, not self-reference).",
    "User email: @db.VarChar(254) per RFC 5321, @unique for Prisma compatibility, LOWER() functional unique index documented as added via raw SQL (DAT-015 comment lines 27-29).",
    "User.login: @unique present, unbounded String acceptable (login is not email-length-constrained).",
    "audit_logs ON DELETE NO ACTION: prevents cascade DELETE from users hitting immutability trigger (SetNull path correctly removed)",
    "audit_logs immutability (dat009): BEFORE UPDATE OR DELETE trigger created AFTER all backfill UPDATEs — ordering is correct and documented",
    "audit_logs immutability: trigger function uses ERRCODE='check_violation' (23514) distinguishable from role denial (42501)",
    "dat003/004: 14 CHECK constraints cover date-range ordering (>=) and numeric bounds; NULL semantics (CHECK passes when NULL) correctly noted for nullable columns",
    "dat003: events.recurrenceEndDate >= date (nullable column — NULL check passes cleanly per SQL 3VL)",
    "dat003: leaves.days > 0 strict positive (product invariant: calculateLeaveDays floored at 0.5); leave_balances.totalDays >= 0 allows zero balance",
    "dat005: Float→NUMERIC(5,2/6,2) conversions use USING cast with backup tables pre-created in companion migration 20260524100000; no silent data loss on precision reduction",
    "dat005: No monetary columns in schema — 'money uses Decimal' checklist item N/A per migration comment",
    "dat005: time_entries.hours NUMERIC(5,2) — single-row max 999.99h, not a 24h ceiling (single TimeEntry, not daily aggregate); dat032/033 adds hours<=24 CHECK",
    "dat007: Epic/Milestone/ProjectMember/ProjectClient/ProjectThirdPartyMember intentionally stay CASCADE (operational link data, not audit history) — verified consistent with migration rationale",
    "dat007: tasks/project_snapshots/documents/time_entries FK switched from CASCADE/SetNull to RESTRICT — hard-delete of project with history is now blocked at DB level",
    "dat010: four composite/range indexes on leaves table (userId+status, validator_id+status, startDate+endDate, leave_type_id+status) — additive, no lock concern on small HR table",
    "dat012: AppSettingsCategory enum values ('display','general','planning') — lowercase matches existing data; default 'general' re-set correctly",
    "dat012: String→enum promotion uses DROP DEFAULT / ALTER TYPE USING / SET DEFAULT pattern correctly; no missing default transitions",
    "dat013: HH:MM format CHECK regex '^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$' is lenient superset of DTO regex — no app-accepts/DB-rejects mismatch; rejects '9:5', '25:99', '' as stated",
    "dat013: NULL time columns pass CHECK (NULL semantics) — no spurious NOT NULL constraint introduced",
    "dat014: enum_range(NULL::\"LeaveType\") used instead of hardcoded list — future enum members auto-included",
    "dat014: known limitation (config-side code change does not propagate to existing leaves) documented in migration; out of scope for this check",
    "dat014: leaves_sync_type_from_config BEFORE INSERT OR UPDATE trigger derives type from leaveTypeId FK — eliminates dual source of truth; one-time backfill runs before trigger attach",
    "dat016: departments_name_key UNIQUE globally; services_departmentId_name_key composite UNIQUE per department — matches app semantics",
    "dat017: tasks_parent_requires_project_ck CHECK (projectId IS NOT NULL OR (epicId IS NULL AND milestoneId IS NULL)) — NULL semantics correctly analyzed in comment",
    "dat018: UPDATE correctness — OLD row excluded via (TG_OP='INSERT' OR id <> OLD.id) in both CTE arms",
    "dat018: task_dependencies_no_self_ck CHECK taskId <> dependsOnTaskId; cycle-prevention trigger uses RECURSIVE CTE UNION (dedup terminates on pre-existing cycles)",
    "dat021 GIN index on audit_logs(payload jsonb_path_ops): correct operator class for containment @> queries; btree on schemaVersion for dispatch",
    "dat021 hash-chain consequence: acknowledged that recompute script must run post-migration; pre-existing rows verify only after recompute (documented)",
    "dat021 schemaVersion ADD COLUMN NOT NULL DEFAULT 1: Postgres PG11+ stores the default in catalog without table rewrite — no row-level UPDATE, no immutability trigger fire",
    "dat023: CREATE EXTENSION IF NOT EXISTS is idempotent; superuser requirement documented in migration comment and deploy doc",
    "dat023: EXCLUDE USING gist with btree_gist — daterange(...,'[]') inclusive bounds match checkOverlap inclusive semantics",
    "dat023: partial WHERE (status='APPROVED') — re-checks on PENDING→APPROVED transition (the TOCTOU race path) and allows PENDING/REJECTED/CANCELLED overlaps",
    "dat032: subtasks_position_ck CHECK position >= 0 — no negative positions",
    "dat033: TOCTOU aggregate cap (concurrent same-day entries summing past 24h) acknowledged as out of scope — correct assessment; cross-row CHECKs not expressible per-row",
    "dat033: time_entries_hours_ck CHECK hours >= 0 AND hours <= 24 — admits hours=0 for dismissal rows (101 in dev), correct superset of DTO bound",
    "dat035: project_members_role_length_ck CHECK char_length(role) BETWEEN 1 AND 100 — whitespace-only acknowledged as out of scope; DTO layer trims",
    "dat036: clients_name_key UNIQUE — closes DAT-016 gap; redundant non-unique clients_name_idx dropped first",
    "dat037: epics/milestones cascade triggers fire AFTER UPDATE OF projectId only (not on INSERT) — no spurious cascade on initial project assignment",
    "dat037: tasks_project_consistency_trg skips comparison when projectId IS NULL (defers to DAT-017 CHECK) — no signal shadowing",
    "dat038: UPDATE correctness — OLD row excluded via (TG_OP='INSERT' OR e.id <> OLD.id) in both CTE arms; mirrors DAT-018 learning #3",
    "dat038: events_parent_no_self_ck uses IS DISTINCT FROM (tolerates NULL parentEventId); cycle trigger has NULL short-circuit on hot path",
    "obs012 deployments table: additive CREATE TABLE only, no FK (deployedBy is frozen string, survives user deletion), indexed on deployedAt and (environment, deployedAt)",
    "onDelete behavior: Project→Task is Restrict (prevents orphaned tasks on hard-delete, DAT-007); ProjectSnapshot is Restrict; TimeEntry→Project is Restrict; Document→Project is Restrict. User→Department is Restrict; manager FKs use SetNull throughout. Leave→User is Cascade; LeaveBalance→User is Cascade. AuditLog→User is NoAction (intentional: immutability trigger blocks SetNull, documented at lines 1223-1226). All reviewed and appropriate."
  ]
}
