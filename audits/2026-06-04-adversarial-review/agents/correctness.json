{
  "category": "correctness",
  "finding_count": 69,
  "findings": [
    {
      "id": "COR-001",
      "severity": "blocking",
      "category": "correctness",
      "subcategory": "authorization_scope_bypass",
      "title": "canValidate: delegation check is not scoped to the leave's assigned validator — any delegate can approve any leave",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "1638-1650",
      "code_evidence": "    // Vérifier les délégations actives\n    const today = new Date();\n    const activeDelegation =\n      await this.prisma.leaveValidationDelegate.findFirst({\n        where: {\n          delegateId: validatorId,\n          isActive: true,\n          startDate: { lte: today },\n          endDate: { gte: today },\n        },\n      });\n\n    return activeDelegation !== null;",
      "description": "The delegation branch of `canValidate` finds any active delegation where the calling user is `delegateId`, without requiring that the delegation's `delegatorId` matches `leave.validatorId`. If user Alice was delegated by Manager A (who manages dept A), she can also approve leaves of employees in dept B whose assigned validator is Manager B — as long as she has any active delegation. The three preceding checks (MANAGE_ANY_LEAVES, exact validatorId match, APPROVE_LEAVES+service-scope) are all bypassed by this last fallback.\n\nThe `findValidatorForUser` docstring explicitly says a delegate found for manager B 'must never become the validator for dept A users', but `canValidate` does exactly that through this undiscriminating delegation query.",
      "root_cause": "The `findFirst` query filters on `delegateId` only, omitting the join condition `delegatorId = leave.validatorId` that would scope the delegation to the leave's actual assigned validator.",
      "impact": "Any user who has received at least one active delegation from any manager can approve or reject any pending leave request in the entire organisation, regardless of service scope. This is a complete authorization bypass for the approval workflow.",
      "suggested_fix": "Add `delegatorId: leave.validatorId` to the where clause (requires the leave be fetched with its `validatorId`). If `leave.validatorId` is null (no validator assigned), the delegation fallback should return false. Example fix:\n```ts\nif (leave.validatorId) {\n  const activeDelegation = await this.prisma.leaveValidationDelegate.findFirst({\n    where: {\n      delegatorId: leave.validatorId,\n      delegateId: validatorId,\n      isActive: true,\n      startDate: { lte: today },\n      endDate: { gte: today },\n    },\n  });\n  if (activeDelegation) return true;\n}\nreturn false;",
      "acceptance_criteria": [
        "A user with an active delegation from manager A cannot approve a leave whose validatorId is manager B",
        "A user with an active delegation from the leave's validatorId can approve that leave",
        "Existing tests for canValidate pass with the fix"
      ],
      "verification_command": "npx jest --testPathPattern leaves.service.spec --no-coverage 2>&1 | grep -E 'PASS|FAIL|canValidate'",
      "related_findings": [
        "SEC-009"
      ],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The bug is in the else-branch after the service-scope check succeeds. The fix also aligns with the intent documented in `findValidatorForUser` (COR-005 comment at line 712-714). Adversarial check: both approve() and reject() gate solely on canValidate() with no additional delegation scoping. findValidatorForUser correctly scopes to department manager (delegatorId: managerId) but canValidate does not apply the same filter. Code verbatim confirmed at lines 1638-1650."
    },
    {
      "id": "COR-002",
      "severity": "blocking",
      "category": "correctness",
      "subcategory": "transaction-boundary",
      "title": "importTasks: task created but subtasks written outside transaction — partial failure leaves orphaned task row",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "1451-1482",
      "code_evidence": "        const createdTask = await this.prisma.task.create({\n          data: {\n            title: taskData.title,\n            ...\n          },\n        });\n\n        // Créer les sous-tâches si présentes (séparées par |)\n        if (taskData.subtasks) {\n          ...\n          for (let j = 0; j < subtaskTitles.length; j++) {\n            await this.prisma.subtask.create({\n              data: { title: subtaskTitles[j], taskId: createdTask.id, position: j },\n            });",
      "description": "In importTasks(), each task is created with prisma.task.create() and then its subtasks are created in a sequential loop of individual prisma.subtask.create() calls. There is no wrapping $transaction. If any subtask creation fails (e.g. DB constraint violation, connection interruption), the parent task row already exists in the database and will NOT be rolled back. The outer catch only records the error in errorDetails and increments result.errors — the dangling task row with no subtasks remains committed. Additionally, the duplicate-title check at line 1389 is a read followed by a create (non-atomic), creating a TOCTOU race.",
      "root_cause": "No database transaction wraps the task.create + subtask.create sequence, so failure between them leaves partially-written data.",
      "impact": "Orphaned task rows in the DB (no subtasks despite CSV specifying them); repeated imports will report the title as duplicate and skip it; data integrity inconsistency visible to end users.",
      "suggested_fix": "Wrap the entire per-task block (task.create + all subtask.create) in a prisma.$transaction(). Move the duplicate check inside the transaction using upsert semantics or rely on a DB unique constraint to avoid the TOCTOU gap.",
      "acceptance_criteria": [
        "If subtask creation throws, the parent task row is rolled back",
        "Concurrent imports with the same title do not both succeed"
      ],
      "verification_command": "grep -n 'prisma.task.create\\|prisma.subtask.create\\|\\$transaction' apps/api/src/tasks/tasks.service.ts | head -30",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "validateImport() (dry-run) does not have this issue as it does not write. Verified at lines 1451-1482: no $transaction wrapping.",
      "adversarial_check": "Confirmed. Lines 1451-1482 show prisma.task.create() followed by individual prisma.subtask.create() calls in a for-loop, with no $transaction wrapper. The catch block at line 1485 catches per-task errors but cannot roll back the already-committed task.create."
    },
    {
      "id": "COR-003",
      "severity": "blocking",
      "category": "correctness",
      "subcategory": "scope-leak / privilege-escalation-by-side-effect",
      "title": "findAll auto-expand writes telework rows for ALL users when a non-privileged caller supplies startDate+endDate without userId",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "164-182",
      "code_evidence": "    const permissions =\n      await this.permissionsService.getPermissionsForRole(currentUserRole);\n    if (!permissions.includes('telework:readAll')) {\n      where.userId = currentUserId;\n    } else if (userId) {\n      where.userId = userId;\n    }\n\n    if (startDate || endDate) {\n      where.date = {};\n      if (startDate) where.date.gte = new Date(startDate);\n      if (endDate) where.date.lte = new Date(endDate);\n    }\n\n    // Auto-expand recurring rules into individual schedules for the requested range\n    if (startDate && endDate) {\n      await this.expandRecurringRulesForRange(startDate, endDate, userId);\n    }",
      "description": "When a non-privileged user (lacking telework:readAll) calls GET /telework?startDate=X&endDate=Y, the read scope is correctly narrowed to `where.userId = currentUserId`. However, the subsequent `expandRecurringRulesForRange(startDate, endDate, userId)` call passes the DTO-level `userId` query param, not the enforced `currentUserId`. A CONTRIBUTEUR who omits `userId` from the query will send `filterUserId=undefined` into `expandRecurringRulesForRange`, which then materialises TeleworkSchedule rows for every user in the system whose active recurring rules overlap the date range. The caller only reads their own data afterwards, but the DB has been silently mutated for others. An attacker can enumerate all colleagues' telework days simply by calling the read endpoint.",
      "root_cause": "The expansion call uses the raw `userId` query param instead of the already-scope-narrowed `where.userId`, so the side-effect bypasses the permission gate applied to the read.",
      "impact": "Any authenticated user with telework:read can trigger mass materialisation of other users' telework_schedule rows, violating data ownership and introducing false telework entries that would appear in team-view reports and planning overviews. Reverse: a record created this way looks identical to a user-declared one and cannot be distinguished in the UI.",
      "suggested_fix": "Pass the actually-enforced user scope to expandRecurringRulesForRange. Replace line 181 with:\n  `await this.expandRecurringRulesForRange(startDate, endDate, where.userId as string | undefined);`\nThis ensures non-privileged calls expand only the authenticated user's rules, while admin calls with an explicit userId= filter expand that user's rules, and admin calls without a filter expand all (intended).",
      "acceptance_criteria": [
        "A CONTRIBUTEUR calling GET /telework?startDate=X&endDate=Y does not trigger creation of TeleworkSchedule rows for users other than themselves.",
        "An ADMIN calling GET /telework?startDate=X&endDate=Y&userId=other-id expands only that other user's rules.",
        "An ADMIN calling GET /telework?startDate=X&endDate=Y (no userId) still expands rules for all users."
      ],
      "verification_command": "grep -n 'expandRecurringRulesForRange' apps/api/src/telework/telework.service.ts",
      "related_findings": [
        "COR-032",
        "COR-033"
      ],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED. Verbatim code verified at lines 164-182. The `expandRecurringRulesForRange` signature at line 292-295 shows `filterUserId?: string` — when undefined, the `ruleWhere.userId` filter is not applied (line 308: `if (filterUserId) { ruleWhere.userId = filterUserId; }`), causing all active rules to be expanded. No mitigating guard exists between the scope decision at line 167-171 and the expansion call at line 181. findForPlanningOverview (line 246) has the same pattern — see correctness-S6-2."
    },
    {
      "id": "COR-004",
      "severity": "important",
      "category": "correctness",
      "subcategory": "misleading_semantics",
      "title": "MilestonesCompletionResponseDto.total means 'due milestones' not 'all milestones'",
      "file": "apps/api/src/analytics/advanced/services/milestones-completion.service.ts",
      "line": "75-77",
      "code_evidence": "    const total = completed + overdue;\n    const onTime = completed;\n    const ratio = total > 0 ? onTime / total : 0;\n",
      "description": "`total` is computed as `completed + overdue`, deliberately excluding `upcoming` milestones. Yet the returned DTO also exposes `upcoming` as a peer field alongside `total`. Any consumer that interprets `total` as 'total milestones in scope' (the natural reading of the field name) will derive a wrong ratio manually (e.g. `completed / total` is not the same as `completed / (completed + overdue + upcoming)`). The `ratio` itself is correct given its definition, but the `total` field is misleading and can cause bugs in UI layers or downstream integrations that compute secondary metrics from the raw fields.",
      "root_cause": "The variable named `total` is assigned `completed + overdue` rather than the actual total count of all milestones; the naming is ambiguous and conflicts with the natural meaning of 'total'.",
      "impact": "Consumers computing their own ratios from `total` will get inflated percentages (e.g. 5 completed / 7 total instead of 5 / 10 when 3 are upcoming). Dashboard secondary metrics or exports could misreport milestone health.",
      "suggested_fix": "Rename the field to `due` (or `dueTotal`) in the DTO and return object to reflect its actual semantics. Keep `total` as `completed + overdue + upcoming` if a true total is needed. Update `ratio` documentation to clarify it is `completed / (completed + overdue)`.",
      "acceptance_criteria": [
        "DTO field name unambiguously conveys 'milestones that have passed their due date or are completed'",
        "Consumers cannot derive a wrong ratio by using the wrong denominator"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Verbatim confirmed at lines 75-77. The return object at lines 115-124 exposes: onTime, total (=completed+overdue), ratio, completed, overdue, upcoming — so a consumer using total as 'all milestones' gets the wrong denominator."
    },
    {
      "id": "COR-005",
      "severity": "important",
      "category": "correctness",
      "subcategory": "data_inconsistency",
      "title": "taskStatusData and metrics diverge when scope exceeds PROJECT_DETAILS_LIMIT (50)",
      "file": "apps/api/src/analytics/analytics.service.ts",
      "line": "103-127",
      "code_evidence": "    const [projects, tasks, users] = await Promise.all([\n      this.getProjects(projectId, projectWhere),\n      this.getTasks(projectId, projectWhere),\n      this.getActiveUsers(projectWhere),\n    ]);\n\n    const projectIds = projects.map((p) => p.id);\n    const taskStatusGroupBy = await this.prisma.task.groupBy({\n      by: ['projectId', 'status'],\n      where: { projectId: { in: projectIds } },\n      _count: { _all: true },\n    });\n\n    const metrics = this.calculateMetrics(projects, tasks, users);\n    const taskStatusData = this.getTaskStatusData(taskStatusGroupBy);",
      "description": "`getProjects` applies `take: PROJECT_DETAILS_LIMIT` (50), truncating the project list. `getTasks` is scoped by `projectWhere` (no limit), so it can return tasks from all projects in scope (e.g. 200). `taskStatusGroupBy` is then built from only the 50 returned project IDs. As a result, `calculateMetrics` (consuming the full `tasks` array) counts all tasks across all projects, while `getTaskStatusData` (consuming `taskStatusGroupBy`) only covers the 50-project subset. The two panels on the analytics dashboard will show contradictory numbers: the overall 'Taux de Completion' metric counts tasks from projects 51-N, but the per-status breakdown chart silently omits them.",
      "root_cause": "`getProjects` hard-caps at 50 rows but `getTasks` has no corresponding cap; the shared `taskStatusGroupBy` is then scoped to only the 50 returned project IDs instead of using the same unrestricted scope.",
      "impact": "Analytics dashboard shows inconsistent KPIs: the completion-rate metric reflects a larger task set than the status-breakdown chart. The discrepancy grows with the number of projects in scope. Users with >50 projects in scope will see a completion rate that does not match the sum of the status chart values.",
      "suggested_fix": "Either (a) apply no `take` limit in `getProjects` for analytics (use a paginated or export path for the details table separately), or (b) derive `taskStatusGroupBy` from the same `projectWhere` clause as `getTasks` rather than from the post-truncated `projectIds`. If the 50-project cap must be kept for payload size, apply it consistently to `tasks` as well and document the sampling clearly.",
      "acceptance_criteria": [
        "When a user has 60 projects in scope, sum(taskStatusData[].value) equals metrics[1].change numerator (completedTasks count)",
        "Integration test: seed 60 projects, call GET /analytics, verify taskStatusData sums == metrics totalTasks"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Verbatim verified: getProjects uses `take: PROJECT_DETAILS_LIMIT` at line 154; getTasks has no take at line 235; taskStatusGroupBy scoped to projectIds (truncated list) at lines 113-118; calculateMetrics uses unbounded tasks array. The PER-025 comment at line 109 incorrectly states the groupBy is 'equivalent to the getTasks relation filter'. It is equivalent only when projects.length < PROJECT_DETAILS_LIMIT."
    },
    {
      "id": "COR-006",
      "severity": "important",
      "category": "correctness",
      "subcategory": "hash-chain integrity",
      "title": "AuditService.log passes undefined-valued payload keys to computeRowHash; stored JSONB normalizes them differently, risking hash divergence on external recompute",
      "file": "apps/api/src/audit/audit.service.ts",
      "line": "190-199",
      "code_evidence": "      payload: {\n          ip: event.ip,\n          details: event.details,\n          success: event.success,\n          timestamp: entry.timestamp,\n          ...(event.ua !== undefined ? { ua: event.ua } : {}),\n          ...(event.reason !== undefined ? { reason: event.reason } : {}),\n          ...(event.before !== undefined ? { before: event.before } : {}),\n          ...(event.after !== undefined ? { after: event.after } : {}),\n        },",
      "description": "AuditService.log always includes `ip: event.ip` and `details: event.details` in the payload object, even when those values are `undefined`. The payload is then passed to AuditPersistenceService.log, which calls computeRowHash → stableStringify(payload). In stableStringify, Object.keys() includes keys with undefined values, and stableStringify(undefined) returns 'null'. So the hash is computed over a payload like {\"details\":null,\"ip\":null,\"success\":true,\"timestamp\":\"...\"}.\n\nHowever, when this payload is stored in PostgreSQL JSONB via Prisma, undefined-valued keys are dropped (JSON.stringify behavior), so the stored JSONB becomes {\"success\":true,\"timestamp\":\"...\"}. When an external recompute reads the stored row and calls computeRowHash(storedRow), stableStringify produces {\"success\":true,\"timestamp\":\"...\"}, which differs from the original canonical string. The stored rowHash cannot be reproduced from the stored data.\n\nThis divergence is NOT assumption-dependent: empirical verification (node -e) confirms that JSON.stringify({ip: undefined, success: true}) = '{\"success\":true}' (drops undefined), while stableStringify({ip: undefined, success: true}) = '{\"ip\":null,\"success\":true}' (includes as null). Prisma uses JSON.stringify semantics for JSONB, so the divergence is real.\n\nSecondary vector (same root cause, assumption-free): the payload-schemas.ts comment states snapshots carry 'Dates, Prisma.Decimal, nested rows' (z.unknown() fields). stableStringify(new Date()) returns '{}' (no enumerable own keys on a Date), but PostgreSQL JSONB stores a Date as an ISO string. On read-back the stored form is a string, and stableStringify('2026-...')  returns '\"2026-...\"'. These are different canonical forms for the same logical value, breaking recompute for any row whose before/after/snapshot contained a JS Date object.",
      "root_cause": "computeRowHash canonicalizes the in-memory JS object (where undefined stays and Date has no enumerable keys) while PostgreSQL JSONB normalizes the stored form (undefined dropped, Date coerced to ISO string), so the hash input at write time and the hash input at re-read time can diverge.",
      "impact": "An external or maintenance recompute of the audit hash chain (e.g. recompute-chain-on-schema-bump.ts run after a new schema bump, or an independent auditor verifying the chain) would find that rowHash values for rows written via AuditService.log do not match the hash recomputed from stored fields. This defeats the tamper-detection guarantee that computeRowHash's own doc advertises: 'Exported so an external verifier (production runbook, chain-audit script) can recompute and compare against stored values.' No live functionality is broken — the audit module is write-only in production and the recompute scripts self-heal by overwriting — but the integrity guarantee is latent-broken for affected rows.",
      "suggested_fix": "Strip undefined-valued keys and normalise non-serializable types before calling computeRowHash. One approach: after the payload is assembled in AuditService.log, round-trip it through JSON.parse(JSON.stringify(payload)) to produce a JSONB-canonical plain object (drops undefined, serialises Date → ISO string, Prisma.Decimal → string). Pass this normalised form to both computeRowHash and the DB insert. Alternatively, guard ip/details with the same !== undefined pattern already used for ua/reason/before/after.",
      "acceptance_criteria": [
        "When AuditService.log is called without ip/details (both undefined), the stored payload in audit_logs contains no 'ip' or 'details' keys, and computeRowHash(storedRow) === storedRow.rowHash.",
        "When a before/after value contains a Date object, computeRowHash(storedRow) === storedRow.rowHash after the row is read back from PostgreSQL.",
        "A chain verify pass over rows written via AuditService.log finds 0 rowHash mismatches."
      ],
      "verification_command": "pnpm test:integration -- --testPathPattern audit-persistence; also: insert a row via AuditService.log with no ip/details, then run: docker exec orchestr-a-db psql -U postgres orchestra -c \"SELECT payload, \\\"rowHash\\\" FROM audit_logs ORDER BY \\\"createdAt\\\" DESC LIMIT 1\" and manually call computeRowHash with the stored fields to compare.",
      "related_findings": [],
      "cluster_id": "H",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Confirmed via empirical test: node -e proves JSON.stringify drops undefined-valued keys while stableStringify maps them to 'null'. Prisma uses JSON.stringify semantics for JSONB. Therefore hash-at-write-time ≠ hash-recomputed-from-stored-JSONB. Upgraded from 'medium' to 'high' confidence — the critical unknown (Prisma JSONB behavior on undefined) is resolved by standard JS behavior: JSON.stringify({ip: undefined}) = '{}'. The validatePayloadForAction gate (payload-schemas.ts:262-273) returns void and does NOT normalize the payload; the raw object with undefined-valued keys goes directly to computeRowHash. The Date secondary vector is also confirmed: stableStringify(new Date()) = '{}' while stored JSONB encodes Date as ISO string."
    },
    {
      "id": "COR-007",
      "severity": "important",
      "category": "correctness",
      "subcategory": "TOCTOU / missing-transaction",
      "title": "auth.service resetPassword(): token validity check and usedAt marking are not atomic — concurrent reset with the same token succeeds twice",
      "file": "apps/api/src/auth/auth.service.ts",
      "line": "545-575",
      "code_evidence": "    const resetToken = await this.prisma.passwordResetToken.findUnique({\n      where: { token: tokenHash },\n    });\n\n    if (!resetToken) {\n      throw new UnauthorizedException('Token de réinitialisation invalide');\n    }\n\n    if (resetToken.usedAt !== null) {\n      throw new UnauthorizedException(\n        'Ce token de réinitialisation a déjà été utilisé',\n      );\n    }\n\n    if (resetToken.expiresAt < new Date()) {\n      throw new UnauthorizedException('Ce token de réinitialisation a expiré');\n    }\n\n    const passwordHash = await bcrypt.hash(newPassword, 12);\n\n    await this.prisma.user.update({\n      where: { id: resetToken.userId },\n      data: { passwordHash },\n    });\n\n    await this.prisma.passwordResetToken.update({\n      where: { token: tokenHash },\n      data: { usedAt: new Date() },\n    });",
      "description": "The token validity check (findUnique → check usedAt === null → check expiresAt) and the subsequent user.update() + passwordResetToken.update() are three separate non-transactional statements. Between the read (usedAt is null) and the write (usedAt gets set), a concurrent request with the same token passes the same checks and proceeds. Both requests update the user password; the second write wins. The token gets marked used by both, but both password hashes land — the last one written wins, which is non-deterministic under load.",
      "root_cause": "Read-then-write on the token row is not wrapped in a transaction nor uses an atomic conditional UPDATE (UPDATE ... WHERE usedAt IS NULL RETURNING).",
      "impact": "Under concurrent race, a reset token can be consumed twice: both requests hash a password and update the user row. The attacker who intercepts the token link can race the victim's submission. The second write silently wins. Practical exploitability is low (requires simultaneous submission of the same one-time link) but the invariant that a token is single-use is violated.",
      "suggested_fix": "Wrap the entire sequence in a $transaction with Serializable isolation. Alternatively, replace findUnique + conditional check with a single atomic UPDATE ... SET usedAt = NOW() WHERE token = $hash AND usedAt IS NULL AND expiresAt > NOW() RETURNING *; reject if 0 rows updated. The existing refresh-token rotate() uses the serializable-transaction pattern.",
      "acceptance_criteria": [
        "Two concurrent requests with the same valid token: exactly one succeeds, the other receives 401.",
        "After successful reset, passwordResetToken.usedAt is non-null."
      ],
      "verification_command": "N/A — manual verification via concurrent HTTP test or integration test with parallel requests.",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "refresh-token.service.ts rotate() avoids this correctly by using { isolationLevel: 'Serializable' } transaction. Adversarial check: no $transaction exists anywhere in auth.service.ts. schema.prisma confirms token field is @unique but usedAt has no DB-level unique partial constraint — no DB guard prevents dual usedAt writes."
    },
    {
      "id": "COR-008",
      "severity": "important",
      "category": "correctness",
      "subcategory": "race-condition / missing transaction",
      "title": "hardDelete: count-then-delete without transaction allows deletion of client with active project links",
      "file": "apps/api/src/clients/clients.service.ts",
      "line": "259-274",
      "code_evidence": "    const projectsCount = await this.prisma.projectClient.count({\n      where: { clientId: id },\n    });\n    if (projectsCount > 0) {\n      throw new ConflictException(\n        `Client ${id} cannot be deleted: it is linked to ${projectsCount} project(s). Remove the associations first or archive the client instead.`,\n      );\n    }\n\n    await this.prisma.client.delete({ where: { id } });",
      "description": "The `hardDelete` method reads the project-association count (line 265) then, in a separate operation, deletes the client (line 274). Between the count check and the delete, a concurrent `assignClientToProject` call can insert a `ProjectClient` row, meaning the client ends up deleted while a live project association still references it. The DB may cascade-delete the `ProjectClient` row (depending on FK rules), silently orphaning project records or causing inconsistent state depending on the cascade configuration.",
      "root_cause": "Guard-then-delete pattern without wrapping both operations in a `$transaction`, leaving a TOCTOU window.",
      "impact": "A client linked to a project can be hard-deleted if another request creates the association concurrently, potentially violating referential integrity or silently removing a project link. Confirmed: ProjectClient FK has onDelete: Cascade on client — concurrent insert+delete will result in the ProjectClient row silently disappearing when the client is deleted, destroying the association without any guard.",
      "suggested_fix": "Wrap both the count check and the delete inside a `$transaction`: `await this.prisma.$transaction(async (tx) => { const count = await tx.projectClient.count({...}); if (count > 0) throw new ConflictException(...); await tx.client.delete({...}); });`",
      "acceptance_criteria": [
        "Concurrent `assignClientToProject` and `hardDelete` calls never result in a client being deleted when it has active project associations",
        "The entire guard-then-delete sequence executes within a single DB transaction"
      ],
      "verification_command": "grep -n '\\$transaction' apps/api/src/clients/clients.service.ts",
      "adversarial_check": "Verified: (1) code_evidence is verbatim at lines 265-274. (2) No $transaction wraps hardDelete — grep confirms only one $transaction usage in the file (findAll). (3) Schema shows ProjectClient.client has onDelete: Cascade, meaning the race window does silently cascade-delete the association. Finding stands.",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The same pattern exists in update() (findUnique then update) but the consequence there is lower severity since update does not violate referential integrity."
    },
    {
      "id": "COR-009",
      "severity": "important",
      "category": "correctness",
      "subcategory": "TOCTOU / soft-delete bypass",
      "title": "documents.update: prisma.document.update has no deletedAt filter — can mutate soft-deleted document in race window",
      "file": "apps/api/src/documents/documents.service.ts",
      "line": "172-181",
      "code_evidence": "  async update(id: string, updateDocumentDto: UpdateDocumentDto) {\n    await this.findOne(id);\n    return this.prisma.document.update({\n      where: { id },\n      data: updateDocumentDto,\n      include: {\n        project: { select: { id: true, name: true } },\n      },\n    });\n  }",
      "description": "`findOne(id)` (line 173) checks `document.deletedAt` and throws NotFoundException if the document is soft-deleted. However, `prisma.document.update({ where: { id } })` (line 174) uses only the `id` filter with no `deletedAt: null` guard. If a concurrent soft-delete sets `deletedAt` between the `findOne` call and the `update` call, the update will silently succeed on a logically-deleted document, resurrecting its state partially or overwriting the `deletedAt`-based tombstone.",
      "root_cause": "The existence/soft-delete check and the write are two separate DB roundtrips with no transaction or atomic WHERE clause combining both conditions.",
      "impact": "In a concurrent scenario, a soft-deleted document can be updated (e.g., its metadata changed), violating the soft-delete invariant that deleted documents are immutable. If `updateDocumentDto` includes `deletedAt: null`, it would hard-undelete the document without any audit trail.",
      "suggested_fix": "Change the update where clause to `{ id, deletedAt: null }` and handle the Prisma P2025 'record not found' error as a NotFoundException, or wrap findOne + update in a `$transaction`.",
      "acceptance_criteria": [
        "Calling update() on a soft-deleted document always returns 404",
        "No update succeeds on a document with deletedAt set"
      ],
      "verification_command": "grep -n 'deletedAt' apps/api/src/documents/documents.service.ts",
      "adversarial_check": "Verified: code_evidence is verbatim at lines 172-181. No $transaction and no deletedAt filter in the update where clause. Finding stands.",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The same TOCTOU is present for the remove() → findOne() → update({deletedAt: new Date()}) pattern, though its impact is lower: double-soft-delete is idempotent."
    },
    {
      "id": "COR-010",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing authorization / business invariant gap",
      "title": "documents.update accepts projectId change without checking user access to the new project",
      "file": "apps/api/src/documents/documents.service.ts",
      "line": "172-181",
      "code_evidence": "  async update(id: string, updateDocumentDto: UpdateDocumentDto) {\n    await this.findOne(id);\n    return this.prisma.document.update({\n      where: { id },\n      data: updateDocumentDto,\n      include: {\n        project: { select: { id: true, name: true } },\n      },\n    });\n  }",
      "description": "`UpdateDocumentDto` extends `PartialType(CreateDocumentDto)` (see update-document.dto.ts), which means `projectId` is an optional field in the update payload. The `update()` method passes `updateDocumentDto` directly to `prisma.document.update()` without checking whether the caller has access to the new `projectId`. The `OwnershipGuard` at the controller layer only verifies ownership of the document being updated — it does NOT verify that the destination project is accessible to the caller. A document owner can move their document to any arbitrary project (including confidential or restricted projects) by including a `projectId` in the PATCH payload.",
      "root_cause": "The service `create()` correctly calls `assertCanAccessProject(createDocumentDto.projectId, ...)` but `update()` has no equivalent check for a projectId change.",
      "impact": "A document owner can attach their document to any project, including ones they have no visibility into, potentially leaking document content to project members of the target project who can now list/read the document.",
      "suggested_fix": "In `update()`, detect if `updateDocumentDto.projectId` is defined and differs from the current document's `projectId`; if so, call `accessScope.assertCanAccessProject(updateDocumentDto.projectId, currentUser, ...)`. The `update()` signature needs to accept `currentUser`.",
      "acceptance_criteria": [
        "PATCH /documents/:id with a projectId the caller has no access to returns 403",
        "PATCH /documents/:id with the caller's own projectId succeeds"
      ],
      "verification_command": "grep -n 'projectId\\|assertCanAccessProject' apps/api/src/documents/documents.service.ts",
      "adversarial_check": "Verified: (1) UpdateDocumentDto is confirmed as `PartialType(CreateDocumentDto)` — projectId IS passable. (2) Controller PATCH calls `documentsService.update(id, updateDocumentDto)` with no currentUser. (3) OwnershipGuard only checks document ownership via OwnershipService.isOwner() — no body inspection for projectId. (4) No assertCanAccessProject call in update(). Finding stands.",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The create() method correctly enforces this check at line 57-62, but update() has no equivalent."
    },
    {
      "id": "COR-011",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing validator / business invariant gap",
      "title": "CreateDocumentDto.size has no @IsNumber() or @Min(0) — negative/float sizes accepted",
      "file": "apps/api/src/documents/dto/create-document.dto.ts",
      "line": "88-90",
      "code_evidence": "  @ApiProperty({ description: 'Taille en bytes', example: 2048576 })\n  @IsNotEmpty()\n  size: number;",
      "description": "The `size` field carries only `@IsNotEmpty()` — which checks for non-empty/null/undefined but does NOT validate that the value is a number, an integer, or non-negative. `@IsNotEmpty()` on a number type passes `0` and any negative value. The service's upper-bound check (`size > MAX_DOCUMENT_SIZE_BYTES` in documents.service.ts:50) only rejects oversized values; it does not reject negative sizes (e.g., `size: -1` would satisfy `!isNotEmpty && -1 < 200_000_000`). There is no `@IsInt()`, `@IsNumber()`, or `@Min(0)` decorator. A client can store a document with `size: -1` or `size: 3.14`, which is semantically invalid for a byte count and may cause incorrect quota calculations.",
      "root_cause": "Missing class-validator numeric type decorators on the `size` field; only an upper-bound guard was added at the service layer.",
      "impact": "Negative or fractional byte counts can be persisted, leading to incorrect summaries (e.g., `budgetHoursTotal` analogues for storage), and bypassing any quota logic that trusts the stored size.",
      "suggested_fix": "Add `@IsInt()` and `@Min(0)` (both from `class-validator`) to `size` in `CreateDocumentDto`. Also check that `UpdateDocumentDto` (via `PartialType`) inherits these constraints.",
      "acceptance_criteria": [
        "POST /documents with size: -1 returns 400 Bad Request",
        "POST /documents with size: 3.14 returns 400 Bad Request",
        "POST /documents with size: 0 succeeds (zero-byte documents are valid)"
      ],
      "verification_command": "grep -n 'IsInt\\|IsNumber\\|Min\\|size' apps/api/src/documents/dto/create-document.dto.ts",
      "adversarial_check": "Verified: full DTO read confirms only @IsNotEmpty() on the size field. Imports are IsString, IsNotEmpty, IsOptional, IsUUID, IsUrl, IsIn, MaxLength, MinLength — no IsInt, IsNumber, or Min imported. Service-level guard at line 50 only checks size > MAX (200MB), does not reject negatives. Finding stands.",
      "related_findings": [],
      "cluster_id": "F",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The service-side check `createDocumentDto.size > MAX_DOCUMENT_SIZE_BYTES` is bypassed by any negative value, and only applies in create(), not in update()."
    },
    {
      "id": "COR-012",
      "severity": "important",
      "category": "correctness",
      "subcategory": "data_integrity — missing transaction boundary",
      "title": "Recurring event child occurrences created outside any transaction — partial failure leaves orphaned parent with no children",
      "file": "apps/api/src/events/events.service.ts",
      "line": "233-245",
      "code_evidence": "      for (const occ of occurrences) {\n        await this.prisma.event.create({\n          data: {\n            ...occ,\n            ...(participantIds &&\n              participantIds.length > 0 && {\n                participants: {\n                  create: participantIds.map((userId) => ({ userId })),\n                },\n              }),\n          },\n        });\n      }",
      "description": "After the parent event is committed (line 140–198), child occurrences are written in a sequential for-loop with N separate Prisma `event.create` calls, each in its own auto-commit transaction. If any child create fails mid-loop (e.g. DB connection reset, constraint violation on one participant, resource limit), the parent event and the already-created children are persisted but the request returns HTTP 500. The client never receives the parent ID and may retry, creating a duplicate recurrence series. Contrast with the `update()` method (lines 480–535) which correctly wraps participant rewrites in `$transaction`. The asymmetry is likely an oversight.",
      "root_cause": "Occurrence loop runs as N independent writes after the parent has already committed, with no wrapping transaction to roll back on failure.",
      "impact": "On any child-create failure the DB is left with a partially constructed recurrence series (parent + k < N children). A retry creates a second series from scratch. Users see duplicate events or a broken series with no API feedback beyond 500.",
      "suggested_fix": "Wrap the parent create and all child creates in a single `this.prisma.$transaction(async (tx) => { ... })`. Move the `event.create` for the parent and the occurrence loop inside the transaction callback, substituting `tx.event.create` for all writes.",
      "acceptance_criteria": [
        "A simulated DB error on the 2nd child create rolls back the parent and the 1st child (DB is clean after failure)",
        "A successful call with N=3 child occurrences produces exactly 1 parent + 3 children in a single atomic write",
        "The existing PER-024 test (event.create called parent + 2 occurrences) still passes after the refactor"
      ],
      "verification_command": "grep -n 'prisma.event.create\\|prisma.$transaction' apps/api/src/events/events.service.ts | head -30",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "update() wraps participant rewrite in $transaction (line 480) — the asymmetry with create() strengthens this finding. Adversarial check: no outer $transaction exists in the create() method; the parent event.create at line 140 is inside a try/catch for cycle detection only, not a tx. The occurrence loop at lines 233-245 runs after the parent has already committed. No global transaction interceptor found. Confirmed."
    },
    {
      "id": "COR-013",
      "severity": "important",
      "category": "correctness",
      "subcategory": "tz_naive_date_arithmetic",
      "title": "countWorkingDays: local-TZ setDate/getDate arithmetic causes DST off-by-one — one day may be counted twice or skipped",
      "file": "apps/api/src/holidays/holidays.service.ts",
      "line": "460-477",
      "code_evidence": "    let count = 0;\n    const current = new Date(startDate);\n\n    while (current <= endDate) {\n      const dayOfWeek = current.getDay();\n      const dateStr = current.toISOString().split('T')[0];\n\n      // Exclut samedi (6) et dimanche (0) et jours fériés non ouvrés\n      if (\n        dayOfWeek !== 0 &&\n        dayOfWeek !== 6 &&\n        !nonWorkingHolidayDates.has(dateStr)\n      ) {\n        count++;\n      }\n\n      current.setDate(current.getDate() + 1);\n    }",
      "description": "Three separate TZ issues interact here:\n1. `current.setDate(current.getDate() + 1)` uses local-TZ date components. On a Europe/Paris host at the DST transition (end of March: clocks spring forward from 02:00 to 03:00), adding one local day advances the UTC timestamp by only 23 hours, not 24. On the autumn transition (02:00 → 01:00), advancing by one local day adds 25 UTC hours — making `current > endDate` false when it should be true, causing the last day to be counted twice.\n2. `current.getDay()` is local-TZ; `dateStr = current.toISOString().split('T')[0]` is UTC. If startDate is provided as a UTC-midnight Date (typical for @db.Date columns) and the host runs at Europe/Paris (+01/+02), `getDay()` returns the local day-of-week (same as UTC in this case) but `dateStr` is already correct. However, `nonWorkingHolidayDates` is built via `.toISOString().split('T')[0]` on `h.date` which is also UTC — so these two UTC strings match. The primary risk is the DST loop increment.",
      "root_cause": "`setDate/getDate` operates in local calendar time; on a Paris host the DST spring-forward transition causes a loop iteration that advances by only 23 hours, allowing one date to appear in two consecutive loop iterations.",
      "impact": "On the spring DST boundary (end of March, one night per year), `countWorkingDays` may return count+1 (one extra workday) for ranges that span the transition. Used by the `/holidays/working-days/count` API endpoint. Since this endpoint is informational and not used in leave-day balance gating, the impact is limited to incorrect display data, but the same flaw pattern exists if this function is ever reused in balance calculations.",
      "suggested_fix": "Replace the cursor-advance with UTC arithmetic matching the pattern in `leave-year-window.ts`:\n```ts\ncurrent.setUTCDate(current.getUTCDate() + 1);\n```\nand use `current.getUTCDay()` for the day-of-week check to stay consistently in UTC.",
      "acceptance_criteria": [
        "countWorkingDays('2025-03-28', '2025-03-31') returns 2 (Fri 28 + Mon 31, excluding DST Sunday 30 and Saturday 29)",
        "countWorkingDays('2025-10-24', '2025-10-27') returns 2 (Fri 24 + Mon 27)"
      ],
      "verification_command": "node -e \"const d = new Date(Date.UTC(2025,2,29)); d.setDate(d.getDate()+1); console.log(d.toISOString())\" # should be 2025-03-30T22:00:00.000Z on Europe/Paris host, not 2025-03-30T23:00:00.000Z",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The leave-year-window.ts module correctly uses setUTCDate/getUTCDate throughout. This function in holidays.service.ts should adopt the same pattern. Adversarial check: countWorkingDays is only called from the holidays controller (informational endpoint) — confirmed via grep. It is not wired into balance calculations, so impact is limited to display data. Code verbatim confirmed at lines 460-477."
    },
    {
      "id": "COR-014",
      "severity": "important",
      "category": "correctness",
      "subcategory": "state_machine_missing_audit",
      "title": "rejectCancellation writes no audit log — CANCELLATION_REQUESTED→APPROVED transition is invisible to the audit trail",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2283-2319",
      "code_evidence": "    return this.prisma.$transaction(async (tx) => {\n      const current = await tx.leave.findUnique({ where: { id } });\n      if (!current) {\n        throw new NotFoundException('Demande de congé introuvable');\n      }\n      if (current.status !== LeaveStatus.CANCELLATION_REQUESTED) {\n        throw new ConflictException(\n          'La demande de congé a été modifiée pendant le traitement. Veuillez réessayer.',\n        );\n      }\n\n      return tx.leave.update({\n        where: { id },\n        data: { status: LeaveStatus.APPROVED },\n        include: {",
      "description": "All other status transitions in LeavesService (approve, reject, cancel, requestCancel) emit an audit log entry via `auditPersistence.log` inside or right after the transaction. `rejectCancellation` is the only transition that performs no audit write. A manager who denies a cancellation request leaves zero trace in `audit_logs`. The `cancel()` docstring at line 2113 explicitly notes 'APPROVE_CANCELLATION is merged here', implying the intent was to instrument both paths. `rejectCancellation` was overlooked.",
      "root_cause": "The `rejectCancellation` $transaction callback returns immediately after `tx.leave.update` without calling `auditPersistence.log`, and the outer function has no post-tx audit emit.",
      "impact": "Under a French public-sector audit (RGPD/CADA/Code du travail), every mutation to an employee's leave record must be traceable. A denied cancellation that restores a leave to APPROVED has real payroll consequences (the leave stays consumed) but is now unrecoverable from logs.",
      "suggested_fix": "Add an `await this.auditPersistence.log(...)` call inside the transaction callback, after the update, mirroring the pattern in `cancel()` and `requestCancel()`. Build an actorSnapshot before the transaction using `buildActorSnapshot(currentUserId ?? '', { roleCode: currentUserRole ?? null })`.",
      "acceptance_criteria": [
        "Calling rejectCancellation produces one audit_logs row with action LEAVE_CANCELLATION_REJECTED (or equivalent) and the correct entityId",
        "The audit row is present even when the transaction commits atomically"
      ],
      "verification_command": "N/A — manual verification: call POST /leaves/:id/reject-cancellation then SELECT * FROM audit_logs WHERE entity_id = ':id' ORDER BY created_at DESC LIMIT 5;",
      "related_findings": [],
      "cluster_id": "G",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "All other six status-transition methods have audit writes; this one is the sole exception. Adversarial check: read requestCancel() (lines 2220-2233) and cancel() (lines 2112-2135) — both have auditPersistence.log inside the $transaction. rejectCancellation transaction (lines 2283-2319) returns immediately after tx.leave.update with no audit call. Confirmed verbatim."
    },
    {
      "id": "COR-015",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing_field_in_query_projection",
      "title": "getAvailableDays: existing leaves fetched without endHalfDay field — consumed days under-counted when stored leave has endHalfDay",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2587-2614",
      "code_evidence": "    const intersecting = await db.leave.findMany({\n      where: {\n        userId,\n        leaveTypeId,\n        status: {\n          in: [\n            LeaveStatus.APPROVED,\n            LeaveStatus.CANCELLATION_REQUESTED,\n            LeaveStatus.PENDING,\n          ],\n        },\n        startDate: { lt: yearEnd },\n        endDate: { gte: yearStart },\n        ...(options.excludeLeaveId\n          ? { id: { not: options.excludeLeaveId } }\n          : {}),\n      },\n      select: { startDate: true, endDate: true, halfDay: true },\n    });\n\n    const usedThisYear = intersecting.reduce((sum, l) => {\n      const buckets = splitLeaveByYear(\n        l.startDate,\n        l.endDate,\n        l.halfDay ?? null,\n        null,\n        holidayKeys,\n      );",
      "description": "The `select` projection on line 2604 only fetches `halfDay` (the start-half-day) but not `endHalfDay`. When `splitLeaveByYear` is called with `null` as the fourth argument (endHalfDay), it computes consumed days without the end-half-day deduction. Since the database `Leave` model only persists `halfDay` (not `endHalfDay` — verified in schema.prisma), and `create()` only stores `effectiveHalfDay || undefined` in the `halfDay` field (which is `halfDay || startHalfDay`), the `endHalfDay` is intentionally transient and never persisted. However, the stored `days` column DOES include the endHalfDay deduction (via `calculateLeaveDays` in `create()`). This means there is a discrepancy: the `days` column reflects the endHalfDay, but `getAvailableDays` re-derives consumed days without it, causing the available balance to be understated by 0.5 day for each existing leave that had `endHalfDay` set.",
      "root_cause": "The Leave schema has no `endHalfDay` column, so the value is permanently lost after `create()`. `getAvailableDays` re-derives consumption from stored records but cannot recover the missing endHalfDay, leading to 0.5-day under-counting of available balance.",
      "impact": "For any user with an existing approved/pending leave where `endHalfDay` was set at creation time, `getAvailableDays` will report 0.5 fewer available days than were actually consumed (i.e., the user appears to have consumed more leave than they actually did). This can block legitimate new leave requests with a spurious 'Solde insuffisant' error.",
      "suggested_fix": "Either: (a) Add an `endHalfDay HalfDay?` column to the `Leave` model in schema.prisma and persist it in `create()` and `update()` — then include it in the `select` and pass it to `splitLeaveByYear`; or (b) Deprecate `endHalfDay` in the DTO and clarify in documentation that only `halfDay`/`startHalfDay` is persisted.",
      "acceptance_criteria": [
        "Creating a leave with endHalfDay=AFTERNOON and then querying getAvailableDays shows the correctly deducted balance (not 0.5 extra deducted)",
        "The days column stored in DB matches the value computed by getAvailableDays when re-deriving usedThisYear"
      ],
      "verification_command": "N/A — manual verification: create leave with endHalfDay, then call GET /leaves/me/balance and verify available = allocated - stored_days",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The issue is architectural: endHalfDay is accepted in the DTO, affects the `days` computation, but is never persisted. The `getAvailableDays` re-derivation cannot be correct without it. The fix with lowest risk is option (a) — add the column. Adversarial check: schema.prisma confirmed only `halfDay HalfDay?` at line 618, no endHalfDay field. splitLeaveByYear confirmed to use endHalfDay parameter (lines 140-141 of leave-year-window.ts). create() confirmed stores only `halfDay: effectiveHalfDay` (line 614). Finding stands."
    },
    {
      "id": "COR-016",
      "severity": "important",
      "category": "correctness",
      "subcategory": "tz_naive_year_computation",
      "title": "getLeaveBalance uses new Date().getFullYear() (host-TZ) while balance windows use Paris-anchored parisYearWindow — mismatch on Jan 1 between 00:00 and 01:00 Paris time",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2635",
      "code_evidence": "    const currentYear = new Date().getFullYear();",
      "description": "The leave balance system is explicitly anchored to Europe/Paris calendar year boundaries via `parisYearWindow` (which uses `fromZonedTime('${year}-01-01 00:00:00', 'Europe/Paris')`). However, `getLeaveBalance` computes `currentYear` using `new Date().getFullYear()`, which returns the year in the API process's local timezone (UTC when deployed on standard Linux containers). Between 2025-12-31 23:00 UTC and 2026-01-01 00:00 UTC (Paris midnight is at 23:00 UTC in winter, UTC+1), `getFullYear()` returns 2025 (UTC) but a Paris employee is already in 2026 (Paris time). The balance query then fetches 2025 allocations while the Paris employee expects their 2026 balance.",
      "root_cause": "`new Date().getFullYear()` is UTC-based on most Linux deployments, while Paris year boundaries start one hour before UTC midnight in winter.",
      "impact": "During the 1-hour window on Jan 1 (23:00–00:00 UTC = 00:00–01:00 Paris), a Paris employee checking their balance will see their prior year balance instead of their new year balance. This is a 1-hour/year edge case but can cause confusion during year-end payroll reconciliation.",
      "suggested_fix": "Use `formatInTimeZone(new Date(), LEAVE_TIMEZONE, 'yyyy')` from `date-fns-tz` (already imported in `leave-year-window.ts`) to compute the Paris year:\n```ts\nimport { formatInTimeZone } from 'date-fns-tz';\nimport { LEAVE_TIMEZONE } from './leave-year-window';\nconst currentYear = parseInt(formatInTimeZone(new Date(), LEAVE_TIMEZONE, 'yyyy'), 10);\n```",
      "acceptance_criteria": [
        "At 2026-12-31T23:30:00Z (23:30 UTC = 00:30 Paris on Jan 1 2027), getLeaveBalance returns 2027 as the year (Paris is already in 2027)",
        "Integration test for this boundary passes"
      ],
      "verification_command": "N/A — manual verification: mock Date to 2026-12-31T23:30:00Z and call getLeaveBalance, expect year=2027 in the response",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Low probability (1 hour per year) but year-end is exactly when HR teams query balances for statutory reporting. Adversarial check: parisYearWindow confirmed to use fromZonedTime with Europe/Paris (leave-year-window.ts lines 52-55). The acceptance criteria example in the original finding had UTC/Paris direction reversed; corrected here — the bug manifests when UTC is still in year N but Paris has crossed to year N+1 (i.e., 23:xx UTC on Dec 31 = 00:xx Paris on Jan 1). Code verbatim confirmed at line 2635."
    },
    {
      "id": "COR-017",
      "severity": "important",
      "category": "correctness",
      "subcategory": "non_atomic_write_then_audit",
      "title": "deleteBalance: hard-delete committed before audit log — crash between them produces silent deletion with no audit trace",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2989-3010",
      "code_evidence": "    await this.prisma.leaveBalance.delete({ where: { id } });\n\n    await this.auditPersistence.log({\n      action: AuditAction.LEAVE_BALANCE_ADJUSTED,\n      entityType: 'Leave',\n      entityId: id,\n      actorId: actorId ?? null,\n      payload: {\n        actor: actorSnapshot,\n        subject: {\n          balanceId: id,\n          userId: balance.userId,",
      "description": "The balance is deleted on line 2989 and the audit write is attempted on line 2991, two separate awaits with no enclosing transaction. If the process crashes, is OOM-killed, or `auditPersistence.log` throws between these two lines, the balance row is gone from the database with no audit trace. This contrasts with `remove()` at line 1571 which wraps both the delete and the audit inside a single `this.prisma.$transaction`. The `remove()` docstring at line 1566 acknowledges the audit uses its own Prisma client and is not fully atomic — but `deleteBalance` here does not even attempt the transaction wrapper.",
      "root_cause": "The `deleteBalance` method issues two sequential awaits (delete, then audit) without a transaction, unlike `remove()` which uses `$transaction` to couple them.",
      "impact": "Admin-deleted leave balance overrides leave no audit trail if the API process restarts between the two writes. In a HR audit scenario, a balance that vanished without a trace could be attributed to data corruption rather than an admin action.",
      "suggested_fix": "Wrap the delete + audit in a `this.prisma.$transaction` as done in `remove()`, accepting the same acknowledged trade-off that the audit uses its own client (so the tx guards the delete commit, not the audit write durability):\n```ts\nawait this.prisma.$transaction(async (tx) => {\n  await tx.leaveBalance.delete({ where: { id } });\n  await this.auditPersistence.log({ ... });\n});\n```",
      "acceptance_criteria": [
        "Simulated crash between delete and audit does not result in a deleted balance with no audit row",
        "The audit row appears in audit_logs after a successful deleteBalance call"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The acknowledged trade-off (audit uses its own Prisma client) is documented in the remove() method. deleteBalance should at minimum match that level of coupling. Adversarial check: code verbatim confirmed at lines 2989-3010. No transaction wrapper present. The remove() method does wrap in $transaction. Finding stands."
    },
    {
      "id": "COR-018",
      "severity": "important",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "importMilestones(): per-row findFirst+create is a TOCTOU race with no uniqueness guarantee",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "193-231",
      "code_evidence": "    for (let i = 0; i < milestones.length; i++) {\n      const milestoneData = milestones[i];\n      const lineNum = i + 2; // +2 car ligne 1 = header, index commence à 0\n\n      try {\n        // Vérifier que le nom n'existe pas déjà dans le projet\n        const existingMilestone = await this.prisma.milestone.findFirst({\n          where: {\n            projectId,\n            name: milestoneData.name,\n          },\n        });\n\n        if (existingMilestone) {\n          result.skipped++;\n",
      "description": "The import loop performs a `findFirst` followed by a `create` in separate, non-transactional DB calls for each milestone. If two concurrent import requests are in flight simultaneously (or two rows in the same batch with identical names), both may pass the `findFirst` check (finding no existing milestone) and both attempt `create`. The `create` would then fail with a DB constraint violation (if a unique constraint on (projectId, name) exists) or silently succeed creating a duplicate (if no such constraint exists). The catch block at line 226-231 would catch the constraint error and count it as `errors`, not `skipped`, producing misleading stats. Also, the sequential per-row loop performs N times 2 DB queries for N milestones instead of a batch approach.",
      "root_cause": "No transactional read-then-write or upsert pattern; the check-then-act is separated in time.",
      "impact": "Concurrent imports may create duplicate milestones (no DB unique constraint on (projectId, name) exists per schema.prisma lines 284-299) or report misleading error counts. Performance degrades linearly with import size.",
      "suggested_fix": "Pre-fetch all existing milestone names for the project in one query before the loop (like validateImport does at line 268-272), then use `createMany` with `skipDuplicates: true` for the batch. Or use `upsert` per row. This eliminates the TOCTOU window.",
      "acceptance_criteria": [
        "Two concurrent POST /milestones/project/:id/import requests with the same milestone names do not create duplicates",
        "Import of N milestones performs O(1) DB queries for the existence check, not O(N)"
      ],
      "verification_command": "grep -n 'findFirst\\|createMany\\|skipDuplicates' apps/api/src/milestones/milestones.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED and severity note added. The Milestone schema (schema.prisma lines 284-299) has NO @@unique([projectId, name]) constraint — only @@index([projectId]). This means duplicate milestones by name CAN be silently inserted with no DB-level guard. The TOCTOU race is unprotected at both application and DB layers. validateImport() correctly pre-fetches names before the loop but importMilestones() does not follow the same pattern."
    },
    {
      "id": "COR-019",
      "severity": "important",
      "category": "correctness",
      "subcategory": "toctou-count-limit",
      "title": "personal-todos.service.ts create() has TOCTOU race on the 20-todo limit",
      "file": "apps/api/src/personal-todos/personal-todos.service.ts",
      "line": "31-47",
      "code_evidence": "  async create(userId: string, dto: CreatePersonalTodoDto) {\n    // Vérifier la limite de 20 todos\n    const count = await this.prisma.personalTodo.count({\n      where: { userId },\n    });\n\n    if (count >= MAX_TODOS) {\n      throw new BadRequestException(`Limite de ${MAX_TODOS} to-dos atteinte`);\n    }\n\n    return this.prisma.personalTodo.create({\n      data: {\n        userId,\n        text: dto.text,\n      },\n    });\n  }",
      "description": "The 20-todo limit is enforced by reading the count and then creating. There is no DB-level constraint capping the count per user, and no transaction around the count+create pair. Two concurrent requests for the same user can both read count=19 (or any value < 20), both pass the guard, and both successfully create a new todo, leaving the user with 21 todos. This violates the stated business invariant documented in CLAUDE.md ('Personal Todos: hard-coded 20-item limit').",
      "root_cause": "The count-then-create pattern is inherently non-atomic in a concurrent environment without a transaction or a DB-level constraint to enforce the limit.",
      "impact": "Any user who submits two or more simultaneous create requests (e.g., double-click, slow network retry, browser tab duplicate) can exceed the 20-item limit. The CLAUDE.md notes this as a hard-coded invariant, so the violation is a correctness regression.",
      "suggested_fix": "Wrap the count+create in a `prisma.$transaction()` with serializable isolation, or use a DB-level `CHECK` constraint or trigger, or at minimum accept that occasional over-limit is acceptable (nit) and document it. Minimal fix:\n```typescript\nawait this.prisma.$transaction(async (tx) => {\n  const count = await tx.personalTodo.count({ where: { userId } });\n  if (count >= MAX_TODOS) throw new BadRequestException(...);\n  return tx.personalTodo.create({ data: { userId, text: dto.text } });\n});\n```\nNote: Prisma default transactions use read-committed; for a true count lock you need a SELECT FOR UPDATE or a raw query. Alternatively add a unique partial index or trigger in Postgres.",
      "acceptance_criteria": [
        "Concurrent double-submit for the same user does not result in more than 20 todos",
        "The 21st todo attempt returns HTTP 400 even under concurrent load"
      ],
      "verification_command": "grep -n '\\$transaction\\|count\\|MAX_TODOS' apps/api/src/personal-todos/personal-todos.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "CLAUDE.md explicitly states 'Personal Todos: hard-coded 20-item limit'. The findByUser() cleanup (cleanupOldCompleted) runs before the count check, which at least reduces the window, but does not close it. Adversarial check: code at lines 31-47 matches exactly; no DB CHECK constraint found in schema.prisma for PersonalTodo count per user; no $transaction wrapping confirmed absent."
    },
    {
      "id": "COR-020",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business_invariant — incorrect range filter",
      "title": "ICS export silently drops leaves that span the requested date window (startDate before and endDate after the range)",
      "file": "apps/api/src/planning-export/planning-export.service.ts",
      "line": "100-108",
      "code_evidence": "    const leaves = await this.prisma.leave.findMany({\n      where: {\n        userId,\n        status: 'APPROVED',\n        ...(start || end\n          ? { OR: [{ startDate: dateFilter }, { endDate: dateFilter }] }\n          : {}),\n      },\n      include: { leaveType: true },\n    });",
      "description": "The date filter for leaves uses `OR: [{startDate: dateFilter}, {endDate: dateFilter}]`. This matches leaves that START within the window or END within the window, but misses leaves that SPAN the window entirely (startDate < window.start AND endDate > window.end). A common real-world case: annual leave running Jan 1–Dec 31, exported for June 1–30 — neither condition matches and the leave is absent from the ICS. Events and telework days use a point-in-time `date` field so they are unaffected. Only the leave filter has this semantic gap.",
      "root_cause": "The OR filter checks that startDate falls in the range OR endDate falls in the range, but never checks for spanning overlap (startDate <= end AND endDate >= start).",
      "impact": "Any approved leave whose full duration spans the export window is silently omitted from the calendar export. Users relying on ICS sync to their calendar app will see gaps — particularly for long continuous leaves (parental leave, extended sick leave). No error is raised; the export appears successful.",
      "suggested_fix": "Replace the OR filter with an overlap predicate: `{ startDate: { lte: new Date(end) }, endDate: { gte: new Date(start) } }`. This is the standard \"intervals overlap\" condition and matches all three cases (leave starting in range, ending in range, or spanning).",
      "acceptance_criteria": [
        "An APPROVED leave with startDate='2026-01-01' and endDate='2026-12-31' is included in an ICS export requested for start='2026-06-01' end='2026-06-30'",
        "A leave that starts and ends within the window is still included",
        "A leave entirely outside the window is still excluded"
      ],
      "verification_command": "grep -n 'startDate\\|endDate\\|dateFilter' apps/api/src/planning-export/planning-export.service.ts",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Events and telework use a single date point so they are unaffected. Only the leaves query has this spanning-overlap gap. Adversarial check: dateFilter = { gte: new Date(start), lte: new Date(end) } (lines 55-57). Applied as OR: [{startDate: dateFilter}, {endDate: dateFilter}] — confirmed spans are missed. No secondary filter or post-query correction found. Confirmed."
    },
    {
      "id": "COR-021",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "createBulkAssignment: telework check snapshot race — assertTeleworkCompatibility runs before the bulk inserts, but concurrent mutations can invalidate it",
      "file": "apps/api/src/predefined-tasks/predefined-tasks.service.ts",
      "line": "288-334",
      "code_evidence": "  async createBulkAssignment(assignedById: string, dto: BulkAssignmentDto) {\n    // Check predefined task exists\n    const task = await this.prisma.predefinedTask.findUnique(...);\n    if (!task || !task.isActive) { ... }\n    await this.assertTeleworkCompatibility(task, dto.userIds, dto.dates);\n\n    const results = { created: 0, skipped: 0, errors: [] as string[] };\n\n    for (const userId of dto.userIds) {\n      for (const dateStr of dto.dates) {\n        try {\n          await this.prisma.predefinedTaskAssignment.create({",
      "description": "The telework compatibility check is performed once as a snapshot before the loop starts. The bulk insert loop then runs N×M individual creates outside any transaction. Between the check and the insert loop (or between any two iterations of the loop), a concurrent request could change a user's telework schedule for one of the dates. More importantly, if the bulk insert itself takes seconds (large N×M), the check is stale. There is no transactional or re-check mechanism.",
      "root_cause": "assertTeleworkCompatibility is an upfront read with no lock, and the subsequent writes are not in a transaction, creating a TOCTOU window.",
      "impact": "Telework-incompatible assignments can slip through during concurrent schedule changes; for large bulk operations the window is proportionally large.",
      "suggested_fix": "Either wrap the entire operation in a REPEATABLE READ transaction and re-validate inside, or re-run the telework check per-assignment pair inside the loop before creating.",
      "acceptance_criteria": [
        "A concurrent telework schedule update during bulk assignment is detected and the conflicting assignment is rejected"
      ],
      "verification_command": "grep -n 'assertTeleworkCompatibility\\|predefinedTaskAssignment.create' apps/api/src/predefined-tasks/predefined-tasks.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "createAssignment (single) has the same pattern but the window is much smaller.",
      "adversarial_check": "Confirmed. Lines 288-334 verified: assertTeleworkCompatibility called once before loop at line 298, then N×M predefinedTaskAssignment.create calls in nested for-loops with no transaction wrapper. The TOCTOU window exists."
    },
    {
      "id": "COR-022",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "assertTeleworkCompatibility: date comparison uses naive new Date(string) — TZ ambiguity with date-only strings",
      "file": "apps/api/src/predefined-tasks/predefined-tasks.service.ts",
      "line": "33-38",
      "code_evidence": "    const teleworkSchedules = await this.prisma.teleworkSchedule.findMany({\n      where: {\n        userId: { in: userIds },\n        date: { in: dates.map((date) => new Date(date)) },\n        isTelework: true,\n      },\n      select: { userId: true, date: true },\n    });",
      "description": "The dates array received from the DTO contains ISO date strings. These are passed through `new Date(date)` and used in an `IN` filter against the DB `date` column (@db.Date). The TeleworkSchedule.date is a PostgreSQL DATE type. If date-only strings like '2026-03-25' are passed (which are valid @IsDateString() inputs), new Date('2026-03-25') in Node.js parses as midnight UTC — which happens to be correct for UTC servers. However, occurrence-generator.ts uses Date.UTC() strictly, creating an inconsistency in convention. On non-UTC deployments, date-only strings would produce wrong dates.",
      "root_cause": "Inconsistent date construction: occurrence-generator uses Date.UTC() strictly; assertTeleworkCompatibility uses new Date(string) which is TZ-sensitive for date-only strings.",
      "impact": "On non-UTC server deployments or when date strings lack time component, the telework conflict check returns false negatives, allowing telework-incompatible assignments to be created. Low risk on current UTC prod deployment.",
      "suggested_fix": "Normalize date strings to midnight UTC before querying: `dates.map(d => { const p = d.split('T')[0]; const [y,m,day] = p.split('-').map(Number); return new Date(Date.UTC(y, m-1, day)); })` — matching the occurrence-generator convention.",
      "acceptance_criteria": [
        "Telework conflict check correctly detects conflicts on any server TZ setting",
        "Integration test passes with TZ=America/New_York"
      ],
      "verification_command": "grep -n 'new Date(date)\\|new Date(dateStr)\\|Date.UTC' apps/api/src/predefined-tasks/predefined-tasks.service.ts apps/api/src/predefined-tasks/occurrence-generator.ts",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Prod is UTC (MEMORY.md), TeleworkSchedule.date is @db.Date. BulkAssignmentDto uses @IsDateString() with UTC-qualified examples. Risk is latent but real for date-only inputs or non-UTC deployments.",
      "adversarial_check": "Confirmed at reduced severity. Code at line 35 verified: `dates.map((date) => new Date(date))`. Schema confirmed @db.Date on TeleworkSchedule.date. BulkAssignmentDto allows any @IsDateString() input including date-only strings. occurrence-generator uses Date.UTC() strictly. Inconsistency is real; current prod exposure is low (UTC server + UTC-qualified inputs in examples)."
    },
    {
      "id": "COR-023",
      "severity": "important",
      "category": "correctness",
      "subcategory": "date-time",
      "title": "captureSnapshots() uses server-local midnight instead of UTC midnight for deduplication query",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1158-1166",
      "code_evidence": "    const now = new Date();\n    const startOfDay = new Date();\n    startOfDay.setHours(0, 0, 0, 0);\n\n    // Batch-fetch existing snapshots for today (1 query)\n    const existingToday = await this.prisma.projectSnapshot.findMany({\n      where: {\n        projectId: { in: projects.map((p) => p.id) },\n        date: startOfDay,\n      },",
      "description": "The schema comment on ProjectSnapshot explicitly states `date stored as startOfDay (midnight UTC)` (@@unique([projectId, date]) // COR-014: DB-level race guard; date stored as startOfDay (midnight UTC)). The deduplication lookup uses `startOfDay.setHours(0, 0, 0, 0)` which sets to server local midnight, not UTC midnight. If the prod server is not in UTC (or its TZ env differs), the `date: startOfDay` query will never match existing rows, causing the `alreadySnapshotted` set to always be empty. This defeats the deduplication check, and every invocation of captureSnapshots() on non-UTC servers will try to insert a new snapshot (relying only on the DB unique constraint's skipDuplicates guard), meaning `captured` always returns 0 (all are skipped by skipDuplicates) but the dedup logic is silently broken.",
      "root_cause": "`setHours(0,0,0,0)` resets to local timezone midnight, not UTC midnight, while PostgreSQL stores and compares DateTime columns in UTC.",
      "impact": "On non-UTC servers (e.g. Europe/Paris), the lookup timestamp will be off by 1-2 hours; existing snapshots are never found in the dedup query; every call attempts insertion and is silently skipped by skipDuplicates. The design intent (COR-014) is violated.",
      "suggested_fix": "Replace `startOfDay.setHours(0, 0, 0, 0)` with UTC-anchored midnight: `startOfDay.setUTCHours(0, 0, 0, 0)`. Also use `setUTCHours` for `now` if it is used for the same day boundary. This aligns the dedup query with what PostgreSQL stored.",
      "acceptance_criteria": [
        "On a server with TZ=Europe/Paris, running captureSnapshots() twice in the same day returns {captured: 0} on the second call (dedup works)",
        "The date stored in project_snapshots equals the UTC midnight of the current day"
      ],
      "verification_command": "grep -n 'setHours\\|setUTCHours' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED — line 1160 uses setHours(0,0,0,0); schema comment at schema.prisma:229 explicitly states 'midnight UTC'. No UTC normalization found elsewhere in captureSnapshots(). Prod server noted as UTC (MEMORY) but code is fragile and violates documented convention."
    },
    {
      "id": "COR-024",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "update() does not validate partial date changes against existing project dates",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "583-588",
      "code_evidence": "    // Vérifier les dates si fournies\n    if (startDate && endDate && new Date(endDate) < new Date(startDate)) {\n      throw new BadRequestException(\n        'La date de fin doit être postérieure ou égale à la date de début',\n      );\n    }",
      "description": "The date invariant (endDate >= startDate) is only enforced when both `startDate` and `endDate` are simultaneously provided in the PATCH request. The existing project's dates (in `existingProject`) are loaded at line 558-560 but are never used in the validation. A PATCH with only `endDate: '2020-01-01'` on a project with `startDate: '2025-01-01'` passes validation and stores an end date before the start date, violating the core scheduling invariant. Similarly, a PATCH with only `startDate: '2030-01-01'` on a project with `endDate: '2025-01-01'` also bypasses validation.",
      "root_cause": "The date comparison condition requires both `startDate && endDate` to be truthy, but partial updates only supply one; the check is never evaluated against the existing persisted value.",
      "impact": "Projects can end up with endDate < startDate after a partial PATCH, corrupting scheduling logic, analytics (date ranges), and milestone overdue calculations that depend on this invariant.",
      "suggested_fix": "Compute the effective start and end: `const effectiveStart = startDate ? new Date(startDate) : existingProject.startDate; const effectiveEnd = endDate ? new Date(endDate) : existingProject.endDate;` Then validate `if (effectiveEnd && effectiveStart && effectiveEnd < effectiveStart) throw BadRequestException(...)`.",
      "acceptance_criteria": [
        "PATCH /projects/:id with only endDate='2020-01-01' on a project with startDate='2025-01-01' returns 400",
        "PATCH /projects/:id with only startDate='2030-01-01' on a project with endDate='2025-01-01' returns 400",
        "PATCH /projects/:id with both valid dates in correct order returns 200"
      ],
      "verification_command": "grep -n 'startDate && endDate' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED — existingProject is fetched at line 558 but existingProject.startDate/endDate are not referenced in the date validation block at line 584. The guard fires only when both startDate and endDate are truthy in the PATCH payload."
    },
    {
      "id": "COR-025",
      "severity": "important",
      "category": "correctness",
      "subcategory": "transaction-boundary",
      "title": "hardDelete(): audit log written outside transaction — audit records 'deleted' even if delete fails",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "855-883",
      "code_evidence": "    // Final snapshot to the audit trail BEFORE the row is erased (audit\n    // suggested-fix b). Plain await — not transactional with the delete — to\n    // match the existing archive()/unarchive() emission pattern; the audit\n    // pipeline (AuditPersistenceService) is out of DAT-007 scope.\n    await this.auditPersistence.log({\n      action: AuditAction.PROJECT_DELETED,\n      entityType: 'Project',\n      entityId: id,\n      actorId: user.id,\n      payload: {\n        snapshot: {\n          id: project.id,",
      "description": "The PROJECT_DELETED audit event is emitted and awaited before `prisma.project.delete()`. If the delete fails (e.g., a new FK constraint is added between the dependency check and the delete, or any unexpected DB error occurs), the audit log contains a 'deleted' record for a project that still exists. The comment explicitly acknowledges this: 'Plain await — not transactional with the delete'. This is a known trade-off documented in code, but it creates an inconsistency: the immutable audit trail claims the project was deleted while it remains in the DB.",
      "root_cause": "Audit log and delete are two separate non-transactional operations; failure of the second leaves the audit trail in a false state.",
      "impact": "The audit trail, which is supposed to be an immutable record for compliance purposes, will contain a PROJECT_DELETED entry for a project that still exists if the delete fails. Compliance audits relying on this trail will be incorrect.",
      "suggested_fix": "Wrap both `auditPersistence.log` and `prisma.project.delete` in a single `$transaction`. If the audit service does not support transactional context, emit the audit event AFTER the delete succeeds. Alternatively, treat the audit as a best-effort post-delete step and explicitly document this as an accepted risk.",
      "acceptance_criteria": [
        "If prisma.project.delete() throws, no PROJECT_DELETED audit record should exist for that project",
        "Or: clear documentation that this is an accepted risk with compensating controls"
      ],
      "verification_command": "grep -n 'auditPersistence.log\\|project.delete' apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED with downgraded confidence (high to medium). The code comment explicitly acknowledges the non-transactional pattern as intentional and scoped out of DAT-007. The failure window is narrow (dependency check runs first, then audit, then delete). However the compliance implication is real — a failed delete after audit write produces a false PROJECT_DELETED record in the immutable chain. The inline comment does not constitute a formally accepted-risk decision documented outside the code."
    },
    {
      "id": "COR-026",
      "severity": "important",
      "category": "correctness",
      "subcategory": "non-atomic-delete",
      "title": "services.service.ts remove() performs 3 sequential DB writes without a transaction — inconsistent state on crash",
      "file": "apps/api/src/services/services.service.ts",
      "line": "308-343",
      "code_evidence": "    // Détacher les utilisateurs liés avant suppression\n    if (service._count.userServices > 0) {\n      await this.prisma.userService.deleteMany({\n        where: { serviceId: id },\n      });\n    }\n\n    // Retirer le manager du service\n    if (service.managerId) {\n      await this.prisma.service.update({\n        where: { id },\n        data: { managerId: null },\n      });\n    }\n\n    await this.prisma.service.delete({\n      where: { id },\n    });",
      "description": "The `remove()` method executes three separate database writes sequentially: (1) deleteMany on userService pivot rows, (2) update service.managerId to null, (3) delete the service. None of these are wrapped in a `prisma.$transaction()`. If the process crashes or the connection drops after step 1 but before step 3, all userServices pivot rows are gone but the service still exists, leaving the data in an inconsistent state. Step 2 (update managerId=null before deleting the row) is also logically redundant since the service is immediately deleted, but it still incurs an extra round-trip and is evidence of confused logic.\n\nAdversarial note: UserService has `onDelete: Cascade` on the service FK (schema.prisma line 158), so step 1 is redundant — the DB cascade handles it. However, the absence of a transaction still allows a crash after step 2 (managerId set to null) but before step 3 (service deleted) to leave a service row with managerId=null in a permanently inconsistent intermediate state visible to other reads.",
      "root_cause": "The delete operation was built incrementally without wrapping all writes in a single Prisma transaction, leaving the multi-step operation non-atomic.",
      "impact": "A process crash or transient DB connection error mid-delete leaves `service` rows intact with orphaned (but deleted) `userService` rows, or a service row with managerId=null that was never cleaned up. Re-running the delete would then find zero userServices but a non-null managerId, producing an incoherent intermediate state that other reads could observe. Impact somewhat mitigated by onDelete: Cascade on UserService which auto-cleans pivot rows on service delete — but does not eliminate the managerId=null partial state.",
      "suggested_fix": "Wrap all three operations in `prisma.$transaction()`. Also remove the redundant managerId nulling step since the row will be deleted:\n```typescript\nawait this.prisma.$transaction(async (tx) => {\n  if (service._count.userServices > 0) {\n    await tx.userService.deleteMany({ where: { serviceId: id } });\n  }\n  await tx.service.delete({ where: { id } });\n});\n```\nNote: if the DB schema uses `onDelete: Cascade` on userServices, even step 1 can be removed.",
      "acceptance_criteria": [
        "DELETE /services/:id completes atomically — no partial state visible under concurrent reads",
        "A simulated crash between the first and third writes leaves the service intact (rolled back)"
      ],
      "verification_command": "grep -n '\\$transaction\\|deleteMany\\|service.update\\|service.delete' apps/api/src/services/services.service.ts",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The managerId=null update step (lines 332-337) is entirely redundant before delete — it should be removed along with the transaction wrapping fix. Adversarial check: onDelete: Cascade on UserService.service (schema.prisma line 158) makes step 1 redundant, but the missing transaction is still a real correctness gap — worst case is managerId=null service row surviving if crash between steps 2 and 3."
    },
    {
      "id": "COR-027",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing-p2002-catch",
      "title": "skills.service.ts create() and update() missing P2002 catch — concurrent duplicate name causes unhandled 500",
      "file": "apps/api/src/skills/skills.service.ts",
      "line": "28-57",
      "code_evidence": "  async create(createSkillDto: CreateSkillDto) {\n    const { name, description, category, requiredCount } = createSkillDto;\n\n    // Vérifier l'unicité du nom\n    const existing = await this.prisma.skill.findFirst({\n      where: { name },\n    });\n\n    if (existing) {\n      throw new ConflictException('Une compétence avec ce nom existe déjà');\n    }\n\n    const skill = await this.prisma.skill.create({",
      "description": "The `create()` method performs a pre-check `findFirst` then calls `prisma.skill.create()` without wrapping the create in a try/catch for Prisma P2002 (unique constraint violation). `Skill.name` has a `@unique` constraint in schema.prisma. Under concurrent requests (two users submitting the same skill name simultaneously), both pass the findFirst check, then the second `prisma.skill.create()` throws a `PrismaClientKnownRequestError` with code P2002 that is unhandled. This propagates as a 500 Internal Server Error. The same issue exists in `update()` at line 165 which also has no try/catch around `prisma.skill.update()`.\n\nBy contrast, `departments.service.ts` and `services.service.ts` explicitly define an `isUniqueViolation()` helper and wrap their create/update calls in try/catch blocks to map P2002 → 409 ConflictException. `skills.service.ts` lacks this defense.",
      "root_cause": "`skills.service.ts` never adopted the P2002 catch pattern that `departments.service.ts` and `services.service.ts` introduced (COR-034), leaving the unique-constraint race unhandled.",
      "impact": "A concurrent double-submit of a skill with the same name (or a client retry) returns HTTP 500 instead of 409, exposing an internal Prisma error in the response body and breaking any client that expects a structured conflict error.",
      "suggested_fix": "Add `isUniqueViolation` helper and wrap both `create()` and `update()` calls in try/catch blocks, mirroring the pattern in `departments.service.ts`:\n```typescript\nfunction isUniqueViolation(err: unknown): boolean {\n  return err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002';\n}\n// In create():\ntry {\n  const skill = await this.prisma.skill.create({ ... });\n  return skill;\n} catch (err) {\n  if (isUniqueViolation(err)) throw new ConflictException('Une compétence avec ce nom existe déjà');\n  throw err;\n}\n```",
      "acceptance_criteria": [
        "Two concurrent POST /skills with the same name both return HTTP 409 (not 500)",
        "PATCH /skills/:id renaming to an existing name returns HTTP 409 under concurrency"
      ],
      "verification_command": "grep -n 'isUniqueViolation\\|P2002\\|catch' apps/api/src/skills/skills.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "departments.service.ts lines 12-19 and 69-74 show the correct pattern. importSkills() at line 725 already has a try/catch per item. Only the standalone create()/update() methods are missing it. Adversarial check: grep confirmed zero P2002/catch/isUniqueViolation in skills.service.ts outside line 725 (importSkills). schema.prisma line 743 confirms @unique on Skill.name."
    },
    {
      "id": "COR-028",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "importTasks: missing date validity check — invalid startDate/endDate strings silently stored as Invalid Date",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "1461-1462",
      "code_evidence": "            startDate: taskData.startDate ? new Date(taskData.startDate) : null,\n            endDate: taskData.endDate ? new Date(taskData.endDate) : null,",
      "description": "In importTasks() (the actual write path, distinct from validateImport()), dates are passed directly to `new Date()` without validating the result with isNaN(). If taskData.startDate is a non-ISO string that the Date constructor cannot parse, `new Date(taskData.startDate)` returns an Invalid Date object, which Prisma will attempt to pass to PostgreSQL. The corresponding validateImport() path validates dates (lines 1653-1673), but only when BOTH startDate and endDate are present (condition: `if (taskData.startDate && taskData.endDate)`). The actual write path has no validation at all. DTO field startDate/endDate use @IsString() not @IsDateString().",
      "root_cause": "Date validation is only performed in the dry-run path (validateImport), not in the real write path (importTasks). Additionally validateImport only validates when both dates are present.",
      "impact": "Tasks with invalid dates will cause a Prisma/DB error caught by the per-row catch block — the task won't be created silently with corrupt dates, but the error message will be a cryptic Prisma error rather than a user-friendly validation message.",
      "suggested_fix": "Add `isNaN(new Date(taskData.startDate).getTime())` guards in importTasks() before constructing Date objects, consistent with the pattern in validateImport() lines 1655-1669.",
      "acceptance_criteria": [
        "POST /project/:projectId/import with an invalid startDate returns an error for that row, not a task with null/invalid date"
      ],
      "verification_command": "grep -n 'new Date(taskData\\.' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The ImportTaskDto uses @IsString() not @IsDateString() for startDate/endDate (verified). Impact is slightly softened: Prisma will throw on Invalid Date, caught by per-row catch, so no silent data corruption — but error message is cryptic.",
      "adversarial_check": "Confirmed. Lines 1461-1462 verified: no isNaN guards. ImportTaskDto at startDate/endDate fields uses @IsString() @IsOptional() (no @IsDateString()). validateImport at line 1652 only validates when both dates exist. Write path has zero date validation."
    },
    {
      "id": "COR-029",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "reorderSubtasks: subtask IDs from payload not validated to belong to taskId — cross-task position manipulation possible",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "2039-2060",
      "code_evidence": "  async reorderSubtasks(\n    taskId: string,\n    subtaskIds: string[],\n    currentUser?: AccessUser,\n  ) {\n    ...\n    const task = await this.prisma.task.findUnique({ where: { id: taskId } });\n    if (!task) throw new NotFoundException('Tâche introuvable');\n\n    await this.prisma.$transaction(\n      subtaskIds.map((id, index) =>\n        this.prisma.subtask.update({\n          where: { id },\n          data: { position: index },\n        }),\n      ),\n    );\n\n    return this.getSubtasks(taskId);",
      "description": "reorderSubtasks() verifies that the parent task exists, but does not verify that each subtaskId in the payload actually belongs to that task (i.e., subtask.taskId === taskId). An authenticated user with tasks:update permission can send subtask IDs from a different task and silently overwrite position values on subtasks they should have no access to. The getSubtasks(taskId) result returned will be correct for the requested task but the foreign subtasks are mutated.",
      "root_cause": "The $transaction only filters by subtask PK (id), not by the composite (id, taskId), so it operates on any subtask regardless of ownership.",
      "impact": "Position data corruption on subtasks of arbitrary tasks; potential information-leakage side-channel (update succeeds vs 404 reveals existence of subtask IDs across task boundaries).",
      "suggested_fix": "Either (a) pre-validate that all subtaskIds belong to taskId via a findMany({where:{id:{in:subtaskIds}, taskId}}) length check, or (b) change each update where clause to {id, taskId} so unowned subtasks produce a P2025 not-found error.",
      "acceptance_criteria": [
        "Sending a subtaskId belonging to a different task returns 404 or 400",
        "Position of subtasks on the other task is unmodified"
      ],
      "verification_command": "grep -n 'reorderSubtasks\\|subtask.update' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "updateSubtask and deleteSubtask correctly use findFirst({where:{id, taskId}}) — reorderSubtasks diverges from that pattern. Verified at lines 2006-2007 and 2029-2030.",
      "adversarial_check": "Confirmed. updateSubtask (line 2006) and deleteSubtask (line 2029) both use findFirst({where:{id:subtaskId, taskId}}) before acting. reorderSubtasks (line 2051-2057) uses where:{id} only, no taskId filter. Cross-task mutation is possible."
    },
    {
      "id": "COR-030",
      "severity": "important",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "update(): date clearing is impossible — falsy check prevents clearing startDate/endDate to null",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "795-796",
      "code_evidence": "          ...(startDate && { startDate: new Date(startDate) }),\n          ...(endDate && { endDate: new Date(endDate) }),",
      "description": "In update(), dates are set only when the value is truthy. However, the UpdateTaskDto fields are optional strings — if a client sends `startDate: null` or an explicit empty string to clear a date, the falsy check skips the field entirely and the existing date is retained. There is no way to clear a date via PATCH. This is a business-logic correctness gap: date clearing is a valid user action (removing a deadline). The same pattern exists for create() lines 233-234 but create context is less critical since you can just omit the field.",
      "root_cause": "Using a falsy guard (`...(startDate && {...})`) instead of an explicit `!== undefined` check; null/empty-string inputs are silently ignored.",
      "impact": "Once a startDate or endDate is set on a task, it cannot be cleared via the PATCH API; the UI or any client that sends null to remove a date will receive a stale non-null date in the response.",
      "suggested_fix": "Change to `...(startDate !== undefined && { startDate: startDate ? new Date(startDate) : null })` so explicit null/empty string clears the field.",
      "acceptance_criteria": [
        "PATCH /tasks/:id with {endDate: null} results in task.endDate being null",
        "PATCH /tasks/:id without endDate key leaves endDate unchanged"
      ],
      "verification_command": "grep -n 'startDate && {\\|endDate && {' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "projectId clearing uses `...(projectId && {projectId})` with the same issue (line 789) but that case is handled by detachFromProject endpoint.",
      "adversarial_check": "Confirmed. Lines 795-796 verified verbatim: `...(startDate && { startDate: new Date(startDate) })` and `...(endDate && { endDate: new Date(endDate) })`. No undefined check. Null/empty string inputs skip the field entirely."
    },
    {
      "id": "COR-031",
      "severity": "important",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "addDependency: check-then-create pattern is not race-safe — duplicate dependency race between circular-check and create",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "922-964",
      "code_evidence": "    // Vérifier qu'on ne crée pas une dépendance circulaire\n    const hasCircularDependency = await this.checkCircularDependency(\n      dependsOnTaskId,\n      taskId,\n    );\n\n    if (hasCircularDependency) {\n      throw new BadRequestException(\n        'Cette dépendance créerait une dépendance circulaire',\n      );\n    }\n\n    // Vérifier que la dépendance n'existe pas déjà\n    const existingDependency = await this.prisma.taskDependency.findUnique({",
      "description": "The circular-dependency check (checkCircularDependency) traverses the graph with a series of individual SELECT queries outside any transaction. Between the final check and the INSERT, another concurrent request can insert the reverse dependency, creating a cycle that neither check catches. The existing-dependency check is similarly non-atomic. While the DB unique constraint on (taskId, dependsOnTaskId) will prevent true duplicates, the circular check has no DB-level enforcement and can be raced by two simultaneous symmetric requests (A→B and B→A submitted concurrently).",
      "root_cause": "The multi-step graph traversal + insert sequence has no serialization, allowing concurrent symmetric dependency creation to slip through the circular check.",
      "impact": "Circular dependency cycle in the task graph; all graph traversals or rendering logic that follow dependency chains may enter infinite loops or produce incorrect critical-path calculations.",
      "suggested_fix": "Wrap the circular-check + insert in a SERIALIZABLE transaction or use an advisory lock keyed on the lower/upper task UUIDs. Alternatively add a DB-level trigger or deferred constraint to enforce DAG invariant.",
      "acceptance_criteria": [
        "Concurrent POST /:id/dependencies with reverse direction is rejected for at least one request",
        "No cycle is observable in task dependency graph after concurrent inserts"
      ],
      "verification_command": "grep -n 'checkCircularDependency\\|taskDependency.create\\|taskDependency.findUnique' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "checkCircularDependency itself is BFS, correct for sequential execution.",
      "adversarial_check": "Confirmed. Schema has @@unique([taskId, dependsOnTaskId]) which prevents exact duplicate inserts (P2002), but circular race (A→B concurrent with B→A) has no DB-level guard. Both requests see a cycle-free graph, pass the check, then both succeed creating a cycle."
    },
    {
      "id": "COR-032",
      "severity": "important",
      "category": "correctness",
      "subcategory": "scope-leak / unintended write",
      "title": "findForPlanningOverview calls expandRecurringRulesForRange with no userId filter, materialising rows for all users",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "246-253",
      "code_evidence": "    await this.expandRecurringRulesForRange(startDate, endDate);\n\n    return this.prisma.teleworkSchedule.findMany({\n      where: {\n        userId: { in: [...new Set(userIds)] },\n        date: {\n          gte: new Date(startDate),\n          lte: new Date(endDate),\n        },",
      "description": "findForPlanningOverview is called internally by planning/overview routes and receives an explicit `userIds` list that is the already-authorised visible scope. However, it calls `expandRecurringRulesForRange(startDate, endDate)` without a filterUserId argument, causing the expansion to materialise TeleworkSchedule rows for every active recurring rule in the system, not just those belonging to the visible users. The subsequent findMany read is correctly scoped to `userIds`, but the side-effect is global.",
      "root_cause": "The filterUserId argument to expandRecurringRulesForRange is omitted; the function's optional param defaults to undefined → queries all rules.",
      "impact": "Every overview page load that spans a date range triggers creation of telework rows for all users with active recurring rules, irrespective of the viewer's scope. Scales O(users × rules × days). At modest scale (50 users, 5 rules each, 30-day range) this is 7500+ DB inserts per overview load.",
      "suggested_fix": "Pass the userIds set to the expansion. Because expandRecurringRulesForRange takes a single optional userId string, the simplest change is to loop or refactor to accept string[]. Short-term: call `Promise.all(userIds.map(uid => this.expandRecurringRulesForRange(startDate, endDate, uid)))` before the findMany. Long-term: refactor expandRecurringRulesForRange to accept `userId?: string | string[]`.",
      "acceptance_criteria": [
        "Calling findForPlanningOverview with userIds=['A','B'] does not create TeleworkSchedule rows for user C.",
        "The expansion still materialises rows for A and B correctly."
      ],
      "verification_command": "grep -n 'findForPlanningOverview\\|expandRecurringRulesForRange' apps/api/src/telework/telework.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED. Verbatim code verified at lines 237-284. The call at line 246 `await this.expandRecurringRulesForRange(startDate, endDate)` passes no third argument. The function at line 292 shows `filterUserId?: string` — when absent, all active rules are processed regardless of which users are in scope."
    },
    {
      "id": "COR-033",
      "severity": "important",
      "category": "correctness",
      "subcategory": "TOCTOU / lost-update / unhandled P2002",
      "title": "expandRecurringRulesForRange and generateSchedulesFromRules do non-atomic findUnique→create, causing unhandled P2002 on concurrent requests",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "338-356",
      "code_evidence": "          const existing = await this.prisma.teleworkSchedule.findUnique({\n            where: {\n              userId_date: {\n                userId: rule.userId,\n                date: dateOnly,\n              },\n            },\n          });\n\n          if (!existing) {\n            await this.prisma.teleworkSchedule.create({\n              data: {\n                userId: rule.userId,\n                date: dateOnly,\n                isTelework: true,\n                isException: false,\n              },\n            });",
      "description": "Both expandRecurringRulesForRange (lines 338-356) and generateSchedulesFromRules (lines 908-925) use the same read-then-write pattern: findUnique checks whether a row exists; if not, create is called. Under READ COMMITTED isolation (Postgres default), two concurrent calls for overlapping ranges will both see null from findUnique and both attempt create. The second create hits the `userId_date` unique constraint and throws a Prisma P2002 (unique constraint violation) that is not caught anywhere in the call stack. NestJS bubbles this as an unhandled exception → HTTP 500. Because findAll auto-calls expansion on every date-range GET, this is easily triggered in production by parallel frontend requests.",
      "root_cause": "Non-atomic check-then-insert outside a transaction with no P2002 catch or upsert fallback.",
      "impact": "Concurrent GET /telework?startDate&endDate requests from the same (or different) users produce HTTP 500 responses instead of idempotent success. The first succeeds, the second crashes. In the worst case, repeated errors fill logs and block the user.",
      "suggested_fix": "Replace findUnique+create with a Prisma upsert using skipDuplicates, or wrap in a try/catch and ignore P2002:\n```ts\ntry {\n  await this.prisma.teleworkSchedule.create({ data: { ... } });\n} catch (e: unknown) {\n  if ((e as { code?: string }).code !== 'P2002') throw e;\n  // row already exists from concurrent insert — safe to ignore\n}\n```\nAlternatively use createMany with skipDuplicates if batching is acceptable.",
      "acceptance_criteria": [
        "Two concurrent calls to expandRecurringRulesForRange for the same userId and overlapping date range both return without error.",
        "No duplicate rows are created — the unique constraint is respected.",
        "HTTP 200 is returned in both cases."
      ],
      "verification_command": "grep -n 'prisma.teleworkSchedule.create\\|P2002\\|skipDuplicates' apps/api/src/telework/telework.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED. Full-file grep for 'P2002', 'skipDuplicates', 'upsert', 'catch' in telework.service.ts returned no results — no error-handling exists for the unique constraint violation. The @@unique([userId, date]) constraint on TeleworkSchedule (schema.prisma line 733) is confirmed. generateSchedulesFromRules lines 908-925 contain an identical copy of the same pattern and need the same fix."
    },
    {
      "id": "COR-034",
      "severity": "important",
      "category": "correctness",
      "subcategory": "timezone / local-TZ date arithmetic",
      "title": "getWeeklySchedule uses local-TZ getDay()/setDate()/setHours() for week boundary calculation, breaking on non-UTC servers",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "417-429",
      "code_evidence": "    const referenceDate = date ? new Date(date) : new Date();\n\n    // Calculer le début et la fin de la semaine (lundi à dimanche)\n    const dayOfWeek = referenceDate.getDay();\n    const diff = dayOfWeek === 0 ? -6 : 1 - dayOfWeek; // Lundi = début\n\n    const start = new Date(referenceDate);\n    start.setDate(referenceDate.getDate() + diff);\n    start.setHours(0, 0, 0, 0);\n\n    const end = new Date(start);\n    end.setDate(start.getDate() + 6); // Dimanche\n    end.setHours(23, 59, 59, 999);",
      "description": "getWeeklySchedule computes week boundaries using local-TZ methods (getDay, getDate, setDate, setHours). This contradicts the module's own documented approach: all other date operations in this file use UTC-anchored arithmetic (dayKeyToUTCDate, teleworkDayKey, Date.UTC) explicitly to avoid local-TZ DST hazards. On a server running in Europe/Paris (or any non-UTC TZ), getDay() returns the local calendar day, setHours(0,0,0,0) sets local midnight, and the resulting Date values have UTC representations that may be off by ±1–2 hours relative to UTC-midnight stored DB dates. The stored dates are always UTC-midnight (enforced by dayKeyToUTCDate). The gte/lte comparison therefore uses mismatched anchors: local-midnight vs UTC-midnight, producing off-by-one boundary mismatches during DST transitions.",
      "root_cause": "Date boundary math uses local-TZ getters/setters instead of the UTC-anchored helpers (`dayKeyToUTCDate`, `Date.UTC`) established elsewhere in the same file for precisely this reason.",
      "impact": "On DST transition dates, the week query can miss the first or last day of the week. On any non-UTC server (the prod box is UTC per memory, so this is a latent risk on dev machines and potential future prod infra changes).",
      "suggested_fix": "Use the same day-key arithmetic already in the file:\n```ts\nconst refKey = teleworkDayKey(referenceDate);\nconst jsDay = new Date(Date.UTC(...refKey.split('-').map(Number))).getUTCDay();\nconst diff = jsDay === 0 ? -6 : 1 - jsDay;\n// advance/retreat by diff days using nextTeleworkDayKey loop or UTC arithmetic\nconst startKey = /* cursor from refKey by diff steps */;\nconst endKey = /* startKey + 6 days */;\nconst start = dayKeyToUTCDate(startKey);\nconst end = new Date(dayKeyToUTCDate(endKey).getTime() + 86400000 - 1);\n```",
      "acceptance_criteria": [
        "On a process running with TZ=Europe/Paris, getWeeklySchedule('2025-03-30') returns Monday=2025-03-24 as weekStart (UTC midnight), not 2025-03-23T23:00:00Z.",
        "Week boundaries match the UTC-midnight dates stored by dayKeyToUTCDate."
      ],
      "verification_command": "TZ=Europe/Paris node -e \"const d=new Date('2025-03-30');d.setHours(0,0,0,0);console.log(d.toISOString())\"",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED as stated. Verbatim code verified at lines 417-429. Note: the module-level constant TELEWORK_TZ = 'Europe/Paris' (line 18) and the use of formatInTimeZone for teleworkDayKey explicitly anchor all date computations to the Paris calendar. getWeeklySchedule deviates from this design contract by using local-TZ methods. Prod server is UTC per MEMORY.md (so on UTC, local midnight = UTC midnight and getDay() = UTC day), making this latent on prod but active on dev machines running Europe/Paris TZ. Severity correctly set to important rather than blocking given prod UTC mitigation."
    },
    {
      "id": "COR-035",
      "severity": "important",
      "category": "correctness",
      "subcategory": "timezone / local-TZ date arithmetic",
      "title": "getUserStats builds year boundaries with local-TZ new Date(year,0,1) and getTeamSchedule uses local-midnight setHours(0,0,0,0) for exact-match against UTC-midnight DB dates",
      "file": "apps/api/src/telework/telework.service.ts",
      "line": "463-466",
      "code_evidence": "    const currentYear = year ?? new Date().getFullYear();\n\n    const startDate = new Date(currentYear, 0, 1);\n    const endDate = new Date(currentYear, 11, 31, 23, 59, 59, 999);",
      "description": "getUserStats constructs year boundaries using the local-TZ constructor `new Date(year, month, day)`. On a Europe/Paris server, `new Date(2026, 0, 1)` evaluates to `2025-12-31T23:00:00Z` (UTC), not `2026-01-01T00:00:00Z`. The `gte` filter would therefore include one entry from Dec 31 of the prior year. Separately, getTeamSchedule (line 948-952) sets `targetDate.setHours(0,0,0,0)` (local midnight) then uses `where.date = targetDate` as an exact equality filter against UTC-midnight stored dates. On a non-UTC server, local midnight ≠ UTC midnight → the exact-match filter returns zero rows for every query.",
      "root_cause": "Local-TZ constructors and setters used where UTC-anchored Date.UTC() is required, inconsistent with the module-level COR-012/COR-027 design.",
      "impact": "getUserStats on a Europe/Paris server incorrectly includes the last day of December of the previous year (off-by-one on startDate). getTeamSchedule returns an empty array for all dates on any non-UTC server.",
      "suggested_fix": "For getUserStats: use `new Date(Date.UTC(currentYear, 0, 1))` and `new Date(Date.UTC(currentYear, 11, 31, 23, 59, 59, 999))`. For getTeamSchedule: derive the target day key and use `dayKeyToUTCDate(teleworkDayKey(targetDate))` for the exact-match value.",
      "acceptance_criteria": [
        "On a TZ=Europe/Paris process, getUserStats(user, 2026) does not include rows dated 2025-12-31.",
        "On a TZ=Europe/Paris process, getTeamSchedule('2026-01-15') returns rows whose date field is 2026-01-15T00:00:00Z."
      ],
      "verification_command": "TZ=Europe/Paris node -e \"console.log(new Date(2026,0,1).toISOString())\"",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED as stated. Verbatim code verified at lines 463-466 (getUserStats) and 947-953 (getTeamSchedule — targetDate.setHours(0,0,0,0) then used as exact equality filter). Both deviate from the module's UTC-anchored design contract. Prod server is UTC so latent on prod, active on Europe/Paris dev machines. Same latency caveat as S6-5."
    },
    {
      "id": "COR-036",
      "severity": "important",
      "category": "correctness",
      "subcategory": "race-condition / business invariant bypass",
      "title": "ThirdPartiesService.update: LEGAL_ENTITY invariant checked without transaction — concurrent partial updates can violate it",
      "file": "apps/api/src/third-parties/third-parties.service.ts",
      "line": "115-151",
      "code_evidence": "  async update(id: string, dto: UpdateThirdPartyDto): Promise<ThirdParty> {\n    const existing = await this.prisma.thirdParty.findUnique({ where: { id } });\n    if (!existing) {\n      throw new NotFoundException(`Third party ${id} not found`);\n    }\n\n    const nextType = dto.type ?? existing.type;\n    const nextFirstName =\n      dto.contactFirstName !== undefined\n        ? dto.contactFirstName\n        : existing.contactFirstName;",
      "description": "The LEGAL_ENTITY invariant (no contactFirstName/contactLastName allowed) is validated by merging the incoming dto fields with the current DB row (`existing`). However, this `findUnique` + invariant-check + `update` sequence is NOT wrapped in a transaction. Two concurrent PATCH requests — one changing `type` to LEGAL_ENTITY and another setting `contactFirstName = 'John'` — can both read the same `existing` row, pass their respective invariant checks, then execute their `update` calls. The final DB state may have `type=LEGAL_ENTITY` and `contactFirstName='John'`, violating the invariant.",
      "root_cause": "Read-check-write pattern for a multi-field business invariant without a serializable transaction or optimistic concurrency (no version/updated_at in WHERE clause).",
      "impact": "The LEGAL_ENTITY type invariant can be violated via concurrent partial updates, resulting in a DB record in an invalid state that downstream code does not expect.",
      "suggested_fix": "Wrap `findUnique`, invariant check, and `update` in a `$transaction` with isolation level SERIALIZABLE, or add an `updated_at` version field and use it in the `update WHERE` clause for optimistic concurrency control.",
      "acceptance_criteria": [
        "Concurrent PATCH requests that individually pass the invariant check but collectively violate it are rejected",
        "The final state of a third party always satisfies: type=LEGAL_ENTITY implies contactFirstName IS NULL AND contactLastName IS NULL"
      ],
      "verification_command": "grep -n '\\$transaction\\|LEGAL_ENTITY\\|contactFirstName' apps/api/src/third-parties/third-parties.service.ts",
      "adversarial_check": "Verified: code_evidence is verbatim at lines 115-125. The update at line 140 is a bare prisma.thirdParty.update with no transaction wrapper. The invariant is purely a service-layer check with no DB CHECK constraint backup. Finding stands. Confidence remains medium (concurrent writes are uncommon in single-user admin usage, but the invariant is still breakable under load).",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "This is a true concurrent-write race; under normal single-user traffic it does not manifest. The LEGAL_ENTITY restriction is also only a service-layer invariant — not enforced via a DB CHECK constraint."
    },
    {
      "id": "COR-037",
      "severity": "important",
      "category": "correctness",
      "subcategory": "TOCTOU / concurrency / missing unique constraint",
      "title": "upsertDismissal findFirst→create inside $transaction does not prevent duplicate dismissal rows under READ COMMITTED",
      "file": "apps/api/src/time-tracking/time-tracking.service.ts",
      "line": "296-330",
      "code_evidence": "    const userId = currentUser.id;\n    return this.prisma.$transaction(async (tx) => {\n      const existing = await tx.timeEntry.findFirst({\n        where: { userId, taskId, isDismissal: true },\n        select: { id: true },\n      });\n      if (existing) {\n        return tx.timeEntry.update({\n          where: { id: existing.id },\n          data: { updatedAt: new Date() },\n          include: { task: true, project: true },\n        });\n      }",
      "description": "The comment at line 282 acknowledges that Prisma does not support upsert on a non-unique composite key. The $transaction wraps the findFirst+create, but under READ COMMITTED isolation (Postgres default), a $transaction does NOT prevent a second concurrent transaction from also reading null from findFirst and then attempting create. There is no partial unique index on (userId, taskId, isDismissal=true) in the schema (confirmed by the code comment). Both transactions therefore succeed, producing two dismissal rows for the same (userId, taskId). Business invariant broken: a task can then show as dismissed twice, and subsequent idempotent calls return different rows on each call.",
      "root_cause": "Logical uniqueness (userId, taskId, isDismissal=true) is not enforced at the DB level, and the transaction does not use SELECT FOR UPDATE or SERIALIZABLE isolation to block concurrent inserts.",
      "impact": "Concurrent dismissal requests for the same task produce duplicate rows in time_entry. Downstream queries counting or displaying dismissals see inflated results. The described idempotence guarantee (D9 in comments) is not actually enforced.",
      "suggested_fix": "Add a partial unique index in the Prisma schema:\n```\n@@unique([userId, taskId], map: \"uq_dismissal_user_task\", where: { isDismissal: true })\n```\nOr use a `prisma.$executeRaw` INSERT ON CONFLICT DO UPDATE to achieve true upsert semantics. The DB-level constraint is the only reliable guard under READ COMMITTED.",
      "acceptance_criteria": [
        "Two concurrent calls to upsertDismissal for the same (userId, taskId) result in exactly one row in time_entry with isDismissal=true.",
        "Both calls return without error (one creates, one updates or both succeed idempotently)."
      ],
      "verification_command": "grep -n 'isDismissal\\|partial\\|unique' packages/database/prisma/schema.prisma | grep -i 'timeentry\\|time_entry' || echo 'check schema for TimeEntry unique constraints'",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED. TimeEntry model in schema.prisma (lines 446-474) has only plain @@index entries, no @@unique and no partial unique index. The code comment at lines 281-283 ('Prisma ne supporte pas d\\'upsert sur clé composite non-unique') explicitly acknowledges the gap. The $transaction buys atomicity for the read-modify-write within one request, but not across concurrent requests under READ COMMITTED isolation."
    },
    {
      "id": "COR-038",
      "severity": "important",
      "category": "correctness",
      "subcategory": "TOCTOU / daily-cap race (acknowledged residual)",
      "title": "ensureDailyCapNotExceeded is non-transactional: concurrent same-day creates can both pass the 24h cap and collectively exceed it",
      "file": "apps/api/src/time-tracking/time-tracking.service.ts",
      "line": "84-120",
      "code_evidence": "  private async ensureDailyCapNotExceeded(\n    actor:\n      | { kind: 'user'; userId: string }\n      | { kind: 'thirdParty'; thirdPartyId: string },\n    date: Date,\n    newHours: number,\n    excludeEntryId?: string,\n  ): Promise<void> {\n    const startOfDay = new Date(\n      Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate()),\n    );\n    const endOfDay = new Date(startOfDay);\n    endOfDay.setUTCDate(endOfDay.getUTCDate() + 1);\n\n    const actorWhere =\n      actor.kind === 'user'\n        ? { userId: actor.userId }\n        : { thirdPartyId: actor.thirdPartyId };\n\n    const aggregate = await this.prisma.timeEntry.aggregate({",
      "description": "The daily-hours cap is enforced via aggregate→check→create, three separate DB roundtrips with no transaction. Two concurrent create requests for the same (userId, date) both read an aggregate sum below 24, both pass the check, and both commit rows that together exceed 24h. The code documents this as an accepted residual at line 203 ('The TOCTOU residual (non-transactional read-then-write race ... ) remains unaddressed') and line 80 ('Carries the same non-transactional TOCTOU residual'). The per-row CHECK (DAT-033, @Min(0.25)/@Max(24)) does not close the aggregate race.",
      "root_cause": "The aggregate check and the subsequent create are not wrapped in a serialisable transaction or backed by a DB-level CHECK constraint on the daily sum.",
      "impact": "Under concurrent load, a user can log more than 24h on a single day. Affects payroll/reporting accuracy. Bounded: the per-row @Max(24) limits how far over 24h any single race can push the total (max double would be 48h).",
      "suggested_fix": "The code acknowledges this residual. Definitive fix requires either: (a) wrapping aggregate+create in a SERIALIZABLE $transaction (performance impact), or (b) a DB-level trigger/constraint on the daily sum. A practical improvement is a $transaction with SELECT ... FOR UPDATE on a synthetic lock row keyed on (userId, date), preventing concurrent inserts for the same actor+day.",
      "acceptance_criteria": [
        "Two concurrent POST /time-tracking requests for the same user and date summing to > 24h result in exactly one accepted and one 400.",
        "Or: the total stored hours for the user on that date never exceeds 24h."
      ],
      "verification_command": "N/A — manual verification via concurrent load test",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "ADVERSARIAL REVIEW: CONFIRMED. Verbatim code verified at lines 84-120. The aggregate→check→create sequence has no wrapping transaction. The code comment at lines 79-82 explicitly documents this as an accepted TOCTOU residual. Reported for completeness; severity is important rather than blocking because the code owns the limitation."
    },
    {
      "id": "COR-039",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing-session-invalidation",
      "title": "users.service changePassword() and admin resetPassword() do not call jwtNotBefore.bumpUser() — live access tokens remain valid after password change for up to the access TTL",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1041-1046 and 1123-1129",
      "code_evidence": "    await this.prisma.user.update({\n      where: { id: userId },\n      data: { passwordHash: newPasswordHash, forcePasswordChange: false },\n    });\n\n    await this.refreshTokenService.revokeAllForUser(userId);",
      "description": "changePassword() (self-service) revokes all refresh tokens via revokeAllForUser() but does NOT call jwtNotBefore.bumpUser(userId). The admin path resetPassword() in users.service.ts (line 1129) has the same omission. By contrast, the token-based resetPassword() in auth.service.ts (line 585) correctly calls bumpUser(). Without bumping the per-user nbf, any access token minted before the password change remains fully valid until its natural expiry (up to 15 minutes, configurable). JwtStrategy.validate will admit these tokens because the nbf gate only fires if a nbf key exists for the user.",
      "root_cause": "The SEC-019 nbf invalidation pattern was applied to auth.service.ts resetPassword() but was not applied to the two password-mutation paths in users.service.ts.",
      "impact": "A user who changes their own password (or has it changed by an admin) still has up to 15 minutes of valid access via any pre-existing access token. An attacker holding a stolen access token for a user who changes their password retains access for that window. For admin-initiated resets (e.g., after account compromise), this gap is security-relevant.",
      "suggested_fix": "Add `await this.jwtNotBefore.bumpUser(userId);` in changePassword() after revokeAllForUser() (line ~1046), and the same in resetPassword() (users.service.ts) after revokeAllForUser() (line ~1129). JwtNotBeforeService is already injected into UsersService.",
      "acceptance_criteria": [
        "After changePassword(), an access token minted before the change is rejected by JwtStrategy.validate.",
        "After admin resetPassword(), an access token minted before the reset is rejected by JwtStrategy.validate."
      ],
      "verification_command": "grep -n 'bumpUser' apps/api/src/users/users.service.ts",
      "related_findings": [],
      "cluster_id": "N",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "jwtNotBefore is already injected at line 62. The fix is two one-line additions. Adversarial check: grep confirmed bumpUser only called at lines 781 and 826 (both for isActive true→false transitions in update() and remove() respectively). changePassword() (lines 1041-1067) and admin resetPassword() (lines 1123-1158) both end after revokeAllForUser() with no bumpUser call. auth.service.ts resetPassword() at line 585 correctly calls bumpUser — the asymmetry is confirmed."
    },
    {
      "id": "COR-040",
      "severity": "important",
      "category": "correctness",
      "subcategory": "missing-transaction / partial-failure",
      "title": "users.service update(): service-membership deleteMany+createMany execute outside the user.update() transaction — partial failure leaves the user with no services",
      "file": "apps/api/src/users/users.service.ts",
      "line": "620-676",
      "code_evidence": "    if (updateUserDto.serviceIds !== undefined) {\n      await this.prisma.userService.deleteMany({\n        where: { userId: id },\n      });\n\n      if (updateUserDto.serviceIds.length > 0) {\n        await this.prisma.userService.createMany({\n          data: updateUserDto.serviceIds.map((serviceId) => ({\n            userId: id,\n            serviceId,\n          })),\n        });\n      }\n    }\n\n    const user = await this.prisma.user.update({",
      "description": "When a PATCH /users/:id includes serviceIds, the existing UserService rows are deleted and new ones inserted BEFORE the main user.update() call. These three operations run as separate, non-transactional statements. If user.update() subsequently fails (e.g., a ConflictException on email/login, or a DB constraint), the UserService rows have already been wiped but the user row is unchanged, leaving the user with zero service memberships and no error to the caller's state matching. Similarly, if createMany fails after deleteMany succeeds, the deletion is permanent.",
      "root_cause": "Multi-write path (deleteMany + createMany + update) is not wrapped in a $transaction, violating ACID intent.",
      "impact": "A user can permanently lose all service memberships if any error occurs after deleteMany but before/during user.update(). Under concurrent load, a window exists where the user has no services at all — visible to any concurrent reader during that gap.",
      "suggested_fix": "Wrap the entire update block in a $transaction: move deleteMany, createMany, and user.update() inside a single prisma.$transaction(async (tx) => { ... }) call. Reference: refresh-token.service.ts rotate() already uses this pattern correctly.",
      "acceptance_criteria": [
        "If user.update() throws a ConflictException, UserService rows are rolled back to their pre-call state.",
        "No window exists where a user has zero services between deleteMany and createMany."
      ],
      "verification_command": "N/A — manual verification; inject a failing roleCode after valid serviceIds in a test to confirm rollback.",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Compare with hardDelete() which correctly uses $transaction for all owned-data deletions. Adversarial check: only one $transaction exists in users.service.ts (line 948, in hardDelete()). Pre-validation of serviceIds (lines 599-606) and email/login (lines 543-588) happens before deleteMany, but a concurrent constraint violation or unexpected DB error in user.update() (line 635) after deleteMany has committed would leave the user with no services."
    },
    {
      "id": "COR-041",
      "severity": "important",
      "category": "correctness",
      "subcategory": "inconsistent-auth-state-on-login-failure",
      "title": "Login/register: JWT stored in localStorage before permissions fetch — broken auth state on network error",
      "file": "apps/web/app/[locale]/login/page.tsx",
      "line": "27-43",
      "code_evidence": "    try {\n      const response = await authService.login(formData);\n      const permsRes = await api.get<{ permissions: string[] }>(\n        \"/auth/me/permissions\",\n      );\n      setAuth(response.user, permsRes.data.permissions);\n      toast.success(t(\"login.success\"));\n      router.push(`/${locale}/dashboard`);\n    } catch (err) {\n      const axiosError = err as { response?: { data?: { message?: string } } };\n      toast.error(\n        axiosError.response?.data?.message || t(\"login.errors.generic\"),\n      );\n    }",
      "description": "`authService.login()` persists the JWT to `localStorage` (`persistSession`) before `api.get('/auth/me/permissions')` is called. If the permissions request fails (transient network error, rate-limiting, server overload), the catch block shows a generic error toast, but the JWT is already stored. The user sees 'login failed' but is actually authenticated. On the next page load, `useAuthBootstrap` re-hydrates cleanly (it fetches both endpoints in parallel), so the broken state is self-healing — but only on reload. If the user clicks 'submit' again, `authService.login()` makes a second login call with the same credentials while already having a valid JWT, wasting a session. The same issue exists in `register/page.tsx` at lines 37-54.",
      "root_cause": "`persistSession` in `auth.service.ts` stores the token immediately on login response, before the secondary permissions fetch completes.",
      "impact": "Transient network errors on the permissions endpoint leave users with a JWT in localStorage but no in-memory auth state, causing a confusing UX (error toast + stay on login page despite valid session). Self-healing on reload, but users may try to log in again unnecessarily.",
      "suggested_fix": "In `auth.service.ts`, defer `localStorage.setItem(AUTH_TOKEN_KEY, ...)` until after the permissions fetch succeeds, OR in the login page handler, catch the permissions failure separately and fall back to `useAuthBootstrap`'s re-hydration rather than showing a generic error. The cleanest fix is to not call `/auth/me/permissions` in the login handler at all — `useAuthBootstrap` already does this on mount, so a `router.push` after login is sufficient; the dashboard layout's bootstrap will populate permissions.",
      "acceptance_criteria": [
        "If the server returns 200 on login but the permissions endpoint times out, the user is redirected to the dashboard and useAuthBootstrap fetches permissions successfully",
        "No JWT is stored if login itself fails (HTTP 401)"
      ],
      "verification_command": "N/A — manual verification: throttle /auth/me/permissions in devtools; confirm login still works",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed in login/page.tsx lines 27-43 and register/page.tsx lines 37-54. auth.service.ts:persistSession() at line 68 is called within authService.login() before the caller has a chance to fetch permissions. No guard elsewhere reverses this. Confirmed."
    },
    {
      "id": "COR-042",
      "severity": "important",
      "category": "correctness",
      "subcategory": "unhandled-error",
      "title": "handleUpdateProject has no error handling — silent failure on project update",
      "file": "apps/web/app/[locale]/projects/[id]/page.tsx",
      "line": "959-965",
      "code_evidence": "  // Project update handler\n  const handleUpdateProject = async (data: UpdateProjectDto) => {\n    await projectsService.update(projectId, data);\n    toast.success(t(\"messages.updateSuccess\"));\n    // Refresh project data\n    const projectData = await projectsService.getById(projectId);\n    setProject(projectData);\n  };",
      "description": "`handleUpdateProject` awaits two API calls (`projectsService.update` and `projectsService.getById`) with no try/catch. If either call throws (network error, 403, 422 validation), the promise rejects and the error propagates uncaught to the caller (ProjectEditModal's `onSave`). Since ProjectEditModal likely does not wrap this in its own try/catch (it calls `onSave` which it receives as a prop), the user sees no feedback and the modal may remain open or close silently depending on the modal's own error handling.",
      "root_cause": "No try/catch around async API calls in `handleUpdateProject`, unlike every other mutation handler in this file which all have try/catch blocks.",
      "impact": "When a project update fails (e.g. validation error, network outage), the user receives no error toast and the UI may be left in an inconsistent state (edit modal still open, stale project data).",
      "suggested_fix": "Wrap the body in try/catch matching the pattern used in `handleHardDeleteProject`:\n```ts\ntry {\n  await projectsService.update(projectId, data);\n  toast.success(t('messages.updateSuccess'));\n  const projectData = await projectsService.getById(projectId);\n  setProject(projectData);\n} catch (err) {\n  const axiosError = err as { response?: { data?: { message?: string } } };\n  toast.error(axiosError.response?.data?.message || t('messages.updateError'));\n  throw err; // re-throw so ProjectEditModal can keep modal open\n}\n```",
      "acceptance_criteria": [
        "A simulated 422 from projectsService.update triggers an error toast",
        "The edit modal remains open after a failed save"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Adversarial check: verified at lines 958-965. handleHardDeleteProject immediately below (line 968) uses try/catch — confirms the missing pattern is inconsistent. No higher-level global error handler wraps this call. Confirmed."
    },
    {
      "id": "COR-043",
      "severity": "important",
      "category": "correctness",
      "subcategory": "unhandled-error",
      "title": "onArchive / onUnarchive in projects/page.tsx have no error handling",
      "file": "apps/web/app/[locale]/projects/page.tsx",
      "line": "323-337",
      "code_evidence": "  const onArchive = async (id: string) => {\n    if (\n      !confirm(\n        \"Archiver ce projet ? Il n'apparaîtra plus dans le suivi général mais restera accessible.\",\n      )\n    )\n      return;\n    await projectsService.archive(id);\n    await fetchProjects();\n  };\n\n  const onUnarchive = async (id: string) => {\n    await projectsService.unarchive(id);\n    await fetchProjects();\n  };",
      "description": "Both `onArchive` and `onUnarchive` are async functions that await API calls (`projectsService.archive`, `projectsService.unarchive`) without any try/catch. On failure, the unhandled rejection propagates silently — the user receives no error feedback, and no UI update occurs (no toast, no re-render). This contrasts with all other mutation handlers in the same file which use toast.error on catch.",
      "root_cause": "Missing try/catch in async event handlers that perform network mutations.",
      "impact": "If archiving or unarchiving fails (network error, permission denied, server error), the user clicks the button and nothing happens with no feedback. The project list is also not refreshed if fetchProjects itself throws.",
      "suggested_fix": "Wrap each in try/catch with toast.error:\n```ts\nconst onArchive = async (id: string) => {\n  if (!confirm(...)) return;\n  try {\n    await projectsService.archive(id);\n    toast.success('Projet archivé');\n    await fetchProjects();\n  } catch (err) {\n    toast.error('Erreur lors de l\\'archivage');\n  }\n};\n```",
      "acceptance_criteria": [
        "A simulated API error on archive displays a toast.error",
        "A simulated API error on unarchive displays a toast.error"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Adversarial check: code verified verbatim at lines 323-337. No surrounding try/catch in callers. Confirmed."
    },
    {
      "id": "COR-044",
      "severity": "important",
      "category": "correctness",
      "subcategory": "stale-closure",
      "title": "fetchProjects useCallback missing hasPermission dependency — stale permission closure",
      "file": "apps/web/app/[locale]/projects/page.tsx",
      "line": "85-130",
      "code_evidence": "  const fetchProjects = useCallback(async () => {\n    try {\n      setLoading(true);\n      let projectsData: Project[] = [];\n\n      // Si ?member=me dans l'URL, on force le scope \"mes projets\" quel que\n      // soit le rôle (pour les CTA du Dashboard notamment).\n      if (memberMeFilter && user?.id) {\n        try {\n          projectsData = await projectsService.getByUser(user.id);\n        } catch (err) {\n          const axiosError = err as { response?: { status?: number } };\n          if (axiosError.response?.status !== 404) {\n            throw err;\n          }\n        }\n      } else if (hasPermission(\"projects:read\")) {",
      "description": "The useCallback at line 85 closes over `hasPermission` (line 101) but the dependency array at line 130 only lists `[user, memberMeFilter, showArchived]`. If the user's permissions change (e.g. after a session refresh or role change), `fetchProjects` will not be re-created and will keep using the stale closure over the old `hasPermission` function. The project fetch scope (getAll vs getByUser) depends entirely on the result of `hasPermission('projects:read')`, so a stale closure here will silently return wrong data.",
      "root_cause": "`hasPermission` is used inside a `useCallback` but omitted from its dependency array, causing the callback to close over a potentially stale reference to the permission-checking function.",
      "impact": "After a permission reload (e.g. role change or token refresh), the page continues to use the old permission check to decide which API call to make, potentially showing more or fewer projects than the user is entitled to see.",
      "suggested_fix": "Add `hasPermission` to the `useCallback` dependency array: `}, [user, memberMeFilter, showArchived, hasPermission]);`. Since `hasPermission` comes from `usePermissions()`, verify that the hook returns a stable reference (memoized with `useCallback`); if not, stabilize it at the hook level first.",
      "acceptance_criteria": [
        "ESLint react-hooks/exhaustive-deps rule passes without disable comments on this callback",
        "After a mock permission change, fetchProjects re-runs and returns the correct scope"
      ],
      "verification_command": "cd /home/alex/Documents/REPO/ORCHESTRA && npx eslint apps/web/app/\\[locale\\]/projects/page.tsx --rule '{\"react-hooks/exhaustive-deps\": \"error\"}' 2>&1 | grep fetchProjects",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Adversarial check: usePermissions already wraps hasPermission in useCallback([permissions]) — so hasPermission IS stable when permissions don't change. However, when permissions DO change (e.g. role swap), the new hasPermission reference is NOT in fetchProjects dep array, so the callback is never re-created. The stale closure is real. Same pattern in tasks/page.tsx — see frontend-F2-2."
    },
    {
      "id": "COR-045",
      "severity": "important",
      "category": "correctness",
      "subcategory": "locale/i18n routing bug",
      "title": "ProjectsDetailTable: Link href and router.push use locale-less paths, breaking client-side navigation",
      "file": "apps/web/app/[locale]/reports/components/ProjectsDetailTable.tsx",
      "line": "379-380, 428",
      "code_evidence": "                  <Link\n                    href={`/projects/${row.id}`}\n                    className=\"font-semibold text-gray-900 hover:text-blue-600 hover:underline inline-flex items-center gap-1.5\"\n                  >\n...\n                          router.push(`/tasks?projectId=${row.id}&overdue=true`)",
      "description": "The `ProjectsDetailTable` component navigates to `/projects/${row.id}` and `/tasks?projectId=${row.id}&overdue=true` without a locale prefix. The app uses Next.js + next-intl locale routing under `app/[locale]/`. All internal navigation links must be prefixed with `/${locale}/` (e.g., `/${locale}/projects/${row.id}`). next-intl middleware adds the locale prefix only on full page loads (SSR/navigation), not on client-side `router.push()` calls. The component does not import `useLocale`. By contrast, `reports/page.tsx` correctly uses `/${locale}/tasks?overdue=true` for the same pattern.",
      "root_cause": "The component uses standard `next/link` and `useRouter` without importing `useLocale` from `next-intl`, omitting the locale segment required by the `[locale]` app router structure.",
      "impact": "Clicking a project name link or the 'en retard' button navigates to a URL without locale (e.g., `/projects/abc` instead of `/fr/projects/abc`), causing a 404 or incorrect page load. Affects all users using the Reports > Overview tab.",
      "suggested_fix": "1. Add `import { useLocale } from 'next-intl'` and `const locale = useLocale()` inside `ProjectsDetailTable`. 2. Change `href={\\`/projects/${row.id}\\`}` to `href={\\`/${locale}/projects/${row.id}\\`}`. 3. Change `router.push(\\`/tasks?projectId=${row.id}&overdue=true\\`)` to `router.push(\\`/${locale}/tasks?projectId=${row.id}&overdue=true\\`)`.",
      "acceptance_criteria": [
        "Clicking a project name link navigates to /{locale}/projects/{id}",
        "Clicking 'en retard' button navigates to /{locale}/tasks?projectId={id}&overdue=true",
        "No 404 errors on either navigation"
      ],
      "verification_command": "grep -n 'useLocale\\|locale' apps/web/app/\\[locale\\]/reports/components/ProjectsDetailTable.tsx",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code evidence verified verbatim. No useLocale import present in ProjectsDetailTable.tsx. Routes only exist under [locale]/ (confirmed by directory scan) — /projects/abc has no corresponding page at app root. next-intl middleware (middleware.ts) uses createIntlMiddleware and only redirects on full-page requests, not client-side Link/router.push. PortfolioGantt.tsx line 72-78 confirmed to correctly use useLocale() and /${locale}/projects/${row.id}."
    },
    {
      "id": "COR-046",
      "severity": "important",
      "category": "correctness",
      "subcategory": "stale-closure",
      "title": "fetchData useCallback in tasks/page.tsx missing hasPermission dependency — stale permission closure",
      "file": "apps/web/app/[locale]/tasks/page.tsx",
      "line": "54-144",
      "code_evidence": "  const fetchData = useCallback(async () => {\n    try {\n      setLoading(true);\n\n      // Fetch projects (requires projects:read)\n      let projectsData: Project[] = [];\n      if (hasPermission(\"projects:read\")) {\n        try {\n          if (hasPermission(\"tasks:readAll\")) {\n            const response = await projectsService.getAll();\n            projectsData = Array.isArray(response.data) ? response.data : [];\n          } else if (user?.id) {\n            projectsData = await projectsService.getByUser(user.id);\n",
      "description": "`fetchData` uses `hasPermission` at lines 60, 62, 87, 104, and 117 to decide which API calls to make and which data sets to load, but `hasPermission` is absent from the `useCallback` dependency array `[user, assigneeMeFilter]` at line 144. This is the same stale-closure pattern as in projects/page.tsx (finding F2-1) but more severe here because `hasPermission` gates five different branches: projects:read, tasks:readAll, assignee-only, tasks:create (orphans), tasks:update+users:read.",
      "root_cause": "`hasPermission` is referenced inside `useCallback` but omitted from its deps, capturing the value from initial render and never updating if permissions change.",
      "impact": "Following a role change or permission reload, the tasks page will continue fetching data according to the old role's access level. An OBSERVATEUR promoted to ADMIN would still only see their own tasks until the page is fully remounted.",
      "suggested_fix": "Add `hasPermission` to the dependency array: `}, [user, assigneeMeFilter, hasPermission]);`. Ensure `usePermissions` returns a stable `hasPermission` reference.",
      "acceptance_criteria": [
        "ESLint react-hooks/exhaustive-deps passes on this callback without disable comment",
        "Permission change causes fetchData to re-run with the new permission set"
      ],
      "verification_command": "cd /home/alex/Documents/REPO/ORCHESTRA && npx eslint apps/web/app/\\[locale\\]/tasks/page.tsx --rule '{\"react-hooks/exhaustive-deps\": \"error\"}' 2>&1 | grep fetchData",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Adversarial check: usePermissions wraps hasPermission in useCallback([permissions]) — stable unless permissions array changes. When permissions DO change, fetchData dep array [user, assigneeMeFilter] does not include hasPermission, so the callback retains the stale closure. Confirmed real."
    },
    {
      "id": "COR-047",
      "severity": "important",
      "category": "correctness",
      "subcategory": "unhandled_rejection",
      "title": "usersService.getAll() in telework/page.tsx has no .catch() — silent failure in user selector",
      "file": "apps/web/app/[locale]/telework/page.tsx",
      "line": "395-404",
      "code_evidence": "  useEffect(() => {\n    if (!user) return;\n    setSelectedUserId(user.id);\n\n    if (canManageOthers && canListUsers) {\n      usersService.getAll().then((data) => {\n        setAllUsers(Array.isArray(data) ? data : []);\n      });\n    }\n  }, [user, canManageOthers, canListUsers]);",
      "description": "The `usersService.getAll()` promise has a `.then()` handler but no `.catch()`. If the request fails (network error, 403, 500), the rejection is unhandled. In modern browsers and Next.js, unhandled promise rejections in effects can produce console errors that reach the user-visible error reporter (via `logger.error` if it were called, but it isn't here). More critically, `allUsers` is never populated, so the manager's user-selector dropdown will be empty with no error feedback. The user has no way to know the fetch failed.",
      "root_cause": "Missing `.catch()` on a fire-and-forget async operation inside a useEffect.",
      "impact": "Managers (telework:manage_any) who encounter a transient API error will see an empty collaborator selector with no error toast. They cannot view or manage other users' telework calendars until they reload. No observability.",
      "suggested_fix": "Add a `.catch()` handler: `usersService.getAll().then(data => { setAllUsers(Array.isArray(data) ? data : []); }).catch(err => { logger.error('Error fetching users for telework:', err); toast.error(tc('errors.serverError')); });`",
      "acceptance_criteria": [
        "When usersService.getAll() rejects, a toast error is shown to the manager.",
        "logger.error is called with the error context.",
        "The selector renders empty with a recoverable state."
      ],
      "verification_command": "grep -n 'usersService.getAll' apps/web/app/\\[locale\\]/telework/page.tsx",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code is verbatim confirmed at lines 395-404. No .catch() is present anywhere on this promise chain."
    },
    {
      "id": "COR-048",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "dst_sensitivity",
      "title": "daysFromNow in MilestonesCompletionService uses fixed MS_PER_DAY, off-by-one across DST transitions",
      "file": "apps/api/src/analytics/advanced/services/milestones-completion.service.ts",
      "line": "20-98",
      "code_evidence": "const MS_PER_DAY = 1000 * 60 * 60 * 24;\n...\n      const dayMs = milestone.dueDate.getTime() - now.getTime();\n      const daysFromNow = Math.round(dayMs / MS_PER_DAY);",
      "description": "Dividing a millisecond delta by a fixed `MS_PER_DAY` (86400000 ms) is incorrect across DST boundaries. On the Europe/Paris clock (used by the scheduler) the spring-forward transition produces a 23-hour day and the fall-back transition a 25-hour day. A milestone due exactly at midnight on the day after DST change can produce `daysFromNow = 0` when it should be `1` (or vice-versa). `Math.round` mitigates but does not fully eliminate the issue for milestones due within the transition hour.",
      "root_cause": "Using a fixed millisecond constant for 'one day' ignores DST; the correct approach is calendar-aware date arithmetic (e.g. UTC midnight difference).",
      "impact": "Off-by-one on `daysFromNow` near DST transitions (twice a year). A milestone 1 day away could display as 0 (today/overdue) or 2, misleading users. Only affects the `details` array, not the aggregate counts.",
      "suggested_fix": "Compute `daysFromNow` using UTC midnight differences: `const nowMidnightUTC = Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate()); const dueMidnightUTC = Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()); const daysFromNow = Math.round((dueMidnightUTC - nowMidnightUTC) / MS_PER_DAY);`",
      "acceptance_criteria": [
        "daysFromNow is correct on both sides of a Europe/Paris DST boundary",
        "Unit test with mocked 'now' at a DST transition date verifies correct daysFromNow value"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Verbatim confirmed: MS_PER_DAY at line 20, daysFromNow computation at lines 97-98. Low blast radius — affects display only. The aggregate overdue/completed/upcoming counts use direct date comparison (milestone.dueDate < now) which is unaffected by this issue."
    },
    {
      "id": "COR-049",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "resource_leak",
      "title": "SnapshotSchedulerService creates a Redis connection in constructor with no shutdown hook",
      "file": "apps/api/src/analytics/advanced/snapshot-scheduler.service.ts",
      "line": "32-45",
      "code_evidence": "  constructor(\n    private readonly projectsService: ProjectsService,\n    private readonly configService: ConfigService,\n  ) {\n    const redisUrl = this.configService.get<string>('REDIS_URL');\n    if (redisUrl) {\n      this.redis = new Redis(redisUrl);\n    } else {\n      const host = this.configService.get<string>('REDIS_HOST', 'localhost');\n      const port = this.configService.get<number>('REDIS_PORT', 6379);\n      const password = this.configService.get<string>('REDIS_PASSWORD');\n      this.redis = new Redis({ host, port, password: password || undefined });\n    }\n  }",
      "description": "A new `ioredis` client is instantiated directly in the constructor, creating an additional Redis TCP connection on top of any existing CacheService or Bull connections. There is no `onApplicationShutdown()` or `onModuleDestroy()` lifecycle hook that calls `this.redis.disconnect()` or `this.redis.quit()`. During graceful shutdown or in test environments that create/destroy NestJS modules, the connection is left open, preventing the process from exiting cleanly. In tests, this can cause 'open handles' warnings and hang the test runner.",
      "root_cause": "Raw `ioredis` client is created in-constructor without registering a shutdown hook to close it; NestJS lifecycle cleanup is never invoked.",
      "impact": "Unclosed Redis connection on module teardown; process may not exit cleanly in prod restarts or in test suites, potentially causing port exhaustion under frequent restart scenarios.",
      "suggested_fix": "Implement `OnModuleDestroy` and call `await this.redis.quit()` in `onModuleDestroy()`. Alternatively, inject a shared Redis client via a NestJS provider so the connection lifecycle is managed centrally.",
      "acceptance_criteria": [
        "Jest/Vitest test runner exits without 'open handles' warning when SnapshotSchedulerService is included in test module",
        "Redis connection is closed on NestJS graceful shutdown"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Verbatim confirmed at lines 32-45. The service implements OnModuleInit (line 28, 47-51) but only for logging — no OnModuleDestroy or onApplicationShutdown. The redis field is typed as `private readonly redis: Redis` with no teardown. This is a correctness/resource-management issue rather than a functional bug. The cron job itself works correctly."
    },
    {
      "id": "COR-050",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "TOCTOU / error-mapping",
      "title": "auth.service register() and users.service create(): duplicate-check race condition surfaces as 500 instead of 409 when two concurrent registrations collide on the DB unique constraint",
      "file": "apps/api/src/auth/auth.service.ts",
      "line": "286-361",
      "code_evidence": "    const existingUser = await this.prisma.user.findFirst({\n      where: {\n        // DAT-015: case-insensitive lookup matches the LOWER() unique index\n        OR: [\n          { email: { equals: registerDto.email, mode: 'insensitive' } },\n          { login: { equals: registerDto.login, mode: 'insensitive' } },\n        ],\n      },\n    });\n\n    if (existingUser) {\n      // DAT-015: case-fold before comparing\n      if (\n        existingUser.email.toLowerCase() === registerDto.email.toLowerCase()\n      ) {\n        throw new ConflictException('Cet email est déjà utilisé');\n      }",
      "description": "The duplicate-email/login check is a read-then-write without a transaction. Two concurrent POST /auth/register requests with the same email can both pass the findFirst check (neither row exists yet), proceed to bcrypt.hash(), then reach user.create(). The second create() hits the DB-level LOWER() unique constraint and throws a Prisma PrismaClientKnownRequestError (P2002) which is not caught anywhere in register(). AllExceptionsFilter maps this to a 500 'Internal server error', instead of the expected 409. The same pattern exists in users.service.ts create() (same lines). The DB constraint prevents actual data corruption, but the HTTP response code is wrong.",
      "root_cause": "No try/catch around user.create() to translate Prisma P2002 unique-constraint errors into ConflictException.",
      "impact": "A concurrent duplicate registration returns 500 instead of 409. The client receives an opaque error and cannot distinguish 'email taken' from a real server error. The second user is NOT created (DB constraint holds), so no data corruption occurs.",
      "suggested_fix": "Wrap user.create() in a try/catch that catches PrismaClientKnownRequestError with code P2002 and re-throws as ConflictException. Same fix for users.service.ts create(). Example: `import { Prisma } from 'database'; catch (e) { if (e instanceof Prisma.PrismaClientKnownRequestError && e.code === 'P2002') throw new ConflictException('...'); throw e; }`",
      "acceptance_criteria": [
        "Two concurrent POST /auth/register with the same email: both return 409 (not one 201 and one 500).",
        "Prisma P2002 error is translated to ConflictException in register() and create()."
      ],
      "verification_command": "grep -n 'PrismaClientKnownRequestError\\|P2002' apps/api/src/auth/auth.service.ts apps/api/src/users/users.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Same pattern in users.service.ts create() at lines 94-116. The fix is the same in both places. Adversarial check: grep confirmed zero P2002 or PrismaClientKnownRequestError references in auth.service.ts or users.service.ts. Other services (departments, leaves, holidays) do handle P2002 correctly — the omission is specific to these two files. AllExceptionsFilter confirmed: non-HttpException → opaque 500 with no P2002 translation."
    },
    {
      "id": "COR-051",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "missing existence validation",
      "title": "listProjectClients, listTaskAssignees, listProjectMembers return empty list for non-existent parent — should return 404",
      "file": "apps/api/src/clients/clients.service.ts",
      "line": "290-298",
      "code_evidence": "  async listProjectClients(projectId: string) {\n    return this.prisma.projectClient.findMany({\n      where: { projectId },\n      include: {\n        client: true,\n      },\n      orderBy: { createdAt: 'asc' },\n    });\n  }",
      "description": "`listProjectClients` (clients.service.ts:290), `listTaskAssignees` (third-parties.service.ts:310), and `listProjectMembers` (third-parties.service.ts:323) all skip any existence check on the parent resource. If the `projectId` or `taskId` does not exist, Prisma returns an empty array. The API then returns HTTP 200 with `[]`, which is misleading — a caller cannot distinguish 'the project exists but has no clients' from 'the project does not exist at all'. This violates the REST contract implied by a nested resource route (`/projects/:projectId/clients`).",
      "root_cause": "No `findUnique` or `count` check on the parent entity before querying the child join table.",
      "impact": "Callers (including tests and frontend) cannot detect non-existent parent IDs via these endpoints. Integration bugs can be masked by silent 200+[] responses.",
      "suggested_fix": "Before calling `findMany`, verify the parent exists with `prisma.project.count({ where: { id: projectId } })` and throw NotFoundException if zero. Similarly for task in listTaskAssignees.",
      "acceptance_criteria": [
        "GET /projects/:projectId/clients with non-existent projectId returns 404",
        "GET /tasks/:taskId/third-party-assignees with non-existent taskId returns 404",
        "GET /projects/:projectId/third-party-members with non-existent projectId returns 404"
      ],
      "verification_command": "grep -n 'listProjectClients\\|listTaskAssignees\\|listProjectMembers' apps/api/src/third-parties/third-parties.service.ts apps/api/src/clients/clients.service.ts",
      "adversarial_check": "Verified: (1) listProjectClients at lines 290-298 verbatim — no parent existence check. (2) listTaskAssignees at lines 310-321 and listProjectMembers at lines 323-334 confirmed — both only do findMany with no parent check. (3) Contrast with getClientProjects which does check client existence first. Finding stands.",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "The same list methods in the same codebase for getClientProjects (line 119) DO correctly check existence first."
    },
    {
      "id": "COR-052",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "epics.service update()/remove(): double-read TOCTOU — epic fetched in membership check then again in findOne()",
      "file": "apps/api/src/epics/epics.service.ts",
      "line": "78-99",
      "code_evidence": "    if (currentUserId) {\n      await this.assertProjectMembership(id, currentUserId, currentUserRole);\n    }\n    await this.findOne(id);\n    return this.prisma.epic.update({\n      where: { id },\n      data: updateEpicDto,\n      include: { project: { select: { id: true, name: true } } },\n    });",
      "description": "The `update()` and `remove()` methods each make 3 separate DB calls: (1) `assertProjectMembership` fetches the epic (with project+members), (2) `findOne` fetches the epic again, (3) the actual update/delete. Between calls 1 and 3, another actor could delete the epic, causing the final update/delete to fail silently or with an unhandled P2025 Prisma error. The P2025 'Record not found' from `prisma.epic.update()` where the row was deleted between the access check and the update would surface as an unhandled internal server error rather than a clean 404.",
      "root_cause": "Authorization check, existence check, and mutation are three separate non-transactional operations with a TOCTOU window.",
      "impact": "A concurrent deletion between the membership/existence checks and the actual update/delete produces an unhandled Prisma P2025 error instead of a clean 404 NotFoundException, resulting in a 500 internal server error.",
      "suggested_fix": "Catch P2025 Prisma errors in update/delete and convert to NotFoundException. Alternatively, consolidate into a single transaction that reads and writes atomically. The membership check could be done once, and the findOne result reused instead of re-fetching.",
      "acceptance_criteria": [
        "If an epic is deleted by another actor between the membership check and the update, the API returns 404, not 500"
      ],
      "verification_command": "grep -n 'findOne\\|assertProjectMembership\\|epic.update\\|epic.delete' apps/api/src/epics/epics.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED. Code verbatim at lines 78-99. AllExceptionsFilter (all-exceptions.filter.ts lines 57-68) passes HttpException through and returns 500 for all other errors — no P2025 to 404 mapping exists anywhere in the codebase. The race window is real; severity remains nit because concurrent epic deletion between these two reads is rare in practice."
    },
    {
      "id": "COR-053",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business_invariant — recurrenceEndDate prune outside transaction",
      "title": "update() recurrenceEndDate child-prune runs outside the update transaction, creating a window where pruned children exist alongside a committed new endDate",
      "file": "apps/api/src/events/events.service.ts",
      "line": "543-557",
      "code_evidence": "    // Si recurrenceEndDate est mise à jour sur un événement parent récurrent,\n    // supprimer les enfants dont la date dépasse la nouvelle date de fin\n    if (\n      updateEventDto.recurrenceEndDate &&\n      existingEvent.isRecurring &&\n      !existingEvent.parentEventId\n    ) {\n      const newEndDate = new Date(updateEventDto.recurrenceEndDate);\n      await this.prisma.event.deleteMany({\n        where: {\n          parentEventId: id,\n          date: { gt: newEndDate },\n        },\n      });\n    }",
      "description": "The parent event is updated inside `$transaction` (lines 480–535) which commits before this code runs. The child-prune `deleteMany` at lines 551–556 executes as a separate independent write. If the server crashes or is killed between the transaction commit and this write, the DB is left with a parent whose `recurrenceEndDate` has been shortened but whose out-of-range children still exist. Additionally, shortening an end date prunes children but extending it never regenerates them, which may be intentional by design but is undocumented.",
      "root_cause": "Child pruning is implemented as a post-transaction side-effect rather than being included inside the $transaction scope at lines 480–535.",
      "impact": "On crash between the transaction commit and the deleteMany, stale child events persist beyond the new recurrenceEndDate. The invariant 'no child.date > parent.recurrenceEndDate' is temporarily violated. Planners relying on the planning view may see ghost occurrences after a recurrence is shortened.",
      "suggested_fix": "Move the child `deleteMany` inside the `$transaction` callback (before or after the `tx.event.update`) so all changes are atomic.",
      "acceptance_criteria": [
        "Simulated crash between update and deleteMany leaves no stale children after recovery",
        "The child prune and parent update either both commit or both roll back"
      ],
      "verification_command": "grep -n 'deleteMany\\|\\$transaction' apps/api/src/events/events.service.ts",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Extending recurrenceEndDate never regenerates children; this is a separate limitation (not filed as a separate finding — insufficient confidence that this is unintentional vs. out-of-scope). Adversarial check: $transaction closes at line 535 (closing brace of async tx callback + .catch handler). The deleteMany at lines 551-556 executes via `this.prisma.event.deleteMany` (not `tx.event.deleteMany`), confirming it is outside the transaction scope. Confirmed."
    },
    {
      "id": "COR-054",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race_condition — TOCTOU on unique constraint",
      "title": "addParticipant uses check-then-create pattern susceptible to race; duplicate concurrent request leaks P2002 as HTTP 500",
      "file": "apps/api/src/events/events.service.ts",
      "line": "762-783",
      "code_evidence": "    const existingParticipation = await this.prisma.eventParticipant.findUnique(\n      {\n        where: {\n          eventId_userId: {\n            eventId,\n            userId,\n          },\n        },\n      },\n    );\n\n    if (existingParticipation) {\n      throw new BadRequestException('Cet utilisateur est déjà participant');\n    }\n\n    // Ajouter le participant\n    await this.prisma.eventParticipant.create({\n      data: {\n        eventId,\n        userId,\n      },\n    });",
      "description": "Two concurrent requests to add the same (eventId, userId) pair both pass the findUnique existence check before either write commits. The second create hits the DB-level unique constraint `eventId_userId` and Prisma raises P2002, which is unhandled and surfaces as HTTP 500 instead of the intended 400 BadRequestException. DB integrity is maintained; only the error code is wrong. The `update()` path avoids this by wrapping inside a transaction.",
      "root_cause": "Check-then-act pattern without unique-constraint error handling; concurrent requests can both pass the guard before the DB serializes them.",
      "impact": "Under concurrent load (unlikely but possible), a duplicate-add request returns 500 instead of 400, misleading clients into treating a constraint violation as a server fault.",
      "suggested_fix": "Wrap the create in a try/catch and map Prisma P2002 on `eventId_userId` to `BadRequestException('Cet utilisateur est déjà participant')`, or use `createMany` with `skipDuplicates: true`.",
      "acceptance_criteria": [
        "Concurrent duplicate addParticipant calls both return 400 (not 500)",
        "Unit test: mock prisma.eventParticipant.create to throw a Prisma P2002 error; service throws BadRequestException"
      ],
      "verification_command": "grep -n 'P2002\\|skipDuplicates\\|existingParticipation' apps/api/src/events/events.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "DB integrity is safe due to the unique constraint. Only the error response code is affected in the concurrent scenario. Adversarial check: AllExceptionsFilter (apps/api/src/common/filters/all-exceptions.filter.ts) maps all non-HttpException errors to HTTP 500 with opaque 'Internal server error' message (line 71-101). P2002 is a PrismaClientKnownRequestError (not an HttpException), so it will return 500. No P2002 catch in addParticipant(). Other services (holidays, departments, leaves, clients) explicitly catch P2002 — this service does not. Confirmed."
    },
    {
      "id": "COR-055",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "inconsistent_error_reporting",
      "title": "importLeaves catch block resets result.created=0 but not result.skipped — error response reports misleading skipped count after full tx rollback",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "3627-3637",
      "code_evidence": "    } catch (error) {\n      // A leave.create failure (or any unrecoverable DB error) aborts the\n      // entire $transaction. Record a single error for the whole batch.\n      result.created = 0;\n      result.errors++;\n      // COR-037 — translate the DB exclusion-constraint violation to a clean message.\n      const message = isLeaveOverlapViolation(error)\n        ? 'Chevauchement détecté avec un congé approuvé existant'\n        : (error as Error).message || 'Erreur inconnue';\n      result.errorDetails.push(`Import annulé: ${message}`);\n    }",
      "description": "When the $transaction rolls back due to a DB error, `result.created` is reset to 0 (correctly — no rows were persisted). However, `result.skipped` is NOT reset. Skipped rows are those that failed validation before reaching `tx.leave.create` (bad email, unknown leave type, bad dates, overlap). Since the entire transaction rolls back, no rows were persisted — but `result.skipped` retains any non-zero count accumulated before the error. The API caller receives a response like `{created:0, skipped:3, errors:1}` which implies 3 rows were intentionally skipped, when in reality the skipped rows were also 'not imported' because of the rollback. The caller might retry only the 'error' row, not realising they need to re-submit the entire file.",
      "root_cause": "The catch block zeroes `result.created` but does not reset `result.skipped` to match the rolled-back reality.",
      "impact": "Misleading API response: callers may believe some rows were deliberately skipped (validation failure) when the entire batch was atomically aborted. Could cause callers to re-import only a subset of the original file, leaving data incomplete.",
      "suggested_fix": "Add `result.skipped = 0;` in the catch block alongside `result.created = 0;`, and clear `result.errorDetails` before pushing the single 'Import annulé' message (to avoid mixing pre-rollback row-level skip details with the global abort message).",
      "acceptance_criteria": [
        "A batch with 2 skipped rows + 1 DB error returns {created:0, skipped:0, errors:1} after catch",
        "The errorDetails array contains only the 'Import annulé' message, not the per-skip messages"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Whether errorDetails should also be cleared depends on desired UX — keeping row-level skip details can help the user fix their file even after a rollback. Adversarial check: result.skipped++ increments confirmed inside the $transaction callback at lines 3478, 3490, 3502, 3508, 3524, 3542. Catch block at lines 3627-3637 only resets result.created. Code verbatim confirmed."
    },
    {
      "id": "COR-056",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race-condition",
      "title": "milestones.service update()/remove(): same double-read TOCTOU as epics — unhandled P2025 on concurrent delete",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "102-135",
      "code_evidence": "    if (currentUserId) {\n      await this.assertProjectMembership(id, currentUserId, currentUserRole);\n    }\n    await this.findOne(id);\n    const { dueDate, ...data } = updateMilestoneDto;\n\n    return this.prisma.milestone.update({\n      where: { id },\n      data: {\n        ...data,\n        ...(dueDate && { dueDate: new Date(dueDate) }),\n      },\n      include: { project: { select: { id: true, name: true } } },\n    });",
      "description": "Same triple-fetch TOCTOU pattern as epics.service.ts: assertProjectMembership fetches the milestone (with project+members), findOne fetches it again, then update/delete runs. A concurrent delete between calls 1 and 3 would cause prisma.milestone.update() or delete() to throw an unhandled P2025, surfacing as a 500 instead of a 404.",
      "root_cause": "Authorization check, existence check, and mutation are three separate non-transactional operations with a TOCTOU window.",
      "impact": "A concurrent deletion between checks and the actual mutation produces an unhandled Prisma P2025 error instead of a clean 404, resulting in a 500 internal server error.",
      "suggested_fix": "Same as correctness-S2-5: catch P2025 and map to NotFoundException, or consolidate the reads.",
      "acceptance_criteria": [
        "If a milestone is deleted by another actor between the membership check and the update, the API returns 404, not 500"
      ],
      "verification_command": "grep -n 'findOne\\|assertProjectMembership\\|milestone.update\\|milestone.delete' apps/api/src/milestones/milestones.service.ts",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "adversarial_verdict": "CONFIRMED. Code verbatim at lines 102-135. Same AllExceptionsFilter analysis applies — no P2025 to 404 mapping. complete() at lines 162-168 also has a double-fetch (findOne then update) without P2025 handling."
    },
    {
      "id": "COR-057",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "milestones complete() bypasses project membership authorization check",
      "file": "apps/api/src/milestones/milestones.service.ts",
      "line": "162-168",
      "code_evidence": "  async complete(id: string) {\n    await this.findOne(id);\n    return this.prisma.milestone.update({\n      where: { id },\n      data: { status: MilestoneStatus.COMPLETED },\n    });\n  }",
      "description": "The `complete()` method does not perform an `assertProjectMembership` check, unlike `update()` and `remove()`. Any authenticated user with the `milestones:update` permission (required at the controller level) can mark any milestone as COMPLETED regardless of whether they are a member of the project that owns the milestone. The `update()` and `remove()` methods both enforce membership; `complete()` should do the same.",
      "root_cause": "The membership guard was added to update() and remove() but not to complete(), creating an inconsistency in authorization enforcement.",
      "impact": "A user in project A with milestones:update permission can complete milestones belonging to project B, even if they have no membership in project B.",
      "suggested_fix": "Add the same pattern as update()/remove(): accept currentUserId and currentUserRole parameters, call assertProjectMembership() before the update. Also update the controller to pass the current user's id and role code.",
      "acceptance_criteria": [
        "POST /milestones/:id/complete by a user not in the milestone's project returns 403",
        "POST /milestones/:id/complete by a member of the project returns 200"
      ],
      "verification_command": "grep -n 'complete\\|assertProjectMembership' apps/api/src/milestones/milestones.service.ts",
      "related_findings": [],
      "cluster_id": "A",
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED. Controller line 116: `complete(@Param('id', ParseUUIDPipe) id: string)` — no @CurrentUser injected, no user context passed to service. Service complete() at lines 162-168 has no assertProjectMembership call. update() and remove() both enforce membership. The authorization bypass is real and consistent across service and controller layers."
    },
    {
      "id": "COR-058",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "captureSnapshots() uses hardcoded string literal 'ACTIVE' instead of ProjectStatus enum",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1147",
      "code_evidence": "    const projects = await this.prisma.project.findMany({\n      where: { status: 'ACTIVE' },",
      "description": "The `captureSnapshots` method uses the raw string `'ACTIVE'` instead of the `ProjectStatus.ACTIVE` enum imported at line 21. While Prisma accepts strings for enum fields, this bypasses TypeScript's compile-time enum safety: if `ProjectStatus.ACTIVE` were ever renamed or restructured, this hardcoded string would silently stop matching any projects, capturing zero snapshots forever with no type error. The rest of the codebase consistently uses `ProjectStatus.*` enum values for status comparisons.",
      "root_cause": "Direct string literal used instead of the imported `ProjectStatus` enum.",
      "impact": "Silent snapshot collection failure if the enum value changes; inconsistency with codebase convention; TypeScript type safety not leveraged.",
      "suggested_fix": "Replace `{ status: 'ACTIVE' }` with `{ status: ProjectStatus.ACTIVE }`. The `ProjectStatus` enum is already imported at line 21.",
      "acceptance_criteria": [
        "captureSnapshots() uses ProjectStatus.ACTIVE in the where clause",
        "No raw string 'ACTIVE' for status filtering in projects.service.ts"
      ],
      "verification_command": "grep -n \"status: 'ACTIVE'\" apps/api/src/projects/projects.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "adversarial_verdict": "CONFIRMED. Line 1147 verbatim: `where: { status: 'ACTIVE' }`. ProjectStatus enum imported at line 21 as `import { ProjectStatus, TaskStatus } from 'database'`. All other status references in the file use ProjectStatus.* (lines 122, 568, 683). This is a genuine inconsistency with no compensating mechanism."
    },
    {
      "id": "COR-059",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race-condition / non-atomic",
      "title": "roles.service createRole()/updateRole(): isDefault singleton management is non-atomic — two concurrent requests can create two roles with isDefault=true",
      "file": "apps/api/src/rbac/roles.service.ts",
      "line": "139-157",
      "code_evidence": "    if (dto.isDefault) {\n      if (caller) {\n        prevDefaultRoleId = await this.captureCurrentDefaultId();\n      }\n      await this.unsetCurrentDefault();\n    }\n\n    const created = await this.prisma.role.create({\n      data: {\n        code: dto.code,\n        label: dto.label,\n        templateKey: dto.templateKey,\n        description: dto.description ?? null,\n        isSystem: false,\n        isDefault: dto.isDefault ?? false,\n      },",
      "description": "The isDefault singleton (at most one role with isDefault=true) is managed via a two-step: (1) updateMany({isDefault:true}, {isDefault:false}) to unset the current default, then (2) create/update the new one with isDefault:true. These are non-transactional. Two concurrent createRole(isDefault:true) requests can both read the existing default, both call unsetCurrentDefault(), then both proceed to create with isDefault:true — leaving two rows with isDefault=true. The same race exists in updateRole(). There is no DB-level UNIQUE partial index enforcing the singleton.",
      "root_cause": "unsetCurrentDefault() + role.create/update are not wrapped in a $transaction, and there is no database-level unique constraint on isDefault=true.",
      "impact": "AuthService.register() uses findFirst({where:{isDefault:true}}) as the first choice for the default role. With two default roles, the result is non-deterministic (whichever Prisma returns first). This is a low-frequency race (only admin role management), but the invariant is not enforced at any layer.",
      "suggested_fix": "Wrap unsetCurrentDefault() + create/update in a $transaction. Additionally, consider adding a partial unique index in schema.prisma: @@unique([isDefault]) WHERE isDefault = true (PostgreSQL partial index). This enforces the singleton at the DB level regardless of application-layer races.",
      "acceptance_criteria": [
        "Two concurrent POST /roles with isDefault:true results in exactly one role with isDefault=true.",
        "The second request either sees the first's commit and overwrites it, or receives a constraint error."
      ],
      "verification_command": "N/A — manual verification via concurrent HTTP test.",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Low exploitability (requires concurrent admin requests to /roles). Marked nit. The partial index fix would be the most robust mitigation. Adversarial check: schema.prisma confirmed Role model has @@index([isDefault]) (an index, not a unique constraint) — no DB-level singleton enforcement. Code evidence verbatim at lines 139-157."
    },
    {
      "id": "COR-060",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "non_atomic_bulk_write",
      "title": "SettingsService.bulkUpdate performs sequential un-transacted upserts — partial failure leaves inconsistent state",
      "file": "apps/api/src/settings/settings.service.ts",
      "line": "270-279",
      "code_evidence": "  async bulkUpdate(settings: Record<string, unknown>) {\n    const results: ParsedSetting[] = [];\n\n    for (const [key, value] of Object.entries(settings)) {\n      const result = await this.update(key, value);\n      results.push(result);\n    }\n\n    return results;\n  }",
      "description": "Each iteration calls `this.update(key, value)` sequentially with an individual `prisma.appSettings.upsert()`. If the N-th key triggers a `BadRequestException` (unknown key) or a DB error, keys 1..N-1 are already committed while keys N..end are not applied. The caller receives an error but has no way to know which keys were applied. This violates the expected atomicity of a 'bulk update' endpoint.",
      "root_cause": "No `$transaction` wrapper around the loop; each `upsert` is committed immediately with no rollback on partial failure.",
      "impact": "A bulk update with a mix of valid and invalid keys silently persists partial state changes. For example, updating `appName` and `invalidKey` in one call would persist `appName` and fail on `invalidKey`, leaving the settings in an inconsistent partially-updated state. Severity is reduced because the key allowlist blocks truly unknown keys, but a DB error mid-loop still causes the same issue.",
      "suggested_fix": "Wrap the loop in a Prisma interactive transaction (`this.prisma.$transaction(async (tx) => { ... })`) so all-or-nothing semantics are enforced. The inner `update()` should accept an optional `tx` context.",
      "acceptance_criteria": [
        "POST /settings/bulk with [validKey, invalidKey] does not persist validKey if the call as a whole fails",
        "POST /settings/bulk with all valid keys persists all updates atomically"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "B",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Verbatim confirmed at lines 270-279. Currently the ALLOWED_KEYS guard in update() makes the invalid-key path the most likely trigger of partial failure."
    },
    {
      "id": "COR-061",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "unguarded_delete",
      "title": "SettingsService.remove throws unhandled Prisma P2025 for non-existent non-default keys",
      "file": "apps/api/src/settings/settings.service.ts",
      "line": "310-321",
      "code_evidence": "  async remove(key: string) {\n    // Ne pas supprimer les paramètres par défaut\n    if (DEFAULT_SETTINGS[key]) {\n      return this.resetToDefault(key);\n    }\n\n    await this.prisma.appSettings.delete({\n      where: { key },\n    });\n\n    return { message: 'Paramètre supprimé' };\n  }",
      "description": "When `remove(key)` is called with a key that is neither in `DEFAULT_SETTINGS` nor stored in the database, `prisma.appSettings.delete()` throws Prisma error P2025 (`An operation failed because it depends on one or more records that were required but not found`). This error is unhandled and bubbles up as an uncaught exception, resulting in a generic 500 response rather than an appropriate 404. Because `update()` already validates `isKnownKey()`, only unknown (non-default, non-stored) keys can reach this code path, but the controller does not pre-validate the key before calling `remove()`.",
      "root_cause": "`prisma.delete()` throws on missing record; the method does not check existence before deleting and does not catch the P2025 error.",
      "impact": "DELETE /settings/:key with an unknown key returns 500 instead of 404, hiding the root cause and potentially masking application errors in monitoring.",
      "suggested_fix": "Add existence check before delete, or catch PrismaClientKnownRequestError with code P2025 and throw NotFoundException. Example:\n```ts\nconst existing = await this.prisma.appSettings.findUnique({ where: { key } });\nif (!existing) throw new NotFoundException(`Setting '${key}' not found`);\nawait this.prisma.appSettings.delete({ where: { key } });\n```",
      "acceptance_criteria": [
        "DELETE /settings/nonexistent-key returns 404 with a descriptive message",
        "DELETE /settings/:known-key still returns 200 and deletes the record"
      ],
      "verification_command": "curl -s -o /dev/null -w '%{http_code}' -X DELETE http://localhost:3000/api/settings/nonexistent-key -H 'Authorization: Bearer <token>'",
      "related_findings": [],
      "cluster_id": "C",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Verbatim confirmed at lines 310-321. Controller (settings.controller.ts line 108) calls settingsService.remove(key) with no pre-validation. The ALLOWED_KEYS guard lives only in update(), not in remove(). No global Prisma exception filter was found to convert P2025 to 404."
    },
    {
      "id": "COR-062",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "pagination",
      "title": "findAll: default limit is 1000 but page default is 1 — no hard cap when limit param is omitted via service default",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "305",
      "code_evidence": "    const safeLimit = Math.min(limit || 1000, 1000);",
      "description": "When limit is not provided (undefined), `limit || 1000` evaluates to 1000 and safeLimit is capped at 1000. This is intentional but means any call to findAll() without a limit returns up to 1000 tasks. Combined with the fact that the controller does not enforce a default limit (it passes undefined if not provided), a caller can retrieve 1000 tasks in one request. The findForPlanningOverview path applies a hard cap of 500 but findAll does not have such a comment explaining the 1000 choice. This is a minor concern — not incorrect per se, but the inconsistency between 500 (planning) and 1000 (findAll) warrants documenting.",
      "root_cause": "The two query paths use different hard caps without explanation; findAll cap of 1000 is implicit default, not a named constant.",
      "impact": "Large result sets on findAll can cause performance issues in large deployments; inconsistent cap between endpoints.",
      "suggested_fix": "Define a named constant TASKS_HARD_CAP = 1000 (analogous to PLANNING_HARD_CAP = 500), document why different caps exist, and ensure the default is applied at the controller layer too.",
      "acceptance_criteria": [
        "GET /tasks without limit param returns at most 1000 results",
        "The cap constant is documented"
      ],
      "verification_command": "grep -n 'PLANNING_HARD_CAP\\|safeLimit\\|Math.min' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": "D",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "comments/comments.service.ts has the same pattern with default 1000.",
      "adversarial_check": "Confirmed. Line 305 verified verbatim: `const safeLimit = Math.min(limit || 1000, 1000);`. The cap is enforced (1000 max), but the inconsistency with the 500 planning cap and the implicit default behavior are real."
    },
    {
      "id": "COR-063",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "business-invariant",
      "title": "update(): progress not recalculated when status changes and subtasks exist",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "797-800",
      "code_evidence": "          ...(taskData.status &&\n            (await tx.subtask.count({ where: { taskId: id } })) === 0 && {\n              progress: getTaskProgress(taskData.status),\n            }),",
      "description": "When a task has subtasks, update() deliberately skips updating progress (the condition `=== 0` is false). This is consistent with the design choice that progress is driven by subtask completion when subtasks exist. However, if the task has subtasks AND the caller explicitly sets `status: DONE`, the task is marked as DONE but progress remains at whatever partial value it had. A task can therefore be DONE at 40% progress which is contradictory. recalcTaskProgress() handles the subtask path, but it is only called from subtask create/update/delete — not from task status update.",
      "root_cause": "Progress update when status changes is skipped entirely if subtasks exist; status=DONE with partial subtask completion creates an inconsistent state.",
      "impact": "Task can appear as DONE with progress < 100% in reports and dashboards; inconsistent data for any analytics that combines status + progress.",
      "suggested_fix": "When status is set to DONE, override progress to 100 regardless of subtask count, OR prevent setting DONE when not all subtasks are completed. Add a check: `if (taskData.status === TaskStatus.DONE) { progress = 100; }` and apply unconditionally.",
      "acceptance_criteria": [
        "Setting a task with subtasks to status=DONE results in progress=100",
        "Setting status=IN_PROGRESS with subtasks keeps progress driven by subtask completion"
      ],
      "verification_command": "grep -n 'recalcTaskProgress\\|getTaskProgress\\|progress' apps/api/src/tasks/tasks.service.ts | head -20",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "correctness",
      "confidence": "high",
      "notes": "recalcTaskProgress at line 2063 exits early if subtasks.length === 0, so it would not fix this if called from update().",
      "adversarial_check": "Confirmed. Lines 797-800 verified: progress only updated when subtask count === 0. recalcTaskProgress (line 2063-2074) is not called from update(). task_progress.helper.ts maps DONE→100, but this path is bypassed when subtasks exist. Status=DONE with partial subtasks creates inconsistency."
    },
    {
      "id": "COR-064",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "date-time / timezone",
      "title": "getUsersPresence(): setHours(0,0,0,0) uses local server timezone — presence window is offset when Node.js process timezone differs from UTC",
      "file": "apps/api/src/users/users.service.ts",
      "line": "1680-1684",
      "code_evidence": "    const targetDate = dateStr ? new Date(dateStr) : new Date();\n    const startOfDay = new Date(targetDate);\n    startOfDay.setHours(0, 0, 0, 0);\n    const endOfDay = new Date(targetDate);\n    endOfDay.setHours(23, 59, 59, 999);",
      "description": "setHours() operates in the local timezone of the Node.js process. When a client sends dateStr='2026-06-04', new Date('2026-06-04') parses as UTC midnight. Then setHours(0,0,0,0) converts to LOCAL midnight — which may differ from UTC midnight if the container TZ differs from UTC. The resulting timestamps are then passed to PostgreSQL (which stores in UTC) as Prisma tagged-template literals. If the Node process and PostgreSQL are both UTC this is a no-op, but there is no TZ=UTC enforcement in Docker or the runtime. MEMORY.md states 'Prod=UTC' for the DB, but the Node process TZ is not pinned.",
      "root_cause": "Using setHours() (local-time) instead of UTC-equivalent operations (setUTCHours or explicit UTC string construction) for day boundary computation.",
      "impact": "In a non-UTC deployment, the presence report covers the wrong calendar day — e.g., a UTC+1 server would show presence for 23:00 previous day to 23:00 target day rather than 00:00–24:00 target day. Affects all callers of GET /users/presence.",
      "suggested_fix": "Replace setHours(0,0,0,0) with setUTCHours(0,0,0,0) and setHours(23,59,59,999) with setUTCHours(23,59,59,999). Alternatively, construct the boundaries from the date string directly: `new Date(dateStr + 'T00:00:00.000Z')` and `new Date(dateStr + 'T23:59:59.999Z')`.",
      "acceptance_criteria": [
        "When the Node.js process TZ is set to Europe/Paris, GET /users/presence?date=2026-06-04 returns the same result as when TZ=UTC.",
        "PostgreSQL receives timestamps that represent UTC midnight of the requested date."
      ],
      "verification_command": "TZ=Europe/Paris node -e \"const d=new Date('2026-06-04'); d.setHours(0,0,0,0); console.log(d.toISOString());\"",
      "related_findings": [],
      "cluster_id": "J",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Low practical impact if the Docker container always runs TZ=UTC (common default), but not enforced in code — marked nit rather than blocking. Adversarial check: neither docker-compose.yml nor docker-compose.prod.yml set TZ environment variable for the API container, confirming no enforcement. Code evidence verbatim at lines 1680-1684."
    },
    {
      "id": "COR-065",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "duplicate-permissions-fetch",
      "title": "Login/register pages fetch `/auth/me/permissions` redundantly — useAuthBootstrap does this on every mount",
      "file": "apps/web/app/[locale]/login/page.tsx",
      "line": "29-31",
      "code_evidence": "      const permsRes = await api.get<{ permissions: string[] }>(\n        \"/auth/me/permissions\",\n      );\n      setAuth(response.user, permsRes.data.permissions);",
      "description": "Both `login/page.tsx` and `register/page.tsx` fetch `/auth/me/permissions` after a successful login/register, then call `setAuth`. However, the main layout's `useAuthBootstrap` hook does the exact same thing on mount (fetching `/auth/me` + `/auth/me/permissions` in parallel). After `router.push` redirects to the dashboard, `useAuthBootstrap` fires again and overwrites the in-memory state. The explicit permissions fetch in the login handler is therefore redundant — it adds latency and an extra network request for no benefit.",
      "root_cause": "The login flow was built before `useAuthBootstrap` was in place (or as a defensive duplicate), and the redundancy was never cleaned up.",
      "impact": "Two network requests instead of one on each login/register. Minor performance impact.",
      "suggested_fix": "Remove the `api.get('/auth/me/permissions')` call and the `setAuth` call from `login/page.tsx` and `register/page.tsx`. Just call `router.push(\\`/${locale}/dashboard\\`)` after `authService.login()` succeeds; `useAuthBootstrap` in the target layout will handle permission loading.",
      "acceptance_criteria": [
        "Login redirects to dashboard without making a second /auth/me/permissions call",
        "Dashboard loads correctly with permissions populated from useAuthBootstrap"
      ],
      "verification_command": "N/A — manual verification via network tab in devtools",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed. useAuthBootstrap (src/hooks/useAuthBootstrap.ts:48-53) fetches both /auth/me and /auth/me/permissions in parallel on every mount via Promise.all. The login page also fetches /auth/me/permissions at line 29-31. Confirmed redundancy."
    },
    {
      "id": "COR-066",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "dead-ui-element",
      "title": "Profile 'Preferences' tab has a Save button with no onClick handler — button does nothing",
      "file": "apps/web/app/[locale]/profile/page.tsx",
      "line": "519-523",
      "code_evidence": "              <div className=\"pt-6\">\n                <button className=\"px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition\">\n                  {t(\"preferences.save\")}\n                </button>\n              </div>",
      "description": "The 'Sauvegarder' button in the Preferences tab has no `onClick` handler and no `type=\"submit\"` inside a form. Clicking it does nothing. The language/theme preferences are already applied immediately on select (via `router.push` and `setTheme`), so the button is visually misleading — it implies unsaved state but is a no-op.",
      "root_cause": "The save button was added as a UI scaffold but the corresponding save handler (e.g. `usersService.update` or a settings API call) was never wired up.",
      "impact": "UX confusion: users may believe their preferences are not saved because they never clicked 'Save', when in fact they are applied immediately. Or they may believe clicking 'Save' does something important.",
      "suggested_fix": "Either remove the button (since preferences apply immediately) or wire it to an actual save API call if server-side preference persistence is intended. If keeping, at minimum add a comment explaining the button is intentionally a no-op.",
      "acceptance_criteria": [
        "Either the Save button is removed from the Preferences tab, OR it has an onClick handler that calls an API endpoint",
        "No button exists in the UI that has no effect when clicked"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code evidence verbatim confirmed at profile/page.tsx lines 519-523. Button has no onClick, no type=submit, not inside a form. Confirmed."
    },
    {
      "id": "COR-067",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "stale-closure",
      "title": "fetchThirdPartyMembers and fetchProjectClients close over canRead* flags but effect deps are suppressed",
      "file": "apps/web/app/[locale]/projects/[id]/page.tsx",
      "line": "843-875",
      "code_evidence": "  useEffect(() => {\n    if (activeTab === \"thirdParties\") {\n      fetchThirdPartyMembers();\n    }\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [activeTab, projectId]);\n\n  const fetchProjectClients = async () => {\n    if (!canReadClients) return;\n    try {\n      const data = await clientsService.listProjectClients(projectId);\n      setProjectClients(data);\n    } catch (err) {\n      logger.error(\"Error loading project clients:\", err);\n    }\n  };\n\n  useEffect(() => {\n    if (activeTab === \"clients\") {\n      fetchProjectClients();\n    }\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [activeTab, projectId]);",
      "description": "Both useEffect hooks for tab-driven data fetching use `eslint-disable-next-line react-hooks/exhaustive-deps` to suppress the exhaustive-deps rule. The functions `fetchThirdPartyMembers` and `fetchProjectClients` close over `canReadThirdParties`/`canReadClients` and `projectId`, which change only on initial load in practice. However, the pattern hides potential staleness if permissions reload. Additionally, defining async helper functions outside useCallback and referencing them inside effects is an anti-pattern that can lead to stale captures as the component evolves.",
      "root_cause": "Helper functions were defined as plain async functions outside useCallback, making ESLint require them as deps. The `disable` comment was used instead of refactoring to useCallback or inlining the logic.",
      "impact": "Low in practice (permissions rarely change mid-session), but the eslint-disable comment hides any future staleness issues introduced by extending these functions.",
      "suggested_fix": "Either wrap `fetchThirdPartyMembers` and `fetchProjectClients` in `useCallback` with the correct deps, or inline the logic directly inside the effects and remove the eslint-disable comments.",
      "acceptance_criteria": [
        "No eslint-disable-next-line react-hooks/exhaustive-deps comment on these two effects",
        "Third-party and clients tabs still fetch data correctly on tab switch"
      ],
      "verification_command": "grep -n 'eslint-disable-next-line react-hooks/exhaustive-deps' apps/web/app/\\[locale\\]/projects/\\[id\\]/page.tsx",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Adversarial check: both eslint-disable-next-line comments confirmed at lines 857 and 874. canReadThirdParties/canReadClients are derived from hasPermission() at component render time (lines 838-839) — they're boolean values captured by closure, not stable refs. Impact is low since permissions rarely change mid-session, but the disable comment is a real code smell. Confidence kept at medium."
    },
    {
      "id": "COR-068",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "stale closure / dead code",
      "title": "reports/page.tsx: canView = !permissionsLoaded || hasPermission('reports:view') — the !permissionsLoaded branch is dead code",
      "file": "apps/web/app/[locale]/reports/page.tsx",
      "line": "49",
      "code_evidence": "  const canView = !permissionsLoaded || hasPermission(\"reports:view\");",
      "description": "The `AuthProvider` (apps/web/src/components/AuthProvider.tsx line 63) renders a loading spinner and returns early if `!permissionsLoaded`. This means the reports page only ever renders when `permissionsLoaded === true`. Therefore `!permissionsLoaded` is always `false` by the time this line executes, and `canView` always equals `hasPermission('reports:view')`. The intent appears to have been 'allow access while loading' to avoid a flash of the unauthorized message, but the AuthProvider already handles this at a higher level. The dead code creates a confusing false impression that the permission check is bypassed during loading.",
      "root_cause": "The developer added `!permissionsLoaded` as a pessimistic guard without accounting for the AuthProvider's own loading gate above in the tree.",
      "impact": "No security impact (the condition is never true in practice). Code readability and maintainability concern — a future developer might remove the AuthProvider loading gate without noticing this implicit dependency.",
      "suggested_fix": "Simplify to `const canView = hasPermission('reports:view')`. If the previous intent was to avoid loading flash, it is already handled by AuthProvider.",
      "acceptance_criteria": [
        "reports/page.tsx uses const canView = hasPermission('reports:view') with no !permissionsLoaded branch",
        "No flash of unauthorized message visible in normal page load"
      ],
      "verification_command": "grep -n 'canView' apps/web/app/\\[locale\\]/reports/page.tsx",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "high",
      "notes": "Code evidence verified verbatim at line 49. AuthProvider.tsx line 63 confirmed: `if (!ready || isLoading || (isAuthenticated && !permissionsLoaded))` returns loading spinner before children render. AuthProvider is in root layout.tsx (app/layout.tsx line 42), wrapping all pages. The permission guard in reports/page.tsx at line 164 already checks `if (permissionsLoaded && !canView)` — consistent with the finding that !permissionsLoaded in canView is dead code."
    },
    {
      "id": "COR-069",
      "severity": "nit",
      "category": "correctness",
      "subcategory": "race condition in role filter re-fetch",
      "title": "UsersPage: role filter useEffect fires a redundant re-fetch on initial mount when roleFilter='' and availableRoles just loaded",
      "file": "apps/web/app/[locale]/users/page.tsx",
      "line": "190-195",
      "code_evidence": "  useEffect(() => {\n    // Re-fetch when the role filter changes (skip very first mount — handled above).\n    if (availableRoles.length === 0) return;\n    fetchUsers(roleFilter);\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [roleFilter]);",
      "description": "The guard `if (availableRoles.length === 0) return` is designed to skip the first mount. However, `availableRoles` is populated inside `fetchUsers()` (via `setAvailableRoles(roles)`). When `fetchUsers` completes on mount, `availableRoles` goes from [] to non-empty. This does NOT re-trigger the `roleFilter` effect (because `roleFilter` did not change). The guard is therefore effective. However, if the user sets a roleFilter BEFORE the first fetch completes (unlikely UI race), the guard fires `return` and the filter is silently ignored until the next filter change. The `eslint-disable-next-line react-hooks/exhaustive-deps` comment suppresses the missing `fetchUsers` dependency warning, masking the real coupling.",
      "root_cause": "Two interleaved effects (mount effect + filter effect) share mutable state (`availableRoles`) without proper coordination.",
      "impact": "Minor: in very fast networks, the filter applied before mount fetch completes is silently dropped. No data corruption. The user must re-apply the filter.",
      "suggested_fix": "Pass `roleFilter` as a parameter to the initial `fetchUsers` call at mount, remove the secondary effect entirely, and handle filter changes by calling `fetchUsers(roleFilter)` directly in the filter change handler.",
      "acceptance_criteria": [
        "Setting a role filter before data loads correctly applies the filter",
        "No double fetch on initial page load"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [],
      "cluster_id": "M",
      "found_by": "correctness",
      "confidence": "medium",
      "notes": "Code evidence verified verbatim at lines 190-195. The eslint-disable comment is present. In practice this race is unlikely to be hit. Severity nit."
    }
  ],
  "verified_clean": [
    "API proxy route.ts uses process.env.API_URL — not user-controlled, no SSRF via URL manipulation",
    "AdvancedAnalyticsTab sub-components (WorkloadChart, RecentActivity, MilestonesCompletion): correct TanStack Query usage with staleTime",
    "All DTOs: class-validator decorators match business constraints (MaxLength, MinLength, IsEnum, IsUUID, IsOptional used correctly)",
    "All pages use 'use client' directive correctly — no attempted server-side secret access",
    "All services use Axios (api instance) with Bearer token from localStorage — no cookie-based auth mixing issues",
    "Auth decisions (canEdit/canDelete) are not computed client-side: role/permissions come from server via useAuthBootstrap",
    "Auth decisions (canEditProject, canDeleteProject, canEdit, canCreateProject, canCreateTask) are all derived from hasPermission() which reads from the RBAC permission store — no client-side role string comparison",
    "AuthProvider guards all pages against unauthenticated access and blocks render until permissionsLoaded=true",
    "Blob URL for template download (leaves/page.tsx:524) correctly calls URL.revokeObjectURL() immediately after click",
    "CSP is correctly applied to response headers in production only (dev correctly skipped for HMR)",
    "CSP nonce: pages use no inline scripts or style injection that would require nonce handling",
    "DTOs: CreateTeleworkDto — @IsDateString() validates format; userId is optional with @IsOptional().",
    "DTOs: CreateTimeEntryDto — @Min(0.25)/@Max(24) with @ValidateIf(!isDismissal) correctly allows hours=0 for dismissals.",
    "DTOs: GenerateSchedulesDto — both startDate and endDate required via @IsNotEmpty().",
    "GanttChart is lazy-loaded with dynamic() and ssr:false — correct for a heavy client-side component",
    "ImportPreviewModal: no XSS risk; all preview data rendered as text nodes via JSX",
    "JWT/access_token localStorage storage is intentional per CLAUDE.md (not flagged)",
    "Locale/i18n routing: useLocale() used consistently for locale prefix; no manual locale string hardcoding",
    "Next.js catch-all [...path] route normalizes path segments at the router level, mitigating naive ../ traversal",
    "No XSS via CSS injection — leaveType.color from API is used directly in style prop (backgroundColor, color) without HTML interpolation; CSS property value injection has no code-execution path in React",
    "No XSS via dangerouslySetInnerHTML anywhere in scanned scope",
    "No barrel import performance issues in the scanned service files (each imports directly from @/lib/api)",
    "No console.* calls with PII — all files use the centralized logger with PII scrubbing (logger.ts)",
    "No console.log with PII: all debug logging goes through the logger utility imported from @/lib/logger",
    "No console.log/console.error with PII in scanned files (replaced by structured logger)",
    "No console.log/warn/debug calls with PII in scanned pages or components",
    "No dangerouslySetInnerHTML usage found in any scanned file",
    "No dangerouslySetInnerHTML usage in any of the 8 scanned files",
    "No dangerouslySetInnerHTML usage in any scanned file",
    "No hardcoded role constants (Role.ADMIN, Role.MANAGER) in client-side auth decisions — all RBAC checks use hasPermission() from usePermissions hook",
    "No localStorage or sessionStorage access for tokens in any scanned file",
    "No open redirect (no user-controlled string passed directly to router.push/replace without validation beyond the ProjectsDetailTable locale bug which is not exploitable for external redirects)",
    "No open redirect vulnerabilities — all router.push() calls use hardcoded locale-prefixed paths",
    "No open redirect: all router.push() calls use locale-prefixed internal paths constructed from controlled variables",
    "No open redirect: login and register pages hardcode /${locale}/dashboard post-login; no searchParams redirect parameter",
    "No secrets or API keys embedded in client bundles in scanned files",
    "No secrets or API tokens hardcoded in client-side source files",
    "No secrets or tokens leaked to client bundle (no API keys, JWT_SECRET, etc. in component files)",
    "No secrets, tokens, or credentials exposed to the client bundle in any scanned file",
    "No unbounded list renders in the scanned auth/profile pages",
    "Numeric precision: MAX_SAFE_INTEGER (9007199254740991) in witness payload is exactly representable as IEEE-754 double; no precision loss on JSONB round-trip",
    "Optimistic updates: no optimistic update pattern is used — all mutations await the API call before updating state (safe but slower)",
    "Password reset token is read from URL searchParams (not localStorage) — no persistence risk",
    "PlanningView is lazy-loaded with dynamic() and ssr:false — correct for a calendar/planning component",
    "QuickTimeEntryInput: properly validates hours before submitting; no SSRF or injection risk",
    "SkillsMatrix: no XSS risk; all user data is text-rendered via JSX, not innerHTML",
    "SuiviPage fetches user data before performing access check and stops all downstream fetches if access is denied",
    "TanStack Query is not used in scanned pages (they use direct service calls or Zustand) — no staleTime issues",
    "TaskCard correctly uses useLocale and prefixes all navigation with /${locale}/",
    "Test files exist for leaves (TST-023) and telework covering RBAC affordances",
    "UUID chain tiebreak: UUID (random) tiebreak does not break chain consistency — write path reads DESC, recompute walks ASC; both orderings are symmetric for same-timestamp pairs",
    "UserAvatar uses Next.js Image component with auth-gated blob URLs for uploaded avatars (SEC-016 addressed)",
    "Zustand auth store does not persist role/permissions to localStorage (only minimal display cache)",
    "_dev/gantt-base/page.tsx: dev page uses logger.info correctly, no production exposure",
    "apps/api/src/analytics/advanced/analytics-advanced.controller.ts — all endpoints require reports:view permission; currentUser correctly extracted",
    "apps/api/src/analytics/advanced/dto/ — all DTO files use class-validator decorators correctly; no missing validation",
    "apps/api/src/analytics/advanced/services/milestones-completion.service.ts — aggregate counts (completed, overdue, upcoming) correctly use direct Date comparison, not the DST-sensitive daysFromNow calculation",
    "apps/api/src/analytics/advanced/services/project-health.service.ts — computeHealth logic: endDate comparison uses current Date() instance, consistent within the call",
    "apps/api/src/analytics/advanced/services/project-health.service.ts — progressPct from snapshot: Number() cast from Decimal is correct",
    "apps/api/src/analytics/advanced/services/recent-activity.service.ts — UTC bucket alignment (Date.UTC) is correct and DST-safe for bucket boundaries",
    "apps/api/src/analytics/advanced/services/recent-activity.service.ts — updatedAt used as completedAt proxy is documented and consistent",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — UTC day grouping via toISOString().slice(0,10) is DST-safe for aggregation purposes",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — per-project dedup correctly keeps latest-timestamp snapshot per UTC day",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — portfolioAverage is mean of all snapshots (not mean-of-means), consistent with spec comment",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — query.from/to date parsing via new Date(string) is safe; ISO strings validated by IsDateString decorator",
    "apps/api/src/analytics/advanced/services/tasks-breakdown.service.ts — exhaustive if/else chain over Priority and TaskStatus enums; no missing cases given the 4/5 enum values",
    "apps/api/src/analytics/advanced/services/workload.service.ts — UNION DISTINCT deduplication via Map<taskId> correctly handles tasks in both assigneeId and taskAssignments relations",
    "apps/api/src/analytics/advanced/services/workload.service.ts — limit applied after in-memory sort, consistent behaviour",
    "apps/api/src/analytics/advanced/snapshot-scheduler.service.ts — Redis SET NX EX leader-lock correctly prevents duplicate runs; fail-open semantics documented",
    "apps/api/src/analytics/analytics.controller.ts — role access correctly delegated to RequirePermissions; currentUser properly passed to service",
    "apps/api/src/analytics/analytics.service.ts — Promise.all ordering is safe (independent queries); getProjectDetails Promise.all safe (independent groupBys)",
    "apps/api/src/analytics/analytics.service.ts — cache key is user-scoped (userId in key), preventing cross-user cache poisoning",
    "apps/api/src/analytics/analytics.service.ts — getActiveUsers: hasProjectScope guard correctly prevents unbounded user scan when scope is empty",
    "apps/api/src/analytics/analytics.service.ts — overdue task date comparison uses new Date() consistently within a single request; no TZ-sensitive Date arithmetic (plain comparison of JS Date objects)",
    "apps/api/src/analytics/analytics.service.ts — progress computation: division-by-zero guarded (total === 0 ? 0 : ...)",
    "apps/api/src/analytics/analytics.service.ts — timeEntry hours use Number() cast from Prisma Decimal; no floating-point precision issue beyond normal JS float",
    "apps/api/src/analytics/dto/analytics-query.dto.ts — input validation via class-validator decorators; projectId validated as UUID",
    "apps/api/src/clients/clients.service.ts — assignClientToProject() correctly validates project + client before insert",
    "apps/api/src/clients/clients.service.ts — create() correctly maps P2002 to ConflictException",
    "apps/api/src/clients/clients.service.ts — findAll() uses $transaction for consistent count + data read",
    "apps/api/src/clients/clients.service.ts — getClientProjects() correctly validates client existence before fetching",
    "apps/api/src/clients/clients.service.ts — removeClientFromProject() uses findUnique before delete, no TOCTOU for delete (find-verify-delete with unique-key WHERE is race-safe at the delete step)",
    "apps/api/src/clients/dto/create-client.dto.ts — name has @Length(1,255), @IsString, @Transform trim",
    "apps/api/src/clients/dto/query-clients.dto.ts — pagination defaults, @Min(1)/@Max(1000) enforced",
    "apps/api/src/comments/comments.controller.ts — no business logic; routing only",
    "apps/api/src/comments/comments.service.ts create() — double-reads task then checks scope, harmless ordering; no missing error path",
    "apps/api/src/comments/comments.service.ts remove() — correctly checks author or elevated permission",
    "apps/api/src/common/services/access-scope.service.ts — canAccessProject() count-based check is safe for read-only access evaluation",
    "apps/api/src/common/services/access-scope.service.ts — documentReadWhere() correctly scopes to uploader or project member",
    "apps/api/src/documents/documents.service.ts — audit log in findOne is fire-and-forget with .catch() — intentional and correctly documented",
    "apps/api/src/documents/documents.service.ts — create() correctly checks assertCanAccessProject before insert",
    "apps/api/src/documents/documents.service.ts — findAll() correctly filters deletedAt: null",
    "apps/api/src/documents/documents.service.ts — findAll() uses Promise.all correctly (read-only parallel — no ordering concern)",
    "apps/api/src/documents/documents.service.ts — remove() correctly calls findOne() first (which checks deletedAt), then soft-deletes",
    "apps/api/src/documents/dto/create-document.dto.ts — URL scheme allowlist (http/https only, require_tld)",
    "apps/api/src/documents/dto/create-document.dto.ts — mimeType @IsIn allowlist enforced",
    "apps/api/src/epics/epics.service.ts — assertProjectMembership(): correctly handles null role via getPermissionsForRole",
    "apps/api/src/epics/epics.service.ts — findAll(): Promise.all for count+data is correct",
    "apps/api/src/metrics/metrics.controller.ts — METRICS_TOKEN check uses strict equality; no timing-attack risk for internal monitoring token",
    "apps/api/src/metrics/metrics.interceptor.ts — catchError correctly re-throws original error after recording; metrics recording failure is swallowed without affecting response",
    "apps/api/src/metrics/metrics.service.ts — Prometheus text output correctly emits _count and _sum for summary type",
    "apps/api/src/metrics/metrics.service.ts — in-process Map counters are correctly accumulated; no concurrency issue in single-threaded Node.js event loop",
    "apps/api/src/milestones/milestones.service.ts — create(): status hardcoded to MilestoneStatus.PENDING is correct business logic",
    "apps/api/src/milestones/milestones.service.ts — exportProjectMilestonesCsv(): audit emission is fire-and-forget (emitDataExported), consistent with documented pattern",
    "apps/api/src/milestones/milestones.service.ts — findAll(): Promise.all for count+data is correct",
    "apps/api/src/milestones/milestones.service.ts — validateImport(): pre-fetches all existing names before loop (correct TOCTOU avoidance for dry-run)",
    "apps/api/src/predefined-tasks/occurrence-generator.ts — pure function, UTC-anchored throughout, DST-safe, WEEKLY interval math verified",
    "apps/api/src/predefined-tasks/predefined-tasks.controller.ts — routing only, no business logic",
    "apps/api/src/predefined-tasks/predefined-tasks.service.ts bulkCreateRecurringRules() — wrapped in $transaction, correct",
    "apps/api/src/predefined-tasks/predefined-tasks.service.ts generateFromRules() — P2002 (duplicate) correctly skipped, other errors re-thrown",
    "apps/api/src/projects/dto/add-member.dto.ts — allocation validated 0-100 with @Min/@Max",
    "apps/api/src/projects/dto/archived-filter.dto.ts — archivedWhere() switch is exhaustive with default",
    "apps/api/src/projects/projects.service.ts — Promise.all in findAll() (lines 284-354) and getProjectStats() (lines 1322-1347): ordering is correct, results used independently, no partial-failure inconsistency",
    "apps/api/src/projects/projects.service.ts — archive()/unarchive(): correctly wrapped in $transaction (lines 705-723 and 743-759)",
    "apps/api/src/projects/projects.service.ts — assertProjectOwnershipOrBypass(): throws ForbiddenException when user is null/undefined",
    "apps/api/src/projects/projects.service.ts — checkProjectDependencies(): Promise.all is correct (lines 779-784), counts are independent",
    "apps/api/src/projects/projects.service.ts — create(): correctly wrapped in $transaction (lines 115-211)",
    "apps/api/src/projects/projects.service.ts — getProjectStats(): sevenDaysLater computation is correct (7 * 24 * 60 * 60 * 1000 ms)",
    "apps/api/src/projects/projects.service.ts — pagination: safeLimit = Math.min(limit || 1000, 1000), no off-by-one, totalPages uses Math.ceil",
    "apps/api/src/projects/projects.service.ts — removeMember(): existence check before delete, correct error handling",
    "apps/api/src/projects/projects.service.ts — update() with clientIds: correctly wrapped in $transaction (lines 623-648)",
    "apps/api/src/settings/settings.controller.ts — PUT :key JSON.parse in controller: fall-through to raw string is correct and consistent with service",
    "apps/api/src/settings/settings.service.ts — ALLOWED_KEYS guard in update() prevents injection of unknown setting keys",
    "apps/api/src/settings/settings.service.ts — findByCategory: AppSettingsCategory enum check prevents invalid enum comparison reaching Postgres",
    "apps/api/src/settings/settings.service.ts — onModuleInit error is caught and logged; startup does not fail if settings table is absent (migration not yet run)",
    "apps/api/src/settings/settings.service.ts — parseValue catch returns raw string on JSON parse failure; no exception leaks",
    "apps/api/src/tasks/dto/create-recurring-rule.dto.ts — IsValidRecurrenceConfig cross-field constraint logic is correct",
    "apps/api/src/tasks/dto/create-task.dto.ts — ProjectRequiredWhenParentedConstraint validated, logic correct",
    "apps/api/src/tasks/dto/import-tasks.dto.ts — DTOs are data containers, no logic",
    "apps/api/src/tasks/task-progress.helper.ts — simple enum map, no async, no edge cases",
    "apps/api/src/tasks/tasks.controller.ts — passes currentUser correctly; no business logic beyond routing",
    "apps/api/src/tasks/tasks.service.ts checkCircularDependency() — BFS is correct, visited set prevents infinite loop on existing cycles",
    "apps/api/src/third-parties/third-parties.service.ts — assertAssignedToTaskOrProject() logic correct for task+project membership check",
    "apps/api/src/third-parties/third-parties.service.ts — assignToTask() validates task existence + assertExistsAndActive before insert",
    "apps/api/src/third-parties/third-parties.service.ts — attachToProject() validates project existence + assertExistsAndActive before insert",
    "apps/api/src/third-parties/third-parties.service.ts — findAll() uses $transaction for consistent count + data",
    "apps/api/src/third-parties/third-parties.service.ts — hardDelete() relies on FK cascade — no pre-delete guard needed (cascade documented in comment)",
    "apps/api/src/third-parties/third-parties.service.ts — unassignFromTask() / detachFromProject() use findUnique check before delete",
    "audit/audit-persistence.service.ts: DAT-006 outerTx path acquires advisory lock on caller's tx — correctly scoped to outer transaction commit",
    "audit/audit-persistence.service.ts: createdAt set before transaction open but included in hash — intentional; both stored and hashed values are identical at write time",
    "audit/audit-persistence.service.ts: pg_advisory_xact_lock serialises all inserts — correct total-order guarantee for hash chain",
    "audit/audit-persistence.service.ts: prevHash read uses ORDER BY createdAt DESC, id DESC — consistent with recompute ASC ordering for same-timestamp rows",
    "audit/audit-persistence.service.ts: validatePayloadForAction called BEFORE transaction opens — correctly rejects malformed payloads without holding the advisory lock",
    "audit/audit.service.ts: bcrypt-shape guard returns 'unknown' before hashing — correct credential-in-log prevention",
    "audit/audit.service.ts: hashAttemptedSubject throws if AUDIT_HASH_KEY missing — correct defence-in-depth, reject-not-fallback pattern",
    "audit/audit.service.ts: resolveEntityId correctly routes LOGIN_FAILURE/ACCOUNT_LOCKED to HMAC, others to targetId/userId/'unknown' — correct",
    "audit/payload-schemas.ts: .strict() rejects unknown top-level keys — correct gate for payload predictability",
    "audit/payload-schemas.ts: AUDIT_PAYLOAD_SCHEMAS satisfies Record<AuditAction, ZodTypeAny> — exhaustiveness enforced at compile time via compile-witness",
    "audit/payload-schemas.ts: LEAVE_APPROVED union(leaveAudit, securityEnvelope) — correctly handles dual-provenance writes",
    "audit/recompute-chain.ts: in-transaction verification re-reads and re-hashes before commit — correct catch of walk logic errors",
    "audit/recompute-chain.ts: predecessor-untouched assertion prevents off-by-one rewriting sealed prefix rows — correct safety guard",
    "audit/recompute-chain.ts: tuple comparison (createdAt, id) ordering consistent between write (DESC) and recompute (ASC) for same-timestamp tiebreak — verified consistent",
    "auth.service.ts logout() redirects to bare /login (handled by next-intl middleware locale routing) — not a user-controlled URL",
    "auth/auth.controller.ts: dual-name cookie read for SEC-014 transition; clearRefreshCookie on logout; jti blacklist only called when user.jti and user.exp are present",
    "auth/auth.service.ts generateResetToken(): hierarchy check via roleHierarchy.assertCanAssignRole(); token stored hashed; cleartext only exposed in dev",
    "auth/auth.service.ts login(): auditService.log is fire-and-forget (not awaited) — acceptable for non-critical audit sink; refresh token issued after access token",
    "auth/auth.service.ts validateUser(): disabled-account check runs AFTER bcrypt to prevent timing-based account enumeration (SEC-005)",
    "auth/guards/force-password-change.guard.ts: reads live DB flag from request.user (not stale token claim); correctly allows @AllowPasswordChange routes",
    "auth/guards/jwt-auth.guard.ts: isPublic check correctly bypasses for @Public routes",
    "auth/guards/throttler-behind-proxy.guard.ts: uses clientIp() (req.ip) not req.ips[0]",
    "auth/jwt-blacklist.service.ts: fail-closed on Redis error for both read and write; TTL correctly uses remaining lifetime",
    "auth/jwt-not-before.service.ts: correctly stores nbf in seconds (floor(now/1000)+1) to guard same-second tokens; TTL margin errs long",
    "auth/login-lockout.service.ts: progressive lockout; fail-open on Redis error (documented intentional tradeoff); keyId hashes avoid raw PII in Redis",
    "auth/refresh-token.service.ts: rotate() uses Serializable $transaction for TOCTOU-safe token rotation; reuse detection implemented",
    "auth/strategies/jwt.strategy.ts: nbf/iat comparison uses seconds correctly; fail-closed blacklist check before fail-open nbf check; user.isActive enforced",
    "canEdit and canDelete flags in leaves/page.tsx (lines 757, 767) correctly use API-returned flags from the leave object, not client-side computation",
    "change-password/page.tsx correctly redirects to /${locale}/login after success (not to arbitrary URL)",
    "clients/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "clients/page.tsx and third-parties/page.tsx permission checks (canCreate, canUpdate, canDelete) correctly use hasPermission() hook",
    "common/fastify/cors.config.ts: production boot assertion enforced if CORS_ORIGIN is absent",
    "common/fastify/trust-proxy.config.ts: trusts loopback+uniquelocal only; clientIp() uses req.ip (Fastify semantics)",
    "common/fastify/uploads-auth.hook.ts: OPTIONS preflight allowed through to avoid blocking CORS preflight; only Bearer token accepted",
    "common/filters/all-exceptions.filter.ts: non-HttpException maps to opaque 500; stack trace never sent to client",
    "common/services/access-scope.service.ts: ADMIN template bypass for canManageUser; userReadWhere returns id=__no_access__ sentinel for unauthenticated user",
    "common/services/role-hierarchy.service.ts: ADMIN target requires ADMIN caller; parallel resolveTemplateKey calls via Promise.all",
    "common/upload/magic-bytes.validator.ts: assertMagicBytes reads actual file magic bytes, not just Content-Type claim",
    "dashboard/page.tsx: uses Promise.all with per-catch error isolation for parallel fetches, no unhandled rejection",
    "departments.controller.ts: ParseUUIDPipe used on all :id params, no raw string injection risk",
    "departments.service.ts: create() — has isUniqueViolation catch handler wrapping prisma.department.create()",
    "departments.service.ts: getDepartmentStats() — read-only aggregate, no state changes",
    "departments.service.ts: remove() — single prisma.department.delete(), no multi-step; users/services count guard is a business invariant enforced before delete",
    "departments.service.ts: update() — has isUniqueViolation catch handler wrapping prisma.department.update()",
    "departments/page.tsx: no locale routing bugs; uses inline modals with no navigation",
    "deployments dual-write non-atomicity: intentional by design — audit row is best-effort, deployments row is source of truth",
    "deployments/deployments.service.ts: audit.log is fire-and-forget (AuditService.log is synchronous void) — intentional, documented",
    "deployments/deployments.service.ts: isDeployContext uses NODE_ENV=production OR RELEASE_SHA present — correct dual guard",
    "deployments/deployments.service.ts: onApplicationBootstrap wraps recordBoot in try/catch — correctly prevents startup failure",
    "deployments/deployments.service.ts: readAppliedMigrations catch returns [] — intentional best-effort, does not block boot",
    "events/dto/create-event.dto.ts — recurrenceDay validated with @IsInt @Min(0) @Max(6), @IsOptional — validation is structurally correct even if the stored value is not applied in the occurrence loop",
    "events/events.service.ts — findAll() RBAC scoping: non-readAll users are scoped to events they created or participate in via OR clause; admin/readAll path allows userId filter",
    "events/events.service.ts — findOne() IDOR check: users without events:readAll cannot access events they neither created nor participate in (ForbiddenException)",
    "events/events.service.ts — isEventParentCycleViolation helper correctly covers both P0001 (trigger path) and 23514 CHECK constraint path with AND of two signals to avoid false positives",
    "events/events.service.ts — stopRecurrence() correctly guards against child events (parentEventId non-null) and non-recurring events before executing deleteMany+update",
    "events/events.service.ts — stopRecurrence() uses today.setHours(0,0,0,0) which is local-midnight; on Prod=UTC this is equivalent to UTC midnight — acceptable",
    "events/events.service.ts — update() participant rewrite is correctly wrapped in $transaction (lines 480–535) with deleteMany + createMany inside the callback",
    "events/page.tsx canCreateEvent() and canDeleteEvent() correctly use hasPermission() hook",
    "forgot-password/page.tsx is a static info page — no form submission, no CSRF risk",
    "health/health.service.ts: DB ping error path (catch block logs, sets dbStatus='down') — correct, no swallowed exception",
    "health/health.service.ts: Redis lazyConnect + enableOfflineQueue=false — correct fail-fast health check semantics",
    "health/health.service.ts: Redis ping error path (catch block logs, sets redisStatus='down') — correct, no swallowed exception",
    "health/health.service.ts: check() throws ServiceUnavailableException when either dep is down — correct status code mapping",
    "holidays/holidays.service.ts addDays(): uses setUTCDate — no DST issue",
    "holidays/holidays.service.ts calculateEaster(): uses Date.UTC — no host-TZ shift for Easter computation",
    "holidays/holidays.service.ts findByYear(): uses Date.UTC for year boundaries — correct on any host TZ",
    "holidays/holidays.service.ts importFrenchHolidays(): P2002 collision silently skipped — idempotent import",
    "holidays/holidays.service.ts importFrenchHolidays(): all fixed holidays use Date.UTC — no off-by-one on +UTC hosts",
    "holidays/holidays.service.ts onApplicationBootstrap(): error caught and logged — bootstrap failure does not crash the app",
    "leave-types/dto/reorder-leave-types.dto.ts: IsUUID(4, each:true) validates each orderedIds element",
    "leave-types/leave-types.service.ts remove(): guard on _count.leaves before hard-delete — no orphaned leave references",
    "leave-types/leave-types.service.ts reorder(): wrapped in $transaction — all sortOrder updates atomic",
    "leave-types/leave-types.service.ts update(): system type field restriction enforced before update",
    "leaves/dto/create-leave.dto.ts: class-validator decorators present for all required fields",
    "leaves/leave-year-window.ts: all date arithmetic uses UTC (setUTCDate/getUTCDate/parisDayKey via formatInTimeZone) — no host-TZ leakage",
    "leaves/leave-year-window.ts: splitLeaveByYear shortfall distribution is proportional across years — correct cross-year bucket accounting",
    "leaves/leaves.service.ts approve(): balance re-validation inside tx (COR-008) — reduction between PENDING and approval caught",
    "leaves/leaves.service.ts approve(): re-read inside transaction before update — concurrent PENDING→APPROVED race guarded",
    "leaves/leaves.service.ts canManageLeave(): permission check via dynamic RBAC (no hardcoded roles)",
    "leaves/leaves.service.ts cancel(): re-read inside transaction for both APPROVED and CANCELLATION_REQUESTED — concurrent modification detected",
    "leaves/leaves.service.ts checkOverlap(): correctly excludes CANCELLED/REJECTED from overlap window",
    "leaves/leaves.service.ts create(): Serializable isolation + P2034 single-retry — TOCTOU between balance gate and insert properly handled",
    "leaves/leaves.service.ts create(): balance snapshot re-verification after gate before insert — correctly catches concurrent balance modification",
    "leaves/leaves.service.ts findValidatorForUser(): delegation lookup correctly scoped to department manager (COR-005)",
    "leaves/leaves.service.ts getAvailableDays(): uses parisYearWindow for year boundaries — UTC-correct",
    "leaves/leaves.service.ts getHolidayKeySet(): widened by ±1 day — edge-case holidays at boundaries included",
    "leaves/leaves.service.ts getManagedUserIds(): correctly returns 'all' for leaves:manage_any permission",
    "leaves/leaves.service.ts importLeaves(): wrapped in single $transaction for atomic batch import (COR-024)",
    "leaves/leaves.service.ts isLeaveOverlapViolation(): correctly matches both 23P01 and leaves_no_overlap in message",
    "leaves/leaves.service.ts reject(): re-read inside transaction — TOCTOU guarded same as approve()",
    "leaves/leaves.service.ts rejectCancellation(): re-read inside transaction — TOCTOU guarded (COR-009)",
    "leaves/leaves.service.ts remove(): $transaction wraps delete + audit — delete committed atomically with audit",
    "leaves/leaves.service.ts requestCancel(): re-read inside transaction — TOCTOU guarded (COR-009)",
    "leaves/leaves.service.ts update(): excludeLeaveId passed to getAvailableDays — existing leave not double-counted during edit",
    "leaves/leaves.service.ts upsertBalance (global): retry loop on P2002 correctly handles concurrent create race without transaction",
    "leaves/leaves.service.ts upsertBalance (individual): Prisma upsert on compound key is atomic",
    "localStorage token exposure: auth state is in Zustand store (memory), not directly read/written in these pages",
    "localStorage usage in MyTasksSection is limited to UI preference (undeclaredExpanded) — no sensitive data stored",
    "mailto: href in third-parties/[id]/page.tsx uses server-sourced contactEmail — no open redirect to external HTTP URLs",
    "middleware.ts uses crypto.randomUUID() for nonce — cryptographically secure",
    "personal-todos.controller.ts: ParseUUIDPipe used on :id param; CurrentUser injects authenticated user — no horizontal privilege escalation via URL manipulation",
    "personal-todos.service.ts: cleanupOldCompleted() — date math using setDate/getDate on UTC server is safe; completedAt is nullable and filtered correctly; no data corruption risk",
    "personal-todos.service.ts: delete() — findUnique + ownership check + delete is correct",
    "personal-todos.service.ts: findByUser() — cleanup before read is non-atomic but acceptable (cleanup failure does not corrupt; at worst stale rows remain visible briefly)",
    "personal-todos.service.ts: update() — findUnique + ownership check + update is correct; no race since update is keyed on id (PK)",
    "planning-export/dto/import-ics.dto.ts — @MaxLength(5 * 1024 * 1024) caps import payload size appropriately",
    "planning-export/planning-export.service.ts — events and teleworkDays date filter uses single date point so no spanning-overlap gap (only leaves are affected by S7-2)",
    "planning-export/planning-export.service.ts — exportIcs timed event local setHours math (lines 77-86): Prod=UTC neutralizes DST/offset drift; not a practical issue",
    "planning-export/planning-export.service.ts — fire-and-forget audit log correctly uses void + .catch() to avoid propagating audit failures to the export response",
    "planning-export/planning-export.service.ts — importIcs swallowed catch (line 264) is intentional resilience (per-event skip-on-error); confirmed by spec asserting skip-on-create-error",
    "planning/__tests__/page.test.tsx: test file exists and covers basic rendering",
    "planning/dto/planning-overview-query.dto.ts — @IsISO8601 + @IsNotEmpty correctly validates both startDate and endDate",
    "planning/planning.service.ts — canViewPredefinedTasks permission gate correctly resolves to Promise.resolve([]) when user lacks the permission",
    "planning/planning.service.ts — dateOnlyStart/dateOnlyEnd slicing (slice(0,10)) correctly truncates ISO8601 timestamps to YYYY-MM-DD for services expecting date-only strings",
    "planning/planning.service.ts — getOverview() two-wave Promise.all: pure read aggregation, partial failure is acceptable (returns 500 to caller, no writes involved)",
    "planning/planning.service.ts — leavesResult dual-shape handling (Array.isArray guard) correctly covers both paginated and raw-array return shapes from LeavesService.findAll",
    "prisma/prisma.service.ts: Decimal.toJSON() patched for numeric serialization; cleanDatabase() gated on non-production",
    "projects/[id]/page.tsx: handleHardDeleteProject, handleAddMember, handleAttachClients, handleDetachClient, handleSaveMilestone, handleSaveTask all have try/catch with user-facing error toasts",
    "projects/__tests__/page.test.tsx: test file exists and covers filter and rendering behavior",
    "projects/page.tsx: handleCreate has try/catch with user-facing error toast",
    "rbac/permissions.guard.ts: correctly returns false for unauthenticated users; enforce mode refuses undecorated routes; @AllowSelfService bypasses permission check",
    "rbac/permissions.service.ts: singleflight coalesces concurrent DB lookups; negative-cache prevents repeated DB hits for orphan roles; fail-soft on Redis error",
    "rbac/roles.service.ts deleteRole(): checks for dependent users before delete; cache invalidated after delete; snapshot written before delete",
    "reset-password/page.tsx correctly disables inputs when token is missing",
    "schema-constraints/: all files are integration spec files exercising DB-level constraints; no source logic to audit for correctness bugs",
    "school-vacations/school-vacations.service.ts create(): P2002 caught and re-thrown as ConflictException",
    "school-vacations/school-vacations.service.ts importFromOpenData(): AbortController timeout correctly cleared in finally block",
    "school-vacations/school-vacations.service.ts importFromOpenData(): P2002 on create → skipped (idempotent)",
    "school-vacations/school-vacations.service.ts importFromOpenData(): deduplication by description takes widest date range — correct merge",
    "scripts/backfill-snapshots.ts: app.close() in finally block — correct cleanup even on error",
    "scripts/normalize-action-codes.ts: DATABASE_MIGRATION_URL guard fails fast before Nest boot — correct privilege enforcement",
    "scripts/normalize-action-codes.ts: DryRunRollback throws inside transaction — trigger DISABLE rolled back automatically, no stuck-disabled state",
    "scripts/normalize-action-codes.ts: STARTED emitted before transaction, COMPLETED after — ordering correct; STARTED row re-hashed by the recompute walk",
    "scripts/normalize-action-codes.ts: app.close() in finally block — correct cleanup",
    "scripts/recompute-chain-on-schema-bump.ts: app.close() in finally block — correct cleanup",
    "scripts/recompute-chain-on-schema-bump.ts: full-chain recompute from genesis anchors on null predecessor — correctly handles empty-chain case",
    "scripts/recompute-chain-on-schema-bump.ts: idempotency correct — second run recomputes identical hashes, verification still passes",
    "scripts/system-backfill-audit.ts: emitSystemBackfill awaits (not fire-and-forget) for durable maintenance event — correct",
    "services.controller.ts: ParseUUIDPipe used on all :id params",
    "services.service.ts: create() — has isUniqueViolation catch wrapping prisma.service.create()",
    "services.service.ts: findAll() Promise.all([findMany, count]) — both queries use same where clause, correct",
    "services.service.ts: getServiceStats() — read-only aggregate",
    "services.service.ts: update() — has isUniqueViolation catch wrapping prisma.service.update()",
    "settings/page.tsx: isAdmin evaluated only after AuthProvider ensures permissionsLoaded=true, no flash redirect",
    "skills.controller.ts: route ordering — /me/assign and /me/remove/:skillId and /me/my-skills appear before /:id, correct NestJS route priority",
    "skills.service.ts: assignSkillToUser() — proper upsert pattern (check existing, update or create), no TOCTOU risk because the unique key is (userId, skillId) and Prisma would throw P2002 only on a truly concurrent duplicate which would update correctly",
    "skills.service.ts: findAll() Promise.all([findMany, count]) — both queries use the same `where` clause, correct",
    "skills.service.ts: getSkillsMatrix() — Promise.all([users, skills]) is correct, both queries are independent reads",
    "skills.service.ts: importSkills() — each prisma.skill.create() is wrapped in try/catch at line 725, errors are recorded and execution continues",
    "skills.service.ts: removeSkillFromUser() — findUnique then delete is fine, the delete will throw NotFoundException-equivalent if the row disappeared between the two calls (but it maps to NotFoundException on findUnique first)",
    "skills.service.ts: validateImport() — sequential processing with seenNames Set avoids in-file duplicates, existingNames snapshot is taken before loop, no atomic write risk since this is read-only dry-run",
    "stableStringify undefined at top-level: if value === undefined returns 'null' — only affects in-memory representation before DB storage, not an issue for keys passed through Object.keys",
    "tasks/[id]/page.tsx: handleSave, handleDelete, handleDependencyChange all have try/catch with user-facing error toasts",
    "tasks/__tests__/page.test.tsx: test file exists and covers multiple interaction flows",
    "tasks/page.tsx: handleStatusChange, handleDeleteTask, handleDateChange all have try/catch with user-facing error toasts",
    "telework.controller.ts: All mutation routes have OwnershipCheck or service-level equivalent.",
    "telework.controller.ts: Route ordering — /recurring-rules prefix routes are defined before /:id wildcard, preventing shadowing.",
    "telework.service.ts: COR-012/COR-027 date normalisation in create(), update(), expandRecurringRulesForRange(), generateSchedulesFromRules() — all use teleworkDayKey+dayKeyToUTCDate UTC-anchored pipeline; correct.",
    "telework.service.ts: No try/catch blocks → no swallowed exceptions.",
    "telework.service.ts: Pagination in findAll — skip/take correct; Math.min(limit,1000) safety cap present.",
    "telework.service.ts: Permission checks in create/update/remove/findOne/createRecurringRule/updateRecurringRule/removeRecurringRule are complete and symmetric.",
    "telework.service.ts: Promise.all in findAll (lines 184-219) is two independent reads (findMany + count); ordering does not matter; no partial-write risk.",
    "telework.service.ts: create() — TOCTOU between findUnique check and create for userId_date is mitigated by the unique index (P2002 would bubble as 409 if ConflictException is thrown, but actually as 500 — see S6-3 for the expansion paths; the single-create path here throws ConflictException correctly after findUnique returns non-null at line 120-122).",
    "telework.service.ts: modelDayOfWeekFromKey() uses getUTCDay() — DST-safe.",
    "telework.service.ts: nextTeleworkDayKey() uses Date.UTC to advance by one calendar day — DST-safe.",
    "telework/page.tsx fetchTeleworkData useCallback has correct deps [currentMonth, selectedUserId, tc]",
    "testing/testing.controller.ts: production guard at handler level as defense-in-depth in addition to conditional module registration — correct",
    "third-parties/[id]/page.tsx permission checks correctly use hasPermission() hook",
    "time-tracking.controller.ts: toActor() helper correctly maps AuthenticatedUser to the service's {id, role} shape.",
    "time-tracking.service.ts: Decimal hours via Prisma Decimal type converted with Number() — safe at these magnitudes (max 24h per entry).",
    "time-tracking.service.ts: create() — isDismissal early-return bypasses resolveActor and daily-cap checks (correct: dismissals have hours=0 and are exempt).",
    "time-tracking.service.ts: ensureCanMutate uses OwnershipService.isOwner then permission fallback — no swallowed exception.",
    "time-tracking.service.ts: ensureDailyCapNotExceeded uses Date.UTC for startOfDay/endOfDay — UTC-anchored and correct for stored @db.Date fields.",
    "time-tracking.service.ts: findAll — partial-failure in Promise.all (findMany + count) is safe; both are reads; no inconsistent state possible.",
    "time-tracking.service.ts: getProjectReport — Promise.all over 6 queries is safe; all independent reads.",
    "time-tracking.service.ts: getProjectReport — byDate key via row.date.toISOString().split('T')[0] is correct for @db.Date fields stored at UTC midnight.",
    "time-tracking.service.ts: getUserReport — Promise.all over 4 aggregate/groupBy queries is safe; all independent reads.",
    "time-tracking.service.ts: update() cross-actor mutation guard ('thirdPartyId' in updateTimeEntryDto check) — correctly blocks actor type changes.",
    "time-tracking.service.ts: upsertDismissal — task.projectId can be null (orphan task); create stores null projectId which is allowed by the schema.",
    "useAuthBootstrap correctly fetches /auth/me + /auth/me/permissions in parallel and is the source of truth",
    "usePermissions hook reads from server-hydrated permissions array, not from client-computed role checks",
    "users/users.service.ts create(): bcrypt hash verification after generation (defensive); role hierarchy checked before creation; serviceIds validated before user.create — NOTE: P2002 not translated to 409 (see correctness-S1-5)",
    "users/users.service.ts findAll(): safeLimit caps to HARD_CEILING; Promise.all([findMany, count]) ordering is safe (independent queries)",
    "users/users.service.ts hardDelete(): full owned-data erasure in $transaction; avatar removal outside transaction (intentional — cannot roll back file I/O); audit snapshot before erasure",
    "users/users.service.ts importUsers(): per-row error isolation; password policy checked imperatively for non-HTTP paths; role hierarchy enforced per row",
    "users/users.service.ts remove(): jwtNotBefore.bumpUser() called on isActive true→false transition",
    "users/users.service.ts update(): jwtNotBefore.bumpUser() called when isActive goes true→false",
    "users/users.service.ts uploadAvatar(): MIME type checked via both mimetype field AND magic bytes; old avatar files cleaned before write; avatarUrl set to null when preset chosen",
    "withAccessControl HOC correctly renders null when permissions are absent"
  ]
}
