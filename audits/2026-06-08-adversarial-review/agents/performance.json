{
  "dimension": "performance",
  "findings": [
    {
      "id": "PER-003",
      "severity": "important",
      "category": "performance",
      "subcategory": "full-table-scan",
      "title": "checkCircularDependency loads entire task_dependency table with no WHERE clause",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "1424-1473",
      "code_evidence": "const allDeps = await this.prisma.taskDependency.findMany({\n  select: { taskId: true, dependsOnTaskId: true },\n});\n// BFS over allDeps in memory …",
      "description": "Every call to POST /tasks/:id/dependencies fetches the entire `task_dependency` table with no project-scoped or task-scoped WHERE predicate. The comment says 'pre-fetch strategy' (PER-023), but the strategy has no upper bound. In a deployment with thousands of tasks the query materialises the full edge list into the Node.js process on every dependency mutation.",
      "root_cause": "The BFS pre-load was optimised to avoid N round-trips but the WHERE clause was never added. A graph traversal starting from a single task only needs the reachable subgraph, not the global edge set.",
      "impact": "Full table scan on every dependency write. With 10,000 dependency rows the query transfers ~320 KB per call. Memory pressure and query time grow linearly with the total dependency count, not with the task graph size.",
      "suggested_fix": "Scope the initial fetch to the same project: add `where: { task: { projectId: startTask.projectId } }`. Alternatively pre-load only direct successors/predecessors lazily during BFS traversal (one indexed lookup per hop). Either approach requires an index on `task_dependency(taskId)` and `task_dependency(dependsOnTaskId)` — verify via `\\d task_dependency` in psql.",
      "acceptance_criteria": [
        "findMany call includes a WHERE clause scoping to project or relevant task IDs",
        "No regression on the existing circular-dependency spec tests",
        "Query plan shows Index Scan on task_dependency instead of Seq Scan"
      ],
      "verification_command": "grep -n 'findMany' apps/api/src/tasks/tasks.service.ts | grep -i 'taskDependency'",
      "related_findings": [
        "COR-001"
      ],
      "cluster_id": "F",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-004",
      "severity": "important",
      "category": "performance",
      "subcategory": "n+1-query",
      "title": "getLeaveBalance issues 1-2 DB round-trips per leave type via Promise.all(leaveTypes.map(resolveAllocatedDays))",
      "file": "apps/api/src/leaves/leaves.service.ts",
      "line": "2840-2850",
      "code_evidence": "const balancesByType = await Promise.all(\n  leaveTypes.map(async (lt) => {\n    const totalDays = await this.resolveAllocatedDays(\n      userId,\n      lt.id,\n      currentYear,\n    );\n    // … per-type used/remaining calcs …\n  }),\n);",
      "description": "For each active leave type the service calls `resolveAllocatedDays`, which issues a `findUnique` (individual allocation) and optionally a `findFirst` (global default allocation). With N leave types this is N to 2N parallel DB queries. The outer `allLeaves` fetch above this block is correctly batched, making the allocation resolution the dominant query cost.",
      "root_cause": "`resolveAllocatedDays` was designed as a single-item resolver. The call-site maps over all leave types without pre-fetching all relevant allocations in a single query.",
      "impact": "For a user with 10 leave types: 10-20 additional DB round-trips per `/leaves/balance` request. Under concurrent users (e.g., 50 users checking balance simultaneously) this generates 500-1000 DB queries per second for a single endpoint.",
      "suggested_fix": "Pre-fetch all relevant allocations in two bulk queries before the map: one `findMany` for individual allocations (`userId + leaveTypeId IN [...]`) and one `findMany` for global defaults (`leaveTypeId IN [...] AND userId IS NULL`). Build lookup Maps, then resolve in-memory inside the loop with zero additional DB calls.",
      "acceptance_criteria": [
        "At most 2 DB queries total inside getLeaveBalance (bulk individual + bulk global), regardless of N leave types",
        "Existing balance calculation logic and outputs unchanged",
        "Unit tests for getLeaveBalance pass"
      ],
      "verification_command": "grep -n 'resolveAllocatedDays\\|findUnique\\|findFirst' apps/api/src/leaves/leaves.service.ts | head -40",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-005",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded-query",
      "title": "RecentActivityService.getRecentActivity fetches all active tasks with no take limit",
      "file": "apps/api/src/analytics/advanced/services/recent-activity.service.ts",
      "line": "38-48",
      "code_evidence": "const tasks = await this.prisma.task.findMany({\n  where: {\n    project: { status: 'ACTIVE', AND: [projectScope, archivedClause] },\n  },\n  select: {\n    status: true,\n    createdAt: true,\n    updatedAt: true,\n    endDate: true,\n  },\n});",
      "description": "No `take` clause on `task.findMany`. For an ADMIN user `projectScope` is `{}`, so this fetches every task row across all active projects. The select is narrow (4 columns) but the row count is unbounded. This endpoint is called as part of the advanced analytics fan-out.",
      "root_cause": "The service was written to compute recent-activity metrics that need a full status distribution, but no upper bound was added. Analytics endpoints often accept full scans in exchange for caching, but here the caching layer is one level up (analytics controller).",
      "impact": "In a large deployment with 50,000 tasks across active projects an ADMIN request transfers 50,000 rows from Postgres to Node.js on each analytics refresh. Memory spike of several MB per request, potential OOM under concurrent analytics access.",
      "suggested_fix": "Add a recency filter (`updatedAt >= thirtyDaysAgo`) matching the 'recent activity' semantic: `where: { updatedAt: { gte: new Date(Date.now() - 30 * 86400_000) }, project: { ... } }`. This aligns the query with the feature's intent and adds an indexable time filter. Alternatively add `take: 5000` as a safety ceiling and document the cap.",
      "acceptance_criteria": [
        "Query includes a time-based WHERE predicate (e.g. updatedAt >= 30 days ago) or explicit take cap",
        "EXPLAIN ANALYZE shows Index Scan on tasks(updatedAt) rather than Seq Scan",
        "Analytics output values remain correct for typical data ranges"
      ],
      "verification_command": "grep -n 'findMany\\|take\\|updatedAt' apps/api/src/analytics/advanced/services/recent-activity.service.ts",
      "related_findings": [
        "PER-016",
        "PER-017",
        "PER-018"
      ],
      "cluster_id": "I",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-006",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded payload",
      "title": "usersService.getAll() hardcodes limit=1000 — always fetches the entire user table",
      "file": "apps/web/src/services/users.service.ts",
      "line": "11",
      "code_evidence": "  async getAll(\n    page?: number,\n    limit?: number,\n    roleCode?: string,\n  ): Promise<User[] | PaginatedResponse<User>> {\n    const params = new URLSearchParams();\n    params.append(\"limit\", (limit ?? 1000).toString());",
      "description": "When callers omit the `limit` parameter (the common case — 13+ call sites), the default silently becomes 1000. Every page that calls usersService.getAll() (Users, Projects, Tasks, Leaves, Events, Suivi, etc.) downloads the entire user list in a single response.",
      "root_cause": "The default parameter `limit ?? 1000` was chosen as a simple workaround for the backend's 10-row default, but it means any growth in user count (government org with 500+ agents) makes every page render significantly slower.",
      "impact": "At 500 users with their role, department, services, and skills relations eager-loaded, the response payload easily exceeds 100 KB. Every page navigation that involves a user dropdown triggers this download afresh (no TanStack Query cache on most of these raw useEffect calls).",
      "suggested_fix": "Add a dedicated `search` or `autocomplete` endpoint (GET /users/autocomplete?q=&limit=50) for dropdowns. For list pages (Users page), use server-side pagination. For lookup dropdowns inside modals (Projects, Tasks), replace with a lazy-search component that fetches on keystroke.",
      "acceptance_criteria": [
        "No call site passes limit >100 except an intentional full-export scenario",
        "Dropdown pickers use a search-backed component with max 50 results",
        "Users page paginates server-side (next/prev buttons)"
      ],
      "verification_command": "grep -rn 'usersService.getAll' apps/web/app --include='*.tsx' | wc -l",
      "related_findings": [
        "PER-020",
        "PER-007",
        "PER-008",
        "PER-012",
        "PER-019"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-007",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded payload",
      "title": "projectsService.getAll() hardcodes limit=1000 — fetches entire project list on every load",
      "file": "apps/web/src/services/projects.service.ts",
      "line": "22",
      "code_evidence": "  async getAll(\n    page?: number,\n    limit?: number,\n    status?: ProjectStatus,\n    archived?: ArchivedFilter,\n  ): Promise<PaginatedResponse<Project>> {\n    const params = new URLSearchParams();\n    params.append(\"limit\", (limit ?? 1000).toString());",
      "description": "Same pattern as users: default limit is 1000. Called without arguments from tasks/page.tsx, events/page.tsx, tasks/[id]/page.tsx, and the reports page's `loadProjects` (via raw `api.get('/projects')` which hits the backend's own default). All project metadata (members, milestones, snapshots partial data) is downloaded at once.",
      "root_cause": "Defensive workaround for the backend pagination default; no upper bound was set.",
      "impact": "An organisation with hundreds of projects (common in local government) will download a multi-hundred KB payload on every page requiring a project selector dropdown. The tasks page fetches limit=1000 projects AND limit=200 tasks in the same render cycle.",
      "suggested_fix": "Use limit=50 or a project search endpoint for dropdowns. The Projects list page already has client-side filters; keep server pagination with a 50-item page size.",
      "acceptance_criteria": [
        "projectsService.getAll() with no arguments defaults to limit=50 at most",
        "Tasks/Events pages use a lazy-loaded project picker instead of pre-fetching all projects"
      ],
      "verification_command": "grep -rn 'projectsService.getAll()' apps/web/app --include='*.tsx'",
      "related_findings": [
        "PER-020",
        "PER-006",
        "PER-008",
        "PER-012",
        "PER-019"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-008",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded payload",
      "title": "milestonesService.getAll() and getByProject() both hardcode limit=1000",
      "file": "apps/web/src/services/milestones.service.ts",
      "line": "7-27",
      "code_evidence": "  async getAll(): Promise<PaginatedResponse<Milestone>> {\n    const response = await api.get<PaginatedResponse<Milestone>>(\n      \"/milestones?limit=1000\",\n    );\n    return response.data;\n  },\n\n  // ...\n\n      `/milestones?projectId=${projectId}&limit=1000`,",
      "description": "Both the global milestone list and per-project milestone list unconditionally request 1000 rows. Projects with heavy milestone use (roadmaps, legislative cycles) could accumulate thousands of milestones over time.",
      "root_cause": "No upper bound defined; 1000 chosen as a safe upper limit that will hide pagination issues until growth occurs.",
      "impact": "Per-project detail page loads all project milestones in one request. With milestone details (tasks counts, dates, etc.) this can be a large payload for mature projects.",
      "suggested_fix": "For the project detail page, limit=200 is a more realistic upper bound. For the global list, add pagination UI or lazy loading.",
      "acceptance_criteria": [
        "milestonesService.getByProject() uses limit=200 maximum",
        "milestonesService.getAll() is not called in production flows (only used in admin export scenarios)"
      ],
      "verification_command": "grep -rn 'milestonesService.getAll\\|milestonesService.getByProject' apps/web/app --include='*.tsx'",
      "related_findings": [
        "PER-020",
        "PER-006",
        "PER-007",
        "PER-012",
        "PER-019"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-009",
      "severity": "important",
      "category": "performance",
      "subcategory": "request waterfall",
      "title": "projects/[id]/page.tsx fetches project, stats, tasks, milestones sequentially (4 serial awaits)",
      "file": "apps/web/app/[locale]/projects/[id]/page.tsx",
      "line": "150-202",
      "code_evidence": "        setLoading(true);\n\n        // Fetch project details\n        const projectData = await projectsService.getById(projectId);\n        setProject(projectData);\n        setHiddenStatuses(projectData.hiddenStatuses || []);\n\n        // Fetch project stats\n        try {\n          const statsData = await projectsService.getStats(projectId);\n          setStats(statsData);\n        } catch (err) {\n",
      "description": "The project detail page executes 4 sequential API calls: getById, getStats, getByProject (tasks), getByProject (milestones). Each waits for the previous to complete before starting. Total TTFB-to-data is approximately 4× a single round-trip.",
      "root_cause": "Incremental feature additions placed each fetch in sequence without consolidating into Promise.all.",
      "impact": "On a 100ms RTT connection, the page waits 400ms+ before rendering any content. This affects the core project management workflow — the most-used page in the application.",
      "suggested_fix": "Parallelise all 4 calls: `const [projectData, statsData, tasksData, milestonesData] = await Promise.all([projectsService.getById(projectId), projectsService.getStats(projectId), tasksService.getByProject(projectId), milestonesService.getByProject(projectId)])`. Note: hiddenStatuses extraction from projectData still works after the promise resolves.",
      "acceptance_criteria": [
        "All 4 fetches fire concurrently using Promise.all",
        "Error handling remains isolated per fetch (use Promise.allSettled if partial failures should be non-blocking)"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-021",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-010",
      "severity": "important",
      "category": "performance",
      "subcategory": "request waterfall",
      "title": "suivi/page.tsx makes sequential usersService.getAll() followed by 7 parallel fetches — extra serial hop",
      "file": "apps/web/app/[locale]/users/[id]/suivi/page.tsx",
      "line": "174-199",
      "code_evidence": "      // Fetch accessible users for the dropdown (scoped to access)\n      try {\n        const usersData = await usersService.getAll();\n        const usersList: User[] = Array.isArray(usersData)\n          ? usersData\n          : (usersData as { data: User[] }).data || [];\n        // Filter to only users the current user can access\n        setAllUsers(\n          usersList.filter((u: User) => u.isActive && checkAccess(u)),\n        );\n      } catch {\n        // Non-blocking: dropdown just won't work\n      }\n\n      // Fetch all related data in parallel\n      const { start, end } = periodDates;\n      try {\n        const results = await Promise.allSettled([",
      "description": "The suivi page awaits `usersService.getAll()` (which fetches 1000 users) before launching the 7 parallel data fetches. Since the users dropdown is non-blocking (\"// Non-blocking\"), all 7 fetches could be started in parallel with the users call.",
      "root_cause": "The users call was placed before the parallel block due to its non-critical nature, but it was not made concurrent.",
      "impact": "Adds one full RTT (plus the time to deserialise potentially 1000 users) before the 7 content fetches start. On a typical LAN, this could add 50–200ms to page load.",
      "suggested_fix": "Start the users fetch as a floating promise alongside the `Promise.allSettled` block: fire `usersService.getAll()` in parallel and await it separately only when setting state.",
      "acceptance_criteria": [
        "usersService.getAll() and the Promise.allSettled block fire concurrently",
        "The users dropdown populates asynchronously after the main data is displayed"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-011",
      "severity": "important",
      "category": "performance",
      "subcategory": "missing TanStack Query cache",
      "title": "Most pages use raw useState+useEffect instead of TanStack Query — no deduplication or cache across pages",
      "file": "apps/web/app/[locale]/projects/page.tsx",
      "line": "85-131",
      "code_evidence": "  const fetchProjects = useCallback(async () => {\n    try {\n      setLoading(true);\n      let projectsData: Project[] = [];\n\n      // Si ?member=me dans l'URL, on force le scope \"mes projets\" quel que\n      // soit le rôle (pour les CTA du Dashboard notamment).\n      if (memberMeFilter && user?.id) {\n        try {\n          projectsData = await projectsService.getByUser(user.id);\n        } catch (err) {",
      "description": "While TanStack Query hooks (useProjects, useUsers, useTasks) exist for the common list queries, the majority of pages bypass them and use raw useEffect + setState. This means navigating Projects → Dashboard → Projects triggers three separate `/projects` fetches with no cache reuse.",
      "root_cause": "The TanStack Query hooks were added as a partial refactor but not applied throughout the pages. The pages retain the original manual fetch pattern.",
      "impact": "Every page mount re-fetches the full dataset. The QueryClient cache (30s staleTime) is never hit for page-level data. On a shared dashboard → tasks → projects navigation, the backend receives 3× the API calls needed.",
      "suggested_fix": "Replace the raw useEffect fetch in projects/page.tsx, tasks/page.tsx, and users/page.tsx with the existing `useProjects()`, `useTasks()`, and `useUsers()` hooks. Extend the hooks to accept filter parameters and add query key variants.",
      "acceptance_criteria": [
        "projects/page.tsx, tasks/page.tsx, users/page.tsx use their respective TanStack Query hooks",
        "Back-navigation does not trigger a new network request within the 30s staleTime window"
      ],
      "verification_command": "grep -rn 'useProjects\\|useTasks\\|useUsers' apps/web/app --include='*.tsx' | wc -l",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-013",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-012",
      "severity": "important",
      "category": "performance",
      "subcategory": "unbounded payload",
      "title": "leavesService.getByUser() and getByDateRange() hardcode limit=1000 with no upper bound",
      "file": "apps/web/src/services/leaves.service.ts",
      "line": "113-145",
      "code_evidence": "  async getByUser(userId: string): Promise<Leave[]> {\n    // The API exposes a user's leaves via the filtered list GET /leaves?userId=\n    // (paginated {data,meta} envelope) — there is no /leaves/user/:id path route.\n    const response = await api.get<{ data: Leave[] } | Leave[]>(\n      `/leaves?userId=${userId}&limit=1000`,\n    );\n    if (response.data && \"data\" in response.data) {\n      return response.data.data;\n    }",
      "description": "Both leave-fetching paths request up to 1000 leave records. For a long-tenured employee or a manager viewing a large team's leaves, this could return hundreds of records with full user and leave-type relations embedded.",
      "root_cause": "Workaround for the API's default 10-row pagination; no practical upper bound was evaluated.",
      "impact": "The suivi page fetches up to 1000 leaves for one user on page load. The leaves page itself uses limit=100 for getAll (acceptable) but the getByUser path is unbounded.",
      "suggested_fix": "Apply a year-scoped filter: `limit=200&year=currentYear` for most UI contexts. For historical data (audit), expose a separate paginated endpoint.",
      "acceptance_criteria": [
        "getByUser uses limit=200 maximum",
        "suivi page passes a year filter to scope the leave data to the relevant period"
      ],
      "verification_command": "grep -rn 'limit=1000' apps/web/src/services/leaves.service.ts",
      "related_findings": [
        "PER-020",
        "PER-006",
        "PER-007",
        "PER-008",
        "PER-019"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-013",
      "severity": "important",
      "category": "performance",
      "subcategory": "redundant refetch",
      "title": "tasks/page.tsx calls fetchData() (5 API calls) on every status change, delete, and date change",
      "file": "apps/web/app/[locale]/tasks/page.tsx",
      "line": "150-195",
      "code_evidence": "  const handleStatusChange = async (taskId: string, newStatus: TaskStatus) => {\n    try {\n      await tasksService.update(taskId, { status: newStatus });\n      toast.success(t(\"messages.statusUpdateSuccess\"));\n      fetchData();\n    } catch {\n      toast.error(t(\"messages.statusUpdateError\"));\n    }\n  };\n\n  const handleDeleteTask = async (taskId: string) => {\n    try {\n      await tasksService.delete(taskId);\n      toast.success(\n        t(\"messages.deleteSuccess\", { defaultValue: \"Tâche supprimée\" }),\n      );\n      fetchData();\n    } catch {",
      "description": "After every mutation (status change, delete, date change), the page calls `fetchData()` which re-fetches projects, tasks (up to 200), orphan tasks, users (up to 1000), and services — 5 separate API calls. This happens for every status toggle in the Kanban view.",
      "root_cause": "Full re-fetch pattern as defensive measure; no optimistic updates or targeted cache invalidation.",
      "impact": "A Kanban user dragging 5 tasks between columns triggers 25 API calls. On a slow network this causes visible flicker and server load.",
      "suggested_fix": "Use optimistic local state updates for status change and date change. For delete, remove the item from local state. Reserve fetchData() for cases where server-authoritative state is critical (e.g., after import).",
      "acceptance_criteria": [
        "handleStatusChange, handleDeleteTask, and handleDateChange apply optimistic state updates",
        "fetchData is not called after single-task mutations"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-015",
      "severity": "nit",
      "category": "performance",
      "subcategory": "sequential-await-in-loop",
      "title": "importTasks inner loop uses sequential await tx.subtask.create instead of createMany",
      "file": "apps/api/src/tasks/tasks.service.ts",
      "line": "1640-1648",
      "code_evidence": "for (let j = 0; j < subtaskTitles.length; j++) {\n  await tx.subtask.create({\n    data: {\n      title: subtaskTitles[j],\n      taskId: createdTask.id,\n      position: j,\n    },\n  });\n}",
      "description": "Inside the per-task `$transaction` callback, subtask rows are inserted one by one with sequential `await`. This means a task with 5 subtasks generates 5 sequential round-trips inside the transaction, and the outer loop serialises all tasks. For a CSV import of 100 tasks each with 5 subtasks: 100 × 6 = 600 sequential DB calls.",
      "root_cause": "The outer per-task transaction was likely added to ensure atomicity per task. The inner subtask loop was not converted to `createMany` when the feature was implemented.",
      "impact": "Import of 100 tasks with 5 subtasks each is 5× slower than necessary for the subtask insertion step. Perceived as a slow import UX on large CSV files.",
      "suggested_fix": "Replace the inner subtask loop with a single `tx.subtask.createMany({ data: subtaskTitles.map((title, j) => ({ title, taskId: createdTask.id, position: j })) })`. Preserves transaction atomicity, reduces round-trips to 1 per task.",
      "acceptance_criteria": [
        "Inner subtask loop replaced with a single createMany call",
        "Import integration tests pass with subtask counts > 1",
        "No change to subtask position ordering"
      ],
      "verification_command": "grep -n 'subtask.create\\b' apps/api/src/tasks/tasks.service.ts",
      "related_findings": [],
      "cluster_id": null,
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-016",
      "severity": "nit",
      "category": "performance",
      "subcategory": "memory-materialisation",
      "title": "captureSnapshots materialises all task rows for ACTIVE projects instead of using groupBy",
      "file": "apps/api/src/projects/projects.service.ts",
      "line": "1217-1223",
      "code_evidence": "const projects = await this.prisma.project.findMany({\n  where: { status: ProjectStatus.ACTIVE },\n  include: {\n    tasks: { select: { status: true } },\n    milestones: { select: { status: true, dueDate: true } },\n  },\n});",
      "description": "Called from a scheduled job, this fetches all task and milestone rows for every active project. With 200 active projects averaging 100 tasks each, 20,000 task rows are transferred to compute status counts. A `groupBy` on `task` grouped by `(projectId, status)` would return the same aggregate with far fewer bytes.",
      "root_cause": "The snapshot logic counts tasks per status in JS after fetching all rows (`tasks.filter(t => t.status === 'DONE').length`). Moving this to a DB-side groupBy would eliminate the materialisation.",
      "impact": "Scheduled job transfers O(all_tasks) per run instead of O(distinct_status × projects). At scale this is a memory spike every cron interval.",
      "suggested_fix": "Replace the `include: { tasks }` with a `groupBy` query: `prisma.task.groupBy({ by: ['projectId', 'status'], where: { project: { status: 'ACTIVE' } }, _count: true })`. Build a lookup Map keyed by projectId to reconstruct the snapshot counts in-memory with no task row transfer.",
      "acceptance_criteria": [
        "No task rows materialised in Node.js; status counts come from groupBy aggregate",
        "Snapshot captured values identical to current implementation",
        "Scheduled job memory footprint reduced (verifiable via process.memoryUsage() logging)"
      ],
      "verification_command": "grep -n 'captureSnapshots\\|tasks.*select\\|filter.*status' apps/api/src/projects/projects.service.ts | head -20",
      "related_findings": [
        "PER-005",
        "PER-017",
        "PER-018"
      ],
      "cluster_id": "I",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-017",
      "severity": "nit",
      "category": "performance",
      "subcategory": "unbounded-query",
      "title": "MilestonesCompletionService fetches all milestone rows with no take cap",
      "file": "apps/api/src/analytics/advanced/services/milestones-completion.service.ts",
      "line": "39-44",
      "code_evidence": "const milestones = await this.prisma.milestone.findMany({\n  where: { project: { AND: [projectScope, archivedClause] } },\n  include: {\n    project: { select: { id: true, name: true } },\n  },\n});",
      "description": "No `take` clause. An ADMIN user scoped to all projects fetches every milestone row. The `include.project` select is narrow but there is no upper bound on row count.",
      "root_cause": "Milestones are a smaller table than tasks but the same pattern of missing `take` applies. Analytics services share this anti-pattern.",
      "impact": "Low severity given typical milestone counts per deployment (hundreds not hundreds of thousands), but consistent with the unbounded pattern in sibling analytics services.",
      "suggested_fix": "Add `take: 2000` as a safety ceiling, or add a recency filter on `dueDate` / `createdAt` matching the analytics window. Document the cap in a comment.",
      "acceptance_criteria": [
        "Query has explicit take cap or date-range filter",
        "Analytics output unchanged for data sets within the cap"
      ],
      "verification_command": "grep -n 'findMany\\|take\\|milestone' apps/api/src/analytics/advanced/services/milestones-completion.service.ts",
      "related_findings": [
        "PER-005",
        "PER-016",
        "PER-018"
      ],
      "cluster_id": "I",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-018",
      "severity": "nit",
      "category": "performance",
      "subcategory": "unbounded-query",
      "title": "TasksBreakdownService fetches all active task rows with no take cap",
      "file": "apps/api/src/analytics/advanced/services/tasks-breakdown.service.ts",
      "line": "37-46",
      "code_evidence": "const tasks = await this.prisma.task.findMany({\n  select: { priority: true, status: true },\n  where: {\n    project: {\n      status: 'ACTIVE',\n      AND: [projectScope, archivedClause],\n    },\n  },\n});",
      "description": "Two-column select (`priority`, `status`) but no `take` limit. For ADMIN users this returns every task across all active projects. Similar pattern to RecentActivityService but the 2-column select is even narrower.",
      "root_cause": "The task breakdown is a status/priority distribution — semantically a `groupBy` query. Materialising all rows to count them in JS is the common anti-pattern across these analytics services.",
      "impact": "Transfers O(all_active_tasks) rows per analytics request. With a 2-column select the per-row size is minimal but row count is unbounded.",
      "suggested_fix": "Replace `findMany` with `prisma.task.groupBy({ by: ['priority', 'status'], where: { ... }, _count: { _all: true } })`. This computes the breakdown entirely in Postgres and returns O(distinct_priority × distinct_status) rows regardless of total task count.",
      "acceptance_criteria": [
        "findMany replaced with groupBy or take cap added",
        "Priority/status distribution output unchanged",
        "Query plan shows aggregate rather than full table scan"
      ],
      "verification_command": "grep -n 'findMany\\|groupBy\\|take' apps/api/src/analytics/advanced/services/tasks-breakdown.service.ts",
      "related_findings": [
        "PER-005",
        "PER-016",
        "PER-017"
      ],
      "cluster_id": "I",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-019",
      "severity": "nit",
      "category": "performance",
      "subcategory": "unbounded payload",
      "title": "timeTrackingService.getAll() fetches ALL time entries with no date or user bound",
      "file": "apps/web/src/services/time-tracking.service.ts",
      "line": "12-20",
      "code_evidence": "  async getAll(): Promise<TimeEntry[]> {\n    const response = await api.get<{ data: TimeEntry[] } | TimeEntry[]>(\n      \"/time-tracking\",\n    );\n    // API returns {data: [], meta: {}} - extract the array\n    if (response.data && \"data\" in response.data) {\n      return response.data.data;\n    }\n    return Array.isArray(response.data) ? response.data : [];",
      "description": "getAll() fetches all time-tracking entries with no pagination or date filter. Fortunately, call sites in app pages (time-tracking/page.tsx) use `getMyEntries()` instead, so getAll() is not called in production page flows. However, the method exists as a footgun for future callers.",
      "root_cause": "Legacy convenience method; not actively used but presents a risk if wired up.",
      "impact": "Low currently — no active page calls this. Risk if added to a new page without a limit.",
      "suggested_fix": "Add a deprecation comment and require callers to use getMyEntries() or getByUser() instead. Alternatively, add a default limit: `GET /time-tracking?limit=100`.",
      "acceptance_criteria": [
        "timeTrackingService.getAll() is not called in any page",
        "Method has a JSDoc deprecation notice"
      ],
      "verification_command": "grep -rn 'timeTrackingService.getAll()' apps/web/app --include='*.tsx'",
      "related_findings": [
        "PER-020",
        "PER-006",
        "PER-007",
        "PER-008",
        "PER-012"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "medium",
      "notes": ""
    },
    {
      "id": "PER-020",
      "severity": "suggestion",
      "category": "performance",
      "subcategory": "silent-truncation",
      "title": "planning.getOverview hardcodes take:1000 for users and services — silent truncation above 1000",
      "file": "apps/api/src/planning/planning.service.ts",
      "line": "67-70",
      "code_evidence": "const [usersResult, servicesResult, tasksResult] = await Promise.all([\n  this.usersService.findAll(1, 1000, undefined, undefined, {\n    allowFullScan: true,\n  }),\n  this.servicesService.findAll(1, 1000),",
      "description": "The planning overview grid fetches up to 1000 users and 1000 services. Any deployment with more than 1000 active users silently receives a truncated planning grid with no error, no warning, and no pagination meta exposed to the frontend.",
      "root_cause": "The planning grid was designed for medium-sized public administrations. The 1000 cap was chosen as 'safe enough' but is hardcoded with no overflow detection.",
      "impact": "Silent data loss in the planning view for organisations with >1000 users. The frontend renders a partial grid with no indication that rows are missing. Low probability in typical French local government deployments but correctness risk.",
      "suggested_fix": "Add an overflow guard: if `usersResult.meta.total > 1000`, log a warning and optionally surface a `truncated: true` flag in the API response. For large deployments, implement cursor-based pagination or service-scoped filtering for the planning grid.",
      "acceptance_criteria": [
        "When total users > 1000, response includes a truncation warning or error",
        "Log entry emitted at WARN level when truncation occurs",
        "Frontend can display an informational banner when planning data is truncated"
      ],
      "verification_command": "grep -n 'allowFullScan\\|1000\\|findAll' apps/api/src/planning/planning.service.ts",
      "related_findings": [
        "PER-006",
        "PER-007",
        "PER-008",
        "PER-012",
        "PER-019"
      ],
      "cluster_id": "G",
      "found_by": "performance",
      "confidence": "medium",
      "notes": "MEMORY.md PER-021 explicitly documents a 1000-row cap as intentional for scoped task endpoints. This finding is about the planning grid specifically where truncation is invisible to the user."
    },
    {
      "id": "PER-021",
      "severity": "minor",
      "category": "performance",
      "subcategory": "request waterfall",
      "title": "reports/page.tsx calls loadProjects() serially inside loadAnalytics() — creates a waterfall",
      "file": "apps/web/app/[locale]/reports/page.tsx",
      "line": "63-81",
      "code_evidence": "  const loadAnalytics = useCallback(async () => {\n    try {\n      setLoading(true);\n      const params = new URLSearchParams({ dateRange });\n      if (selectedProject !== \"all\") {\n        params.append(\"projectId\", selectedProject);\n      }\n\n      const response = await api.get(`/analytics?${params.toString()}`);\n      const analyticsData = response.data;\n      setData(analyticsData);\n\n      // Load projects for filter if not already loaded\n      loadProjects();\n",
      "description": "`loadProjects()` is called inside `loadAnalytics()` after the analytics response is received. While `loadProjects` does not await (it is called without await), it still adds an extra HTTP round-trip that could have been fired in parallel with the analytics call. Furthermore, `loadProjects` is memoized with `useCallback` but called inside `loadAnalytics`'s dependency chain causing double-execution on every dateRange or selectedProject change.",
      "root_cause": "Projects fetch was added as a convenience inside analytics loading rather than parallelised at component mount.",
      "impact": "Two serial HTTP round trips on every date range change instead of one parallel pair. On slow connections this adds perceptible latency to the reports page.",
      "suggested_fix": "Fire both fetches with `Promise.all([loadAnalytics(), loadProjects()])` inside a top-level effect, or lift `loadProjects` into a mount-only `useEffect(()=>{ loadProjects(); }, [])` (as intended by the comment) and remove it from `loadAnalytics`.",
      "acceptance_criteria": [
        "loadProjects and the analytics fetch are initiated in parallel on first render",
        "loadProjects is not re-called on every dateRange/selectedProject change"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-022",
      "severity": "minor",
      "category": "performance",
      "subcategory": "redundant refetch",
      "title": "dashboard/page.tsx re-fetches all assignee tasks after every status change — full reload on mutation",
      "file": "apps/web/app/[locale]/dashboard/page.tsx",
      "line": "107-143",
      "code_evidence": "  const handleStatusChange = async (taskId: string, newStatus: TaskStatus) => {\n    try {\n      await tasksService.update(taskId, { status: newStatus });\n      toast.success(t(\"tasks.success.statusUpdated\"));\n\n      // Refresh les tâches\n      if (user?.id) {\n        const tasks = await tasksService.getByAssignee(user.id);\n\n        const filteredTasks = Array.isArray(tasks)",
      "description": "After a single task status update, the handler fetches the complete task list for the user again. On a page that also renders a PlanningView (itself loading the planning overview), this compounds the request count.",
      "root_cause": "Optimistic update was not implemented; full re-fetch chosen for simplicity.",
      "impact": "Each status toggle triggers two API calls (update + getByAssignee). With a user who has 50+ tasks, this fetches the full task list on every checkbox toggle. The handleQuickEntrySuccess callback (line 154) already shows the correct optimistic update pattern.",
      "suggested_fix": "Apply the same optimistic update pattern as `handleQuickEntrySuccess`: `setMyTasks(prev => prev.map(t => t.id === taskId ? {...t, status: newStatus} : t))`. Only re-fetch if the mutation fails.",
      "acceptance_criteria": [
        "handleStatusChange updates local state optimistically without an extra network request",
        "On error, revert the local state and show an error toast"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "medium",
      "notes": ""
    },
    {
      "id": "PER-023",
      "severity": "nit",
      "category": "performance",
      "subcategory": "client-side RSC missed opportunity",
      "title": "Most app pages are \"use client\" unnecessarily — no RSC data-fetching benefits",
      "file": "apps/web/app/[locale]/projects/page.tsx",
      "line": "1",
      "code_evidence": "\"use client\";\n\nimport { useEffect, useState, useCallback, useMemo } from \"react\";",
      "description": "The majority of list pages (projects, tasks, users, leaves, reports) are marked `use client` and fetch data entirely on the client via useEffect. In Next.js 16 App Router, RSC could pre-render static structure (headers, filters, shell) server-side and stream content, reducing client-side JavaScript bundle size and time-to-first-paint.",
      "root_cause": "All pages were authored in the classic CSR pattern; RSC migration was not prioritised when upgrading to Next.js App Router.",
      "impact": "No streaming, no server-side data prefetch, no bundle splitting benefit from RSC for data-heavy pages. All JS is shipped to the client and executed on mount.",
      "suggested_fix": "Convert the shell layout and static filter controls to RSC; keep only interactive components (modals, form state) as client components. Use Next.js `loading.tsx` and Suspense boundaries for progressive rendering.",
      "acceptance_criteria": [
        "Pages do not have top-level 'use client' if only parts need interactivity",
        "Data loading uses server-side fetch or TanStack Query's prefetch pattern"
      ],
      "verification_command": "grep -rn '^\"use client\"' apps/web/app --include='*.tsx' | wc -l",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-024",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "medium",
      "notes": ""
    },
    {
      "id": "PER-024",
      "severity": "nit",
      "category": "performance",
      "subcategory": "client-side linear scan",
      "title": "getDayCell() in usePlanningData runs O(N) linear scans over tasks/leaves/events/telework on every cell render",
      "file": "apps/web/src/hooks/usePlanningData.ts",
      "line": "364-516",
      "code_evidence": "    const getDayCell = useCallback(\n      (userId: string, date: Date): DayCell => {\n        const dateStr = format(date, \"yyyy-MM-dd\");\n        const dayPredefinedAssignments = predefinedAssignments.filter(\n          (a) => a.userId === userId && a.date.slice(0, 10) === dateStr,\n        );\n        const dayTasks = tasks.filter((t) => {\n          // Le filtrage des tâches DONE est délégué au filtre légende côté DayCell",
      "description": "getDayCell is called once per (user × day) cell in the planning grid. Each call runs a full `.filter()` pass over the tasks array, leaves array, events array, telework array, and holidays array. For a month view with 30 users × 22 working days, this is 660 cell renders × 5 linear scans = 3300 O(N) operations on every render cycle.",
      "root_cause": "The data was kept as flat arrays for simplicity; no pre-indexing by (userId, date) was applied.",
      "impact": "On the month view with 30+ users, getDayCell is computationally expensive. Combined with React's reconciliation, this can cause noticeable jank when scrolling or changing filters on large planning grids.",
      "suggested_fix": "Pre-index data in a useMemo: build a Map<userId, Map<dateStr, DayCell>> once when the raw arrays change. getDayCell then becomes a O(1) lookup. Example: `const cellIndex = useMemo(() => buildCellIndex(tasks, leaves, events, telework, holidays, predefinedAssignments), [tasks, leaves, events, telework, holidays, predefinedAssignments])`.",
      "acceptance_criteria": [
        "getDayCell performs a Map lookup instead of array.filter()",
        "The cell index is rebuilt only when source data arrays change (memoized)"
      ],
      "verification_command": "N/A — manual verification",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-025"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "high",
      "notes": ""
    },
    {
      "id": "PER-025",
      "severity": "suggestion",
      "category": "performance",
      "subcategory": "missing cache staleTime on analytics",
      "title": "RecentActivity TanStack Query uses staleTime=60s — may re-fetch on every tab switch between advanced analytics sub-components",
      "file": "apps/web/app/[locale]/reports/components/advanced/RecentActivity.tsx",
      "line": "38-42",
      "code_evidence": "  const { data, isLoading, isError } = useQuery({\n    queryKey: [\"analytics\", \"advanced\", \"recent-activity\", { days }],\n    queryFn: () => analyticsService.getAdvancedRecentActivity(days),\n    staleTime: 1 * 60 * 1000,\n  });",
      "description": "RecentActivity has staleTime=60s while WorkloadChart and MilestonesCompletion use staleTime=300s. Switching between the Overview tab and Advanced tab (which unmounts/remounts AdvancedAnalyticsTab since it is tab-conditional rendering) causes RecentActivity to re-fetch every minute. The analytics data changes at a much slower cadence than 60s in practice.",
      "root_cause": "Conservative staleTime choice to keep the widget feeling fresh.",
      "impact": "Minor: extra unnecessary request on tab switch if >60s has elapsed. Low severity but inconsistent with the 5-minute staleTime used by the other two analytics widgets.",
      "suggested_fix": "Align staleTime to 5 * 60 * 1000 (5 minutes) for all advanced analytics widgets, consistent with WorkloadChart and MilestonesCompletion.",
      "acceptance_criteria": [
        "All three advanced analytics components use the same staleTime (5 minutes)"
      ],
      "verification_command": "grep -rn 'staleTime' apps/web/app/[locale]/reports --include='*.tsx'",
      "related_findings": [
        "PER-021",
        "PER-009",
        "PER-010",
        "PER-022",
        "PER-011",
        "PER-013",
        "PER-023",
        "PER-024"
      ],
      "cluster_id": "H",
      "found_by": "performance",
      "confidence": "medium",
      "notes": ""
    }
  ],
  "verified_clean": [
    "apps/api/src/tasks/tasks.service.ts — findAll: safeLimit = Math.min(limit ?? 10, 100) correctly capped at 100 (PER-027)",
    "apps/api/src/tasks/tasks.service.ts — getTasksByAssignee / getTasksByProject: take:1000 cap intentional per PER-021",
    "apps/api/src/tasks/tasks.service.ts — reorderSubtasks: uses transaction array (batch), no sequential await loop",
    "apps/api/src/tasks/tasks.service.ts — importTasks title pre-fetch: single findMany before outer loop (PER-024)",
    "apps/api/src/tasks/tasks.controller.ts — no performance issues; all list endpoints delegate pagination to service",
    "apps/api/src/projects/projects.service.ts — findAll: Math.min(limit ?? 10, 100) capped, uses groupBy for task counts (PER-005/PER-017)",
    "apps/api/src/projects/projects.service.ts — getProjectsByUser: scoped + groupBy fan-out (PER-015) acceptable",
    "apps/api/src/projects/projects.service.ts — getProjectStats: uses aggregate(_sum) not findMany for hours (SA-PERF-018)",
    "apps/api/src/projects/projects.service.ts — getSnapshots: capped at 365 rows with default 90-day window (SA-PERF-017)",
    "apps/api/src/analytics/analytics.service.ts — Redis cache with 60s TTL, user-scoped key prevents cross-user leakage",
    "apps/api/src/analytics/analytics.service.ts — getProjects: capped at PROJECT_DETAILS_LIMIT=50",
    "apps/api/src/analytics/analytics.service.ts — getProjectDetails: parallel groupBy for time entries, no N+1",
    "apps/api/src/leaves/leaves.service.ts — findAll: safeLimit = Math.min(limit || 500, 500) capped at 500",
    "apps/api/src/leaves/leaves.service.ts — allLeaves pre-fetch correctly batched before balance resolution loop",
    "apps/api/src/users/users.service.ts — findAll: HARD_CEILING=200 (or 1000 with allowFullScan flag, explicit opt-in)",
    "apps/api/src/users/users.service.ts — two projection tiers (FULL_LIST_SELECT vs DIRECTORY_LIST_SELECT) limit column count",
    "apps/api/src/common/services/cache.service.ts — fail-open Redis wrapper with try/catch; no stampede at this layer (handled by PermissionsService singleflight)",
    "apps/api/src/rbac/permissions.service.ts — Redis 5min TTL + in-process inflight Map (singleflight) for stampede protection; negative cache 30s TTL",
    "apps/api/src/events/events.service.ts — findAll: safePageSize = Math.min(pageSize || 100, 200) capped at 200; paginated with {data,meta}",
    "apps/api/src/events/events.service.ts — recurrence expansion uses createMany batch insert (PER-005)",
    "apps/api/src/telework/telework.service.ts — findAll: safeLimit = Math.min(limit || 1000, 1000) plus SEC-024 366-day range guard",
    "apps/api/src/telework/telework.service.ts — expandRecurringRulesForRange: bulk findMany + single createMany({skipDuplicates:true}) (PER-028/COR-033)",
    "apps/api/src/time-tracking/time-tracking.service.ts — create: SERIALIZABLE transaction with P2034 retry; ensureDailyCapNotExceeded uses aggregate(_sum) not findMany",
    "apps/api/src/analytics/advanced/services/workload.service.ts — single user.findMany with nested task selects; dedup via Map; no N+1",
    "apps/api/src/analytics/advanced/services/project-health.service.ts — scoped to ACTIVE+user+archived; snapshots take:1; no unbounded fan-out",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts — two-step: resolve project IDs then single scoped snapshot findMany; N+1 avoided",
    "apps/api/src/documents/documents.service.ts — findAll: safeLimit = Math.min(limit ?? 1000, 100) hard cap 100 (PER-043)",
    "apps/api/src/services/services.service.ts — findAll: safeLimit = Math.min(limit || 50, 100) capped at 100",
    "apps/api/src/audit/audit.service.ts — fire-and-forget writes; synchronous advisory lock for hash chain ordering is a documented design decision (PERF-001)",
    "usePlanningData.ts: single aggregated /planning/overview call replaces 9 historical parallel requests — correct batch design",
    "planningService.getOverview: date-bounded API call (startDate/endDate params), no unbounded fetch",
    "useUsers hook (src/hooks/useUsers.ts): uses TanStack Query with 30s staleTime and deduplication",
    "useProjects hook (src/hooks/useProjects.ts): uses TanStack Query with 30s staleTime and deduplication",
    "useTasks hook (src/hooks/useTasks.ts): uses TanStack Query with 30s staleTime and deduplication",
    "WorkloadChart.tsx: TanStack Query with staleTime=5min, bounded limit=15",
    "MilestonesCompletion.tsx: TanStack Query with staleTime=5min",
    "useAuthedAvatar.ts: module-level blob URL cache prevents repeated authenticated fetches for the same avatar",
    "useAuthedAvatar.ts: in-flight deduplication via inflight Map prevents stampede on concurrent renders",
    "dashboard/page.tsx: Promise.all fires projects, tasks, and undeclared tasks in parallel (not serial)",
    "users/[id]/suivi/page.tsx: 7 data fetches use Promise.allSettled (concurrent)",
    "leavesService.getAll(): default limit=100 is reasonable for pagination (not 1000)",
    "clientsService.getAll(): passes limit through query param; projects page calls it with limit=500 explicitly",
    "planning/page.tsx: uses dynamic() with ssr:false for PlanningView (heavy component) — correct code splitting",
    "reports/page.tsx: PortfolioGantt and AdvancedAnalyticsTab are dynamic() imported with ssr:false — correct bundle splitting",
    "dashboard/page.tsx: PlanningView is dynamic() imported with ssr:false — correct bundle splitting",
    "projects/[id]/page.tsx: GanttChart is dynamic() imported — correct code splitting for heavy Gantt library",
    "QueryProvider.tsx: global staleTime=30s configured to reduce redundant refetches across page navigation",
    "tasks/page.tsx: uses limit=200 for getAll (bounded, below 1000, though still high)",
    "getDayCell sorting: filteredTasks.sort and filteredEvents.sort operate on already-small per-cell arrays (not O(N) on full dataset)",
    "getGroupTaskCount: not called on hot render path (only on group header render), low impact",
    "auth.store.ts: permissions array is not persisted to localStorage, backend is always the authority on re-mount"
  ],
  "scanned_files": [
    "apps/api/src/tasks/tasks.service.ts",
    "apps/api/src/tasks/tasks.controller.ts",
    "apps/api/src/projects/projects.service.ts",
    "apps/api/src/analytics/analytics.service.ts",
    "apps/api/src/leaves/leaves.service.ts",
    "apps/api/src/users/users.service.ts",
    "apps/api/src/common/services/cache.service.ts",
    "apps/api/src/rbac/permissions.service.ts",
    "apps/api/src/planning/planning.service.ts",
    "apps/api/src/events/events.service.ts",
    "apps/api/src/telework/telework.service.ts",
    "apps/api/src/time-tracking/time-tracking.service.ts",
    "apps/api/src/analytics/advanced/services/recent-activity.service.ts",
    "apps/api/src/analytics/advanced/services/workload.service.ts",
    "apps/api/src/analytics/advanced/services/project-health.service.ts",
    "apps/api/src/analytics/advanced/services/snapshots-query.service.ts",
    "apps/api/src/analytics/advanced/services/milestones-completion.service.ts",
    "apps/api/src/analytics/advanced/services/tasks-breakdown.service.ts",
    "apps/api/src/documents/documents.service.ts",
    "apps/api/src/audit/audit.service.ts",
    "apps/api/src/services/services.service.ts",
    "apps/api/src/common/services/access-scope.service.ts",
    "apps/api/src/planning-export/planning-export.service.ts",
    "apps/web/app/[locale]/dashboard/page.tsx",
    "apps/web/app/[locale]/planning/page.tsx",
    "apps/web/app/[locale]/reports/page.tsx",
    "apps/web/app/[locale]/reports/components/advanced/AdvancedAnalyticsTab.tsx",
    "apps/web/app/[locale]/reports/components/advanced/WorkloadChart.tsx",
    "apps/web/app/[locale]/reports/components/advanced/RecentActivity.tsx",
    "apps/web/app/[locale]/reports/components/advanced/MilestonesCompletion.tsx",
    "apps/web/app/[locale]/reports/types.ts",
    "apps/web/app/[locale]/projects/page.tsx",
    "apps/web/app/[locale]/projects/[id]/page.tsx",
    "apps/web/app/[locale]/tasks/page.tsx",
    "apps/web/app/[locale]/users/page.tsx",
    "apps/web/app/[locale]/users/[id]/suivi/page.tsx",
    "apps/web/app/[locale]/leaves/page.tsx",
    "apps/web/app/[locale]/time-tracking/page.tsx",
    "apps/web/app/api/[...path]/route.ts",
    "apps/web/app/layout.tsx",
    "apps/web/src/services/analytics.service.ts",
    "apps/web/src/services/auth.service.ts",
    "apps/web/src/services/clients.service.ts",
    "apps/web/src/services/departments.service.ts",
    "apps/web/src/services/events.service.ts",
    "apps/web/src/services/export.service.ts",
    "apps/web/src/services/leaves.service.ts",
    "apps/web/src/services/milestones.service.ts",
    "apps/web/src/services/personal-todos.service.ts",
    "apps/web/src/services/planning.service.ts",
    "apps/web/src/services/planning-export.service.ts",
    "apps/web/src/services/predefined-tasks.service.ts",
    "apps/web/src/services/projects.service.ts",
    "apps/web/src/services/services.service.ts",
    "apps/web/src/services/settings.service.ts",
    "apps/web/src/services/skills.service.ts",
    "apps/web/src/services/tasks.service.ts",
    "apps/web/src/services/telework.service.ts",
    "apps/web/src/services/third-parties.service.ts",
    "apps/web/src/services/time-tracking.service.ts",
    "apps/web/src/services/users.service.ts",
    "apps/web/src/services/leave-types.service.ts",
    "apps/web/src/services/role-management.service.ts",
    "apps/web/src/services/roles.service.ts",
    "apps/web/src/services/permissions.service.ts",
    "apps/web/src/stores/auth.store.ts",
    "apps/web/src/stores/planningView.store.ts",
    "apps/web/src/stores/settings.store.ts",
    "apps/web/src/stores/theme.store.ts",
    "apps/web/src/hooks/useAuthBootstrap.ts",
    "apps/web/src/hooks/useAuthedAvatar.ts",
    "apps/web/src/hooks/usePermissions.ts",
    "apps/web/src/hooks/usePlanningData.ts",
    "apps/web/src/hooks/useProjects.ts",
    "apps/web/src/hooks/useTasks.ts",
    "apps/web/src/hooks/useUsers.ts",
    "apps/web/src/components/planning/UserRow.tsx"
  ]
}