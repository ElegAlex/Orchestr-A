[
  {"id":"PER-001","severity":"blocking","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/analytics/analytics.service.ts","line":145,"title":"Classic N+1 in analytics: per-project task fetch to recompute progress","description":"getProjects() runs projects.map(async p => calculateProjectProgress(p.id)) which fires one prisma.task.findMany({ where: { projectId } }) PER project. With 36 projects in the typical CPAM92 scope, this is 36+1 queries on every /analytics call, returning the same status columns already obtainable via a single groupBy. Each request also re-fetches tasks a SECOND time in getTasks() — full duplicate read of the entire task corpus.","root_cause":"Architectural: per-project async recompute pattern instead of a single SQL groupBy.","suggested_fix":"Replace the .map(async) with a single prisma.task.groupBy({ by: ['projectId', 'status'], where: { projectId: { in: ids }, project: projectWhere } }) and compute progress from the resulting per-project status counts. Drop the duplicate getTasks().","evidence":"return Promise.all(projects.map(async (project) => { const progress = await this.calculateProjectProgress(project.id); ... }));"},
  {"id":"PER-002","severity":"blocking","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/leaves/leaves.service.ts","line":2046,"title":"N+1 on leave balance: 2 queries per leave type","description":"getLeaveBalance() iterates leaveTypes.map(async lt => { resolveAllocatedDays(...); prisma.leave.findMany(approved); prisma.leave.findMany(pending); }). With N leave types active (typically 5-10), this fires 2N parallel leave queries per user — every dashboard mount triggers ~16-20 queries. On a planning page that lists 50 users, this multiplies catastrophically.","root_cause":"Per-type async expansion instead of one groupBy on (leaveTypeId, status) for the user-year window.","suggested_fix":"Replace with prisma.leave.groupBy({ by: ['leaveTypeId', 'status'], where: { userId, startDate: { gte, lte }, status: { in: [APPROVED, CANCELLATION_REQUESTED, PENDING] } }, _sum: { days: true } }) then join in-memory.","evidence":"const balancesByType = await Promise.all(leaveTypes.map(async (lt) => { ... approvedLeaves = findMany(...); pendingLeaves = findMany(...); }))"},
  {"id":"PER-003","severity":"blocking","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/projects/projects.service.ts","line":983,"title":"Daily snapshot cron N+1: findFirst per project then create","description":"captureSnapshots() (called nightly) does projects.map(async p => prisma.projectSnapshot.findFirst({ projectId, date: { gte: startOfDay } })) then serial prisma.projectSnapshot.create(). For P active projects this is 2P queries inside Promise.all. With 100+ projects this hits the DB 200+ times.","root_cause":"Idempotency enforced in app code instead of via @@unique([projectId, dayBucket]) + createMany skipDuplicates.","suggested_fix":"Fetch existing snapshots in one query, build set, then createMany({ data: filtered, skipDuplicates: true }). Add a unique index for true idempotency.","evidence":"results = await Promise.all(projects.map(async (project) => { const existing = await prisma.projectSnapshot.findFirst(...); ... await prisma.projectSnapshot.create(...) }))"},
  {"id":"PER-004","severity":"important","category":"performance","subcategory":"pagination","file":"apps/api/src/users/users.service.ts","line":189,"title":"User listing default limit 1000 — effective unbounded","description":"findAll() signature (page = 1, limit = 20, ...) is overridden by Math.min(limit || 1000, 1000). PlanningService.getOverview calls usersService.findAll(1, 1000) returning up to 1000 users WITH role/department/userServices/managedServices joins, easily 5-20 KB per row → 5-20 MB payload per /planning hit.","root_cause":"Lack of contract on max page size; pagination uses 1000 fallback masking absence of pagination.","suggested_fix":"Hard ceiling at 100 (or 200), require explicit opt-in. Planning view should request a directory-light shape via a dedicated endpoint.","evidence":"const safeLimit = Math.min(limit || 1000, 1000);"},
  {"id":"PER-005","severity":"important","category":"performance","subcategory":"pagination","file":"apps/api/src/projects/projects.service.ts","line":228,"title":"Projects findAll default limit 1000 + N tasks per row","description":"Same 1000 ceiling. Each project row includes tasks: { select: { status: true } } (all tasks, unbounded) just to compute progress in JS. With 50 projects × 200 tasks = 10 000 task rows pulled per request only to count DONE/total. Also includes members: { take: 5 } but clients (no take) is unbounded.","root_cause":"Progress computed in app code instead of via projection or denormalized progress column.","suggested_fix":"Replace tasks include with prisma.task.groupBy. Or rely on the snapshot table.","evidence":"tasks: { select: { status: true } } ... progress: Math.round((tasks.filter(t => t.status === 'DONE').length / tasks.length) * 100)"},
  {"id":"PER-006","severity":"important","category":"performance","subcategory":"pagination","file":"apps/api/src/leaves/leaves.service.ts","line":639,"title":"Leaves findAll: 1000 default + unbounded when date filter present","description":"Same Math.min(limit || 1000, 1000) ceiling. Worse, when startDate/endDate are passed (planning use case) the controller returns the FULL filtered array, no pagination. The where condition triggers a sequential scan because neither startDate nor endDate is indexed (see PER-010).","root_cause":"Mixed return shape and 1000 cap; no hard ceiling for date-windowed queries.","suggested_fix":"Force pagination even on date queries (cap 500 rows/page), expose ?format=stream if export needed. Split planning endpoint from CRUD list.","evidence":"const safeLimit = Math.min(limit || 1000, 1000); ... if (startDate || endDate) { return enrichedLeaves; }"},
  {"id":"PER-007","severity":"blocking","category":"performance","subcategory":"pagination","file":"apps/api/src/planning/planning.service.ts","line":66,"title":"Planning overview multiplies unbounded subqueries","description":"getOverview() requests usersService.findAll(1, 1000), servicesService.findAll(1, 1000), tasksService.findAll(1, 1000, …, startDate, endDate, …) — but tasksService.findAll DISABLES skip/take when a date filter is present, so the 1000 cap is bypassed: ALL tasks intersecting [start, end] are returned. Combined: a single planning load can pull thousands of tasks + 1000 users + 1000 services + leaves + telework + events + holidays + school-vacations + predefined assignments. Payload regularly multi-MB.","root_cause":"Planning overview was bolted on top of CRUD list endpoints rather than a purpose-built aggregated read model.","suggested_fix":"Introduce a dedicated /planning/overview SQL view or Prisma raw query returning only the minimal columns the grid needs.","evidence":"Promise.all([ usersService.findAll(1, 1000), servicesService.findAll(1, 1000), tasksService.findAll(1, 1000, undefined, undefined, undefined, startDate, endDate, undefined, currentUser) ])"},
  {"id":"PER-008","severity":"important","category":"performance","subcategory":"over_fetching","file":"apps/api/src/tasks/tasks.service.ts","line":348,"title":"Tasks findAll bypasses pagination on date filter","description":"...(hasDateFilter ? {} : { skip, take: safeLimit }) — when a planning window is provided, pagination is intentionally disabled. With assignees, project, _count subqueries, each row is ~2-5 KB. A 6-month window can exceed 5000 rows.","root_cause":"Reusing CRUD endpoint for planning where unbounded ranges are legitimate.","suggested_fix":"Keep pagination always on. Introduce /tasks/planning?from&to&userIds= returning minimal grid shape.","evidence":"...(hasDateFilter ? {} : { skip, take: safeLimit })"},
  {"id":"PER-009","severity":"important","category":"performance","subcategory":"over_fetching","file":"apps/api/src/users/users.service.ts","line":2409,"title":"Leave import preview loads ALL active users + ALL pending/approved leaves","description":"On CSV preview, the service does prisma.user.findMany({ where: { isActive: true } }) (no select projection — returns passwordHash) and prisma.leave.findMany({ status: { in: [PENDING, APPROVED] } }) over the whole history.","root_cause":"Bulk import wants global context, but pulls EVERYTHING into Node memory.","suggested_fix":"Project users to {id, email, firstName, lastName} only. Query overlap leaves filtered by the date span detected in the uploaded CSV (min..max).","evidence":"const users = await this.prisma.user.findMany({ where: { isActive: true } }); ... const existingLeaves = await this.prisma.leave.findMany({ ... });"},
  {"id":"PER-010","severity":"blocking","category":"performance","subcategory":"index","file":"packages/database/prisma/schema.prisma","line":569,"title":"Leave model has ZERO indexes — every leaves query is a seq scan","description":"Model Leave defines no @@index at all. The service runs heavy queries filtered by userId, userId + leaveTypeId + status + startDate, validatorId, status IN (...), startDate <= end AND endDate >= start constantly. On a leaves table growing ~1 row/user/leave/year, this is the highest-traffic table for HR features and is fully unindexed.","root_cause":"Schema design oversight — indexes documented for TimeEntry, Task, PredefinedTask but skipped for Leave.","suggested_fix":"Add: @@index([userId, startDate]), @@index([validatorId, status]), @@index([status, startDate]), @@index([userId, leaveTypeId, startDate]), @@index([startDate, endDate]).","evidence":"model Leave { ... @@map(\"leaves\") } — no @@index lines"},
  {"id":"PER-011","severity":"important","category":"performance","subcategory":"index","file":"packages/database/prisma/schema.prisma","line":17,"title":"User table missing indexes on isActive, departmentId, roleId","description":"User table is queried by isActive: true constantly (presence, analytics, workload, leaves import). Also by departmentId (organigramme), and roleId joins. None indexed.","root_cause":"Schema indexes were added selectively; common hot predicates missed.","suggested_fix":"@@index([isActive]), @@index([departmentId]), @@index([roleId]) on User.","evidence":"model User { ... @@map(\"users\") } — no @@index"},
  {"id":"PER-012","severity":"important","category":"performance","subcategory":"index","file":"packages/database/prisma/schema.prisma","line":880,"title":"Event table missing indexes on date, projectId, createdById","description":"Events queries filter by date BETWEEN, projectId, createdById, participants.userId. None have an index. Every /planning hit calls events.findAll with a 6-month window — seq scan.","root_cause":"Same oversight as PER-010/011.","suggested_fix":"@@index([date]), @@index([projectId]), @@index([createdById, date]). Add @@index([userId]) to EventParticipant.","evidence":"model Event { ... date DateTime ... projectId String? ... createdById String ... @@map(\"events\") }"},
  {"id":"PER-013","severity":"important","category":"performance","subcategory":"index","file":"packages/database/prisma/schema.prisma","line":276,"title":"Task missing indexes on endDate/startDate/milestoneId/epicId","description":"Task has only [projectId], [assigneeId], [status]. Planning queries filter on endDate >= and startDate <= heavily. milestoneId/epicId queries (mass updates) also unindexed.","root_cause":"Indexes only target single-column hot filters; composite date filters not anticipated.","suggested_fix":"Add @@index([endDate]), @@index([startDate]), @@index([milestoneId]), @@index([epicId]), @@index([projectId, status]).","evidence":"@@index([projectId]) @@index([assigneeId]) @@index([status])"},
  {"id":"PER-014","severity":"important","category":"performance","subcategory":"cache","file":"apps/api/src/rbac/permissions.service.ts","line":55,"title":"Role-permissions cache stampede risk + no negative caching","description":"getPermissionsForRole reads Redis, falls back to Prisma. There is NO single-flight or lock — under a burst, all concurrent requests hit prisma.role.findUnique simultaneously. Missing/unknown role returns [] but is NOT cached — every request for a bad/legacy roleCode hits Postgres forever.","root_cause":"Cache-aside without coalescing; absent rows never memoized.","suggested_fix":"Wrap DB lookup with an in-process Map<roleCode, Promise> singleflight. Cache empty results with shorter TTL. On templateKey mutation, scan role-permissions:* and DEL in pipeline.","evidence":"const cached = await this.redis.get(cacheKey); if (cached) return JSON.parse(...); ... role = await this.prisma.role.findUnique(...)"},
  {"id":"PER-015","severity":"important","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/leaves/leaves.service.ts","line":102,"title":"Repeated service+managedService+userService pattern (4 queries) on every leaves list call","description":"The trio managedServices + userServices + usersInServices appears 5 times for every leaves endpoint — for every request, the user's perimeter is recomputed from scratch via 3 SQL hits. Combined with PER-014, every leaves request triggers 4+ supplementary lookups before the main query.","root_cause":"No memoized AccessScope per request.","suggested_fix":"Cache the user's managedUserIds set on the request (REQUEST-scoped provider) or in Redis with 60s TTL. Better: a single SQL CTE/raw query computing the userIds in one round trip.","evidence":"const managedServices = await this.prisma.service.findMany({ where: { managerId: currentUserId } }); const userServices = await this.prisma.userService.findMany({ ... }); const usersInServices = await this.prisma.userService.findMany({ ... })"},
  {"id":"PER-016","severity":"important","category":"performance","subcategory":"over_fetching","file":"apps/api/src/users/users.service.ts","line":1197,"title":"getUsersPresence loads 4 large tables in parallel without paging","description":"Fetches ALL active users, ALL telework for the day, ALL approved leaves intersecting the day, ALL external tasks with nested assignees, ALL external events for the day with participants. Five unindexed queries fan out. For an org of 1500 users this is multi-MB on a feature that polls regularly.","root_cause":"Single endpoint denormalizes 5 sources; no incremental loading.","suggested_fix":"Convert to a Postgres view or compute via raw query joining only the userIds with status (PRESENT/REMOTE/ABSENT/EXTERNAL).","evidence":"users = findMany(isActive:true) ; teleworkSchedules = findMany(...) ; leaves = findMany(...) ; externalTasks = findMany(...) ; externalEvents = findMany(...)"},
  {"id":"PER-017","severity":"important","category":"performance","subcategory":"frontend_render","file":"apps/web/app/[locale]/dashboard/page.tsx","line":243,"title":"Dashboard does 3 sequential client-side fetches in one useEffect — render storm + waterfall","description":"useEffect dependency on [user] triggers fetchData which serially fetches projectsService.getByUser, tasksService.getByAssignee, tasksService.getMyDoneUndeclared (3 awaited round-trips, not Promise.all). The page is 'use client' so not SSR'd. No TanStack Query, no cache between navigations.","root_cause":"Convention of 'use client' + useEffect + axios instead of RSC + server fetch or TanStack Query.","suggested_fix":"Promise.all the three independent fetches. Better: convert to a server component. Or migrate to TanStack Query with staleTime ≥ 30s.","evidence":"useEffect(() => { const fetchData = async () => { ... projects = await projectsService.getByUser(...); tasks = await tasksService.getByAssignee(...); undeclared = await tasksService.getMyDoneUndeclared(); ... }; }, [user]);"},
  {"id":"PER-018","severity":"important","category":"performance","subcategory":"frontend_render","file":"apps/web/app/[locale]/projects/page.tsx","line":133,"title":"Projects page has 4 stacked useEffect with overlapping data fetches","description":"4 useEffect blocks (lines 133, 138, 146, 158) — classic effect-chain. Each one's setState retriggers downstream effects, causing N renders per page mount. No TanStack Query.","root_cause":"Effects used as data-flow orchestrator; should be Query keys or RSC fetches.","suggested_fix":"Audit each useEffect for true side-effect vs derived state. Convert all data-loading effects to useQuery with proper keys.","evidence":"apps/web/app/[locale]/projects/page.tsx:133:  useEffect(() => { ... 138: useEffect(... ; 146: useEffect(...; 158: useEffect(..."},
  {"id":"PER-019","severity":"important","category":"performance","subcategory":"frontend_render","file":"apps/web/app/[locale]/dashboard/page.tsx","line":243,"title":"TanStack Query barely used (3 components total) — no client-side cache","description":"Grep across apps/web for useQuery|staleTime returns hits only in 3 advanced reports components. All other pages use raw axios + useEffect + useState. Every page mount refetches, no dedup, no background revalidation, no cross-page sharing.","root_cause":"TanStack Query was added late and never globally rolled out.","suggested_fix":"Standardize on TanStack Query for ALL list/detail endpoints. Set sensible defaults (staleTime 30-60s, retry 1). Provide useUsers/useProjects/useTasks hooks layer.","evidence":"grep -rn 'staleTime|useQuery' apps/web → only 3 files, all under reports/components/advanced"},
  {"id":"PER-020","severity":"important","category":"performance","subcategory":"bundle","file":"apps/web/next.config.ts","line":1,"title":"next.config.ts has no image domains, no bundle optimization, no analyzer","description":"Config is minimal: only sets standalone output and security headers. No images config, no experimental.optimizePackageImports, no modularizeImports for icon/util libraries, no @next/bundle-analyzer.","root_cause":"Config never tuned beyond defaults.","suggested_fix":"Add experimental.optimizePackageImports: ['lucide-react', '@radix-ui/react-icons', 'date-fns']. Wire up @next/bundle-analyzer.","evidence":"const nextConfig: NextConfig = { output: 'standalone', devIndicators: false, async headers() { ... } };"},
  {"id":"PER-021","severity":"suggestion","category":"performance","subcategory":"bundle","file":"apps/web/app/[locale]/projects/[id]/page.tsx","line":41,"title":"Only GanttChart is code-split — analytics dashboards loaded eagerly","description":"Grep finds exactly ONE next/dynamic usage. Reports/AdvancedAnalyticsTab, PortfolioGantt, PlanningView are heavy yet imported statically.","root_cause":"No systematic split policy for heavy/optional UI.","suggested_fix":"Wrap heavy chart/grid/Gantt components in dynamic(() => import(...), { ssr: false }). Audit imports >50 KB gzipped via bundle analyzer.","evidence":"grep -rn 'dynamic(' apps/web → only apps/web/app/[locale]/projects/[id]/page.tsx:49"},
  {"id":"PER-022","severity":"important","category":"performance","subcategory":"aggregation","file":"apps/api/src/time-tracking/time-tracking.service.ts","line":619,"title":"User/project time reports do reduce() aggregations in JS instead of SQL groupBy","description":"getUserReport and getProjectReport fetch ALL entries via findMany then loop to build byType, byProject, byDate maps in memory. On annual exports this materializes thousands of rows + heavy joins only to sum hours. SQL can do it in a single groupBy.","root_cause":"Aggregation in Node instead of pushed to Postgres.","suggested_fix":"Use prisma.timeEntry.groupBy({ by: ['activityType'], _sum: { hours: true }, where }) and equivalent for project/date.","evidence":"const entries = await this.prisma.timeEntry.findMany(...); const totalHours = entries.reduce(...); const byType = entries.reduce(...);"},
  {"id":"PER-023","severity":"important","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/tasks/tasks.service.ts","line":1101,"title":"Two-round-trip + extra groupBy on every getByAssignee call","description":"getByAssignee fetches all tasks with deep include (project, assignee, assignees.user, _count for 4 relations), then issues a SECOND query timeEntry.groupBy({ by: ['taskId'] }). With assignees + 4 _count subqueries, a single user with 80 tasks generates 1 main query + 4 implicit count subqueries (Prisma LATERAL) + 1 groupBy. The dashboard calls this on every mount.","root_cause":"No caching on user-scoped derived views; eager include of expensive _counts.","suggested_fix":"Cache result in Redis with key tasks:assignee:<userId> TTL 30-60s, bust on task mutation. Drop unused _count fields.","evidence":"const tasks = await this.prisma.task.findMany({ where: { ... }, include: { _count: { select: { dependencies: true, dependents: true, raci: true, comments: true } } } }); const sums = ... timeEntry.groupBy(...)"},
  {"id":"PER-024","severity":"important","category":"performance","subcategory":"n_plus_one","file":"apps/api/src/events/events.service.ts","line":192,"title":"Recurring events: createMany then immediate findMany to wire participants","description":"When creating a recurring event with participants, the code does createMany for child events, then prisma.event.findMany({ parentEventId }) to get their IDs back, then eventParticipant.createMany. With a yearly weekly recurrence (52 occurrences) × 30 participants = 1 select + 1560-row insert. The entire block runs synchronously.","root_cause":"Recurrence expansion done synchronously at request time.","suggested_fix":"Either (a) use prisma.event.create in a loop with participants.create nested; (b) push expansion to a BullMQ/queue worker; (c) materialize occurrences lazily on read.","evidence":"await this.prisma.event.createMany({ data: occurrences }); ... const childEvents = await this.prisma.event.findMany({ where: { parentEventId: event.id } }); ..."},
  {"id":"PER-025","severity":"important","category":"performance","subcategory":"aggregation","file":"apps/api/src/analytics/analytics.service.ts","line":277,"title":"Analytics projectProgressData / taskStatusData filter in JS over full task array","description":"getProjectProgressData maps projects then tasks.filter(t => t.projectId === project.id).length — O(P × T) work in app code. getTaskStatusData runs 5 separate filter() passes. With T=10 000 tasks and P=100, a million comparisons per request.","root_cause":"Computing aggregates over a giant in-memory array instead of asking Postgres.","suggested_fix":"Replace with prisma.task.groupBy({ by: ['projectId', 'status'], _count: true }) and assemble both views from a single grouped result.","evidence":"projects.map(project => ({ ..., tasks: tasks.filter(t => t.projectId === project.id).length })); ..."},
  {"id":"PER-026","severity":"suggestion","category":"performance","subcategory":"cache","file":"apps/api/src/auth/jwt-blacklist.service.ts","line":1,"title":"Redis usage limited to JWT blacklist + role-permissions — no caching on heavy reports","description":"Only two services use Redis. Hot expensive endpoints — analytics, planning overview, users presence, leaves balance, project lists — re-execute their full Prisma plans on every request.","root_cause":"No cache strategy outside auth/RBAC.","suggested_fix":"Introduce a thin CacheService and wrap: /analytics (60s), /planning/overview (15-30s), /users/presence (30s), /leaves/balance/:userId (60s, bust on mutation).","evidence":"find apps/api/src -name '*.ts' | xargs grep -l 'redis' → only jwt-blacklist.service.ts, permissions.service.ts"},
  {"id":"PER-027","severity":"important","category":"performance","subcategory":"other","file":"apps/api/src/analytics/analytics.service.ts","line":62,"title":"Analytics /reports payload is unbounded — confirms documented regression","description":"Per MEMORY: 'dateRange is not a scope filter'. getAnalytics ignores dateRange entirely — response covers FULL accessible project scope. AnalyticsResponseDto.projectDetails has one entry per project (36+ per scope), each containing full task aggregate and time entry sums. NO upper bound: tenant-admin sees ALL projects.","root_cause":"Reports endpoint conflates 'period filter' with 'project filter'.","suggested_fix":"Cap projectDetails to top-N (e.g. 50) with 'see all' paginated endpoint. Add take to the projects findMany.","evidence":"projects findMany has no take. Code comment: 'incident: 36 projects in scope, only 7 displayed'."},
  {"id":"PER-028","severity":"nit","category":"performance","subcategory":"frontend_render","file":"apps/web/src/components/planning/PlanningGrid.tsx","line":1,"title":"Planning grid components: 17 useEffect vs only 20 useMemo/useCallback across the planning folder","description":"Ratio suggests row-level rendering may not be memoized. Gantt-like grids with N users × M days × event lookups need stable references. Without row-level React.memo + stable selectors, every store update repaints the entire grid.","root_cause":"Component composition not optimized for big-grid rendering.","suggested_fix":"Wrap DayCell, row, and group-header in React.memo with shallow-equal props. Move derived per-cell state into useMemo. Profile with React DevTools Profiler.","evidence":"grep useEffect in apps/web/src/components/planning → 17 ; grep React.memo|useMemo|useCallback → 20"},
  {"id":"PER-029","severity":"suggestion","category":"performance","subcategory":"background_job","file":"apps/api/src/analytics/advanced/snapshot-scheduler.service.ts","line":20,"title":"Daily snapshot cron runs in-process on the API — risk of blocking event loop","description":"@Cron('0 23 * * *') runs captureSnapshots() directly in the API node. With PER-003's N+1 it can take a multi-second event-loop hit nightly. If multiple API replicas are running, all replicas run the cron simultaneously → duplicate findFirst races and 2-3x DB load.","root_cause":"No singleton lock for crons in a horizontally-scaled NestJS deployment.","suggested_fix":"Either run cron on a single 'scheduler' replica (env flag), or use a Redis-based lock (SET NX EX). After PER-003 fix, latency drops below 1s.","evidence":"@Cron(SNAPSHOT_CRON, { timeZone: SNAPSHOT_TZ }) async captureDailySnapshots() { ... await this.projectsService.captureSnapshots(); }"},
  {"id":"PER-030","severity":"nit","category":"performance","subcategory":"over_fetching","file":"apps/api/src/leaves/leaves.service.ts","line":681,"title":"Leave list includes full leaveType object instead of selected fields","description":"include: { leaveType: true } pulls every column of LeaveTypeConfig for every leave row. The UI only needs {id, code, name, color, icon}. With pagination cap = 1000 leaves × ~10-15 leaveType columns = wasted bandwidth.","root_cause":"include instead of select boilerplate.","suggested_fix":"Replace with leaveType: { select: { id: true, code: true, name: true, color: true, icon: true } }.","evidence":"include: { ... leaveType: true, ... }"}
]
