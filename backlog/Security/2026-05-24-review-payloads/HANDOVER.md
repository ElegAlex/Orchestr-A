# ORCHESTRA Remediation — Session Handover

## Context
Repo: /home/alex/Documents/REPO/ORCHESTRA (NestJS + Prisma + Next.js monorepo). User: Alexandre, DSI CPAM 92 (communicates in French; code/docs in English). Read `CLAUDE_SESSION_CONTRACT.md` first. Master-only (no PRs). Prod = VPS (see memory `project_prod_server_access`).

## Current state
- **Prod deployed HEAD: `6dc8e1c8`** (BIG-SYNC migration deploy 2026-06-04 15:25 UTC — see §Next top entry; api image `ab024a9a`, rollback anchor `orchestra-api:pre-deploy-6dc8e1c8` = `2cde14fe`; ledger 74 rows/73 distinct; RELEASE_SHA truthful first-time). Supersedes SEC-015 `6e461aa`. **Prior `6e461aa` detail:** SEC-015, deployed 2026-05-31 ~20:57 UTC; detached checkout, RELEASE_SHA + `deployments` row + `RELEASE_DEPLOYED` audit all `6e461aa` first-time, `deployedBy ab@alexandre-berge.fr`, node v22.22.3; api image `48fe7900`; rollback anchor `orchestra-api:pre-sec015` (`ebddc73e`, the SEC-011 image); no migration (migrations=56). Chronology of recent prod HEADs: `9b528d9` (SEC-006+013) → `9046234` (SEC-014) → `f329c36` (SEC-009) → `6c597a6` (SEC-010) → `07afdebc` (SEC-011) → `6e461aa` (SEC-015) → **`6dc8e1c8` (BIG-SYNC + OBS-028, current 2026-06-04)**. NOTE: the old `6e461aa`/SEC-015 RELEASE_SHA env was later found MISLABELED vs the in-image code; trust the in-image migration set, not the env label. See §Next per-task records for each deploy's evidence/rollback.
- **All runtime deltas now live.** SEC-006 (login lockout), SEC-013 (real client IP), SEC-007 (password policy), and **SEC-014 (refresh-cookie `__Host-` hardening, deployed 2026-05-31, prod HEAD `9046234`, api img `c3b28075`)** deployed and smoke-verified (see §Next for evidence). Everything else on master is docs/backlog-only. **SEC-014 transition follow-up:** remove the legacy-name fallback in `readRefreshCookie` after one `JWT_REFRESH_TTL` window (7d) → ~2026-06-07.
- Two stray `docker compose run` leftover containers on the box (`orchestra-api-run-b72b172396ef`, `-71414d8cfef4`, up 7d/4w) — not the live service (that's `orchestr-a-api-prod`); harmless but worth pruning in a maintenance pass.
- BACKLOG coherence gate green for all DONE entries; `audit_logs` 5-layer defense-in-depth live in prod.

## Process learnings (the two load-bearing ones)
- **#15 — `@Global()` discovery before module wiring.** Before adding an `imports[]` to a NestJS module for an RBAC/audit service, check for `@Global()` first (`grep -r "@Global()" apps/api/src`). `RbacModule` is `@Global()`, so `PermissionsService` injects everywhere without an explicit import — adding one is a smell.
- **#17 — Operator-control invariant: no silent substitution, no auth forgery as a default.** When the operator picks an option from a surfaced menu, execution follows that pick. If mid-execution you judge another option better for a concrete reason, HALT and re-surface — do not substitute silently, even with good intent. (Origin: smoke-auth method on the 2026-05-29 micro-deploy.)

## Next
**Next: 🛠️ 2026-06-04 ADVERSARIAL REMEDIATION PASS — PARTIAL DONE (2026-06-05, LOCAL only — no push/deploy). Full manifest: `backlog/Security/2026-06-04-adversarial-review/MANIFEST-2026-06-05.md`.** Phase 1 + Phase 2 (code-verifiable) of that cycle's `BACKLOG.md`; Phase 3/4/5 EXCLUDED. **183 in-scope → 103 DONE (95 folded with code+RED→GREEN witness, gate-green · 8 reconciled-away/anchor `fe91b806`+`99511a42`) · 80 still TODO (all Phase-2).** Coherence gate green; whole-repo gate green; **integration suite green (29 files / 130)** — covers the DB/schema closes. ALL Phase-1 blockers done; ALL wave-0 + wave-1 FIXED findings folded (every API service group + DTOs + main.ts + web pages + skills pilot); DB-inline batch done (indexes, FK-Restrict, DAT-008/013/015/016 CHECK/TRUNCATE/login). **NOT done: Playwright E2E (CLAUDE.md mandate) for the folded authz set — known gap.**
- **Still-open (80, see manifest §3-5):** (a) **audit-emit cluster** OBS-004/006/008/009/010/012/014/015/016/017 — do the cross-module audit-action.enum + payload-schema + entity-map BATCH FIRST, then per-service emit (OBS-016/017 emit committed in users via a `'USER_CREATED' as AuditAction` cast, runtime-safe entityType→'Auth', convert in the batch; **OBS-014 over-closed in d7a2bd76 then REOPENED**); (b) 4 DB-inline (DAT-011/012 recurrence CHECK, SA-DAT-002 validatedBy-snapshot=decision, SA-DAT-005 hash-chain test guard); (c) ~66 waves-2-5 + stragglers (SA-PERF-013 cache-invalidation, SA-OBS-008/009 metrics) — authoring scripts staged at `/tmp/recon/wave{2..5}_script.js` + `wave_{2..5}.json`.
- **Engine (validated, reusable):** parallel worktree Sonnet authors (blind — no node_modules) → spec+src patches on disk → orchestrator folds serially: spec-first RED → source GREEN → whole-repo gate → multi-`[closes]` commit + Closed_by follow-up. Gotchas: worktrees branch from session-start HEAD (already-touched files → `git apply --reject` + manual merge); controller-signature changes break sibling controller specs; blind patches can have TS errors (import-type, DayPeriod) + cross-module breaks (events→planning) — gate catches them; **a src patch can CREATE a new file at a non-obvious path (swagger-basic-auth.ts) — always `git add -A <area>` + verify HEAD builds standalone (`git stash -u; gate; pop`); web findings can need backend completion (SEC-029/OBS-018/OBS-019 done in f7bd2096)**; verify each agent persisted its patches (metrics agent failed StructuredOutput → re-authored inline). **Fence: LOCAL ONLY — deploy is a separate explicitly-authorized step.**

**Next: 🗂️ AUDIT MERGE DONE (2026-06-05, LOCAL only — no push/deploy). Both 2026-06-04 adversarial runs reconciled into ONE prioritized remediation backlog.** New cycle dir `backlog/Security/2026-06-04-adversarial-review/` (sibling to this 2026-05-24 dir; same `CLAUDE_SESSION_CONTRACT.md` rules). `BACKLOG.md` = **329 tasks** = 268 primary (optimized run, canonical) + 61 sessionA-only; **38** sessionA findings deduped into a primary entry (cross-linked, never dropped). **13** tagged `requires-live-verification` (capped at `important` — TLS edge, CI/E2E boot, prod env, deploy-time locks; **not blocking on static evidence alone**). SEC-001 carried as `important` (prod terminates TLS at an out-of-repo certbot host nginx; reframed residue = HSTS missing + TLS-terminator IaC drift + weak `ssl_protocols` floor). Coherence gate green (0 DONE/VERIFIED yet); `### ` blocks = 329; deterministic generator + gate copy live under that dir's `scripts/`.
- **Remediate audit-derived findings via an orchestrated Dynamic-Workflow** (map per cluster → serial gate-green fold), lean/fenced, **LOCAL ONLY**. **Start with the leave-delegation authz bypass** (`COR-001`, Phase 1, cross-validated — `canValidate` delegation not scoped to the leave's assigned validator). Then the rest of Phase 1 (`COR-002` non-transactional task import, `COR-003` telework auto-expand write, `DAT-001` audit `ON DELETE SET NULL`, `DAT-002` `TaskRACI.userId` no FK), then Phase 2 important clusters.
- **`requires-live-verification` items (Phase 3) need a live check BEFORE fix/rating** — confirm the claim against the running system first, then re-rate; never promote to blocking on static evidence. **No deploy without explicit per-task operator authorization.**
- **Dedup scope caveat:** overlap with THIS (2026-05-24) cycle was NOT deduped — some 2026-06-04 items may already be DONE/superseded here; reconcile during the verify pass.

**Next: 🌐 WEB REDEPLOY DONE (2026-06-04 18:20 UTC, authorized web-only) — COR-039 LIVE, web now HEALTHY.** Shipped `a5b7080c` (the COR-039 redirect fix) to prod. Pushed `7c5e1ae5`→**`56a93f20`** (delta verified = ONLY COR-039 code `a5b7080c` + docs; no other gated code rode in). Web rebuilt from origin/master HEAD `56a93f20`, image **`af33682e`** stamped `orchestra-web:56a93f20`. Rollback anchor **`orchestra-web:pre-deploy-56a93f20`** (img `5a19ac45` = the unhealthy `7c5e1ae5` web). `up -d --no-deps web` (web ONLY); **api `7f8ecc72`/started-15:25:28 + postgres `55c2a684` proven UNCHANGED + healthy**; no api/DB/.env/migration/**nginx** touched.
- **Smoke ALL PASS (fresh REAL login, operator creds):** **redirect restored** — `/`→307 `/fr`, `/` (Accept-Language: en)→307 `/en`, `/fr/login`+`/fr/dashboard`=200; **web container flipped `unhealthy`→`HEALTHY`** in ~12s (the COR-039 goal — `/` healthcheck now passes, clearing the `nginx depends_on web:service_healthy` landmine). **Render-under-CSP re-validated:** dashboard/congés/projets render, **0 console errors / 0 CSP violations**, 0 failed/cross-origin resources (matches the prior SEC-CSP-001 QA — `a5b7080c` didn't touch CSP). **Avatars display** (blob, decoded 2048²; `useAuthedAvatar` preserved). `/api/health`=200 (api untouched).
- **Carried (out of THIS task's scope — no nginx change allowed):** the **duplicate nginx CSP header** (`script-src 'self' 'unsafe-inline'`) still rides alongside the web nonce CSP — functionally clean + still secure (intersection blocks non-nonced inline), but SEC-CSP-001's intended single-source CSP needs the nginx HTML-CSP removed in a separate nginx.conf change. SEC-CSP-001 itself = ratify-or-incident operator decision (its pre-deploy QA was bypassed by `7c5e1ae5`; the live-CSP browser QA came back CLEAN). **In flight: none.**

**Next: COR-039 CLOSED (2026-06-04, LOCAL only — no push/deploy). Restores the bare-`/` locale redirect the web deploy lost.** Root cause (from git history, not a blind re-add): the SEC-CSP-001 middleware rewrite (`785e6617`) passed next-intl a plain `new Request(request,{headers})` in its **prod** branch — a plain `Request` has no `.nextUrl`, which next-intl needs for the `/`→`/<locale>` redirect, so prod `/` 404'd (dev branch passed the real `NextRequest`, masking it). This 404 also failed the compose `/` healthcheck → web `unhealthy` → `nginx depends_on web:service_healthy` gate. Fix `a5b7080c` (`[closes COR-039]`, Closed_by set by this follow-up): `apps/web/middleware.ts` prod branch now builds `new NextRequest(request,{headers})` (preserves `.nextUrl`), restoring full next-intl routing incl. Accept-Language detection; **SEC-CSP-001 nonce logic untouched** (no pre-empt/collision; `csp-nonce.test.ts` green). Witness `middleware-locale-redirect.test.ts` RED→GREEN. Verified by a local prod run: `/`→307 `/fr` (`/en` under `Accept-Language: en`), `/fr/*`=200, `wget --spider /`→rc 0 (healthcheck now passes). Gate green (web jest 657 / build / lint 0-err / coherence). **In flight: none.** **→ This fix is on master LOCAL only; the web container stays `unhealthy` on prod until a follow-up web redeploy (SEPARATE authorized step) ships `a5b7080c`.**
> ⚠️ **STATE CORRECTION (was mislabeled): SEC-CSP-001 is NOT "blocked/undeployed" anymore — its middleware (the per-request nonce CSP, prod path included) went LIVE on prod with the `7c5e1ae5` web cutover (2026-06-04 17:17).** What was actually skipped is SEC-CSP-001's *required pre-deploy gate* — operator sign-off + a real-browser white-screen/CSP-violation QA — which the web cutover bypassed inadvertently. The post-cutover browser smoke (login/dashboard/Congés/Projets, 0 console errors) is partial evidence the CSP isn't white-screening, but it is NOT the formal QA. **OPERATOR DECISION (separate from COR-039): either formally ratify the now-live SEC-CSP-001 CSP after a proper browser-QA pass, or treat the bypass as an incident.** When the COR-039 web redeploy ships `a5b7080c`, its browser smoke MUST include a render-under-CSP check (console clean / no CSP violations), not just status codes — the standalone-build local run could not check CSP (no static assets served).

**Next: 🌐 WEB PROD DEPLOY DONE (2026-06-04 17:17 UTC, authorized web-only cutover) — avatars FIXED.** Closed the api/web gap left by the BIG-SYNC api-only deploy. Web rebuilt from origin/master HEAD **`7c5e1ae5`** (code-identical to the api's `6dc8e1c8` — only docs differ), image **`5a19ac45`** stamped `orchestra-web:7c5e1ae5`. Rollback anchor **`orchestra-web:pre-deploy-7c5e1ae5`** (img `124c2362`, the 8-day-old web). Deploy: `git checkout --detach 7c5e1ae5` → `build web` (build-cache prune first freed ~8 G; disk 6.6 G→16 G) → **`up -d --no-deps web`** (web ONLY). **api `7f8ecc72`/started-15:25:28 + postgres `55c2a684` proven UNCHANGED + healthy** (id+StartedAt identical before/after); no .env/DB/migration touched; **no push** (origin already had `7c5e1ae5`).
- **Smoke PASS (fresh REAL login, operator creds — stale 2h token cleared first):** avatars now **DISPLAY** — `GET /api/uploads/avatars/843555cf….png` → **200** (was 401), `<img>` `src=blob:` decoded **2048×2048**, **0 console errors** (the 2 avatar-401s are GONE); `useAuthedAvatar` blob+Bearer path live. Dashboard + Congés ("Gestion des congés") + Projets (~35) render with real data, 0 console errors each.
- **⚠️ REQUIRED FOLLOW-UP (one-line; not a rollback) — web container is `unhealthy`.** The new build returns **404 at bare `/`** (publicly too: `curl /`=404; `/fr`,`/fr/login`,`/fr/dashboard`=200), but the compose healthcheck probes `/` (`wget --spider`), so it fails → web `unhealthy`. The OLD web redirected `/`→`/fr` (app-side middleware, since lost), which is why it was healthy. **No active incident:** RestartPolicy `unless-stopped` does NOT act on unhealthy (`restarts=0`), nginx/web keep serving (`/fr/login`=200, `/api/health`=200). **The exposure:** `nginx depends_on web: condition: service_healthy` → the **next deliberate nginx recreate** (`down`+`up`, `--force-recreate`, nginx-config change) will **error and refuse** until web is healthy again (it won't touch the already-running nginx; a host reboot/daemon restart is fine — `depends_on` health gating is compose-up-time only; cert-renew is fine if it `nginx -s reload`/SIGHUP). **Fix (either, both out of this task's scope = compose/code edit + rebuild):** (a) **band-aid** — repoint the web healthcheck `/`→`/fr` in `docker-compose.prod.yml` (clears unhealthy + the nginx gate; leaves bare-domain `/`=404); (b) **complete** — restore the `/`→`/fr` locale redirect in web middleware + rebuild web (fixes healthcheck AND bare-domain UX; architecturally correct — the app owns the redirect, nginx still passes `/` through untouched).
> This HANDOVER update is committed **LOCAL-only (un-pushed)** — the web task forbade `git push`. Push with the next authorized push.

**Next: 🚀 BIG-SYNC PROD DEPLOY DONE (2026-06-04 15:25 UTC, authorized cutover). Prod HEAD `6e461aa`→`6dc8e1c8`.** First **migration-bearing** prod deploy; closed the ~248-commit gap between prod and master in one controlled api-only image swap. Now LIVE: OBS-027/028 (audit PII minimisation + `AUDIT_HASH_KEY` HMAC), DAT-008/026 (full-erasure + anonymised shell), SEC-016 (authed uploads hook), and the whole DAT-/PER-/COR- schema-hardening batch via the 11 pending migrations.
- **Blocker found + fixed FIRST (operator-approved):** `docker-compose.prod.yml` had **no `AUDIT_HASH_KEY` passthrough** in the api `environment:` block → an `.env.production` value would never reach the container → OBS-028 `assertAuditHashKey` would crash-loop the new image. OBS-028 shipped the boot gate but not the compose wiring. One-line fix committed `6dc8e1c8` (`AUDIT_HASH_KEY: ${AUDIT_HASH_KEY:?AUDIT_HASH_KEY is required}`, mirrors the `JWT_SECRET` `:?` pattern). **THIS is the deployed HEAD** (`574635c5` + the fix). Caught by a compose-propagation grep before any prod write.
- **Order (RUNBOOK):** pre-cutover `pg_dump` rollback point **`backups/pre-deploy-6dc8e1c8-20260604-151652.sql.gz`** (388K, sha256 `f55c44ed…`, gzip-t OK, dump-complete; data unchanged since 14:06 so the dry-run stayed valid) → `git push origin master` (174 commits, ff `5ec65d71→6dc8e1c8`; docker-publish.yml is **tags-only** so push triggered no CI deploy) → `.env.production`: appended `AUDIT_HASH_KEY` (`openssl rand -hex 32`, 64 chars, STABLE/long-lived, **value never echoed**) + stamped `RELEASE_SHA=6dc8e1c8` (**only these 2 keys**; backup `.env.production.bak-predeploy-6dc8e1c8`) → VPS `git checkout --detach 6dc8e1c8` (compose fix confirmed present post-checkout) → `build api` (img **`ab024a9a`**) → `up -d --no-deps api` (entrypoint ran `prisma migrate deploy` on boot).
- **Migration 11/11 applied clean on boot** (per011/012/013, dat025, dat022, dat028, add_task_confidential_flag, add_cancelled_to_leave_status, dat029, dat008_026, dat026); ledger 63→**74 rows / 73 distinct applied**; `prisma migrate status` = **"Database schema is up to date!"**. De-risked beforehand by a full throwaway dry-run on real prod data (zero COR-014-class failure; cor014 dual-row ledger needed **no** `migrate resolve`).
- **RELEASE_SHA now TRUTHFUL `6dc8e1c8`** (the prior `6e461aa0` env was MISLABELED — actual prior live code was bounded `[e9dc6953..<129da90b]`, proven via in-image migration set). `deployments` row + `RELEASE_DEPLOYED` audit both `6dc8e1c8` first-time, `deployedBy ab@alexandre-berge.fr`, node v22.22.3. **Rollback anchor `orchestra-api:pre-deploy-6dc8e1c8`** (img `2cde14fe`); rollback = retag→latest + repin `RELEASE_SHA=6e461aa0` + `up -d --no-deps api` (11 migs additive, old code forward-compatible); restore the pre-cutover dump only if data integrity is in doubt.
- **Smoke ALL PASS:** health `{status:ok,db:ok,redis:ok}`; LOGIN_SUCCESS 200 (audit `entityId` = user id `843555cf`, **not** login); authed reads leaves(congés)/projects/auth-me all 200; LOGIN_FAILURE 401 (audit `entityId` = 64-hex **HMAC**, `is_raw=f`; raw smoke identifier appears **nowhere** in audit_logs); hash-chain newest-5 `chain_ok=t`; audit_logs 108→111 (+RELEASE_DEPLOYED/+LOGIN_SUCCESS/+LOGIN_FAILURE); error-log scan clean. One transient boot Redis-ping 503 then 200 (warm-up); healthy by ~24s.
- **⚠️ Web NOT rebuilt** (api-only per authorized scope). **BROWSER-VERIFIED post-deploy** (Playwright, operator creds, https://orchestr-a.com): login page + login + dashboard + **Congés** (7 pending + real entries) + **Projets** (all 35 with descriptions) ALL render with real data; **every data API 200** (auth/me/permissions, settings, projects/user, personal-todos, tasks/*, leave-types, planning/overview) — **NO shape-drift breakage** despite the 174-commit gap. **ONE cosmetic regression:** user avatars `GET /api/uploads/avatars/*.png` → **401** (the only failing request) because **SEC-016's backend auth-hook is now live but its frontend `useAuthedAvatar` blob-loading is in the un-rebuilt web** — old `<img src>` can't send a Bearer → 401 → falls back to initials. Not app-breaking; **operator follow-up: rebuild/redeploy web** (fixes avatars + closes the api/web gap). **`AUDIT_HASH_KEY` rotation breaks historical HMAC correlation — keep it stable.** Two stray `orchestra-api-run-*` orphans still on the box (prune in a maintenance pass). **In flight: none.** This deploy resolves the OBS-028 "PROD DEPLOY DEPENDENCY" warning below and the "prod sync is a separate authorized task" items (SEC-016, DAT-008/026, all schema migrations).

**Next: OBS-028 CLOSED (2026-06-04, LOCAL only — no push/deploy).** Closed the two remaining PII gaps from OBS-027 — FORWARD-ONLY, no migration, OBS-002/existing rows untouched. (A) `LOGIN_FAILURE`/`ACCOUNT_LOCKED` `entityId` is now a **keyed HMAC-SHA256(AUDIT_HASH_KEY, trim+lowercase(identifier))** instead of the raw attempted login/email — deterministic (forensic correlation per target) but not dictionary-reversible; bcrypt-shaped → 'unknown'; OBS-013 stdout digest unchanged. (B) `hardDelete`'s anonymised-shell path now wipes `passwordHash` (NOT NULL → `'!ANONYMIZED-NO-LOGIN!'`). New `AUDIT_HASH_KEY` validated fail-closed at boot in **every** env (`assertAuditHashKey`, main.ts). Fix `fa83599d` (`[closes OBS-028]`), Closed_by set by this follow-up. Witnesses RED→GREEN (audit.service.spec HMAC + correlation, audit-hash-key.spec boot-fail, users.service.spec passwordHash wipe); audit-immutability.int.spec.ts green. Gate green (2040 api unit + 126 int + types + lint + build). **In flight: none.**
> ⚠️ **PROD DEPLOY DEPENDENCY (operator):** `AUDIT_HASH_KEY` (≥32 chars, `openssl rand -hex 32`, STABLE/long-lived) MUST be set in prod `.env.production` BEFORE this deploys — the app **refuses to boot** without it. `.env.production` was NOT touched (separate authorized step). Rotating the key later breaks historical hash-correlation. Existing raw `LOGIN_FAILURE` entityIds + historical `USER_DELETED` snapshots remain (immutable — operator/DPO call).

**Next: OBS-027 CLOSED (2026-06-04, LOCAL only — no push/deploy).** PII minimisation in the immutable audit trail (RGPD) — follow-up to the DAT-008/026 anonymised-shell decision: since `audit_logs` are immutable (OBS-002) + retained forever, any identifier written there survives user erasure. Primary-source enumeration of EVERY audit writer flagged 5 emitters; all made **reference-only** (opaque id): `USER_DELETED` snapshot dropped email/login/firstName/lastName/avatarUrl/avatarPreset (keeps id/roleId/departmentId/isActive/timestamps); `LOGIN_SUCCESS`/`REGISTER`/`PASSWORD_CHANGED`(reset-token gen + admin reset) `details` now use `${user.id}` not `${user.login}`. `LOGIN_FAILURE`/`ACCOUNT_LOCKED` entityId (attacker-controlled attempted login, OBS-013-governed) deliberately kept + documented. **FORWARD-ONLY** — existing rows + OBS-002 trigger untouched, no migration; existing historical `USER_DELETED` snapshots still hold identifiers (immutable, operator/DPO call). Fix commit `d8743e51` (`[closes OBS-027]`), Closed_by set by this follow-up. Witnesses RED→GREEN (users.service.spec snapshot, auth.service.spec LOGIN_SUCCESS details); `audit-immutability.int.spec.ts` green. Gate green (2035 api unit + 126 int + types + lint + build). **In flight: none.** Prior state below.

**Next: DAT-008/026 CLOSED (2026-06-04, operator decision A′, LOCAL only — no push/deploy).** Full-erasure user deletion + anonymised-shell for audit-bearing users. Fix commit `db1efdfa` (`[closes DAT-008]` `[closes DAT-026]`), Closed_by set by this follow-up. **In flight: none** — gate green (2035 api unit + 126 int + types + lint + build). **Pending prod sync (separate authorized task):** 2 new LOCAL-dev migrations (`20260604103344` secondary-FK SetNull, `20260604110510` `User.deletedAt`) + the schema change. See the DAT-008/026 delta below for details. Prior cleanup-pass state retained below.

**Next: POST-RUN CLEANUP PASS COMPLETE (2026-06-04, LOCAL only — no push/deploy).** Cleared everything in-hand from the full-backlog run except operator-only items. DELTA:
- **SEC-016 — CLOSED `2b23a19` (`[closes SEC-016]`), Closed_by doc `16faa24`.** Real fix: authenticated `onRequest` hook on `/api/uploads/*` (`common/fastify/uploads-auth.hook.ts`, gated on a valid Bearer access token via `app.get(JwtService)`; `@fastify/static` kept so traversal confinement stays) + `UserAvatar`/`useAuthedAvatar` blob-loading on the frontend. Witness proves anon GET 200→401 (stash-RED). Live smoke (dev :4000): anon 401 / bad-token 401 / valid-token+file 200 / missing 404. **Follow-up `5be23c8`: the hook now passes CORS `OPTIONS` preflights through (it ran before `enableCors()` and was 401-ing the preflight of the cross-origin authed blob fetch → would have broken avatar loading in dev; invisible in prod same-origin). Live-verified preflight → 204 + ACAO :4001; anon GET still 401. Gate green (api vitest 2035 + lint + build).** **NOT deployed — prod sync is a separate authorized task.**
- **Lint — FIXED + re-enabled `cd9466f`.** Root cause of the run-wide exclusion: the root pnpm `ajv:^8` override forced v8 onto `eslint`/`@eslint/eslintrc` which need the v6 API → crash. Fix = scoped nested overrides `eslint>ajv` + `@eslint/eslintrc>ajv` → `^6.12.6` (global v8 kept for angular-devkit). Then prettier `--fix` tree-wide + ~28 hand-fixes (incl. a genuine parsing bug: unescaped `@map'd` apostrophe in `dat011-fk-indexes.int.spec.ts`) + `^_` no-unused-vars ignore pattern matching the codebase convention. **`pnpm lint` now exits 0 (0 errors; ~1148 `no-unsafe-*`/`require-await` stay `warn` per existing config). Lint is BACK IN THE STANDING GATE** (gate = tests + types + **lint** + build). **Caveat (honest): the api `lint` script is `eslint … --fix`, so in the gate it hard-fails only on NON-auto-fixable errors (the real lint rules) and silently auto-fixes the prettier/formatting class rather than failing — a CI run would rewrite files and pass. To make the gate fail-closed on formatting too, add a no-`--fix` `lint:check` script; not done here to avoid changing the pre-existing script semantics.**
- **Re-verified 4 judgment-call non-closes from primary source `257de22`** — all KEPT FAILED with grounded reasoning in their Learnings: COR-011 (already fixed by PER-006 4b2fd004, uniform {data,meta}), COR-025 (mutual-exclusion non-defect), COR-033 (backend-enforced at update:1239/remove:1502, non-defect), DAT-027 (Description's 3 indexes present via DAT-011+PER-012; only the advisory `[projectId,date]` composite + `@db.Date` note unimplemented).
- **e2e — partially validated, runtime left OPERATOR-VERIFIED.** Browser binaries present (Chromium 1217) → headless boots. Harness COMPILES: `playwright test --list` → **4746 tests / 45 files** (validates the structural-only folds + TST-E2E-001 produce runnable code). Stood up a throwaway `orchestr_a_v2_e2e` DB (migrated + seeded the 6 role users). **Full browser run blocked locally (env-specific, not test defects):** (a) `next.config.ts` has NO `/api` rewrite, so plain `next dev` on :4001 can't serve `auth.setup`'s `:4001/api/auth/login` — the local harness assumes a fronting proxy (nginx/compose stack); (b) `globalSetup` POSTs `/api/testing/reset` which **truncates** the target DB (would wipe dev data — avoided via the throwaway DB); (c) **FINDING: `pnpm db:seed` is currently broken** — `seed.ts:1620` TS error (`E2E_SEED` env key not declared on `ProcessEnv`), ts-node typecheck fails; worked around with `ts-node --transpile-only`. The seeded `orchestr_a_v2_e2e` DB is left in place (reusable).
- **DAT-008/026 — NOW CLOSED `db1efdfa` (`[closes DAT-008]` `[closes DAT-026]`), 2026-06-04.** Superseded the decision-prep: operator chose A′ (full erasure of PII + owned operational data in ALL cases; audit-bearing users → anonymised shell keeping the immutable audit trail; no retention; **audit_logs/OBS-002 untouched**). Implemented: 12 secondary User-referencing FKs → nullable+SetNull (mig `20260604103344`) so an actor leaving never blocks (was Restrict→P2003) or over-deletes others'/shared data (was Cascade); `User.deletedAt` + `@@index([deletedAt])` (mig `20260604110510`) as the shell marker; `hardDelete` branches trail-less→physical delete / audit-bearing→anonymise-in-place (email/login tombstone keyed by id, firstName/lastName tombstone constant [NOT NULL], avatarUrl/preset null + on-disk file removed, deletedAt set, isActive=false); `checkDependencies` no longer blocks (audit count informational); findAll/findOne filter `deletedAt:null`. DAT-022 confirmed needs no change (User→Department Restrict, already closed `0ac82e3`). Witnesses RED→GREEN (secondary-FK + anonymised-shell int specs, unit specs). Spec `DAT-008-026-COMPLIANCE-SPEC.md` superseded to A′. LOCAL only — NOT deployed.

**Still failed/halted after this pass:** COR-011/025/033, DAT-027 (honest non-closes); DAT-019/020, TST-019/021 (prior run's other non-closes, untouched); SEC-CSP-001 (BLOCKED — needs operator sign-off + prod browser QA; a passing local e2e would NOT authorize folding/deploying it); SEC-014-CLEANUP (time-gated, earliest 2026-06-07). *(DAT-008/026 are no longer here — CLOSED `db1efdfa`, see the newer §Next entry above.)* **Operator follow-ups:** SEC-016 prod deploy; fix `seed.ts:1620` so `pnpm db:seed` runs without `--transpile-only`; prod schema sync (now incl. the 2 DAT-008/026 migrations `20260604103344` + `20260604110510`).

---

**Prior: FULL-BACKLOG autonomous run COMPLETE (2026-06-03→04) — 114 folded / 8 failed / 1 manual / 2 HALTED-operator / 1 skipped = 126.** Whole-tree `build`+`test` green, coherence gate green, schema drift probe empty. See `RUN_MANIFEST.md` for the per-task breakdown + caveats. **OPERATOR ACTIONS PENDING:** (1) **DAT-008 + DAT-026** HALTED awaiting your compliance spec (PII fields / retention window / reversibility) — not auto-decided; (2) **SEC-016** FIXED LOCAL 2026-06-04 (`2b23a19`, authenticated uploads hook + UserAvatar blob-loading) — NOT deployed, prod sync is a separate authorized task; (3) e2e folds verified STRUCTURALLY (no browser boot) — runtime e2e is operator-verified; (4) **SEC-CSP-001** needs browser white-screen QA before deploy; (5) all 17 schema migrations are LOCAL-dev only — **prod sync is a separate authorized task** (prod also still missing per011/012/013). `lint` excluded run-wide (ESLint/ajv crash on master). Prior pointer retained below.
**Prior: auto-pick the next Phase-6/Cluster-J or lowest-Phase TODO** — or SEC-CSP-001 when an operator opts into the web/nginx CSP work. **SEC-015 is DONE + DEPLOYED to prod 2026-05-31 (`6e461aa`)** — see its record below; cluster J Phase-6 trio (SEC-010/SEC-011/SEC-015) is fully closed and live. Deploy live-verified by a self-contained avatar upload→delete cycle on a throwaway user (right file removed, 8 real avatars untouched, prod restored to baseline).

---

**✅ SEC-016 closed `2b23a19` (`[closes SEC-016]`) 2026-06-04 — LOCAL only, NOT deployed.** The remaining "MANUAL follow-up" from the full-backlog run. `@fastify/static` served `/api/uploads/*` as raw Fastify routes bypassing Nest's global guards → anon GET `/api/uploads/avatars/<uuid>.<ext>` = 200. Real fix (multi-file scope authorized): new `common/fastify/uploads-auth.hook.ts` `onRequest` hook registered before `@fastify/static` (main.ts), gating every `/api/uploads/` request on a valid Bearer access token via `app.get(JwtService)`; `@fastify/static` kept (retains its `../` traversal confinement). Scope = authenticated-only (matches avatarUrl already being returned to every authenticated user; signature+expiry only). Frontend: an `<img>` cannot carry a Bearer header, so `UserAvatar` + new `useAuthedAvatar` hook load uploaded avatars via the authenticated axios client → blob → object URL (preset/external pass through; object URLs cached at module scope, not revoked — shared across mounted components, bounded by distinct avatars, cleared on logout reload). Witness `uploads-auth.hook.spec.ts` (real Fastify + `@fastify/static` + `inject()`, no DB): control suite locks anon-200, fix suite asserts anon→401 / Bearer→200 / bad→401; stash-RED proven (`expected 200 to be 401`). Gate green: api `vitest` 2034 + web `jest` 654 + `nest build` + `pnpm build`; lint of touched files clean after the ajv tooling fix (this session). **Live smoke (dev :4000):** anon→401, bad Bearer→401, valid Bearer+file→200, valid+missing→404, `/api/health` reached. AC#4 N/A (read-only). No migration. **Deploy: separate authorized task** (SEC-016 not yet on prod).

---

**✅ SEC-015 closed `d31b8c0` (`[closes SEC-015]`) 2026-05-31 — DEPLOYED to prod 2026-05-31 (`6e461aa`).** `deleteAvatar` derived the FS path by prefix-stripping the DB-stored `avatarUrl` (`replace(/^\/api\//,'')`) and `join(process.cwd(), …)` then `fs.unlink` → arbitrary file deletion if a traversing value (`/api/../../../etc/passwd`) ever reached the DB. **Layered on SEC-010** (which locked `avatarUrl` at the write layer with the anchored matcher and its sweep found 0 traversal values → live exploit already neutralized); this hardens `deleteAvatar` so it's safe regardless of input validation — NOT closed as "already done". **Primary-source check:** `uploadAvatar` names files `${userId}${ext}` (ext ∈ `.jpg`/`.png`/`.webp` from the real MIME extMap), userId-named not random → reconstruct from userId. **Fix shape:** `deleteAvatar` no longer reads `avatarUrl` at all (removed the now-dead `findUnique`, in-scope — it existed only to feed the vulnerable derivation); it mirrors `uploadAvatar`'s own `readdir(uploadsDir)` + `startsWith(userId + '.'/'_')` cleanup (readdir entries are bare basenames, cannot traverse) rather than iterate an extension whitelist — equally secure but won't orphan a legacy `userId_*` file. Belt-and-suspenders `resolve()` + `startsWith(resolvedUploadsDir + sep)` boundary guard before every unlink so even a malformed userId can't escape the dir. **AC#4 N/A** confirmed — self-service file I/O, not in the audit-sensitive list; no audit emit added. **Witness** (`users.service.spec.ts`, red→green proven by running against unfixed code first): a stored `avatarUrl` crafted to traverse (injected directly, bypassing SEC-010) → unlink never escapes uploads dir (RED captured pre-fix, GREEN post-fix); plus a normal case deleting the reconstructed in-dir file by userId stem and leaving other users' files. **No avatar-delete e2e exists** (avatar-unification/avatar-screenshots are render-only; security/upload-validation only tests POST rejection) — the upload→delete behavioral guard lives in the unit witness. **No migration.** Gate green: full API `vitest` (1789) + `npx nest build`; lint = known env breakage, skipped. **🚀 DEPLOYED 2026-05-31 ~20:57 UTC.** Prod before→after: git HEAD `07afdebc`→`6e461aa` (detached); RELEASE_SHA `07afdebc`→`6e461aa` (repinned in `.env.production` BEFORE build per runbook → container env + boot `deployments` row + `RELEASE_DEPLOYED` audit all read `6e461aa` first-time, 20:56:54, `deployedBy ab@alexandre-berge.fr`, node v22.22.3); api image `ebddc73e`→`48fe7900`. **No migration** (migrations=56, unchanged baseline). Code-only micro-deploy (`build api` → `up -d --no-deps api`, `--env-file .env.production`, `docker-compose.prod.yml`); healthy after 3rd poll (2× 502 during recreate then 200), boot log clean. Rollback anchor `orchestra-api:pre-sec015` (`ebddc73e`, the SEC-011 image); rollback = repin RELEASE_SHA `07afdebc` + `up -d --no-deps api` that tag (no migration to undo). Pre-deploy `pg_dump` `backups/pre-sec015-20260531-205452.sql.gz` (447 KB, 13785 lines — note: first dump attempt produced an empty 20-byte gz because the DB user/name was guessed `orchestra/orchestra`; real creds are `orchestr_a`/`orchestr_a_prod`, redone valid). **Dist verified structurally** (comments are stripped, so checked behavior not the comment): running `/app/apps/api/dist/users/users.service.js` `deleteAvatar` carries the `readdir(uploadsDir)` + `startsWith(userId + '.'/'_')` reconstruction + `resolve()`/`sep` boundary guard, and the old `replace(/^\/api\//` derivation is **gone** (grep count 0). **Live smoke PASS — self-contained throwaway-user upload→delete cycle (NOT the operator's account: the uploads dir holds 8 REAL user avatars incl. the operator's `843555cf-….png`, so a real-account cycle would have wiped a real avatar):** forged an admin JWT (HS256, real prod `JWT_SECRET`, operator sub `843555cf`, role claim cosmetic — strategy re-reads role from DB) → `POST /api/users` `roleCode:OBSERVATEUR_DSI` (non-system/assignable) → **201** active user `86c2967c`; forged THAT user's JWT (forge not login — a login would emit a `LOGIN_SUCCESS` audit row with the test user as *actor* and block the cleanup hard-delete; avatar ops emit no audit so the forged-token cycle leaves zero audit rows) → `POST /api/users/me/avatar` (1×1 PNG, 70 B, valid magic) → **200** `avatarUrl:/api/uploads/avatars/86c2967c-….png`, file confirmed present in the dir → `DELETE /api/users/me/avatar` → **200** `avatarUrl:null`, file confirmed **GONE** and the dir **diff-identical to the 8-file baseline** (the 8 real avatars untouched — proves it reconstructs-from-userId and removes ONLY that file) → cleanup `DELETE /api/users/86c2967c/hard` → **200** "supprimé définitivement". Final invariant: `0` `smk-sec015%` users remaining — prod fully restored to baseline. **Cluster J Phase-6 trio (SEC-010/SEC-011/SEC-015) now fully closed AND live.**

---

**⤳ SEC-023 SUPERSEDED → re-scoped to SEC-CSP-001 2026-05-31 (audit premise empirically falsified; NO CODE, NO DEPLOY).** Re-derived against live prod (operator chose "reframe & correct audit only"). The audit's "Helmet vs nginx CSPs intersect, weakest survives → inline allowed; fix = make Helmet sole CSP, remove nginx's" is **wrong on the mechanism AND a security regression**: (1) per CSP spec multiple headers are each enforced → **strictest** wins, not weakest (`curl -kI .../api/health` shows Helmet `script-src 'self'` + nginx `'unsafe-inline'` both present → inline **blocked** on `/api`, which is JSON anyway); (2) the real XSS surface is the **frontend `/`**, which carries **only nginx's** CSP (`script-src 'self' 'unsafe-inline'`) — **Helmet runs on the API only and never reaches the Next.js frontend** (`web:3000`; `next.config.ts` sets headers but no CSP), so "remove nginx CSP, keep Helmet" would leave the frontend with **NO CSP** — worse. `main.ts` Helmet `scriptSrc` is already `'self'`-only (line 71). (3) Strict frontend `script-src` white-screens the app: served HTML has **6 inline hydration `<script>` (no `src`, no `nonce`)**; stock nginx can't mint nonces → the nonce must come from `apps/web/middleware.ts`. (4) Audit missed a **third** weak CSP at `nginx/nginx.conf:160`. **Correct fix surface = `nginx/nginx.conf` + `apps/web/middleware.ts` (nonce-based), NOT `main.ts`** — captured as new TODO **SEC-CSP-001** (cross-validated, Phase 5, Blocked_by none). Real fix is a two-surface deploy (nginx reload + web image) with white-screen risk → **mandatory real-browser smoke**; TST-E2E-001 drift won't catch it. **Next: SEC-CSP-001** when an operator opts into the web/nginx CSP work. AC#4 N/A. Backlog coherence gate stays green (SEC-023 left non-DONE, no Closed_by required).

---

**✅ SEC-011 closed `07afdebc` (`[closes SEC-011]`) 2026-05-31 — NOT deployed (HOLD, write-path change).** `CreateUserDto` exposed `@IsOptional @IsBoolean isActive?: boolean` (default true) → a `users:create`/`users:import` caller could mint pre-activated accounts, bypassing approval. **Fix = operator Model A override** (NOT the entry's "Suggested fix" of default-false + a `users:activate` route): admins create active users directly; the only change is the **caller can no longer set `isActive` on create — the server controls it.** (1) Removed `isActive` from `CreateUserDto` (was `create-user.dto.ts:116`, stale `:91` pointer) + the now-unused `IsBoolean` import. (2) **CRITICAL PartialType cascade handled:** `UpdateUserDto extends PartialType(CreateUserDto)`, so removing `isActive` from create would also drop it from update and break the admin deactivate/reactivate PATCH — so `UpdateUserDto` now **re-declares `isActive` directly** (`@IsOptional @IsBoolean`); the `USER_DEACTIVATED`/`USER_REACTIVATED` emits are fully preserved (existing service tests still green). (3) `users.service.create()` hardcodes `isActive: true` (was `createUserDto.isActive ?? true`). (4) Import flow already safe: `ImportUserDto` is a separate DTO with no `isActive`, set server-side in `importUsers()` — out of scope. **⚠️ `forbidNonWhitelisted` interaction (deviates from the literal AC#2 "ignored" wording):** the global `ValidationPipe` (main.ts:96) has `whitelist + forbidNonWhitelisted` → once `isActive` leaves the DTO, a create payload carrying it is **rejected 400** ("property isActive should not exist"), NOT silently stripped. Stronger guarantee, same purpose (caller value never honored). **The deploy smoke must expect 400, not "created-but-active".** **AC#4 N/A** — Model A emits no audit on creation (account born active). **Witness:** `create-user.dto.spec.ts` SEC-011 block runs the **exact** global pipe — `isActive` on create → 400, absent → passes (no `isActive`), `isActive` on update → validates; `users.service.spec.ts` adds a defense-in-depth check that `create()` forces `isActive:true` even when a caller smuggles `isActive:false` past the type. FAILS pre-fix, PASSES post-fix. **No migration.** Gate green: `pnpm test` (1787) + `npx nest build`. Lint = known env breakage, skipped. **e2e NOT run live** — stack (api/web :4001) down this session; booting it is disproportionate for a DTO-scope change whose HTTP-validation behavior is exercised by the DTO-pipe witness (same pipe config) + controller/service specs; post-deploy smoke covers the real path. **🚀 DEPLOYED 2026-05-31 ~20:30 UTC.** Prod before→after: git HEAD `6c597a6`→`07afdebc` (detached); RELEASE_SHA `6c597a6`→`07afdebc` (repinned in `.env.production` BEFORE build per runbook → container env + boot `deployments` row + `RELEASE_DEPLOYED` audit all read `07afdebc` first-time, 20:29:58, `deployedBy ab@alexandre-berge.fr`); api image `fd2cad8`→`ebddc73e`. **No migration** (DTO validation). Code-only micro-deploy (`build api` → `up -d --no-deps api`, `--env-file .env.production`); healthy after 4th poll, `https://orchestr-a.com/api/health`→200, boot log clean (only the `RELEASE_DEPLOYED` row). Rollback anchor `orchestra-api:pre-sec011` (`fd2cad8`, the SEC-010 image); rollback = repin RELEASE_SHA `6c597a6` + `up -d --no-deps api` that tag (no migration to undo). Pre-deploy `pg_dump` `backups/pre-sec011-20260531-202554.sql.gz` (437 KB). **Live smoke PASS — NON-MUTATING** (forged ADMIN_DSI JWT for `alexandre.berge` `843555cf`, both payloads 400 at validation so nothing persists): `POST /api/users` `{…valid…, email:"not-an-email", isActive:false}`→**400** `["property isActive should not exist","email must be an email"]` (proves caller-supplied `isActive` is now rejected); control `POST /api/users` `{…valid…, email:"not-an-email"}` (NO isActive)→**400** `["email must be an email"]` only (isolates the rejection to isActive). Counter-invariant verified: user count `41`→`41`, `0` `smk-sec011%` users. **Mutating happy-path ALSO live-verified on operator request (create→patch→hard-delete, prod left clean):** `POST /api/users` (valid, NO isActive, role `CONTRIB_DEV` — only institutional/non-system roles are assignable in prod) → **201** with `isActive:true` (server default, born active); `PATCH /api/users/:id {isActive:false}` → **200** `isActive:false` (deactivation path intact); a `USER_DEACTIVATED` audit row was emitted with `actorId` = the admin (AUD-EMIT-001 preserved); cleanup `DELETE /api/users/:id/hard` → **200** "supprimé définitivement". Final counter-invariant: users `41`→`41`, `0` `smk-sec011-live%` left — prod fully restored to baseline. So **both the security fix (isActive→400) and the create-201-active / PATCH-deactivate regressions are live-verified.** **Sibling cluster J Phase-6 TODO remains: SEC-015** (avatar-delete path traversal).

---

**✅ SEC-010 closed `6c597a6` (`[closes SEC-010]`) 2026-05-31 — DEPLOYED to prod 2026-05-31.** `CreateUserDto.avatarUrl` was `@IsOptional @IsString` → a `users:create`/`users:import` caller could store `javascript:alert(1)`, a `data:`/`file:` URI, or an external tracking-pixel host; rendered as `<img src>`/`background-image` across the UI → stored XSS / cross-origin leak / SSRF-from-browser. **Fix diverges from SEC-009 (NO `@IsUrl`):** every avatar is server-issued by `users.service.uploadAvatar` as `/api/uploads/avatars/<id>.<ext>` — there is NO Gravatar/OAuth/external-avatar flow, and a stored-value sweep (dev=1, prod=7, seed=0) found ONLY that shape. So `avatarUrl` → `@Matches(AVATAR_URL_PATTERN)` + `@MaxLength(256)`, regex `^/api/uploads/avatars/[A-Za-z0-9_-]+\.[A-Za-z0-9]+$` — accepts only the server-issued relative path. **Strongest fix:** zero external hosts (kills tracking-pixel/SSRF), and by forbidding `/` after the prefix + a single literal dot it also denies the `../` traversal that feeds **SEC-015**'s avatar-delete path. **PATCH covered free:** `UpdateUserDto extends PartialType(CreateUserDto)` (witnessed). **Deploy-safety (the de-risker):** frontend create/update payloads (`users/page.tsx`) NEVER include `avatarUrl` — avatars use dedicated `/users/me/avatar` upload/preset/delete endpoints that bypass the DTO → no empty-string/round-trip PATCH can 400; the server's own value matches the matcher. **AC#4 N/A** — avatarUrl validation emits no new audit row. No e2e sends avatarUrl through the DTO (avatar e2e uses upload/preset → regex-matching values), so it can't regress e2e. **Witness** (`dto/create-user.dto.spec.ts`): `javascript:`/`data:`/`file:`, external `https` host, and `../` traversal → `matches`; server-issued jpg/png/webp + omitted → 0 errors; one `UpdateUserDto` case proves propagation — the 5 rejection cases FAIL pre-fix (`@IsString` accepts any string), all 8 PASS post-fix (verified by stash). **No migration.** Gate green: `npx nest build` + full API `vitest` (1782). Lint = known env breakage, skipped. - **🚀 DEPLOYED 2026-05-31 ~19:50 UTC.** Prod before→after: git HEAD `f329c36`→`6c597a6` (detached); RELEASE_SHA `f329c36`→`6c597a6` (repinned in `.env.production` BEFORE build per runbook → container env + boot `deployments` row both read `6c597a6` first-time, 19:50:45); api image `8f1095d`→`fd2cad8`. **No migration** (DTO validation). Code-only micro-deploy via `docker-compose.prod.yml` (`build api` → `up -d --no-deps api`, `--env-file .env.production`); healthy after 4th poll, `https://orchestr-a.com/api/health`→200, boot logs clean. Rollback anchor `orchestra-api:pre-sec010` (`8f1095d`, the SEC-009 image); rollback = repin RELEASE_SHA `f329c36` + `up -d --no-deps api` that tag (no migration to undo). Pre-deploy `pg_dump` `backups/pre-sec010-20260531-194837.sql.gz` (446 KB). **Live smoke PASS** (forged ADMIN_DSI JWT for `alexandre.berge`, all NON-MUTATING — each returns 400, none persists): `POST /api/users` `avatarUrl=javascript:alert(1)`→**400** `"avatarUrl must be a server-issued avatar path (/api/uploads/avatars/<file>)"`; external `https://evil.com/pixel.png`→**400** same (confirms relative-only); valid relative `/api/uploads/avatars/<id>.png` + invalid email→**400** with **only** `"email must be an email"` (NO avatarUrl error → relative value accepted, nothing persisted). Counter-invariant verified: `0` `smk%` users created. **NOTE on prior state:** live prod was at `f329c36` (SEC-009), NOT `9046234`/SEC-014 — the §top "prod HEAD 9046234" line was stale; SEC-014/019/021 are git ancestors of `f329c36` so they were already live. **Sibling cluster J Phase-6 TODOs remain:** SEC-011 (`isActive` writable on create) and SEC-015 (avatar-delete path traversal — now partially blocked at the write layer by this matcher, but the `deleteAvatar` path-derivation fix still stands).

---

**✅ SEC-009 closed `6ffc405` (`[closes SEC-009]`) 2026-05-31 — DEPLOYED to prod 2026-05-31.** `CreateDocumentDto.url` was `@IsString @IsNotEmpty` only → a `documents:create` user could store `javascript:alert(document.cookie)`, rendered as `<a href={doc.url}>` (stored XSS). Fix (`dto/create-document.dto.ts`): `url` → `@IsUrl({protocols:['http','https'],require_protocol:true,require_tld:true})` + `@MaxLength(2048)` (blocks `javascript:`/`data:`/`file:`); `mimeType` → `@IsIn(ALLOWED_DOCUMENT_MIME_TYPES)` (pragmatic office/image/text set anchored on `application/pdf`, **no canonical app source exists** — no upload pipeline, no frontend allowlist; `image/svg+xml` excluded, inline-script). **PATCH path covered for free:** `UpdateDocumentDto extends PartialType(CreateDocumentDto)`; **no non-DTO ingest** (create/update both `...dto`→Prisma). `require_tld:true` ships safe — every existing/test document URL is external `https` with a TLD (swept e2e/seed/specs; dev DB 0 rows; no documents e2e exists). **AC#4 N/A** — `create()` emits no audit today (only `findOne` emits `DOCUMENT_READ`), validation adds none. **Witness** (`dto/create-document.dto.spec.ts`): `javascript:`/`data:`/`file:`→`isUrl`, >2048→`maxLength`, bad mime→`isIn`, normal https→0 errors, + one `UpdateDocumentDto` case proving propagation — FAILS pre-fix (`javascript:` accepted), PASSES post-fix. **No migration.** Gate green: `pnpm test` (1774) + `npx nest build`. Lint = known env breakage, skipped. **Sibling:** SEC-010 (`avatarUrl`) is the same stored-XSS class — same `@IsUrl` treatment applies.
- **🚀 DEPLOYED 2026-05-31.** Prod before→after: git HEAD `f0cd0b1`→`f329c36`; RELEASE_SHA `f0cd0b1`→`f329c36` (repinned in `.env.production` BEFORE build per runbook → container env + boot provenance both read `f329c36` first-time); api image `6bef86a`→`8f1095d`. **No migration** (DTO validation). Build→up only via **`docker-compose.prod.yml`** (`build api` → `up -d --no-deps api`, `--env-file .env.production`) — NOTE: the API/web/nginx services live in `docker-compose.prod.yml`, NOT the default `docker-compose.yml` (which has only postgres/redis); a `compose build api` without `-f docker-compose.prod.yml` fails "no such service: api". Healthy first poll, `https://orchestr-a.com/api/health`→200. Rollback image retained `orchestra-api:pre-sec009` (`6bef86a`, the prior SEC-021 image); rollback = repin RELEASE_SHA + `up -d` that tag (no migration to undo). **Live smoke PASS** (forged ADMIN_DSI JWT for `alexandre.berge`, real project): `POST /api/documents` `url=javascript:alert(1)`→**400** `"url must be a URL address"`, valid `https`→**201**, `mimeType=application/x-evil`→**400**; the 201 smoke doc was DELETEd after (200) → prod clean.

---

**✅ SEC-022 closed `ef9f0b6` (`[closes SEC-022]`) 2026-05-31 — MITIGATED BY SEC-014, guard-test only, NO DEPLOY (no behavior change).** Audit premise was STALE: the documented vector (a credentialed cross-origin `POST /auth/refresh` attaches the victim's refresh cookie because CORS is `credentials:true`, and `/auth/refresh` reads the token from body OR cookie) is structurally dead — **SEC-014 set the refresh cookie to `SameSite=Strict` UNCONDITIONALLY** in `buildRefreshCookie` (`auth.controller.ts:89`; only `Secure` is prod-gated). A `SameSite=Strict` cookie is never attached on ANY cross-site request regardless of `credentials:true`, so the browser sends no cookie to an attacker origin; the body path requires the token, which is HttpOnly and unreadable by JS. **Rejected Suggested-fix option (a)** (body-only refresh) — would break the by-design HttpOnly cookie flow. **No anti-CSRF header added** — belt-and-suspenders over an already-closed vector, frontend complexity for zero residual on the documented threat. **Fix = GUARD TEST only:** the prod cookie spec already asserted `SameSite=Strict` (`auth.controller.spec.ts:184/189`), but the DEV/test case asserted nothing about SameSite — and `SameSite=Lax` is exactly the audit's premise — so a dev regression to Lax could have slipped through; added `SameSite=Strict` / not-`Lax` assertions to the dev case. **Witness:** temporarily flipping line 89 to `SameSite=Lax` turns BOTH the prod assertions AND the new dev assertion RED (2 failed / 21 passed captured); revert → 23/23 green — the only honest FAILS-before evidence for a mitigated close with no fix to toggle. **File-scope divergence (documented, intentional):** entry `File:` is `main.ts:56` / verification names `main.spec.ts`, but the cookie construction + CSRF mitigation live in `auth/auth.controller.ts`, so the witness was added in `auth/auth.controller.spec.ts`; `main.ts` only holds the unchanged `credentials:true` CORS line. **AC#4 N/A** (no behavior change, no code path, no auth mutation → no audit emit). **No migration.** Gate green: `pnpm test` (1768) + `npx nest build`; auth refresh e2e uses the body path so SameSite is orthogonal — not broken. Lint = known env breakage, skipped. **Deploy: NONE warranted** — guard-test-only, no runtime delta; rides along with the next grouped deploy if/when one ships. **Follow-up (NOT scoped, candidate):** harden `ALLOWED_ORIGINS` parsing in `main.ts` (reject wildcards / require `https` in prod) so a config typo can't admit a hostile origin — defense-in-depth against the "if a malicious origin is ever added" framing; lower priority since SameSite=Strict already neutralizes the cookie vector.

---
**✅ SEC-021 closed `d4f3815` (`[closes SEC-021]`) 2026-05-31 — DEPLOYED to prod 2026-05-31 (prod HEAD `f0cd0b1`, RELEASE_SHA repinned BEFORE build → boot `RELEASE_DEPLOYED` audit reads `f0cd0b1`, rollback img `orchestra-api:pre-sec021` = `6e1f6bb9`). Live gate smoke PASS:** forged JWT for a real active user → `/api/auth/logout` returned **204** with Redis up, and the `jwt:blacklist:<jti>` key was confirmed written (TTL 291s) — proving the 204 means *genuinely revoked*, not a silent no-op. `JwtBlacklistService.blacklist()` swallowed Redis write errors and only logged them, so `/auth/logout` returned 204 as if revoked — with Redis down, a stolen-after-logout token stayed usable for the access TTL (~15 min). Asymmetric with `isBlacklisted()`, which already fails-CLOSED. Fix: `blacklist()` now logs AND throws `ServiceUnavailableException` (NestJS → HTTP 503; a plain `throw` would give 500) on Redis write failure, so the client retries an idempotent op instead of trusting a false 204. **Ordering regression caught (in-scope):** `logout()` reordered so the Redis-INDEPENDENT durable ops run FIRST (refresh-token revoke in Postgres + `clearRefreshCookie`), then the jti blacklist LAST — had the throw been added at its original first position, a Redis outage would also have skipped refresh-revoke + cookie-clear (strictly worse). Now only the response code depends on Redis; retry is idempotent. **Deliberate asymmetry (the inverse of SEC-006/SEC-019):** those reads fail-OPEN for availability; this revocation WRITE fails-CLOSED — a full Redis outage makes logout 503 for everyone, which is correct (an honest 503 the client retries beats a false 204 that didn't revoke). **AC#4:** `/auth/logout` emits NO audit row (no controller call, no global audit interceptor, `revoke()` is bare Prisma `updateMany`) → no false "token revoked" success possible; satisfied by absence (backlog's literal "create audit row" superseded by the operator's "no false-success row" reframing). **Witnesses:** unit `jwt-blacklist.service.spec` "throws ServiceUnavailableException when Redis write fails" — FAILS pre-fix (swallowed → resolved), PASSES post-fix; controller `auth.controller.spec` "propagates 503 … instead of a false 204" + asserts durable ops ran before the throw (guards the regression + future re-swallowing). **No migration** (Postgres durable-fallback declined as out-of-scope; candidate follow-up); reuses the existing per-service ioredis client. Pointer `jwt-blacklist.service.ts:28` was accurate (no drift this time). Gate green: `pnpm test` (1768) + `npx nest build` + e2e SEC-04 "logout blacklists access token; /auth/me → 401" (`@smoke`, 204-when-Redis-up). The one e2e failure seen — SEC-04 "replaying old refresh", `rotated.refresh_token` undefined — is **PRE-EXISTING SEC-014 cookie-migration drift** (refresh token moved to `__Host-` cookie, no longer in `/auth/refresh` body); reproduced identically on the pre-change tree (stash-verified), unrelated to SEC-021, out of scope. Lint = known env breakage, skipped.

**✅ SEC-019 closed `63cf2b9` (`[closes SEC-019]`) 2026-05-31 — DEPLOYED to prod 2026-05-31.** `resetPassword` revoked refresh tokens but left already-issued ACCESS tokens valid until expiry (≤15 min) — a stolen access token outlived the victim's reset; the jti blacklist is per-token (logout only), no "revoke all for user X". Fix: new **`JwtNotBeforeService`** (`jwt:nbf:<userId>`, **SECONDS**) — its own ioredis connection to the SAME Redis as JwtBlacklistService/LoginLockoutService (no new dependency). `resetPassword` bumps the user's nbf to `floor(now)+1s` alongside `revokeAllForUser`; **`JwtStrategy.validate` rejects any token with `iat < nbf`** (strict), placed right after the fail-CLOSED jti blacklist check and BEFORE the Prisma fetch (cheap, keyed off `payload.sub`). **Decisions:** iat & nbf both UNIX seconds (no ms/s mismatch — the #1 trap); the `+1` baked into the stored nbf + strict `<` rejects a token minted in the reset second while admitting one minted the next second; nbf read **fails OPEN** (matches SEC-006) — safe because the prior blacklist check fails CLOSED and every access token carries a jti, so a full Redis outage already 401s everything before the nbf gate; key TTL = accessTTL + 60s margin with a parser that errs LONG (a stale key can't expire under a live token). **AC#4:** existing `PASSWORD_CHANGED` audit `details` now states access tokens were invalidated (no new row). **Witness** (`jwt.strategy.spec`): `iat=nbf-1`→401, `iat=nbf`→pass, `iat=nbf+1`→pass, same-second pre-reset (`iat=bumpSec`, stored `nbf=bumpSec+1`)→401 — FAILS pre-fix (token accepted), PASSES post-fix. Plus `jwt-not-before.service.spec` (seconds-unit, +1, TTL parser, fail-open). **No migration** (Redis + validate logic). Scope: `auth.service.ts` + `strategies/jwt.strategy.ts` + `auth.module.ts` + new `jwt-not-before.service.ts` (adjacent files documented per contract rule 1; the audit `File:` pointer `auth.service.ts:360` was stale — re-derived). Gate green: `pnpm test` (1765) + `nest build` + auth reset→revocation `@smoke` e2e + all 6 API-based role logins. Lint = known env breakage, skipped. The 6 failing `workflows/auth.spec.ts` cases are the pre-existing UI-login timeouts **already ticketed under TST-E2E-001** ("6× web-UI timeouts", cited in the SEC-005 closure below) — web/Next render, violate the project's "auth via API in tests" rule, orthogonal to this backend-only change (confirmed independently: `/login` 307s, form field never renders; my diff touches no web code). **Out of scope (noted, NOT implemented):** wiring the same `bumpUser` into role-change / forced-logout / `isActive`-flip sinks (BACKLOG ~line 8184 ties this to the avatar-throttle entry) — reuse the same nbf bump when those are tackled; SEC-021 fail-open hardening; CAPTCHA.
- **🚀 DEPLOYED 2026-05-31.** Prod before→after: git HEAD `9046234`→`f34060b`; RELEASE_SHA `9046234`→`f34060b` (repinned in `.env.production` BEFORE build per runbook → container env + boot provenance both read `f34060b` first-time, no second recreate needed); api image `c3b28075`→`6e1f6bb9`. **No migration.** Build→up only (`build api` → `up -d --no-deps api`, source-baked, `--env-file .env.production`); clean boot, healthy first poll, `https://orchestr-a.com/api/health`→**200**. Rollback image retained `orchestra-api:pre-sec019` (`c3b28075`, the prior SEC-014 image); rollback = repin RELEASE_SHA + `up -d` that tag (no migration to undo). Running dist confirmed to carry `jwt-not-before.service.js` + the `iat < nbf` gate. **This deploy also shipped OBS-013** (fbb74d1, LOGIN_FAILURE stdout-hash delta — the "ships with next grouped deploy" it was waiting on; now LIVE).
- **Redis pre-flight + post-deploy recheck (load-bearing — the gate is a no-op without Redis):** live `PING→PONG` + `SET/GET/TTL` via ioredis from inside `orchestr-a-api-prod` both before build and after recreate. Fail-open on a transient nbf-key error is capped by the 15-min access TTL; a full Redis outage 401s everything via the prior fail-CLOSED blacklist gate.
- **Live gate smoke (non-mutating, zero real-user impact):** forged two HS256 tokens (real prod `JWT_SECRET`, real active userId) with `iat` straddling a manually-set `jwt:nbf:<sub>` — exactly what `bumpUser` writes — and hit `GET /api/auth/me`: **pre-nbf old token → 200, post-nbf old token → 401 (the security proof — pre-reset token rejected), newer token → 200**. `verdict:PASS`. The nbf key was deleted by the script (`jwt:nbf:*` scan empty afterwards); no password/user mutated, so the real reset HTTP path was not exercised on prod (its `bumpUser` wiring is unit-verified + the code is confirmed baked in dist). Leftover: a stray `sec019smoke.js` sits in the API container's **read-only** rootfs overlay (rm denied even as root; never imported — app entry is `dist/main`); discarded on next recreate, harmless like the existing `orchestra-api-run-*` leftovers.

---
**✅ OBS-013 closed `fbb74d1` (`[closes OBS-013]`) 2026-05-31 — DEPLOY HOLD (log-only delta).** The audit `File:` pointer (`auth.service.ts:99`) was **stale**: SEC-005 already removed the raw login from the persisted row (constant `details` + sanitized `entityId`), and `auth.service.ts` has no direct logger — its only sink is `auditService.log(...)`. The residual leak lived in the **shared** sink `audit.service.ts`, where `{ timestamp, ...event }` spread the raw `attemptedEmail` (the login OBS-001 passes as a structured field) verbatim into `logger.warn(JSON.stringify(entry))` → stdout, unmasked email PII. Fix: strip raw `attemptedEmail` from the stdout entry, substitute `attemptedEmailHash = sha256(login).slice(0,8)` (`hashAttemptedLogin` helper). **Persistence path untouched** — `entityId` keeps the OBS-001 sanitized subject in the RGPD-controlled `audit_logs`; two sinks, two policies (controlled DB readable, broadly-shipped stdout non-reversible). Scope: `audit.service.ts` (adjacent to stale `File:`, documented per contract rule 1). Witness (`audit.service.spec.ts`): LOGIN_FAILURE with known login → warn JSON has no raw login, `attemptedEmailHash` only, persisted `entityId` unchanged — **demonstrated FAIL-before / PASS-after** (stashed fix → red). AC#4 N/A. Gate green: `pnpm test` (1751) + `nest build` clean. **Deploy: HOLD** — pure stdout-format delta (LOGIN_FAILURE now prints a hash instead of the raw login), no schema/behavior/audit-row change; ships with the next grouped deploy, no standalone deploy warranted. **Leak-class lesson:** a `{ ...event }` spread into a log sink re-exposes every field a *later* task adds (OBS-001's `attemptedEmail` re-leaked post-SEC-005); the `...before`/`...after`/`...details` spread is the same future surface — candidate for a dedicated audit-sink-redaction pass.

---
**✅ SEC-014 closed `9ed92b7` (`[closes SEC-014]`) 2026-05-31 — DEPLOYED to prod 2026-05-31.** Prod before→after: git HEAD `ae8402c`→`9046234`; RELEASE_SHA `5d3d1a2`→`9046234` (repinned BEFORE build per runbook; running container env + boot provenance both read `9046234` first-time, no recreate needed); api image `4a6d51d0`→`c3b28075`. **No migration.** Build→up only (`build api` → `up -d --no-deps api`); clean boot, healthy, `/api/health`→200. Rollback image retained `orchestra-api:pre-sec014` (`4a6d51d0`, the prior SEC-007 image); rollback = repin RELEASE_SHA + `up -d` that tag (no migration to undo). Running dist confirmed to carry the `__Host-` cookie logic + `NODE_ENV=production`. **Live HTTPS smoke** (disposable refresh token inserted for the operator's own account, exercised, then both rows purged — `remaining_test_rows=0`, zero real-user impact): `POST https://orchestr-a.com/api/auth/refresh` with the `__Host-` cookie → **200** + `access_token`, and `Set-Cookie: __Host-orchestr_a_refresh_token=…; Max-Age=604800; Path=/; HttpOnly; SameSite=Strict; Secure` — every hardened attribute present, **no `Domain`**, read path resolves the new name. **Dual-name read chosen → existing sessions NOT force-logged-out** (they migrate on next refresh). Refresh cookie hardened against the shared-domain/sibling-subdomain plant-or-read vector + top-level-nav CSRF. Prod cookie becomes `__Host-orchestr_a_refresh_token` with `Path=/` + `Secure` + `HttpOnly` + `SameSite=Strict` (no `Domain`); dev/e2e keep the non-prefixed name without `Secure` (http://localhost can't carry Secure cookies). `@fastify/cookie` is NOT registered, so the raw `Set-Cookie` construction was kept (registering it would widen scope to `main.ts`/`package.json` for no security gain). Scope: `auth.controller.ts` only. AC#4 N/A (cookie attributes only; refresh/logout audit emits live in the untouched services). Gate green: `pnpm test` (1750) + `nest build`; auth login→cookie-backed refresh+rotate→logout-clear verified **live** against the running dev API under the dev cookie config (SameSite=Strict does not break same-origin carriage).
- **⚠️ DEPLOY DECISION (existing-session impact) — chose dual-name read, so NO forced re-login.** Renaming to `__Host-orchestr_a_refresh_token` changes the cookie NAME; existing prod users hold the OLD name (`orchestr_a_refresh_token`, Path=/api/auth, 7-day cookie). The read path (`readRefreshCookie`: refresh + logout) resolves BOTH names — prod `__Host-` first, legacy fallback — so on the first refresh after deploy each session silently migrates to the new cookie. **Existing sessions are NOT logged out.** On single-domain prod (current topology), reading the legacy name during the window is exactly today's behavior → zero new risk now; it only delays full `__Host-` benefit by one refresh window. *Alternative not taken:* drop the fallback and accept a one-time forced re-login (also defensible). If the operator prefers the cleaner re-login, remove the legacy branch in `readRefreshCookie` before deploy.
- **FOLLOW-UP (transition cleanup):** remove the legacy-name fallback in `readRefreshCookie` after one `JWT_REFRESH_TTL` window (7d default) post-deploy → ~2026-06-07. Until removed, the fix is half-applied (legacy name still accepted).
- **Deploy notes when greenlit:** no migration; build→up only (`build api` → `up -d --no-deps api`), source-baked. Repin `RELEASE_SHA` BEFORE build (runbook lesson). Smoke after: real login over HTTPS → `Set-Cookie` carries `__Host-orchestr_a_refresh_token; Path=/; Secure; SameSite=Strict` (no Domain); cookie-backed refresh returns a new `access_token` + rotated cookie; logout clears it (`Max-Age=0`).

---
**✅ SEC-007 closed `6935950` (`[closes SEC-007]`) 2026-05-31 — DEPLOYED to prod 2026-05-31.** Prod before→after: git HEAD `9b528d9`→`5d3d1a2`; RELEASE_SHA `9b528d9`→`5d3d1a2` (repinned in `.env.production` BEFORE build per the SEC-006/013 runbook lesson, so boot provenance is correct first-time — running env + boot `RELEASE_DEPLOYED` both read `5d3d1a2`); api image `5555758`→`4a6d51d0`. **No migration** (validation only). Build→up only (`build api` → `up -d --no-deps api`); clean boot, healthy, `/api/health`→200. Rollback image retained `orchestra-api:pre-sec007` (`55557581bd22`, the prior SEC-006+013 image); rollback = repin RELEASE_SHA + `up -d` that tag (no migration to undo). Running dist confirmed to contain `validatePasswordStrength` (`password-policy.js` + `users.service.js`). **Live smoke** (public `@Post('reset-password')`, side-effect-free — validation runs before the token check): weak `newPassword:"a"` → **400** with both policy messages ("…au moins 8 caractères" + "…une majuscule, un chiffre et un caractère spécial"); strong `"P@ssword1"` → passes validation, **401 "Token de réinitialisation invalide"** (proves strong clears the policy, no over-rejection). Two stray `orchestra-api-run-*` leftover containers still on the box (harmless; prune in a maintenance pass).
- Shared `PasswordPolicy` (`common/validators/password-policy.ts`: `IsStrongPassword()` + `validatePasswordStrength`/`assertStrongPassword`) consolidating 7 duplicated `@MinLength(8)+@Matches` literals; applied on all creation/mutation DTOs (Register/CreateUser/UpdateUser/ImportUser/ChangePassword/auth-ResetPassword/AdminResetPassword). **LoginDto deliberately untouched** (validates an existing credential — tightening it locks out users with older weak passwords). Teeth: `UsersService.importUsers` now enforces the policy imperatively before bcrypt (the HTTP `/import` path was ALREADY protected by ImportUserDto decorators + global ValidationPipe nested-validation — verified empirically; the audit's "import 'a'" premise was stale, but the service method itself had no check → non-HTTP bypass). Also: `validateImport` dry-run uses the shared policy (was `length<8` only); `seed.ts` rejects a weak operator-supplied `SEED_ADMIN_PASSWORD` (random branch exempt). Gate green: `pnpm test` (1746) + `nest build` + seed `tsc`; AC#4 N/A; no migration; e2e not run (behavior-preserving for strong passwords, no password-strength e2e exists, seed-throw is config-gated and nothing wired triggers it). Witness at service level (HTTP path green pre-fix): `importUsers([{password:'a'}])` → RED `created:1` pre-fix, GREEN `created:0,errors:1` post-fix.
- **SEED implication (now LIVE — surface before any future provisioning):** with SEC-007 deployed, any future deploy that seeds/provisions an admin requires `SEED_ADMIN_PASSWORD` to satisfy the policy or `db:seed` THROWS. This deploy was build→up only (NO seed run), and the prod seed path is env-gated (`docker-entrypoint.sh` only seeds when `SEED_ADMIN_PASSWORD` is explicitly set) — current prod admin already exists and was untouched. Nothing wired sets a weak value today.
- **Follow-up candidates (not done):** (1) CLAUDE.md's dev login `admin / admin123` is stale — `admin123` fails the policy and the hardcoded default was removed by SEC-02; refresh the doc + dev `.env` guidance. (2) Existing already-weak passwords are NOT retroactively remediated — force-rotation via SEC-004's `forcePasswordChange` is the real follow-up.

---
**✅ SEC-006 + SEC-013 DEPLOYED to prod 2026-05-31 (grouped). Prod before→after: git HEAD `5ac6eb0`→`9b528d9`; running code `5d530b3` (SEC-005)→`9b528d9` (SEC-006+SEC-013); RELEASE_SHA `5d530b3`→`9b528d9`; api image `2b9b77a3`→`5555758`.**
- **RELEASE_SHA ordering lesson (runbook fix):** `RELEASE_SHA` is a runtime `environment:` var (`${RELEASE_SHA:-unknown}`), NOT a baked build-arg (Dockerfile has no ref). I repinned `.env.production` *after* `up`, so the first boot (11:31) still injected `5d530b3` and the app's boot-time `RELEASE_DEPLOYED` audit (`payload.after.releaseSha`) recorded `5d530b3` for `9b528d9` code. Fixed with a no-rebuild `up -d --no-deps api` recreate → running env + a fresh 11:46 `RELEASE_DEPLOYED` row both now read `9b528d9`. The 11:31 row remains (append-only, accurate-for-its-moment); the latest row is correct. **Next deploy: repin RELEASE_SHA BEFORE build/up.**
Pre-flight (both gates PASS): (1) Redis reachable from API container — live `PING→PONG` + write + TTL via ioredis from inside `orchestr-a-api-prod`, so SEC-006's fail-OPEN lockout is genuinely active, not a silent no-op; (2) topology matches SEC-013's `['loopback','uniquelocal']` — `docker port orchestr-a-api-prod` empty (API not host-published), nginx (`172.19.0.6`) sole ingress.
Smoke (real `/auth/login` via nginx/HTTPS, X-Forwarded-For to simulate clients):
- **SEC-006 per-IP throttle:** 6 rapid fails from one IP (`203.0.113.50`) → `401×5, 429` on the 6th (app 429, not nginx 503).
- **SEC-013 cross-IP isolation (load-bearing):** a fail from a *different* IP (`198.51.100.77`) → `401`, NOT 429 — proves per-REAL-IP keying. Would have FAILED pre-SEC-013 (collective nginx-IP bucket).
- **SEC-013 IP-in-key proof:** Redis `login:lockout:until:<sha256(login|203.0.113.50)>` TTL=858 (lock armed on real client); the `<…|172.19.0.6>` (nginx) key is ABSENT (`-2`). Audit rows for the run carry `payload.ip = 203.0.113.50/198.51.100.77/192.0.2.123` (real clients) — and an OLDER pre-deploy `LOGIN_FAILURE` row shows `172.19.0.6` (the nginx IP = the exact bug, now fixed). Before/after in one table.
- **ACCOUNT_LOCKED audit:** row present, real client IP, `success=false`, `rowHash`+`prevHash` non-null, chain link verified (newest.prevHash == prev.rowHash). Live Redis lock TTL counting down; escalation `level=1`.
- **No regression:** invalid creds → SEC-005 generic `401 "Login ou mot de passe incorrect"`; authenticated path intact (forged-JWT `GET /api/auth/me` → `200`; no token → `401`). NOTE: a real password-login-200 was NOT run (prod admin password not held) — the forged-JWT GET is the auth-path-intact proxy.
- Smoke side-effects (auto-expire ≤15min, disposable usernames + TEST-NET IPs, zero real-user impact): lockout/fail Redis keys for `smokesec013nobody`/`anothernobody`; permanent (legitimate) failed-login audit rows.
- Candidate next TODO: **SEC-007** (LoginDto MinLength(6) vs RegisterDto 8+, same Cluster K / Phase 5).

---
## Prior (closed, pre-deploy notes retained)
**SEC-013 closed `e3e37e2` (`[closes SEC-013]`) 2026-05-31.** Enabled Fastify `trustProxy` AND fixed the real client-IP resolution. The audit's premise was wrong: `trustProxy` alone is insufficient because both the throttler guard and `auth.controller` `extractMeta` read `req.ips[0]`, which under Fastify is the **nginx socket** in every trustProxy config (`req.ips = [socketAddr, ...XFF]`; the `req.ips[0]`-is-client assumption is Express, not Fastify). Centralised the fix in new `common/fastify/trust-proxy.config.ts` (`TRUST_PROXY = ['loopback','uniquelocal']` + `clientIp(req)=req.ip`, the leftmost untrusted hop). The same `req.ips[0]` bug had spread to **7 audit-IP call sites** total — extended to the `extractMeta` helpers in `documents`/`tasks`/`milestones`/`planning-export`/`leaves` controllers in follow-up `2b9b78b` (9 files total: main.ts, guard, auth.controller, 5 module controllers, new config) + witness. Gate green: `pnpm test` (1736, incl. 3 new) + `nest build`; no migration, AC#4 N/A (changes which IP an existing emit records, no new emit). Without it, SEC-006's 5/min IP throttle + (account, IP) lockout key collapse to the nginx IP across all users.
**SEC-006 closed `9e5a577` (`[closes SEC-006]`) 2026-05-31 — DEPLOYED 2026-05-31 (grouped with SEC-013, see top of §Next).** Login brute-force hardened: `/auth/login` IP burst cut 30→5/min, plus a new Redis-backed per-(account, IP) progressive lockout (`LoginLockoutService`: 5 fails → 15min, ×2 escalation cap 24h, reset on success). Keyed account+IP to dodge the bare-account lockout DoS; **fails OPEN** on Redis error (the IP throttle is the independent outer bound). New `ACCOUNT_LOCKED` audit action emits a before/after row on lock (AC#4). Gate green: `pnpm test` (1733) + `nest build` + Playwright SEC-05 (`--no-deps --reporter=line`) — the spec is bucket-sensitive at limit 5, run it isolated. Cross-update: **TST-E2E-001 SEC-05 throttle sub-failure (#2) resolved** (noted in entry, NOT closed — seed/refresh/UI sub-failures #1/#3/#4 remain).

---
SEC-005 closed (`c198772`, `[closes SEC-005]`) **and DEPLOYED to prod 2026-05-31** (prod `acf4693`→`5d530b3`). Login enumeration killed: `validateUser` folds the disabled-account case into the same generic 401 (was a distinct `'Compte désactivé'`), and LOGIN_FAILURE `details` no longer carries the attempted-login plaintext (OBS-001 `attemptedEmail`→entityId subject kept). Post-deploy smoke on the real `/auth/login` (via nginx) confirmed live: bad-password on a known account AND unknown user BOTH return identical `401 {"message":"Login ou mot de passe incorrect"}`; a known-good credential returns `200` + `access_token` (success path intact); the resulting LOGIN_FAILURE rows carry the generic value `payload->>'details' = "Failed login attempt"` (the `details` field lives inside the `payload` jsonb column — there is no top-level `details` column) with NO login plaintext, and the sanitized subject in `entityId` (`admin` / `no-such-user-zzz999`). Verified directly in prod against the two fresh smoke rows; the legacy pre-deploy row `deploy-smoke-20260526` still shows the old leaky `"...for login: <plaintext>"` form, correctly immutable (append-only audit) — clean before/after contrast. Running dist confirms 0 occurrences of `Compte désactivé`. **Auth e2e still NOT run green** — pre-existing-red in the local dev-DB/dev-watch harness (not CI's seeded `orchestr_a_v2_e2e` + compiled `api start`); failures orthogonal to SEC-005 (SEC-03 role-shape/seed drift, SEC-05 throttle expects 429 but login is limit:30 — blocked_by SEC-006, SEC-04 refresh untouched, 6× web-UI timeouts — captured in TST-E2E-001). Scoped-out adjacency flagged: unknown-user timing oracle (null before bcrypt) — future constant-time hardening, not SEC-005. Also filed this arc: SEC-FE-001 TODO (`a259273`, SEC-004 frontend counterpart) and TST-E2E-001 TODO (`5d530b3`, auth e2e drift). Phase 5 SEC-005 done + live; operator to pick next direction.
