
======================================================================
# FAMILY 1-WEB — 20 findings
======================================================================

### COR-041 [🟠 important] phase2
FILE: `apps/web/app/[locale]/login/page.tsx:27-43`
TITLE: Login/register: JWT stored in localStorage before permissions fetch — broken auth state on network error
REASON: Verified in current code. `auth.service.ts:68` calls `persistSession()` inside `login()` immediately after the API response, before the caller's permissions fetch. `login/page.tsx:28-39` confirms: `authService.login(formData)` persists JWT, then `api.get('/auth/me/permissions')` 
FIX: Option A (cleanest): remove the `/auth/me/permissions` call from login and register handlers — `useAuthBootstrap` in layout already fetches it on mount; just `router.push` after `authService.login`. Option B: wrap the permissions call in its own try/catch; on failure call `clearSession()` to roll back the JWT. RED test: mock `api.get('/au

### COR-042 [🟠 important] phase2
FILE: `apps/web/app/[locale]/projects/[id]/page.tsx:959-965`
TITLE: handleUpdateProject has no error handling — silent failure on project update
REASON: Verified in current code. `apps/web/app/[locale]/projects/[id]/page.tsx:953-959`: `handleUpdateProject` does `await projectsService.update(projectId, data)` and `await projectsService.getById(projectId)` with no try/catch wrapper. The immediately-following `handleHardDeleteProjec
FIX: Wrap lines 953-959 in try/catch matching `handleHardDeleteProject` pattern: catch, extract `axiosError.response?.data?.message`, call `toast.error(...)`. Re-throw to keep the modal open if the caller checks for errors. RED test: mock `projectsService.update` to reject with 422; assert `toast.error` fires.

### COR-043 [🟠 important] phase2
FILE: `apps/web/app/[locale]/projects/page.tsx:323-337`
TITLE: onArchive / onUnarchive in projects/page.tsx have no error handling
REASON: Verified in current code. `apps/web/app/[locale]/projects/page.tsx:323-337`: `onArchive` awaits `projectsService.archive(id)` then `fetchProjects()` with no try/catch. `onUnarchive` (lines 334-337) does the same. On any API failure the rejection propagates silently — no toast, no
FIX: Wrap each body in try/catch with toast.error matching the file's existing pattern. For `onArchive`: `try { await projectsService.archive(id); toast.success('Projet archivé'); await fetchProjects(); } catch { toast.error('Erreur lors de l\'archivage'); }`. Same for `onUnarchive`. RED test: mock `projectsService.archive` to reject; assert `

### COR-044 [🟠 important] phase2
FILE: `apps/web/app/[locale]/projects/page.tsx:85-130`
TITLE: fetchProjects useCallback missing hasPermission dependency — stale permission closure
REASON: Verified in current code. `apps/web/app/[locale]/projects/page.tsx:130` dep array is `[user, memberMeFilter, showArchived]` — `hasPermission` is absent. `usePermissions.ts:24-28` confirms `hasPermission` is `useCallback([permissions])`, meaning it yields a new reference when `per
FIX: Add `hasPermission` to the dep array at line 130: `}, [user, memberMeFilter, showArchived, hasPermission]);`. Because `hasPermission` is already memoized by `usePermissions`, this only triggers re-creation when permissions actually change. RED test: ESLint `react-hooks/exhaustive-deps` error + behavioral test: simulate permission change v

### COR-045 [🟠 important] phase2
FILE: `apps/web/app/[locale]/reports/components/ProjectsDetailTable.tsx:379-380, 428`
TITLE: ProjectsDetailTable: Link href and router.push use locale-less paths, breaking client-side navigation
REASON: Verified in current code. `apps/web/app/[locale]/reports/components/ProjectsDetailTable.tsx` imports only `{ fr } from 'date-fns/locale'` — no `useLocale` from `next-intl`. Line 379 uses `href={\`/projects/${row.id}\`}` and line 428 uses `router.push(\`/tasks?projectId=${row.id}&
FIX: Add `import { useLocale } from 'next-intl';` and `const locale = useLocale();` inside the component. Replace line 379 with `href={\`/${locale}/projects/${row.id}\`}` and line 428 with `router.push(\`/${locale}/tasks?projectId=${row.id}&overdue=true\`)`. RED test: render component, simulate row click, assert navigation target starts with `

### COR-047 [🟠 important] phase2
FILE: `apps/web/app/[locale]/telework/page.tsx:395-404`
TITLE: usersService.getAll() in telework/page.tsx has no .catch() — silent failure in user selector
REASON: Verified in current code. `apps/web/app/[locale]/telework/page.tsx:400-402`: `usersService.getAll().then((data) => { setAllUsers(Array.isArray(data) ? data : []); })` — no `.catch()` on the promise chain. A 403 or network failure leaves `allUsers` unpopulated and the manager's us
FIX: Add `.catch(err => { logger.error('Error fetching users for telework:', err); toast.error(tc('errors.serverError')); setAllUsers([]); })` after the `.then()`, or convert to async/await with try/catch. RED test: mock `usersService.getAll` to reject; assert `toast.error` fires and user selector is in recoverable state.

### PER-031 [🟠 important] phase2
FILE: `apps/web/app/[locale]/clients/page.tsx:147-155`
TITLE: handleExportExcel fires Promise.all over all active clients without concurrency cap (up to 200 requests)
REASON: Verified in current code. `apps/web/app/[locale]/clients/page.tsx:147-154`: `Promise.all(cap.map((c) => clientsService.getProjectsWithSummary(c.id)...))` with no concurrency limit. `cap` is set from `clientsService.getAll({ limit: 200 })` at line 43. The `> 100` confirm dialog at
FIX: Replace `Promise.all` with a concurrency-limited chunked loop (chunks of 10): `for (const chunk of chunkArray(cap, 10)) { const chunkResults = await Promise.all(chunk.map(...)); results.push(...chunkResults); }`. Or add a bulk backend endpoint. RED test: mock and track concurrent call count; assert no more than 10 simultaneous in-flight r

### PER-032 [🟠 important] phase2
FILE: `apps/web/app/[locale]/projects/[id]/page.tsx:182-185`
TITLE: milestonesService.getAll() fetches ALL milestones system-wide then filters client-side in two pages
REASON: Triage correctly identified the tasks/[id]/page.tsx as still unfixed. Confirmed: `apps/web/app/[locale]/tasks/[id]/page.tsx:97-100` calls `milestonesService.getAll()` which fetches all milestones system-wide (`/milestones?limit=1000`), then does `milestonesData.data.filter((m) =>
FIX: In `apps/web/app/[locale]/tasks/[id]/page.tsx` replace lines 97-100 with `const projectMilestones = await milestonesService.getByProject(taskData.projectId); setMilestones(projectMilestones);`. CAUTION: `getByProject` returns bare `Milestone[]` not `{ data: Milestone[] }` — remove the `.data.filter(...)` pattern entirely. RED test: mock `

### PER-033 [🟠 important] phase2
FILE: `apps/web/app/[locale]/reports/components/advanced/AdvancedAnalyticsTab.tsx:8-29`
TITLE: AdvancedAnalyticsTab creates a module-scope QueryClient inside its own QueryClientProvider, bypassing the app-level cache
REASON: Verified in current code. `apps/web/app/[locale]/reports/components/advanced/AdvancedAnalyticsTab.tsx:8-12`: `const queryClient = new QueryClient({...})` is at module scope. Lines 24-29: `AdvancedAnalyticsTab` wraps children in `<QueryClientProvider client={queryClient}>`. This b
FIX: Remove the `QueryClientProvider` wrapper from `AdvancedAnalyticsTab`. Child components `WorkloadChart`, `RecentActivity`, `MilestonesCompletion` will use the app-level QueryProvider already in layout.tsx. If the `staleTime: 5 * 60 * 1000` config is needed, set it per-query in each child component. RED test: render inside app-level provide

### SEC-031 [🟠 important] phase2
FILE: `apps/web/app/api/[...path]/route.ts:4-18`
TITLE: API proxy forwards `host` header to internal backend (host-header injection)
REASON: Verified in current code. `apps/web/app/api/[...path]/route.ts:4-12`: `HOP_BY_HOP` set contains only `connection, keep-alive, transfer-encoding, upgrade, proxy-connection, te, trailer` — `host`, `x-forwarded-host`, and `x-real-ip` are absent. `filterHeaders` at line 14-22 forward
FIX: Add `'host'`, `'x-forwarded-host'`, `'x-real-ip'` to `HOP_BY_HOP` in `apps/web/app/api/[...path]/route.ts:4-12`. No response-shape risk — header-only change. RED test: send request with `Host: evil.com` to the proxy; assert forwarded fetch to backend does NOT include `host: evil.com` header.

### SEC-061 [🟡 nit] phase4
FILE: `apps/web/src/lib/csp.ts:16-26`
TITLE: CSP policy missing `object-src 'none'` and `base-uri 'self'` directives
REASON: Verified in current code: apps/web/src/lib/csp.ts:16-25 defines exactly 7 directives (default-src, script-src, style-src, img-src, font-src, connect-src, frame-ancestors). Neither `object-src 'none'` nor `base-uri 'self'` is present. No fix commit found. Both are zero-risk additi
FIX: In apps/web/src/lib/csp.ts, add two entries to the directives array: `object-src 'none'` and `base-uri 'self'`. Failing test: snapshot test on buildCsp() output asserting both directives present. No response-shape risk.

### PER-058 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/leaves/page.tsx:1`
TITLE: All 6 pages use manual fetch + useState with no TanStack Query caching — repeated fetches on every navigation
REASON: Verified: apps/web/app/[locale]/leaves/page.tsx line 3 imports `useEffect, useState` with no TanStack Query imports. The page uses manual useEffect+setState for all data fetching with no staleTime, deduplication, or background revalidation — confirmed by reading lines 1-80. This 
FIX: Migrate each data-fetch useEffect to useQuery with domain-scoped query keys and staleTime:30_000. Replace fetchAll() calls in mutation handlers with queryClient.invalidateQueries. Large refactor touching multiple pages; no response-shape risk.

### SA-PERF-024 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/projects/page.tsx:243-263`
TITLE: fetchManagersAndDepartments called on every Create button click — not cached between invocations
REASON: Verified: apps/web/app/[locale]/projects/page.tsx lines 243-263 confirm `fetchManagersAndDepartments` calls `usersService.getAll()` and `departmentsService.getAll()` in a Promise.all with no cache. Line 376 confirms it is called on every Create Project button click (`onClick={() 
FIX: Fetch users and departments once on component mount via useEffect with empty deps array, or use TanStack Query with `staleTime: 5 * 60 * 1000`. No response-shape risk. Failing test: mock usersService.getAll, render component, click Create Project twice, assert mock called exactly once.

### COR-066 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/profile/page.tsx:519-523`
TITLE: Profile 'Preferences' tab has a Save button with no onClick handler — button does nothing
REASON: Confirmed in profile/page.tsx lines 527-530: `<button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">{t("preferences.save")}</button>` — no `onClick`, no `type`, not inside a `<form>`. The button is dead UI. Language changes apply immediately
FIX: profile/page.tsx lines 527-531: remove the Save button entirely (changes are already applied on change). Alternatively add a toast/confirmation `onClick` if visual feedback is desired. Failing-test: Playwright test on /profile preferences tab asserting the save button is absent from the DOM, or clicking it and asserting no unexpected API 

### COR-067 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/projects/[id]/page.tsx:843-875`
TITLE: fetchThirdPartyMembers and fetchProjectClients close over canRead* flags but effect deps are suppressed
REASON: Confirmed in projects/[id]/page.tsx: `fetchThirdPartyMembers` (lines 837-845) and `fetchProjectClients` (lines 854-862) are plain async functions (not useCallback) that close over `canReadThirdParties` (line 832) and `canReadClients` (line 833). Effects at lines 847-852 and 864-8
FIX: projects/[id]/page.tsx: wrap `fetchThirdPartyMembers` and `fetchProjectClients` in `useCallback` with deps `[canReadThirdParties, projectId]` and `[canReadClients, projectId]` respectively, then add those callbacks to the effect dep arrays and remove the eslint-disable comments.

### COR-068 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/reports/page.tsx:49`
TITLE: reports/page.tsx: canView = !permissionsLoaded || hasPermission('reports:view') — the !permissionsLoaded branch is dead code
REASON: Confirmed at reports/page.tsx line 49: `const canView = !permissionsLoaded || hasPermission('reports:view');`. The `!permissionsLoaded` branch is dead code when the page renders for an authenticated user. AuthProvider (root layout.tsx line 42) renders a loading spinner at line 63
FIX: reports/page.tsx line 49: replace `const canView = !permissionsLoaded || hasPermission('reports:view');` with `const canView = hasPermission('reports:view');` and remove the `permissionsLoaded` destructure if unused elsewhere on the page.

### COR-069 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/users/page.tsx:190-195`
TITLE: UsersPage: role filter useEffect fires a redundant re-fetch on initial mount when roleFilter='' and availableRoles just loaded
REASON: Confirmed in users/page.tsx lines 184-195: initial `useEffect` at 184-188 calls `fetchUsers()` (no roleFilter) and the secondary effect at 190-195 guards on `availableRoles.length === 0`. Since `fetchUsers` itself populates `availableRoles` (line 153), a roleFilter change fired b
FIX: users/page.tsx: pass `roleFilter` to the initial mount `fetchUsers(roleFilter)` call (line 185), remove the secondary roleFilter effect (lines 190-195). Filter changes are handled by the existing direct `fetchUsers(roleFilter)` calls in handlers (lines 244, 321). The `availableRoles.length === 0` guard becomes unnecessary once `fetchUsers

### OBS-026 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/layout.tsx:1-30`
TITLE: No error boundary anywhere in the [locale] subtree — unhandled render errors crash the entire app
REASON: Verified: find across apps/web/app/[locale] returns error.tsx only for events, leaves, telework, time-tracking, third-parties, clients — none at [locale]/ root. Also confirmed no app/error.tsx or app/global-error.tsx exists at the app root level (ls returned ENOENT for both). The
FIX: Create apps/web/app/[locale]/error.tsx as a 'use client' component with `useEffect(() => console.error(error), [error])` and a user-facing fallback UI with a retry button (Next.js App Router signature: `({ error, reset }: { error: Error & { digest?: string }; reset: () => void })`). A single locale-level error.tsx covers all unprotected s

### OBS-027 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/profile/page.tsx:23-639`
TITLE: Profile page (`profile/page.tsx`) has no error boundary — uncaught render errors cause a blank screen
REASON: Verified: find in apps/web/app/[locale]/profile returns no error.tsx. Also confirmed no app/error.tsx or app/global-error.tsx exists at app root. Profile page (page.tsx, ~640 lines) integrates avatar upload, ICS import/export, password change modal, and async service calls with n
FIX: Resolved by the [locale]/error.tsx from OBS-026, or create a dedicated apps/web/app/[locale]/profile/error.tsx for finer granularity. No API shape change, no PER-021 risk.

### OBS-028 [🟡 nit] phase4
FILE: `apps/web/app/[locale]/projects/page.tsx:1`
TITLE: No error boundary wrapping any of the three page-level components
REASON: Verified: find in apps/web/app/[locale]/projects, tasks, and planning returns no error.tsx files. No app/error.tsx or app/global-error.tsx exists at app root. These are high-traffic routes; unhandled render errors produce a blank screen. Subsumed by OBS-026 fix.
FIX: Resolved by the [locale]/error.tsx from OBS-026, covering all three subroutes. No API shape change.

======================================================================
# FAMILY 2-DTO-MaxLength — 17 findings
======================================================================

### SEC-042 [🟡 nit] phase4
FILE: `apps/api/src/leaves/dto/create-leave.dto.ts:74-81`
TITLE: CreateLeaveDto.reason and ImportLeaveDto.comment lack @MaxLength, allowing oversized free-text storage
REASON: Verified in current code: apps/api/src/leaves/dto/create-leave.dto.ts lines 79-81 confirm `@IsString() @IsOptional() reason?: string;` with no @MaxLength. apps/api/src/leaves/dto/import-leaves.dto.ts lines 56-59 confirm `@IsString() @IsOptional() comment?: string;` with no @MaxLe
FIX: apps/api/src/leaves/dto/create-leave.dto.ts: add MaxLength to the import from class-validator and add @MaxLength(2000) to the reason field (line 79). apps/api/src/leaves/dto/import-leaves.dto.ts: add MaxLength to the import and add @MaxLength(2000) to the comment field (line 57). Failing test: POST /leaves with reason of length 2001 shoul

### SEC-047 [🟡 nit] phase4
FILE: `apps/api/src/projects/dto/create-project.dto.ts:28-36`
TITLE: description fields lack @MaxLength on CreateProjectDto, CreateEpicDto, CreateMilestoneDto
REASON: Verified in current code: apps/api/src/projects/dto/create-project.dto.ts line 36 — `@IsString() @IsOptional() description?: string;` has no @MaxLength (name at line 25-26 has @MaxLength(100)). apps/api/src/epics/dto/create-epic.dto.ts line 25-27 — `@IsString() @IsOptional() desc
FIX: Add @MaxLength(2000) to description in CreateProjectDto (line 36), CreateEpicDto (line 27), CreateMilestoneDto (line 22). In ImportMilestoneDto add @MaxLength(200) @MinLength(1) to name (line 16) and @MaxLength(2000) to description (line 21). MaxLength/MinLength are already imported in create-*.dto.ts files; add to import-milestones.dto.t

### SEC-035 [🟡 nit] phase4
FILE: `apps/api/src/clients/dto/query-clients.dto.ts:13-16`
TITLE: QueryClientsDto.search and QueryThirdPartyDto.search missing @MaxLength — unbounded query string parameter
REASON: Verified in current code: apps/api/src/clients/dto/query-clients.dto.ts lines 13-16 — `@IsOptional() @IsString() search?: string;` with no @MaxLength. apps/api/src/third-parties/dto/query-third-party.dto.ts lines 30-33 — `@IsOptional() @IsString() search?: string;` with no @MaxLe
FIX: Add MaxLength to the class-validator import in both files and apply @MaxLength(200) to the search field in QueryClientsDto and QueryThirdPartyDto. Failing test: GET /clients?search=<201-char string> should return 400; GET /third-parties?search=<201-char string> should return 400.

### SEC-037 [🟡 nit] phase4
FILE: `apps/api/src/documents/dto/create-document.dto.ts:53-60`
TITLE: CreateDocumentDto.description missing @MaxLength — unbounded free-text field
REASON: Verified in current code: apps/api/src/documents/dto/create-document.dto.ts lines 59-62 — `@IsString() @IsOptional() description?: string;` with no @MaxLength. The same file has @MaxLength(255) on name (line 52) and @MaxLength(2048) on url (line 76-77); MaxLength is already in th
FIX: Add @MaxLength(2000) above the @IsString() @IsOptional() decorators on the description field in CreateDocumentDto at line 59. MaxLength is already imported in the file. Failing test: POST /documents with description.length = 2001 should return 400.

### SEC-040 [🟡 nit] phase4
FILE: `apps/api/src/events/dto/create-event.dto.ts:36-38`
TITLE: CreateEventDto.description has no @MaxLength — unbounded DB write
REASON: Verified in current code: apps/api/src/events/dto/create-event.dto.ts lines 35-38 — `@IsString() @IsOptional() description?: string;` with no @MaxLength. The same file has @MaxLength(200) on title at line 28. MaxLength is already in the import list at line 11. UpdateEventDto exte
FIX: Add @MaxLength(2000) to the description field in CreateEventDto at line 35, before @IsString(). MaxLength is already imported. Failing test: POST /events with description.length = 2001 should return 400.

### SEC-046 [🟡 nit] phase4
FILE: `apps/api/src/predefined-tasks/dto/create-predefined-task.dto.ts:16-47`
TITLE: CreatePredefinedTaskDto free-text fields (name, description, color, icon) lack @MaxLength
REASON: Verified create-predefined-task.dto.ts: imports at lines 1-12 include IsString, IsNotEmpty, IsOptional, IsEnum, IsBoolean, IsInt, Min, Max, Matches — MaxLength is absent. Lines 21-23: name has @IsString() @IsNotEmpty() only. Lines 29-31: description has @IsString() @IsOptional() 
FIX: apps/api/src/predefined-tasks/dto/create-predefined-task.dto.ts: add MaxLength to imports from class-validator, then add @MaxLength(200) on name, @MaxLength(2000) on description, @MaxLength(20) on color, @MaxLength(10) on icon. Failing test: POST /predefined-tasks with name of 201 chars (authenticated, predefined_tasks:create) should retu

### SEC-048 [🟡 nit] phase4
FILE: `apps/api/src/projects/dto/create-project.dto.ts:87-103`
TITLE: managerId and sponsorId in CreateProjectDto use @IsString instead of @IsUUID
REASON: Verified create-project.dto.ts: imports at lines 2-12 include IsString, IsNotEmpty, IsOptional, IsEnum, IsDateString, IsArray, MaxLength, MinLength, ArrayUnique — IsUUID is absent. Lines 92-94: managerId has @IsString() @IsOptional() only. Lines 101-103: sponsorId has @IsString()
FIX: apps/api/src/projects/dto/create-project.dto.ts: add IsUUID to imports from class-validator; replace @IsString() with @IsUUID() on both managerId and sponsorId. Failing test: POST /projects with managerId='not-a-uuid' (authenticated, projects:create) should return HTTP 400 not 500.

### SEC-052 [🟡 nit] phase4
FILE: `apps/api/src/settings/dto/update-setting.dto.ts:4-20`
TITLE: UpdateSettingDto.value and .description lack @MaxLength — unbounded strings stored to DB
REASON: Verified update-setting.dto.ts: line 2 imports only { IsString, IsNotEmpty, IsOptional, IsObject } — MaxLength is absent. Lines 9-11: value field has @IsString() @IsNotEmpty() only. Lines 17-19: description has @IsString() @IsOptional() only. No length constraints on either field
FIX: apps/api/src/settings/dto/update-setting.dto.ts: add MaxLength to imports from class-validator; add @MaxLength(10000) to value and @MaxLength(500) to description. Failing test: PATCH /settings/:key with value of 10001 chars (authenticated, settings:update) should return HTTP 400.

### SEC-053 [🟡 nit] phase4
FILE: `apps/api/src/skills/dto/create-skill.dto.ts:27-35`
TITLE: description fields in CreateSkillDto, CreateDepartmentDto, CreateServiceDto have no @MaxLength
REASON: Verified all three DTOs: create-department.dto.ts lines 29-31: @IsString() @IsOptional() description?: string — MaxLength is imported (line 7) for name but not applied to description. create-service.dto.ts lines 30-32: @IsString() @IsOptional() description?: string — MaxLength is
FIX: Add @MaxLength(1000) to description in create-department.dto.ts, create-service.dto.ts, and create-skill.dto.ts. MaxLength is already imported in all three files. Failing test: POST /api/departments with description of 1001 chars (authenticated, departments:create) should return HTTP 400.

### SEC-054 [🟡 nit] phase4
FILE: `apps/api/src/skills/dto/import-skills.dto.ts:15-44`
TITLE: ImportSkillDto.name and .description lack @MaxLength — inconsistent with CreateSkillDto which enforces @MaxLength(100)
REASON: Verified import-skills.dto.ts: imports at lines 2-12 include IsString, IsNotEmpty, IsOptional, IsEnum, IsNumber, IsArray, ArrayMaxSize, ValidateNested, Min — no MinLength or MaxLength. Lines 21-23: ImportSkillDto.name has @IsString() @IsNotEmpty() only. Lines 32-35: description h
FIX: apps/api/src/skills/dto/import-skills.dto.ts: add MinLength, MaxLength to imports from class-validator; add @MinLength(2) @MaxLength(100) to name and @MaxLength(500) to description in ImportSkillDto. Failing test: POST /api/skills/import with skills array containing a name of 101 chars (authenticated, skills:import) should return HTTP 400

### SEC-055 [🟡 nit] phase4
FILE: `apps/api/src/tasks/dto/import-tasks.dto.ts:39-45`
TITLE: ImportTaskDto.assigneeEmail has no @IsEmail — email validation is done by case-insensitive Map lookup instead
REASON: Verified import-tasks.dto.ts: imports at lines 2-12 include IsString, IsNotEmpty, IsOptional, IsEnum, IsNumber, IsArray, ValidateNested, Min, ArrayMaxSize — IsEmail is absent. Lines 44-46: assigneeEmail has @IsString() @IsOptional() only. The service lookup provides functional mi
FIX: apps/api/src/tasks/dto/import-tasks.dto.ts: add IsEmail to imports from class-validator; replace @IsString() with @IsEmail() and add @MaxLength(254) on assigneeEmail. Failing test: POST /tasks/project/:id/import with assigneeEmail='not-an-email' should return HTTP 400.

### SEC-057 [🟡 nit] phase4
FILE: `apps/api/src/third-parties/dto/create-third-party.dto.ts:36-39`
TITLE: CreateThirdPartyDto.contactEmail missing @MaxLength
REASON: Verified create-third-party.dto.ts: MaxLength is imported (line 8). organizationName (line 22) has @MaxLength(255). contactFirstName (line 28) has @MaxLength(100). contactLastName (line 33) has @MaxLength(100). notes (line 44) has @MaxLength(2000). Lines 36-39: contactEmail has @
FIX: apps/api/src/third-parties/dto/create-third-party.dto.ts: add @MaxLength(254) before @IsEmail() on contactEmail. MaxLength is already imported at line 8. Failing test: POST /third-parties with contactEmail of 300 chars (valid email format, authenticated, third_parties:create) should return HTTP 400.

### SEC-058 [🟡 nit] phase4
FILE: `apps/api/src/time-tracking/dto/create-time-entry.dto.ts:71-73`
TITLE: description field in CreateTimeEntryDto lacks @MaxLength constraint
REASON: Verified create-time-entry.dto.ts: imports at lines 2-14 include IsBoolean, IsNotEmpty, IsDateString, IsNumber, IsOptional, IsString, IsUUID, IsEnum, Min, Max, ValidateIf — MaxLength is absent. Lines 71-73: description has @IsString() @IsOptional() only. No length constraint. The
FIX: apps/api/src/time-tracking/dto/create-time-entry.dto.ts: add MaxLength to imports from class-validator; add @MaxLength(2000) to description field. Failing test: POST /api/time-tracking with description of 2001 chars (authenticated, time_tracking:create) should return HTTP 400.

### SEC-059 [🟡 nit] phase4
FILE: `apps/api/src/users/dto/create-user.dto.ts:46-52`
TITLE: CreateUserDto.login and ImportUserDto.login missing @MaxLength — unbounded login field
REASON: Verified create-user.dto.ts: MaxLength IS imported (line 11) but is not applied to login (lines 48-50: @IsString() @MinLength(3) login: string — no @MaxLength). Verified import-users.dto.ts: MaxLength IS imported (line 6) but not applied to login (lines 28-30: @IsString() @MinLen
FIX: apps/api/src/users/dto/create-user.dto.ts and apps/api/src/users/dto/import-users.dto.ts: add @MaxLength(50) after @MinLength(3) on the login field in both DTOs. MaxLength is already imported in both files. Failing test: POST /users with login of 51 chars (authenticated, users:create) should return HTTP 400.

### SA-SEC-004 [🟡 nit] phase4
FILE: `apps/api/src/auth/dto/refresh-token.dto.ts:1-22`
TITLE: RefreshTokenDto missing @MaxLength — unbounded refresh token in POST /auth/refresh
REASON: Verified in current code: apps/api/src/auth/dto/refresh-token.dto.ts imports only `IsString, IsOptional` from class-validator (line 2). Both RefreshTokenDto.refreshToken (lines 9-11) and LogoutDto.refreshToken (lines 19-21) have no @MaxLength decorator. The 48-byte base64url toke
FIX: In refresh-token.dto.ts, add `MaxLength` to the import and apply `@MaxLength(256)` to both refreshToken fields (generous headroom above the 64-char token). Failing test: POST /auth/refresh with 10KB body refreshToken expects 400. No shape-change risk.

### SA-SEC-010 [🟡 nit] phase4
FILE: `apps/api/src/epics/dto/create-epic.dto.ts:24-27`
TITLE: CreateEpicDto.description and CreateMilestoneDto.description missing @MaxLength
REASON: Verified in current code: create-epic.dto.ts:24-27 has `@IsString() @IsOptional() description?: string` with no @MaxLength. create-milestone.dto.ts:20-23 has the identical pattern. Both files import MaxLength (line 8 in each) and apply it to the `name` field, but not to `descript
FIX: In both create-epic.dto.ts and create-milestone.dto.ts, add `@MaxLength(5000)` to the description field. Failing tests: POST /epics and POST /milestones with 5001-char description expect 400. No shape-change or pagination risk.

### SEC-066 [🔵 suggestion] phase5
FILE: `apps/api/src/planning-export/planning-export.service.ts:211-269`
TITLE: ICS import processes unbounded VEVENT count with sequential DB writes and no per-field length cap
REASON: Verified in current code. AC#1 (VEVENT count cap) is fully done: MAX_IMPORT_EVENTS=500 at line 230, early return at lines 243-244, createMany at line 315. AC#2 (per-field length truncation) is NOT done: line 296 writes `stripHtml(strVal(vevent.summary)) || 'Evenement importe'` wi
FIX: apps/api/src/planning-export/planning-export.service.ts: After stripHtml(strVal(vevent.summary)), apply .slice(0, 200) for title and .slice(0, 5000) for description before pushing to batch. Failing test: pass a VEVENT with a 500-char SUMMARY; assert batch[0].title.length <= 200.

======================================================================
# FAMILY 3-DB-CHECK — 8 findings
======================================================================

### DAT-028 [🟡 nit] phase4
FILE: `packages/database/prisma/migrations/20260525190000_audit_logs_immutability_hash_chain_actor_snapshot/migration.sql:101-104`
TITLE: audit_logs actorId FK retains ON UPDATE CASCADE after migration removes ON DELETE CASCADE — asymmetry can trigger immutability violation
REASON: Verified packages/database/prisma/migrations/20260525190000_audit_logs_immutability_hash_chain_actor_snapshot/migration.sql:102-104: `ADD CONSTRAINT audit_logs_actorId_fkey FOREIGN KEY (actorId) REFERENCES users(id) ON DELETE NO ACTION ON UPDATE CASCADE`. No subsequent migration 
FIX: New migration: ALTER TABLE audit_logs DROP CONSTRAINT audit_logs_actorId_fkey; ALTER TABLE audit_logs ADD CONSTRAINT audit_logs_actorId_fkey FOREIGN KEY (actorId) REFERENCES users(id) ON DELETE NO ACTION ON UPDATE NO ACTION; Failing test: integration test attempting UPDATE users SET id=gen_random_uuid() WHERE id=<testUserId> asserts FK vi

### DAT-024 [🟡 nit] phase4
FILE: `packages/database/prisma/migrations/20260224231534_add_event_recurrence/migration.sql:4`
TITLE: events.recurrenceDay has no CHECK constraint — invalid day-of-week/month values silently accepted
REASON: Grepped all migrations for 'recurrenceDay' and 'recurrenceWeekInterval' combined with 'check|constraint|BETWEEN' — zero hits. The only migration touching these columns is 20260224231534_add_event_recurrence which adds them as bare INTEGER. Schema.prisma confirms `recurrenceDay In
FIX: Add a migration: `ALTER TABLE "events" ADD CONSTRAINT "events_recurrenceDay_ck" CHECK ("recurrenceDay" IS NULL OR "recurrenceDay" BETWEEN 0 AND 6); ALTER TABLE "events" ADD CONSTRAINT "events_recurrenceWeekInterval_ck" CHECK ("recurrenceWeekInterval" IS NULL OR "recurrenceWeekInterval" >= 1);`. Witness: integration spec inserting recurren

### DAT-027 [🟡 nit] phase4
FILE: `packages/database/prisma/migrations/20260321112607_add_predefined_tasks_telework_recurring_password_reset/migration.sql:7-53`
TITLE: telework_recurring_rules.dayOfWeek and predefined_task_recurring_rules.dayOfWeek have no CHECK BETWEEN 0 AND 6
REASON: Migration 20260605201203_dat011_012_recurrence_check adds ptrr_ranges_ck on predefined_task_recurring_rules which includes `("dayOfWeek" IS NULL OR ("dayOfWeek" BETWEEN 0 AND 6))` — confirmed read. For telework_recurring_rules, grepped all migrations for 'twr|telework_recurring' 
FIX: Add a migration: `ALTER TABLE "telework_recurring_rules" ADD CONSTRAINT "twr_dayofweek_ck" CHECK ("dayOfWeek" BETWEEN 0 AND 6);`. Witness: integration spec inserting dayOfWeek=7 into telework_recurring_rules should raise check_violation. No response-shape risk.

### DAT-029 [🟡 nit] phase4
FILE: `packages/database/prisma/migrations/20260603214608_dat025_document_fk_softdelete/migration.sql:2-3`
TITLE: documents.contentSha256 is TEXT with no CHECK constraint — any arbitrary string can be stored as a SHA-256 hash
REASON: Grepped all migrations for 'contentSha256' combined with 'check|constraint' — zero hits. Migration 20260603214608_dat025_document_fk_softdelete adds the column as bare TEXT with no CHECK. Schema.prisma line 812 confirms `contentSha256 String?` with no annotation. No subsequent mi
FIX: Add a migration: `ALTER TABLE "documents" ADD CONSTRAINT "documents_contentSha256_ck" CHECK ("contentSha256" ~ '^[0-9a-f]{64}$');`. Witness: insert a 32-char string or 'notahex' into documents.contentSha256; expect check_violation. Insert a valid 64-char lowercase hex; expect success.

### SA-DAT-008 [🔵 suggestion] phase5
FILE: `packages/database/prisma/schema.prisma:968, 1007, 1114`
TITLE: Event.recurrenceDay, TeleworkRecurringRule.dayOfWeek, and PredefinedTaskRecurringRule.dayOfWeek have no BETWEEN 0 AND 6 DB CHECK
REASON: Confirmed still open for events.recurrenceDay and telework_recurring_rules.dayOfWeek. Migration 20260605201203_dat011_012_recurrence_check adds ptrr_ranges_ck with `dayOfWeek BETWEEN 0 AND 6` but it targets ONLY `predefined_task_recurring_rules` (both ALTER TABLE statements in th
FIX: New migration adding: `ALTER TABLE events ADD CONSTRAINT events_recurrenceDay_ck CHECK (recurrenceDay IS NULL OR recurrenceDay BETWEEN 0 AND 6); ALTER TABLE telework_recurring_rules ADD CONSTRAINT telework_recurring_rules_dayofweek_ck CHECK (dayOfWeek BETWEEN 0 AND 6);`. Failing-test witness: int.spec.ts — INSERT event with recurrenceDay=

### SA-DAT-009 [🔵 suggestion] phase5
FILE: `packages/database/prisma/schema.prisma:1116-1119, 967`
TITLE: PredefinedTaskRecurringRule.weekInterval, monthlyOrdinal, monthlyDayOfMonth and Event.recurrenceWeekInterval have no DB-level range CHECKs
REASON: Confirmed still open for events.recurrenceWeekInterval. Migration 20260605201203_dat011_012_recurrence_check adds ptrr_ranges_ck with `weekInterval >= 1` on predefined_task_recurring_rules, plus monthlyOrdinal 1-5 and monthlyDayOfMonth 1-31 also on that same table only. Schema.pr
FIX: New migration: `ALTER TABLE events ADD CONSTRAINT events_recurrenceweekinterval_ck CHECK (recurrenceWeekInterval IS NULL OR recurrenceWeekInterval > 0);`. Failing-test witness: int.spec.ts — INSERT event with recurrenceWeekInterval=0 → check_violation; INSERT with recurrenceWeekInterval=NULL → passes.

### SA-DAT-010 [🔵 suggestion] phase5
FILE: `packages/database/prisma/schema.prisma:326-328, 956-958, 1062-1063`
TITLE: startTime/endTime string columns have no DB CHECK enforcing endTime >= startTime
REASON: Confirmed: no endTime >= startTime ordering validation exists anywhere. The DTOs for tasks (create-task.dto.ts:189-204), events (create-event.dto.ts:55-70), and predefined-tasks (create-predefined-task.dto.ts:64-77) validate only format via @Matches HH:MM regex — no cross-field o
FIX: New migration (after pre-flight: SELECT COUNT(*) WHERE startTime IS NOT NULL AND endTime IS NOT NULL AND endTime < startTime — if zero, safe to add): `ALTER TABLE tasks ADD CONSTRAINT tasks_time_order_ck CHECK (startTime IS NULL OR endTime IS NULL OR endTime >= startTime); ALTER TABLE events ADD CONSTRAINT events_time_order_ck CHECK (star

### SA-DAT-011 [🔵 suggestion] phase5
FILE: `packages/database/prisma/schema.prisma:242-259`
TITLE: ProjectMember.startDate/endDate has no date-ordering CHECK (endDate >= startDate)
REASON: Confirmed: schema.prisma:244-261 shows ProjectMember with startDate DateTime? and endDate DateTime?, no ordering constraint. Migration 20260527120000_dat003_dat004_business_invariants adds endDate >= startDate CHECKs on projects, epics, leaves, telework_recurring_rules, leave_val
FIX: New migration: `ALTER TABLE project_members ADD CONSTRAINT project_members_dates_ck CHECK (endDate IS NULL OR startDate IS NULL OR endDate >= startDate);`. Failing-test witness: int.spec.ts — INSERT project_member with startDate='2027-01-01T00:00:00Z', endDate='2026-01-01T00:00:00Z' → check_violation; INSERT with startDate set, endDate=NU

======================================================================
# FAMILY 4-P2002 — 2 findings
======================================================================

### COR-050 [🟡 nit] phase4
FILE: `apps/api/src/auth/auth.service.ts:286-361`
TITLE: auth.service register() and users.service create(): duplicate-check race condition surfaces as 500 instead of 409 when two concurrent regist
REASON: Confirmed. auth.service.ts:298-320 uses `prisma.user.findFirst()` with OR on email/login before the `prisma.user.create()` at line 343. No try/catch for P2002/PrismaClientKnownRequestError surrounds the create. Grep confirms zero P2002 handling in auth.service.ts. users.service.t
FIX: apps/api/src/auth/auth.service.ts and apps/api/src/users/users.service.ts — wrap each `prisma.user.create()` in try/catch: catch PrismaClientKnownRequestError with code 'P2002', throw ConflictException('Email ou login déjà utilisé'). Test: mock prisma.user.create to throw P2002; assert service returns ConflictException (HTTP 409).

### COR-054 [🟡 nit] phase4
FILE: `apps/api/src/events/events.service.ts:762-783`
TITLE: addParticipant uses check-then-create pattern susceptible to race; duplicate concurrent request leaks P2002 as HTTP 500
REASON: Confirmed at events.service.ts:831-852. addParticipant() does `prisma.eventParticipant.findUnique()` at line 831, checks result at line 842, then `prisma.eventParticipant.create()` at line 847 with no P2002 catch. Two concurrent requests for the same (eventId, userId) can both se
FIX: apps/api/src/events/events.service.ts — wrap `prisma.eventParticipant.create()` in try/catch: map P2002 to BadRequestException('Cet utilisateur est déjà participant'). Alternatively use `upsert` or `createMany({skipDuplicates:true})`. Test: mock prisma.eventParticipant.create to throw P2002; assert service throws BadRequestException (HTTP

======================================================================
# FAMILY 5-AUDIT — 7 findings
======================================================================

### SA-OBS-004 [🟠 important] phase2
FILE: `apps/api/src/audit/audit-action.enum.ts:49-53`
TITLE: DOCUMENT_DOWNLOADED action is defined but wired to no emitter (binary egress unobservable)
REASON: Confirmed open. grep of all non-spec/non-definition TS files for DOCUMENT_DOWNLOADED returns only audit.service.ts:97 (ENTITY_TYPE map) and payload-schemas.ts:204 (schema definition) — zero emitters exist. The comment at audit-action.enum.ts:58-63 explains the bypass architecture
FIX: Option (a): apps/api/src/documents/documents.service.ts — add a proxied download endpoint that streams from Document.url through the API and emits AuditAction.DOCUMENT_DOWNLOADED on success. Option (b): amend audit-action.enum.ts comment to include 'SA-OBS-004 open — RGPD remediation required: track via <issue>' and create a formal RGPD t

### SA-DAT-002 [🟠 important] phase2
FILE: `packages/database/prisma/schema.prisma:763-778`
TITLE: UserSkill.validatedBy is an untyped String? with no FK to users — dangling reference risk
REASON: Confirmed open. schema.prisma:779 still declares 'validatedBy String?' with no Prisma @relation and no @map annotation. The init migration (20251116093059) added it as plain TEXT with no FK. A search across all 74+ migrations finds no subsequent migration adding a FK constraint o
FIX: packages/database/prisma/schema.prisma: rename 'validatedBy String?' to 'validatedBySnapshot String?' (frozen UUID or display-name at write time, matching the audit_logs.actorLabel snapshot pattern) — avoids a FK migration on a composite-PK table and eliminates dangling-reference risk. Alternatively add 'validatedById String? @map("valida

### OBS-021 [🟡 nit] phase4
FILE: `apps/api/src/auth/auth.service.ts:524-529`
TITLE: generateResetToken() logs PASSWORD_CHANGED (wrong action) — action mislabeling confirmed, non-persistence claim refuted
REASON: Verified in apps/api/src/auth/auth.service.ts:537-542: `this.auditService.log({ action: AuditAction.PASSWORD_CHANGED, ... details: 'Password reset token generated for user ${targetUser.id}' })`. The AuditAction enum (audit-action.enum.ts) has PASSWORD_CHANGED (line 35) and PASSWO
FIX: 1. apps/api/src/audit/audit-action.enum.ts: add `PASSWORD_RESET_TOKEN_ISSUED = 'PASSWORD_RESET_TOKEN_ISSUED'`. 2. apps/api/src/audit/audit.service.ts ENTITY_TYPE_BY_ACTION: add entry 'User'. 3. apps/api/src/audit/payload-schemas.ts: add schema entry. 4. auth.service.ts:538: replace AuditAction.PASSWORD_CHANGED with AuditAction.PASSWORD_RE

### OBS-022 [🟡 nit] phase4
FILE: `apps/api/src/comments/comments.service.ts:160-184`
TITLE: comments: create/update/delete emit no audit_log row
REASON: Verified apps/api/src/comments/comments.service.ts: grep for AuditPersistenceService, auditPersistence, AuditService returns nothing. The remove() method (lines 160-185) calls prisma.comment.delete() and returns a plain message object with no audit call whatsoever, even for cross
FIX: Inject AuditPersistenceService into CommentsService. Emit COMMENT_DELETED (with comment.id, comment.authorId, actorId) in remove() after the DB delete, specifically (or at minimum) when actorId !== comment.authorId. Failing test: POST /comments/:id as admin on another user's comment → assert audit_logs row with action=COMMENT_DELETED exis

### OBS-024 [🟡 nit] phase4
FILE: `apps/api/src/events/events.service.ts:578-585`
TITLE: events: create/update/delete and participant mutations emit no audit_log row
REASON: Verified apps/api/src/events/events.service.ts: grep for AuditPersistenceService, auditPersistence, AuditService returns nothing. The remove() method (line 613), addParticipant(), removeParticipant(), and stopRecurrence() all write to the DB without any audit emission. The siblin
FIX: Inject AuditPersistenceService into EventsService. Add EVENT_CREATED, EVENT_UPDATED, EVENT_DELETED, EVENT_PARTICIPANT_ADDED, EVENT_PARTICIPANT_REMOVED to AuditAction enum and ENTITY_TYPE_BY_ACTION. Use void .catch() fire-and-forget pattern (as in documents.service.ts DOCUMENT_READ). Failing test: DELETE /events/:id → assert audit_logs row

### OBS-025 [🟡 nit] phase4
FILE: `apps/api/src/projects/projects.service.ts:891-1041`
TITLE: Project member add/update/remove emit no audit rows
REASON: Verified apps/api/src/projects/projects.service.ts: AuditPersistenceService IS injected (lines 19, 45) and used for PROJECT_DELETED (line 931) and other project-level writes (lines 665, 694, 729, 761, 799). However addMember() (lines 945-1005), updateMember() (lines 1011-1057), a
FIX: Add PROJECT_MEMBER_ADDED, PROJECT_MEMBER_UPDATED, PROJECT_MEMBER_REMOVED to AuditAction enum and ENTITY_TYPE_BY_ACTION. Emit in each method after the DB write using the existing this.auditPersistence.log() call pattern (matching style at line 931). Failing test: POST /projects/:id/members → assert audit_logs row with action=PROJECT_MEMBER

### SA-OBS-012 [🟡 nit] phase4
FILE: `packages/database/prisma/migrations/20260525190000_audit_logs_immutability_hash_chain_actor_snapshot/migration.sql:1-123`
TITLE: No declared retention policy or partitioning for audit_logs; unbounded table growth
REASON: Verified: grep for retention, pg_partman, and partition.*audit across all migrations returns nothing referencing audit_logs retention policy (only unrelated retention comment in dat008_026 migration about app not being legal SIRH). The 20260525190000 migration establishes immutab
FIX: Minimum: add a SQL COMMENT ON TABLE audit_logs documenting a retention policy. Better: migration implementing pg_partman range partitioning by month on createdAt with archive/detach semantics. Note: partitioning must preserve hash-chain ordering invariant (the dual-hash scheme uses position-based chain; range partitioning by month preserv

======================================================================
# FAMILY 6-DB-INDEX — 4 findings
======================================================================

### PER-055 [🟡 nit] phase4
FILE: `apps/api/src/telework/telework.service.ts:431-449`
TITLE: TeleworkSchedule: no index on (userId, date) range queries — only unique constraint
REASON: Verified: packages/database/prisma/schema.prisma lines 734-747 — TeleworkSchedule only has `@@unique([userId, date])`, no `@@index([date, userId])`. The `findForPlanningOverview` query at telework.service.ts lines 289-296 queries `{ userId: { in: [...new Set(userIds)] }, date: { 
FIX: Add `@@index([date, userId])` to TeleworkSchedule in schema.prisma, then run `pnpm run db:migrate`. Verify with EXPLAIN ANALYZE on a large table.

### PER-060 [🟡 nit] phase4
FILE: `packages/database/prisma/schema.prisma:1081-1108`
TITLE: PredefinedTaskAssignment has no index on predefinedTaskId — per-task assignment lookups may seq-scan
REASON: Verified: packages/database/prisma/schema.prisma lines 1117-1119 — PredefinedTaskAssignment has `@@unique([predefinedTaskId, userId, date, period])`, `@@index([date, userId])`, `@@index([completionStatus, date])` but NO `@@index([userId])`. The controller at predefined-tasks.cont
FIX: Add `@@index([userId])` to PredefinedTaskAssignment in schema.prisma, then run `pnpm run db:migrate`.

### COR-059 [🟡 nit] phase4
FILE: `apps/api/src/rbac/roles.service.ts:139-157`
TITLE: roles.service createRole()/updateRole(): isDefault singleton management is non-atomic — two concurrent requests can create two roles with is
REASON: Confirmed at roles.service.ts:139-156: createRole() calls `await this.unsetCurrentDefault()` at line 143, then `await this.prisma.role.create({...isDefault:dto.isDefault??false,...})` at line 146 as two separate non-transactional operations. updateRole() at lines 205-215 has the 
FIX: apps/api/src/rbac/roles.service.ts — wrap `unsetCurrentDefault()` + `role.create/update` in `this.prisma.$transaction(async (tx) => {...})` passing tx to both operations. Consider adding a DB-level partial unique index in schema.prisma via raw migration. Test: mock both DB operations and verify they use the same transaction context.

### SA-PERF-025 [🔵 suggestion] phase5
FILE: `apps/api/src/audit/audit-persistence.service.ts:154-156`
TITLE: AuditLog has no index on createdAt+id — ORDER BY createdAt DESC, id DESC scans the full table
REASON: Verified in current code. audit-persistence.service.ts:154-156 runs `SELECT rowHash FROM audit_logs ORDER BY createdAt DESC, id DESC LIMIT 1` on every audit write. schema.prisma AuditLog model has @@index([entityType, entityId]), @@index([actorId, createdAt]), @@index([schemaVers
FIX: New raw SQL migration: `CREATE INDEX CONCURRENTLY audit_logs_created_at_id_desc_idx ON audit_logs (createdAt DESC, id DESC);`. Use CONCURRENTLY to avoid locking the append-only table in production. Verify with EXPLAIN (ANALYZE, BUFFERS) SELECT rowHash FROM audit_logs ORDER BY createdAt DESC, id DESC LIMIT 1 — should show Index Scan not So

======================================================================
# FAMILY 7-TEST-SPEC — 25 findings
======================================================================

### TST-002 [🟠 important] phase2
FILE: `apps/api/src/:N/A`
TITLE: 13 controllers have no sibling *.spec.ts file
REASON: Confirmed: `find apps/api/src -name '*.controller.ts' | while read f; do base=$(echo $f | sed 's/.ts$/.spec.ts/'); [ -f "$base" ] || echo "MISSING: $f"; done` returns 13 missing controller specs today. The only exception is metrics.controller.ts which has 12 references to Metrics
FIX: Create *.controller.spec.ts for each of the 12 uncovered controllers following apps/api/src/tasks/tasks.controller.spec.ts as template. Priority: rbac/roles.controller.spec.ts, leave-types/leave-types.controller.spec.ts, settings/settings.controller.spec.ts. RED witness: find command returns 0 lines after fix.

### TST-003 [🟠 important] phase2
FILE: `apps/api/src/analytics/analytics.controller.spec.ts:8-88`
TITLE: analytics.controller.spec.ts has only happy-path tests — no error/auth failure coverage
REASON: Confirmed: apps/api/src/analytics/analytics.controller.spec.ts is 88 lines with exactly 3 tests (lines 33, 38, 63). `grep -c 'rejects|throw|Forbidden|NotFound|403|404|400' analytics.controller.spec.ts` returns 0. The only tests are 'should be defined', happy-path getAnalytics, an
FIX: Add to analytics.controller.spec.ts: (1) test where mockService.getAnalytics rejects with NotFoundException; (2) test where mockService.exportAnalytics rejects with ForbiddenException; (3) RequirePermissions metadata assertions. RED witness: grep -c 'rejects|NotFoundException|ForbiddenException' must be > 0.

### TST-004 [🟠 important] phase2
FILE: `apps/api/src/events/events.controller.spec.ts:11-287`
TITLE: events.controller.spec.ts, clients.controller.spec.ts, and third-parties.controller.spec.ts have zero error-path tests
REASON: Confirmed: all three controller specs exist (events.controller.spec.ts, clients.controller.spec.ts, third-parties.controller.spec.ts). `grep -c 'rejects|throw|Forbidden|NotFound|403|404|400|409|Conflict'` returns 0 for all three files. Zero error-path tests in each.
FIX: For each of the three controller specs, add at least one it() block where the mocked service rejects with a typed NestJS exception (NotFoundException for findOne, ConflictException for create duplicate). RED witness: grep -c 'rejects' events.controller.spec.ts must be > 0.

### TST-005 [🟠 important] phase2
FILE: `apps/api/src/leave-types/leave-types.service.spec.ts:1-234`
TITLE: LeaveTypesService.reorder() method has zero test coverage — $transaction branch untested
REASON: Confirmed: apps/api/src/leave-types/leave-types.service.ts has reorder() at line 194. apps/api/src/leave-types/leave-types.service.spec.ts is 253 lines with describe blocks for create/findAll/findOne/update/remove/SA-DAT-003 but `grep -c 'reorder' leave-types.service.spec.ts` ret
FIX: Add `describe('reorder')` block to leave-types.service.spec.ts covering: (1) happy path asserting $transaction called with N update promises each with correct sortOrder, (2) then calls findAll(). RED witness: grep -c 'reorder' leave-types.service.spec.ts must be > 0.

### TST-006 [🟠 important] phase2
FILE: `apps/api/src/planning-export/planning-export.controller.spec.ts:12-39`
TITLE: planning-export.controller.spec.ts has only happy-path tests — no error propagation tests
REASON: Confirmed: apps/api/src/planning-export/planning-export.controller.spec.ts is 39 lines with 3 tests (lines 24, 28, 34). exportIcs mock is defined at line 7 but `grep -c 'rejects|throw|exportIcs.*test|it.*exportIcs'` returns 0 — no test calls controller.exportIcs() and no error-pa
FIX: Add: (1) test for exportIcs controller method; (2) test where previewImport service rejects with BadRequestException; (3) test where importIcs service rejects with NotFoundException. RED witness: grep -c 'rejects' planning-export.controller.spec.ts must be > 0.

### TST-007 [🟠 important] phase2
FILE: `e2e/helpers.ts:1-13`
TITLE: Root-level e2e specs use UI login helper with hardcoded admin/admin123 credentials
REASON: Confirmed: e2e/helpers.ts exports `login(page, username='admin', password='admin123')` — UI-based login via page.fill+click. All 5 root-level specs (projects, leaves, tasks, planning, full-workflow) import from './helpers' and call await login(page) in beforeEach. The 'chromium' 
FIX: Add `dependencies: ['setup']` and `storageState: authPath('admin')` to the chromium project, or migrate the 5 specs to e2e/tests/ using the storageState pattern. Remove or restrict helpers.ts. RED witness: grep -rn 'await login(' e2e/*.spec.ts must return 0.

### SA-TEST-001 [🟠 important] phase2
FILE: `e2e/full-workflow.spec.ts:1-9`
TITLE: 5 legacy E2E specs use UI login (helpers.ts) instead of API-based storageState
REASON: Confirmed duplicate of TST-007 (sessionA namespaced). Same 5 root-level e2e specs (full-workflow.spec.ts:2, planning.spec.ts:2, leaves.spec.ts:2, tasks.spec.ts:2, projects.spec.ts:2) all import from './helpers' and call await login(page). The chromium playwright project has no de
FIX: Same fix as TST-007: add storageState+dependencies to chromium playwright project, replace login() calls with storageState. RED witness: grep -rn 'import.*helpers' e2e/*.spec.ts must return 0.

### SA-TEST-002 [🟠 important] phase2
FILE: `e2e/leaves.spec.ts:38-43`
TITLE: e2e/leaves.spec.ts: tautological assertions — test always passes even when page is broken
REASON: Confirmed: e2e/leaves.spec.ts line 41 defines `const isOnLeavesPage = page.url().includes('/leaves')` — always true after page.goto('/leaves'). Line 43 `expect(hasList || hasEmptyMessage || isOnLeavesPage).toBeTruthy()` is a tautological assertion that cannot fail. Line 208 repea
FIX: Replace `expect(hasList || hasEmptyMessage || isOnLeavesPage).toBeTruthy()` with `await expect(page.locator('h1')).toContainText(/congés/i)`. Remove catch swallows. RED witness: grep -c 'isOnLeavesPage' e2e/leaves.spec.ts must return 0.

### SA-TEST-003 [🟠 important] phase2
FILE: `e2e/planning.spec.ts:170`
TITLE: e2e/planning.spec.ts: two `expect(true).toBeTruthy()` assertions — tests can never fail
REASON: Confirmed: e2e/planning.spec.ts line 170 has `expect(true).toBeTruthy()` (drag-and-drop placeholder) and line 257 has `expect(true).toBeTruthy()` ('navigate to today' placeholder). `grep -n 'expect(true).toBeTruthy' e2e/planning.spec.ts` returns both lines. Line 199 also has `.ca
FIX: Replace `expect(true).toBeTruthy()` with meaningful assertions (e.g., `expect(page.url()).toContain('/planning')` as a floor, or add data-testid to planning grid cells and assert task location after drag). RED witness: grep -c 'expect(true).toBeTruthy' e2e/planning.spec.ts must return 0.

### SA-TEST-004 [🟠 important] phase2
FILE: `apps/api/src/settings/settings.controller.ts:1-110`
TITLE: settings.controller.ts has no controller spec (8 endpoints, all @RequirePermissions)
REASON: Confirmed: `find apps/api/src/settings -name '*.controller.spec.ts'` returns nothing. Directory contains only settings.controller.ts, settings.service.ts, settings.service.spec.ts, settings.module.ts, and dto/. No controller spec exists.
FIX: Create apps/api/src/settings/settings.controller.spec.ts following tasks.controller.spec.ts pattern: mock SettingsService, test all 8 endpoints for happy path and NotFoundException/BadRequestException. RED witness: find apps/api/src/settings -name '*.controller.spec.ts' must return a path.

### SA-TEST-005 [🟠 important] phase2
FILE: `apps/api/src/leave-types/leave-types.controller.ts:1-120`
TITLE: leave-types.controller.ts has no controller spec (7 endpoints, all @RequirePermissions)
REASON: Confirmed: `find apps/api/src/leave-types -name '*.controller.spec.ts'` returns nothing. Directory contains leave-types.controller.ts, leave-types.service.ts, leave-types.service.spec.ts, leave-types.module.ts, and dto/ — no controller spec.
FIX: Create apps/api/src/leave-types/leave-types.controller.spec.ts: mock LeaveTypesService, test all 7 endpoints including reorder happy-path, NotFoundException on unknown id, BadRequestException on system-type deletion. RED witness: find returns a path.

### SA-TEST-006 [🟠 important] phase2
FILE: `apps/api/src/rbac/roles.controller.ts:1-120`
TITLE: rbac/roles.controller.ts has no controller spec (6 endpoints, all @RequirePermissions)
REASON: Confirmed: apps/api/src/rbac/__tests__/ contains permissions.guard.spec.ts, permissions.service.spec.ts, roles.service.spec.ts but no roles.controller.spec.ts. `find apps/api/src/rbac -name 'roles.controller.spec.ts'` returns nothing.
FIX: Create apps/api/src/rbac/__tests__/roles.controller.spec.ts: mock RolesService, test listTemplates/create/findAll/findOne/update/delete with happy paths plus ForbiddenException for system-role deletion and ConflictException for roles-with-users deletion. RED witness: find returns a path.

### SA-TEST-007 [🟠 important] phase2
FILE: `apps/api/src/analytics/advanced/analytics-advanced.controller.ts:1-140`
TITLE: analytics-advanced.controller.ts has no controller spec (6 endpoints, all @RequirePermissions)
REASON: Confirmed: apps/api/src/analytics/advanced/ contains analytics-advanced.controller.ts, analytics-advanced.module.ts, dto/, services/, snapshot-scheduler.service.spec.ts, snapshot-scheduler.service.ts — but no analytics-advanced.controller.spec.ts. `find apps/api/src/analytics/adv
FIX: Create apps/api/src/analytics/advanced/analytics-advanced.controller.spec.ts: mock all 6 analytics services, verify each endpoint forwards currentUser context correctly and RequirePermissions('reports:view') metadata is present. RED witness: find returns a path.

### SA-TEST-013 [🟠 important] phase2
FILE: `e2e/tasks.spec.ts:18-28`
TITLE: e2e/tasks.spec.ts: task-list test swallows selector failure and asserts only URL
REASON: Confirmed: e2e/tasks.spec.ts is 29 lines with 2 tests. The 'should display task list' test (lines 15-28) wraps waitForSelector in `.catch(() => {` (line 23, swallowing selector failures) and then asserts only `expect(page.url()).toContain('/tasks')` (line 27) — a URL always true 
FIX: Replace the catch+URL-only assertion with `await expect(page.locator('h1')).toContainText(/tâches|tasks/i)` consistent with the first test. RED witness: grep -c '.catch(() => {' e2e/tasks.spec.ts must return 0.

### TST-009 [🟡 nit] phase4
FILE: `apps/api/src/common/fastify/uploads-auth.hook.spec.ts:83-96`
TITLE: expect(res.statusCode).not.toBe(401) in uploads-auth.hook.spec.ts passes on 200/500/404 — intent is ambiguous
REASON: Read uploads-auth.hook.spec.ts in full. Line 95 is confirmed: `expect(res.statusCode).not.toBe(401)` on the OPTIONS preflight to AVATAR_URL. The inline comment at lines 93-95 explicitly acknowledges the actual value is 404 ('Fastify routing yields 404'). Line 129 in a separate te
FIX: In uploads-auth.hook.spec.ts line 95, replace `expect(res.statusCode).not.toBe(401)` with `expect(res.statusCode).toBe(404)` and update the comment to explain 404 is Fastify routing with no CORS plugin. Witness: changing the assertion to toBe(200) or toBe(401) should make the test RED.

### TST-010 [🟡 nit] phase4
FILE: `apps/api/src/leaves/leaves-balance-gating.int.spec.ts:269-271`
TITLE: describe.skipIf in leaves-balance-gating.int.spec.ts silently disables mutation witness in default CI
REASON: Confirmed `const runMutationWitness = process.env.MUTATION_WITNESS === '1'` at leaves-balance-gating.int.spec.ts line 269 and `describe.skipIf(!runMutationWitness)` at line 271. Grepped all .github/workflows/ for 'MUTATION_WITNESS': zero hits. The mutation witness (the definitive
FIX: Add a dedicated CI job (or scheduled workflow step) that sets MUTATION_WITNESS=1 and runs `pnpm --filter api test:integration`. Alternatively, document it as a required manual step before any leave-balance code change, with a comment in the spec referencing that document.

### SA-TEST-008 [🟡 nit] phase4
FILE: `apps/api/src/planning/planning.controller.ts:1-48`
TITLE: planning.controller.ts has no controller spec (1 endpoint, @RequirePermissions)
REASON: `find apps/api/src/planning -name '*.spec.ts'` returns only planning.service.spec.ts. No planning.controller.spec.ts exists. PlanningController confirmed at planning.controller.ts: single `@Get('overview')` endpoint with `@RequirePermissions('users:read')` that passes `query.star
FIX: Create apps/api/src/planning/planning.controller.spec.ts. Mock PlanningService; assert getOverview forwards startDate, endDate from query and user.id from @CurrentUser to planningService.getOverview. Witness: RED if the controller ignores user.id.

### SA-TEST-009 [🟡 nit] phase4
FILE: `apps/api/src/personal-todos/personal-todos.controller.ts:1-50`
TITLE: personal-todos.controller.ts has no controller spec (@AllowSelfService on all 4 endpoints)
REASON: `find apps/api/src/personal-todos -name '*.spec.ts'` returns only personal-todos.service.spec.ts. No personal-todos.controller.spec.ts exists. Controller confirmed: all 4 endpoints use `@AllowSelfService()` and pass `user.id` to the service. Correct user.id threading is untested 
FIX: Create apps/api/src/personal-todos/personal-todos.controller.spec.ts. Mock PersonalTodosService; assert each endpoint passes currentUser.id to the service. Witness: RED if user.id is hardcoded or omitted.

### SA-TEST-010 [🟡 nit] phase4
FILE: `apps/api/src/holidays/holidays.controller.ts:1-40`
TITLE: holidays.controller.ts has no controller spec (9 endpoints, all @RequirePermissions)
REASON: `find apps/api/src/holidays -name '*.spec.ts'` returns only holidays.service.spec.ts. No holidays.controller.spec.ts exists. HolidaysController confirmed: 9 endpoints all decorated with @RequirePermissions. Permission guard wiring is untested at the controller layer.
FIX: Create apps/api/src/holidays/holidays.controller.spec.ts. Mock HolidaysService; test that GET / calls service.findAll and passes query params. Witness: RED if the controller skips a param.

### SA-TEST-011 [🟡 nit] phase4
FILE: `apps/api/src/school-vacations/school-vacations.controller.ts:1-60`
TITLE: school-vacations.controller.ts has no controller spec (6 endpoints, all @RequirePermissions)
REASON: `find apps/api/src/school-vacations -name '*.spec.ts'` returns only school-vacations.spec.ts (service-only). No school-vacations.controller.spec.ts exists. Controller confirmed at school-vacations.controller.ts: injects both SchoolVacationsService and SettingsService. The importF
FIX: Create apps/api/src/school-vacations/school-vacations.controller.spec.ts. Mock both SchoolVacationsService and SettingsService; test that importFromOpenData calls settingsService.getValue to get the default zone. Witness: RED if zone-defaulting logic is removed from the controller.

### SA-TEST-012 [🟡 nit] phase4
FILE: `apps/api/src/clients/projects-clients.controller.ts:1-65`
TITLE: Three sub-controllers have no specs: projects-clients, projects-third-party-members, tasks-third-party-assignees
REASON: `find apps/api/src -name 'projects-clients.controller.spec.ts' -o -name 'projects-third-party-members.controller.spec.ts' -o -name 'tasks-third-party-assignees.controller.spec.ts'` returns no output — all three sub-controller specs are absent. ProjectsClientsController confirmed:
FIX: Add specs to apps/api/src/clients/ and apps/api/src/third-parties/ for the sub-resource controllers. Mock parent services; assert assign endpoint forwards projectId to service. Add a 404 error path test. Witness: RED if projectId is ignored.

### SA-TEST-019 [🟡 nit] phase4
FILE: `e2e/tests/avatar-screenshots.spec.ts:1-30`
TITLE: e2e/tests/avatar-screenshots.spec.ts: 8 tests with zero assertions (screenshot-only)
REASON: Read e2e/tests/avatar-screenshots.spec.ts in full (29 lines). `grep -c 'expect' avatar-screenshots.spec.ts` returns 0 — confirmed zero assertions. The file contains 8 tests that navigate to 8 URLs, wait 1500ms, and write screenshots. Tests can only fail on navigation timeout, nev
FIX: Delete e2e/tests/avatar-screenshots.spec.ts. If visual regression on avatar rendering is desired, promote assertions from e2e/tests/avatar-unification.spec.ts to cover the same 8 zones with locator-based assertions.

### SA-TEST-015 [🟡 nit] phase4
FILE: `e2e/tests/kanban.spec.ts:64`
TITLE: e2e/tests/kanban.spec.ts: two unconditional test.skip(true, ...) due to seed-dependency
REASON: Read e2e/tests/kanban.spec.ts in full. Confirmed `test.skip(true, 'No TODO task available')` at line 64 and `test.skip(true, 'Need at least 2 TODO tasks to verify sort order')` at line 102. A third conditional skip exists at lines 25-28 ('No TODO task to drag'). All three skips a
FIX: In kanban.spec.ts, add a beforeAll that creates at least 2 TODO tasks via POST /api/tasks (with admin Bearer token) and an afterAll that deletes them. Remove the conditional test.skip blocks. Witness: with beforeAll creating fixtures, the tests should never skip. RED if the drag-and-drop logic breaks.

### TST-012 [🔵 suggestion] phase5
FILE: `e2e/fixtures/permission-matrix.ts:389-407`
TITLE: Permission matrix has no entry for GET /api/analytics/advanced/* endpoints (6 routes using reports:view)
REASON: Verified in current code. analytics-advanced.controller.ts has 6 GET routes (lines 49-139) each decorated with @RequirePermissions('reports:view'). grep for 'analytics/advanced' in e2e/fixtures/permission-matrix.ts returns empty — no matrix entries exist for any advanced analytic
FIX: e2e/fixtures/permission-matrix.ts: Add at least one entry for /api/analytics/advanced/workload with allowedRoles: ['admin','responsable','manager','observateur'] and deniedRoles: ['referent','contributeur'] — same RBAC profile as reports:view. One representative entry validates the guard wiring for the entire controller.

### SA-TEST-018 [🔵 suggestion] phase5
FILE: `packages/database/prisma/migrations:start-end`
TITLE: 73 migrations total; 50 have no dedicated integration spec (only schema-constraint-named ones covered)
REASON: Confirmed: 24 int.spec.ts files exist in apps/api/src/schema-constraints/. The file dat008-015-check-constraints.int.spec.ts exists but covers project_third_party_members.allocation (a different 'DAT-008') and project_snapshots numeric bounds (a different 'DAT-015') — it does NOT
FIX: Create apps/api/src/schema-constraints/dat015-email-case-insensitive.int.spec.ts: INSERT user with email 'test@example.com', then attempt INSERT with email 'TEST@EXAMPLE.COM' → unique constraint violation from users_email_lower_uk index (real DB, not mock). Create apps/api/src/schema-constraints/dat021-audit-schema-version.int.spec.ts: qu

======================================================================
# FAMILY 8-PAGINATION — 7 findings
======================================================================

### PER-041 [🟡 nit] phase4
FILE: `apps/api/src/comments/comments.service.ts:59-65`
TITLE: findAll comments uses default limit=1000 — very high default page size
REASON: Confirmed at comments.service.ts lines 59-65: async findAll(page = 1, limit = 1000, ...) with safeLimit = Math.min(limit || 1000, 1000). The {data, meta} envelope exists (lines 94-102 return {data, meta: {total, page, limit, totalPages}}). No web comments.service.ts exists in app
FIX: apps/api/src/comments/comments.service.ts line 65: change Math.min(limit || 1000, 1000) to Math.min(limit ?? 1000, 100). Keep the default parameter at 1000 until web callers are audited. The envelope shape is stable, no PER-021 risk. Failing-test witness: GET /comments?limit=200 should return meta.limit === 100.

### PER-043 [🟡 nit] phase4
FILE: `apps/api/src/documents/documents.service.ts:75-81`
TITLE: findAll documents uses default limit=1000 — very high default page size
REASON: Confirmed at documents.service.ts lines 89-95: async findAll(page = 1, limit = 1000, ...) with safeLimit = Math.min(limit || 1000, 1000). The {data, meta} envelope exists (lines 116-124). No web documents.service.ts in apps/web/src/services/. The web types reference documents as 
FIX: apps/api/src/documents/documents.service.ts line 95: change Math.min(limit || 1000, 1000) to Math.min(limit ?? 1000, 100). Keep the default parameter at 1000 until web callers are audited. Envelope shape is stable, no PER-021 risk. Failing-test witness: GET /documents?limit=200 should return meta.limit === 100.

### PER-046 [🟡 nit] phase4
FILE: `apps/api/src/leaves/leaves.service.ts:1101-1139`
TITLE: getOwnLeaves: unbounded findMany on leave history — no limit
REASON: Confirmed at leaves.service.ts lines 1151-1189: prisma.leave.findMany({ where: { userId: currentUserId }, include: { user, leaveType, validator, validatedBy }, orderBy: { startDate: 'desc' } }) — no take. Returns a bare .map(...) array (line 1192-1200), not an envelope. The web s
FIX: apps/api/src/leaves/leaves.service.ts: add take: 200 to the findMany call in getOwnLeaves at line 1151. Keep the bare-array return shape — do NOT add envelope or pagination (PER-021 regression risk since web getMyLeaves() expects bare Leave[]). Failing-test witness: seed a user with 201 leaves, call GET /leaves/me, assert response array l

### PER-047 [🟡 nit] phase4
FILE: `apps/api/src/leaves/leaves.service.ts:761-781`
TITLE: findAll leaves: default limit 1000 with effective cap 500 — higher than recommended 100
REASON: Confirmed at leaves.service.ts line 825: safeLimit = Math.min(limit || 500, 500). Web service at leaves.service.ts:113 (getByUser) passes limit=1000 and at line 145 (getByDateRange) passes limit=1000 — both are already silently capped at 500 by the current code. Triage proposal t
FIX: apps/api/src/leaves/leaves.service.ts line 825: lower cap to 200 — Math.min(limit || 500, 200). Do NOT change the default parameter of 1000, since the web service explicitly passes limit=1000 at lines 113 and 145 (would still hit the 200 cap, not a regression, just a tighter bound). Failing-test witness: GET /leaves?limit=500 should retur

### PER-049 [🟡 nit] phase4
FILE: `apps/api/src/predefined-tasks/predefined-tasks.service.ts:186-231`
TITLE: findAssignments (predefined-tasks): no pagination and no maximum result cap
REASON: Confirmed at predefined-tasks.service.ts lines 225-263: findAssignments calls findMany with date-range filter but no take. Bare array returned. Critically: the predefined-tasks.controller.ts lines 116-128 show the @Get('assignments') endpoint only accepts @Query params userId, st
FIX: apps/api/src/predefined-tasks/predefined-tasks.service.ts: add take: 1000 to the findMany call in findAssignments at line 225. Keep bare-array return shape (the web service AssignmentsResponse unwraps to bare array). Note: the web client sends limit=200 which is silently discarded by the controller — optionally wire up a limit param in th

### PER-056 [🟡 nit] phase4
FILE: `apps/api/src/time-tracking/time-tracking.service.ts:363`
TITLE: findAll time-tracking: effective cap is 1000 rows — far exceeds recommended 100
REASON: Confirmed at time-tracking.service.ts line 435: limit default is now 10 (AC#1 satisfied — default is already below 50). Line 455: safeLimit = Math.min(limit || 1000, 1000) — cap is still 1000, so AC#2 fails (GET /time-tracking?limit=2000 would return meta.limit=1000, not 100). Th
FIX: apps/api/src/time-tracking/time-tracking.service.ts line 455: change Math.min(limit || 1000, 1000) to Math.min(limit || 10, 100). The web service getAll() sends no explicit limit so uses the default=10, which hits the new cap=100 fine. No PER-021 risk since the {data, meta} envelope shape is already in place. Failing-test witness: GET /ti

### PER-039 [🟡 nit] phase4
FILE: `apps/api/src/audit/recompute-chain.ts:87-112`
TITLE: recomputeChainFrom() issues one UPDATE per audit row in a serial for-loop — O(N) round-trips hold the advisory lock for the full duration
REASON: Verified: apps/api/src/audit/recompute-chain.ts lines 96-112 still has a serial `for (const row of rows)` loop issuing one `await tx.$executeRaw UPDATE audit_logs ... WHERE id = ${row.id}` per iteration. No batching is present. The unbounded SELECT at line 87-93 also has no LIMIT
FIX: Compute all (id, prevHash, rowHash) pairs in the JS loop first (chain dependency requires serial computation), then issue batched UPDATE using `FROM (VALUES ...) AS v(id, prev_hash, row_hash) WHERE audit_logs.id = v.id::uuid` in chunks of 1000 via `$executeRaw`.

======================================================================
# FAMILY 9-OTHER — 43 findings
======================================================================

### PER-003 [🟠 important] phase2
FILE: `apps/api/src/common/services/role-hierarchy.service.ts:83-103`
TITLE: assertCanAssignRole executes 4 DB queries when the caller is not ADMIN (resolveTemplateKey called twice in assertCanAssignRole + twice again
REASON: Confirmed in current code at /apps/api/src/common/services/role-hierarchy.service.ts. `assertCanAssignRole` (lines 88-91) calls `this.resolveTemplateKey` twice via `Promise.all`, then on the non-ADMIN path calls `this.canAssignRole(callerRoleCode, targetRoleCode)` at line 98, whi
FIX: apps/api/src/common/services/role-hierarchy.service.ts: extract private `_canAssignRoleFromKeys(callerTplKey: string | null, targetTplKey: string | null): boolean` implementing the rank comparison. Rewrite `canAssignRole` to resolve keys then delegate. Rewrite `assertCanAssignRole` to resolve once, guard ADMIN, then call `_canAssignRoleFr

### SEC-007 [🟠 important] phase2
FILE: `apps/api/src/health/health.service.ts:78-86`
TITLE: Unauthenticated health endpoint reveals per-component infrastructure status (DB/Redis) to any caller
REASON: Confirmed in current code. `health.service.ts` lines 78-86: `ServiceUnavailableException({ status: 'degraded', db: dbStatus, redis: redisStatus })` and `return { status: 'ok', db: dbStatus, redis: redisStatus }`. `health.controller.ts` lines 14-18: `@Public()` decorator with no a
FIX: apps/api/src/health/health.service.ts: define a public `HealthStatus = { status: 'ok' | 'degraded' }` return shape; keep per-component status in a private type. Log component detail server-side (already done via `this.logger.warn`). Return only `{ status }` in the HTTP body. Failing witness: `curl -s GET /api/health | jq 'keys'` → assert 

### SEC-021 [🟠 important] phase2
FILE: `apps/api/src/tasks/tasks.controller.ts:407-429`
TITLE: DELETE /tasks/:taskId/raci/:userId/:role — :role path param is unvalidated (no ParseEnumPipe)
REASON: Confirmed in current code. `tasks.controller.ts` line 422: `@Param('role') role: RACIRole` — no `ParseEnumPipe(RACIRole)` present. `ParseUUIDPipe` is applied to the sibling params at lines 420-421, proving the pattern is known and used selectively. In `tasksService.removeRACI` (l
FIX: apps/api/src/tasks/tasks.controller.ts line 422: change to `@Param('role', new ParseEnumPipe(RACIRole)) role: RACIRole`. Failing witness: send DELETE /tasks/:validId/raci/:validUserId/INVALID_ROLE with a valid auth token → assert HTTP response status is 400 (not 500). DELETE with a valid RACIRole value should still return 200 or 404 (vali

### SA-PERF-013 [🟠 important] phase2
FILE: `apps/api/src/analytics/analytics.service.ts:23`
TITLE: Analytics cache has no mutation invalidation — stale data served for up to 60s after changes
REASON: Confirmed in current code. `analytics.service.ts` line 26: `const ANALYTICS_CACHE_TTL = 60`. `cache.service.ts` lines 52-58: `del(key: string)` accepts only an exact key — no `delByPattern` or SCAN-based wildcard invalidation exists. The comment at line 13 explicitly states 'TTL-
FIX: apps/api/src/common/services/cache.service.ts: add `async delByPattern(pattern: string)` using Redis SCAN + DEL pipeline. apps/api/src/tasks/tasks.service.ts and apps/api/src/projects/projects.service.ts: inject CacheService and call `cache.delByPattern('cache:analytics:*')` in mutation methods (update status, archive, unarchive). Failing

### SA-DAT-005 [🟠 important] phase2
FILE: `packages/database/prisma/migrations/20260526120000_dat021_audit_payload_schema_version_gin_index/migration.sql:27-31`
TITLE: DAT-021 hash-chain recompute is operator-manual with no CI/migration guard that it executed
REASON: Confirmed open. grep of .github/workflows/ returns no results for 'recompute', 'schema-bump', or 'schemaVersion'. deploy-prod.sh steps are: [1/5] git checkout, [2/5] env pin, [3/5] docker build (line 87), [4/5] docker up (line 91), [5/5] echo verify — no recompute invocation. Lin
FIX: Minimal: add a witness assertion to apps/api/src/audit/audit-immutability.int.spec.ts that verifies chain integrity over all seeded audit_logs rows after migration (RED until recompute runs or is integrated). Full fix: add a step after 'up -d' in scripts/deploy-prod.sh that runs 'docker compose exec api node dist/scripts/recompute-chain-o

### SEC-039 [🟡 nit] phase4
FILE: `apps/api/src/epics/epics.controller.ts:54`
TITLE: projectId query filter in epics/milestones findAll not validated as UUID
REASON: Verified in current code: apps/api/src/epics/epics.controller.ts line 54 — `@Query('projectId') projectId?: string,` with no ParseUUIDPipe. apps/api/src/milestones/milestones.controller.ts line 82 — `@Query('projectId') projectId?: string,` with no ParseUUIDPipe. Both controllers
FIX: In epics.controller.ts line 54 replace `@Query('projectId') projectId?: string` with `@Query('projectId', new ParseUUIDPipe({ optional: true })) projectId?: string`. Apply the same change to milestones.controller.ts line 82. ParseUUIDPipe is already imported in both controllers. Failing test: GET /epics?projectId=not-a-uuid should return 

### SEC-041 [🟡 nit] phase4
FILE: `apps/api/src/holidays/holidays.controller.ts:127-131`
TITLE: GET /holidays/import-french: year query param parsed via parseInt without range bounds or ParseIntPipe
REASON: Verified in current code: apps/api/src/holidays/holidays.controller.ts lines 127-131 — `@Query('year') year: string` then `const targetYear = year ? parseInt(year, 10) : new Date().getFullYear();` with no ParseIntPipe and no isNaN guard. The holidays.service.ts importFrenchHolida
FIX: Replace `@Query('year') year: string` with `@Query('year', new ParseIntPipe({ optional: true })) year?: number` in the importFrenchHolidays handler (controller line 127). ParseIntPipe is already imported in the controller. Optionally add a range guard in the service. Failing test: POST /holidays/import-french?year=abc (as admin) should re

### SEC-043 [🟡 nit] phase4
FILE: `apps/api/src/leaves/dto/import-leaves.dto.ts:13-20`
TITLE: ImportLeaveDto.userEmail uses @IsString/@IsNotEmpty instead of @IsEmail, and CreateLeaveDto.leaveTypeId / ImportLeaveDto lack @IsUUID
REASON: Verified in current code: apps/api/src/leaves/dto/import-leaves.dto.ts lines 19-21 — `@IsString() @IsNotEmpty() userEmail: string;` with no @IsEmail(). apps/api/src/leaves/dto/create-leave.dto.ts lines 16-18 — `@IsString() @IsNotEmpty() leaveTypeId: string;` with no @IsUUID(); li
FIX: Add IsEmail to imports in import-leaves.dto.ts and apply @IsEmail() to ImportLeaveDto.userEmail. Add IsUUID to imports in create-leave.dto.ts and apply @IsUUID() to leaveTypeId and targetUserId. Add IsUUID to imports in upsert-leave-balance.dto.ts and apply @IsUUID() to userId and leaveTypeId. Failing test: POST /leaves/import with userEm

### SEC-045 [🟡 nit] phase4
FILE: `apps/api/src/planning-export/planning-export.service.ts:56-57`
TITLE: Unvalidated date query strings passed to new Date() in exportIcs — possible HTTP 500 instead of 400
REASON: Verified in current code: apps/api/src/planning-export/planning-export.controller.ts lines 51-52 — `@Query('start') start?: string, @Query('end') end?: string` with no DTO or pipe validation. apps/api/src/planning-export/planning-export.service.ts lines 56-57 — `if (start) dateFi
FIX: Create a PlanningExportQueryDto with `@IsISO8601() @IsOptional() start?: string` and `@IsISO8601() @IsOptional() end?: string` and use `@Query() query: PlanningExportQueryDto` in the controller. Or add an isNaN guard in the service: `if (start && isNaN(new Date(start).getTime())) throw new BadRequestException('Invalid start date')`. Faili

### SEC-049 [🟡 nit] phase4
FILE: `apps/api/src/projects/dto/update-project.dto.ts:18-29`
TITLE: visibleStatuses in UpdateProjectDto uses @IsString (not @IsEnum) allowing arbitrary status strings
REASON: Verified update-project.dto.ts: line 16 has @IsEnum(TaskStatus, { each: true }) on hiddenStatuses (correctly typed TaskStatus[]). Line 27 has @IsString({ each: true }) on visibleStatuses (typed string[]). The asymmetry is present as described. An arbitrary string like 'INVALID_ST
FIX: apps/api/src/projects/dto/update-project.dto.ts: replace @IsString({ each: true }) with @IsEnum(TaskStatus, { each: true }) on visibleStatuses; change type from string[] to TaskStatus[]. Failing test: PATCH /projects/:id with visibleStatuses=['INVALID_STATUS'] should return HTTP 400.

### SEC-051 [🟡 nit] phase4
FILE: `apps/api/src/services/services.controller.ts:59-75`
TITLE: departmentId query parameter in GET /services and GET /skills/matrix is not validated as UUID — invalid values cause DB-level errors surface
REASON: Verified services.controller.ts line 72: @Query('departmentId') departmentId?: string — no pipe. ParseUUIDPipe is imported (line 13) and used on path params (line 89) but not on this query param. Verified skills.controller.ts line 99 in getMatrix(): @Query('departmentId') departm
FIX: apps/api/src/services/services.controller.ts:72 and apps/api/src/skills/skills.controller.ts:99: change to @Query('departmentId', new ParseUUIDPipe({ optional: true })) departmentId?: string. Failing test: GET /api/services?departmentId=not-a-uuid (authenticated) should return HTTP 400, currently returns 500.

### SEC-060 [🟡 nit] phase4
FILE: `apps/api/src/users/users.service.ts:1679-1684`
TITLE: getUsersPresence accepts arbitrary dateStr with no format validation — invalid date causes NaN Date passed to $queryRaw
REASON: Verified in current code: users.controller.ts:192 passes `@Query('date') date?: string` raw to `usersService.getUsersPresence(date)` with no validation pipe. users.service.ts:1751 does `const targetDate = dateStr ? new Date(dateStr) : new Date()` with no isNaN guard before the Na
FIX: Option 1 (service guard): at top of getUsersPresence add `if (dateStr && isNaN(new Date(dateStr).getTime())) throw new BadRequestException('Invalid date format — use YYYY-MM-DD')`. Option 2 (controller pipe): apply `@Query('date', ParseDatePipe)` if a pipe exists. Failing test: `GET /users/presence?date=not-a-date` expects 400. No fronten

### SEC-034 [🟡 nit] phase4
FILE: `apps/api/src/audit/audit.service.ts:103-104`
TITLE: Stdout audit log uses unkeyed plain SHA256 (8 chars) for attempted-login identifier — rainbow-table reversible for common emails
REASON: Verified in current code: audit.service.ts:164-165 defines `hashAttemptedLogin` using `createHash('sha256').update(value).digest('hex').slice(0, 8)` — unkeyed plain SHA-256. The persisted path at line 321-328 (`hashAttemptedSubject`) correctly uses `createHmac('sha256', key)`. Th
FIX: In audit.service.ts, replace `hashAttemptedLogin`: `const hashAttemptedLogin = (value: string): string => createHmac('sha256', process.env['AUDIT_HASH_KEY']!).update(value.trim().toLowerCase()).digest('hex').slice(0, 8)`. AUDIT_HASH_KEY is already boot-asserted in main.ts. Failing test: verify stdout digest !== unkeyed sha256 of same inpu

### SA-SEC-009 [🟡 nit] phase4
FILE: `apps/api/src/leaves/dto/create-leave.dto.ts:12-18`
TITLE: CreateLeaveDto.leaveTypeId uses @IsString() instead of @IsUUID()
REASON: Verified in current code: schema.prisma:600 confirms `LeaveTypeConfig { id String @id @default(uuid()) }`. create-leave.dto.ts:16-18 uses `@IsString() @IsNotEmpty() leaveTypeId: string` with no @IsUUID. The Swagger example 'lt-cp-001' at line 13 is stale documentation pre-dating 
FIX: In create-leave.dto.ts, replace `@IsString() @IsNotEmpty()` on leaveTypeId with `@IsUUID('4') @IsNotEmpty()`. Update Swagger example to a UUID v4 placeholder. Failing test: POST /leaves with leaveTypeId='lt-cp-001' expects 400 not 404/500. No shape-change risk.

### SA-SEC-012 [🟡 nit] phase4
FILE: `apps/api/src/metrics/metrics.controller.ts:14-16`
TITLE: METRICS_TOKEN not documented in env templates — metrics endpoint is open by default
REASON: Verified in current code: grep of both .env.example (91 lines) and .env.production.example (105 lines) returns no METRICS_TOKEN entry. metrics.controller.ts:15 explicitly states 'If METRICS_TOKEN is unset (dev/test), the endpoint is fully open'. METRICS_TOKEN is also absent from 
FIX: Add `METRICS_TOKEN=` with a generation comment to .env.example (MONITORING section) and as required (`METRICS_TOKEN=` with comment 'REQUIRED: generate with openssl rand -hex 32') to .env.production.example. Add `METRICS_TOKEN: ${METRICS_TOKEN:?METRICS_TOKEN required in production}` to the api service environment in docker-compose.prod.yml

### PER-037 [🟡 nit] phase4
FILE: `apps/api/src/analytics/advanced/dto/snapshots-query.dto.ts:12-22`
TITLE: SnapshotsQueryDto.projectIds and TasksBreakdownQueryDto.projectIds have no array size limit — unbounded IN-clause fan-out
REASON: Confirmed in current code. snapshots-query.dto.ts lines 18-22: @IsOptional() @IsArray() @IsUUID('all', { each: true }) @Type(() => String) projectIds?: string[] — no @ArrayMaxSize decorator. tasks-breakdown.dto.ts lines 12-16: identical pattern, no @ArrayMaxSize. Both DTOs only i
FIX: apps/api/src/analytics/advanced/dto/snapshots-query.dto.ts and apps/api/src/analytics/advanced/dto/tasks-breakdown.dto.ts: add `import { ArrayMaxSize } from 'class-validator'` and place `@ArrayMaxSize(200)` above `@IsArray()` on the projectIds field in both DTOs. Failing-test witness: POST/GET with a projectIds array of 201 UUIDs should r

### PER-050 [🟡 nit] phase4
FILE: `apps/api/src/predefined-tasks/predefined-tasks.service.ts:352-387`
TITLE: findRecurringRules: no pagination — unbounded findMany with includes
REASON: Confirmed at predefined-tasks.service.ts lines 394-424: findRecurringRules calls findMany({ where: { isActive: true, ... }, include: { predefinedTask, user, createdBy } }) with no take. Bare array returned. The controller at lines 180-194 accepts only userId and predefinedTaskId 
FIX: apps/api/src/predefined-tasks/predefined-tasks.service.ts: add take: 500 to the findMany call in findRecurringRules at line 394. Keep bare-array return shape. Failing-test witness: create 501 active recurring rules for one user, call GET /predefined-tasks/recurring-rules?userId=..., assert response length is exactly 500.

### PER-057 [🟡 nit] phase4
FILE: `apps/api/src/users/users.service.ts:1479-1481`
TITLE: validateImport fetches ALL users (email+login) for duplicate detection — unbounded full-table scan
REASON: Verified in current code: apps/api/src/users/users.service.ts line 1550-1552 still has `this.prisma.user.findMany({ select: { email: true, login: true } })` with no `where` clause — a full-table scan. The sibling `importUsers` fix (PER-030, line 1345-1353) is present with a bound
FIX: In `validateImport`, before the Promise.all, extract `const allEmails = users.map(u => u.email); const allLogins = users.map(u => u.login);` and replace the unbounded findMany with `{ where: { OR: [{ email: { in: allEmails, mode: 'insensitive' } }, { login: { in: allLogins, mode: 'insensitive' } }] }, select: { email: true, login: true } 

### PER-045 [🟡 nit] phase4
FILE: `apps/api/src/leave-types/leave-types.service.ts:192-203`
TITLE: leave-types reorder: N individual update queries instead of bulk upsert
REASON: Verified: apps/api/src/leave-types/leave-types.service.ts lines 195-202 still uses `const updates = orderedIds.map((id, index) => this.prisma.leaveTypeConfig.update({ where: { id }, data: { sortOrder: index } })); await this.prisma.$transaction(updates);` — N individual UPDATE st
FIX: Replace with a single `$executeRaw` CASE-based UPDATE, or at minimum add `@ArrayMaxSize(50)` guard on the DTO. No response-shape risk (returns findAll() result unchanged).

### PER-053 [🟡 nit] phase4
FILE: `apps/api/src/settings/settings.service.ts:109-126`
TITLE: settings.service `initializeDefaultSettings` issues N×2 sequential DB queries at every module init
REASON: Verified: apps/api/src/settings/settings.service.ts lines 115-130 still has the serial `for (const [key, config] of Object.entries(DEFAULT_SETTINGS)) { const existing = await this.prisma.appSettings.findUnique({ where: { key } }); if (!existing) { await this.prisma.appSettings.cr
FIX: Replace with `await this.prisma.appSettings.createMany({ data: Object.entries(DEFAULT_SETTINGS).map(([key, config]) => ({ key, value: JSON.stringify(config.value), category: config.category, description: config.description })), skipDuplicates: true })`.

### PER-038 [🟡 nit] phase4
FILE: `apps/api/src/analytics/advanced/services/snapshots-query.service.ts:43-149`
TITLE: Six advanced analytics endpoints have no caching despite being identical in contract to the cached main analytics endpoint
REASON: Verified: grep of all files under apps/api/src/analytics/advanced/ returns zero hits for CacheService, cache.get, cache.set. The analytics-advanced.module.ts imports CommonModule but CommonModule (apps/api/src/common/common.module.ts) does NOT export CacheService — it only export
FIX: Add CacheService to analytics-advanced.module.ts providers and inject into each of the 6 service constructors, wrapping results with per-user per-params cache key (TTL=60s) following the existing buildAnalyticsCacheKey pattern in analytics.service.ts.

### PER-042 [🟡 nit] phase4
FILE: `apps/api/src/common/services/cache.service.ts:1-59`
TITLE: CacheService uses TTL-only eviction — no mutation invalidation for analytics endpoints
REASON: Verified: apps/api/src/common/services/cache.service.ts line 13 confirms 'TTL-only eviction (no mutation-bust hooks) within this initial bounded scope (AC#6 forbids touching mutation service paths in this commit)'. Grep of `cacheService.del` across all src/ files (excluding cache
FIX: In ProjectsService.create/update/archive/delete, call cacheService.del() using the same key schema as buildAnalyticsCacheKey to invalidate the caller's analytics cache. Alternatively reduce analytics TTL to 30s for acceptable staleness as a lower-effort fix.

### PER-048 [🟡 nit] phase4
FILE: `apps/api/src/personal-todos/personal-todos.service.ts:18-28`
TITLE: findByUser() (GET /personal-todos) issues an unconditional DELETE on every read — unnecessary write roundtrip on each list call
REASON: Verified: apps/api/src/personal-todos/personal-todos.service.ts lines 18-20 confirm `async findByUser(userId) { await this.cleanupOldCompleted(userId); ... }`. Lines 111-124 confirm `cleanupOldCompleted` calls `this.prisma.personalTodo.deleteMany({ where: { userId, completed: tru
FIX: Add a count-gate before deleteMany: `const staleCount = await this.prisma.personalTodo.count({ where: { userId, completed: true, completedAt: { lt: cutoffDate } } }); if (staleCount > 0) { await this.prisma.personalTodo.deleteMany(...); }`. Or move to a @Cron job.

### COR-053 [🟡 nit] phase4
FILE: `apps/api/src/events/events.service.ts:543-557`
TITLE: update() recurrenceEndDate child-prune runs outside the update transaction, creating a window where pruned children exist alongside a commit
REASON: Confirmed in current code. events.service.ts:528-583: the `$transaction` callback closes at line 583. Lines 591-604 then execute `await this.prisma.event.deleteMany({ where: { parentEventId: id, date: { gt: newEndDate } } })` using `this.prisma` (not `tx`), entirely outside the t
FIX: apps/api/src/events/events.service.ts — move the `event.deleteMany` block inside the `$transaction` callback (before returning from `tx.event.update`), using `tx.event.deleteMany`. Test: unit test that mocks `tx.event.update` to succeed and verifies `tx.event.deleteMany` is called within the same transaction callback; integration test ver

### COR-055 [🟡 nit] phase4
FILE: `apps/api/src/leaves/leaves.service.ts:3627-3637`
TITLE: importLeaves catch block resets result.created=0 but not result.skipped — error response reports misleading skipped count after full tx roll
REASON: Confirmed in current code. The `$transaction` callback starts at leaves.service.ts:3630. The `result.skipped++` increments at lines 3664, 3676, 3688, 3694, 3710, 3728 run inside the transaction callback and mutate the in-memory `result` JS object. When the transaction aborts (DB 
FIX: apps/api/src/leaves/leaves.service.ts — add `result.skipped = 0;` in the catch block at line 3813 alongside `result.created = 0;`. Optionally clear `result.errorDetails` before pushing the 'Import annulé' message. Test: unit test with a batch having 2 validation-skipped rows + 1 tx failure; assert catch returns `{created:0, skipped:0, err

### COR-060 [🟡 nit] phase4
FILE: `apps/api/src/settings/settings.service.ts:270-279`
TITLE: SettingsService.bulkUpdate performs sequential un-transacted upserts — partial failure leaves inconsistent state
REASON: Confirmed at settings.service.ts:301-309: `async bulkUpdate(settings: Record<string, unknown>, actorId?: string)` iterates with `for (const [key, value] of Object.entries(settings)) { const result = await this.update(key, value, undefined, actorId); results.push(result); }`. Each
FIX: apps/api/src/settings/settings.service.ts — wrap the loop in `this.prisma.$transaction(async (tx) => {...})`. Refactor `update()` to accept an optional Prisma transaction client param. Move `isKnownKey` validation before the loop for fail-fast. Test: POST /settings/bulk with [{validKey:val}, {invalidKey:val}]; assert validKey was NOT pers

### COR-052 [🟡 nit] phase4
FILE: `apps/api/src/epics/epics.service.ts:78-99`
TITLE: epics.service update()/remove(): double-read TOCTOU — epic fetched in membership check then again in findOne()
REASON: Confirmed at epics.service.ts:93-121. update() calls assertProjectMembership (line 100), findOne (line 102), then `prisma.epic.update()` (line 103) as three separate non-transactional operations. remove() (lines 110-121) has the same pattern ending with `prisma.epic.delete()`. Gr
FIX: apps/api/src/epics/epics.service.ts — catch PrismaClientKnownRequestError with code 'P2025' around epic.update/delete and re-throw as NotFoundException('Epic introuvable'). Test: mock prisma.epic.update to throw P2025; assert service returns NotFoundException (HTTP 404).

### COR-056 [🟡 nit] phase4
FILE: `apps/api/src/milestones/milestones.service.ts:102-135`
TITLE: milestones.service update()/remove(): same double-read TOCTOU as epics — unhandled P2025 on concurrent delete
REASON: Confirmed at milestones.service.ts:102-134. update() calls assertProjectMembership (line 109), findOne (line 111), then `prisma.milestone.update()` (line 114) as three non-transactional ops. remove() (lines 124-134) calls assertProjectMembership (line 130), findOne (line 132), th
FIX: apps/api/src/milestones/milestones.service.ts — catch PrismaClientKnownRequestError P2025 around milestone.update/delete and re-throw as NotFoundException('Milestone introuvable'). Test: mock prisma.milestone.update to throw P2025; assert service returns NotFoundException (HTTP 404).

### COR-061 [🟡 nit] phase4
FILE: `apps/api/src/settings/settings.service.ts:310-321`
TITLE: SettingsService.remove throws unhandled Prisma P2025 for non-existent non-default keys
REASON: Confirmed at settings.service.ts:350-357: `const previous = await this.prisma.appSettings.findUnique({where:{key}});` followed by `const before = previous ? this.parseValue(previous.value) : null;` and then unconditionally `await this.prisma.appSettings.delete({where:{key}});`. W
FIX: apps/api/src/settings/settings.service.ts:353 — insert `if (!previous) throw new NotFoundException(\`Setting '${key}' not found\`);` after the findUnique null check. Test: call remove('nonexistent-custom-key'); assert NotFoundException (HTTP 404). Also verify DELETE /settings/:known-custom-key still returns 200.

### COR-048 [🟡 nit] phase4
FILE: `apps/api/src/analytics/advanced/services/milestones-completion.service.ts:20-98`
TITLE: daysFromNow in MilestonesCompletionService uses fixed MS_PER_DAY, off-by-one across DST transitions
REASON: Confirmed in current code: milestones-completion.service.ts line 20 defines `const MS_PER_DAY = 1000 * 60 * 60 * 24;` and line 102 uses `Math.round(dayMs / MS_PER_DAY)`. Fixed-length divisor is wrong across DST transitions. No git commit closes COR-048. Aggregate counts at lines 
FIX: milestones-completion.service.ts lines 101-102: replace with UTC midnight diff — `const nowMidnightUTC = Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate()); const dueMidnightUTC = Date.UTC(milestone.dueDate.getUTCFullYear(), milestone.dueDate.getUTCMonth(), milestone.dueDate.getUTCDate()); const daysFromNow = Math.round((

### COR-064 [🟡 nit] phase4
FILE: `apps/api/src/users/users.service.ts:1680-1684`
TITLE: getUsersPresence(): setHours(0,0,0,0) uses local server timezone — presence window is offset when Node.js process timezone differs from UTC
REASON: Confirmed in users.service.ts lines 1752-1755: `startOfDay.setHours(0, 0, 0, 0)` and `endOfDay.setHours(23, 59, 59, 999)` use local-timezone arithmetic. Neither docker-compose.yml nor docker-compose.prod.yml sets TZ for the API container (grep returns empty). For task and event o
FIX: users.service.ts lines 1753 and 1755: replace `setHours(0, 0, 0, 0)` with `setUTCHours(0, 0, 0, 0)` and `setHours(23, 59, 59, 999)` with `setUTCHours(23, 59, 59, 999)`. Failing-test: integration test with `TZ=Europe/Paris` env calling `getUsersPresence('2026-06-04')` and asserting `startOfDay.toISOString() === '2026-06-04T00:00:00.000Z'`.

### COR-049 [🟡 nit] phase4
FILE: `apps/api/src/analytics/advanced/snapshot-scheduler.service.ts:32-45`
TITLE: SnapshotSchedulerService creates a Redis connection in constructor with no shutdown hook
REASON: Confirmed in snapshot-scheduler.service.ts: class at line 28 declares `implements OnModuleInit` only; constructor at lines 36-44 creates `new Redis(redisUrl)` or `new Redis({...})`; grep for `onModuleDestroy`, `OnModuleDestroy`, `disconnect`, `quit`, `close` returns empty. No tea
FIX: snapshot-scheduler.service.ts: add `OnModuleDestroy` to imports from `@nestjs/common`, add `implements OnModuleDestroy` to class declaration, add `async onModuleDestroy() { await this.redis.quit(); }`. Failing-test: Vitest test that creates+destroys the NestJS test module containing `SnapshotSchedulerService` and asserts the runner exits 

### COR-058 [🟡 nit] phase4
FILE: `apps/api/src/projects/projects.service.ts:1147`
TITLE: captureSnapshots() uses hardcoded string literal 'ACTIVE' instead of ProjectStatus enum
REASON: Confirmed in projects.service.ts line 1218: `where: { status: 'ACTIVE' }` is a raw string literal inside `captureSnapshots()`. `ProjectStatus` is imported at line 21 (`import { ProjectStatus, TaskStatus } from 'database'`) and used with enum syntax at lines 122, 583, 724. The raw
FIX: projects.service.ts line 1218: replace `{ status: 'ACTIVE' }` with `{ status: ProjectStatus.ACTIVE }`. No frontend response-shape change. TypeScript compile verification: rename `ProjectStatus.ACTIVE` in schema and run `npx nest build` — should fail at this line post-fix, proving the enum guard works.

### COR-063 [🟡 nit] phase4
FILE: `apps/api/src/tasks/tasks.service.ts:797-800`
TITLE: update(): progress not recalculated when status changes and subtasks exist
REASON: Confirmed in tasks.service.ts lines 838-841: `...(taskData.status && (await tx.subtask.count({ where: { taskId: id } })) === 0 && { progress: getTaskProgress(taskData.status) })`. When a task has subtasks and status=DONE, the count is >0 so the condition is false and progress is 
FIX: tasks.service.ts update() method: after the existing progress spread (lines 838-841), add an unconditional override when status is DONE: `...(taskData.status === TaskStatus.DONE && { progress: 100 })`. Failing-test: unit test creating a task with 2 subtasks (progress=50%), calling `update()` with `status: DONE`, and asserting `task.progre

### SA-COR-009 [🟡 nit] phase4
FILE: `apps/api/src/tasks/tasks.service.ts:1673`
TITLE: validateImport (tasks): end <= start is classified as a warning, but create() rejects end < start as an error — inconsistent boundary semant
REASON: Confirmed inconsistency: tasks.service.ts line 1847 `if (end <= start)` (less-than-or-equal, marks same-day tasks as warning) vs line 211 `if (new Date(endDate) < new Date(startDate))` (strict less-than, allows same-day). Line 211 comment says 'dates égales autorisées pour les tâ
FIX: tasks.service.ts line 1847: change `end <= start` to `end < start` and update the message at line 1850 from 'La date de fin est antérieure ou égale à la date de début' to 'La date de fin est antérieure à la date de début'. Failing-test: unit test calling `validateImport` with a task where `startDate === endDate` and asserting `previewItem

### OBS-023 [🟡 nit] phase4
FILE: `apps/api/src/common/fastify/redact.config.ts:1-37`
TITLE: req.body.login not in Fastify redact paths — user identifier would appear in logs if body serialization is enabled
REASON: Verified apps/api/src/common/fastify/redact.config.ts (lines 1-37): the paths array covers req.body.password, req.body.currentPassword, req.body.newPassword, req.body.refreshToken, req.body.token, req.query.token, req.body.validationComment, req.body.reason, req.body.motif, req.b
FIX: apps/api/src/common/fastify/redact.config.ts: add 'req.body.login' to the paths array under the auth/token block. Test: assert `fastifyLoggerOptions.redact.paths` contains 'req.body.login'.

### SA-OBS-010 [🟡 nit] phase4
FILE: `apps/api/src/users/users.service.ts:147-150`
TITLE: console.error() in UsersService.create() bypasses NestJS Logger and Fastify log redaction
REASON: Verified apps/api/src/users/users.service.ts:151-153: `console.error('[CRITICAL] bcrypt hash verification failed for user ${createUserDto.login}')` — raw PII login value logged outside the Fastify pino pipeline, bypassing LOG_LEVEL filtering and redact.config.ts. All other busine
FIX: apps/api/src/users/users.service.ts:151: replace `console.error(...)` with `this.logger.error('[CRITICAL] bcrypt hash verification failed (login redacted)')` omitting the raw login value. Verify `private readonly logger = new Logger(UsersService.name)` is declared. Failing test: grep assertion that no console.error|warn|log appears in non

### SA-OBS-011 [🟡 nit] phase4
FILE: `apps/api/src/settings/settings.service.ts:99-103`
TITLE: console.warn() in SettingsService.onModuleInit() bypasses NestJS Logger
REASON: Verified apps/api/src/settings/settings.service.ts:104-107: `console.warn('Warning: Could not initialize default settings. Table may not exist yet.', error instanceof Error ? error.message : error)` in onModuleInit(). Bypasses NestJS Logger, losing structured fields and LOG_LEVEL
FIX: apps/api/src/settings/settings.service.ts: add `private readonly logger = new Logger(SettingsService.name);` and replace console.warn() with `this.logger.warn(...)`. Failing test: same grep assertion as SA-OBS-010 covering both files.

### SEC-065 [🔵 suggestion] phase5
FILE: `apps/api/src/events/events.service.ts:681-690`
TITLE: getEventsByRange and findOne RBAC filter defaults to unscoped on null role — inconsistent with findAll
REASON: Verified in current code. events.service.ts:739 uses `if (currentUserId && currentUserRole)` before the getPermissionsForRole call — default-open on null role. events.service.ts:437-440 has the same pattern for the IDOR check. By contrast, findAll at line 314-315 calls getPermiss
FIX: apps/api/src/events/events.service.ts: In getEventsByRange, replace `if (currentUserId && currentUserRole)` with an unconditional getPermissionsForRole(currentUserRole) call; guard only the where.OR assignment with `if (currentUserId)` to avoid a broken Prisma filter on undefined userId. Same fix for findOne IDOR check. Failing test: unit

### PER-061 [🔵 suggestion] phase5
FILE: `apps/api/src/leaves/leaves.service.ts:2678-2732`
TITLE: getLeaveBalance: Promise.all fan-out issues N×2 DB queries, one resolveAllocatedDays per leave type
REASON: Verified in current code. resolveAllocatedDays at leaves.service.ts:2622 issues leaveBalance.findUnique (individual), then at 2632 issues leaveBalance.findFirst (global) — exactly 2 sequential queries per invocation. The fan-out at lines 2839-2845 uses Promise.all(leaveTypes.map(
FIX: apps/api/src/leaves/leaves.service.ts: Add resolveAllocatedDaysForAll(userId, leaveTypeIds, year) issuing 2 findMany queries total (individual overrides + global balances for all types), returning a Record<leaveTypeId, number>. Replace the Promise.all fan-out in getLeaveBalance. Failing test: spy on prisma.leaveBalance.findMany; assert ca

### DAT-031 [🔵 suggestion] phase5
FILE: `packages/database/prisma/migrations/20260604050007_dat028_password_reset_token_indexes/migration.sql:2-3`
TITLE: password_reset_tokens index (userId, usedAt) is non-partial — hot query path WHERE usedAt IS NULL will scan all rows including consumed toke
REASON: Verified in current code. The only migration touching this index is 20260604050007_dat028_password_reset_token_indexes/migration.sql which creates `CREATE INDEX password_reset_tokens_userId_usedAt_idx ON password_reset_tokens(userId, usedAt)` — a full composite index, not partial
FIX: New raw SQL migration required (Prisma 6 PSL cannot express WHERE-clause partial indexes): `DROP INDEX IF EXISTS password_reset_tokens_userId_usedAt_idx; CREATE INDEX password_reset_tokens_userId_active_idx ON password_reset_tokens(userId) WHERE usedAt IS NULL;`. Failing test: EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM password_reset_tokens

### SA-DAT-012 [🔵 suggestion] phase5
FILE: `packages/database/prisma/schema.prisma:35-38`
TITLE: User.roleId remains nullable — users without a role bypass RBAC entirely
REASON: Confirmed in current code: packages/database/prisma/schema.prisma:37 still reads `roleId String?` (nullable) with the comment 'passera NOT NULL après backfill stable. Cf. contract-03 §9.'. No migration in the full set (including the most recent 20260605201203_dat011_012_recurrenc
FIX: packages/database/prisma/schema.prisma: change line 37 to `roleId String` (non-optional). New migration: pre-flight Assert `SELECT COUNT(*) FROM users WHERE roleId IS NULL` = 0, then `ALTER TABLE "users" ALTER COLUMN "roleId" SET NOT NULL`. Failing-test witness: integration test INSERT INTO users without roleId → should fail with NOT NULL

### SA-SEC-017 [🔵 suggestion] phase5
FILE: `apps/api/src/app.controller.ts:1-24`
TITLE: AppController GET / exposes docs and internal API endpoint list publicly without auth
REASON: Confirmed: apps/api/src/app.controller.ts:1-24 still returns full endpoint map including version '2.0.0', docs path '/api/docs', and named endpoint paths under the @Public() @Get() route. No consumer of the root response body found in apps/web/src/ or nginx config. docker-compose
FIX: apps/api/src/app.controller.ts: replace the return value with `{ status: 'operational', message: 'API is running. Access endpoints via /api/*' }` (remove version string and endpoints object). Failing-test witness: unit test GET / → response body must not contain 'version' or 'endpoints' or 'docs' keys.