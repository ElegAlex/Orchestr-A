# Stage-4 Manifest — 2026-06-04 Adversarial Remediation Pass (Phase 1 + Phase 2)

> Updated 2026-06-05. LOCAL ONLY — all work is local commits; deploy is a separate explicitly-authorized step.
> Scope: Phase-1 (5 blockers) + Phase-2 code-verifiable (178) = 183 in-scope. Phase 3/4/5 EXCLUDED.

## Totals
- **In-scope:** 183
- **DONE: 142/183 (78%)**  —  132 folded (code + RED→GREEN witness, gate-green) · 10 reconciled-away/anchor
- **Still-open: 41** (all Phase-2)

## Delta — 2026-06-06 (LOCAL only, no push/deploy)
- **SEC-030 HR follow-up → `91e849d7`** (operator decision: HR_OFFICER must read leave balances ORG-WIDE; SEC-030 over-restricted it). **Resolution = new READ-scoped permission, NOT `leaves:manage_any`** (which would over-grant org-wide approve/modify). Why a *new* permission: HR_OFFICER and MANAGER are indistinguishable on the leaves axis (both have `leaves:approve` + `leaves:readAll`, neither has `manage_any`), and `leaves:readAll` is near-universal (23/26 templates), so no existing permission isolates HR_OFFICER; a role-code check would violate the no-hardcoded-roles rule. Added `leaves:read_balance_any` (catalogue 117→118); the balance handler now lifts the perimeter for `leaves:manage_any` **OR** `leaves:read_balance_any`. Granted **inline to HR_OFFICER only**; ADMIN inherits it via the full catalogue (harmless — already org-wide via `manage_any`); **ADMIN_DELEGATED EXCLUDES it** (`without(...)`) to stay perimeter-scoped per [[project_responsable_scope_perimeter]]; MANAGER/MANAGER_HR_FOCUS do NOT get it → **SEC-030 preserved for them**. Counts updated (templates.spec ADMIN 117→118, HR_OFFICER 40→41, CATALOG 118; permissions.service.spec 118). Witness (real DB, `sec030-by-user-scope.int.spec.ts`): HR_OFFICER reads out-of-perimeter balance; MANAGER STILL 403 (nothing-else-widened); HR_OFFICER still can't `assertCanManageUser` (read-only, no write widening). Discriminating RED captured (drop the grant → HR 403, others unchanged). permission-matrix-coverage gate green (runtime check, no new `@RequirePermissions`). **docs/rbac/* NOT updated** — stale derived snapshots (last touched 2026-04-23, already missing `leaves:self_approve` etc.), ungated, no generator, consumed by no code; source of truth is `templates.ts`.
- **⚠️ CORRECTION + new finding (surfaced, NOT auto-fixed):** the SEC-030 entry below claims tasks/leaves/telework by-user reads "force self / 403 for MANAGER" (already-safe). **That is WRONG.** Runtime resolution: `tasks:readAll`=21/26, `telework:readAll`=23/26, `leaves:readAll`=23/26 templates (all incl. BASIC_USER/EXTERNAL_PRESTATAIRE/STAGIAIRE). So `GET /tasks/assignee/:userId`, `GET /leaves?userId=`, `GET /telework?userId=` are **org-wide reads for ~all authenticated roles**, not perimeter-scoped. SEC-030's *balance* + *skills* fixes are unaffected and correct (those endpoints don't gate on `*:readAll`; balance gates on `manage_any`). But SEC-030 did NOT actually scope tasks/leaves/telework — they were never scoped. **Operator decision needed (separate finding):** is near-universal org-wide read of task assignments / leave lists / telework calendars intended transparency, or an RGPD over-exposure? Scoping it would be a ~21-role RBAC change. Out of scope for this task. **→ RESOLVED 2026-06-06 (operator decision): INTENDED transparency — ACCEPTED as `AB-001` (intended design, WONT-FIX); `*:readAll` NOT tightened, no code change. See the "Accepted behaviors" section above + CLAUDE_SESSION_CONTRACT.md §"Accepted behaviors".**
- **OBS-006 → `98c31c76`** (Closed_by `98c31c76`). Audit-emit cluster slice 2: `DOCUMENT_CREATED` / `DOCUMENT_UPDATED` / `DOCUMENT_DELETED` added to the 3 compile-locked layers + emitted in DocumentsService create/update/remove (all 3 AC; update carries a before/after metadata snapshot). Build gotcha hit + fixed: the Document column is `mimeType`, not `type` (the DTO field). Witnesses assert each emit + `validatePayloadForAction` schema conformance.
- **OBS-003 / OBS-016 / OBS-017 → `d0ca9124`** (Closed_by `d0ca9124`). First slice of the audit-emit cluster's cross-module batch: added `LOGOUT` + `USER_CREATED` to the 3 compile-locked layers (enum ∩ ENTITY_TYPE_BY_ACTION ∩ AUDIT_PAYLOAD_SCHEMAS). OBS-003 = new logout emit (AuthController injects AuditService); OBS-016/017 = promoted the `'USER_CREATED' as AuditAction` cast to the real member + strict schema (cast removed).
  - **Witness strategy for the audit-emit cluster (decided 2026-06-06):** unit witness = capture the payload handed to the (mocked) audit `log()` + assert the REAL `validatePayloadForAction(action, payload)` does not throw. This is STRONGER on the two properties that actually fail for an audit emit — (a) is it wired into the mutation path, (b) does the payload pass its `.strict()` schema at INSERT (a non-schema key is swallowed by the fire-and-forget `.catch` = silent audit loss) — than re-proving DB/hash-chain mechanics 13× (already covered generically by `audit-immutability.int.spec`). Not a corner cut: it targets the load-bearing failure mode directly.
- **SEC-030 → `fd671b74`** (folded; Closed_by `fd671b74`). Was BLOCKED in §4. Server-side managed-scope on the by-user reads. Reconciliation refined the manifest's "6 endpoints" prescription: 5 of 7 suivi calls were already equal-or-stronger (NOT touched — see the SEC-030 backlog Notes); only 2 over-exposed and were tightened via `AccessScopeService.assertCanManageUser` — `GET /leaves/balance/:userId` (global `leaves:approve` → +scope) and `GET /skills/user/:userId` (no scope → +scope). Discriminating real-DB witness `sec030-by-user-scope.int.spec.ts` (RED→GREEN) + controller unit coverage. **Operator flag:** leaves-balance scoping also restricts `HR_OFFICER` to its perimeter. **→ RESOLVED 2026-06-06** (operator decision): granted the new read-scoped `leaves:read_balance_any` to HR_OFFICER (NOT `leaves:manage_any` — that would over-grant write/approve). See the SEC-030 HR follow-up entry at the top of this Delta.

## Accepted behaviors (WONT-FIX — intended design, do not resurface)
Standing record; authoritative copy in `../2026-05-24-review-payloads/CLAUDE_SESSION_CONTRACT.md` §"Accepted behaviors" (read first by every session). Future audits' equivalent findings resolve to ACCEPTED, not blocking/important.
- **AB-001 — Org-wide READ of tasks, leaves/congés, telework, and the user directory (`GET /users*`) is INTENDED (operator decision 2026-06-06).** `tasks:readAll` (21/26 templates), `leaves:readAll` (23/26), `telework:readAll` (23/26) are deliberately near-universal (down to BASIC_USER) — Orchestr'A is a planning tool where everyone sees everyone's tasks/leave/telework. Read paths `GET /tasks/assignee/:userId`, `GET /leaves?userId=`, `GET /telework?userId=` are NOT broken access. **Extended 2026-06-06:** the same planning-tool principle accepts org-wide READ of the user *directory* — `GET /users`, `GET /users/:id`, `GET /users/{department,service,role}/…`, `GET /users/presence`, all gated by the read-only `users:read` permission (the 2026-05-24 SEC-031/SEC-030 directory findings' READ exposure → ACCEPTED). Resolves the SEC-030 §Delta "broader exposure" correction → ACCEPTED. **No code change; `*:readAll`/`users:read` NOT tightened.** Does NOT touch SEC-030's balance/skills scoping or `leaves:read_balance_any` (different endpoints, stand). Does NOT accept any user-directory WRITE (`users:create`/`update`/`delete`/`reset_password`/`manage_roles`) or any other write/role/scope mutation — READ only (the three planning domains + the `users:read`-gated directory).

## Verification
- **Unit gate every fold:** build (nest typecheck) + test (vitest API + jest web, mocked DB) + lint (0 err) + coherence — GREEN at HEAD; each fold verified to build STANDALONE (`git stash -u; gate; pop`).
- **Integration suite (real DB):** `pnpm --filter api test:integration` GREEN (29 files / 132). Covers all DB/schema closes: DAT-002 FK+hardDelete, DAT-004/009 Restrict, reverse-FK indexes, DAT-008/015 CHECK, DAT-011/012 recurrence CHECK, DAT-013 audit no-TRUNCATE, OBS-019 lastLoginAt.
- **NOT done — Playwright E2E** (CLAUDE.md mandate) for the folded authz/security set — known gap.

## 1. Folded (id → Closed_by SHA) — 127

- `COR-001` → `d599e2cb`  ·  canValidate: delegation check is not scoped to the leave's
- `COR-002` → `cc51aaf4`  ·  importTasks: task created but subtasks written outside tra
- `COR-003` → `f86b28ff`  ·  findAll auto-expand writes telework rows for ALL users whe
- `COR-004` → `f8ef9b67`  ·  MilestonesCompletionResponseDto.total means 'due milestone
- `COR-005` → `52abc47a`  ·  taskStatusData and metrics diverge when scope exceeds PROJ
- `COR-006` → `c9a1e7e5`  ·  AuditService.log passes undefined-valued payload keys to c
- `COR-007` → `20425fb5`  ·  auth.service resetPassword(): token validity check and use
- `COR-008` → `12d48876`  ·  hardDelete: count-then-delete without transaction allows d
- `COR-009` → `a2f9ba00`  ·  documents.update: prisma.document.update has no deletedAt 
- `COR-010` → `a2f9ba00`  ·  documents.update accepts projectId change without checking
- `COR-011` → `f8ef9b67`  ·  CreateDocumentDto.size has no @IsNumber() or @Min(0) — neg
- `COR-012` → `f4809c29`  ·  Recurring event child occurrences created outside any tran
- `COR-013` → `97a5b2bd`  ·  countWorkingDays: local-TZ setDate/getDate arithmetic caus
- `COR-014` → `0ed06468`  ·  rejectCancellation writes no audit log — CANCELLATION_REQU
- `COR-015` → `c5962c24`  ·  getAvailableDays: existing leaves fetched without endHalfD
- `COR-016` → `0ed06468`  ·  getLeaveBalance uses new Date().getFullYear() (host-TZ) wh
- `COR-017` → `0ed06468`  ·  deleteBalance: hard-delete committed before audit log — cr
- `COR-018` → `006605b3`  ·  importMilestones(): per-row findFirst+create is a TOCTOU r
- `COR-019` → `f8ef9b67`  ·  personal-todos.service.ts create() has TOCTOU race on the 
- `COR-020` → `95ea1d6f`  ·  ICS export silently drops leaves that span the requested d
- `COR-021` → `d7d60074`  ·  createBulkAssignment: telework check snapshot race — asser
- `COR-022` → `d7d60074`  ·  assertTeleworkCompatibility: date comparison uses naive ne
- `COR-024` → `08693a50`  ·  update() does not validate partial date changes against ex
- `COR-025` → `08693a50`  ·  hardDelete(): audit log written outside transaction — audi
- `COR-026` → `f8ef9b67`  ·  services.service.ts remove() performs 3 sequential DB writ
- `COR-027` → `074f307a`  ·  skills.service.ts create() and update() missing P2002 catc
- `COR-028` → `d41e8566`  ·  importTasks: missing date validity check — invalid startDa
- `COR-029` → `d41e8566`  ·  reorderSubtasks: subtask IDs from payload not validated to
- `COR-030` → `d41e8566`  ·  update(): date clearing is impossible — falsy check preven
- `COR-031` → `d41e8566`  ·  addDependency: check-then-create pattern is not race-safe 
- `COR-032` → `8fc2323f`  ·  findForPlanningOverview calls expandRecurringRulesForRange
- `COR-033` → `8fc2323f`  ·  expandRecurringRulesForRange and generateSchedulesFromRule
- `COR-034` → `8fc2323f`  ·  getWeeklySchedule uses local-TZ getDay()/setDate()/setHour
- `COR-035` → `8fc2323f`  ·  getUserStats builds year boundaries with local-TZ new Date
- `COR-036` → `d7a2bd76`  ·  ThirdPartiesService.update: LEGAL_ENTITY invariant checked
- `COR-037` → `5f096b41`  ·  upsertDismissal findFirst→create inside $transaction does 
- `COR-038` → `5f096b41`  ·  ensureDailyCapNotExceeded is non-transactional: concurrent
- `COR-039` → `93c3073d`  ·  users.service changePassword() and admin resetPassword() d
- `COR-040` → `93c3073d`  ·  users.service update(): service-membership deleteMany+crea
- `COR-046` → `36e5e291`  ·  fetchData useCallback in tasks/page.tsx missing hasPermiss
- `DAT-002` → `61194f42`  ·  TaskRACI.userId has no FK relation — orphaned RACI rows, n
- `DAT-004` → `85278391`  ·  ON DELETE CASCADE on leave_balances.leaveTypeId destroys b
- `DAT-008` → `7f5b715a`  ·  project_third_party_members.allocation INTEGER has no CHEC
- `DAT-009` → `85278391`  ·  time_entries.thirdPartyId ON DELETE CASCADE silently destr
- `DAT-011` → `47a480be`  ·  predefined_task_recurring_rules lacks a XOR constraint ens
- `DAT-012` → `47a480be`  ·  predefined_task_recurring_rules.monthlyOrdinal and monthly
- `DAT-013` → `edbe5b64`  ·  audit_logs immutability trigger does not cover TRUNCATE — 
- `DAT-015` → `7f5b715a`  ·  project_snapshots.progress lacks CHECK (BETWEEN 0 AND 100)
- `DAT-016` → `5043b353`  ·  DAT-015 creates redundant dual-layer uniqueness on email: 
- `DAT-017` → `375a2386`  ·  TaskDependency.dependsOnTaskId has no index — cascade dele
- `DAT-018` → `375a2386`  ·  UserSkill.skillId has no standalone index — cascade delete
- `OBS-002` → `c9a1e7e5`  ·  requestId never populated in persisted audit-log payload d
- `OBS-005` → `000d3161`  ·  Sentry / GlitchTip / OpenTelemetry stubs are no-ops — unha
- `OBS-007` → `0ed06468`  ·  rejectCancellation() (CANCELLATION_REQUESTED → APPROVED) e
- `OBS-018` → `36e5e291`  ·  No React error boundary or Next.js error.tsx in any of the
- `OBS-019` → `36e5e291`  ·  Profile page shows `new Date()` (current time) as 'last lo
- `PER-001` → `52abc47a`  ·  analytics.service `getTasks` fetches all Task columns with
- `PER-002` → `f8ef9b67`  ·  assertCanAccessProject issues two project.count queries fo
- `PER-004` → `26308212`  ·  assertProjectMembership in EpicsService fetches all projec
- `PER-005` → `f4809c29`  ·  Recurring event creation: sequential per-occurrence `event
- `PER-006` → `f4809c29`  ·  All `events.findAll` / `getEventsByUser` / `getEventsByRan
- `PER-007` → `0ed06468`  ·  importLeaves: getHolidayKeySet + findValidatorForUser call
- `PER-008` → `0ed06468`  ·  getPendingForValidator: unbounded findMany — no limit/pagi
- `PER-009` → `f8ef9b67`  ·  MetricsInterceptor uses raw URL path as label key — unboun
- `PER-010` → `006605b3`  ·  importMilestones executes N sequential findFirst + create 
- `PER-011` → `95ea1d6f`  ·  ICS import: sequential `event.create` per VEVENT — up to 5
- `PER-012` → `d7d60074`  ·  createBulkAssignment: one DB INSERT per (user × date) pair
- `PER-013` → `d7d60074`  ·  bulkCreateRecurringRules: one DB INSERT per (user × dayOfW
- `PER-014` → `d7d60074`  ·  generateFromRules: one DB INSERT per occurrence date, sequ
- `PER-015` → `08693a50`  ·  getProjectsByUser fetches ALL tasks for every project to c
- `PER-016` → `08693a50`  ·  getProjectStats fetches all tasks and all members without 
- `PER-017` → `08693a50`  ·  Default and max page size of 1000 on list endpoints (proje
- `PER-018` → `074f307a`  ·  findUsersBySkill() fetches ALL users with a skill then fil
- `PER-019` → `074f307a`  ·  Default page limit is 1000 on skills/departments/services 
- `PER-020` → `074f307a`  ·  importSkills() issues one INSERT per row in a serial for-l
- `PER-021` → `d41e8566`  ·  getTasksByAssignee and getTasksByProject have no paginatio
- `PER-022` → `d41e8566`  ·  getMyDoneUndeclaredTasks: unbounded findMany with NOT EXIS
- `PER-023` → `d41e8566`  ·  checkCircularDependency: one DB query per BFS node — poten
- `PER-024` → `d41e8566`  ·  importTasks: sequential await inside for-loop — N DB round
- `PER-025` → `d41e8566`  ·  findOrphans: unbounded findMany on tasks with projectId IS
- `PER-026` → `d41e8566`  ·  reorderSubtasks: one DB UPDATE per subtask in a transactio
- `PER-027` → `d41e8566`  ·  findAll tasks allows limit=1000 — 10x higher than document
- `PER-028` → `8fc2323f`  ·  expandRecurringRulesForRange: N+1 findUnique+create inside
- `PER-029` → `8fc2323f`  ·  generateSchedulesFromRules: same N+1 findUnique+create loo
- `PER-030` → `93c3073d`  ·  importUsers executes N sequential DB round-trips (findFirs
- `PER-034` → `36e5e291`  ·  tasksService.getAll(1, 1000) hard-coded limit of 1000 task
- `PER-035` → `375a2386`  ·  TaskDependency.dependsOnTaskId has no index — reverse depe
- `PER-036` → `375a2386`  ·  TaskRACI has no index on userId — per-user RACI lookups re
- `SA-COR-004` → `d41e8566`  ·  addDependency allows two orphan tasks (projectId = null) t
- `SA-COR-008` → `d41e8566`  ·  tasks.update: assigning an epicId or milestoneId that belo
- `SA-DAT-003` → `97a5b2bd`  ·  LeaveTypeConfig.remove() checks only leaves count — silent
- `SA-DAT-004` → `93c3073d`  ·  USER_DELETED audit entry committed before erasure transact
- `SA-OBS-003` → `000d3161`  ·  ACCESS_DENIED action is defined and schema'd but never emi
- `SA-OBS-008` → `97a5b2bd`  ·  In-memory Prometheus metrics are fully reset on every cont
- `SA-OBS-009` → `97a5b2bd`  ·  No database connection-pool metrics or Redis latency metri
- `SA-PERF-001` → `08693a50`  ·  projects.findAll default limit is 1000 — effectively unbou
- `SA-PERF-011` → `52abc47a`  ·  analytics.getAnalytics fires TWO identical task.groupBy qu
- `SA-PERF-012` → `52abc47a`  ·  analytics members include: loads ALL project members with 
- `SA-PERF-017` → `08693a50`  ·  getSnapshots has no pagination or limit — returns all hist
- `SA-PERF-018` → `08693a50`  ·  getProjectStats issues 2 separate TimeEntry findMany — sho
- `SEC-003` → `000d3161`  ·  LoginDto.password has no @MaxLength — long password input 
- `SEC-004` → `12d48876`  ·  GET /projects/:projectId/clients — missing project-scope a
- `SEC-005` → `000d3161`  ·  CreateCommentDto.content and UpdateCommentDto.content lack
- `SEC-006` → `26308212`  ·  epics findAll and milestones findAll return all records re
- `SEC-008` → `c8d7bbf8`  ·  ImportLeavesDto.leaves array has no upper-bound limit, ena
- `SEC-009` → `0ed06468`  ·  canValidate() delegation fallback grants approve rights ac
- `SEC-010` → `c8d7bbf8`  ·  Swagger Basic Auth: passwords containing ':' are silently 
- `SEC-011` → `f8ef9b67`  ·  Metrics METRICS_TOKEN comparison uses non-constant-time st
- `SEC-012` → `81149871`  ·  Prometheus metric labels built from unsanitized req.path —
- `SEC-013` → `006605b3`  ·  POST /milestones/:id/complete lacks project-membership che
- `SEC-015` → `7b0d2a2b`  ·  GET /projects/:id/snapshots uses @Param('id') without Pars
- `SEC-016` → `08693a50`  ·  Snapshot date filter params (from/to) are not validated an
- `SEC-017` → `08693a50`  ·  hardDelete lacks defense-in-depth ownership assertion pres
- `SEC-019` → `c8d7bbf8`  ·  CreateTaskDto.tags field has no @IsArray / @IsString({ eac
- `SEC-022` → `d41e8566`  ·  reorderSubtasks does not verify that subtaskIds belong to 
- `SEC-023` → `000d3161`  ·  userId in CreateTeleworkDto and CreateRecurringRuleDto use
- `SEC-024` → `8fc2323f`  ·  Unbounded date range in GET /telework triggers O(days × ru
- `SEC-025` → `d7a2bd76`  ·  GET /tasks/:taskId/third-party-assignees — missing task-sc
- `SEC-026` → `d7a2bd76`  ·  GET /projects/:projectId/third-party-members — missing pro
- `SEC-027` → `c8d7bbf8`  ·  POST /users/:id/reset-password uses 'users:manage_roles' p
- `SEC-028` → `93c3073d`  ·  Admin password reset (POST /users/:id/reset-password) does
- `SEC-029` → `36e5e291`  ·  Leave cancellation request button gated by client-side use
- `SEC-030` → `fd671b74`  ·  SuiviPage by-user reads: +server-side managed-scope on leaves-balance & skills (5/7 already safe)
- `OBS-003` → `d0ca9124`  ·  POST /auth/logout now emits a LOGOUT audit row (envelope, entityType Auth)
- `OBS-016` → `d0ca9124`  ·  importUsers emits USER_CREATED per row — cast promoted to real member + strict schema
- `OBS-017` → `d0ca9124`  ·  POST /users emits USER_CREATED — cast promoted to real member + strict schema
- `OBS-006` → `98c31c76`  ·  documents create/update/delete audit emits (DOCUMENT_CREATED/UPDATED/DELETED, +before/after on update)
- `OBS-015` → `b4eda3f2`  ·  time-tracking create/update/delete audit emits (TIME_ENTRY_*/TimeEntry; emit AFTER the SERIALIZABLE tx, dismissal not audited)
- `OBS-012` → `cf928b54`  ·  task create/update/delete audit emits (TASK_*/Task; UPDATED before/after inside the default-isolation tx)
- `OBS-013` → `4f8b7b04`  ·  telework CRUD + recurring-rule CRUD + generate audit emits (TELEWORK_*/Telework, 7 actions; targetUserId in payloads; generate emits only when created>0)
- `OBS-004` → `cd600fd6`  ·  client CRUD + project-link audit emits (CLIENT_*/Client, 5 actions; +actorId threaded through both controllers; DELETE inside the COR-008 tx)
- `OBS-011` → `43123c4a`  ·  settings write audit emits (SETTINGS_CHANGED/Settings; chokepoint update() before/after; +actorId threaded into all 5 write handlers)

## 2. Reconciled-away / superseded (anchors) — 10
- `fe91b806`: DAT-001/003/005/006/007/010 (already satisfied), OBS-020 (deliberate retention policy — CONFLICTS).
- `99511a42`: SEC-014 (materially fixed by PER-012 @ArrayMaxSize).
- `b26d3430`: SEC-018/SEC-020 (materially fixed by PER-020/PER-024 @ArrayMaxSize(500)).

## 3. Still-open — audit-emit cluster — 4
Pattern (established by the `d0ca9124` slice): per group, extend the 3 compile-locked layers
(`audit-action.enum.ts` ∩ `ENTITY_TYPE_BY_ACTION` in `audit.service.ts` ∩ `AUDIT_PAYLOAD_SCHEMAS` in `payload-schemas.ts`)
with that group's actions, then wire `auditPersistence.log({action, entityType, entityId, actorId, payload}, tx?)` into each
mutation (emit INSIDE the mutation `$transaction` per the projects precedent; single-row creates emit awaited after the write).
Witness = unit: capture the payload to the mocked `log()` + assert REAL `validatePayloadForAction(action, payload)` (see §Delta rationale).
- **DONE 2026-06-06 (`d0ca9124`):** OBS-003 (LOGOUT/Auth), OBS-016, OBS-017 (USER_CREATED/User).
- **DONE 2026-06-06 (`98c31c76`):** OBS-006 (DOCUMENT_CREATED/UPDATED/DELETED/Document).
- **DONE 2026-06-06 (`b4eda3f2`):** OBS-015 (TIME_ENTRY_CREATED/UPDATED/DELETED/TimeEntry). **Emit-placement lesson (applies to OBS-009):** `create()`/`upsertDismissal` run under `isolationLevel:'Serializable'` with a P2034 retry — the audit emit MUST be placed AFTER the tx commits, not inside (the hash-chain read needs READ-COMMITTED snapshot semantics; inside SERIALIZABLE it forks the chain or burns the retry). The dismissal toggle (hours:0) is NOT audited (UI marker).
- **DONE 2026-06-06 (`cf928b54`):** OBS-012 (TASK_CREATED/UPDATED/DELETED/Task; UPDATED before/after inside the default-isolation tx, passing `tx`).
- **DONE 2026-06-06 (`4f8b7b04`):** OBS-013 (7 TELEWORK_* actions/`Telework`; single-entry + recurring-rule CRUD + bulk generate, generate gated on created>0; payloads carry targetUserId).
- **DONE 2026-06-06 (`cd600fd6`):** OBS-004 (5 CLIENT_* actions/`Client`; +actorId threaded through clients.controller + projects-clients.controller; DELETE emitted inside the COR-008 tx).
- **DONE 2026-06-06 (`43123c4a`):** OBS-011 (SETTINGS_CHANGED/`Settings`; chokepoint update() before/after; +actorId threaded into all 5 write handlers).
- **Remaining 4 — suggested grouping (new entityTypes in parens):**
  - OBS-008 leave delegations (`Leave`) + OBS-010 projects create/update/soft-delete→CANCELLED (`Project` exists)
  - **OBS-014 SEPARATE** — third-parties create/update/hardDelete/assign/detach (`ThirdParty`). `d7a2bd76` claimed an emit but third-parties.service has **0 emit callsites** (confirmed 2026-06-06): genuinely open, actorId threaded only.
  - **OBS-009 SEPARATE** — NOT mechanical: leave create emits no durable audit for PENDING/declaredByManager + selfApprove path mislabeled. Own fold (mislabel fix + branch-specific emits), reuses existing LEAVE_* actions; grep the 11 existing leaves emit-callsites first (double-emit risk).
- **Cluster follow-ups (non-blocking, next pass — do NOT re-open the verified commits):**
  1. **One parametrized int spec over all new cluster payloads** (belt-and-suspenders). The per-emit witnesses call `validatePayloadForAction` directly (proves payload↔schema), but `audit-immutability.int.spec` exercises the hash chain with its own fixed actions — so LOGOUT/USER_CREATED/DOCUMENT_* (and future cluster actions) never INSERT through the real `AuditPersistenceService.log`. Add one int spec that drives each new action's representative payload through a real INSERT once. Low risk (simple payloads, opaque-id entityIds).
  2. **Cosmetic (`98c31c76`):** the `DOCUMENT_UPDATED` emit sits between `prisma.document.update` and `return updated`, INSIDE the P2025 try/catch. Harmless today (audit_logs only INSERTs, never raises P2025, so the "Document introuvable" mistranslation path is unreachable; the awaited-emit→500-on-committed-update coupling is the accepted house pattern). Cleaner placement = after the try/catch. Fold opportunistically when next editing the file.

## 4. Still-open — web + secondary + reverted — 21
- **Web (blind-patch breakage-prone — fold per-group with jest verification):** COR-041/042/043/044/045/047, PER-031/032/033, SA-PERF-023, SEC-031. Wave-F authored patches exist at `/tmp/patches/web_*` (NOTE: /tmp was cleared — re-derive) but several broke their own jest suites; fold one at a time.
- **SEC-030 — DONE `fd671b74` (2026-06-06).** Was BLOCKED; resolved + prescription refined (only 2 of the 7 by-user calls genuinely over-exposed → tightened; the other 5 already equal-or-stronger). See §Delta above + the SEC-030 backlog Notes.
- **Reverted blind-patch breakages (re-author carefully):** SEC-007 (health — non-constructor test mock), PER-003 (role-hierarchy — broke users.service tests, cross-module), SEC-021 (tasks-controller — RED=0 hollow witness, verify the real gap).
- **Conflicts with session-modified files (fold with --reject):** SA-PERF-013 (analytics cache-invalidation — also needs CacheService.delByPattern), SA-PERF-015 (planning), SA-PERF-014 (audit-persistence).
- **Decisions / misc:** SA-DAT-002 (UserSkill.validatedBy→snapshot = schema-rename DECISION), SA-DAT-005 (hash-chain recompute CI guard = add int spec), SA-OBS-004 (DOCUMENT_DOWNLOADED unused enum), SA-OBS-005 (getRequestId unused — BLOCKED, ALS wiring).

## 5. Still-open — test/e2e/script nits — 16
Lower-value: missing controller specs (TST-002/003/004/005/006, SA-TEST-004/005/006/007), e2e UI-login refactors (TST-007, SA-TEST-001), tautological/swallowed e2e asserts (SA-TEST-002/003/013, TST-008), SA-DAT-006 (backup-script encryption).
- `SA-DAT-006` `SA-TEST-001` `SA-TEST-002` `SA-TEST-003` `SA-TEST-004` `SA-TEST-005` `SA-TEST-006` `SA-TEST-007` `SA-TEST-013` `TST-002` `TST-003` `TST-004` `TST-005` `TST-006` `TST-007` `TST-008`

## 6. Engine + gotchas (validated across waves 0/1/F)
Parallel worktree Sonnet authors (blind, no node_modules) → spec+src patches on disk → orchestrator folds serially:
spec-first RED → source GREEN → whole-repo gate → multi-`[closes]` commit + Closed_by. Gotchas hit & handled:
worktrees branch from session-start HEAD (already-touched files → `git apply --reject` + manual merge);
controller-signature changes break sibling specs; blind patches yield TS errors (import-type, DayPeriod), cross-module
breaks (events→planning), lint errors (require()), non-constructor mocks, **RED=0 hollow/already-done witnesses (verify per-group, never batch-trust)**,
new files at non-obvious paths (`swagger-basic-auth.ts` → always `git add -A` + standalone-build check),
frontend-only fixes needing backend (SEC-029/OBS-018/OBS-019 completed in f7bd2096); one agent failed StructuredOutput (re-author inline).