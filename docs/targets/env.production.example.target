# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                        ORCHESTR'A - Configuration Production                 ║
# ║                                                                              ║
# ║  INSTRUCTIONS:                                                               ║
# ║  Option 1 (Recommandé): ./scripts/init-env.sh                                ║
# ║            Génère automatiquement les secrets et crée .env.production        ║
# ║                                                                              ║
# ║  Option 2 (Manuel): cp .env.production.example .env.production               ║
# ║            Puis remplir les valeurs marquées REQUIRED                        ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ SECRETS [REQUIRED]                                                           │
# │ Générer avec: openssl rand -base64 32 | tr -dc 'a-zA-Z0-9' | head -c 32     │
# └──────────────────────────────────────────────────────────────────────────────┘

# Mot de passe PostgreSQL (min 16 caractères recommandé)
DATABASE_PASSWORD=

# Mot de passe Redis (min 16 caractères recommandé)
REDIS_PASSWORD=

# Secret JWT pour signer les tokens (min 32 caractères)
# Générer avec: openssl rand -base64 64
JWT_SECRET=

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ DOMAINE [REQUIRED]                                                           │
# └──────────────────────────────────────────────────────────────────────────────┘

# URL complète de votre domaine de production (pour CORS)
# Exemples:
#   - https://orchestr-a.example.com
#   - https://app.monentreprise.fr
CORS_ORIGIN=

# URL de l'API vue depuis le navigateur
# Utilisez /api si derrière un reverse proxy (configuration standard)
NEXT_PUBLIC_API_URL=/api

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ BASE DE DONNÉES [Optionnel - valeurs par défaut OK]                          │
# └──────────────────────────────────────────────────────────────────────────────┘

DATABASE_NAME=orchestr_a_prod
DATABASE_USER=orchestr_a

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ AUTHENTIFICATION [Optionnel]                                                 │
# └──────────────────────────────────────────────────────────────────────────────┘

# Durée de validité des tokens JWT
# Formats: 1h, 8h, 1d, 7d, 30d
JWT_EXPIRES_IN=7d

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ SÉCURITÉ [Optionnel]                                                         │
# └──────────────────────────────────────────────────────────────────────────────┘

# Rate limiting - nombre max de requêtes par fenêtre de temps
THROTTLE_LIMIT=100
THROTTLE_TTL=60

# Activer la documentation Swagger (false recommandé en production)
SWAGGER_ENABLED=false

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ RÉSEAU [Optionnel - modifier uniquement si conflit de ports]                 │
# └──────────────────────────────────────────────────────────────────────────────┘

HTTP_PORT=80
HTTPS_PORT=443

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ EMAIL [Optionnel - décommenter pour activer]                                 │
# └──────────────────────────────────────────────────────────────────────────────┘

# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=
# SMTP_FROM=ORCHESTR'A <noreply@example.com>

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ MONITORING [Optionnel - décommenter pour activer]                            │
# └──────────────────────────────────────────────────────────────────────────────┘

# Sentry (error tracking)
# SENTRY_DSN=https://xxx@sentry.io/xxx
# SENTRY_ENVIRONMENT=production

# ┌──────────────────────────────────────────────────────────────────────────────┐
# │ STOCKAGE FICHIERS [Optionnel - décommenter pour activer]                     │
# └──────────────────────────────────────────────────────────────────────────────┘

# STORAGE_TYPE=local
# STORAGE_PATH=/app/uploads
# MAX_FILE_SIZE=10485760
